"""
Transformer - Generate code transformations and patches.

Applies refactoring rules based on tier classification:
- Tier A: Safe auto-apply (formatting, imports)
- Tier B: Apply with tests (type hints, extraction)
- Tier C: Suggest only (architecture changes)

Author: mbaetiong
Generated: 2025-12-17

Safeguards:
- Tier-based safety classification
- Dry-run mode by default
- Patch validation before apply
- Rollback capability
"""

from __future__ import annotations

import ast
import difflib
import json
import logging
import shutil
import subprocess
from dataclasses import dataclass, field
from datetime import datetime, timezone
from enum import Enum
from pathlib import Path
from typing import Any, Optional

logger = logging.getLogger(__name__)
from inspect import signature as _mutmut_signature
from typing import Annotated
from typing import Callable
from typing import ClassVar


MutantDict = Annotated[dict[str, Callable], "Mutant"]


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg = None):
    """Forward call to original or mutated function, depending on the environment"""
    import os
    mutant_under_test = os.environ['MUTANT_UNDER_TEST']
    if mutant_under_test == 'fail':
        from mutmut.__main__ import MutmutProgrammaticFailException
        raise MutmutProgrammaticFailException('Failed programmatically')      
    elif mutant_under_test == 'stats':
        from mutmut.__main__ import record_trampoline_hit
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__)
        result = orig(*call_args, **call_kwargs)
        return result
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_'
    if not mutant_under_test.startswith(prefix):
        result = orig(*call_args, **call_kwargs)
        return result
    mutant_name = mutant_under_test.rpartition('.')[-1]
    if self_arg is not None:
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs)
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs)
    return result


class Tier(Enum):
    """Transformation tier classification."""
    A = "safe_auto_apply"
    B = "apply_with_tests"
    C = "suggest_only"


@dataclass
class Patch:
    """A code patch to apply.
    
    Attributes:
        file_path: Relative path to file
        original: Original content
        modified: Modified content
        diff: Unified diff string
        rule_id: ID of the rule that generated this patch
        tier: Transformation tier
        description: Human-readable description
    """
    file_path: str
    original: str
    modified: str
    diff: str
    rule_id: str
    tier: Tier
    description: str
    
    def to_dict(self) -> dict[str, Any]:
        """Convert to dictionary."""
        return {
            "file_path": self.file_path,
            "rule_id": self.rule_id,
            "tier": self.tier.name,
            "description": self.description,
            "diff": self.diff,
        }


@dataclass
class TransformResult:
    """Result of transformation operation.
    
    Attributes:
        snapshot_id: Snapshot being transformed
        timestamp: When transformation was performed
        tier_a_patches: Auto-applied patches
        tier_b_patches: Patches requiring tests
        tier_c_suggestions: Suggestions for manual review
        applied: Whether patches were applied
        errors: Any errors encountered
    """
    snapshot_id: str
    timestamp: datetime
    tier_a_patches: list[Patch] = field(default_factory=list)
    tier_b_patches: list[Patch] = field(default_factory=list)
    tier_c_suggestions: list[dict[str, Any]] = field(default_factory=list)
    applied: bool = False
    errors: list[str] = field(default_factory=list)
    
    def to_dict(self) -> dict[str, Any]:
        """Convert to dictionary."""
        return {
            "snapshot_id": self.snapshot_id,
            "timestamp": self.timestamp.isoformat(),
            "tier_a_patches": [p.to_dict() for p in self.tier_a_patches],
            "tier_b_patches": [p.to_dict() for p in self.tier_b_patches],
            "tier_c_suggestions": self.tier_c_suggestions,
            "applied": self.applied,
            "errors": self.errors,
        }
    
    def save(self, directory: Path) -> None:
        """Save patches to directory."""
        directory.mkdir(parents=True, exist_ok=True)
        
        # Save summary
        summary_path = directory / "transform-summary.json"
        with summary_path.open("w", encoding="utf-8") as f:
            json.dump(self.to_dict(), f, indent=2)
        
        # Save individual patches
        if self.tier_a_patches:
            tier_a_path = directory / "tier-a.patch"
            with tier_a_path.open("w", encoding="utf-8") as f:
                for patch in self.tier_a_patches:
                    f.write(f"# {patch.rule_id}: {patch.description}\n")
                    f.write(patch.diff)
                    f.write("\n")
        
        if self.tier_b_patches:
            tier_b_path = directory / "tier-b.patch"
            with tier_b_path.open("w", encoding="utf-8") as f:
                for patch in self.tier_b_patches:
                    f.write(f"# {patch.rule_id}: {patch.description}\n")
                    f.write(patch.diff)
                    f.write("\n")
        
        if self.tier_c_suggestions:
            tier_c_dir = directory / "tier-c-suggestions"
            tier_c_dir.mkdir(exist_ok=True)
            for i, suggestion in enumerate(self.tier_c_suggestions):
                path = tier_c_dir / f"suggestion-{i+1}.md"
                with path.open("w", encoding="utf-8") as f:
                    f.write(f"# {suggestion.get('rule_id', 'unknown')}\n\n")
                    f.write(f"## Description\n{suggestion.get('description', '')}\n\n")
                    f.write("## Checklist\n")
                    for item in suggestion.get("checklist", []):
                        f.write(f"- [ ] {item}\n")


def x__create_diff__mutmut_orig(original: str, modified: str, file_path: str) -> str:
    """Create unified diff between original and modified content."""
    original_lines = original.splitlines(keepends=True)
    modified_lines = modified.splitlines(keepends=True)
    
    diff = difflib.unified_diff(
        original_lines,
        modified_lines,
        fromfile=f"a/{file_path}",
        tofile=f"b/{file_path}",
    )
    
    return "".join(diff)


def x__create_diff__mutmut_1(original: str, modified: str, file_path: str) -> str:
    """Create unified diff between original and modified content."""
    original_lines = None
    modified_lines = modified.splitlines(keepends=True)
    
    diff = difflib.unified_diff(
        original_lines,
        modified_lines,
        fromfile=f"a/{file_path}",
        tofile=f"b/{file_path}",
    )
    
    return "".join(diff)


def x__create_diff__mutmut_2(original: str, modified: str, file_path: str) -> str:
    """Create unified diff between original and modified content."""
    original_lines = original.splitlines(keepends=None)
    modified_lines = modified.splitlines(keepends=True)
    
    diff = difflib.unified_diff(
        original_lines,
        modified_lines,
        fromfile=f"a/{file_path}",
        tofile=f"b/{file_path}",
    )
    
    return "".join(diff)


def x__create_diff__mutmut_3(original: str, modified: str, file_path: str) -> str:
    """Create unified diff between original and modified content."""
    original_lines = original.splitlines(keepends=False)
    modified_lines = modified.splitlines(keepends=True)
    
    diff = difflib.unified_diff(
        original_lines,
        modified_lines,
        fromfile=f"a/{file_path}",
        tofile=f"b/{file_path}",
    )
    
    return "".join(diff)


def x__create_diff__mutmut_4(original: str, modified: str, file_path: str) -> str:
    """Create unified diff between original and modified content."""
    original_lines = original.splitlines(keepends=True)
    modified_lines = None
    
    diff = difflib.unified_diff(
        original_lines,
        modified_lines,
        fromfile=f"a/{file_path}",
        tofile=f"b/{file_path}",
    )
    
    return "".join(diff)


def x__create_diff__mutmut_5(original: str, modified: str, file_path: str) -> str:
    """Create unified diff between original and modified content."""
    original_lines = original.splitlines(keepends=True)
    modified_lines = modified.splitlines(keepends=None)
    
    diff = difflib.unified_diff(
        original_lines,
        modified_lines,
        fromfile=f"a/{file_path}",
        tofile=f"b/{file_path}",
    )
    
    return "".join(diff)


def x__create_diff__mutmut_6(original: str, modified: str, file_path: str) -> str:
    """Create unified diff between original and modified content."""
    original_lines = original.splitlines(keepends=True)
    modified_lines = modified.splitlines(keepends=False)
    
    diff = difflib.unified_diff(
        original_lines,
        modified_lines,
        fromfile=f"a/{file_path}",
        tofile=f"b/{file_path}",
    )
    
    return "".join(diff)


def x__create_diff__mutmut_7(original: str, modified: str, file_path: str) -> str:
    """Create unified diff between original and modified content."""
    original_lines = original.splitlines(keepends=True)
    modified_lines = modified.splitlines(keepends=True)
    
    diff = None
    
    return "".join(diff)


def x__create_diff__mutmut_8(original: str, modified: str, file_path: str) -> str:
    """Create unified diff between original and modified content."""
    original_lines = original.splitlines(keepends=True)
    modified_lines = modified.splitlines(keepends=True)
    
    diff = difflib.unified_diff(
        None,
        modified_lines,
        fromfile=f"a/{file_path}",
        tofile=f"b/{file_path}",
    )
    
    return "".join(diff)


def x__create_diff__mutmut_9(original: str, modified: str, file_path: str) -> str:
    """Create unified diff between original and modified content."""
    original_lines = original.splitlines(keepends=True)
    modified_lines = modified.splitlines(keepends=True)
    
    diff = difflib.unified_diff(
        original_lines,
        None,
        fromfile=f"a/{file_path}",
        tofile=f"b/{file_path}",
    )
    
    return "".join(diff)


def x__create_diff__mutmut_10(original: str, modified: str, file_path: str) -> str:
    """Create unified diff between original and modified content."""
    original_lines = original.splitlines(keepends=True)
    modified_lines = modified.splitlines(keepends=True)
    
    diff = difflib.unified_diff(
        original_lines,
        modified_lines,
        fromfile=None,
        tofile=f"b/{file_path}",
    )
    
    return "".join(diff)


def x__create_diff__mutmut_11(original: str, modified: str, file_path: str) -> str:
    """Create unified diff between original and modified content."""
    original_lines = original.splitlines(keepends=True)
    modified_lines = modified.splitlines(keepends=True)
    
    diff = difflib.unified_diff(
        original_lines,
        modified_lines,
        fromfile=f"a/{file_path}",
        tofile=None,
    )
    
    return "".join(diff)


def x__create_diff__mutmut_12(original: str, modified: str, file_path: str) -> str:
    """Create unified diff between original and modified content."""
    original_lines = original.splitlines(keepends=True)
    modified_lines = modified.splitlines(keepends=True)
    
    diff = difflib.unified_diff(
        modified_lines,
        fromfile=f"a/{file_path}",
        tofile=f"b/{file_path}",
    )
    
    return "".join(diff)


def x__create_diff__mutmut_13(original: str, modified: str, file_path: str) -> str:
    """Create unified diff between original and modified content."""
    original_lines = original.splitlines(keepends=True)
    modified_lines = modified.splitlines(keepends=True)
    
    diff = difflib.unified_diff(
        original_lines,
        fromfile=f"a/{file_path}",
        tofile=f"b/{file_path}",
    )
    
    return "".join(diff)


def x__create_diff__mutmut_14(original: str, modified: str, file_path: str) -> str:
    """Create unified diff between original and modified content."""
    original_lines = original.splitlines(keepends=True)
    modified_lines = modified.splitlines(keepends=True)
    
    diff = difflib.unified_diff(
        original_lines,
        modified_lines,
        tofile=f"b/{file_path}",
    )
    
    return "".join(diff)


def x__create_diff__mutmut_15(original: str, modified: str, file_path: str) -> str:
    """Create unified diff between original and modified content."""
    original_lines = original.splitlines(keepends=True)
    modified_lines = modified.splitlines(keepends=True)
    
    diff = difflib.unified_diff(
        original_lines,
        modified_lines,
        fromfile=f"a/{file_path}",
        )
    
    return "".join(diff)


def x__create_diff__mutmut_16(original: str, modified: str, file_path: str) -> str:
    """Create unified diff between original and modified content."""
    original_lines = original.splitlines(keepends=True)
    modified_lines = modified.splitlines(keepends=True)
    
    diff = difflib.unified_diff(
        original_lines,
        modified_lines,
        fromfile=f"a/{file_path}",
        tofile=f"b/{file_path}",
    )
    
    return "".join(None)


def x__create_diff__mutmut_17(original: str, modified: str, file_path: str) -> str:
    """Create unified diff between original and modified content."""
    original_lines = original.splitlines(keepends=True)
    modified_lines = modified.splitlines(keepends=True)
    
    diff = difflib.unified_diff(
        original_lines,
        modified_lines,
        fromfile=f"a/{file_path}",
        tofile=f"b/{file_path}",
    )
    
    return "XXXX".join(diff)

x__create_diff__mutmut_mutants : ClassVar[MutantDict] = {
'x__create_diff__mutmut_1': x__create_diff__mutmut_1, 
    'x__create_diff__mutmut_2': x__create_diff__mutmut_2, 
    'x__create_diff__mutmut_3': x__create_diff__mutmut_3, 
    'x__create_diff__mutmut_4': x__create_diff__mutmut_4, 
    'x__create_diff__mutmut_5': x__create_diff__mutmut_5, 
    'x__create_diff__mutmut_6': x__create_diff__mutmut_6, 
    'x__create_diff__mutmut_7': x__create_diff__mutmut_7, 
    'x__create_diff__mutmut_8': x__create_diff__mutmut_8, 
    'x__create_diff__mutmut_9': x__create_diff__mutmut_9, 
    'x__create_diff__mutmut_10': x__create_diff__mutmut_10, 
    'x__create_diff__mutmut_11': x__create_diff__mutmut_11, 
    'x__create_diff__mutmut_12': x__create_diff__mutmut_12, 
    'x__create_diff__mutmut_13': x__create_diff__mutmut_13, 
    'x__create_diff__mutmut_14': x__create_diff__mutmut_14, 
    'x__create_diff__mutmut_15': x__create_diff__mutmut_15, 
    'x__create_diff__mutmut_16': x__create_diff__mutmut_16, 
    'x__create_diff__mutmut_17': x__create_diff__mutmut_17
}

def _create_diff(*args, **kwargs):
    result = _mutmut_trampoline(x__create_diff__mutmut_orig, x__create_diff__mutmut_mutants, args, kwargs)
    return result 

_create_diff.__signature__ = _mutmut_signature(x__create_diff__mutmut_orig)
x__create_diff__mutmut_orig.__name__ = 'x__create_diff'


def x__resolve_tool__mutmut_orig(tool: str) -> Optional[str]:
    """Resolve a tool path from PATH for safer subprocess execution."""
    tool_path = shutil.which(tool)
    if not tool_path:
        logger.debug("%s not found, skipping", tool)
        return None
    return str(Path(tool_path).resolve())


def x__resolve_tool__mutmut_1(tool: str) -> Optional[str]:
    """Resolve a tool path from PATH for safer subprocess execution."""
    tool_path = None
    if not tool_path:
        logger.debug("%s not found, skipping", tool)
        return None
    return str(Path(tool_path).resolve())


def x__resolve_tool__mutmut_2(tool: str) -> Optional[str]:
    """Resolve a tool path from PATH for safer subprocess execution."""
    tool_path = shutil.which(None)
    if not tool_path:
        logger.debug("%s not found, skipping", tool)
        return None
    return str(Path(tool_path).resolve())


def x__resolve_tool__mutmut_3(tool: str) -> Optional[str]:
    """Resolve a tool path from PATH for safer subprocess execution."""
    tool_path = shutil.which(tool)
    if tool_path:
        logger.debug("%s not found, skipping", tool)
        return None
    return str(Path(tool_path).resolve())


def x__resolve_tool__mutmut_4(tool: str) -> Optional[str]:
    """Resolve a tool path from PATH for safer subprocess execution."""
    tool_path = shutil.which(tool)
    if not tool_path:
        logger.debug(None, tool)
        return None
    return str(Path(tool_path).resolve())


def x__resolve_tool__mutmut_5(tool: str) -> Optional[str]:
    """Resolve a tool path from PATH for safer subprocess execution."""
    tool_path = shutil.which(tool)
    if not tool_path:
        logger.debug("%s not found, skipping", None)
        return None
    return str(Path(tool_path).resolve())


def x__resolve_tool__mutmut_6(tool: str) -> Optional[str]:
    """Resolve a tool path from PATH for safer subprocess execution."""
    tool_path = shutil.which(tool)
    if not tool_path:
        logger.debug(tool)
        return None
    return str(Path(tool_path).resolve())


def x__resolve_tool__mutmut_7(tool: str) -> Optional[str]:
    """Resolve a tool path from PATH for safer subprocess execution."""
    tool_path = shutil.which(tool)
    if not tool_path:
        logger.debug("%s not found, skipping", )
        return None
    return str(Path(tool_path).resolve())


def x__resolve_tool__mutmut_8(tool: str) -> Optional[str]:
    """Resolve a tool path from PATH for safer subprocess execution."""
    tool_path = shutil.which(tool)
    if not tool_path:
        logger.debug("XX%s not found, skippingXX", tool)
        return None
    return str(Path(tool_path).resolve())


def x__resolve_tool__mutmut_9(tool: str) -> Optional[str]:
    """Resolve a tool path from PATH for safer subprocess execution."""
    tool_path = shutil.which(tool)
    if not tool_path:
        logger.debug("%S NOT FOUND, SKIPPING", tool)
        return None
    return str(Path(tool_path).resolve())


def x__resolve_tool__mutmut_10(tool: str) -> Optional[str]:
    """Resolve a tool path from PATH for safer subprocess execution."""
    tool_path = shutil.which(tool)
    if not tool_path:
        logger.debug("%s not found, skipping", tool)
        return None
    return str(None)


def x__resolve_tool__mutmut_11(tool: str) -> Optional[str]:
    """Resolve a tool path from PATH for safer subprocess execution."""
    tool_path = shutil.which(tool)
    if not tool_path:
        logger.debug("%s not found, skipping", tool)
        return None
    return str(Path(None).resolve())

x__resolve_tool__mutmut_mutants : ClassVar[MutantDict] = {
'x__resolve_tool__mutmut_1': x__resolve_tool__mutmut_1, 
    'x__resolve_tool__mutmut_2': x__resolve_tool__mutmut_2, 
    'x__resolve_tool__mutmut_3': x__resolve_tool__mutmut_3, 
    'x__resolve_tool__mutmut_4': x__resolve_tool__mutmut_4, 
    'x__resolve_tool__mutmut_5': x__resolve_tool__mutmut_5, 
    'x__resolve_tool__mutmut_6': x__resolve_tool__mutmut_6, 
    'x__resolve_tool__mutmut_7': x__resolve_tool__mutmut_7, 
    'x__resolve_tool__mutmut_8': x__resolve_tool__mutmut_8, 
    'x__resolve_tool__mutmut_9': x__resolve_tool__mutmut_9, 
    'x__resolve_tool__mutmut_10': x__resolve_tool__mutmut_10, 
    'x__resolve_tool__mutmut_11': x__resolve_tool__mutmut_11
}

def _resolve_tool(*args, **kwargs):
    result = _mutmut_trampoline(x__resolve_tool__mutmut_orig, x__resolve_tool__mutmut_mutants, args, kwargs)
    return result 

_resolve_tool.__signature__ = _mutmut_signature(x__resolve_tool__mutmut_orig)
x__resolve_tool__mutmut_orig.__name__ = 'x__resolve_tool'


def x__run_black__mutmut_orig(file_path: Path) -> Optional[str]:
    """Run black formatter and return modified content."""
    try:
        tool_path = _resolve_tool("black")
        if not tool_path:
            return None
        # Security: Using 'black' from PATH - assumes it's a trusted code formatter
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Black formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_black__mutmut_1(file_path: Path) -> Optional[str]:
    """Run black formatter and return modified content."""
    try:
        tool_path = None
        if not tool_path:
            return None
        # Security: Using 'black' from PATH - assumes it's a trusted code formatter
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Black formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_black__mutmut_2(file_path: Path) -> Optional[str]:
    """Run black formatter and return modified content."""
    try:
        tool_path = _resolve_tool(None)
        if not tool_path:
            return None
        # Security: Using 'black' from PATH - assumes it's a trusted code formatter
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Black formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_black__mutmut_3(file_path: Path) -> Optional[str]:
    """Run black formatter and return modified content."""
    try:
        tool_path = _resolve_tool("XXblackXX")
        if not tool_path:
            return None
        # Security: Using 'black' from PATH - assumes it's a trusted code formatter
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Black formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_black__mutmut_4(file_path: Path) -> Optional[str]:
    """Run black formatter and return modified content."""
    try:
        tool_path = _resolve_tool("BLACK")
        if not tool_path:
            return None
        # Security: Using 'black' from PATH - assumes it's a trusted code formatter
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Black formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_black__mutmut_5(file_path: Path) -> Optional[str]:
    """Run black formatter and return modified content."""
    try:
        tool_path = _resolve_tool("black")
        if tool_path:
            return None
        # Security: Using 'black' from PATH - assumes it's a trusted code formatter
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Black formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_black__mutmut_6(file_path: Path) -> Optional[str]:
    """Run black formatter and return modified content."""
    try:
        tool_path = _resolve_tool("black")
        if not tool_path:
            return None
        # Security: Using 'black' from PATH - assumes it's a trusted code formatter
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = None
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Black formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_black__mutmut_7(file_path: Path) -> Optional[str]:
    """Run black formatter and return modified content."""
    try:
        tool_path = _resolve_tool("black")
        if not tool_path:
            return None
        # Security: Using 'black' from PATH - assumes it's a trusted code formatter
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            None,
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Black formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_black__mutmut_8(file_path: Path) -> Optional[str]:
    """Run black formatter and return modified content."""
    try:
        tool_path = _resolve_tool("black")
        if not tool_path:
            return None
        # Security: Using 'black' from PATH - assumes it's a trusted code formatter
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=None,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Black formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_black__mutmut_9(file_path: Path) -> Optional[str]:
    """Run black formatter and return modified content."""
    try:
        tool_path = _resolve_tool("black")
        if not tool_path:
            return None
        # Security: Using 'black' from PATH - assumes it's a trusted code formatter
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=None,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Black formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_black__mutmut_10(file_path: Path) -> Optional[str]:
    """Run black formatter and return modified content."""
    try:
        tool_path = _resolve_tool("black")
        if not tool_path:
            return None
        # Security: Using 'black' from PATH - assumes it's a trusted code formatter
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            timeout=None,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Black formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_black__mutmut_11(file_path: Path) -> Optional[str]:
    """Run black formatter and return modified content."""
    try:
        tool_path = _resolve_tool("black")
        if not tool_path:
            return None
        # Security: Using 'black' from PATH - assumes it's a trusted code formatter
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Black formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_black__mutmut_12(file_path: Path) -> Optional[str]:
    """Run black formatter and return modified content."""
    try:
        tool_path = _resolve_tool("black")
        if not tool_path:
            return None
        # Security: Using 'black' from PATH - assumes it's a trusted code formatter
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Black formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_black__mutmut_13(file_path: Path) -> Optional[str]:
    """Run black formatter and return modified content."""
    try:
        tool_path = _resolve_tool("black")
        if not tool_path:
            return None
        # Security: Using 'black' from PATH - assumes it's a trusted code formatter
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Black formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_black__mutmut_14(file_path: Path) -> Optional[str]:
    """Run black formatter and return modified content."""
    try:
        tool_path = _resolve_tool("black")
        if not tool_path:
            return None
        # Security: Using 'black' from PATH - assumes it's a trusted code formatter
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Black formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_black__mutmut_15(file_path: Path) -> Optional[str]:
    """Run black formatter and return modified content."""
    try:
        tool_path = _resolve_tool("black")
        if not tool_path:
            return None
        # Security: Using 'black' from PATH - assumes it's a trusted code formatter
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "XX--quietXX", str(file_path)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Black formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_black__mutmut_16(file_path: Path) -> Optional[str]:
    """Run black formatter and return modified content."""
    try:
        tool_path = _resolve_tool("black")
        if not tool_path:
            return None
        # Security: Using 'black' from PATH - assumes it's a trusted code formatter
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--QUIET", str(file_path)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Black formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_black__mutmut_17(file_path: Path) -> Optional[str]:
    """Run black formatter and return modified content."""
    try:
        tool_path = _resolve_tool("black")
        if not tool_path:
            return None
        # Security: Using 'black' from PATH - assumes it's a trusted code formatter
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(None)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Black formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_black__mutmut_18(file_path: Path) -> Optional[str]:
    """Run black formatter and return modified content."""
    try:
        tool_path = _resolve_tool("black")
        if not tool_path:
            return None
        # Security: Using 'black' from PATH - assumes it's a trusted code formatter
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=False,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Black formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_black__mutmut_19(file_path: Path) -> Optional[str]:
    """Run black formatter and return modified content."""
    try:
        tool_path = _resolve_tool("black")
        if not tool_path:
            return None
        # Security: Using 'black' from PATH - assumes it's a trusted code formatter
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=False,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Black formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_black__mutmut_20(file_path: Path) -> Optional[str]:
    """Run black formatter and return modified content."""
    try:
        tool_path = _resolve_tool("black")
        if not tool_path:
            return None
        # Security: Using 'black' from PATH - assumes it's a trusted code formatter
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            timeout=31,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Black formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_black__mutmut_21(file_path: Path) -> Optional[str]:
    """Run black formatter and return modified content."""
    try:
        tool_path = _resolve_tool("black")
        if not tool_path:
            return None
        # Security: Using 'black' from PATH - assumes it's a trusted code formatter
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode != 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Black formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_black__mutmut_22(file_path: Path) -> Optional[str]:
    """Run black formatter and return modified content."""
    try:
        tool_path = _resolve_tool("black")
        if not tool_path:
            return None
        # Security: Using 'black' from PATH - assumes it's a trusted code formatter
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 1:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Black formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_black__mutmut_23(file_path: Path) -> Optional[str]:
    """Run black formatter and return modified content."""
    try:
        tool_path = _resolve_tool("black")
        if not tool_path:
            return None
        # Security: Using 'black' from PATH - assumes it's a trusted code formatter
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding=None)
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Black formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_black__mutmut_24(file_path: Path) -> Optional[str]:
    """Run black formatter and return modified content."""
    try:
        tool_path = _resolve_tool("black")
        if not tool_path:
            return None
        # Security: Using 'black' from PATH - assumes it's a trusted code formatter
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="XXutf-8XX")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Black formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_black__mutmut_25(file_path: Path) -> Optional[str]:
    """Run black formatter and return modified content."""
    try:
        tool_path = _resolve_tool("black")
        if not tool_path:
            return None
        # Security: Using 'black' from PATH - assumes it's a trusted code formatter
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="UTF-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Black formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_black__mutmut_26(file_path: Path) -> Optional[str]:
    """Run black formatter and return modified content."""
    try:
        tool_path = _resolve_tool("black")
        if not tool_path:
            return None
        # Security: Using 'black' from PATH - assumes it's a trusted code formatter
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(None)
        logger.debug("Black formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_black__mutmut_27(file_path: Path) -> Optional[str]:
    """Run black formatter and return modified content."""
    try:
        tool_path = _resolve_tool("black")
        if not tool_path:
            return None
        # Security: Using 'black' from PATH - assumes it's a trusted code formatter
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug(None, file_path, exc)
    return None


def x__run_black__mutmut_28(file_path: Path) -> Optional[str]:
    """Run black formatter and return modified content."""
    try:
        tool_path = _resolve_tool("black")
        if not tool_path:
            return None
        # Security: Using 'black' from PATH - assumes it's a trusted code formatter
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Black formatting skipped for %s: %s", None, exc)
    return None


def x__run_black__mutmut_29(file_path: Path) -> Optional[str]:
    """Run black formatter and return modified content."""
    try:
        tool_path = _resolve_tool("black")
        if not tool_path:
            return None
        # Security: Using 'black' from PATH - assumes it's a trusted code formatter
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Black formatting skipped for %s: %s", file_path, None)
    return None


def x__run_black__mutmut_30(file_path: Path) -> Optional[str]:
    """Run black formatter and return modified content."""
    try:
        tool_path = _resolve_tool("black")
        if not tool_path:
            return None
        # Security: Using 'black' from PATH - assumes it's a trusted code formatter
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug(file_path, exc)
    return None


def x__run_black__mutmut_31(file_path: Path) -> Optional[str]:
    """Run black formatter and return modified content."""
    try:
        tool_path = _resolve_tool("black")
        if not tool_path:
            return None
        # Security: Using 'black' from PATH - assumes it's a trusted code formatter
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Black formatting skipped for %s: %s", exc)
    return None


def x__run_black__mutmut_32(file_path: Path) -> Optional[str]:
    """Run black formatter and return modified content."""
    try:
        tool_path = _resolve_tool("black")
        if not tool_path:
            return None
        # Security: Using 'black' from PATH - assumes it's a trusted code formatter
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Black formatting skipped for %s: %s", file_path, )
    return None


def x__run_black__mutmut_33(file_path: Path) -> Optional[str]:
    """Run black formatter and return modified content."""
    try:
        tool_path = _resolve_tool("black")
        if not tool_path:
            return None
        # Security: Using 'black' from PATH - assumes it's a trusted code formatter
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("XXBlack formatting skipped for %s: %sXX", file_path, exc)
    return None


def x__run_black__mutmut_34(file_path: Path) -> Optional[str]:
    """Run black formatter and return modified content."""
    try:
        tool_path = _resolve_tool("black")
        if not tool_path:
            return None
        # Security: Using 'black' from PATH - assumes it's a trusted code formatter
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("black formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_black__mutmut_35(file_path: Path) -> Optional[str]:
    """Run black formatter and return modified content."""
    try:
        tool_path = _resolve_tool("black")
        if not tool_path:
            return None
        # Security: Using 'black' from PATH - assumes it's a trusted code formatter
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("BLACK FORMATTING SKIPPED FOR %S: %S", file_path, exc)
    return None

x__run_black__mutmut_mutants : ClassVar[MutantDict] = {
'x__run_black__mutmut_1': x__run_black__mutmut_1, 
    'x__run_black__mutmut_2': x__run_black__mutmut_2, 
    'x__run_black__mutmut_3': x__run_black__mutmut_3, 
    'x__run_black__mutmut_4': x__run_black__mutmut_4, 
    'x__run_black__mutmut_5': x__run_black__mutmut_5, 
    'x__run_black__mutmut_6': x__run_black__mutmut_6, 
    'x__run_black__mutmut_7': x__run_black__mutmut_7, 
    'x__run_black__mutmut_8': x__run_black__mutmut_8, 
    'x__run_black__mutmut_9': x__run_black__mutmut_9, 
    'x__run_black__mutmut_10': x__run_black__mutmut_10, 
    'x__run_black__mutmut_11': x__run_black__mutmut_11, 
    'x__run_black__mutmut_12': x__run_black__mutmut_12, 
    'x__run_black__mutmut_13': x__run_black__mutmut_13, 
    'x__run_black__mutmut_14': x__run_black__mutmut_14, 
    'x__run_black__mutmut_15': x__run_black__mutmut_15, 
    'x__run_black__mutmut_16': x__run_black__mutmut_16, 
    'x__run_black__mutmut_17': x__run_black__mutmut_17, 
    'x__run_black__mutmut_18': x__run_black__mutmut_18, 
    'x__run_black__mutmut_19': x__run_black__mutmut_19, 
    'x__run_black__mutmut_20': x__run_black__mutmut_20, 
    'x__run_black__mutmut_21': x__run_black__mutmut_21, 
    'x__run_black__mutmut_22': x__run_black__mutmut_22, 
    'x__run_black__mutmut_23': x__run_black__mutmut_23, 
    'x__run_black__mutmut_24': x__run_black__mutmut_24, 
    'x__run_black__mutmut_25': x__run_black__mutmut_25, 
    'x__run_black__mutmut_26': x__run_black__mutmut_26, 
    'x__run_black__mutmut_27': x__run_black__mutmut_27, 
    'x__run_black__mutmut_28': x__run_black__mutmut_28, 
    'x__run_black__mutmut_29': x__run_black__mutmut_29, 
    'x__run_black__mutmut_30': x__run_black__mutmut_30, 
    'x__run_black__mutmut_31': x__run_black__mutmut_31, 
    'x__run_black__mutmut_32': x__run_black__mutmut_32, 
    'x__run_black__mutmut_33': x__run_black__mutmut_33, 
    'x__run_black__mutmut_34': x__run_black__mutmut_34, 
    'x__run_black__mutmut_35': x__run_black__mutmut_35
}

def _run_black(*args, **kwargs):
    result = _mutmut_trampoline(x__run_black__mutmut_orig, x__run_black__mutmut_mutants, args, kwargs)
    return result 

_run_black.__signature__ = _mutmut_signature(x__run_black__mutmut_orig)
x__run_black__mutmut_orig.__name__ = 'x__run_black'


def x__run_isort__mutmut_orig(file_path: Path) -> Optional[str]:
    """Run isort and return modified content."""
    try:
        tool_path = _resolve_tool("isort")
        if not tool_path:
            return None
        # Security: Using 'isort' from PATH - assumes it's a trusted import sorting tool
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Isort formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_isort__mutmut_1(file_path: Path) -> Optional[str]:
    """Run isort and return modified content."""
    try:
        tool_path = None
        if not tool_path:
            return None
        # Security: Using 'isort' from PATH - assumes it's a trusted import sorting tool
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Isort formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_isort__mutmut_2(file_path: Path) -> Optional[str]:
    """Run isort and return modified content."""
    try:
        tool_path = _resolve_tool(None)
        if not tool_path:
            return None
        # Security: Using 'isort' from PATH - assumes it's a trusted import sorting tool
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Isort formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_isort__mutmut_3(file_path: Path) -> Optional[str]:
    """Run isort and return modified content."""
    try:
        tool_path = _resolve_tool("XXisortXX")
        if not tool_path:
            return None
        # Security: Using 'isort' from PATH - assumes it's a trusted import sorting tool
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Isort formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_isort__mutmut_4(file_path: Path) -> Optional[str]:
    """Run isort and return modified content."""
    try:
        tool_path = _resolve_tool("ISORT")
        if not tool_path:
            return None
        # Security: Using 'isort' from PATH - assumes it's a trusted import sorting tool
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Isort formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_isort__mutmut_5(file_path: Path) -> Optional[str]:
    """Run isort and return modified content."""
    try:
        tool_path = _resolve_tool("isort")
        if tool_path:
            return None
        # Security: Using 'isort' from PATH - assumes it's a trusted import sorting tool
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Isort formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_isort__mutmut_6(file_path: Path) -> Optional[str]:
    """Run isort and return modified content."""
    try:
        tool_path = _resolve_tool("isort")
        if not tool_path:
            return None
        # Security: Using 'isort' from PATH - assumes it's a trusted import sorting tool
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = None
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Isort formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_isort__mutmut_7(file_path: Path) -> Optional[str]:
    """Run isort and return modified content."""
    try:
        tool_path = _resolve_tool("isort")
        if not tool_path:
            return None
        # Security: Using 'isort' from PATH - assumes it's a trusted import sorting tool
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            None,
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Isort formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_isort__mutmut_8(file_path: Path) -> Optional[str]:
    """Run isort and return modified content."""
    try:
        tool_path = _resolve_tool("isort")
        if not tool_path:
            return None
        # Security: Using 'isort' from PATH - assumes it's a trusted import sorting tool
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=None,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Isort formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_isort__mutmut_9(file_path: Path) -> Optional[str]:
    """Run isort and return modified content."""
    try:
        tool_path = _resolve_tool("isort")
        if not tool_path:
            return None
        # Security: Using 'isort' from PATH - assumes it's a trusted import sorting tool
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=None,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Isort formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_isort__mutmut_10(file_path: Path) -> Optional[str]:
    """Run isort and return modified content."""
    try:
        tool_path = _resolve_tool("isort")
        if not tool_path:
            return None
        # Security: Using 'isort' from PATH - assumes it's a trusted import sorting tool
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            timeout=None,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Isort formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_isort__mutmut_11(file_path: Path) -> Optional[str]:
    """Run isort and return modified content."""
    try:
        tool_path = _resolve_tool("isort")
        if not tool_path:
            return None
        # Security: Using 'isort' from PATH - assumes it's a trusted import sorting tool
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Isort formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_isort__mutmut_12(file_path: Path) -> Optional[str]:
    """Run isort and return modified content."""
    try:
        tool_path = _resolve_tool("isort")
        if not tool_path:
            return None
        # Security: Using 'isort' from PATH - assumes it's a trusted import sorting tool
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Isort formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_isort__mutmut_13(file_path: Path) -> Optional[str]:
    """Run isort and return modified content."""
    try:
        tool_path = _resolve_tool("isort")
        if not tool_path:
            return None
        # Security: Using 'isort' from PATH - assumes it's a trusted import sorting tool
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Isort formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_isort__mutmut_14(file_path: Path) -> Optional[str]:
    """Run isort and return modified content."""
    try:
        tool_path = _resolve_tool("isort")
        if not tool_path:
            return None
        # Security: Using 'isort' from PATH - assumes it's a trusted import sorting tool
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Isort formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_isort__mutmut_15(file_path: Path) -> Optional[str]:
    """Run isort and return modified content."""
    try:
        tool_path = _resolve_tool("isort")
        if not tool_path:
            return None
        # Security: Using 'isort' from PATH - assumes it's a trusted import sorting tool
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "XX--quietXX", str(file_path)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Isort formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_isort__mutmut_16(file_path: Path) -> Optional[str]:
    """Run isort and return modified content."""
    try:
        tool_path = _resolve_tool("isort")
        if not tool_path:
            return None
        # Security: Using 'isort' from PATH - assumes it's a trusted import sorting tool
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--QUIET", str(file_path)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Isort formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_isort__mutmut_17(file_path: Path) -> Optional[str]:
    """Run isort and return modified content."""
    try:
        tool_path = _resolve_tool("isort")
        if not tool_path:
            return None
        # Security: Using 'isort' from PATH - assumes it's a trusted import sorting tool
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(None)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Isort formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_isort__mutmut_18(file_path: Path) -> Optional[str]:
    """Run isort and return modified content."""
    try:
        tool_path = _resolve_tool("isort")
        if not tool_path:
            return None
        # Security: Using 'isort' from PATH - assumes it's a trusted import sorting tool
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=False,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Isort formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_isort__mutmut_19(file_path: Path) -> Optional[str]:
    """Run isort and return modified content."""
    try:
        tool_path = _resolve_tool("isort")
        if not tool_path:
            return None
        # Security: Using 'isort' from PATH - assumes it's a trusted import sorting tool
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=False,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Isort formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_isort__mutmut_20(file_path: Path) -> Optional[str]:
    """Run isort and return modified content."""
    try:
        tool_path = _resolve_tool("isort")
        if not tool_path:
            return None
        # Security: Using 'isort' from PATH - assumes it's a trusted import sorting tool
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            timeout=31,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Isort formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_isort__mutmut_21(file_path: Path) -> Optional[str]:
    """Run isort and return modified content."""
    try:
        tool_path = _resolve_tool("isort")
        if not tool_path:
            return None
        # Security: Using 'isort' from PATH - assumes it's a trusted import sorting tool
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode != 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Isort formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_isort__mutmut_22(file_path: Path) -> Optional[str]:
    """Run isort and return modified content."""
    try:
        tool_path = _resolve_tool("isort")
        if not tool_path:
            return None
        # Security: Using 'isort' from PATH - assumes it's a trusted import sorting tool
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 1:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Isort formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_isort__mutmut_23(file_path: Path) -> Optional[str]:
    """Run isort and return modified content."""
    try:
        tool_path = _resolve_tool("isort")
        if not tool_path:
            return None
        # Security: Using 'isort' from PATH - assumes it's a trusted import sorting tool
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding=None)
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Isort formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_isort__mutmut_24(file_path: Path) -> Optional[str]:
    """Run isort and return modified content."""
    try:
        tool_path = _resolve_tool("isort")
        if not tool_path:
            return None
        # Security: Using 'isort' from PATH - assumes it's a trusted import sorting tool
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="XXutf-8XX")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Isort formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_isort__mutmut_25(file_path: Path) -> Optional[str]:
    """Run isort and return modified content."""
    try:
        tool_path = _resolve_tool("isort")
        if not tool_path:
            return None
        # Security: Using 'isort' from PATH - assumes it's a trusted import sorting tool
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="UTF-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Isort formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_isort__mutmut_26(file_path: Path) -> Optional[str]:
    """Run isort and return modified content."""
    try:
        tool_path = _resolve_tool("isort")
        if not tool_path:
            return None
        # Security: Using 'isort' from PATH - assumes it's a trusted import sorting tool
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(None)
        logger.debug("Isort formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_isort__mutmut_27(file_path: Path) -> Optional[str]:
    """Run isort and return modified content."""
    try:
        tool_path = _resolve_tool("isort")
        if not tool_path:
            return None
        # Security: Using 'isort' from PATH - assumes it's a trusted import sorting tool
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug(None, file_path, exc)
    return None


def x__run_isort__mutmut_28(file_path: Path) -> Optional[str]:
    """Run isort and return modified content."""
    try:
        tool_path = _resolve_tool("isort")
        if not tool_path:
            return None
        # Security: Using 'isort' from PATH - assumes it's a trusted import sorting tool
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Isort formatting skipped for %s: %s", None, exc)
    return None


def x__run_isort__mutmut_29(file_path: Path) -> Optional[str]:
    """Run isort and return modified content."""
    try:
        tool_path = _resolve_tool("isort")
        if not tool_path:
            return None
        # Security: Using 'isort' from PATH - assumes it's a trusted import sorting tool
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Isort formatting skipped for %s: %s", file_path, None)
    return None


def x__run_isort__mutmut_30(file_path: Path) -> Optional[str]:
    """Run isort and return modified content."""
    try:
        tool_path = _resolve_tool("isort")
        if not tool_path:
            return None
        # Security: Using 'isort' from PATH - assumes it's a trusted import sorting tool
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug(file_path, exc)
    return None


def x__run_isort__mutmut_31(file_path: Path) -> Optional[str]:
    """Run isort and return modified content."""
    try:
        tool_path = _resolve_tool("isort")
        if not tool_path:
            return None
        # Security: Using 'isort' from PATH - assumes it's a trusted import sorting tool
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Isort formatting skipped for %s: %s", exc)
    return None


def x__run_isort__mutmut_32(file_path: Path) -> Optional[str]:
    """Run isort and return modified content."""
    try:
        tool_path = _resolve_tool("isort")
        if not tool_path:
            return None
        # Security: Using 'isort' from PATH - assumes it's a trusted import sorting tool
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("Isort formatting skipped for %s: %s", file_path, )
    return None


def x__run_isort__mutmut_33(file_path: Path) -> Optional[str]:
    """Run isort and return modified content."""
    try:
        tool_path = _resolve_tool("isort")
        if not tool_path:
            return None
        # Security: Using 'isort' from PATH - assumes it's a trusted import sorting tool
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("XXIsort formatting skipped for %s: %sXX", file_path, exc)
    return None


def x__run_isort__mutmut_34(file_path: Path) -> Optional[str]:
    """Run isort and return modified content."""
    try:
        tool_path = _resolve_tool("isort")
        if not tool_path:
            return None
        # Security: Using 'isort' from PATH - assumes it's a trusted import sorting tool
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("isort formatting skipped for %s: %s", file_path, exc)
    return None


def x__run_isort__mutmut_35(file_path: Path) -> Optional[str]:
    """Run isort and return modified content."""
    try:
        tool_path = _resolve_tool("isort")
        if not tool_path:
            return None
        # Security: Using 'isort' from PATH - assumes it's a trusted import sorting tool
        # installed in the development environment. The file_path is validated as a Path.
        # Arguments are passed as a list to prevent shell injection.
        result = subprocess.run(
            [tool_path, "--quiet", str(file_path)],
            capture_output=True,
            text=True,
            timeout=30,
        )
        if result.returncode == 0:
            return file_path.read_text(encoding="utf-8")
    except (FileNotFoundError, subprocess.TimeoutExpired) as exc:
        logger.debug(f"Exception: {exc}")
        logger.debug("ISORT FORMATTING SKIPPED FOR %S: %S", file_path, exc)
    return None

x__run_isort__mutmut_mutants : ClassVar[MutantDict] = {
'x__run_isort__mutmut_1': x__run_isort__mutmut_1, 
    'x__run_isort__mutmut_2': x__run_isort__mutmut_2, 
    'x__run_isort__mutmut_3': x__run_isort__mutmut_3, 
    'x__run_isort__mutmut_4': x__run_isort__mutmut_4, 
    'x__run_isort__mutmut_5': x__run_isort__mutmut_5, 
    'x__run_isort__mutmut_6': x__run_isort__mutmut_6, 
    'x__run_isort__mutmut_7': x__run_isort__mutmut_7, 
    'x__run_isort__mutmut_8': x__run_isort__mutmut_8, 
    'x__run_isort__mutmut_9': x__run_isort__mutmut_9, 
    'x__run_isort__mutmut_10': x__run_isort__mutmut_10, 
    'x__run_isort__mutmut_11': x__run_isort__mutmut_11, 
    'x__run_isort__mutmut_12': x__run_isort__mutmut_12, 
    'x__run_isort__mutmut_13': x__run_isort__mutmut_13, 
    'x__run_isort__mutmut_14': x__run_isort__mutmut_14, 
    'x__run_isort__mutmut_15': x__run_isort__mutmut_15, 
    'x__run_isort__mutmut_16': x__run_isort__mutmut_16, 
    'x__run_isort__mutmut_17': x__run_isort__mutmut_17, 
    'x__run_isort__mutmut_18': x__run_isort__mutmut_18, 
    'x__run_isort__mutmut_19': x__run_isort__mutmut_19, 
    'x__run_isort__mutmut_20': x__run_isort__mutmut_20, 
    'x__run_isort__mutmut_21': x__run_isort__mutmut_21, 
    'x__run_isort__mutmut_22': x__run_isort__mutmut_22, 
    'x__run_isort__mutmut_23': x__run_isort__mutmut_23, 
    'x__run_isort__mutmut_24': x__run_isort__mutmut_24, 
    'x__run_isort__mutmut_25': x__run_isort__mutmut_25, 
    'x__run_isort__mutmut_26': x__run_isort__mutmut_26, 
    'x__run_isort__mutmut_27': x__run_isort__mutmut_27, 
    'x__run_isort__mutmut_28': x__run_isort__mutmut_28, 
    'x__run_isort__mutmut_29': x__run_isort__mutmut_29, 
    'x__run_isort__mutmut_30': x__run_isort__mutmut_30, 
    'x__run_isort__mutmut_31': x__run_isort__mutmut_31, 
    'x__run_isort__mutmut_32': x__run_isort__mutmut_32, 
    'x__run_isort__mutmut_33': x__run_isort__mutmut_33, 
    'x__run_isort__mutmut_34': x__run_isort__mutmut_34, 
    'x__run_isort__mutmut_35': x__run_isort__mutmut_35
}

def _run_isort(*args, **kwargs):
    result = _mutmut_trampoline(x__run_isort__mutmut_orig, x__run_isort__mutmut_mutants, args, kwargs)
    return result 

_run_isort.__signature__ = _mutmut_signature(x__run_isort__mutmut_orig)
x__run_isort__mutmut_orig.__name__ = 'x__run_isort'


def x__apply_pathlib_migration__mutmut_orig(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_1(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = None
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_2(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'XXos\.path\.join\(([^,]+),\s*([^)]+)\)XX', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_3(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'OS\.PATH\.JOIN\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_4(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'XXPath(\1) / \2XX'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_5(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_6(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'PATH(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_7(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'XXos\.path\.exists\(([^)]+)\)XX', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_8(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'OS\.PATH\.EXISTS\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_9(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'XXPath(\1).exists()XX'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_10(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_11(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'PATH(\1).EXISTS()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_12(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'XXos\.path\.dirname\(([^)]+)\)XX', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_13(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'OS\.PATH\.DIRNAME\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_14(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'XXstr(Path(\1).parent)XX'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_15(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_16(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'STR(PATH(\1).PARENT)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_17(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'XXos\.path\.basename\(([^)]+)\)XX', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_18(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'OS\.PATH\.BASENAME\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_19(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'XXPath(\1).nameXX'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_20(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_21(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'PATH(\1).NAME'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_22(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'XXos\.path\.isfile\(([^)]+)\)XX', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_23(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'OS\.PATH\.ISFILE\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_24(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'XXPath(\1).is_file()XX'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_25(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_26(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'PATH(\1).IS_FILE()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_27(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'XXos\.path\.isdir\(([^)]+)\)XX', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_28(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'OS\.PATH\.ISDIR\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_29(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'XXPath(\1).is_dir()XX'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_30(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_31(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'PATH(\1).IS_DIR()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_32(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = None
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_33(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = None
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_34(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(None, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_35(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, None, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_36(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, None)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_37(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_38(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_39(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, )
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_40(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content or "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_41(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified == content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_42(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "XXfrom pathlib importXX" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_43(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "FROM PATHLIB IMPORT" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_44(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_45(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "XXimport osXX" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_46(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "IMPORT OS" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_47(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" not in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_48(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = None
    
    return modified


def x__apply_pathlib_migration__mutmut_49(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                None,
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_50(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                None,
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_51(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                None
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_52(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_53(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_54(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                )
    
    return modified


def x__apply_pathlib_migration__mutmut_55(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "XXimport osXX",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_56(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "IMPORT OS",
                "import os\nfrom pathlib import Path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_57(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "XXimport os\nfrom pathlib import PathXX",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_58(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import path",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_59(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "IMPORT OS\nFROM PATHLIB IMPORT PATH",
                1
            )
    
    return modified


def x__apply_pathlib_migration__mutmut_60(content: str) -> str:
    """Apply os.path to pathlib migration.
    
    Simple pattern-based replacement for common patterns.
    """
    import re
    
    replacements = [
        (r'os\.path\.join\(([^,]+),\s*([^)]+)\)', r'Path(\1) / \2'),
        (r'os\.path\.exists\(([^)]+)\)', r'Path(\1).exists()'),
        (r'os\.path\.dirname\(([^)]+)\)', r'str(Path(\1).parent)'),
        (r'os\.path\.basename\(([^)]+)\)', r'Path(\1).name'),
        (r'os\.path\.isfile\(([^)]+)\)', r'Path(\1).is_file()'),
        (r'os\.path\.isdir\(([^)]+)\)', r'Path(\1).is_dir()'),
    ]
    
    modified = content
    for pattern, replacement in replacements:
        modified = re.sub(pattern, replacement, modified)
    
    # Add pathlib import if we made changes and it's not already imported
    if modified != content and "from pathlib import" not in modified:
        if "import os" in modified:
            modified = modified.replace(
                "import os",
                "import os\nfrom pathlib import Path",
                2
            )
    
    return modified

x__apply_pathlib_migration__mutmut_mutants : ClassVar[MutantDict] = {
'x__apply_pathlib_migration__mutmut_1': x__apply_pathlib_migration__mutmut_1, 
    'x__apply_pathlib_migration__mutmut_2': x__apply_pathlib_migration__mutmut_2, 
    'x__apply_pathlib_migration__mutmut_3': x__apply_pathlib_migration__mutmut_3, 
    'x__apply_pathlib_migration__mutmut_4': x__apply_pathlib_migration__mutmut_4, 
    'x__apply_pathlib_migration__mutmut_5': x__apply_pathlib_migration__mutmut_5, 
    'x__apply_pathlib_migration__mutmut_6': x__apply_pathlib_migration__mutmut_6, 
    'x__apply_pathlib_migration__mutmut_7': x__apply_pathlib_migration__mutmut_7, 
    'x__apply_pathlib_migration__mutmut_8': x__apply_pathlib_migration__mutmut_8, 
    'x__apply_pathlib_migration__mutmut_9': x__apply_pathlib_migration__mutmut_9, 
    'x__apply_pathlib_migration__mutmut_10': x__apply_pathlib_migration__mutmut_10, 
    'x__apply_pathlib_migration__mutmut_11': x__apply_pathlib_migration__mutmut_11, 
    'x__apply_pathlib_migration__mutmut_12': x__apply_pathlib_migration__mutmut_12, 
    'x__apply_pathlib_migration__mutmut_13': x__apply_pathlib_migration__mutmut_13, 
    'x__apply_pathlib_migration__mutmut_14': x__apply_pathlib_migration__mutmut_14, 
    'x__apply_pathlib_migration__mutmut_15': x__apply_pathlib_migration__mutmut_15, 
    'x__apply_pathlib_migration__mutmut_16': x__apply_pathlib_migration__mutmut_16, 
    'x__apply_pathlib_migration__mutmut_17': x__apply_pathlib_migration__mutmut_17, 
    'x__apply_pathlib_migration__mutmut_18': x__apply_pathlib_migration__mutmut_18, 
    'x__apply_pathlib_migration__mutmut_19': x__apply_pathlib_migration__mutmut_19, 
    'x__apply_pathlib_migration__mutmut_20': x__apply_pathlib_migration__mutmut_20, 
    'x__apply_pathlib_migration__mutmut_21': x__apply_pathlib_migration__mutmut_21, 
    'x__apply_pathlib_migration__mutmut_22': x__apply_pathlib_migration__mutmut_22, 
    'x__apply_pathlib_migration__mutmut_23': x__apply_pathlib_migration__mutmut_23, 
    'x__apply_pathlib_migration__mutmut_24': x__apply_pathlib_migration__mutmut_24, 
    'x__apply_pathlib_migration__mutmut_25': x__apply_pathlib_migration__mutmut_25, 
    'x__apply_pathlib_migration__mutmut_26': x__apply_pathlib_migration__mutmut_26, 
    'x__apply_pathlib_migration__mutmut_27': x__apply_pathlib_migration__mutmut_27, 
    'x__apply_pathlib_migration__mutmut_28': x__apply_pathlib_migration__mutmut_28, 
    'x__apply_pathlib_migration__mutmut_29': x__apply_pathlib_migration__mutmut_29, 
    'x__apply_pathlib_migration__mutmut_30': x__apply_pathlib_migration__mutmut_30, 
    'x__apply_pathlib_migration__mutmut_31': x__apply_pathlib_migration__mutmut_31, 
    'x__apply_pathlib_migration__mutmut_32': x__apply_pathlib_migration__mutmut_32, 
    'x__apply_pathlib_migration__mutmut_33': x__apply_pathlib_migration__mutmut_33, 
    'x__apply_pathlib_migration__mutmut_34': x__apply_pathlib_migration__mutmut_34, 
    'x__apply_pathlib_migration__mutmut_35': x__apply_pathlib_migration__mutmut_35, 
    'x__apply_pathlib_migration__mutmut_36': x__apply_pathlib_migration__mutmut_36, 
    'x__apply_pathlib_migration__mutmut_37': x__apply_pathlib_migration__mutmut_37, 
    'x__apply_pathlib_migration__mutmut_38': x__apply_pathlib_migration__mutmut_38, 
    'x__apply_pathlib_migration__mutmut_39': x__apply_pathlib_migration__mutmut_39, 
    'x__apply_pathlib_migration__mutmut_40': x__apply_pathlib_migration__mutmut_40, 
    'x__apply_pathlib_migration__mutmut_41': x__apply_pathlib_migration__mutmut_41, 
    'x__apply_pathlib_migration__mutmut_42': x__apply_pathlib_migration__mutmut_42, 
    'x__apply_pathlib_migration__mutmut_43': x__apply_pathlib_migration__mutmut_43, 
    'x__apply_pathlib_migration__mutmut_44': x__apply_pathlib_migration__mutmut_44, 
    'x__apply_pathlib_migration__mutmut_45': x__apply_pathlib_migration__mutmut_45, 
    'x__apply_pathlib_migration__mutmut_46': x__apply_pathlib_migration__mutmut_46, 
    'x__apply_pathlib_migration__mutmut_47': x__apply_pathlib_migration__mutmut_47, 
    'x__apply_pathlib_migration__mutmut_48': x__apply_pathlib_migration__mutmut_48, 
    'x__apply_pathlib_migration__mutmut_49': x__apply_pathlib_migration__mutmut_49, 
    'x__apply_pathlib_migration__mutmut_50': x__apply_pathlib_migration__mutmut_50, 
    'x__apply_pathlib_migration__mutmut_51': x__apply_pathlib_migration__mutmut_51, 
    'x__apply_pathlib_migration__mutmut_52': x__apply_pathlib_migration__mutmut_52, 
    'x__apply_pathlib_migration__mutmut_53': x__apply_pathlib_migration__mutmut_53, 
    'x__apply_pathlib_migration__mutmut_54': x__apply_pathlib_migration__mutmut_54, 
    'x__apply_pathlib_migration__mutmut_55': x__apply_pathlib_migration__mutmut_55, 
    'x__apply_pathlib_migration__mutmut_56': x__apply_pathlib_migration__mutmut_56, 
    'x__apply_pathlib_migration__mutmut_57': x__apply_pathlib_migration__mutmut_57, 
    'x__apply_pathlib_migration__mutmut_58': x__apply_pathlib_migration__mutmut_58, 
    'x__apply_pathlib_migration__mutmut_59': x__apply_pathlib_migration__mutmut_59, 
    'x__apply_pathlib_migration__mutmut_60': x__apply_pathlib_migration__mutmut_60
}

def _apply_pathlib_migration(*args, **kwargs):
    result = _mutmut_trampoline(x__apply_pathlib_migration__mutmut_orig, x__apply_pathlib_migration__mutmut_mutants, args, kwargs)
    return result 

_apply_pathlib_migration.__signature__ = _mutmut_signature(x__apply_pathlib_migration__mutmut_orig)
x__apply_pathlib_migration__mutmut_orig.__name__ = 'x__apply_pathlib_migration'


def x_transform__mutmut_orig(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_1(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = True,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_2(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = False,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_3(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = None
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_4(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(None)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_5(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = None
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_6(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=None, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_7(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=None)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_8(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_9(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, )
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_10(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = None
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_11(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(None)
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_12(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob(None))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_13(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("XX*.pyXX"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_14(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.PY"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_15(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info(None, len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_16(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", None)
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_17(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info(len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_18(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", )
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_19(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("XXTransforming %d Python filesXX", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_20(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("transforming %d python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_21(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("TRANSFORMING %D PYTHON FILES", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_22(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = None
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_23(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding=None, errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_24(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors=None)
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_25(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_26(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", )
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_27(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="XXutf-8XX", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_28(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="UTF-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_29(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="XXreplaceXX")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_30(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="REPLACE")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_31(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = None
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_32(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(None)
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_33(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(None))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_34(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None and tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_35(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is not None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_36(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier != Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_37(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_38(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = None
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_39(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(None)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_40(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result or black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_41(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result == original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_42(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = None
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_43(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(None, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_44(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, None, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_45(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, None)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_46(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_47(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_48(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, )
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_49(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(None)
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_50(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=None,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_51(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=None,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_52(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=None,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_53(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=None,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_54(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id=None,
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_55(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=None,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_56(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description=None,
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_57(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_58(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_59(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_60(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_61(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_62(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_63(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_64(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="XXformat-blackXX",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_65(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="FORMAT-BLACK",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_66(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="XXApply Black code formattingXX",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_67(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="apply black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_68(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="APPLY BLACK CODE FORMATTING",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_69(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = None
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_70(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_71(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = None
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_72(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(None)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_73(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result or isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_74(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result == original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_75(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = None
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_76(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(None, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_77(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, None, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_78(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, None)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_79(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_80(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_81(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, )
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_82(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(None)
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_83(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=None,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_84(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=None,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_85(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=None,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_86(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=None,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_87(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id=None,
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_88(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=None,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_89(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description=None,
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_90(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_91(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_92(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_93(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_94(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_95(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_96(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_97(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="XXsort-importsXX",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_98(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="SORT-IMPORTS",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_99(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="XXSort imports with isortXX",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_100(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_101(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="SORT IMPORTS WITH ISORT",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_102(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = None
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_103(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = None
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_104(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(None)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_105(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result == original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_106(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = None
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_107(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(None, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_108(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, None, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_109(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, None)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_110(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_111(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_112(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, )
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_113(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = None
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_114(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=None,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_115(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=None,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_116(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=None,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_117(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=None,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_118(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id=None,
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_119(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=None,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_120(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description=None,
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_121(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_122(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_123(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_124(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_125(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_126(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_127(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_128(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="XXpathlib-migrationXX",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_129(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="PATHLIB-MIGRATION",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_130(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="XXReplace os.path with pathlib equivalentsXX",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_131(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_132(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="REPLACE OS.PATH WITH PATHLIB EQUIVALENTS",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_133(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(None)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_134(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply or not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_135(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_136(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(None, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_137(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding=None)
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_138(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_139(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, )
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_140(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="XXutf-8XX")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_141(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="UTF-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_142(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = None
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_143(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None and tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_144(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is not None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_145(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier != Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_146(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = None
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_147(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(None)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_148(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(None):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_149(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is not None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_150(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(None)
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_151(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=None,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_152(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original=None,
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_153(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified=None,
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_154(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff=None,
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_155(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id=None,
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_156(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=None,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_157(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=None,
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_158(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_159(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_160(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_161(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_162(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_163(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_164(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_165(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="XXXX",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_166(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="XXXX",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_167(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="XXXX",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_168(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="XXadd-type-hintsXX",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_169(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="ADD-TYPE-HINTS",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_170(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                return  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_171(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(None)
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_172(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(None, exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_173(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=None)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_174(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_175(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", )  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_176(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=False)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_177(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None and tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_178(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is not None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_179(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier != Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_180(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original and "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_181(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "XXrequests.XX" in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_182(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "REQUESTS." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_183(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." not in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_184(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "XXurllibXX" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_185(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "URLLIB" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_186(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" not in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_187(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append(None)
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_188(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "XXrule_idXX": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_189(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "RULE_ID": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_190(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "XXasync-conversionXX",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_191(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "ASYNC-CONVERSION",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_192(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "XXfileXX": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_193(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "FILE": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_194(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "XXdescriptionXX": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_195(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "DESCRIPTION": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_196(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "XXConsider converting synchronous HTTP calls to asyncXX",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_197(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "consider converting synchronous http calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_198(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "CONSIDER CONVERTING SYNCHRONOUS HTTP CALLS TO ASYNC",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_199(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "XXwarningXX": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_200(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "WARNING": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_201(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "XXMajor API change; requires full rewrite of callersXX",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_202(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "major api change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_203(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "MAJOR API CHANGE; REQUIRES FULL REWRITE OF CALLERS",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_204(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "XXchecklistXX": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_205(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "CHECKLIST": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_206(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "XXAll callers identified and updatedXX",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_207(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "all callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_208(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "ALL CALLERS IDENTIFIED AND UPDATED",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_209(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "XXEvent loop management reviewedXX",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_210(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_211(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "EVENT LOOP MANAGEMENT REVIEWED",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_212(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "XXError handling preservedXX",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_213(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_214(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "ERROR HANDLING PRESERVED",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_215(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(None)
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_216(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(None)
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_217(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error(None, file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_218(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", None, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_219(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, None)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_220(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error(file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_221(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_222(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, )
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_223(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("XXTransform error for %s: %sXX", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_224(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_225(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("TRANSFORM ERROR FOR %S: %S", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_226(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = None
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_227(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply or not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_228(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_229(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        None,
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_230(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        None,
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_231(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        None,
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_232(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        None,
    )
    
    return result


def x_transform__mutmut_233(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_234(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_235(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_236(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "Transform complete: %d Tier A, %d Tier B, %d Tier C suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        )
    
    return result


def x_transform__mutmut_237(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "XXTransform complete: %d Tier A, %d Tier B, %d Tier C suggestionsXX",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_238(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "transform complete: %d tier a, %d tier b, %d tier c suggestions",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result


def x_transform__mutmut_239(
    source_dir: Path,
    snapshot_id: str,
    tier: Optional[Tier] = None,
    auto_apply: bool = False,
    dry_run: bool = True,
) -> TransformResult:
    """Generate and optionally apply transformations.
    
    Args:
        source_dir: Directory containing source files
        snapshot_id: Snapshot being transformed
        tier: Specific tier to apply (None for all)
        auto_apply: Whether to automatically apply Tier A
        dry_run: If True, don't modify files
        
    Returns:
        TransformResult with generated patches
        
    Example:
        >>> result = transform(Path("source/"), "20251217-abc123", auto_apply=True)
        >>> print(f"Generated {len(result.tier_a_patches)} Tier A patches")
    """
    now = datetime.now(timezone.utc)
    result = TransformResult(snapshot_id=snapshot_id, timestamp=now)
    
    # Find Python files
    python_files = sorted(source_dir.rglob("*.py"))
    logger.info("Transforming %d Python files", len(python_files))
    
    for file_path in python_files:
        try:
            original = file_path.read_text(encoding="utf-8", errors="replace")
            rel_path = str(file_path.relative_to(source_dir))
            
            # === Tier A: Safe Auto-Apply ===
            if tier is None or tier == Tier.A:
                # Black formatting (check only in dry-run)
                if not dry_run:
                    black_result = _run_black(file_path)
                    if black_result and black_result != original:
                        diff = _create_diff(original, black_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=black_result,
                            diff=diff,
                            rule_id="format-black",
                            tier=Tier.A,
                            description="Apply Black code formatting",
                        ))
                        original = black_result
                
                # isort
                if not dry_run:
                    isort_result = _run_isort(file_path)
                    if isort_result and isort_result != original:
                        diff = _create_diff(original, isort_result, rel_path)
                        result.tier_a_patches.append(Patch(
                            file_path=rel_path,
                            original=original,
                            modified=isort_result,
                            diff=diff,
                            rule_id="sort-imports",
                            tier=Tier.A,
                            description="Sort imports with isort",
                        ))
                        original = isort_result
                
                # Pathlib migration
                pathlib_result = _apply_pathlib_migration(original)
                if pathlib_result != original:
                    diff = _create_diff(original, pathlib_result, rel_path)
                    patch = Patch(
                        file_path=rel_path,
                        original=original,
                        modified=pathlib_result,
                        diff=diff,
                        rule_id="pathlib-migration",
                        tier=Tier.A,
                        description="Replace os.path with pathlib equivalents",
                    )
                    result.tier_a_patches.append(patch)
                    
                    if auto_apply and not dry_run:
                        file_path.write_text(pathlib_result, encoding="utf-8")
                        original = pathlib_result
            
            # === Tier B: Apply with Tests ===
            if tier is None or tier == Tier.B:
                # Type hints suggestion - use AST for robust detection
                try:
                    tree = ast.parse(original)
                    for node in ast.walk(tree):
                        if isinstance(node, ast.FunctionDef):
                            # Check if function lacks return annotation
                            if node.returns is None:
                                result.tier_b_patches.append(Patch(
                                    file_path=rel_path,
                                    original="",
                                    modified="",
                                    diff="",
                                    rule_id="add-type-hints",
                                    tier=Tier.B,
                                    description=f"Add type annotations to function '{node.name}' (requires validation)",
                                ))
                                break  # Only suggest once per file
                except SyntaxError as e:
                    logger.debug(f"SyntaxError: {e}")
                    logger.warning(f"SyntaxError: {e}", exc_info=True)  # Skip files with syntax errors
            
            # === Tier C: Suggest Only ===
            if tier is None or tier == Tier.C:
                # Async conversion suggestion
                if "requests." in original or "urllib" in original:
                    result.tier_c_suggestions.append({
                        "rule_id": "async-conversion",
                        "file": rel_path,
                        "description": "Consider converting synchronous HTTP calls to async",
                        "warning": "Major API change; requires full rewrite of callers",
                        "checklist": [
                            "All callers identified and updated",
                            "Event loop management reviewed",
                            "Error handling preserved",
                        ],
                    })
                    
        except Exception as e:
            logger.debug(f"Exception: {e}")
            result.errors.append(f"Error processing {file_path}: {e}")
            logger.error("Transform error for %s: %s", file_path, e)
    
    result.applied = auto_apply and not dry_run
    
    logger.info(
        "TRANSFORM COMPLETE: %D TIER A, %D TIER B, %D TIER C SUGGESTIONS",
        len(result.tier_a_patches),
        len(result.tier_b_patches),
        len(result.tier_c_suggestions),
    )
    
    return result

x_transform__mutmut_mutants : ClassVar[MutantDict] = {
'x_transform__mutmut_1': x_transform__mutmut_1, 
    'x_transform__mutmut_2': x_transform__mutmut_2, 
    'x_transform__mutmut_3': x_transform__mutmut_3, 
    'x_transform__mutmut_4': x_transform__mutmut_4, 
    'x_transform__mutmut_5': x_transform__mutmut_5, 
    'x_transform__mutmut_6': x_transform__mutmut_6, 
    'x_transform__mutmut_7': x_transform__mutmut_7, 
    'x_transform__mutmut_8': x_transform__mutmut_8, 
    'x_transform__mutmut_9': x_transform__mutmut_9, 
    'x_transform__mutmut_10': x_transform__mutmut_10, 
    'x_transform__mutmut_11': x_transform__mutmut_11, 
    'x_transform__mutmut_12': x_transform__mutmut_12, 
    'x_transform__mutmut_13': x_transform__mutmut_13, 
    'x_transform__mutmut_14': x_transform__mutmut_14, 
    'x_transform__mutmut_15': x_transform__mutmut_15, 
    'x_transform__mutmut_16': x_transform__mutmut_16, 
    'x_transform__mutmut_17': x_transform__mutmut_17, 
    'x_transform__mutmut_18': x_transform__mutmut_18, 
    'x_transform__mutmut_19': x_transform__mutmut_19, 
    'x_transform__mutmut_20': x_transform__mutmut_20, 
    'x_transform__mutmut_21': x_transform__mutmut_21, 
    'x_transform__mutmut_22': x_transform__mutmut_22, 
    'x_transform__mutmut_23': x_transform__mutmut_23, 
    'x_transform__mutmut_24': x_transform__mutmut_24, 
    'x_transform__mutmut_25': x_transform__mutmut_25, 
    'x_transform__mutmut_26': x_transform__mutmut_26, 
    'x_transform__mutmut_27': x_transform__mutmut_27, 
    'x_transform__mutmut_28': x_transform__mutmut_28, 
    'x_transform__mutmut_29': x_transform__mutmut_29, 
    'x_transform__mutmut_30': x_transform__mutmut_30, 
    'x_transform__mutmut_31': x_transform__mutmut_31, 
    'x_transform__mutmut_32': x_transform__mutmut_32, 
    'x_transform__mutmut_33': x_transform__mutmut_33, 
    'x_transform__mutmut_34': x_transform__mutmut_34, 
    'x_transform__mutmut_35': x_transform__mutmut_35, 
    'x_transform__mutmut_36': x_transform__mutmut_36, 
    'x_transform__mutmut_37': x_transform__mutmut_37, 
    'x_transform__mutmut_38': x_transform__mutmut_38, 
    'x_transform__mutmut_39': x_transform__mutmut_39, 
    'x_transform__mutmut_40': x_transform__mutmut_40, 
    'x_transform__mutmut_41': x_transform__mutmut_41, 
    'x_transform__mutmut_42': x_transform__mutmut_42, 
    'x_transform__mutmut_43': x_transform__mutmut_43, 
    'x_transform__mutmut_44': x_transform__mutmut_44, 
    'x_transform__mutmut_45': x_transform__mutmut_45, 
    'x_transform__mutmut_46': x_transform__mutmut_46, 
    'x_transform__mutmut_47': x_transform__mutmut_47, 
    'x_transform__mutmut_48': x_transform__mutmut_48, 
    'x_transform__mutmut_49': x_transform__mutmut_49, 
    'x_transform__mutmut_50': x_transform__mutmut_50, 
    'x_transform__mutmut_51': x_transform__mutmut_51, 
    'x_transform__mutmut_52': x_transform__mutmut_52, 
    'x_transform__mutmut_53': x_transform__mutmut_53, 
    'x_transform__mutmut_54': x_transform__mutmut_54, 
    'x_transform__mutmut_55': x_transform__mutmut_55, 
    'x_transform__mutmut_56': x_transform__mutmut_56, 
    'x_transform__mutmut_57': x_transform__mutmut_57, 
    'x_transform__mutmut_58': x_transform__mutmut_58, 
    'x_transform__mutmut_59': x_transform__mutmut_59, 
    'x_transform__mutmut_60': x_transform__mutmut_60, 
    'x_transform__mutmut_61': x_transform__mutmut_61, 
    'x_transform__mutmut_62': x_transform__mutmut_62, 
    'x_transform__mutmut_63': x_transform__mutmut_63, 
    'x_transform__mutmut_64': x_transform__mutmut_64, 
    'x_transform__mutmut_65': x_transform__mutmut_65, 
    'x_transform__mutmut_66': x_transform__mutmut_66, 
    'x_transform__mutmut_67': x_transform__mutmut_67, 
    'x_transform__mutmut_68': x_transform__mutmut_68, 
    'x_transform__mutmut_69': x_transform__mutmut_69, 
    'x_transform__mutmut_70': x_transform__mutmut_70, 
    'x_transform__mutmut_71': x_transform__mutmut_71, 
    'x_transform__mutmut_72': x_transform__mutmut_72, 
    'x_transform__mutmut_73': x_transform__mutmut_73, 
    'x_transform__mutmut_74': x_transform__mutmut_74, 
    'x_transform__mutmut_75': x_transform__mutmut_75, 
    'x_transform__mutmut_76': x_transform__mutmut_76, 
    'x_transform__mutmut_77': x_transform__mutmut_77, 
    'x_transform__mutmut_78': x_transform__mutmut_78, 
    'x_transform__mutmut_79': x_transform__mutmut_79, 
    'x_transform__mutmut_80': x_transform__mutmut_80, 
    'x_transform__mutmut_81': x_transform__mutmut_81, 
    'x_transform__mutmut_82': x_transform__mutmut_82, 
    'x_transform__mutmut_83': x_transform__mutmut_83, 
    'x_transform__mutmut_84': x_transform__mutmut_84, 
    'x_transform__mutmut_85': x_transform__mutmut_85, 
    'x_transform__mutmut_86': x_transform__mutmut_86, 
    'x_transform__mutmut_87': x_transform__mutmut_87, 
    'x_transform__mutmut_88': x_transform__mutmut_88, 
    'x_transform__mutmut_89': x_transform__mutmut_89, 
    'x_transform__mutmut_90': x_transform__mutmut_90, 
    'x_transform__mutmut_91': x_transform__mutmut_91, 
    'x_transform__mutmut_92': x_transform__mutmut_92, 
    'x_transform__mutmut_93': x_transform__mutmut_93, 
    'x_transform__mutmut_94': x_transform__mutmut_94, 
    'x_transform__mutmut_95': x_transform__mutmut_95, 
    'x_transform__mutmut_96': x_transform__mutmut_96, 
    'x_transform__mutmut_97': x_transform__mutmut_97, 
    'x_transform__mutmut_98': x_transform__mutmut_98, 
    'x_transform__mutmut_99': x_transform__mutmut_99, 
    'x_transform__mutmut_100': x_transform__mutmut_100, 
    'x_transform__mutmut_101': x_transform__mutmut_101, 
    'x_transform__mutmut_102': x_transform__mutmut_102, 
    'x_transform__mutmut_103': x_transform__mutmut_103, 
    'x_transform__mutmut_104': x_transform__mutmut_104, 
    'x_transform__mutmut_105': x_transform__mutmut_105, 
    'x_transform__mutmut_106': x_transform__mutmut_106, 
    'x_transform__mutmut_107': x_transform__mutmut_107, 
    'x_transform__mutmut_108': x_transform__mutmut_108, 
    'x_transform__mutmut_109': x_transform__mutmut_109, 
    'x_transform__mutmut_110': x_transform__mutmut_110, 
    'x_transform__mutmut_111': x_transform__mutmut_111, 
    'x_transform__mutmut_112': x_transform__mutmut_112, 
    'x_transform__mutmut_113': x_transform__mutmut_113, 
    'x_transform__mutmut_114': x_transform__mutmut_114, 
    'x_transform__mutmut_115': x_transform__mutmut_115, 
    'x_transform__mutmut_116': x_transform__mutmut_116, 
    'x_transform__mutmut_117': x_transform__mutmut_117, 
    'x_transform__mutmut_118': x_transform__mutmut_118, 
    'x_transform__mutmut_119': x_transform__mutmut_119, 
    'x_transform__mutmut_120': x_transform__mutmut_120, 
    'x_transform__mutmut_121': x_transform__mutmut_121, 
    'x_transform__mutmut_122': x_transform__mutmut_122, 
    'x_transform__mutmut_123': x_transform__mutmut_123, 
    'x_transform__mutmut_124': x_transform__mutmut_124, 
    'x_transform__mutmut_125': x_transform__mutmut_125, 
    'x_transform__mutmut_126': x_transform__mutmut_126, 
    'x_transform__mutmut_127': x_transform__mutmut_127, 
    'x_transform__mutmut_128': x_transform__mutmut_128, 
    'x_transform__mutmut_129': x_transform__mutmut_129, 
    'x_transform__mutmut_130': x_transform__mutmut_130, 
    'x_transform__mutmut_131': x_transform__mutmut_131, 
    'x_transform__mutmut_132': x_transform__mutmut_132, 
    'x_transform__mutmut_133': x_transform__mutmut_133, 
    'x_transform__mutmut_134': x_transform__mutmut_134, 
    'x_transform__mutmut_135': x_transform__mutmut_135, 
    'x_transform__mutmut_136': x_transform__mutmut_136, 
    'x_transform__mutmut_137': x_transform__mutmut_137, 
    'x_transform__mutmut_138': x_transform__mutmut_138, 
    'x_transform__mutmut_139': x_transform__mutmut_139, 
    'x_transform__mutmut_140': x_transform__mutmut_140, 
    'x_transform__mutmut_141': x_transform__mutmut_141, 
    'x_transform__mutmut_142': x_transform__mutmut_142, 
    'x_transform__mutmut_143': x_transform__mutmut_143, 
    'x_transform__mutmut_144': x_transform__mutmut_144, 
    'x_transform__mutmut_145': x_transform__mutmut_145, 
    'x_transform__mutmut_146': x_transform__mutmut_146, 
    'x_transform__mutmut_147': x_transform__mutmut_147, 
    'x_transform__mutmut_148': x_transform__mutmut_148, 
    'x_transform__mutmut_149': x_transform__mutmut_149, 
    'x_transform__mutmut_150': x_transform__mutmut_150, 
    'x_transform__mutmut_151': x_transform__mutmut_151, 
    'x_transform__mutmut_152': x_transform__mutmut_152, 
    'x_transform__mutmut_153': x_transform__mutmut_153, 
    'x_transform__mutmut_154': x_transform__mutmut_154, 
    'x_transform__mutmut_155': x_transform__mutmut_155, 
    'x_transform__mutmut_156': x_transform__mutmut_156, 
    'x_transform__mutmut_157': x_transform__mutmut_157, 
    'x_transform__mutmut_158': x_transform__mutmut_158, 
    'x_transform__mutmut_159': x_transform__mutmut_159, 
    'x_transform__mutmut_160': x_transform__mutmut_160, 
    'x_transform__mutmut_161': x_transform__mutmut_161, 
    'x_transform__mutmut_162': x_transform__mutmut_162, 
    'x_transform__mutmut_163': x_transform__mutmut_163, 
    'x_transform__mutmut_164': x_transform__mutmut_164, 
    'x_transform__mutmut_165': x_transform__mutmut_165, 
    'x_transform__mutmut_166': x_transform__mutmut_166, 
    'x_transform__mutmut_167': x_transform__mutmut_167, 
    'x_transform__mutmut_168': x_transform__mutmut_168, 
    'x_transform__mutmut_169': x_transform__mutmut_169, 
    'x_transform__mutmut_170': x_transform__mutmut_170, 
    'x_transform__mutmut_171': x_transform__mutmut_171, 
    'x_transform__mutmut_172': x_transform__mutmut_172, 
    'x_transform__mutmut_173': x_transform__mutmut_173, 
    'x_transform__mutmut_174': x_transform__mutmut_174, 
    'x_transform__mutmut_175': x_transform__mutmut_175, 
    'x_transform__mutmut_176': x_transform__mutmut_176, 
    'x_transform__mutmut_177': x_transform__mutmut_177, 
    'x_transform__mutmut_178': x_transform__mutmut_178, 
    'x_transform__mutmut_179': x_transform__mutmut_179, 
    'x_transform__mutmut_180': x_transform__mutmut_180, 
    'x_transform__mutmut_181': x_transform__mutmut_181, 
    'x_transform__mutmut_182': x_transform__mutmut_182, 
    'x_transform__mutmut_183': x_transform__mutmut_183, 
    'x_transform__mutmut_184': x_transform__mutmut_184, 
    'x_transform__mutmut_185': x_transform__mutmut_185, 
    'x_transform__mutmut_186': x_transform__mutmut_186, 
    'x_transform__mutmut_187': x_transform__mutmut_187, 
    'x_transform__mutmut_188': x_transform__mutmut_188, 
    'x_transform__mutmut_189': x_transform__mutmut_189, 
    'x_transform__mutmut_190': x_transform__mutmut_190, 
    'x_transform__mutmut_191': x_transform__mutmut_191, 
    'x_transform__mutmut_192': x_transform__mutmut_192, 
    'x_transform__mutmut_193': x_transform__mutmut_193, 
    'x_transform__mutmut_194': x_transform__mutmut_194, 
    'x_transform__mutmut_195': x_transform__mutmut_195, 
    'x_transform__mutmut_196': x_transform__mutmut_196, 
    'x_transform__mutmut_197': x_transform__mutmut_197, 
    'x_transform__mutmut_198': x_transform__mutmut_198, 
    'x_transform__mutmut_199': x_transform__mutmut_199, 
    'x_transform__mutmut_200': x_transform__mutmut_200, 
    'x_transform__mutmut_201': x_transform__mutmut_201, 
    'x_transform__mutmut_202': x_transform__mutmut_202, 
    'x_transform__mutmut_203': x_transform__mutmut_203, 
    'x_transform__mutmut_204': x_transform__mutmut_204, 
    'x_transform__mutmut_205': x_transform__mutmut_205, 
    'x_transform__mutmut_206': x_transform__mutmut_206, 
    'x_transform__mutmut_207': x_transform__mutmut_207, 
    'x_transform__mutmut_208': x_transform__mutmut_208, 
    'x_transform__mutmut_209': x_transform__mutmut_209, 
    'x_transform__mutmut_210': x_transform__mutmut_210, 
    'x_transform__mutmut_211': x_transform__mutmut_211, 
    'x_transform__mutmut_212': x_transform__mutmut_212, 
    'x_transform__mutmut_213': x_transform__mutmut_213, 
    'x_transform__mutmut_214': x_transform__mutmut_214, 
    'x_transform__mutmut_215': x_transform__mutmut_215, 
    'x_transform__mutmut_216': x_transform__mutmut_216, 
    'x_transform__mutmut_217': x_transform__mutmut_217, 
    'x_transform__mutmut_218': x_transform__mutmut_218, 
    'x_transform__mutmut_219': x_transform__mutmut_219, 
    'x_transform__mutmut_220': x_transform__mutmut_220, 
    'x_transform__mutmut_221': x_transform__mutmut_221, 
    'x_transform__mutmut_222': x_transform__mutmut_222, 
    'x_transform__mutmut_223': x_transform__mutmut_223, 
    'x_transform__mutmut_224': x_transform__mutmut_224, 
    'x_transform__mutmut_225': x_transform__mutmut_225, 
    'x_transform__mutmut_226': x_transform__mutmut_226, 
    'x_transform__mutmut_227': x_transform__mutmut_227, 
    'x_transform__mutmut_228': x_transform__mutmut_228, 
    'x_transform__mutmut_229': x_transform__mutmut_229, 
    'x_transform__mutmut_230': x_transform__mutmut_230, 
    'x_transform__mutmut_231': x_transform__mutmut_231, 
    'x_transform__mutmut_232': x_transform__mutmut_232, 
    'x_transform__mutmut_233': x_transform__mutmut_233, 
    'x_transform__mutmut_234': x_transform__mutmut_234, 
    'x_transform__mutmut_235': x_transform__mutmut_235, 
    'x_transform__mutmut_236': x_transform__mutmut_236, 
    'x_transform__mutmut_237': x_transform__mutmut_237, 
    'x_transform__mutmut_238': x_transform__mutmut_238, 
    'x_transform__mutmut_239': x_transform__mutmut_239
}

def transform(*args, **kwargs):
    result = _mutmut_trampoline(x_transform__mutmut_orig, x_transform__mutmut_mutants, args, kwargs)
    return result 

transform.__signature__ = _mutmut_signature(x_transform__mutmut_orig)
x_transform__mutmut_orig.__name__ = 'x_transform'
