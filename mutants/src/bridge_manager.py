"""
Secure Bridge Manager for Cognitive-Copilot Communication

Replaces the fragile file-based IPC at temp/bridge_codex_copilot_bridge
with a secure Named Pipe (FIFO) or Unix domain socket implementation.

Part of Phase 2: Fragile Bridge Elimination
Part of PS-02: IPC Bridge Hardening (Authentication & Audit Trail)

Enhanced with Bridge Protocol v2 (2026-01-09):
- Message compression for large payloads
- Multi-client support
- Protocol headers with integrity verification
"""
from __future__ import annotations

import os
import fcntl
import json
import logging
import socket
import ssl
import tempfile
import secrets
from pathlib import Path
from typing import Any, Dict, Optional
from datetime import datetime, UTC
from dataclasses import dataclass, asdict
from contextlib import contextmanager
from enum import Enum

# Import Bridge Protocol v2 for enhanced features
try:
    from bridge_protocol_v2 import (
        encode_message as v2_encode,
        decode_message as v2_decode,
        MultiClientBridge,
        MAGIC_BYTES,  # Import magic bytes constant for consistency
    )
    HAS_PROTOCOL_V2 = True
except ImportError:
    HAS_PROTOCOL_V2 = False
    # Define fallback to avoid undefined variable errors
    MAGIC_BYTES = b"CBv2"  # Match protocol v2 magic bytes

# Import TLS configuration for distributed bridge
try:
    from security.tls_config import (
        create_server_context,
        create_client_context,
    )
    HAS_TLS_SUPPORT = True
except ImportError:
    HAS_TLS_SUPPORT = False

logger = logging.getLogger(__name__)
from inspect import signature as _mutmut_signature
from typing import Annotated
from typing import Callable
from typing import ClassVar


MutantDict = Annotated[dict[str, Callable], "Mutant"]


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg = None):
    """Forward call to original or mutated function, depending on the environment"""
    import os
    mutant_under_test = os.environ['MUTANT_UNDER_TEST']
    if mutant_under_test == 'fail':
        from mutmut.__main__ import MutmutProgrammaticFailException
        raise MutmutProgrammaticFailException('Failed programmatically')      
    elif mutant_under_test == 'stats':
        from mutmut.__main__ import record_trampoline_hit
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__)
        result = orig(*call_args, **call_kwargs)
        return result
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_'
    if not mutant_under_test.startswith(prefix):
        result = orig(*call_args, **call_kwargs)
        return result
    mutant_name = mutant_under_test.rpartition('.')[-1]
    if self_arg is not None:
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs)
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs)
    return result


class BridgeMode(Enum):
    """Bridge communication mode."""
    NAMED_PIPE = "named_pipe"  # Unix FIFO
    UNIX_SOCKET = "unix_socket"  # Unix domain socket
    TCP_TLS = "tcp_tls"  # TCP with TLS encryption (distributed)
    

@dataclass
class ContextMessage:
    """
    Typed message format for bridge communication.
    
    All messages sent through the bridge must follow this structure
    for type safety and validation.
    """
    timestamp: str  # ISO 8601 format
    source: str  # "cognitive_brain" or "copilot"
    message_type: str  # "context_update", "query", "response", etc.
    context: Dict[str, Any]
    metadata: Optional[Dict[str, Any]] = None
    auth_token: Optional[str] = None  # Authentication token (PS-02)
    
    def to_json(self) -> str:
        """Serialize to JSON string."""
        return json.dumps(asdict(self), indent=2)
    
    @classmethod
    def from_json(cls, json_str: str) -> ContextMessage:
        """Deserialize from JSON string."""
        data = json.loads(json_str)
        return cls(**data)
    
    def validate(self) -> bool:
        """Validate message structure."""
        required_fields = ["timestamp", "source", "message_type", "context"]
        return all(hasattr(self, field) for field in required_fields)


class BridgeLock:
    """
    File-based locking mechanism using fcntl.
    
    Prevents race conditions when multiple processes access the bridge.
    """
    
    def xǁBridgeLockǁ__init____mutmut_orig(self, lock_path: Path):
        """Initialize lock with path to lock file."""
        self.lock_path = lock_path
        self.lock_fd: Optional[int] = None
    
    def xǁBridgeLockǁ__init____mutmut_1(self, lock_path: Path):
        """Initialize lock with path to lock file."""
        self.lock_path = None
        self.lock_fd: Optional[int] = None
    
    def xǁBridgeLockǁ__init____mutmut_2(self, lock_path: Path):
        """Initialize lock with path to lock file."""
        self.lock_path = lock_path
        self.lock_fd: Optional[int] = ""
    
    xǁBridgeLockǁ__init____mutmut_mutants : ClassVar[MutantDict] = {
    'xǁBridgeLockǁ__init____mutmut_1': xǁBridgeLockǁ__init____mutmut_1, 
        'xǁBridgeLockǁ__init____mutmut_2': xǁBridgeLockǁ__init____mutmut_2
    }
    
    def __init__(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁBridgeLockǁ__init____mutmut_orig"), object.__getattribute__(self, "xǁBridgeLockǁ__init____mutmut_mutants"), args, kwargs, self)
        return result 
    
    __init__.__signature__ = _mutmut_signature(xǁBridgeLockǁ__init____mutmut_orig)
    xǁBridgeLockǁ__init____mutmut_orig.__name__ = 'xǁBridgeLockǁ__init__'
    
    def xǁBridgeLockǁacquire__mutmut_orig(self, timeout: int = 5) -> bool:
        """
        Acquire exclusive lock.
        
        Args:
            timeout: Maximum seconds to wait for lock
            
        Returns:
            True if lock acquired, False on timeout
        """
        try:
            # Ensure lock file exists
            self.lock_path.parent.mkdir(parents=True, exist_ok=True)
            self.lock_path.touch(exist_ok=True)
            
            # Open lock file
            self.lock_fd = os.open(str(self.lock_path), os.O_RDWR | os.O_CREAT, 0o600)
            
            # Try to acquire lock with timeout
            fcntl.flock(self.lock_fd, fcntl.LOCK_EX | fcntl.LOCK_NB)
            logger.debug(f"Lock acquired: {self.lock_path}")
            return True
            
        except BlockingIOError:
            logger.warning(f"Failed to acquire lock (timeout): {self.lock_path}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
        except Exception as e:
            logger.error(f"Lock acquisition error: {e}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
    
    def xǁBridgeLockǁacquire__mutmut_1(self, timeout: int = 6) -> bool:
        """
        Acquire exclusive lock.
        
        Args:
            timeout: Maximum seconds to wait for lock
            
        Returns:
            True if lock acquired, False on timeout
        """
        try:
            # Ensure lock file exists
            self.lock_path.parent.mkdir(parents=True, exist_ok=True)
            self.lock_path.touch(exist_ok=True)
            
            # Open lock file
            self.lock_fd = os.open(str(self.lock_path), os.O_RDWR | os.O_CREAT, 0o600)
            
            # Try to acquire lock with timeout
            fcntl.flock(self.lock_fd, fcntl.LOCK_EX | fcntl.LOCK_NB)
            logger.debug(f"Lock acquired: {self.lock_path}")
            return True
            
        except BlockingIOError:
            logger.warning(f"Failed to acquire lock (timeout): {self.lock_path}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
        except Exception as e:
            logger.error(f"Lock acquisition error: {e}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
    
    def xǁBridgeLockǁacquire__mutmut_2(self, timeout: int = 5) -> bool:
        """
        Acquire exclusive lock.
        
        Args:
            timeout: Maximum seconds to wait for lock
            
        Returns:
            True if lock acquired, False on timeout
        """
        try:
            # Ensure lock file exists
            self.lock_path.parent.mkdir(parents=None, exist_ok=True)
            self.lock_path.touch(exist_ok=True)
            
            # Open lock file
            self.lock_fd = os.open(str(self.lock_path), os.O_RDWR | os.O_CREAT, 0o600)
            
            # Try to acquire lock with timeout
            fcntl.flock(self.lock_fd, fcntl.LOCK_EX | fcntl.LOCK_NB)
            logger.debug(f"Lock acquired: {self.lock_path}")
            return True
            
        except BlockingIOError:
            logger.warning(f"Failed to acquire lock (timeout): {self.lock_path}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
        except Exception as e:
            logger.error(f"Lock acquisition error: {e}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
    
    def xǁBridgeLockǁacquire__mutmut_3(self, timeout: int = 5) -> bool:
        """
        Acquire exclusive lock.
        
        Args:
            timeout: Maximum seconds to wait for lock
            
        Returns:
            True if lock acquired, False on timeout
        """
        try:
            # Ensure lock file exists
            self.lock_path.parent.mkdir(parents=True, exist_ok=None)
            self.lock_path.touch(exist_ok=True)
            
            # Open lock file
            self.lock_fd = os.open(str(self.lock_path), os.O_RDWR | os.O_CREAT, 0o600)
            
            # Try to acquire lock with timeout
            fcntl.flock(self.lock_fd, fcntl.LOCK_EX | fcntl.LOCK_NB)
            logger.debug(f"Lock acquired: {self.lock_path}")
            return True
            
        except BlockingIOError:
            logger.warning(f"Failed to acquire lock (timeout): {self.lock_path}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
        except Exception as e:
            logger.error(f"Lock acquisition error: {e}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
    
    def xǁBridgeLockǁacquire__mutmut_4(self, timeout: int = 5) -> bool:
        """
        Acquire exclusive lock.
        
        Args:
            timeout: Maximum seconds to wait for lock
            
        Returns:
            True if lock acquired, False on timeout
        """
        try:
            # Ensure lock file exists
            self.lock_path.parent.mkdir(exist_ok=True)
            self.lock_path.touch(exist_ok=True)
            
            # Open lock file
            self.lock_fd = os.open(str(self.lock_path), os.O_RDWR | os.O_CREAT, 0o600)
            
            # Try to acquire lock with timeout
            fcntl.flock(self.lock_fd, fcntl.LOCK_EX | fcntl.LOCK_NB)
            logger.debug(f"Lock acquired: {self.lock_path}")
            return True
            
        except BlockingIOError:
            logger.warning(f"Failed to acquire lock (timeout): {self.lock_path}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
        except Exception as e:
            logger.error(f"Lock acquisition error: {e}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
    
    def xǁBridgeLockǁacquire__mutmut_5(self, timeout: int = 5) -> bool:
        """
        Acquire exclusive lock.
        
        Args:
            timeout: Maximum seconds to wait for lock
            
        Returns:
            True if lock acquired, False on timeout
        """
        try:
            # Ensure lock file exists
            self.lock_path.parent.mkdir(parents=True, )
            self.lock_path.touch(exist_ok=True)
            
            # Open lock file
            self.lock_fd = os.open(str(self.lock_path), os.O_RDWR | os.O_CREAT, 0o600)
            
            # Try to acquire lock with timeout
            fcntl.flock(self.lock_fd, fcntl.LOCK_EX | fcntl.LOCK_NB)
            logger.debug(f"Lock acquired: {self.lock_path}")
            return True
            
        except BlockingIOError:
            logger.warning(f"Failed to acquire lock (timeout): {self.lock_path}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
        except Exception as e:
            logger.error(f"Lock acquisition error: {e}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
    
    def xǁBridgeLockǁacquire__mutmut_6(self, timeout: int = 5) -> bool:
        """
        Acquire exclusive lock.
        
        Args:
            timeout: Maximum seconds to wait for lock
            
        Returns:
            True if lock acquired, False on timeout
        """
        try:
            # Ensure lock file exists
            self.lock_path.parent.mkdir(parents=False, exist_ok=True)
            self.lock_path.touch(exist_ok=True)
            
            # Open lock file
            self.lock_fd = os.open(str(self.lock_path), os.O_RDWR | os.O_CREAT, 0o600)
            
            # Try to acquire lock with timeout
            fcntl.flock(self.lock_fd, fcntl.LOCK_EX | fcntl.LOCK_NB)
            logger.debug(f"Lock acquired: {self.lock_path}")
            return True
            
        except BlockingIOError:
            logger.warning(f"Failed to acquire lock (timeout): {self.lock_path}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
        except Exception as e:
            logger.error(f"Lock acquisition error: {e}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
    
    def xǁBridgeLockǁacquire__mutmut_7(self, timeout: int = 5) -> bool:
        """
        Acquire exclusive lock.
        
        Args:
            timeout: Maximum seconds to wait for lock
            
        Returns:
            True if lock acquired, False on timeout
        """
        try:
            # Ensure lock file exists
            self.lock_path.parent.mkdir(parents=True, exist_ok=False)
            self.lock_path.touch(exist_ok=True)
            
            # Open lock file
            self.lock_fd = os.open(str(self.lock_path), os.O_RDWR | os.O_CREAT, 0o600)
            
            # Try to acquire lock with timeout
            fcntl.flock(self.lock_fd, fcntl.LOCK_EX | fcntl.LOCK_NB)
            logger.debug(f"Lock acquired: {self.lock_path}")
            return True
            
        except BlockingIOError:
            logger.warning(f"Failed to acquire lock (timeout): {self.lock_path}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
        except Exception as e:
            logger.error(f"Lock acquisition error: {e}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
    
    def xǁBridgeLockǁacquire__mutmut_8(self, timeout: int = 5) -> bool:
        """
        Acquire exclusive lock.
        
        Args:
            timeout: Maximum seconds to wait for lock
            
        Returns:
            True if lock acquired, False on timeout
        """
        try:
            # Ensure lock file exists
            self.lock_path.parent.mkdir(parents=True, exist_ok=True)
            self.lock_path.touch(exist_ok=None)
            
            # Open lock file
            self.lock_fd = os.open(str(self.lock_path), os.O_RDWR | os.O_CREAT, 0o600)
            
            # Try to acquire lock with timeout
            fcntl.flock(self.lock_fd, fcntl.LOCK_EX | fcntl.LOCK_NB)
            logger.debug(f"Lock acquired: {self.lock_path}")
            return True
            
        except BlockingIOError:
            logger.warning(f"Failed to acquire lock (timeout): {self.lock_path}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
        except Exception as e:
            logger.error(f"Lock acquisition error: {e}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
    
    def xǁBridgeLockǁacquire__mutmut_9(self, timeout: int = 5) -> bool:
        """
        Acquire exclusive lock.
        
        Args:
            timeout: Maximum seconds to wait for lock
            
        Returns:
            True if lock acquired, False on timeout
        """
        try:
            # Ensure lock file exists
            self.lock_path.parent.mkdir(parents=True, exist_ok=True)
            self.lock_path.touch(exist_ok=False)
            
            # Open lock file
            self.lock_fd = os.open(str(self.lock_path), os.O_RDWR | os.O_CREAT, 0o600)
            
            # Try to acquire lock with timeout
            fcntl.flock(self.lock_fd, fcntl.LOCK_EX | fcntl.LOCK_NB)
            logger.debug(f"Lock acquired: {self.lock_path}")
            return True
            
        except BlockingIOError:
            logger.warning(f"Failed to acquire lock (timeout): {self.lock_path}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
        except Exception as e:
            logger.error(f"Lock acquisition error: {e}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
    
    def xǁBridgeLockǁacquire__mutmut_10(self, timeout: int = 5) -> bool:
        """
        Acquire exclusive lock.
        
        Args:
            timeout: Maximum seconds to wait for lock
            
        Returns:
            True if lock acquired, False on timeout
        """
        try:
            # Ensure lock file exists
            self.lock_path.parent.mkdir(parents=True, exist_ok=True)
            self.lock_path.touch(exist_ok=True)
            
            # Open lock file
            self.lock_fd = None
            
            # Try to acquire lock with timeout
            fcntl.flock(self.lock_fd, fcntl.LOCK_EX | fcntl.LOCK_NB)
            logger.debug(f"Lock acquired: {self.lock_path}")
            return True
            
        except BlockingIOError:
            logger.warning(f"Failed to acquire lock (timeout): {self.lock_path}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
        except Exception as e:
            logger.error(f"Lock acquisition error: {e}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
    
    def xǁBridgeLockǁacquire__mutmut_11(self, timeout: int = 5) -> bool:
        """
        Acquire exclusive lock.
        
        Args:
            timeout: Maximum seconds to wait for lock
            
        Returns:
            True if lock acquired, False on timeout
        """
        try:
            # Ensure lock file exists
            self.lock_path.parent.mkdir(parents=True, exist_ok=True)
            self.lock_path.touch(exist_ok=True)
            
            # Open lock file
            self.lock_fd = os.open(None, os.O_RDWR | os.O_CREAT, 0o600)
            
            # Try to acquire lock with timeout
            fcntl.flock(self.lock_fd, fcntl.LOCK_EX | fcntl.LOCK_NB)
            logger.debug(f"Lock acquired: {self.lock_path}")
            return True
            
        except BlockingIOError:
            logger.warning(f"Failed to acquire lock (timeout): {self.lock_path}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
        except Exception as e:
            logger.error(f"Lock acquisition error: {e}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
    
    def xǁBridgeLockǁacquire__mutmut_12(self, timeout: int = 5) -> bool:
        """
        Acquire exclusive lock.
        
        Args:
            timeout: Maximum seconds to wait for lock
            
        Returns:
            True if lock acquired, False on timeout
        """
        try:
            # Ensure lock file exists
            self.lock_path.parent.mkdir(parents=True, exist_ok=True)
            self.lock_path.touch(exist_ok=True)
            
            # Open lock file
            self.lock_fd = os.open(str(self.lock_path), None, 0o600)
            
            # Try to acquire lock with timeout
            fcntl.flock(self.lock_fd, fcntl.LOCK_EX | fcntl.LOCK_NB)
            logger.debug(f"Lock acquired: {self.lock_path}")
            return True
            
        except BlockingIOError:
            logger.warning(f"Failed to acquire lock (timeout): {self.lock_path}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
        except Exception as e:
            logger.error(f"Lock acquisition error: {e}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
    
    def xǁBridgeLockǁacquire__mutmut_13(self, timeout: int = 5) -> bool:
        """
        Acquire exclusive lock.
        
        Args:
            timeout: Maximum seconds to wait for lock
            
        Returns:
            True if lock acquired, False on timeout
        """
        try:
            # Ensure lock file exists
            self.lock_path.parent.mkdir(parents=True, exist_ok=True)
            self.lock_path.touch(exist_ok=True)
            
            # Open lock file
            self.lock_fd = os.open(str(self.lock_path), os.O_RDWR | os.O_CREAT, None)
            
            # Try to acquire lock with timeout
            fcntl.flock(self.lock_fd, fcntl.LOCK_EX | fcntl.LOCK_NB)
            logger.debug(f"Lock acquired: {self.lock_path}")
            return True
            
        except BlockingIOError:
            logger.warning(f"Failed to acquire lock (timeout): {self.lock_path}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
        except Exception as e:
            logger.error(f"Lock acquisition error: {e}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
    
    def xǁBridgeLockǁacquire__mutmut_14(self, timeout: int = 5) -> bool:
        """
        Acquire exclusive lock.
        
        Args:
            timeout: Maximum seconds to wait for lock
            
        Returns:
            True if lock acquired, False on timeout
        """
        try:
            # Ensure lock file exists
            self.lock_path.parent.mkdir(parents=True, exist_ok=True)
            self.lock_path.touch(exist_ok=True)
            
            # Open lock file
            self.lock_fd = os.open(os.O_RDWR | os.O_CREAT, 0o600)
            
            # Try to acquire lock with timeout
            fcntl.flock(self.lock_fd, fcntl.LOCK_EX | fcntl.LOCK_NB)
            logger.debug(f"Lock acquired: {self.lock_path}")
            return True
            
        except BlockingIOError:
            logger.warning(f"Failed to acquire lock (timeout): {self.lock_path}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
        except Exception as e:
            logger.error(f"Lock acquisition error: {e}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
    
    def xǁBridgeLockǁacquire__mutmut_15(self, timeout: int = 5) -> bool:
        """
        Acquire exclusive lock.
        
        Args:
            timeout: Maximum seconds to wait for lock
            
        Returns:
            True if lock acquired, False on timeout
        """
        try:
            # Ensure lock file exists
            self.lock_path.parent.mkdir(parents=True, exist_ok=True)
            self.lock_path.touch(exist_ok=True)
            
            # Open lock file
            self.lock_fd = os.open(str(self.lock_path), 0o600)
            
            # Try to acquire lock with timeout
            fcntl.flock(self.lock_fd, fcntl.LOCK_EX | fcntl.LOCK_NB)
            logger.debug(f"Lock acquired: {self.lock_path}")
            return True
            
        except BlockingIOError:
            logger.warning(f"Failed to acquire lock (timeout): {self.lock_path}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
        except Exception as e:
            logger.error(f"Lock acquisition error: {e}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
    
    def xǁBridgeLockǁacquire__mutmut_16(self, timeout: int = 5) -> bool:
        """
        Acquire exclusive lock.
        
        Args:
            timeout: Maximum seconds to wait for lock
            
        Returns:
            True if lock acquired, False on timeout
        """
        try:
            # Ensure lock file exists
            self.lock_path.parent.mkdir(parents=True, exist_ok=True)
            self.lock_path.touch(exist_ok=True)
            
            # Open lock file
            self.lock_fd = os.open(str(self.lock_path), os.O_RDWR | os.O_CREAT, )
            
            # Try to acquire lock with timeout
            fcntl.flock(self.lock_fd, fcntl.LOCK_EX | fcntl.LOCK_NB)
            logger.debug(f"Lock acquired: {self.lock_path}")
            return True
            
        except BlockingIOError:
            logger.warning(f"Failed to acquire lock (timeout): {self.lock_path}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
        except Exception as e:
            logger.error(f"Lock acquisition error: {e}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
    
    def xǁBridgeLockǁacquire__mutmut_17(self, timeout: int = 5) -> bool:
        """
        Acquire exclusive lock.
        
        Args:
            timeout: Maximum seconds to wait for lock
            
        Returns:
            True if lock acquired, False on timeout
        """
        try:
            # Ensure lock file exists
            self.lock_path.parent.mkdir(parents=True, exist_ok=True)
            self.lock_path.touch(exist_ok=True)
            
            # Open lock file
            self.lock_fd = os.open(str(None), os.O_RDWR | os.O_CREAT, 0o600)
            
            # Try to acquire lock with timeout
            fcntl.flock(self.lock_fd, fcntl.LOCK_EX | fcntl.LOCK_NB)
            logger.debug(f"Lock acquired: {self.lock_path}")
            return True
            
        except BlockingIOError:
            logger.warning(f"Failed to acquire lock (timeout): {self.lock_path}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
        except Exception as e:
            logger.error(f"Lock acquisition error: {e}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
    
    def xǁBridgeLockǁacquire__mutmut_18(self, timeout: int = 5) -> bool:
        """
        Acquire exclusive lock.
        
        Args:
            timeout: Maximum seconds to wait for lock
            
        Returns:
            True if lock acquired, False on timeout
        """
        try:
            # Ensure lock file exists
            self.lock_path.parent.mkdir(parents=True, exist_ok=True)
            self.lock_path.touch(exist_ok=True)
            
            # Open lock file
            self.lock_fd = os.open(str(self.lock_path), os.O_RDWR & os.O_CREAT, 0o600)
            
            # Try to acquire lock with timeout
            fcntl.flock(self.lock_fd, fcntl.LOCK_EX | fcntl.LOCK_NB)
            logger.debug(f"Lock acquired: {self.lock_path}")
            return True
            
        except BlockingIOError:
            logger.warning(f"Failed to acquire lock (timeout): {self.lock_path}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
        except Exception as e:
            logger.error(f"Lock acquisition error: {e}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
    
    def xǁBridgeLockǁacquire__mutmut_19(self, timeout: int = 5) -> bool:
        """
        Acquire exclusive lock.
        
        Args:
            timeout: Maximum seconds to wait for lock
            
        Returns:
            True if lock acquired, False on timeout
        """
        try:
            # Ensure lock file exists
            self.lock_path.parent.mkdir(parents=True, exist_ok=True)
            self.lock_path.touch(exist_ok=True)
            
            # Open lock file
            self.lock_fd = os.open(str(self.lock_path), os.O_RDWR | os.O_CREAT, 385)
            
            # Try to acquire lock with timeout
            fcntl.flock(self.lock_fd, fcntl.LOCK_EX | fcntl.LOCK_NB)
            logger.debug(f"Lock acquired: {self.lock_path}")
            return True
            
        except BlockingIOError:
            logger.warning(f"Failed to acquire lock (timeout): {self.lock_path}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
        except Exception as e:
            logger.error(f"Lock acquisition error: {e}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
    
    def xǁBridgeLockǁacquire__mutmut_20(self, timeout: int = 5) -> bool:
        """
        Acquire exclusive lock.
        
        Args:
            timeout: Maximum seconds to wait for lock
            
        Returns:
            True if lock acquired, False on timeout
        """
        try:
            # Ensure lock file exists
            self.lock_path.parent.mkdir(parents=True, exist_ok=True)
            self.lock_path.touch(exist_ok=True)
            
            # Open lock file
            self.lock_fd = os.open(str(self.lock_path), os.O_RDWR | os.O_CREAT, 0o600)
            
            # Try to acquire lock with timeout
            fcntl.flock(None, fcntl.LOCK_EX | fcntl.LOCK_NB)
            logger.debug(f"Lock acquired: {self.lock_path}")
            return True
            
        except BlockingIOError:
            logger.warning(f"Failed to acquire lock (timeout): {self.lock_path}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
        except Exception as e:
            logger.error(f"Lock acquisition error: {e}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
    
    def xǁBridgeLockǁacquire__mutmut_21(self, timeout: int = 5) -> bool:
        """
        Acquire exclusive lock.
        
        Args:
            timeout: Maximum seconds to wait for lock
            
        Returns:
            True if lock acquired, False on timeout
        """
        try:
            # Ensure lock file exists
            self.lock_path.parent.mkdir(parents=True, exist_ok=True)
            self.lock_path.touch(exist_ok=True)
            
            # Open lock file
            self.lock_fd = os.open(str(self.lock_path), os.O_RDWR | os.O_CREAT, 0o600)
            
            # Try to acquire lock with timeout
            fcntl.flock(self.lock_fd, None)
            logger.debug(f"Lock acquired: {self.lock_path}")
            return True
            
        except BlockingIOError:
            logger.warning(f"Failed to acquire lock (timeout): {self.lock_path}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
        except Exception as e:
            logger.error(f"Lock acquisition error: {e}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
    
    def xǁBridgeLockǁacquire__mutmut_22(self, timeout: int = 5) -> bool:
        """
        Acquire exclusive lock.
        
        Args:
            timeout: Maximum seconds to wait for lock
            
        Returns:
            True if lock acquired, False on timeout
        """
        try:
            # Ensure lock file exists
            self.lock_path.parent.mkdir(parents=True, exist_ok=True)
            self.lock_path.touch(exist_ok=True)
            
            # Open lock file
            self.lock_fd = os.open(str(self.lock_path), os.O_RDWR | os.O_CREAT, 0o600)
            
            # Try to acquire lock with timeout
            fcntl.flock(fcntl.LOCK_EX | fcntl.LOCK_NB)
            logger.debug(f"Lock acquired: {self.lock_path}")
            return True
            
        except BlockingIOError:
            logger.warning(f"Failed to acquire lock (timeout): {self.lock_path}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
        except Exception as e:
            logger.error(f"Lock acquisition error: {e}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
    
    def xǁBridgeLockǁacquire__mutmut_23(self, timeout: int = 5) -> bool:
        """
        Acquire exclusive lock.
        
        Args:
            timeout: Maximum seconds to wait for lock
            
        Returns:
            True if lock acquired, False on timeout
        """
        try:
            # Ensure lock file exists
            self.lock_path.parent.mkdir(parents=True, exist_ok=True)
            self.lock_path.touch(exist_ok=True)
            
            # Open lock file
            self.lock_fd = os.open(str(self.lock_path), os.O_RDWR | os.O_CREAT, 0o600)
            
            # Try to acquire lock with timeout
            fcntl.flock(self.lock_fd, )
            logger.debug(f"Lock acquired: {self.lock_path}")
            return True
            
        except BlockingIOError:
            logger.warning(f"Failed to acquire lock (timeout): {self.lock_path}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
        except Exception as e:
            logger.error(f"Lock acquisition error: {e}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
    
    def xǁBridgeLockǁacquire__mutmut_24(self, timeout: int = 5) -> bool:
        """
        Acquire exclusive lock.
        
        Args:
            timeout: Maximum seconds to wait for lock
            
        Returns:
            True if lock acquired, False on timeout
        """
        try:
            # Ensure lock file exists
            self.lock_path.parent.mkdir(parents=True, exist_ok=True)
            self.lock_path.touch(exist_ok=True)
            
            # Open lock file
            self.lock_fd = os.open(str(self.lock_path), os.O_RDWR | os.O_CREAT, 0o600)
            
            # Try to acquire lock with timeout
            fcntl.flock(self.lock_fd, fcntl.LOCK_EX & fcntl.LOCK_NB)
            logger.debug(f"Lock acquired: {self.lock_path}")
            return True
            
        except BlockingIOError:
            logger.warning(f"Failed to acquire lock (timeout): {self.lock_path}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
        except Exception as e:
            logger.error(f"Lock acquisition error: {e}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
    
    def xǁBridgeLockǁacquire__mutmut_25(self, timeout: int = 5) -> bool:
        """
        Acquire exclusive lock.
        
        Args:
            timeout: Maximum seconds to wait for lock
            
        Returns:
            True if lock acquired, False on timeout
        """
        try:
            # Ensure lock file exists
            self.lock_path.parent.mkdir(parents=True, exist_ok=True)
            self.lock_path.touch(exist_ok=True)
            
            # Open lock file
            self.lock_fd = os.open(str(self.lock_path), os.O_RDWR | os.O_CREAT, 0o600)
            
            # Try to acquire lock with timeout
            fcntl.flock(self.lock_fd, fcntl.LOCK_EX | fcntl.LOCK_NB)
            logger.debug(None)
            return True
            
        except BlockingIOError:
            logger.warning(f"Failed to acquire lock (timeout): {self.lock_path}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
        except Exception as e:
            logger.error(f"Lock acquisition error: {e}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
    
    def xǁBridgeLockǁacquire__mutmut_26(self, timeout: int = 5) -> bool:
        """
        Acquire exclusive lock.
        
        Args:
            timeout: Maximum seconds to wait for lock
            
        Returns:
            True if lock acquired, False on timeout
        """
        try:
            # Ensure lock file exists
            self.lock_path.parent.mkdir(parents=True, exist_ok=True)
            self.lock_path.touch(exist_ok=True)
            
            # Open lock file
            self.lock_fd = os.open(str(self.lock_path), os.O_RDWR | os.O_CREAT, 0o600)
            
            # Try to acquire lock with timeout
            fcntl.flock(self.lock_fd, fcntl.LOCK_EX | fcntl.LOCK_NB)
            logger.debug(f"Lock acquired: {self.lock_path}")
            return False
            
        except BlockingIOError:
            logger.warning(f"Failed to acquire lock (timeout): {self.lock_path}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
        except Exception as e:
            logger.error(f"Lock acquisition error: {e}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
    
    def xǁBridgeLockǁacquire__mutmut_27(self, timeout: int = 5) -> bool:
        """
        Acquire exclusive lock.
        
        Args:
            timeout: Maximum seconds to wait for lock
            
        Returns:
            True if lock acquired, False on timeout
        """
        try:
            # Ensure lock file exists
            self.lock_path.parent.mkdir(parents=True, exist_ok=True)
            self.lock_path.touch(exist_ok=True)
            
            # Open lock file
            self.lock_fd = os.open(str(self.lock_path), os.O_RDWR | os.O_CREAT, 0o600)
            
            # Try to acquire lock with timeout
            fcntl.flock(self.lock_fd, fcntl.LOCK_EX | fcntl.LOCK_NB)
            logger.debug(f"Lock acquired: {self.lock_path}")
            return True
            
        except BlockingIOError:
            logger.warning(None)
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
        except Exception as e:
            logger.error(f"Lock acquisition error: {e}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
    
    def xǁBridgeLockǁacquire__mutmut_28(self, timeout: int = 5) -> bool:
        """
        Acquire exclusive lock.
        
        Args:
            timeout: Maximum seconds to wait for lock
            
        Returns:
            True if lock acquired, False on timeout
        """
        try:
            # Ensure lock file exists
            self.lock_path.parent.mkdir(parents=True, exist_ok=True)
            self.lock_path.touch(exist_ok=True)
            
            # Open lock file
            self.lock_fd = os.open(str(self.lock_path), os.O_RDWR | os.O_CREAT, 0o600)
            
            # Try to acquire lock with timeout
            fcntl.flock(self.lock_fd, fcntl.LOCK_EX | fcntl.LOCK_NB)
            logger.debug(f"Lock acquired: {self.lock_path}")
            return True
            
        except BlockingIOError:
            logger.warning(f"Failed to acquire lock (timeout): {self.lock_path}")
            if self.lock_fd:
                os.close(None)
                self.lock_fd = None
            return False
        except Exception as e:
            logger.error(f"Lock acquisition error: {e}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
    
    def xǁBridgeLockǁacquire__mutmut_29(self, timeout: int = 5) -> bool:
        """
        Acquire exclusive lock.
        
        Args:
            timeout: Maximum seconds to wait for lock
            
        Returns:
            True if lock acquired, False on timeout
        """
        try:
            # Ensure lock file exists
            self.lock_path.parent.mkdir(parents=True, exist_ok=True)
            self.lock_path.touch(exist_ok=True)
            
            # Open lock file
            self.lock_fd = os.open(str(self.lock_path), os.O_RDWR | os.O_CREAT, 0o600)
            
            # Try to acquire lock with timeout
            fcntl.flock(self.lock_fd, fcntl.LOCK_EX | fcntl.LOCK_NB)
            logger.debug(f"Lock acquired: {self.lock_path}")
            return True
            
        except BlockingIOError:
            logger.warning(f"Failed to acquire lock (timeout): {self.lock_path}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = ""
            return False
        except Exception as e:
            logger.error(f"Lock acquisition error: {e}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
    
    def xǁBridgeLockǁacquire__mutmut_30(self, timeout: int = 5) -> bool:
        """
        Acquire exclusive lock.
        
        Args:
            timeout: Maximum seconds to wait for lock
            
        Returns:
            True if lock acquired, False on timeout
        """
        try:
            # Ensure lock file exists
            self.lock_path.parent.mkdir(parents=True, exist_ok=True)
            self.lock_path.touch(exist_ok=True)
            
            # Open lock file
            self.lock_fd = os.open(str(self.lock_path), os.O_RDWR | os.O_CREAT, 0o600)
            
            # Try to acquire lock with timeout
            fcntl.flock(self.lock_fd, fcntl.LOCK_EX | fcntl.LOCK_NB)
            logger.debug(f"Lock acquired: {self.lock_path}")
            return True
            
        except BlockingIOError:
            logger.warning(f"Failed to acquire lock (timeout): {self.lock_path}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return True
        except Exception as e:
            logger.error(f"Lock acquisition error: {e}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
    
    def xǁBridgeLockǁacquire__mutmut_31(self, timeout: int = 5) -> bool:
        """
        Acquire exclusive lock.
        
        Args:
            timeout: Maximum seconds to wait for lock
            
        Returns:
            True if lock acquired, False on timeout
        """
        try:
            # Ensure lock file exists
            self.lock_path.parent.mkdir(parents=True, exist_ok=True)
            self.lock_path.touch(exist_ok=True)
            
            # Open lock file
            self.lock_fd = os.open(str(self.lock_path), os.O_RDWR | os.O_CREAT, 0o600)
            
            # Try to acquire lock with timeout
            fcntl.flock(self.lock_fd, fcntl.LOCK_EX | fcntl.LOCK_NB)
            logger.debug(f"Lock acquired: {self.lock_path}")
            return True
            
        except BlockingIOError:
            logger.warning(f"Failed to acquire lock (timeout): {self.lock_path}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
        except Exception as e:
            logger.error(None)
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
    
    def xǁBridgeLockǁacquire__mutmut_32(self, timeout: int = 5) -> bool:
        """
        Acquire exclusive lock.
        
        Args:
            timeout: Maximum seconds to wait for lock
            
        Returns:
            True if lock acquired, False on timeout
        """
        try:
            # Ensure lock file exists
            self.lock_path.parent.mkdir(parents=True, exist_ok=True)
            self.lock_path.touch(exist_ok=True)
            
            # Open lock file
            self.lock_fd = os.open(str(self.lock_path), os.O_RDWR | os.O_CREAT, 0o600)
            
            # Try to acquire lock with timeout
            fcntl.flock(self.lock_fd, fcntl.LOCK_EX | fcntl.LOCK_NB)
            logger.debug(f"Lock acquired: {self.lock_path}")
            return True
            
        except BlockingIOError:
            logger.warning(f"Failed to acquire lock (timeout): {self.lock_path}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
        except Exception as e:
            logger.error(f"Lock acquisition error: {e}")
            if self.lock_fd:
                os.close(None)
                self.lock_fd = None
            return False
    
    def xǁBridgeLockǁacquire__mutmut_33(self, timeout: int = 5) -> bool:
        """
        Acquire exclusive lock.
        
        Args:
            timeout: Maximum seconds to wait for lock
            
        Returns:
            True if lock acquired, False on timeout
        """
        try:
            # Ensure lock file exists
            self.lock_path.parent.mkdir(parents=True, exist_ok=True)
            self.lock_path.touch(exist_ok=True)
            
            # Open lock file
            self.lock_fd = os.open(str(self.lock_path), os.O_RDWR | os.O_CREAT, 0o600)
            
            # Try to acquire lock with timeout
            fcntl.flock(self.lock_fd, fcntl.LOCK_EX | fcntl.LOCK_NB)
            logger.debug(f"Lock acquired: {self.lock_path}")
            return True
            
        except BlockingIOError:
            logger.warning(f"Failed to acquire lock (timeout): {self.lock_path}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
        except Exception as e:
            logger.error(f"Lock acquisition error: {e}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = ""
            return False
    
    def xǁBridgeLockǁacquire__mutmut_34(self, timeout: int = 5) -> bool:
        """
        Acquire exclusive lock.
        
        Args:
            timeout: Maximum seconds to wait for lock
            
        Returns:
            True if lock acquired, False on timeout
        """
        try:
            # Ensure lock file exists
            self.lock_path.parent.mkdir(parents=True, exist_ok=True)
            self.lock_path.touch(exist_ok=True)
            
            # Open lock file
            self.lock_fd = os.open(str(self.lock_path), os.O_RDWR | os.O_CREAT, 0o600)
            
            # Try to acquire lock with timeout
            fcntl.flock(self.lock_fd, fcntl.LOCK_EX | fcntl.LOCK_NB)
            logger.debug(f"Lock acquired: {self.lock_path}")
            return True
            
        except BlockingIOError:
            logger.warning(f"Failed to acquire lock (timeout): {self.lock_path}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return False
        except Exception as e:
            logger.error(f"Lock acquisition error: {e}")
            if self.lock_fd:
                os.close(self.lock_fd)
                self.lock_fd = None
            return True
    
    xǁBridgeLockǁacquire__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁBridgeLockǁacquire__mutmut_1': xǁBridgeLockǁacquire__mutmut_1, 
        'xǁBridgeLockǁacquire__mutmut_2': xǁBridgeLockǁacquire__mutmut_2, 
        'xǁBridgeLockǁacquire__mutmut_3': xǁBridgeLockǁacquire__mutmut_3, 
        'xǁBridgeLockǁacquire__mutmut_4': xǁBridgeLockǁacquire__mutmut_4, 
        'xǁBridgeLockǁacquire__mutmut_5': xǁBridgeLockǁacquire__mutmut_5, 
        'xǁBridgeLockǁacquire__mutmut_6': xǁBridgeLockǁacquire__mutmut_6, 
        'xǁBridgeLockǁacquire__mutmut_7': xǁBridgeLockǁacquire__mutmut_7, 
        'xǁBridgeLockǁacquire__mutmut_8': xǁBridgeLockǁacquire__mutmut_8, 
        'xǁBridgeLockǁacquire__mutmut_9': xǁBridgeLockǁacquire__mutmut_9, 
        'xǁBridgeLockǁacquire__mutmut_10': xǁBridgeLockǁacquire__mutmut_10, 
        'xǁBridgeLockǁacquire__mutmut_11': xǁBridgeLockǁacquire__mutmut_11, 
        'xǁBridgeLockǁacquire__mutmut_12': xǁBridgeLockǁacquire__mutmut_12, 
        'xǁBridgeLockǁacquire__mutmut_13': xǁBridgeLockǁacquire__mutmut_13, 
        'xǁBridgeLockǁacquire__mutmut_14': xǁBridgeLockǁacquire__mutmut_14, 
        'xǁBridgeLockǁacquire__mutmut_15': xǁBridgeLockǁacquire__mutmut_15, 
        'xǁBridgeLockǁacquire__mutmut_16': xǁBridgeLockǁacquire__mutmut_16, 
        'xǁBridgeLockǁacquire__mutmut_17': xǁBridgeLockǁacquire__mutmut_17, 
        'xǁBridgeLockǁacquire__mutmut_18': xǁBridgeLockǁacquire__mutmut_18, 
        'xǁBridgeLockǁacquire__mutmut_19': xǁBridgeLockǁacquire__mutmut_19, 
        'xǁBridgeLockǁacquire__mutmut_20': xǁBridgeLockǁacquire__mutmut_20, 
        'xǁBridgeLockǁacquire__mutmut_21': xǁBridgeLockǁacquire__mutmut_21, 
        'xǁBridgeLockǁacquire__mutmut_22': xǁBridgeLockǁacquire__mutmut_22, 
        'xǁBridgeLockǁacquire__mutmut_23': xǁBridgeLockǁacquire__mutmut_23, 
        'xǁBridgeLockǁacquire__mutmut_24': xǁBridgeLockǁacquire__mutmut_24, 
        'xǁBridgeLockǁacquire__mutmut_25': xǁBridgeLockǁacquire__mutmut_25, 
        'xǁBridgeLockǁacquire__mutmut_26': xǁBridgeLockǁacquire__mutmut_26, 
        'xǁBridgeLockǁacquire__mutmut_27': xǁBridgeLockǁacquire__mutmut_27, 
        'xǁBridgeLockǁacquire__mutmut_28': xǁBridgeLockǁacquire__mutmut_28, 
        'xǁBridgeLockǁacquire__mutmut_29': xǁBridgeLockǁacquire__mutmut_29, 
        'xǁBridgeLockǁacquire__mutmut_30': xǁBridgeLockǁacquire__mutmut_30, 
        'xǁBridgeLockǁacquire__mutmut_31': xǁBridgeLockǁacquire__mutmut_31, 
        'xǁBridgeLockǁacquire__mutmut_32': xǁBridgeLockǁacquire__mutmut_32, 
        'xǁBridgeLockǁacquire__mutmut_33': xǁBridgeLockǁacquire__mutmut_33, 
        'xǁBridgeLockǁacquire__mutmut_34': xǁBridgeLockǁacquire__mutmut_34
    }
    
    def acquire(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁBridgeLockǁacquire__mutmut_orig"), object.__getattribute__(self, "xǁBridgeLockǁacquire__mutmut_mutants"), args, kwargs, self)
        return result 
    
    acquire.__signature__ = _mutmut_signature(xǁBridgeLockǁacquire__mutmut_orig)
    xǁBridgeLockǁacquire__mutmut_orig.__name__ = 'xǁBridgeLockǁacquire'
    
    def xǁBridgeLockǁrelease__mutmut_orig(self) -> None:
        """Release the lock."""
        if self.lock_fd is not None:
            try:
                fcntl.flock(self.lock_fd, fcntl.LOCK_UN)
                os.close(self.lock_fd)
                logger.debug(f"Lock released: {self.lock_path}")
            except Exception as e:
                logger.error(f"Lock release error: {e}")
            finally:
                self.lock_fd = None
    
    def xǁBridgeLockǁrelease__mutmut_1(self) -> None:
        """Release the lock."""
        if self.lock_fd is None:
            try:
                fcntl.flock(self.lock_fd, fcntl.LOCK_UN)
                os.close(self.lock_fd)
                logger.debug(f"Lock released: {self.lock_path}")
            except Exception as e:
                logger.error(f"Lock release error: {e}")
            finally:
                self.lock_fd = None
    
    def xǁBridgeLockǁrelease__mutmut_2(self) -> None:
        """Release the lock."""
        if self.lock_fd is not None:
            try:
                fcntl.flock(None, fcntl.LOCK_UN)
                os.close(self.lock_fd)
                logger.debug(f"Lock released: {self.lock_path}")
            except Exception as e:
                logger.error(f"Lock release error: {e}")
            finally:
                self.lock_fd = None
    
    def xǁBridgeLockǁrelease__mutmut_3(self) -> None:
        """Release the lock."""
        if self.lock_fd is not None:
            try:
                fcntl.flock(self.lock_fd, None)
                os.close(self.lock_fd)
                logger.debug(f"Lock released: {self.lock_path}")
            except Exception as e:
                logger.error(f"Lock release error: {e}")
            finally:
                self.lock_fd = None
    
    def xǁBridgeLockǁrelease__mutmut_4(self) -> None:
        """Release the lock."""
        if self.lock_fd is not None:
            try:
                fcntl.flock(fcntl.LOCK_UN)
                os.close(self.lock_fd)
                logger.debug(f"Lock released: {self.lock_path}")
            except Exception as e:
                logger.error(f"Lock release error: {e}")
            finally:
                self.lock_fd = None
    
    def xǁBridgeLockǁrelease__mutmut_5(self) -> None:
        """Release the lock."""
        if self.lock_fd is not None:
            try:
                fcntl.flock(self.lock_fd, )
                os.close(self.lock_fd)
                logger.debug(f"Lock released: {self.lock_path}")
            except Exception as e:
                logger.error(f"Lock release error: {e}")
            finally:
                self.lock_fd = None
    
    def xǁBridgeLockǁrelease__mutmut_6(self) -> None:
        """Release the lock."""
        if self.lock_fd is not None:
            try:
                fcntl.flock(self.lock_fd, fcntl.LOCK_UN)
                os.close(None)
                logger.debug(f"Lock released: {self.lock_path}")
            except Exception as e:
                logger.error(f"Lock release error: {e}")
            finally:
                self.lock_fd = None
    
    def xǁBridgeLockǁrelease__mutmut_7(self) -> None:
        """Release the lock."""
        if self.lock_fd is not None:
            try:
                fcntl.flock(self.lock_fd, fcntl.LOCK_UN)
                os.close(self.lock_fd)
                logger.debug(None)
            except Exception as e:
                logger.error(f"Lock release error: {e}")
            finally:
                self.lock_fd = None
    
    def xǁBridgeLockǁrelease__mutmut_8(self) -> None:
        """Release the lock."""
        if self.lock_fd is not None:
            try:
                fcntl.flock(self.lock_fd, fcntl.LOCK_UN)
                os.close(self.lock_fd)
                logger.debug(f"Lock released: {self.lock_path}")
            except Exception as e:
                logger.error(None)
            finally:
                self.lock_fd = None
    
    def xǁBridgeLockǁrelease__mutmut_9(self) -> None:
        """Release the lock."""
        if self.lock_fd is not None:
            try:
                fcntl.flock(self.lock_fd, fcntl.LOCK_UN)
                os.close(self.lock_fd)
                logger.debug(f"Lock released: {self.lock_path}")
            except Exception as e:
                logger.error(f"Lock release error: {e}")
            finally:
                self.lock_fd = ""
    
    xǁBridgeLockǁrelease__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁBridgeLockǁrelease__mutmut_1': xǁBridgeLockǁrelease__mutmut_1, 
        'xǁBridgeLockǁrelease__mutmut_2': xǁBridgeLockǁrelease__mutmut_2, 
        'xǁBridgeLockǁrelease__mutmut_3': xǁBridgeLockǁrelease__mutmut_3, 
        'xǁBridgeLockǁrelease__mutmut_4': xǁBridgeLockǁrelease__mutmut_4, 
        'xǁBridgeLockǁrelease__mutmut_5': xǁBridgeLockǁrelease__mutmut_5, 
        'xǁBridgeLockǁrelease__mutmut_6': xǁBridgeLockǁrelease__mutmut_6, 
        'xǁBridgeLockǁrelease__mutmut_7': xǁBridgeLockǁrelease__mutmut_7, 
        'xǁBridgeLockǁrelease__mutmut_8': xǁBridgeLockǁrelease__mutmut_8, 
        'xǁBridgeLockǁrelease__mutmut_9': xǁBridgeLockǁrelease__mutmut_9
    }
    
    def release(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁBridgeLockǁrelease__mutmut_orig"), object.__getattribute__(self, "xǁBridgeLockǁrelease__mutmut_mutants"), args, kwargs, self)
        return result 
    
    release.__signature__ = _mutmut_signature(xǁBridgeLockǁrelease__mutmut_orig)
    xǁBridgeLockǁrelease__mutmut_orig.__name__ = 'xǁBridgeLockǁrelease'


@contextmanager
def bridge_lock(lock_path: Path, timeout: int = 5):
    """
    Context manager for bridge locking.
    
    Usage:
        with bridge_lock(lock_path):
            # Critical section - safe from race conditions
            pass
    """
    lock = BridgeLock(lock_path)
    acquired = lock.acquire(timeout)
    
    if not acquired:
        raise TimeoutError(f"Could not acquire bridge lock: {lock_path}")
    
    try:
        yield lock
    finally:
        lock.release()


class BridgeManager:
    """
    Secure IPC bridge manager with authentication and audit trail.
    
    Replaces temp/bridge_codex_copilot_bridge with secure Named Pipe
    or Unix domain socket with proper permissions, authentication, and logging.
    
    Security Features (PS-02):
    - Named pipes with 0o600 permissions (owner-only)
    - Authentication token validation (CODEX_BRIDGE_TOKEN)
    - Security audit trail logging
    - File-based locking for race condition prevention
    
    Protocol v2 Features (PS-02 Enhancement):
    - Message compression for payloads >100KB
    - Multi-client support with routing
    - CRC32 integrity verification
    """
    
    def xǁBridgeManagerǁ__init____mutmut_orig(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_1(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = False,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_2(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = False,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_3(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = False,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_4(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = False,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_5(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 11,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_6(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "XX0.0.0.0XX",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_7(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8444,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_8(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = False,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_9(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is not None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_10(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = None
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_11(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) * "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_12(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(None) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_13(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "XXcodex_secure_bridgeXX"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_14(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "CODEX_SECURE_BRIDGE"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_15(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = None
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_16(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(None)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_17(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = None
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_18(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = None
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_19(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = None
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_20(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = None
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_21(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = None
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_22(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = None
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_23(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = None
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_24(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = None
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_25(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = None
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_26(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = None
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_27(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = None
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_28(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = ""
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_29(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode != BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_30(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_31(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    None
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_32(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "XXTLS support not available. Install security.tls_config module.XX"
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_33(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "tls support not available. install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_34(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS SUPPORT NOT AVAILABLE. INSTALL SECURITY.TLS_CONFIG MODULE."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_35(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = None
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_36(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 or HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_37(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = None
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_38(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = None
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_39(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = ""
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_40(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(None)
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_41(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = None
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_42(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv(None)
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_43(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("XXCODEX_BRIDGE_TOKENXX")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_44(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("codex_bridge_token")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_45(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth or not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_46(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_47(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                None
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_48(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "XXCODEX_BRIDGE_TOKEN not set. Authentication disabled. XX"
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_49(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "codex_bridge_token not set. authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_50(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN NOT SET. AUTHENTICATION DISABLED. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_51(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "XXSet CODEX_BRIDGE_TOKEN for secure operation.XX"
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_52(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "set codex_bridge_token for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_53(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "SET CODEX_BRIDGE_TOKEN FOR SECURE OPERATION."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_54(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = None
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_55(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = True
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_56(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=None, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_57(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=None)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_58(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_59(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, )
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_60(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=False, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_61(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=False)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_62(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(None, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_63(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, None)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_64(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_65(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, )  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_66(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 449)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_67(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = None
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_68(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir * "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_69(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "XXbridge.lockXX"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_70(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "BRIDGE.LOCK"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_71(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = None
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_72(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir * "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_73(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "XXbridge.fifoXX"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_74(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "BRIDGE.FIFO"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_75(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = None
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_76(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir * "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_77(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "XXbridge.sockXX"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_78(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "BRIDGE.SOCK"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_79(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is not None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_80(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = None
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_81(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir * "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_82(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "XXaudit.logXX"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_83(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "AUDIT.LOG"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_84(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = None
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_85(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_86(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(None, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_87(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, None)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_88(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_89(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, )
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_90(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 385)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_91(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log(None, {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_92(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", None)
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_93(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log({
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_94(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", )
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_95(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("XXBRIDGE_INITXX", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_96(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("bridge_init", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_97(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "XXmodeXX": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_98(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "MODE": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_99(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "XXowner_onlyXX": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_100(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "OWNER_ONLY": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_101(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "XXrequire_authXX": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_102(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "REQUIRE_AUTH": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_103(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "XXbridge_dirXX": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_104(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "BRIDGE_DIR": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_105(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(None)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_106(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(None)
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_107(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode != BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_108(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode != BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_109(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode != BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_110(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(None)
        else:
            raise ValueError(f"Unsupported bridge mode: {mode}")
    
    def xǁBridgeManagerǁ__init____mutmut_111(
        self,
        bridge_dir: Optional[Path] = None,
        mode: BridgeMode = BridgeMode.NAMED_PIPE,
        owner_only: bool = True,
        require_auth: bool = True,
        audit_file: Optional[Path] = None,
        use_protocol_v2: bool = True,
        enable_compression: bool = True,
        max_clients: int = 10,
        # TLS configuration (for TCP_TLS mode)
        tls_host: str = "0.0.0.0",
        tls_port: int = 8443,
        tls_server_cert: Optional[str] = None,
        tls_server_key: Optional[str] = None,
        tls_ca_cert: Optional[str] = None,
        tls_client_cert: Optional[str] = None,
        tls_client_key: Optional[str] = None,
        tls_require_client_cert: bool = True,
    ):
        """
        Initialize bridge manager.
        
        Args:
            bridge_dir: Directory for bridge files (defaults to secure temp location)
            mode: Communication mode (named_pipe, unix_socket, or tcp_tls)
            owner_only: Restrict permissions to owner only (0o600)
            require_auth: Require authentication token (default: True)
            audit_file: Path to audit log file (default: bridge_dir/audit.log)
            use_protocol_v2: Enable Protocol v2 with compression (default: True)
            enable_compression: Enable payload compression (default: True)
            max_clients: Maximum concurrent clients for multi-client mode
            tls_host: Bind address for TLS server (TCP_TLS mode only)
            tls_port: Port for TLS server (TCP_TLS mode only)
            tls_server_cert: Path to server certificate (TCP_TLS mode)
            tls_server_key: Path to server key (TCP_TLS mode)
            tls_ca_cert: Path to CA certificate (TCP_TLS mode)
            tls_client_cert: Path to client certificate (TCP_TLS mode)
            tls_client_key: Path to client key (TCP_TLS mode)
            tls_require_client_cert: Require client certificates (mTLS)
        """
        if bridge_dir is None:
            # Use secure temp directory with restricted permissions
            bridge_dir = Path(tempfile.gettempdir()) / "codex_secure_bridge"
        
        self.bridge_dir = Path(bridge_dir)
        self.mode = mode
        self.owner_only = owner_only
        self.require_auth = require_auth
        
        # TLS configuration
        self.tls_host = tls_host
        self.tls_port = tls_port
        self.tls_server_cert = tls_server_cert
        self.tls_server_key = tls_server_key
        self.tls_ca_cert = tls_ca_cert
        self.tls_client_cert = tls_client_cert
        self.tls_client_key = tls_client_key
        self.tls_require_client_cert = tls_require_client_cert
        self._tls_context: Optional[ssl.SSLContext] = None
        
        # Validate TLS configuration if TCP_TLS mode
        if self.mode == BridgeMode.TCP_TLS:
            if not HAS_TLS_SUPPORT:
                raise RuntimeError(
                    "TLS support not available. Install security.tls_config module."
                )
            self._validate_tls_config()
        
        # Protocol v2 settings
        self.use_protocol_v2 = use_protocol_v2 and HAS_PROTOCOL_V2
        self.enable_compression = enable_compression
        self._max_clients = max_clients
        
        # Multi-client bridge (v2 feature) - lazy initialization
        self._multi_client_bridge: Optional[MultiClientBridge] = None
        if self.use_protocol_v2:
            logger.info(f"Bridge Protocol v2 enabled (compression={enable_compression})")
        
        # Get auth token from environment
        self.auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
        if self.require_auth and not self.auth_token:
            logger.warning(
                "CODEX_BRIDGE_TOKEN not set. Authentication disabled. "
                "Set CODEX_BRIDGE_TOKEN for secure operation."
            )
            self.require_auth = False
        
        # Create bridge directory with secure permissions
        self.bridge_dir.mkdir(parents=True, exist_ok=True)
        if owner_only:
            os.chmod(self.bridge_dir, 0o700)  # Owner only: rwx------
        
        # Set up paths
        self.lock_path = self.bridge_dir / "bridge.lock"
        self.pipe_path = self.bridge_dir / "bridge.fifo"
        self.socket_path = self.bridge_dir / "bridge.sock"
        
        # Set up audit logging (PS-02)
        if audit_file is None:
            audit_file = self.bridge_dir / "audit.log"
        self.audit_file = audit_file
        
        # Create audit log with secure permissions
        if not self.audit_file.exists():
            self.audit_file.touch()
            if owner_only:
                os.chmod(self.audit_file, 0o600)
        
        self._audit_log("BRIDGE_INIT", {
            "mode": mode.value,
            "owner_only": owner_only,
            "require_auth": require_auth,
            "bridge_dir": str(bridge_dir)
        })
        
        logger.info(f"Bridge manager initialized: mode={mode.value}, dir={bridge_dir}")
        
        # Initialize based on mode
        if mode == BridgeMode.NAMED_PIPE:
            self._init_named_pipe()
        elif mode == BridgeMode.UNIX_SOCKET:
            self._init_unix_socket()
        elif mode == BridgeMode.TCP_TLS:
            # TLS sockets created on-demand in start_server/connect methods
            logger.info(f"TCP_TLS mode configured: {self.tls_host}:{self.tls_port}")
        else:
            raise ValueError(None)
    
    xǁBridgeManagerǁ__init____mutmut_mutants : ClassVar[MutantDict] = {
    'xǁBridgeManagerǁ__init____mutmut_1': xǁBridgeManagerǁ__init____mutmut_1, 
        'xǁBridgeManagerǁ__init____mutmut_2': xǁBridgeManagerǁ__init____mutmut_2, 
        'xǁBridgeManagerǁ__init____mutmut_3': xǁBridgeManagerǁ__init____mutmut_3, 
        'xǁBridgeManagerǁ__init____mutmut_4': xǁBridgeManagerǁ__init____mutmut_4, 
        'xǁBridgeManagerǁ__init____mutmut_5': xǁBridgeManagerǁ__init____mutmut_5, 
        'xǁBridgeManagerǁ__init____mutmut_6': xǁBridgeManagerǁ__init____mutmut_6, 
        'xǁBridgeManagerǁ__init____mutmut_7': xǁBridgeManagerǁ__init____mutmut_7, 
        'xǁBridgeManagerǁ__init____mutmut_8': xǁBridgeManagerǁ__init____mutmut_8, 
        'xǁBridgeManagerǁ__init____mutmut_9': xǁBridgeManagerǁ__init____mutmut_9, 
        'xǁBridgeManagerǁ__init____mutmut_10': xǁBridgeManagerǁ__init____mutmut_10, 
        'xǁBridgeManagerǁ__init____mutmut_11': xǁBridgeManagerǁ__init____mutmut_11, 
        'xǁBridgeManagerǁ__init____mutmut_12': xǁBridgeManagerǁ__init____mutmut_12, 
        'xǁBridgeManagerǁ__init____mutmut_13': xǁBridgeManagerǁ__init____mutmut_13, 
        'xǁBridgeManagerǁ__init____mutmut_14': xǁBridgeManagerǁ__init____mutmut_14, 
        'xǁBridgeManagerǁ__init____mutmut_15': xǁBridgeManagerǁ__init____mutmut_15, 
        'xǁBridgeManagerǁ__init____mutmut_16': xǁBridgeManagerǁ__init____mutmut_16, 
        'xǁBridgeManagerǁ__init____mutmut_17': xǁBridgeManagerǁ__init____mutmut_17, 
        'xǁBridgeManagerǁ__init____mutmut_18': xǁBridgeManagerǁ__init____mutmut_18, 
        'xǁBridgeManagerǁ__init____mutmut_19': xǁBridgeManagerǁ__init____mutmut_19, 
        'xǁBridgeManagerǁ__init____mutmut_20': xǁBridgeManagerǁ__init____mutmut_20, 
        'xǁBridgeManagerǁ__init____mutmut_21': xǁBridgeManagerǁ__init____mutmut_21, 
        'xǁBridgeManagerǁ__init____mutmut_22': xǁBridgeManagerǁ__init____mutmut_22, 
        'xǁBridgeManagerǁ__init____mutmut_23': xǁBridgeManagerǁ__init____mutmut_23, 
        'xǁBridgeManagerǁ__init____mutmut_24': xǁBridgeManagerǁ__init____mutmut_24, 
        'xǁBridgeManagerǁ__init____mutmut_25': xǁBridgeManagerǁ__init____mutmut_25, 
        'xǁBridgeManagerǁ__init____mutmut_26': xǁBridgeManagerǁ__init____mutmut_26, 
        'xǁBridgeManagerǁ__init____mutmut_27': xǁBridgeManagerǁ__init____mutmut_27, 
        'xǁBridgeManagerǁ__init____mutmut_28': xǁBridgeManagerǁ__init____mutmut_28, 
        'xǁBridgeManagerǁ__init____mutmut_29': xǁBridgeManagerǁ__init____mutmut_29, 
        'xǁBridgeManagerǁ__init____mutmut_30': xǁBridgeManagerǁ__init____mutmut_30, 
        'xǁBridgeManagerǁ__init____mutmut_31': xǁBridgeManagerǁ__init____mutmut_31, 
        'xǁBridgeManagerǁ__init____mutmut_32': xǁBridgeManagerǁ__init____mutmut_32, 
        'xǁBridgeManagerǁ__init____mutmut_33': xǁBridgeManagerǁ__init____mutmut_33, 
        'xǁBridgeManagerǁ__init____mutmut_34': xǁBridgeManagerǁ__init____mutmut_34, 
        'xǁBridgeManagerǁ__init____mutmut_35': xǁBridgeManagerǁ__init____mutmut_35, 
        'xǁBridgeManagerǁ__init____mutmut_36': xǁBridgeManagerǁ__init____mutmut_36, 
        'xǁBridgeManagerǁ__init____mutmut_37': xǁBridgeManagerǁ__init____mutmut_37, 
        'xǁBridgeManagerǁ__init____mutmut_38': xǁBridgeManagerǁ__init____mutmut_38, 
        'xǁBridgeManagerǁ__init____mutmut_39': xǁBridgeManagerǁ__init____mutmut_39, 
        'xǁBridgeManagerǁ__init____mutmut_40': xǁBridgeManagerǁ__init____mutmut_40, 
        'xǁBridgeManagerǁ__init____mutmut_41': xǁBridgeManagerǁ__init____mutmut_41, 
        'xǁBridgeManagerǁ__init____mutmut_42': xǁBridgeManagerǁ__init____mutmut_42, 
        'xǁBridgeManagerǁ__init____mutmut_43': xǁBridgeManagerǁ__init____mutmut_43, 
        'xǁBridgeManagerǁ__init____mutmut_44': xǁBridgeManagerǁ__init____mutmut_44, 
        'xǁBridgeManagerǁ__init____mutmut_45': xǁBridgeManagerǁ__init____mutmut_45, 
        'xǁBridgeManagerǁ__init____mutmut_46': xǁBridgeManagerǁ__init____mutmut_46, 
        'xǁBridgeManagerǁ__init____mutmut_47': xǁBridgeManagerǁ__init____mutmut_47, 
        'xǁBridgeManagerǁ__init____mutmut_48': xǁBridgeManagerǁ__init____mutmut_48, 
        'xǁBridgeManagerǁ__init____mutmut_49': xǁBridgeManagerǁ__init____mutmut_49, 
        'xǁBridgeManagerǁ__init____mutmut_50': xǁBridgeManagerǁ__init____mutmut_50, 
        'xǁBridgeManagerǁ__init____mutmut_51': xǁBridgeManagerǁ__init____mutmut_51, 
        'xǁBridgeManagerǁ__init____mutmut_52': xǁBridgeManagerǁ__init____mutmut_52, 
        'xǁBridgeManagerǁ__init____mutmut_53': xǁBridgeManagerǁ__init____mutmut_53, 
        'xǁBridgeManagerǁ__init____mutmut_54': xǁBridgeManagerǁ__init____mutmut_54, 
        'xǁBridgeManagerǁ__init____mutmut_55': xǁBridgeManagerǁ__init____mutmut_55, 
        'xǁBridgeManagerǁ__init____mutmut_56': xǁBridgeManagerǁ__init____mutmut_56, 
        'xǁBridgeManagerǁ__init____mutmut_57': xǁBridgeManagerǁ__init____mutmut_57, 
        'xǁBridgeManagerǁ__init____mutmut_58': xǁBridgeManagerǁ__init____mutmut_58, 
        'xǁBridgeManagerǁ__init____mutmut_59': xǁBridgeManagerǁ__init____mutmut_59, 
        'xǁBridgeManagerǁ__init____mutmut_60': xǁBridgeManagerǁ__init____mutmut_60, 
        'xǁBridgeManagerǁ__init____mutmut_61': xǁBridgeManagerǁ__init____mutmut_61, 
        'xǁBridgeManagerǁ__init____mutmut_62': xǁBridgeManagerǁ__init____mutmut_62, 
        'xǁBridgeManagerǁ__init____mutmut_63': xǁBridgeManagerǁ__init____mutmut_63, 
        'xǁBridgeManagerǁ__init____mutmut_64': xǁBridgeManagerǁ__init____mutmut_64, 
        'xǁBridgeManagerǁ__init____mutmut_65': xǁBridgeManagerǁ__init____mutmut_65, 
        'xǁBridgeManagerǁ__init____mutmut_66': xǁBridgeManagerǁ__init____mutmut_66, 
        'xǁBridgeManagerǁ__init____mutmut_67': xǁBridgeManagerǁ__init____mutmut_67, 
        'xǁBridgeManagerǁ__init____mutmut_68': xǁBridgeManagerǁ__init____mutmut_68, 
        'xǁBridgeManagerǁ__init____mutmut_69': xǁBridgeManagerǁ__init____mutmut_69, 
        'xǁBridgeManagerǁ__init____mutmut_70': xǁBridgeManagerǁ__init____mutmut_70, 
        'xǁBridgeManagerǁ__init____mutmut_71': xǁBridgeManagerǁ__init____mutmut_71, 
        'xǁBridgeManagerǁ__init____mutmut_72': xǁBridgeManagerǁ__init____mutmut_72, 
        'xǁBridgeManagerǁ__init____mutmut_73': xǁBridgeManagerǁ__init____mutmut_73, 
        'xǁBridgeManagerǁ__init____mutmut_74': xǁBridgeManagerǁ__init____mutmut_74, 
        'xǁBridgeManagerǁ__init____mutmut_75': xǁBridgeManagerǁ__init____mutmut_75, 
        'xǁBridgeManagerǁ__init____mutmut_76': xǁBridgeManagerǁ__init____mutmut_76, 
        'xǁBridgeManagerǁ__init____mutmut_77': xǁBridgeManagerǁ__init____mutmut_77, 
        'xǁBridgeManagerǁ__init____mutmut_78': xǁBridgeManagerǁ__init____mutmut_78, 
        'xǁBridgeManagerǁ__init____mutmut_79': xǁBridgeManagerǁ__init____mutmut_79, 
        'xǁBridgeManagerǁ__init____mutmut_80': xǁBridgeManagerǁ__init____mutmut_80, 
        'xǁBridgeManagerǁ__init____mutmut_81': xǁBridgeManagerǁ__init____mutmut_81, 
        'xǁBridgeManagerǁ__init____mutmut_82': xǁBridgeManagerǁ__init____mutmut_82, 
        'xǁBridgeManagerǁ__init____mutmut_83': xǁBridgeManagerǁ__init____mutmut_83, 
        'xǁBridgeManagerǁ__init____mutmut_84': xǁBridgeManagerǁ__init____mutmut_84, 
        'xǁBridgeManagerǁ__init____mutmut_85': xǁBridgeManagerǁ__init____mutmut_85, 
        'xǁBridgeManagerǁ__init____mutmut_86': xǁBridgeManagerǁ__init____mutmut_86, 
        'xǁBridgeManagerǁ__init____mutmut_87': xǁBridgeManagerǁ__init____mutmut_87, 
        'xǁBridgeManagerǁ__init____mutmut_88': xǁBridgeManagerǁ__init____mutmut_88, 
        'xǁBridgeManagerǁ__init____mutmut_89': xǁBridgeManagerǁ__init____mutmut_89, 
        'xǁBridgeManagerǁ__init____mutmut_90': xǁBridgeManagerǁ__init____mutmut_90, 
        'xǁBridgeManagerǁ__init____mutmut_91': xǁBridgeManagerǁ__init____mutmut_91, 
        'xǁBridgeManagerǁ__init____mutmut_92': xǁBridgeManagerǁ__init____mutmut_92, 
        'xǁBridgeManagerǁ__init____mutmut_93': xǁBridgeManagerǁ__init____mutmut_93, 
        'xǁBridgeManagerǁ__init____mutmut_94': xǁBridgeManagerǁ__init____mutmut_94, 
        'xǁBridgeManagerǁ__init____mutmut_95': xǁBridgeManagerǁ__init____mutmut_95, 
        'xǁBridgeManagerǁ__init____mutmut_96': xǁBridgeManagerǁ__init____mutmut_96, 
        'xǁBridgeManagerǁ__init____mutmut_97': xǁBridgeManagerǁ__init____mutmut_97, 
        'xǁBridgeManagerǁ__init____mutmut_98': xǁBridgeManagerǁ__init____mutmut_98, 
        'xǁBridgeManagerǁ__init____mutmut_99': xǁBridgeManagerǁ__init____mutmut_99, 
        'xǁBridgeManagerǁ__init____mutmut_100': xǁBridgeManagerǁ__init____mutmut_100, 
        'xǁBridgeManagerǁ__init____mutmut_101': xǁBridgeManagerǁ__init____mutmut_101, 
        'xǁBridgeManagerǁ__init____mutmut_102': xǁBridgeManagerǁ__init____mutmut_102, 
        'xǁBridgeManagerǁ__init____mutmut_103': xǁBridgeManagerǁ__init____mutmut_103, 
        'xǁBridgeManagerǁ__init____mutmut_104': xǁBridgeManagerǁ__init____mutmut_104, 
        'xǁBridgeManagerǁ__init____mutmut_105': xǁBridgeManagerǁ__init____mutmut_105, 
        'xǁBridgeManagerǁ__init____mutmut_106': xǁBridgeManagerǁ__init____mutmut_106, 
        'xǁBridgeManagerǁ__init____mutmut_107': xǁBridgeManagerǁ__init____mutmut_107, 
        'xǁBridgeManagerǁ__init____mutmut_108': xǁBridgeManagerǁ__init____mutmut_108, 
        'xǁBridgeManagerǁ__init____mutmut_109': xǁBridgeManagerǁ__init____mutmut_109, 
        'xǁBridgeManagerǁ__init____mutmut_110': xǁBridgeManagerǁ__init____mutmut_110, 
        'xǁBridgeManagerǁ__init____mutmut_111': xǁBridgeManagerǁ__init____mutmut_111
    }
    
    def __init__(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁBridgeManagerǁ__init____mutmut_orig"), object.__getattribute__(self, "xǁBridgeManagerǁ__init____mutmut_mutants"), args, kwargs, self)
        return result 
    
    __init__.__signature__ = _mutmut_signature(xǁBridgeManagerǁ__init____mutmut_orig)
    xǁBridgeManagerǁ__init____mutmut_orig.__name__ = 'xǁBridgeManagerǁ__init__'
    
    def xǁBridgeManagerǁ_init_named_pipe__mutmut_orig(self) -> None:
        """Initialize Named Pipe (FIFO)."""
        try:
            # Remove existing pipe if present
            if self.pipe_path.exists():
                self.pipe_path.unlink()
            
            # Create FIFO
            os.mkfifo(str(self.pipe_path), 0o600 if self.owner_only else 0o666)
            logger.info(f"Named pipe created: {self.pipe_path}")
            
        except Exception as e:
            logger.error(f"Failed to create named pipe: {e}")
            raise
    
    def xǁBridgeManagerǁ_init_named_pipe__mutmut_1(self) -> None:
        """Initialize Named Pipe (FIFO)."""
        try:
            # Remove existing pipe if present
            if self.pipe_path.exists():
                self.pipe_path.unlink()
            
            # Create FIFO
            os.mkfifo(None, 0o600 if self.owner_only else 0o666)
            logger.info(f"Named pipe created: {self.pipe_path}")
            
        except Exception as e:
            logger.error(f"Failed to create named pipe: {e}")
            raise
    
    def xǁBridgeManagerǁ_init_named_pipe__mutmut_2(self) -> None:
        """Initialize Named Pipe (FIFO)."""
        try:
            # Remove existing pipe if present
            if self.pipe_path.exists():
                self.pipe_path.unlink()
            
            # Create FIFO
            os.mkfifo(str(self.pipe_path), None)
            logger.info(f"Named pipe created: {self.pipe_path}")
            
        except Exception as e:
            logger.error(f"Failed to create named pipe: {e}")
            raise
    
    def xǁBridgeManagerǁ_init_named_pipe__mutmut_3(self) -> None:
        """Initialize Named Pipe (FIFO)."""
        try:
            # Remove existing pipe if present
            if self.pipe_path.exists():
                self.pipe_path.unlink()
            
            # Create FIFO
            os.mkfifo(0o600 if self.owner_only else 0o666)
            logger.info(f"Named pipe created: {self.pipe_path}")
            
        except Exception as e:
            logger.error(f"Failed to create named pipe: {e}")
            raise
    
    def xǁBridgeManagerǁ_init_named_pipe__mutmut_4(self) -> None:
        """Initialize Named Pipe (FIFO)."""
        try:
            # Remove existing pipe if present
            if self.pipe_path.exists():
                self.pipe_path.unlink()
            
            # Create FIFO
            os.mkfifo(str(self.pipe_path), )
            logger.info(f"Named pipe created: {self.pipe_path}")
            
        except Exception as e:
            logger.error(f"Failed to create named pipe: {e}")
            raise
    
    def xǁBridgeManagerǁ_init_named_pipe__mutmut_5(self) -> None:
        """Initialize Named Pipe (FIFO)."""
        try:
            # Remove existing pipe if present
            if self.pipe_path.exists():
                self.pipe_path.unlink()
            
            # Create FIFO
            os.mkfifo(str(None), 0o600 if self.owner_only else 0o666)
            logger.info(f"Named pipe created: {self.pipe_path}")
            
        except Exception as e:
            logger.error(f"Failed to create named pipe: {e}")
            raise
    
    def xǁBridgeManagerǁ_init_named_pipe__mutmut_6(self) -> None:
        """Initialize Named Pipe (FIFO)."""
        try:
            # Remove existing pipe if present
            if self.pipe_path.exists():
                self.pipe_path.unlink()
            
            # Create FIFO
            os.mkfifo(str(self.pipe_path), 385 if self.owner_only else 0o666)
            logger.info(f"Named pipe created: {self.pipe_path}")
            
        except Exception as e:
            logger.error(f"Failed to create named pipe: {e}")
            raise
    
    def xǁBridgeManagerǁ_init_named_pipe__mutmut_7(self) -> None:
        """Initialize Named Pipe (FIFO)."""
        try:
            # Remove existing pipe if present
            if self.pipe_path.exists():
                self.pipe_path.unlink()
            
            # Create FIFO
            os.mkfifo(str(self.pipe_path), 0o600 if self.owner_only else 439)
            logger.info(f"Named pipe created: {self.pipe_path}")
            
        except Exception as e:
            logger.error(f"Failed to create named pipe: {e}")
            raise
    
    def xǁBridgeManagerǁ_init_named_pipe__mutmut_8(self) -> None:
        """Initialize Named Pipe (FIFO)."""
        try:
            # Remove existing pipe if present
            if self.pipe_path.exists():
                self.pipe_path.unlink()
            
            # Create FIFO
            os.mkfifo(str(self.pipe_path), 0o600 if self.owner_only else 0o666)
            logger.info(None)
            
        except Exception as e:
            logger.error(f"Failed to create named pipe: {e}")
            raise
    
    def xǁBridgeManagerǁ_init_named_pipe__mutmut_9(self) -> None:
        """Initialize Named Pipe (FIFO)."""
        try:
            # Remove existing pipe if present
            if self.pipe_path.exists():
                self.pipe_path.unlink()
            
            # Create FIFO
            os.mkfifo(str(self.pipe_path), 0o600 if self.owner_only else 0o666)
            logger.info(f"Named pipe created: {self.pipe_path}")
            
        except Exception as e:
            logger.error(None)
            raise
    
    xǁBridgeManagerǁ_init_named_pipe__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁBridgeManagerǁ_init_named_pipe__mutmut_1': xǁBridgeManagerǁ_init_named_pipe__mutmut_1, 
        'xǁBridgeManagerǁ_init_named_pipe__mutmut_2': xǁBridgeManagerǁ_init_named_pipe__mutmut_2, 
        'xǁBridgeManagerǁ_init_named_pipe__mutmut_3': xǁBridgeManagerǁ_init_named_pipe__mutmut_3, 
        'xǁBridgeManagerǁ_init_named_pipe__mutmut_4': xǁBridgeManagerǁ_init_named_pipe__mutmut_4, 
        'xǁBridgeManagerǁ_init_named_pipe__mutmut_5': xǁBridgeManagerǁ_init_named_pipe__mutmut_5, 
        'xǁBridgeManagerǁ_init_named_pipe__mutmut_6': xǁBridgeManagerǁ_init_named_pipe__mutmut_6, 
        'xǁBridgeManagerǁ_init_named_pipe__mutmut_7': xǁBridgeManagerǁ_init_named_pipe__mutmut_7, 
        'xǁBridgeManagerǁ_init_named_pipe__mutmut_8': xǁBridgeManagerǁ_init_named_pipe__mutmut_8, 
        'xǁBridgeManagerǁ_init_named_pipe__mutmut_9': xǁBridgeManagerǁ_init_named_pipe__mutmut_9
    }
    
    def _init_named_pipe(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁBridgeManagerǁ_init_named_pipe__mutmut_orig"), object.__getattribute__(self, "xǁBridgeManagerǁ_init_named_pipe__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _init_named_pipe.__signature__ = _mutmut_signature(xǁBridgeManagerǁ_init_named_pipe__mutmut_orig)
    xǁBridgeManagerǁ_init_named_pipe__mutmut_orig.__name__ = 'xǁBridgeManagerǁ_init_named_pipe'
    
    def xǁBridgeManagerǁ_init_unix_socket__mutmut_orig(self) -> None:
        """Initialize Unix domain socket."""
        try:
            # Remove existing socket if present
            if self.socket_path.exists():
                self.socket_path.unlink()
            
            # Socket will be created on bind
            logger.info(f"Unix socket path prepared: {self.socket_path}")
            
        except Exception as e:
            logger.error(f"Failed to prepare unix socket: {e}")
            raise
    
    def xǁBridgeManagerǁ_init_unix_socket__mutmut_1(self) -> None:
        """Initialize Unix domain socket."""
        try:
            # Remove existing socket if present
            if self.socket_path.exists():
                self.socket_path.unlink()
            
            # Socket will be created on bind
            logger.info(None)
            
        except Exception as e:
            logger.error(f"Failed to prepare unix socket: {e}")
            raise
    
    def xǁBridgeManagerǁ_init_unix_socket__mutmut_2(self) -> None:
        """Initialize Unix domain socket."""
        try:
            # Remove existing socket if present
            if self.socket_path.exists():
                self.socket_path.unlink()
            
            # Socket will be created on bind
            logger.info(f"Unix socket path prepared: {self.socket_path}")
            
        except Exception as e:
            logger.error(None)
            raise
    
    xǁBridgeManagerǁ_init_unix_socket__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁBridgeManagerǁ_init_unix_socket__mutmut_1': xǁBridgeManagerǁ_init_unix_socket__mutmut_1, 
        'xǁBridgeManagerǁ_init_unix_socket__mutmut_2': xǁBridgeManagerǁ_init_unix_socket__mutmut_2
    }
    
    def _init_unix_socket(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁBridgeManagerǁ_init_unix_socket__mutmut_orig"), object.__getattribute__(self, "xǁBridgeManagerǁ_init_unix_socket__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _init_unix_socket.__signature__ = _mutmut_signature(xǁBridgeManagerǁ_init_unix_socket__mutmut_orig)
    xǁBridgeManagerǁ_init_unix_socket__mutmut_orig.__name__ = 'xǁBridgeManagerǁ_init_unix_socket'
    
    def xǁBridgeManagerǁ_validate_tls_config__mutmut_orig(self) -> None:
        """Validate TLS configuration for TCP_TLS mode."""
        if not all([
            self.tls_server_cert,
            self.tls_server_key,
            self.tls_ca_cert,
        ]):
            raise ValueError(
                "TCP_TLS mode requires server_cert, server_key, and ca_cert"
            )
        
        # Validate paths exist
        for path_name, path_value in [
            ("server_cert", self.tls_server_cert),
            ("server_key", self.tls_server_key),
            ("ca_cert", self.tls_ca_cert),
        ]:
            if not Path(path_value).exists():
                raise FileNotFoundError(f"TLS {path_name} not found: {path_value}")
        
        logger.info("TLS configuration validated")
    
    def xǁBridgeManagerǁ_validate_tls_config__mutmut_1(self) -> None:
        """Validate TLS configuration for TCP_TLS mode."""
        if all([
            self.tls_server_cert,
            self.tls_server_key,
            self.tls_ca_cert,
        ]):
            raise ValueError(
                "TCP_TLS mode requires server_cert, server_key, and ca_cert"
            )
        
        # Validate paths exist
        for path_name, path_value in [
            ("server_cert", self.tls_server_cert),
            ("server_key", self.tls_server_key),
            ("ca_cert", self.tls_ca_cert),
        ]:
            if not Path(path_value).exists():
                raise FileNotFoundError(f"TLS {path_name} not found: {path_value}")
        
        logger.info("TLS configuration validated")
    
    def xǁBridgeManagerǁ_validate_tls_config__mutmut_2(self) -> None:
        """Validate TLS configuration for TCP_TLS mode."""
        if not all(None):
            raise ValueError(
                "TCP_TLS mode requires server_cert, server_key, and ca_cert"
            )
        
        # Validate paths exist
        for path_name, path_value in [
            ("server_cert", self.tls_server_cert),
            ("server_key", self.tls_server_key),
            ("ca_cert", self.tls_ca_cert),
        ]:
            if not Path(path_value).exists():
                raise FileNotFoundError(f"TLS {path_name} not found: {path_value}")
        
        logger.info("TLS configuration validated")
    
    def xǁBridgeManagerǁ_validate_tls_config__mutmut_3(self) -> None:
        """Validate TLS configuration for TCP_TLS mode."""
        if not all([
            self.tls_server_cert,
            self.tls_server_key,
            self.tls_ca_cert,
        ]):
            raise ValueError(
                None
            )
        
        # Validate paths exist
        for path_name, path_value in [
            ("server_cert", self.tls_server_cert),
            ("server_key", self.tls_server_key),
            ("ca_cert", self.tls_ca_cert),
        ]:
            if not Path(path_value).exists():
                raise FileNotFoundError(f"TLS {path_name} not found: {path_value}")
        
        logger.info("TLS configuration validated")
    
    def xǁBridgeManagerǁ_validate_tls_config__mutmut_4(self) -> None:
        """Validate TLS configuration for TCP_TLS mode."""
        if not all([
            self.tls_server_cert,
            self.tls_server_key,
            self.tls_ca_cert,
        ]):
            raise ValueError(
                "XXTCP_TLS mode requires server_cert, server_key, and ca_certXX"
            )
        
        # Validate paths exist
        for path_name, path_value in [
            ("server_cert", self.tls_server_cert),
            ("server_key", self.tls_server_key),
            ("ca_cert", self.tls_ca_cert),
        ]:
            if not Path(path_value).exists():
                raise FileNotFoundError(f"TLS {path_name} not found: {path_value}")
        
        logger.info("TLS configuration validated")
    
    def xǁBridgeManagerǁ_validate_tls_config__mutmut_5(self) -> None:
        """Validate TLS configuration for TCP_TLS mode."""
        if not all([
            self.tls_server_cert,
            self.tls_server_key,
            self.tls_ca_cert,
        ]):
            raise ValueError(
                "tcp_tls mode requires server_cert, server_key, and ca_cert"
            )
        
        # Validate paths exist
        for path_name, path_value in [
            ("server_cert", self.tls_server_cert),
            ("server_key", self.tls_server_key),
            ("ca_cert", self.tls_ca_cert),
        ]:
            if not Path(path_value).exists():
                raise FileNotFoundError(f"TLS {path_name} not found: {path_value}")
        
        logger.info("TLS configuration validated")
    
    def xǁBridgeManagerǁ_validate_tls_config__mutmut_6(self) -> None:
        """Validate TLS configuration for TCP_TLS mode."""
        if not all([
            self.tls_server_cert,
            self.tls_server_key,
            self.tls_ca_cert,
        ]):
            raise ValueError(
                "TCP_TLS MODE REQUIRES SERVER_CERT, SERVER_KEY, AND CA_CERT"
            )
        
        # Validate paths exist
        for path_name, path_value in [
            ("server_cert", self.tls_server_cert),
            ("server_key", self.tls_server_key),
            ("ca_cert", self.tls_ca_cert),
        ]:
            if not Path(path_value).exists():
                raise FileNotFoundError(f"TLS {path_name} not found: {path_value}")
        
        logger.info("TLS configuration validated")
    
    def xǁBridgeManagerǁ_validate_tls_config__mutmut_7(self) -> None:
        """Validate TLS configuration for TCP_TLS mode."""
        if not all([
            self.tls_server_cert,
            self.tls_server_key,
            self.tls_ca_cert,
        ]):
            raise ValueError(
                "TCP_TLS mode requires server_cert, server_key, and ca_cert"
            )
        
        # Validate paths exist
        for path_name, path_value in [
            ("XXserver_certXX", self.tls_server_cert),
            ("server_key", self.tls_server_key),
            ("ca_cert", self.tls_ca_cert),
        ]:
            if not Path(path_value).exists():
                raise FileNotFoundError(f"TLS {path_name} not found: {path_value}")
        
        logger.info("TLS configuration validated")
    
    def xǁBridgeManagerǁ_validate_tls_config__mutmut_8(self) -> None:
        """Validate TLS configuration for TCP_TLS mode."""
        if not all([
            self.tls_server_cert,
            self.tls_server_key,
            self.tls_ca_cert,
        ]):
            raise ValueError(
                "TCP_TLS mode requires server_cert, server_key, and ca_cert"
            )
        
        # Validate paths exist
        for path_name, path_value in [
            ("SERVER_CERT", self.tls_server_cert),
            ("server_key", self.tls_server_key),
            ("ca_cert", self.tls_ca_cert),
        ]:
            if not Path(path_value).exists():
                raise FileNotFoundError(f"TLS {path_name} not found: {path_value}")
        
        logger.info("TLS configuration validated")
    
    def xǁBridgeManagerǁ_validate_tls_config__mutmut_9(self) -> None:
        """Validate TLS configuration for TCP_TLS mode."""
        if not all([
            self.tls_server_cert,
            self.tls_server_key,
            self.tls_ca_cert,
        ]):
            raise ValueError(
                "TCP_TLS mode requires server_cert, server_key, and ca_cert"
            )
        
        # Validate paths exist
        for path_name, path_value in [
            ("server_cert", self.tls_server_cert),
            ("XXserver_keyXX", self.tls_server_key),
            ("ca_cert", self.tls_ca_cert),
        ]:
            if not Path(path_value).exists():
                raise FileNotFoundError(f"TLS {path_name} not found: {path_value}")
        
        logger.info("TLS configuration validated")
    
    def xǁBridgeManagerǁ_validate_tls_config__mutmut_10(self) -> None:
        """Validate TLS configuration for TCP_TLS mode."""
        if not all([
            self.tls_server_cert,
            self.tls_server_key,
            self.tls_ca_cert,
        ]):
            raise ValueError(
                "TCP_TLS mode requires server_cert, server_key, and ca_cert"
            )
        
        # Validate paths exist
        for path_name, path_value in [
            ("server_cert", self.tls_server_cert),
            ("SERVER_KEY", self.tls_server_key),
            ("ca_cert", self.tls_ca_cert),
        ]:
            if not Path(path_value).exists():
                raise FileNotFoundError(f"TLS {path_name} not found: {path_value}")
        
        logger.info("TLS configuration validated")
    
    def xǁBridgeManagerǁ_validate_tls_config__mutmut_11(self) -> None:
        """Validate TLS configuration for TCP_TLS mode."""
        if not all([
            self.tls_server_cert,
            self.tls_server_key,
            self.tls_ca_cert,
        ]):
            raise ValueError(
                "TCP_TLS mode requires server_cert, server_key, and ca_cert"
            )
        
        # Validate paths exist
        for path_name, path_value in [
            ("server_cert", self.tls_server_cert),
            ("server_key", self.tls_server_key),
            ("XXca_certXX", self.tls_ca_cert),
        ]:
            if not Path(path_value).exists():
                raise FileNotFoundError(f"TLS {path_name} not found: {path_value}")
        
        logger.info("TLS configuration validated")
    
    def xǁBridgeManagerǁ_validate_tls_config__mutmut_12(self) -> None:
        """Validate TLS configuration for TCP_TLS mode."""
        if not all([
            self.tls_server_cert,
            self.tls_server_key,
            self.tls_ca_cert,
        ]):
            raise ValueError(
                "TCP_TLS mode requires server_cert, server_key, and ca_cert"
            )
        
        # Validate paths exist
        for path_name, path_value in [
            ("server_cert", self.tls_server_cert),
            ("server_key", self.tls_server_key),
            ("CA_CERT", self.tls_ca_cert),
        ]:
            if not Path(path_value).exists():
                raise FileNotFoundError(f"TLS {path_name} not found: {path_value}")
        
        logger.info("TLS configuration validated")
    
    def xǁBridgeManagerǁ_validate_tls_config__mutmut_13(self) -> None:
        """Validate TLS configuration for TCP_TLS mode."""
        if not all([
            self.tls_server_cert,
            self.tls_server_key,
            self.tls_ca_cert,
        ]):
            raise ValueError(
                "TCP_TLS mode requires server_cert, server_key, and ca_cert"
            )
        
        # Validate paths exist
        for path_name, path_value in [
            ("server_cert", self.tls_server_cert),
            ("server_key", self.tls_server_key),
            ("ca_cert", self.tls_ca_cert),
        ]:
            if Path(path_value).exists():
                raise FileNotFoundError(f"TLS {path_name} not found: {path_value}")
        
        logger.info("TLS configuration validated")
    
    def xǁBridgeManagerǁ_validate_tls_config__mutmut_14(self) -> None:
        """Validate TLS configuration for TCP_TLS mode."""
        if not all([
            self.tls_server_cert,
            self.tls_server_key,
            self.tls_ca_cert,
        ]):
            raise ValueError(
                "TCP_TLS mode requires server_cert, server_key, and ca_cert"
            )
        
        # Validate paths exist
        for path_name, path_value in [
            ("server_cert", self.tls_server_cert),
            ("server_key", self.tls_server_key),
            ("ca_cert", self.tls_ca_cert),
        ]:
            if not Path(None).exists():
                raise FileNotFoundError(f"TLS {path_name} not found: {path_value}")
        
        logger.info("TLS configuration validated")
    
    def xǁBridgeManagerǁ_validate_tls_config__mutmut_15(self) -> None:
        """Validate TLS configuration for TCP_TLS mode."""
        if not all([
            self.tls_server_cert,
            self.tls_server_key,
            self.tls_ca_cert,
        ]):
            raise ValueError(
                "TCP_TLS mode requires server_cert, server_key, and ca_cert"
            )
        
        # Validate paths exist
        for path_name, path_value in [
            ("server_cert", self.tls_server_cert),
            ("server_key", self.tls_server_key),
            ("ca_cert", self.tls_ca_cert),
        ]:
            if not Path(path_value).exists():
                raise FileNotFoundError(None)
        
        logger.info("TLS configuration validated")
    
    def xǁBridgeManagerǁ_validate_tls_config__mutmut_16(self) -> None:
        """Validate TLS configuration for TCP_TLS mode."""
        if not all([
            self.tls_server_cert,
            self.tls_server_key,
            self.tls_ca_cert,
        ]):
            raise ValueError(
                "TCP_TLS mode requires server_cert, server_key, and ca_cert"
            )
        
        # Validate paths exist
        for path_name, path_value in [
            ("server_cert", self.tls_server_cert),
            ("server_key", self.tls_server_key),
            ("ca_cert", self.tls_ca_cert),
        ]:
            if not Path(path_value).exists():
                raise FileNotFoundError(f"TLS {path_name} not found: {path_value}")
        
        logger.info(None)
    
    def xǁBridgeManagerǁ_validate_tls_config__mutmut_17(self) -> None:
        """Validate TLS configuration for TCP_TLS mode."""
        if not all([
            self.tls_server_cert,
            self.tls_server_key,
            self.tls_ca_cert,
        ]):
            raise ValueError(
                "TCP_TLS mode requires server_cert, server_key, and ca_cert"
            )
        
        # Validate paths exist
        for path_name, path_value in [
            ("server_cert", self.tls_server_cert),
            ("server_key", self.tls_server_key),
            ("ca_cert", self.tls_ca_cert),
        ]:
            if not Path(path_value).exists():
                raise FileNotFoundError(f"TLS {path_name} not found: {path_value}")
        
        logger.info("XXTLS configuration validatedXX")
    
    def xǁBridgeManagerǁ_validate_tls_config__mutmut_18(self) -> None:
        """Validate TLS configuration for TCP_TLS mode."""
        if not all([
            self.tls_server_cert,
            self.tls_server_key,
            self.tls_ca_cert,
        ]):
            raise ValueError(
                "TCP_TLS mode requires server_cert, server_key, and ca_cert"
            )
        
        # Validate paths exist
        for path_name, path_value in [
            ("server_cert", self.tls_server_cert),
            ("server_key", self.tls_server_key),
            ("ca_cert", self.tls_ca_cert),
        ]:
            if not Path(path_value).exists():
                raise FileNotFoundError(f"TLS {path_name} not found: {path_value}")
        
        logger.info("tls configuration validated")
    
    def xǁBridgeManagerǁ_validate_tls_config__mutmut_19(self) -> None:
        """Validate TLS configuration for TCP_TLS mode."""
        if not all([
            self.tls_server_cert,
            self.tls_server_key,
            self.tls_ca_cert,
        ]):
            raise ValueError(
                "TCP_TLS mode requires server_cert, server_key, and ca_cert"
            )
        
        # Validate paths exist
        for path_name, path_value in [
            ("server_cert", self.tls_server_cert),
            ("server_key", self.tls_server_key),
            ("ca_cert", self.tls_ca_cert),
        ]:
            if not Path(path_value).exists():
                raise FileNotFoundError(f"TLS {path_name} not found: {path_value}")
        
        logger.info("TLS CONFIGURATION VALIDATED")
    
    xǁBridgeManagerǁ_validate_tls_config__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁBridgeManagerǁ_validate_tls_config__mutmut_1': xǁBridgeManagerǁ_validate_tls_config__mutmut_1, 
        'xǁBridgeManagerǁ_validate_tls_config__mutmut_2': xǁBridgeManagerǁ_validate_tls_config__mutmut_2, 
        'xǁBridgeManagerǁ_validate_tls_config__mutmut_3': xǁBridgeManagerǁ_validate_tls_config__mutmut_3, 
        'xǁBridgeManagerǁ_validate_tls_config__mutmut_4': xǁBridgeManagerǁ_validate_tls_config__mutmut_4, 
        'xǁBridgeManagerǁ_validate_tls_config__mutmut_5': xǁBridgeManagerǁ_validate_tls_config__mutmut_5, 
        'xǁBridgeManagerǁ_validate_tls_config__mutmut_6': xǁBridgeManagerǁ_validate_tls_config__mutmut_6, 
        'xǁBridgeManagerǁ_validate_tls_config__mutmut_7': xǁBridgeManagerǁ_validate_tls_config__mutmut_7, 
        'xǁBridgeManagerǁ_validate_tls_config__mutmut_8': xǁBridgeManagerǁ_validate_tls_config__mutmut_8, 
        'xǁBridgeManagerǁ_validate_tls_config__mutmut_9': xǁBridgeManagerǁ_validate_tls_config__mutmut_9, 
        'xǁBridgeManagerǁ_validate_tls_config__mutmut_10': xǁBridgeManagerǁ_validate_tls_config__mutmut_10, 
        'xǁBridgeManagerǁ_validate_tls_config__mutmut_11': xǁBridgeManagerǁ_validate_tls_config__mutmut_11, 
        'xǁBridgeManagerǁ_validate_tls_config__mutmut_12': xǁBridgeManagerǁ_validate_tls_config__mutmut_12, 
        'xǁBridgeManagerǁ_validate_tls_config__mutmut_13': xǁBridgeManagerǁ_validate_tls_config__mutmut_13, 
        'xǁBridgeManagerǁ_validate_tls_config__mutmut_14': xǁBridgeManagerǁ_validate_tls_config__mutmut_14, 
        'xǁBridgeManagerǁ_validate_tls_config__mutmut_15': xǁBridgeManagerǁ_validate_tls_config__mutmut_15, 
        'xǁBridgeManagerǁ_validate_tls_config__mutmut_16': xǁBridgeManagerǁ_validate_tls_config__mutmut_16, 
        'xǁBridgeManagerǁ_validate_tls_config__mutmut_17': xǁBridgeManagerǁ_validate_tls_config__mutmut_17, 
        'xǁBridgeManagerǁ_validate_tls_config__mutmut_18': xǁBridgeManagerǁ_validate_tls_config__mutmut_18, 
        'xǁBridgeManagerǁ_validate_tls_config__mutmut_19': xǁBridgeManagerǁ_validate_tls_config__mutmut_19
    }
    
    def _validate_tls_config(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁBridgeManagerǁ_validate_tls_config__mutmut_orig"), object.__getattribute__(self, "xǁBridgeManagerǁ_validate_tls_config__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _validate_tls_config.__signature__ = _mutmut_signature(xǁBridgeManagerǁ_validate_tls_config__mutmut_orig)
    xǁBridgeManagerǁ_validate_tls_config__mutmut_orig.__name__ = 'xǁBridgeManagerǁ_validate_tls_config'
    
    def xǁBridgeManagerǁ_create_tls_server_context__mutmut_orig(self) -> ssl.SSLContext:
        """Create TLS context for server mode."""
        if self._tls_context is None:
            self._tls_context = create_server_context(
                cert_path=self.tls_server_cert,
                key_path=self.tls_server_key,
                ca_path=self.tls_ca_cert,
                require_client_cert=self.tls_require_client_cert,
            )
            logger.info("TLS server context created")
        return self._tls_context
    
    def xǁBridgeManagerǁ_create_tls_server_context__mutmut_1(self) -> ssl.SSLContext:
        """Create TLS context for server mode."""
        if self._tls_context is not None:
            self._tls_context = create_server_context(
                cert_path=self.tls_server_cert,
                key_path=self.tls_server_key,
                ca_path=self.tls_ca_cert,
                require_client_cert=self.tls_require_client_cert,
            )
            logger.info("TLS server context created")
        return self._tls_context
    
    def xǁBridgeManagerǁ_create_tls_server_context__mutmut_2(self) -> ssl.SSLContext:
        """Create TLS context for server mode."""
        if self._tls_context is None:
            self._tls_context = None
            logger.info("TLS server context created")
        return self._tls_context
    
    def xǁBridgeManagerǁ_create_tls_server_context__mutmut_3(self) -> ssl.SSLContext:
        """Create TLS context for server mode."""
        if self._tls_context is None:
            self._tls_context = create_server_context(
                cert_path=None,
                key_path=self.tls_server_key,
                ca_path=self.tls_ca_cert,
                require_client_cert=self.tls_require_client_cert,
            )
            logger.info("TLS server context created")
        return self._tls_context
    
    def xǁBridgeManagerǁ_create_tls_server_context__mutmut_4(self) -> ssl.SSLContext:
        """Create TLS context for server mode."""
        if self._tls_context is None:
            self._tls_context = create_server_context(
                cert_path=self.tls_server_cert,
                key_path=None,
                ca_path=self.tls_ca_cert,
                require_client_cert=self.tls_require_client_cert,
            )
            logger.info("TLS server context created")
        return self._tls_context
    
    def xǁBridgeManagerǁ_create_tls_server_context__mutmut_5(self) -> ssl.SSLContext:
        """Create TLS context for server mode."""
        if self._tls_context is None:
            self._tls_context = create_server_context(
                cert_path=self.tls_server_cert,
                key_path=self.tls_server_key,
                ca_path=None,
                require_client_cert=self.tls_require_client_cert,
            )
            logger.info("TLS server context created")
        return self._tls_context
    
    def xǁBridgeManagerǁ_create_tls_server_context__mutmut_6(self) -> ssl.SSLContext:
        """Create TLS context for server mode."""
        if self._tls_context is None:
            self._tls_context = create_server_context(
                cert_path=self.tls_server_cert,
                key_path=self.tls_server_key,
                ca_path=self.tls_ca_cert,
                require_client_cert=None,
            )
            logger.info("TLS server context created")
        return self._tls_context
    
    def xǁBridgeManagerǁ_create_tls_server_context__mutmut_7(self) -> ssl.SSLContext:
        """Create TLS context for server mode."""
        if self._tls_context is None:
            self._tls_context = create_server_context(
                key_path=self.tls_server_key,
                ca_path=self.tls_ca_cert,
                require_client_cert=self.tls_require_client_cert,
            )
            logger.info("TLS server context created")
        return self._tls_context
    
    def xǁBridgeManagerǁ_create_tls_server_context__mutmut_8(self) -> ssl.SSLContext:
        """Create TLS context for server mode."""
        if self._tls_context is None:
            self._tls_context = create_server_context(
                cert_path=self.tls_server_cert,
                ca_path=self.tls_ca_cert,
                require_client_cert=self.tls_require_client_cert,
            )
            logger.info("TLS server context created")
        return self._tls_context
    
    def xǁBridgeManagerǁ_create_tls_server_context__mutmut_9(self) -> ssl.SSLContext:
        """Create TLS context for server mode."""
        if self._tls_context is None:
            self._tls_context = create_server_context(
                cert_path=self.tls_server_cert,
                key_path=self.tls_server_key,
                require_client_cert=self.tls_require_client_cert,
            )
            logger.info("TLS server context created")
        return self._tls_context
    
    def xǁBridgeManagerǁ_create_tls_server_context__mutmut_10(self) -> ssl.SSLContext:
        """Create TLS context for server mode."""
        if self._tls_context is None:
            self._tls_context = create_server_context(
                cert_path=self.tls_server_cert,
                key_path=self.tls_server_key,
                ca_path=self.tls_ca_cert,
                )
            logger.info("TLS server context created")
        return self._tls_context
    
    def xǁBridgeManagerǁ_create_tls_server_context__mutmut_11(self) -> ssl.SSLContext:
        """Create TLS context for server mode."""
        if self._tls_context is None:
            self._tls_context = create_server_context(
                cert_path=self.tls_server_cert,
                key_path=self.tls_server_key,
                ca_path=self.tls_ca_cert,
                require_client_cert=self.tls_require_client_cert,
            )
            logger.info(None)
        return self._tls_context
    
    def xǁBridgeManagerǁ_create_tls_server_context__mutmut_12(self) -> ssl.SSLContext:
        """Create TLS context for server mode."""
        if self._tls_context is None:
            self._tls_context = create_server_context(
                cert_path=self.tls_server_cert,
                key_path=self.tls_server_key,
                ca_path=self.tls_ca_cert,
                require_client_cert=self.tls_require_client_cert,
            )
            logger.info("XXTLS server context createdXX")
        return self._tls_context
    
    def xǁBridgeManagerǁ_create_tls_server_context__mutmut_13(self) -> ssl.SSLContext:
        """Create TLS context for server mode."""
        if self._tls_context is None:
            self._tls_context = create_server_context(
                cert_path=self.tls_server_cert,
                key_path=self.tls_server_key,
                ca_path=self.tls_ca_cert,
                require_client_cert=self.tls_require_client_cert,
            )
            logger.info("tls server context created")
        return self._tls_context
    
    def xǁBridgeManagerǁ_create_tls_server_context__mutmut_14(self) -> ssl.SSLContext:
        """Create TLS context for server mode."""
        if self._tls_context is None:
            self._tls_context = create_server_context(
                cert_path=self.tls_server_cert,
                key_path=self.tls_server_key,
                ca_path=self.tls_ca_cert,
                require_client_cert=self.tls_require_client_cert,
            )
            logger.info("TLS SERVER CONTEXT CREATED")
        return self._tls_context
    
    xǁBridgeManagerǁ_create_tls_server_context__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁBridgeManagerǁ_create_tls_server_context__mutmut_1': xǁBridgeManagerǁ_create_tls_server_context__mutmut_1, 
        'xǁBridgeManagerǁ_create_tls_server_context__mutmut_2': xǁBridgeManagerǁ_create_tls_server_context__mutmut_2, 
        'xǁBridgeManagerǁ_create_tls_server_context__mutmut_3': xǁBridgeManagerǁ_create_tls_server_context__mutmut_3, 
        'xǁBridgeManagerǁ_create_tls_server_context__mutmut_4': xǁBridgeManagerǁ_create_tls_server_context__mutmut_4, 
        'xǁBridgeManagerǁ_create_tls_server_context__mutmut_5': xǁBridgeManagerǁ_create_tls_server_context__mutmut_5, 
        'xǁBridgeManagerǁ_create_tls_server_context__mutmut_6': xǁBridgeManagerǁ_create_tls_server_context__mutmut_6, 
        'xǁBridgeManagerǁ_create_tls_server_context__mutmut_7': xǁBridgeManagerǁ_create_tls_server_context__mutmut_7, 
        'xǁBridgeManagerǁ_create_tls_server_context__mutmut_8': xǁBridgeManagerǁ_create_tls_server_context__mutmut_8, 
        'xǁBridgeManagerǁ_create_tls_server_context__mutmut_9': xǁBridgeManagerǁ_create_tls_server_context__mutmut_9, 
        'xǁBridgeManagerǁ_create_tls_server_context__mutmut_10': xǁBridgeManagerǁ_create_tls_server_context__mutmut_10, 
        'xǁBridgeManagerǁ_create_tls_server_context__mutmut_11': xǁBridgeManagerǁ_create_tls_server_context__mutmut_11, 
        'xǁBridgeManagerǁ_create_tls_server_context__mutmut_12': xǁBridgeManagerǁ_create_tls_server_context__mutmut_12, 
        'xǁBridgeManagerǁ_create_tls_server_context__mutmut_13': xǁBridgeManagerǁ_create_tls_server_context__mutmut_13, 
        'xǁBridgeManagerǁ_create_tls_server_context__mutmut_14': xǁBridgeManagerǁ_create_tls_server_context__mutmut_14
    }
    
    def _create_tls_server_context(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁBridgeManagerǁ_create_tls_server_context__mutmut_orig"), object.__getattribute__(self, "xǁBridgeManagerǁ_create_tls_server_context__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _create_tls_server_context.__signature__ = _mutmut_signature(xǁBridgeManagerǁ_create_tls_server_context__mutmut_orig)
    xǁBridgeManagerǁ_create_tls_server_context__mutmut_orig.__name__ = 'xǁBridgeManagerǁ_create_tls_server_context'
    
    def xǁBridgeManagerǁ_create_tls_client_context__mutmut_orig(self) -> ssl.SSLContext:
        """Create TLS context for client mode."""
        if not all([self.tls_client_cert, self.tls_client_key, self.tls_ca_cert]):
            raise ValueError(
                "Client mode requires client_cert, client_key, and ca_cert"
            )
        
        return create_client_context(
            cert_path=self.tls_client_cert,
            key_path=self.tls_client_key,
            ca_path=self.tls_ca_cert,
            check_hostname=False,  # Internal bridge, no hostname verification
        )
    
    def xǁBridgeManagerǁ_create_tls_client_context__mutmut_1(self) -> ssl.SSLContext:
        """Create TLS context for client mode."""
        if all([self.tls_client_cert, self.tls_client_key, self.tls_ca_cert]):
            raise ValueError(
                "Client mode requires client_cert, client_key, and ca_cert"
            )
        
        return create_client_context(
            cert_path=self.tls_client_cert,
            key_path=self.tls_client_key,
            ca_path=self.tls_ca_cert,
            check_hostname=False,  # Internal bridge, no hostname verification
        )
    
    def xǁBridgeManagerǁ_create_tls_client_context__mutmut_2(self) -> ssl.SSLContext:
        """Create TLS context for client mode."""
        if not all(None):
            raise ValueError(
                "Client mode requires client_cert, client_key, and ca_cert"
            )
        
        return create_client_context(
            cert_path=self.tls_client_cert,
            key_path=self.tls_client_key,
            ca_path=self.tls_ca_cert,
            check_hostname=False,  # Internal bridge, no hostname verification
        )
    
    def xǁBridgeManagerǁ_create_tls_client_context__mutmut_3(self) -> ssl.SSLContext:
        """Create TLS context for client mode."""
        if not all([self.tls_client_cert, self.tls_client_key, self.tls_ca_cert]):
            raise ValueError(
                None
            )
        
        return create_client_context(
            cert_path=self.tls_client_cert,
            key_path=self.tls_client_key,
            ca_path=self.tls_ca_cert,
            check_hostname=False,  # Internal bridge, no hostname verification
        )
    
    def xǁBridgeManagerǁ_create_tls_client_context__mutmut_4(self) -> ssl.SSLContext:
        """Create TLS context for client mode."""
        if not all([self.tls_client_cert, self.tls_client_key, self.tls_ca_cert]):
            raise ValueError(
                "XXClient mode requires client_cert, client_key, and ca_certXX"
            )
        
        return create_client_context(
            cert_path=self.tls_client_cert,
            key_path=self.tls_client_key,
            ca_path=self.tls_ca_cert,
            check_hostname=False,  # Internal bridge, no hostname verification
        )
    
    def xǁBridgeManagerǁ_create_tls_client_context__mutmut_5(self) -> ssl.SSLContext:
        """Create TLS context for client mode."""
        if not all([self.tls_client_cert, self.tls_client_key, self.tls_ca_cert]):
            raise ValueError(
                "client mode requires client_cert, client_key, and ca_cert"
            )
        
        return create_client_context(
            cert_path=self.tls_client_cert,
            key_path=self.tls_client_key,
            ca_path=self.tls_ca_cert,
            check_hostname=False,  # Internal bridge, no hostname verification
        )
    
    def xǁBridgeManagerǁ_create_tls_client_context__mutmut_6(self) -> ssl.SSLContext:
        """Create TLS context for client mode."""
        if not all([self.tls_client_cert, self.tls_client_key, self.tls_ca_cert]):
            raise ValueError(
                "CLIENT MODE REQUIRES CLIENT_CERT, CLIENT_KEY, AND CA_CERT"
            )
        
        return create_client_context(
            cert_path=self.tls_client_cert,
            key_path=self.tls_client_key,
            ca_path=self.tls_ca_cert,
            check_hostname=False,  # Internal bridge, no hostname verification
        )
    
    def xǁBridgeManagerǁ_create_tls_client_context__mutmut_7(self) -> ssl.SSLContext:
        """Create TLS context for client mode."""
        if not all([self.tls_client_cert, self.tls_client_key, self.tls_ca_cert]):
            raise ValueError(
                "Client mode requires client_cert, client_key, and ca_cert"
            )
        
        return create_client_context(
            cert_path=None,
            key_path=self.tls_client_key,
            ca_path=self.tls_ca_cert,
            check_hostname=False,  # Internal bridge, no hostname verification
        )
    
    def xǁBridgeManagerǁ_create_tls_client_context__mutmut_8(self) -> ssl.SSLContext:
        """Create TLS context for client mode."""
        if not all([self.tls_client_cert, self.tls_client_key, self.tls_ca_cert]):
            raise ValueError(
                "Client mode requires client_cert, client_key, and ca_cert"
            )
        
        return create_client_context(
            cert_path=self.tls_client_cert,
            key_path=None,
            ca_path=self.tls_ca_cert,
            check_hostname=False,  # Internal bridge, no hostname verification
        )
    
    def xǁBridgeManagerǁ_create_tls_client_context__mutmut_9(self) -> ssl.SSLContext:
        """Create TLS context for client mode."""
        if not all([self.tls_client_cert, self.tls_client_key, self.tls_ca_cert]):
            raise ValueError(
                "Client mode requires client_cert, client_key, and ca_cert"
            )
        
        return create_client_context(
            cert_path=self.tls_client_cert,
            key_path=self.tls_client_key,
            ca_path=None,
            check_hostname=False,  # Internal bridge, no hostname verification
        )
    
    def xǁBridgeManagerǁ_create_tls_client_context__mutmut_10(self) -> ssl.SSLContext:
        """Create TLS context for client mode."""
        if not all([self.tls_client_cert, self.tls_client_key, self.tls_ca_cert]):
            raise ValueError(
                "Client mode requires client_cert, client_key, and ca_cert"
            )
        
        return create_client_context(
            cert_path=self.tls_client_cert,
            key_path=self.tls_client_key,
            ca_path=self.tls_ca_cert,
            check_hostname=None,  # Internal bridge, no hostname verification
        )
    
    def xǁBridgeManagerǁ_create_tls_client_context__mutmut_11(self) -> ssl.SSLContext:
        """Create TLS context for client mode."""
        if not all([self.tls_client_cert, self.tls_client_key, self.tls_ca_cert]):
            raise ValueError(
                "Client mode requires client_cert, client_key, and ca_cert"
            )
        
        return create_client_context(
            key_path=self.tls_client_key,
            ca_path=self.tls_ca_cert,
            check_hostname=False,  # Internal bridge, no hostname verification
        )
    
    def xǁBridgeManagerǁ_create_tls_client_context__mutmut_12(self) -> ssl.SSLContext:
        """Create TLS context for client mode."""
        if not all([self.tls_client_cert, self.tls_client_key, self.tls_ca_cert]):
            raise ValueError(
                "Client mode requires client_cert, client_key, and ca_cert"
            )
        
        return create_client_context(
            cert_path=self.tls_client_cert,
            ca_path=self.tls_ca_cert,
            check_hostname=False,  # Internal bridge, no hostname verification
        )
    
    def xǁBridgeManagerǁ_create_tls_client_context__mutmut_13(self) -> ssl.SSLContext:
        """Create TLS context for client mode."""
        if not all([self.tls_client_cert, self.tls_client_key, self.tls_ca_cert]):
            raise ValueError(
                "Client mode requires client_cert, client_key, and ca_cert"
            )
        
        return create_client_context(
            cert_path=self.tls_client_cert,
            key_path=self.tls_client_key,
            check_hostname=False,  # Internal bridge, no hostname verification
        )
    
    def xǁBridgeManagerǁ_create_tls_client_context__mutmut_14(self) -> ssl.SSLContext:
        """Create TLS context for client mode."""
        if not all([self.tls_client_cert, self.tls_client_key, self.tls_ca_cert]):
            raise ValueError(
                "Client mode requires client_cert, client_key, and ca_cert"
            )
        
        return create_client_context(
            cert_path=self.tls_client_cert,
            key_path=self.tls_client_key,
            ca_path=self.tls_ca_cert,
            )
    
    def xǁBridgeManagerǁ_create_tls_client_context__mutmut_15(self) -> ssl.SSLContext:
        """Create TLS context for client mode."""
        if not all([self.tls_client_cert, self.tls_client_key, self.tls_ca_cert]):
            raise ValueError(
                "Client mode requires client_cert, client_key, and ca_cert"
            )
        
        return create_client_context(
            cert_path=self.tls_client_cert,
            key_path=self.tls_client_key,
            ca_path=self.tls_ca_cert,
            check_hostname=True,  # Internal bridge, no hostname verification
        )
    
    xǁBridgeManagerǁ_create_tls_client_context__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁBridgeManagerǁ_create_tls_client_context__mutmut_1': xǁBridgeManagerǁ_create_tls_client_context__mutmut_1, 
        'xǁBridgeManagerǁ_create_tls_client_context__mutmut_2': xǁBridgeManagerǁ_create_tls_client_context__mutmut_2, 
        'xǁBridgeManagerǁ_create_tls_client_context__mutmut_3': xǁBridgeManagerǁ_create_tls_client_context__mutmut_3, 
        'xǁBridgeManagerǁ_create_tls_client_context__mutmut_4': xǁBridgeManagerǁ_create_tls_client_context__mutmut_4, 
        'xǁBridgeManagerǁ_create_tls_client_context__mutmut_5': xǁBridgeManagerǁ_create_tls_client_context__mutmut_5, 
        'xǁBridgeManagerǁ_create_tls_client_context__mutmut_6': xǁBridgeManagerǁ_create_tls_client_context__mutmut_6, 
        'xǁBridgeManagerǁ_create_tls_client_context__mutmut_7': xǁBridgeManagerǁ_create_tls_client_context__mutmut_7, 
        'xǁBridgeManagerǁ_create_tls_client_context__mutmut_8': xǁBridgeManagerǁ_create_tls_client_context__mutmut_8, 
        'xǁBridgeManagerǁ_create_tls_client_context__mutmut_9': xǁBridgeManagerǁ_create_tls_client_context__mutmut_9, 
        'xǁBridgeManagerǁ_create_tls_client_context__mutmut_10': xǁBridgeManagerǁ_create_tls_client_context__mutmut_10, 
        'xǁBridgeManagerǁ_create_tls_client_context__mutmut_11': xǁBridgeManagerǁ_create_tls_client_context__mutmut_11, 
        'xǁBridgeManagerǁ_create_tls_client_context__mutmut_12': xǁBridgeManagerǁ_create_tls_client_context__mutmut_12, 
        'xǁBridgeManagerǁ_create_tls_client_context__mutmut_13': xǁBridgeManagerǁ_create_tls_client_context__mutmut_13, 
        'xǁBridgeManagerǁ_create_tls_client_context__mutmut_14': xǁBridgeManagerǁ_create_tls_client_context__mutmut_14, 
        'xǁBridgeManagerǁ_create_tls_client_context__mutmut_15': xǁBridgeManagerǁ_create_tls_client_context__mutmut_15
    }
    
    def _create_tls_client_context(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁBridgeManagerǁ_create_tls_client_context__mutmut_orig"), object.__getattribute__(self, "xǁBridgeManagerǁ_create_tls_client_context__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _create_tls_client_context.__signature__ = _mutmut_signature(xǁBridgeManagerǁ_create_tls_client_context__mutmut_orig)
    xǁBridgeManagerǁ_create_tls_client_context__mutmut_orig.__name__ = 'xǁBridgeManagerǁ_create_tls_client_context'
    
    def xǁBridgeManagerǁ_audit_log__mutmut_orig(self, event: str, details: Dict[str, Any]) -> None:
        """
        Write security audit log entry (PS-02).
        
        Args:
            event: Event type (e.g., "AUTH_SUCCESS", "AUTH_FAILURE", "MESSAGE_SENT")
            details: Event details dictionary
        """
        try:
            timestamp = datetime.now(UTC).isoformat()
            log_entry = {
                "timestamp": timestamp,
                "event": event,
                "pid": os.getpid(),
                "uid": os.getuid(),
                "details": details
            }
            
            with open(self.audit_file, 'a') as f:
                f.write(json.dumps(log_entry) + '\n')
                
        except Exception as e:
            logger.error(f"Failed to write audit log: {e}")
    
    def xǁBridgeManagerǁ_audit_log__mutmut_1(self, event: str, details: Dict[str, Any]) -> None:
        """
        Write security audit log entry (PS-02).
        
        Args:
            event: Event type (e.g., "AUTH_SUCCESS", "AUTH_FAILURE", "MESSAGE_SENT")
            details: Event details dictionary
        """
        try:
            timestamp = None
            log_entry = {
                "timestamp": timestamp,
                "event": event,
                "pid": os.getpid(),
                "uid": os.getuid(),
                "details": details
            }
            
            with open(self.audit_file, 'a') as f:
                f.write(json.dumps(log_entry) + '\n')
                
        except Exception as e:
            logger.error(f"Failed to write audit log: {e}")
    
    def xǁBridgeManagerǁ_audit_log__mutmut_2(self, event: str, details: Dict[str, Any]) -> None:
        """
        Write security audit log entry (PS-02).
        
        Args:
            event: Event type (e.g., "AUTH_SUCCESS", "AUTH_FAILURE", "MESSAGE_SENT")
            details: Event details dictionary
        """
        try:
            timestamp = datetime.now(None).isoformat()
            log_entry = {
                "timestamp": timestamp,
                "event": event,
                "pid": os.getpid(),
                "uid": os.getuid(),
                "details": details
            }
            
            with open(self.audit_file, 'a') as f:
                f.write(json.dumps(log_entry) + '\n')
                
        except Exception as e:
            logger.error(f"Failed to write audit log: {e}")
    
    def xǁBridgeManagerǁ_audit_log__mutmut_3(self, event: str, details: Dict[str, Any]) -> None:
        """
        Write security audit log entry (PS-02).
        
        Args:
            event: Event type (e.g., "AUTH_SUCCESS", "AUTH_FAILURE", "MESSAGE_SENT")
            details: Event details dictionary
        """
        try:
            timestamp = datetime.now(UTC).isoformat()
            log_entry = None
            
            with open(self.audit_file, 'a') as f:
                f.write(json.dumps(log_entry) + '\n')
                
        except Exception as e:
            logger.error(f"Failed to write audit log: {e}")
    
    def xǁBridgeManagerǁ_audit_log__mutmut_4(self, event: str, details: Dict[str, Any]) -> None:
        """
        Write security audit log entry (PS-02).
        
        Args:
            event: Event type (e.g., "AUTH_SUCCESS", "AUTH_FAILURE", "MESSAGE_SENT")
            details: Event details dictionary
        """
        try:
            timestamp = datetime.now(UTC).isoformat()
            log_entry = {
                "XXtimestampXX": timestamp,
                "event": event,
                "pid": os.getpid(),
                "uid": os.getuid(),
                "details": details
            }
            
            with open(self.audit_file, 'a') as f:
                f.write(json.dumps(log_entry) + '\n')
                
        except Exception as e:
            logger.error(f"Failed to write audit log: {e}")
    
    def xǁBridgeManagerǁ_audit_log__mutmut_5(self, event: str, details: Dict[str, Any]) -> None:
        """
        Write security audit log entry (PS-02).
        
        Args:
            event: Event type (e.g., "AUTH_SUCCESS", "AUTH_FAILURE", "MESSAGE_SENT")
            details: Event details dictionary
        """
        try:
            timestamp = datetime.now(UTC).isoformat()
            log_entry = {
                "TIMESTAMP": timestamp,
                "event": event,
                "pid": os.getpid(),
                "uid": os.getuid(),
                "details": details
            }
            
            with open(self.audit_file, 'a') as f:
                f.write(json.dumps(log_entry) + '\n')
                
        except Exception as e:
            logger.error(f"Failed to write audit log: {e}")
    
    def xǁBridgeManagerǁ_audit_log__mutmut_6(self, event: str, details: Dict[str, Any]) -> None:
        """
        Write security audit log entry (PS-02).
        
        Args:
            event: Event type (e.g., "AUTH_SUCCESS", "AUTH_FAILURE", "MESSAGE_SENT")
            details: Event details dictionary
        """
        try:
            timestamp = datetime.now(UTC).isoformat()
            log_entry = {
                "timestamp": timestamp,
                "XXeventXX": event,
                "pid": os.getpid(),
                "uid": os.getuid(),
                "details": details
            }
            
            with open(self.audit_file, 'a') as f:
                f.write(json.dumps(log_entry) + '\n')
                
        except Exception as e:
            logger.error(f"Failed to write audit log: {e}")
    
    def xǁBridgeManagerǁ_audit_log__mutmut_7(self, event: str, details: Dict[str, Any]) -> None:
        """
        Write security audit log entry (PS-02).
        
        Args:
            event: Event type (e.g., "AUTH_SUCCESS", "AUTH_FAILURE", "MESSAGE_SENT")
            details: Event details dictionary
        """
        try:
            timestamp = datetime.now(UTC).isoformat()
            log_entry = {
                "timestamp": timestamp,
                "EVENT": event,
                "pid": os.getpid(),
                "uid": os.getuid(),
                "details": details
            }
            
            with open(self.audit_file, 'a') as f:
                f.write(json.dumps(log_entry) + '\n')
                
        except Exception as e:
            logger.error(f"Failed to write audit log: {e}")
    
    def xǁBridgeManagerǁ_audit_log__mutmut_8(self, event: str, details: Dict[str, Any]) -> None:
        """
        Write security audit log entry (PS-02).
        
        Args:
            event: Event type (e.g., "AUTH_SUCCESS", "AUTH_FAILURE", "MESSAGE_SENT")
            details: Event details dictionary
        """
        try:
            timestamp = datetime.now(UTC).isoformat()
            log_entry = {
                "timestamp": timestamp,
                "event": event,
                "XXpidXX": os.getpid(),
                "uid": os.getuid(),
                "details": details
            }
            
            with open(self.audit_file, 'a') as f:
                f.write(json.dumps(log_entry) + '\n')
                
        except Exception as e:
            logger.error(f"Failed to write audit log: {e}")
    
    def xǁBridgeManagerǁ_audit_log__mutmut_9(self, event: str, details: Dict[str, Any]) -> None:
        """
        Write security audit log entry (PS-02).
        
        Args:
            event: Event type (e.g., "AUTH_SUCCESS", "AUTH_FAILURE", "MESSAGE_SENT")
            details: Event details dictionary
        """
        try:
            timestamp = datetime.now(UTC).isoformat()
            log_entry = {
                "timestamp": timestamp,
                "event": event,
                "PID": os.getpid(),
                "uid": os.getuid(),
                "details": details
            }
            
            with open(self.audit_file, 'a') as f:
                f.write(json.dumps(log_entry) + '\n')
                
        except Exception as e:
            logger.error(f"Failed to write audit log: {e}")
    
    def xǁBridgeManagerǁ_audit_log__mutmut_10(self, event: str, details: Dict[str, Any]) -> None:
        """
        Write security audit log entry (PS-02).
        
        Args:
            event: Event type (e.g., "AUTH_SUCCESS", "AUTH_FAILURE", "MESSAGE_SENT")
            details: Event details dictionary
        """
        try:
            timestamp = datetime.now(UTC).isoformat()
            log_entry = {
                "timestamp": timestamp,
                "event": event,
                "pid": os.getpid(),
                "XXuidXX": os.getuid(),
                "details": details
            }
            
            with open(self.audit_file, 'a') as f:
                f.write(json.dumps(log_entry) + '\n')
                
        except Exception as e:
            logger.error(f"Failed to write audit log: {e}")
    
    def xǁBridgeManagerǁ_audit_log__mutmut_11(self, event: str, details: Dict[str, Any]) -> None:
        """
        Write security audit log entry (PS-02).
        
        Args:
            event: Event type (e.g., "AUTH_SUCCESS", "AUTH_FAILURE", "MESSAGE_SENT")
            details: Event details dictionary
        """
        try:
            timestamp = datetime.now(UTC).isoformat()
            log_entry = {
                "timestamp": timestamp,
                "event": event,
                "pid": os.getpid(),
                "UID": os.getuid(),
                "details": details
            }
            
            with open(self.audit_file, 'a') as f:
                f.write(json.dumps(log_entry) + '\n')
                
        except Exception as e:
            logger.error(f"Failed to write audit log: {e}")
    
    def xǁBridgeManagerǁ_audit_log__mutmut_12(self, event: str, details: Dict[str, Any]) -> None:
        """
        Write security audit log entry (PS-02).
        
        Args:
            event: Event type (e.g., "AUTH_SUCCESS", "AUTH_FAILURE", "MESSAGE_SENT")
            details: Event details dictionary
        """
        try:
            timestamp = datetime.now(UTC).isoformat()
            log_entry = {
                "timestamp": timestamp,
                "event": event,
                "pid": os.getpid(),
                "uid": os.getuid(),
                "XXdetailsXX": details
            }
            
            with open(self.audit_file, 'a') as f:
                f.write(json.dumps(log_entry) + '\n')
                
        except Exception as e:
            logger.error(f"Failed to write audit log: {e}")
    
    def xǁBridgeManagerǁ_audit_log__mutmut_13(self, event: str, details: Dict[str, Any]) -> None:
        """
        Write security audit log entry (PS-02).
        
        Args:
            event: Event type (e.g., "AUTH_SUCCESS", "AUTH_FAILURE", "MESSAGE_SENT")
            details: Event details dictionary
        """
        try:
            timestamp = datetime.now(UTC).isoformat()
            log_entry = {
                "timestamp": timestamp,
                "event": event,
                "pid": os.getpid(),
                "uid": os.getuid(),
                "DETAILS": details
            }
            
            with open(self.audit_file, 'a') as f:
                f.write(json.dumps(log_entry) + '\n')
                
        except Exception as e:
            logger.error(f"Failed to write audit log: {e}")
    
    def xǁBridgeManagerǁ_audit_log__mutmut_14(self, event: str, details: Dict[str, Any]) -> None:
        """
        Write security audit log entry (PS-02).
        
        Args:
            event: Event type (e.g., "AUTH_SUCCESS", "AUTH_FAILURE", "MESSAGE_SENT")
            details: Event details dictionary
        """
        try:
            timestamp = datetime.now(UTC).isoformat()
            log_entry = {
                "timestamp": timestamp,
                "event": event,
                "pid": os.getpid(),
                "uid": os.getuid(),
                "details": details
            }
            
            with open(None, 'a') as f:
                f.write(json.dumps(log_entry) + '\n')
                
        except Exception as e:
            logger.error(f"Failed to write audit log: {e}")
    
    def xǁBridgeManagerǁ_audit_log__mutmut_15(self, event: str, details: Dict[str, Any]) -> None:
        """
        Write security audit log entry (PS-02).
        
        Args:
            event: Event type (e.g., "AUTH_SUCCESS", "AUTH_FAILURE", "MESSAGE_SENT")
            details: Event details dictionary
        """
        try:
            timestamp = datetime.now(UTC).isoformat()
            log_entry = {
                "timestamp": timestamp,
                "event": event,
                "pid": os.getpid(),
                "uid": os.getuid(),
                "details": details
            }
            
            with open(self.audit_file, None) as f:
                f.write(json.dumps(log_entry) + '\n')
                
        except Exception as e:
            logger.error(f"Failed to write audit log: {e}")
    
    def xǁBridgeManagerǁ_audit_log__mutmut_16(self, event: str, details: Dict[str, Any]) -> None:
        """
        Write security audit log entry (PS-02).
        
        Args:
            event: Event type (e.g., "AUTH_SUCCESS", "AUTH_FAILURE", "MESSAGE_SENT")
            details: Event details dictionary
        """
        try:
            timestamp = datetime.now(UTC).isoformat()
            log_entry = {
                "timestamp": timestamp,
                "event": event,
                "pid": os.getpid(),
                "uid": os.getuid(),
                "details": details
            }
            
            with open('a') as f:
                f.write(json.dumps(log_entry) + '\n')
                
        except Exception as e:
            logger.error(f"Failed to write audit log: {e}")
    
    def xǁBridgeManagerǁ_audit_log__mutmut_17(self, event: str, details: Dict[str, Any]) -> None:
        """
        Write security audit log entry (PS-02).
        
        Args:
            event: Event type (e.g., "AUTH_SUCCESS", "AUTH_FAILURE", "MESSAGE_SENT")
            details: Event details dictionary
        """
        try:
            timestamp = datetime.now(UTC).isoformat()
            log_entry = {
                "timestamp": timestamp,
                "event": event,
                "pid": os.getpid(),
                "uid": os.getuid(),
                "details": details
            }
            
            with open(self.audit_file, ) as f:
                f.write(json.dumps(log_entry) + '\n')
                
        except Exception as e:
            logger.error(f"Failed to write audit log: {e}")
    
    def xǁBridgeManagerǁ_audit_log__mutmut_18(self, event: str, details: Dict[str, Any]) -> None:
        """
        Write security audit log entry (PS-02).
        
        Args:
            event: Event type (e.g., "AUTH_SUCCESS", "AUTH_FAILURE", "MESSAGE_SENT")
            details: Event details dictionary
        """
        try:
            timestamp = datetime.now(UTC).isoformat()
            log_entry = {
                "timestamp": timestamp,
                "event": event,
                "pid": os.getpid(),
                "uid": os.getuid(),
                "details": details
            }
            
            with open(self.audit_file, 'XXaXX') as f:
                f.write(json.dumps(log_entry) + '\n')
                
        except Exception as e:
            logger.error(f"Failed to write audit log: {e}")
    
    def xǁBridgeManagerǁ_audit_log__mutmut_19(self, event: str, details: Dict[str, Any]) -> None:
        """
        Write security audit log entry (PS-02).
        
        Args:
            event: Event type (e.g., "AUTH_SUCCESS", "AUTH_FAILURE", "MESSAGE_SENT")
            details: Event details dictionary
        """
        try:
            timestamp = datetime.now(UTC).isoformat()
            log_entry = {
                "timestamp": timestamp,
                "event": event,
                "pid": os.getpid(),
                "uid": os.getuid(),
                "details": details
            }
            
            with open(self.audit_file, 'A') as f:
                f.write(json.dumps(log_entry) + '\n')
                
        except Exception as e:
            logger.error(f"Failed to write audit log: {e}")
    
    def xǁBridgeManagerǁ_audit_log__mutmut_20(self, event: str, details: Dict[str, Any]) -> None:
        """
        Write security audit log entry (PS-02).
        
        Args:
            event: Event type (e.g., "AUTH_SUCCESS", "AUTH_FAILURE", "MESSAGE_SENT")
            details: Event details dictionary
        """
        try:
            timestamp = datetime.now(UTC).isoformat()
            log_entry = {
                "timestamp": timestamp,
                "event": event,
                "pid": os.getpid(),
                "uid": os.getuid(),
                "details": details
            }
            
            with open(self.audit_file, 'a') as f:
                f.write(None)
                
        except Exception as e:
            logger.error(f"Failed to write audit log: {e}")
    
    def xǁBridgeManagerǁ_audit_log__mutmut_21(self, event: str, details: Dict[str, Any]) -> None:
        """
        Write security audit log entry (PS-02).
        
        Args:
            event: Event type (e.g., "AUTH_SUCCESS", "AUTH_FAILURE", "MESSAGE_SENT")
            details: Event details dictionary
        """
        try:
            timestamp = datetime.now(UTC).isoformat()
            log_entry = {
                "timestamp": timestamp,
                "event": event,
                "pid": os.getpid(),
                "uid": os.getuid(),
                "details": details
            }
            
            with open(self.audit_file, 'a') as f:
                f.write(json.dumps(log_entry) - '\n')
                
        except Exception as e:
            logger.error(f"Failed to write audit log: {e}")
    
    def xǁBridgeManagerǁ_audit_log__mutmut_22(self, event: str, details: Dict[str, Any]) -> None:
        """
        Write security audit log entry (PS-02).
        
        Args:
            event: Event type (e.g., "AUTH_SUCCESS", "AUTH_FAILURE", "MESSAGE_SENT")
            details: Event details dictionary
        """
        try:
            timestamp = datetime.now(UTC).isoformat()
            log_entry = {
                "timestamp": timestamp,
                "event": event,
                "pid": os.getpid(),
                "uid": os.getuid(),
                "details": details
            }
            
            with open(self.audit_file, 'a') as f:
                f.write(json.dumps(None) + '\n')
                
        except Exception as e:
            logger.error(f"Failed to write audit log: {e}")
    
    def xǁBridgeManagerǁ_audit_log__mutmut_23(self, event: str, details: Dict[str, Any]) -> None:
        """
        Write security audit log entry (PS-02).
        
        Args:
            event: Event type (e.g., "AUTH_SUCCESS", "AUTH_FAILURE", "MESSAGE_SENT")
            details: Event details dictionary
        """
        try:
            timestamp = datetime.now(UTC).isoformat()
            log_entry = {
                "timestamp": timestamp,
                "event": event,
                "pid": os.getpid(),
                "uid": os.getuid(),
                "details": details
            }
            
            with open(self.audit_file, 'a') as f:
                f.write(json.dumps(log_entry) + 'XX\nXX')
                
        except Exception as e:
            logger.error(f"Failed to write audit log: {e}")
    
    def xǁBridgeManagerǁ_audit_log__mutmut_24(self, event: str, details: Dict[str, Any]) -> None:
        """
        Write security audit log entry (PS-02).
        
        Args:
            event: Event type (e.g., "AUTH_SUCCESS", "AUTH_FAILURE", "MESSAGE_SENT")
            details: Event details dictionary
        """
        try:
            timestamp = datetime.now(UTC).isoformat()
            log_entry = {
                "timestamp": timestamp,
                "event": event,
                "pid": os.getpid(),
                "uid": os.getuid(),
                "details": details
            }
            
            with open(self.audit_file, 'a') as f:
                f.write(json.dumps(log_entry) + '\n')
                
        except Exception as e:
            logger.error(None)
    
    xǁBridgeManagerǁ_audit_log__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁBridgeManagerǁ_audit_log__mutmut_1': xǁBridgeManagerǁ_audit_log__mutmut_1, 
        'xǁBridgeManagerǁ_audit_log__mutmut_2': xǁBridgeManagerǁ_audit_log__mutmut_2, 
        'xǁBridgeManagerǁ_audit_log__mutmut_3': xǁBridgeManagerǁ_audit_log__mutmut_3, 
        'xǁBridgeManagerǁ_audit_log__mutmut_4': xǁBridgeManagerǁ_audit_log__mutmut_4, 
        'xǁBridgeManagerǁ_audit_log__mutmut_5': xǁBridgeManagerǁ_audit_log__mutmut_5, 
        'xǁBridgeManagerǁ_audit_log__mutmut_6': xǁBridgeManagerǁ_audit_log__mutmut_6, 
        'xǁBridgeManagerǁ_audit_log__mutmut_7': xǁBridgeManagerǁ_audit_log__mutmut_7, 
        'xǁBridgeManagerǁ_audit_log__mutmut_8': xǁBridgeManagerǁ_audit_log__mutmut_8, 
        'xǁBridgeManagerǁ_audit_log__mutmut_9': xǁBridgeManagerǁ_audit_log__mutmut_9, 
        'xǁBridgeManagerǁ_audit_log__mutmut_10': xǁBridgeManagerǁ_audit_log__mutmut_10, 
        'xǁBridgeManagerǁ_audit_log__mutmut_11': xǁBridgeManagerǁ_audit_log__mutmut_11, 
        'xǁBridgeManagerǁ_audit_log__mutmut_12': xǁBridgeManagerǁ_audit_log__mutmut_12, 
        'xǁBridgeManagerǁ_audit_log__mutmut_13': xǁBridgeManagerǁ_audit_log__mutmut_13, 
        'xǁBridgeManagerǁ_audit_log__mutmut_14': xǁBridgeManagerǁ_audit_log__mutmut_14, 
        'xǁBridgeManagerǁ_audit_log__mutmut_15': xǁBridgeManagerǁ_audit_log__mutmut_15, 
        'xǁBridgeManagerǁ_audit_log__mutmut_16': xǁBridgeManagerǁ_audit_log__mutmut_16, 
        'xǁBridgeManagerǁ_audit_log__mutmut_17': xǁBridgeManagerǁ_audit_log__mutmut_17, 
        'xǁBridgeManagerǁ_audit_log__mutmut_18': xǁBridgeManagerǁ_audit_log__mutmut_18, 
        'xǁBridgeManagerǁ_audit_log__mutmut_19': xǁBridgeManagerǁ_audit_log__mutmut_19, 
        'xǁBridgeManagerǁ_audit_log__mutmut_20': xǁBridgeManagerǁ_audit_log__mutmut_20, 
        'xǁBridgeManagerǁ_audit_log__mutmut_21': xǁBridgeManagerǁ_audit_log__mutmut_21, 
        'xǁBridgeManagerǁ_audit_log__mutmut_22': xǁBridgeManagerǁ_audit_log__mutmut_22, 
        'xǁBridgeManagerǁ_audit_log__mutmut_23': xǁBridgeManagerǁ_audit_log__mutmut_23, 
        'xǁBridgeManagerǁ_audit_log__mutmut_24': xǁBridgeManagerǁ_audit_log__mutmut_24
    }
    
    def _audit_log(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁBridgeManagerǁ_audit_log__mutmut_orig"), object.__getattribute__(self, "xǁBridgeManagerǁ_audit_log__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _audit_log.__signature__ = _mutmut_signature(xǁBridgeManagerǁ_audit_log__mutmut_orig)
    xǁBridgeManagerǁ_audit_log__mutmut_orig.__name__ = 'xǁBridgeManagerǁ_audit_log'
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_orig(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", {
                "reason": "missing_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log("AUTH_FAILURE", {
                "reason": "invalid_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("AUTH_SUCCESS", {
            "source": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_1(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", {
                "reason": "missing_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log("AUTH_FAILURE", {
                "reason": "invalid_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("AUTH_SUCCESS", {
            "source": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_2(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return False
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", {
                "reason": "missing_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log("AUTH_FAILURE", {
                "reason": "invalid_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("AUTH_SUCCESS", {
            "source": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_3(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if message.auth_token:
            self._audit_log("AUTH_FAILURE", {
                "reason": "missing_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log("AUTH_FAILURE", {
                "reason": "invalid_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("AUTH_SUCCESS", {
            "source": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_4(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log(None, {
                "reason": "missing_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log("AUTH_FAILURE", {
                "reason": "invalid_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("AUTH_SUCCESS", {
            "source": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_5(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", None)
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log("AUTH_FAILURE", {
                "reason": "invalid_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("AUTH_SUCCESS", {
            "source": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_6(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log({
                "reason": "missing_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log("AUTH_FAILURE", {
                "reason": "invalid_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("AUTH_SUCCESS", {
            "source": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_7(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", )
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log("AUTH_FAILURE", {
                "reason": "invalid_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("AUTH_SUCCESS", {
            "source": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_8(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("XXAUTH_FAILUREXX", {
                "reason": "missing_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log("AUTH_FAILURE", {
                "reason": "invalid_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("AUTH_SUCCESS", {
            "source": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_9(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("auth_failure", {
                "reason": "missing_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log("AUTH_FAILURE", {
                "reason": "invalid_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("AUTH_SUCCESS", {
            "source": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_10(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", {
                "XXreasonXX": "missing_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log("AUTH_FAILURE", {
                "reason": "invalid_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("AUTH_SUCCESS", {
            "source": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_11(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", {
                "REASON": "missing_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log("AUTH_FAILURE", {
                "reason": "invalid_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("AUTH_SUCCESS", {
            "source": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_12(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", {
                "reason": "XXmissing_tokenXX",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log("AUTH_FAILURE", {
                "reason": "invalid_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("AUTH_SUCCESS", {
            "source": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_13(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", {
                "reason": "MISSING_TOKEN",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log("AUTH_FAILURE", {
                "reason": "invalid_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("AUTH_SUCCESS", {
            "source": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_14(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", {
                "reason": "missing_token",
                "XXsourceXX": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log("AUTH_FAILURE", {
                "reason": "invalid_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("AUTH_SUCCESS", {
            "source": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_15(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", {
                "reason": "missing_token",
                "SOURCE": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log("AUTH_FAILURE", {
                "reason": "invalid_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("AUTH_SUCCESS", {
            "source": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_16(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", {
                "reason": "missing_token",
                "source": message.source,
                "XXmessage_typeXX": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log("AUTH_FAILURE", {
                "reason": "invalid_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("AUTH_SUCCESS", {
            "source": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_17(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", {
                "reason": "missing_token",
                "source": message.source,
                "MESSAGE_TYPE": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log("AUTH_FAILURE", {
                "reason": "invalid_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("AUTH_SUCCESS", {
            "source": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_18(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", {
                "reason": "missing_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(None)
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log("AUTH_FAILURE", {
                "reason": "invalid_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("AUTH_SUCCESS", {
            "source": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_19(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", {
                "reason": "missing_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return True
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log("AUTH_FAILURE", {
                "reason": "invalid_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("AUTH_SUCCESS", {
            "source": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_20(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", {
                "reason": "missing_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log("AUTH_FAILURE", {
                "reason": "invalid_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("AUTH_SUCCESS", {
            "source": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_21(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", {
                "reason": "missing_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(None, message.auth_token):
            self._audit_log("AUTH_FAILURE", {
                "reason": "invalid_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("AUTH_SUCCESS", {
            "source": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_22(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", {
                "reason": "missing_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, None):
            self._audit_log("AUTH_FAILURE", {
                "reason": "invalid_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("AUTH_SUCCESS", {
            "source": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_23(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", {
                "reason": "missing_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(message.auth_token):
            self._audit_log("AUTH_FAILURE", {
                "reason": "invalid_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("AUTH_SUCCESS", {
            "source": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_24(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", {
                "reason": "missing_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, ):
            self._audit_log("AUTH_FAILURE", {
                "reason": "invalid_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("AUTH_SUCCESS", {
            "source": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_25(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", {
                "reason": "missing_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log(None, {
                "reason": "invalid_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("AUTH_SUCCESS", {
            "source": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_26(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", {
                "reason": "missing_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log("AUTH_FAILURE", None)
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("AUTH_SUCCESS", {
            "source": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_27(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", {
                "reason": "missing_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log({
                "reason": "invalid_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("AUTH_SUCCESS", {
            "source": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_28(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", {
                "reason": "missing_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log("AUTH_FAILURE", )
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("AUTH_SUCCESS", {
            "source": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_29(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", {
                "reason": "missing_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log("XXAUTH_FAILUREXX", {
                "reason": "invalid_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("AUTH_SUCCESS", {
            "source": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_30(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", {
                "reason": "missing_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log("auth_failure", {
                "reason": "invalid_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("AUTH_SUCCESS", {
            "source": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_31(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", {
                "reason": "missing_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log("AUTH_FAILURE", {
                "XXreasonXX": "invalid_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("AUTH_SUCCESS", {
            "source": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_32(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", {
                "reason": "missing_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log("AUTH_FAILURE", {
                "REASON": "invalid_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("AUTH_SUCCESS", {
            "source": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_33(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", {
                "reason": "missing_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log("AUTH_FAILURE", {
                "reason": "XXinvalid_tokenXX",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("AUTH_SUCCESS", {
            "source": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_34(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", {
                "reason": "missing_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log("AUTH_FAILURE", {
                "reason": "INVALID_TOKEN",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("AUTH_SUCCESS", {
            "source": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_35(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", {
                "reason": "missing_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log("AUTH_FAILURE", {
                "reason": "invalid_token",
                "XXsourceXX": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("AUTH_SUCCESS", {
            "source": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_36(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", {
                "reason": "missing_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log("AUTH_FAILURE", {
                "reason": "invalid_token",
                "SOURCE": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("AUTH_SUCCESS", {
            "source": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_37(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", {
                "reason": "missing_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log("AUTH_FAILURE", {
                "reason": "invalid_token",
                "source": message.source,
                "XXmessage_typeXX": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("AUTH_SUCCESS", {
            "source": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_38(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", {
                "reason": "missing_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log("AUTH_FAILURE", {
                "reason": "invalid_token",
                "source": message.source,
                "MESSAGE_TYPE": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("AUTH_SUCCESS", {
            "source": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_39(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", {
                "reason": "missing_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log("AUTH_FAILURE", {
                "reason": "invalid_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(None)
            return False
        
        self._audit_log("AUTH_SUCCESS", {
            "source": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_40(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", {
                "reason": "missing_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log("AUTH_FAILURE", {
                "reason": "invalid_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return True
        
        self._audit_log("AUTH_SUCCESS", {
            "source": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_41(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", {
                "reason": "missing_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log("AUTH_FAILURE", {
                "reason": "invalid_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log(None, {
            "source": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_42(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", {
                "reason": "missing_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log("AUTH_FAILURE", {
                "reason": "invalid_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("AUTH_SUCCESS", None)
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_43(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", {
                "reason": "missing_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log("AUTH_FAILURE", {
                "reason": "invalid_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log({
            "source": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_44(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", {
                "reason": "missing_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log("AUTH_FAILURE", {
                "reason": "invalid_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("AUTH_SUCCESS", )
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_45(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", {
                "reason": "missing_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log("AUTH_FAILURE", {
                "reason": "invalid_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("XXAUTH_SUCCESSXX", {
            "source": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_46(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", {
                "reason": "missing_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log("AUTH_FAILURE", {
                "reason": "invalid_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("auth_success", {
            "source": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_47(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", {
                "reason": "missing_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log("AUTH_FAILURE", {
                "reason": "invalid_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("AUTH_SUCCESS", {
            "XXsourceXX": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_48(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", {
                "reason": "missing_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log("AUTH_FAILURE", {
                "reason": "invalid_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("AUTH_SUCCESS", {
            "SOURCE": message.source,
            "message_type": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_49(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", {
                "reason": "missing_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log("AUTH_FAILURE", {
                "reason": "invalid_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("AUTH_SUCCESS", {
            "source": message.source,
            "XXmessage_typeXX": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_50(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", {
                "reason": "missing_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log("AUTH_FAILURE", {
                "reason": "invalid_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("AUTH_SUCCESS", {
            "source": message.source,
            "MESSAGE_TYPE": message.message_type
        })
        return True
    
    def xǁBridgeManagerǁ_verify_auth_token__mutmut_51(self, message: ContextMessage) -> bool:
        """
        Verify authentication token (PS-02).
        
        Args:
            message: Message to authenticate
            
        Returns:
            True if authentication passed, False otherwise
        """
        if not self.require_auth:
            return True
        
        if not message.auth_token:
            self._audit_log("AUTH_FAILURE", {
                "reason": "missing_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: missing token from {message.source}")
            return False
        
        # Compare tokens directly using constant-time comparison to prevent timing attacks
        # Note: secrets.compare_digest requires same-length inputs for security
        if not secrets.compare_digest(self.auth_token, message.auth_token):
            self._audit_log("AUTH_FAILURE", {
                "reason": "invalid_token",
                "source": message.source,
                "message_type": message.message_type
            })
            logger.warning(f"Authentication failed: invalid token from {message.source}")
            return False
        
        self._audit_log("AUTH_SUCCESS", {
            "source": message.source,
            "message_type": message.message_type
        })
        return False
    
    xǁBridgeManagerǁ_verify_auth_token__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁBridgeManagerǁ_verify_auth_token__mutmut_1': xǁBridgeManagerǁ_verify_auth_token__mutmut_1, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_2': xǁBridgeManagerǁ_verify_auth_token__mutmut_2, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_3': xǁBridgeManagerǁ_verify_auth_token__mutmut_3, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_4': xǁBridgeManagerǁ_verify_auth_token__mutmut_4, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_5': xǁBridgeManagerǁ_verify_auth_token__mutmut_5, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_6': xǁBridgeManagerǁ_verify_auth_token__mutmut_6, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_7': xǁBridgeManagerǁ_verify_auth_token__mutmut_7, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_8': xǁBridgeManagerǁ_verify_auth_token__mutmut_8, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_9': xǁBridgeManagerǁ_verify_auth_token__mutmut_9, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_10': xǁBridgeManagerǁ_verify_auth_token__mutmut_10, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_11': xǁBridgeManagerǁ_verify_auth_token__mutmut_11, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_12': xǁBridgeManagerǁ_verify_auth_token__mutmut_12, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_13': xǁBridgeManagerǁ_verify_auth_token__mutmut_13, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_14': xǁBridgeManagerǁ_verify_auth_token__mutmut_14, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_15': xǁBridgeManagerǁ_verify_auth_token__mutmut_15, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_16': xǁBridgeManagerǁ_verify_auth_token__mutmut_16, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_17': xǁBridgeManagerǁ_verify_auth_token__mutmut_17, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_18': xǁBridgeManagerǁ_verify_auth_token__mutmut_18, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_19': xǁBridgeManagerǁ_verify_auth_token__mutmut_19, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_20': xǁBridgeManagerǁ_verify_auth_token__mutmut_20, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_21': xǁBridgeManagerǁ_verify_auth_token__mutmut_21, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_22': xǁBridgeManagerǁ_verify_auth_token__mutmut_22, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_23': xǁBridgeManagerǁ_verify_auth_token__mutmut_23, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_24': xǁBridgeManagerǁ_verify_auth_token__mutmut_24, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_25': xǁBridgeManagerǁ_verify_auth_token__mutmut_25, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_26': xǁBridgeManagerǁ_verify_auth_token__mutmut_26, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_27': xǁBridgeManagerǁ_verify_auth_token__mutmut_27, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_28': xǁBridgeManagerǁ_verify_auth_token__mutmut_28, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_29': xǁBridgeManagerǁ_verify_auth_token__mutmut_29, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_30': xǁBridgeManagerǁ_verify_auth_token__mutmut_30, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_31': xǁBridgeManagerǁ_verify_auth_token__mutmut_31, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_32': xǁBridgeManagerǁ_verify_auth_token__mutmut_32, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_33': xǁBridgeManagerǁ_verify_auth_token__mutmut_33, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_34': xǁBridgeManagerǁ_verify_auth_token__mutmut_34, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_35': xǁBridgeManagerǁ_verify_auth_token__mutmut_35, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_36': xǁBridgeManagerǁ_verify_auth_token__mutmut_36, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_37': xǁBridgeManagerǁ_verify_auth_token__mutmut_37, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_38': xǁBridgeManagerǁ_verify_auth_token__mutmut_38, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_39': xǁBridgeManagerǁ_verify_auth_token__mutmut_39, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_40': xǁBridgeManagerǁ_verify_auth_token__mutmut_40, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_41': xǁBridgeManagerǁ_verify_auth_token__mutmut_41, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_42': xǁBridgeManagerǁ_verify_auth_token__mutmut_42, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_43': xǁBridgeManagerǁ_verify_auth_token__mutmut_43, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_44': xǁBridgeManagerǁ_verify_auth_token__mutmut_44, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_45': xǁBridgeManagerǁ_verify_auth_token__mutmut_45, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_46': xǁBridgeManagerǁ_verify_auth_token__mutmut_46, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_47': xǁBridgeManagerǁ_verify_auth_token__mutmut_47, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_48': xǁBridgeManagerǁ_verify_auth_token__mutmut_48, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_49': xǁBridgeManagerǁ_verify_auth_token__mutmut_49, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_50': xǁBridgeManagerǁ_verify_auth_token__mutmut_50, 
        'xǁBridgeManagerǁ_verify_auth_token__mutmut_51': xǁBridgeManagerǁ_verify_auth_token__mutmut_51
    }
    
    def _verify_auth_token(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁBridgeManagerǁ_verify_auth_token__mutmut_orig"), object.__getattribute__(self, "xǁBridgeManagerǁ_verify_auth_token__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _verify_auth_token.__signature__ = _mutmut_signature(xǁBridgeManagerǁ_verify_auth_token__mutmut_orig)
    xǁBridgeManagerǁ_verify_auth_token__mutmut_orig.__name__ = 'xǁBridgeManagerǁ_verify_auth_token'
    
    def xǁBridgeManagerǁwrite_message__mutmut_orig(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_1(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_2(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error(None)
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_3(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("XXInvalid message formatXX")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_4(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_5(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("INVALID MESSAGE FORMAT")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_6(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log(None, {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_7(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", None)
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_8(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log({
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_9(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", )
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_10(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("XXMESSAGE_INVALIDXX", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_11(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("message_invalid", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_12(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "XXsourceXX": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_13(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "SOURCE": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_14(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "XXmessage_typeXX": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_15(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "MESSAGE_TYPE": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_16(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return True
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_17(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_18(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(None):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_19(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return True
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_20(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(None):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_21(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode != BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_22(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = None
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_23(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(None)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_24(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode != BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_25(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = None
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_26(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(None)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_27(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = None
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_28(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = True
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_29(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log(None, {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_30(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", None)
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_31(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log({
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_32(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", )
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_33(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("XXMESSAGE_SENTXX", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_34(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("message_sent", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_35(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "XXsourceXX": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_36(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "SOURCE": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_37(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "XXmessage_typeXX": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_38(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "MESSAGE_TYPE": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_39(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "XXmodeXX": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_40(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "MODE": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_41(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(None)
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_42(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log(None, {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_43(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", None)
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_44(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log({
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_45(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", )
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_46(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("XXWRITE_TIMEOUTXX", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_47(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("write_timeout", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_48(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "XXerrorXX": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_49(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "ERROR": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_50(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(None),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_51(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "XXsourceXX": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_52(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "SOURCE": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_53(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return True
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_54(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(None)
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_55(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log(None, {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_56(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", None)
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_57(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log({
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_58(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", )
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_59(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("XXWRITE_ERRORXX", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_60(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("write_error", {
                "error": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_61(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "XXerrorXX": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_62(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "ERROR": str(e),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_63(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(None),
                "source": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_64(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "XXsourceXX": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_65(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "SOURCE": message.source
            })
            return False
    
    def xǁBridgeManagerǁwrite_message__mutmut_66(self, message: ContextMessage) -> bool:
        """
        Write a message to the bridge with locking and authentication (PS-02).
        
        Args:
            message: Context message to send
            
        Returns:
            True if write successful, False otherwise
        """
        if not message.validate():
            logger.error("Invalid message format")
            self._audit_log("MESSAGE_INVALID", {
                "source": message.source,
                "message_type": message.message_type
            })
            return False
        
        # Verify authentication token (PS-02)
        if not self._verify_auth_token(message):
            return False
        
        try:
            with bridge_lock(self.lock_path):
                if self.mode == BridgeMode.NAMED_PIPE:
                    result = self._write_to_pipe(message)
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    result = self._write_to_socket(message)
                else:
                    result = False
                
                if result:
                    self._audit_log("MESSAGE_SENT", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                
                return result
        
        except TimeoutError as e:
            logger.error(f"Bridge write timeout: {e}")
            self._audit_log("WRITE_TIMEOUT", {
                "error": str(e),
                "source": message.source
            })
            return False
        except Exception as e:
            logger.error(f"Bridge write error: {e}")
            self._audit_log("WRITE_ERROR", {
                "error": str(e),
                "source": message.source
            })
            return True
    
    xǁBridgeManagerǁwrite_message__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁBridgeManagerǁwrite_message__mutmut_1': xǁBridgeManagerǁwrite_message__mutmut_1, 
        'xǁBridgeManagerǁwrite_message__mutmut_2': xǁBridgeManagerǁwrite_message__mutmut_2, 
        'xǁBridgeManagerǁwrite_message__mutmut_3': xǁBridgeManagerǁwrite_message__mutmut_3, 
        'xǁBridgeManagerǁwrite_message__mutmut_4': xǁBridgeManagerǁwrite_message__mutmut_4, 
        'xǁBridgeManagerǁwrite_message__mutmut_5': xǁBridgeManagerǁwrite_message__mutmut_5, 
        'xǁBridgeManagerǁwrite_message__mutmut_6': xǁBridgeManagerǁwrite_message__mutmut_6, 
        'xǁBridgeManagerǁwrite_message__mutmut_7': xǁBridgeManagerǁwrite_message__mutmut_7, 
        'xǁBridgeManagerǁwrite_message__mutmut_8': xǁBridgeManagerǁwrite_message__mutmut_8, 
        'xǁBridgeManagerǁwrite_message__mutmut_9': xǁBridgeManagerǁwrite_message__mutmut_9, 
        'xǁBridgeManagerǁwrite_message__mutmut_10': xǁBridgeManagerǁwrite_message__mutmut_10, 
        'xǁBridgeManagerǁwrite_message__mutmut_11': xǁBridgeManagerǁwrite_message__mutmut_11, 
        'xǁBridgeManagerǁwrite_message__mutmut_12': xǁBridgeManagerǁwrite_message__mutmut_12, 
        'xǁBridgeManagerǁwrite_message__mutmut_13': xǁBridgeManagerǁwrite_message__mutmut_13, 
        'xǁBridgeManagerǁwrite_message__mutmut_14': xǁBridgeManagerǁwrite_message__mutmut_14, 
        'xǁBridgeManagerǁwrite_message__mutmut_15': xǁBridgeManagerǁwrite_message__mutmut_15, 
        'xǁBridgeManagerǁwrite_message__mutmut_16': xǁBridgeManagerǁwrite_message__mutmut_16, 
        'xǁBridgeManagerǁwrite_message__mutmut_17': xǁBridgeManagerǁwrite_message__mutmut_17, 
        'xǁBridgeManagerǁwrite_message__mutmut_18': xǁBridgeManagerǁwrite_message__mutmut_18, 
        'xǁBridgeManagerǁwrite_message__mutmut_19': xǁBridgeManagerǁwrite_message__mutmut_19, 
        'xǁBridgeManagerǁwrite_message__mutmut_20': xǁBridgeManagerǁwrite_message__mutmut_20, 
        'xǁBridgeManagerǁwrite_message__mutmut_21': xǁBridgeManagerǁwrite_message__mutmut_21, 
        'xǁBridgeManagerǁwrite_message__mutmut_22': xǁBridgeManagerǁwrite_message__mutmut_22, 
        'xǁBridgeManagerǁwrite_message__mutmut_23': xǁBridgeManagerǁwrite_message__mutmut_23, 
        'xǁBridgeManagerǁwrite_message__mutmut_24': xǁBridgeManagerǁwrite_message__mutmut_24, 
        'xǁBridgeManagerǁwrite_message__mutmut_25': xǁBridgeManagerǁwrite_message__mutmut_25, 
        'xǁBridgeManagerǁwrite_message__mutmut_26': xǁBridgeManagerǁwrite_message__mutmut_26, 
        'xǁBridgeManagerǁwrite_message__mutmut_27': xǁBridgeManagerǁwrite_message__mutmut_27, 
        'xǁBridgeManagerǁwrite_message__mutmut_28': xǁBridgeManagerǁwrite_message__mutmut_28, 
        'xǁBridgeManagerǁwrite_message__mutmut_29': xǁBridgeManagerǁwrite_message__mutmut_29, 
        'xǁBridgeManagerǁwrite_message__mutmut_30': xǁBridgeManagerǁwrite_message__mutmut_30, 
        'xǁBridgeManagerǁwrite_message__mutmut_31': xǁBridgeManagerǁwrite_message__mutmut_31, 
        'xǁBridgeManagerǁwrite_message__mutmut_32': xǁBridgeManagerǁwrite_message__mutmut_32, 
        'xǁBridgeManagerǁwrite_message__mutmut_33': xǁBridgeManagerǁwrite_message__mutmut_33, 
        'xǁBridgeManagerǁwrite_message__mutmut_34': xǁBridgeManagerǁwrite_message__mutmut_34, 
        'xǁBridgeManagerǁwrite_message__mutmut_35': xǁBridgeManagerǁwrite_message__mutmut_35, 
        'xǁBridgeManagerǁwrite_message__mutmut_36': xǁBridgeManagerǁwrite_message__mutmut_36, 
        'xǁBridgeManagerǁwrite_message__mutmut_37': xǁBridgeManagerǁwrite_message__mutmut_37, 
        'xǁBridgeManagerǁwrite_message__mutmut_38': xǁBridgeManagerǁwrite_message__mutmut_38, 
        'xǁBridgeManagerǁwrite_message__mutmut_39': xǁBridgeManagerǁwrite_message__mutmut_39, 
        'xǁBridgeManagerǁwrite_message__mutmut_40': xǁBridgeManagerǁwrite_message__mutmut_40, 
        'xǁBridgeManagerǁwrite_message__mutmut_41': xǁBridgeManagerǁwrite_message__mutmut_41, 
        'xǁBridgeManagerǁwrite_message__mutmut_42': xǁBridgeManagerǁwrite_message__mutmut_42, 
        'xǁBridgeManagerǁwrite_message__mutmut_43': xǁBridgeManagerǁwrite_message__mutmut_43, 
        'xǁBridgeManagerǁwrite_message__mutmut_44': xǁBridgeManagerǁwrite_message__mutmut_44, 
        'xǁBridgeManagerǁwrite_message__mutmut_45': xǁBridgeManagerǁwrite_message__mutmut_45, 
        'xǁBridgeManagerǁwrite_message__mutmut_46': xǁBridgeManagerǁwrite_message__mutmut_46, 
        'xǁBridgeManagerǁwrite_message__mutmut_47': xǁBridgeManagerǁwrite_message__mutmut_47, 
        'xǁBridgeManagerǁwrite_message__mutmut_48': xǁBridgeManagerǁwrite_message__mutmut_48, 
        'xǁBridgeManagerǁwrite_message__mutmut_49': xǁBridgeManagerǁwrite_message__mutmut_49, 
        'xǁBridgeManagerǁwrite_message__mutmut_50': xǁBridgeManagerǁwrite_message__mutmut_50, 
        'xǁBridgeManagerǁwrite_message__mutmut_51': xǁBridgeManagerǁwrite_message__mutmut_51, 
        'xǁBridgeManagerǁwrite_message__mutmut_52': xǁBridgeManagerǁwrite_message__mutmut_52, 
        'xǁBridgeManagerǁwrite_message__mutmut_53': xǁBridgeManagerǁwrite_message__mutmut_53, 
        'xǁBridgeManagerǁwrite_message__mutmut_54': xǁBridgeManagerǁwrite_message__mutmut_54, 
        'xǁBridgeManagerǁwrite_message__mutmut_55': xǁBridgeManagerǁwrite_message__mutmut_55, 
        'xǁBridgeManagerǁwrite_message__mutmut_56': xǁBridgeManagerǁwrite_message__mutmut_56, 
        'xǁBridgeManagerǁwrite_message__mutmut_57': xǁBridgeManagerǁwrite_message__mutmut_57, 
        'xǁBridgeManagerǁwrite_message__mutmut_58': xǁBridgeManagerǁwrite_message__mutmut_58, 
        'xǁBridgeManagerǁwrite_message__mutmut_59': xǁBridgeManagerǁwrite_message__mutmut_59, 
        'xǁBridgeManagerǁwrite_message__mutmut_60': xǁBridgeManagerǁwrite_message__mutmut_60, 
        'xǁBridgeManagerǁwrite_message__mutmut_61': xǁBridgeManagerǁwrite_message__mutmut_61, 
        'xǁBridgeManagerǁwrite_message__mutmut_62': xǁBridgeManagerǁwrite_message__mutmut_62, 
        'xǁBridgeManagerǁwrite_message__mutmut_63': xǁBridgeManagerǁwrite_message__mutmut_63, 
        'xǁBridgeManagerǁwrite_message__mutmut_64': xǁBridgeManagerǁwrite_message__mutmut_64, 
        'xǁBridgeManagerǁwrite_message__mutmut_65': xǁBridgeManagerǁwrite_message__mutmut_65, 
        'xǁBridgeManagerǁwrite_message__mutmut_66': xǁBridgeManagerǁwrite_message__mutmut_66
    }
    
    def write_message(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁBridgeManagerǁwrite_message__mutmut_orig"), object.__getattribute__(self, "xǁBridgeManagerǁwrite_message__mutmut_mutants"), args, kwargs, self)
        return result 
    
    write_message.__signature__ = _mutmut_signature(xǁBridgeManagerǁwrite_message__mutmut_orig)
    xǁBridgeManagerǁwrite_message__mutmut_orig.__name__ = 'xǁBridgeManagerǁwrite_message'
    
    def xǁBridgeManagerǁ_write_to_pipe__mutmut_orig(self, message: ContextMessage) -> bool:
        """Write message to named pipe with optional v2 protocol."""
        try:
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = message.to_json().encode('utf-8')
                payload = v2_encode(payload, compress=self.enable_compression)
                logger.debug(f"Using Protocol v2 (compressed={self.enable_compression})")
                
                # Binary mode for v2 protocol
                with open(self.pipe_path, 'wb') as pipe:
                    pipe.write(payload)
                    pipe.flush()
            else:
                # Text mode for v1 protocol compatibility
                with open(self.pipe_path, 'w') as pipe:
                    pipe.write(message.to_json())
                    pipe.write('\n')  # Message delimiter for v1
                    pipe.flush()
            
            logger.debug(f"Message written to pipe: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Pipe write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_pipe__mutmut_1(self, message: ContextMessage) -> bool:
        """Write message to named pipe with optional v2 protocol."""
        try:
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 or HAS_PROTOCOL_V2:
                payload = message.to_json().encode('utf-8')
                payload = v2_encode(payload, compress=self.enable_compression)
                logger.debug(f"Using Protocol v2 (compressed={self.enable_compression})")
                
                # Binary mode for v2 protocol
                with open(self.pipe_path, 'wb') as pipe:
                    pipe.write(payload)
                    pipe.flush()
            else:
                # Text mode for v1 protocol compatibility
                with open(self.pipe_path, 'w') as pipe:
                    pipe.write(message.to_json())
                    pipe.write('\n')  # Message delimiter for v1
                    pipe.flush()
            
            logger.debug(f"Message written to pipe: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Pipe write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_pipe__mutmut_2(self, message: ContextMessage) -> bool:
        """Write message to named pipe with optional v2 protocol."""
        try:
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = None
                payload = v2_encode(payload, compress=self.enable_compression)
                logger.debug(f"Using Protocol v2 (compressed={self.enable_compression})")
                
                # Binary mode for v2 protocol
                with open(self.pipe_path, 'wb') as pipe:
                    pipe.write(payload)
                    pipe.flush()
            else:
                # Text mode for v1 protocol compatibility
                with open(self.pipe_path, 'w') as pipe:
                    pipe.write(message.to_json())
                    pipe.write('\n')  # Message delimiter for v1
                    pipe.flush()
            
            logger.debug(f"Message written to pipe: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Pipe write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_pipe__mutmut_3(self, message: ContextMessage) -> bool:
        """Write message to named pipe with optional v2 protocol."""
        try:
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = message.to_json().encode(None)
                payload = v2_encode(payload, compress=self.enable_compression)
                logger.debug(f"Using Protocol v2 (compressed={self.enable_compression})")
                
                # Binary mode for v2 protocol
                with open(self.pipe_path, 'wb') as pipe:
                    pipe.write(payload)
                    pipe.flush()
            else:
                # Text mode for v1 protocol compatibility
                with open(self.pipe_path, 'w') as pipe:
                    pipe.write(message.to_json())
                    pipe.write('\n')  # Message delimiter for v1
                    pipe.flush()
            
            logger.debug(f"Message written to pipe: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Pipe write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_pipe__mutmut_4(self, message: ContextMessage) -> bool:
        """Write message to named pipe with optional v2 protocol."""
        try:
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = message.to_json().encode('XXutf-8XX')
                payload = v2_encode(payload, compress=self.enable_compression)
                logger.debug(f"Using Protocol v2 (compressed={self.enable_compression})")
                
                # Binary mode for v2 protocol
                with open(self.pipe_path, 'wb') as pipe:
                    pipe.write(payload)
                    pipe.flush()
            else:
                # Text mode for v1 protocol compatibility
                with open(self.pipe_path, 'w') as pipe:
                    pipe.write(message.to_json())
                    pipe.write('\n')  # Message delimiter for v1
                    pipe.flush()
            
            logger.debug(f"Message written to pipe: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Pipe write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_pipe__mutmut_5(self, message: ContextMessage) -> bool:
        """Write message to named pipe with optional v2 protocol."""
        try:
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = message.to_json().encode('UTF-8')
                payload = v2_encode(payload, compress=self.enable_compression)
                logger.debug(f"Using Protocol v2 (compressed={self.enable_compression})")
                
                # Binary mode for v2 protocol
                with open(self.pipe_path, 'wb') as pipe:
                    pipe.write(payload)
                    pipe.flush()
            else:
                # Text mode for v1 protocol compatibility
                with open(self.pipe_path, 'w') as pipe:
                    pipe.write(message.to_json())
                    pipe.write('\n')  # Message delimiter for v1
                    pipe.flush()
            
            logger.debug(f"Message written to pipe: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Pipe write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_pipe__mutmut_6(self, message: ContextMessage) -> bool:
        """Write message to named pipe with optional v2 protocol."""
        try:
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = message.to_json().encode('utf-8')
                payload = None
                logger.debug(f"Using Protocol v2 (compressed={self.enable_compression})")
                
                # Binary mode for v2 protocol
                with open(self.pipe_path, 'wb') as pipe:
                    pipe.write(payload)
                    pipe.flush()
            else:
                # Text mode for v1 protocol compatibility
                with open(self.pipe_path, 'w') as pipe:
                    pipe.write(message.to_json())
                    pipe.write('\n')  # Message delimiter for v1
                    pipe.flush()
            
            logger.debug(f"Message written to pipe: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Pipe write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_pipe__mutmut_7(self, message: ContextMessage) -> bool:
        """Write message to named pipe with optional v2 protocol."""
        try:
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = message.to_json().encode('utf-8')
                payload = v2_encode(None, compress=self.enable_compression)
                logger.debug(f"Using Protocol v2 (compressed={self.enable_compression})")
                
                # Binary mode for v2 protocol
                with open(self.pipe_path, 'wb') as pipe:
                    pipe.write(payload)
                    pipe.flush()
            else:
                # Text mode for v1 protocol compatibility
                with open(self.pipe_path, 'w') as pipe:
                    pipe.write(message.to_json())
                    pipe.write('\n')  # Message delimiter for v1
                    pipe.flush()
            
            logger.debug(f"Message written to pipe: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Pipe write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_pipe__mutmut_8(self, message: ContextMessage) -> bool:
        """Write message to named pipe with optional v2 protocol."""
        try:
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = message.to_json().encode('utf-8')
                payload = v2_encode(payload, compress=None)
                logger.debug(f"Using Protocol v2 (compressed={self.enable_compression})")
                
                # Binary mode for v2 protocol
                with open(self.pipe_path, 'wb') as pipe:
                    pipe.write(payload)
                    pipe.flush()
            else:
                # Text mode for v1 protocol compatibility
                with open(self.pipe_path, 'w') as pipe:
                    pipe.write(message.to_json())
                    pipe.write('\n')  # Message delimiter for v1
                    pipe.flush()
            
            logger.debug(f"Message written to pipe: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Pipe write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_pipe__mutmut_9(self, message: ContextMessage) -> bool:
        """Write message to named pipe with optional v2 protocol."""
        try:
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = message.to_json().encode('utf-8')
                payload = v2_encode(compress=self.enable_compression)
                logger.debug(f"Using Protocol v2 (compressed={self.enable_compression})")
                
                # Binary mode for v2 protocol
                with open(self.pipe_path, 'wb') as pipe:
                    pipe.write(payload)
                    pipe.flush()
            else:
                # Text mode for v1 protocol compatibility
                with open(self.pipe_path, 'w') as pipe:
                    pipe.write(message.to_json())
                    pipe.write('\n')  # Message delimiter for v1
                    pipe.flush()
            
            logger.debug(f"Message written to pipe: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Pipe write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_pipe__mutmut_10(self, message: ContextMessage) -> bool:
        """Write message to named pipe with optional v2 protocol."""
        try:
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = message.to_json().encode('utf-8')
                payload = v2_encode(payload, )
                logger.debug(f"Using Protocol v2 (compressed={self.enable_compression})")
                
                # Binary mode for v2 protocol
                with open(self.pipe_path, 'wb') as pipe:
                    pipe.write(payload)
                    pipe.flush()
            else:
                # Text mode for v1 protocol compatibility
                with open(self.pipe_path, 'w') as pipe:
                    pipe.write(message.to_json())
                    pipe.write('\n')  # Message delimiter for v1
                    pipe.flush()
            
            logger.debug(f"Message written to pipe: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Pipe write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_pipe__mutmut_11(self, message: ContextMessage) -> bool:
        """Write message to named pipe with optional v2 protocol."""
        try:
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = message.to_json().encode('utf-8')
                payload = v2_encode(payload, compress=self.enable_compression)
                logger.debug(None)
                
                # Binary mode for v2 protocol
                with open(self.pipe_path, 'wb') as pipe:
                    pipe.write(payload)
                    pipe.flush()
            else:
                # Text mode for v1 protocol compatibility
                with open(self.pipe_path, 'w') as pipe:
                    pipe.write(message.to_json())
                    pipe.write('\n')  # Message delimiter for v1
                    pipe.flush()
            
            logger.debug(f"Message written to pipe: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Pipe write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_pipe__mutmut_12(self, message: ContextMessage) -> bool:
        """Write message to named pipe with optional v2 protocol."""
        try:
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = message.to_json().encode('utf-8')
                payload = v2_encode(payload, compress=self.enable_compression)
                logger.debug(f"Using Protocol v2 (compressed={self.enable_compression})")
                
                # Binary mode for v2 protocol
                with open(None, 'wb') as pipe:
                    pipe.write(payload)
                    pipe.flush()
            else:
                # Text mode for v1 protocol compatibility
                with open(self.pipe_path, 'w') as pipe:
                    pipe.write(message.to_json())
                    pipe.write('\n')  # Message delimiter for v1
                    pipe.flush()
            
            logger.debug(f"Message written to pipe: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Pipe write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_pipe__mutmut_13(self, message: ContextMessage) -> bool:
        """Write message to named pipe with optional v2 protocol."""
        try:
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = message.to_json().encode('utf-8')
                payload = v2_encode(payload, compress=self.enable_compression)
                logger.debug(f"Using Protocol v2 (compressed={self.enable_compression})")
                
                # Binary mode for v2 protocol
                with open(self.pipe_path, None) as pipe:
                    pipe.write(payload)
                    pipe.flush()
            else:
                # Text mode for v1 protocol compatibility
                with open(self.pipe_path, 'w') as pipe:
                    pipe.write(message.to_json())
                    pipe.write('\n')  # Message delimiter for v1
                    pipe.flush()
            
            logger.debug(f"Message written to pipe: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Pipe write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_pipe__mutmut_14(self, message: ContextMessage) -> bool:
        """Write message to named pipe with optional v2 protocol."""
        try:
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = message.to_json().encode('utf-8')
                payload = v2_encode(payload, compress=self.enable_compression)
                logger.debug(f"Using Protocol v2 (compressed={self.enable_compression})")
                
                # Binary mode for v2 protocol
                with open('wb') as pipe:
                    pipe.write(payload)
                    pipe.flush()
            else:
                # Text mode for v1 protocol compatibility
                with open(self.pipe_path, 'w') as pipe:
                    pipe.write(message.to_json())
                    pipe.write('\n')  # Message delimiter for v1
                    pipe.flush()
            
            logger.debug(f"Message written to pipe: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Pipe write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_pipe__mutmut_15(self, message: ContextMessage) -> bool:
        """Write message to named pipe with optional v2 protocol."""
        try:
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = message.to_json().encode('utf-8')
                payload = v2_encode(payload, compress=self.enable_compression)
                logger.debug(f"Using Protocol v2 (compressed={self.enable_compression})")
                
                # Binary mode for v2 protocol
                with open(self.pipe_path, ) as pipe:
                    pipe.write(payload)
                    pipe.flush()
            else:
                # Text mode for v1 protocol compatibility
                with open(self.pipe_path, 'w') as pipe:
                    pipe.write(message.to_json())
                    pipe.write('\n')  # Message delimiter for v1
                    pipe.flush()
            
            logger.debug(f"Message written to pipe: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Pipe write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_pipe__mutmut_16(self, message: ContextMessage) -> bool:
        """Write message to named pipe with optional v2 protocol."""
        try:
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = message.to_json().encode('utf-8')
                payload = v2_encode(payload, compress=self.enable_compression)
                logger.debug(f"Using Protocol v2 (compressed={self.enable_compression})")
                
                # Binary mode for v2 protocol
                with open(self.pipe_path, 'XXwbXX') as pipe:
                    pipe.write(payload)
                    pipe.flush()
            else:
                # Text mode for v1 protocol compatibility
                with open(self.pipe_path, 'w') as pipe:
                    pipe.write(message.to_json())
                    pipe.write('\n')  # Message delimiter for v1
                    pipe.flush()
            
            logger.debug(f"Message written to pipe: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Pipe write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_pipe__mutmut_17(self, message: ContextMessage) -> bool:
        """Write message to named pipe with optional v2 protocol."""
        try:
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = message.to_json().encode('utf-8')
                payload = v2_encode(payload, compress=self.enable_compression)
                logger.debug(f"Using Protocol v2 (compressed={self.enable_compression})")
                
                # Binary mode for v2 protocol
                with open(self.pipe_path, 'WB') as pipe:
                    pipe.write(payload)
                    pipe.flush()
            else:
                # Text mode for v1 protocol compatibility
                with open(self.pipe_path, 'w') as pipe:
                    pipe.write(message.to_json())
                    pipe.write('\n')  # Message delimiter for v1
                    pipe.flush()
            
            logger.debug(f"Message written to pipe: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Pipe write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_pipe__mutmut_18(self, message: ContextMessage) -> bool:
        """Write message to named pipe with optional v2 protocol."""
        try:
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = message.to_json().encode('utf-8')
                payload = v2_encode(payload, compress=self.enable_compression)
                logger.debug(f"Using Protocol v2 (compressed={self.enable_compression})")
                
                # Binary mode for v2 protocol
                with open(self.pipe_path, 'wb') as pipe:
                    pipe.write(None)
                    pipe.flush()
            else:
                # Text mode for v1 protocol compatibility
                with open(self.pipe_path, 'w') as pipe:
                    pipe.write(message.to_json())
                    pipe.write('\n')  # Message delimiter for v1
                    pipe.flush()
            
            logger.debug(f"Message written to pipe: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Pipe write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_pipe__mutmut_19(self, message: ContextMessage) -> bool:
        """Write message to named pipe with optional v2 protocol."""
        try:
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = message.to_json().encode('utf-8')
                payload = v2_encode(payload, compress=self.enable_compression)
                logger.debug(f"Using Protocol v2 (compressed={self.enable_compression})")
                
                # Binary mode for v2 protocol
                with open(self.pipe_path, 'wb') as pipe:
                    pipe.write(payload)
                    pipe.flush()
            else:
                # Text mode for v1 protocol compatibility
                with open(None, 'w') as pipe:
                    pipe.write(message.to_json())
                    pipe.write('\n')  # Message delimiter for v1
                    pipe.flush()
            
            logger.debug(f"Message written to pipe: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Pipe write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_pipe__mutmut_20(self, message: ContextMessage) -> bool:
        """Write message to named pipe with optional v2 protocol."""
        try:
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = message.to_json().encode('utf-8')
                payload = v2_encode(payload, compress=self.enable_compression)
                logger.debug(f"Using Protocol v2 (compressed={self.enable_compression})")
                
                # Binary mode for v2 protocol
                with open(self.pipe_path, 'wb') as pipe:
                    pipe.write(payload)
                    pipe.flush()
            else:
                # Text mode for v1 protocol compatibility
                with open(self.pipe_path, None) as pipe:
                    pipe.write(message.to_json())
                    pipe.write('\n')  # Message delimiter for v1
                    pipe.flush()
            
            logger.debug(f"Message written to pipe: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Pipe write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_pipe__mutmut_21(self, message: ContextMessage) -> bool:
        """Write message to named pipe with optional v2 protocol."""
        try:
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = message.to_json().encode('utf-8')
                payload = v2_encode(payload, compress=self.enable_compression)
                logger.debug(f"Using Protocol v2 (compressed={self.enable_compression})")
                
                # Binary mode for v2 protocol
                with open(self.pipe_path, 'wb') as pipe:
                    pipe.write(payload)
                    pipe.flush()
            else:
                # Text mode for v1 protocol compatibility
                with open('w') as pipe:
                    pipe.write(message.to_json())
                    pipe.write('\n')  # Message delimiter for v1
                    pipe.flush()
            
            logger.debug(f"Message written to pipe: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Pipe write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_pipe__mutmut_22(self, message: ContextMessage) -> bool:
        """Write message to named pipe with optional v2 protocol."""
        try:
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = message.to_json().encode('utf-8')
                payload = v2_encode(payload, compress=self.enable_compression)
                logger.debug(f"Using Protocol v2 (compressed={self.enable_compression})")
                
                # Binary mode for v2 protocol
                with open(self.pipe_path, 'wb') as pipe:
                    pipe.write(payload)
                    pipe.flush()
            else:
                # Text mode for v1 protocol compatibility
                with open(self.pipe_path, ) as pipe:
                    pipe.write(message.to_json())
                    pipe.write('\n')  # Message delimiter for v1
                    pipe.flush()
            
            logger.debug(f"Message written to pipe: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Pipe write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_pipe__mutmut_23(self, message: ContextMessage) -> bool:
        """Write message to named pipe with optional v2 protocol."""
        try:
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = message.to_json().encode('utf-8')
                payload = v2_encode(payload, compress=self.enable_compression)
                logger.debug(f"Using Protocol v2 (compressed={self.enable_compression})")
                
                # Binary mode for v2 protocol
                with open(self.pipe_path, 'wb') as pipe:
                    pipe.write(payload)
                    pipe.flush()
            else:
                # Text mode for v1 protocol compatibility
                with open(self.pipe_path, 'XXwXX') as pipe:
                    pipe.write(message.to_json())
                    pipe.write('\n')  # Message delimiter for v1
                    pipe.flush()
            
            logger.debug(f"Message written to pipe: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Pipe write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_pipe__mutmut_24(self, message: ContextMessage) -> bool:
        """Write message to named pipe with optional v2 protocol."""
        try:
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = message.to_json().encode('utf-8')
                payload = v2_encode(payload, compress=self.enable_compression)
                logger.debug(f"Using Protocol v2 (compressed={self.enable_compression})")
                
                # Binary mode for v2 protocol
                with open(self.pipe_path, 'wb') as pipe:
                    pipe.write(payload)
                    pipe.flush()
            else:
                # Text mode for v1 protocol compatibility
                with open(self.pipe_path, 'W') as pipe:
                    pipe.write(message.to_json())
                    pipe.write('\n')  # Message delimiter for v1
                    pipe.flush()
            
            logger.debug(f"Message written to pipe: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Pipe write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_pipe__mutmut_25(self, message: ContextMessage) -> bool:
        """Write message to named pipe with optional v2 protocol."""
        try:
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = message.to_json().encode('utf-8')
                payload = v2_encode(payload, compress=self.enable_compression)
                logger.debug(f"Using Protocol v2 (compressed={self.enable_compression})")
                
                # Binary mode for v2 protocol
                with open(self.pipe_path, 'wb') as pipe:
                    pipe.write(payload)
                    pipe.flush()
            else:
                # Text mode for v1 protocol compatibility
                with open(self.pipe_path, 'w') as pipe:
                    pipe.write(None)
                    pipe.write('\n')  # Message delimiter for v1
                    pipe.flush()
            
            logger.debug(f"Message written to pipe: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Pipe write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_pipe__mutmut_26(self, message: ContextMessage) -> bool:
        """Write message to named pipe with optional v2 protocol."""
        try:
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = message.to_json().encode('utf-8')
                payload = v2_encode(payload, compress=self.enable_compression)
                logger.debug(f"Using Protocol v2 (compressed={self.enable_compression})")
                
                # Binary mode for v2 protocol
                with open(self.pipe_path, 'wb') as pipe:
                    pipe.write(payload)
                    pipe.flush()
            else:
                # Text mode for v1 protocol compatibility
                with open(self.pipe_path, 'w') as pipe:
                    pipe.write(message.to_json())
                    pipe.write(None)  # Message delimiter for v1
                    pipe.flush()
            
            logger.debug(f"Message written to pipe: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Pipe write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_pipe__mutmut_27(self, message: ContextMessage) -> bool:
        """Write message to named pipe with optional v2 protocol."""
        try:
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = message.to_json().encode('utf-8')
                payload = v2_encode(payload, compress=self.enable_compression)
                logger.debug(f"Using Protocol v2 (compressed={self.enable_compression})")
                
                # Binary mode for v2 protocol
                with open(self.pipe_path, 'wb') as pipe:
                    pipe.write(payload)
                    pipe.flush()
            else:
                # Text mode for v1 protocol compatibility
                with open(self.pipe_path, 'w') as pipe:
                    pipe.write(message.to_json())
                    pipe.write('XX\nXX')  # Message delimiter for v1
                    pipe.flush()
            
            logger.debug(f"Message written to pipe: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Pipe write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_pipe__mutmut_28(self, message: ContextMessage) -> bool:
        """Write message to named pipe with optional v2 protocol."""
        try:
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = message.to_json().encode('utf-8')
                payload = v2_encode(payload, compress=self.enable_compression)
                logger.debug(f"Using Protocol v2 (compressed={self.enable_compression})")
                
                # Binary mode for v2 protocol
                with open(self.pipe_path, 'wb') as pipe:
                    pipe.write(payload)
                    pipe.flush()
            else:
                # Text mode for v1 protocol compatibility
                with open(self.pipe_path, 'w') as pipe:
                    pipe.write(message.to_json())
                    pipe.write('\n')  # Message delimiter for v1
                    pipe.flush()
            
            logger.debug(None)
            return True
            
        except Exception as e:
            logger.error(f"Pipe write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_pipe__mutmut_29(self, message: ContextMessage) -> bool:
        """Write message to named pipe with optional v2 protocol."""
        try:
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = message.to_json().encode('utf-8')
                payload = v2_encode(payload, compress=self.enable_compression)
                logger.debug(f"Using Protocol v2 (compressed={self.enable_compression})")
                
                # Binary mode for v2 protocol
                with open(self.pipe_path, 'wb') as pipe:
                    pipe.write(payload)
                    pipe.flush()
            else:
                # Text mode for v1 protocol compatibility
                with open(self.pipe_path, 'w') as pipe:
                    pipe.write(message.to_json())
                    pipe.write('\n')  # Message delimiter for v1
                    pipe.flush()
            
            logger.debug(f"Message written to pipe: {message.message_type}")
            return False
            
        except Exception as e:
            logger.error(f"Pipe write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_pipe__mutmut_30(self, message: ContextMessage) -> bool:
        """Write message to named pipe with optional v2 protocol."""
        try:
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = message.to_json().encode('utf-8')
                payload = v2_encode(payload, compress=self.enable_compression)
                logger.debug(f"Using Protocol v2 (compressed={self.enable_compression})")
                
                # Binary mode for v2 protocol
                with open(self.pipe_path, 'wb') as pipe:
                    pipe.write(payload)
                    pipe.flush()
            else:
                # Text mode for v1 protocol compatibility
                with open(self.pipe_path, 'w') as pipe:
                    pipe.write(message.to_json())
                    pipe.write('\n')  # Message delimiter for v1
                    pipe.flush()
            
            logger.debug(f"Message written to pipe: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(None)
            return False
    
    def xǁBridgeManagerǁ_write_to_pipe__mutmut_31(self, message: ContextMessage) -> bool:
        """Write message to named pipe with optional v2 protocol."""
        try:
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = message.to_json().encode('utf-8')
                payload = v2_encode(payload, compress=self.enable_compression)
                logger.debug(f"Using Protocol v2 (compressed={self.enable_compression})")
                
                # Binary mode for v2 protocol
                with open(self.pipe_path, 'wb') as pipe:
                    pipe.write(payload)
                    pipe.flush()
            else:
                # Text mode for v1 protocol compatibility
                with open(self.pipe_path, 'w') as pipe:
                    pipe.write(message.to_json())
                    pipe.write('\n')  # Message delimiter for v1
                    pipe.flush()
            
            logger.debug(f"Message written to pipe: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Pipe write error: {e}")
            return True
    
    xǁBridgeManagerǁ_write_to_pipe__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁBridgeManagerǁ_write_to_pipe__mutmut_1': xǁBridgeManagerǁ_write_to_pipe__mutmut_1, 
        'xǁBridgeManagerǁ_write_to_pipe__mutmut_2': xǁBridgeManagerǁ_write_to_pipe__mutmut_2, 
        'xǁBridgeManagerǁ_write_to_pipe__mutmut_3': xǁBridgeManagerǁ_write_to_pipe__mutmut_3, 
        'xǁBridgeManagerǁ_write_to_pipe__mutmut_4': xǁBridgeManagerǁ_write_to_pipe__mutmut_4, 
        'xǁBridgeManagerǁ_write_to_pipe__mutmut_5': xǁBridgeManagerǁ_write_to_pipe__mutmut_5, 
        'xǁBridgeManagerǁ_write_to_pipe__mutmut_6': xǁBridgeManagerǁ_write_to_pipe__mutmut_6, 
        'xǁBridgeManagerǁ_write_to_pipe__mutmut_7': xǁBridgeManagerǁ_write_to_pipe__mutmut_7, 
        'xǁBridgeManagerǁ_write_to_pipe__mutmut_8': xǁBridgeManagerǁ_write_to_pipe__mutmut_8, 
        'xǁBridgeManagerǁ_write_to_pipe__mutmut_9': xǁBridgeManagerǁ_write_to_pipe__mutmut_9, 
        'xǁBridgeManagerǁ_write_to_pipe__mutmut_10': xǁBridgeManagerǁ_write_to_pipe__mutmut_10, 
        'xǁBridgeManagerǁ_write_to_pipe__mutmut_11': xǁBridgeManagerǁ_write_to_pipe__mutmut_11, 
        'xǁBridgeManagerǁ_write_to_pipe__mutmut_12': xǁBridgeManagerǁ_write_to_pipe__mutmut_12, 
        'xǁBridgeManagerǁ_write_to_pipe__mutmut_13': xǁBridgeManagerǁ_write_to_pipe__mutmut_13, 
        'xǁBridgeManagerǁ_write_to_pipe__mutmut_14': xǁBridgeManagerǁ_write_to_pipe__mutmut_14, 
        'xǁBridgeManagerǁ_write_to_pipe__mutmut_15': xǁBridgeManagerǁ_write_to_pipe__mutmut_15, 
        'xǁBridgeManagerǁ_write_to_pipe__mutmut_16': xǁBridgeManagerǁ_write_to_pipe__mutmut_16, 
        'xǁBridgeManagerǁ_write_to_pipe__mutmut_17': xǁBridgeManagerǁ_write_to_pipe__mutmut_17, 
        'xǁBridgeManagerǁ_write_to_pipe__mutmut_18': xǁBridgeManagerǁ_write_to_pipe__mutmut_18, 
        'xǁBridgeManagerǁ_write_to_pipe__mutmut_19': xǁBridgeManagerǁ_write_to_pipe__mutmut_19, 
        'xǁBridgeManagerǁ_write_to_pipe__mutmut_20': xǁBridgeManagerǁ_write_to_pipe__mutmut_20, 
        'xǁBridgeManagerǁ_write_to_pipe__mutmut_21': xǁBridgeManagerǁ_write_to_pipe__mutmut_21, 
        'xǁBridgeManagerǁ_write_to_pipe__mutmut_22': xǁBridgeManagerǁ_write_to_pipe__mutmut_22, 
        'xǁBridgeManagerǁ_write_to_pipe__mutmut_23': xǁBridgeManagerǁ_write_to_pipe__mutmut_23, 
        'xǁBridgeManagerǁ_write_to_pipe__mutmut_24': xǁBridgeManagerǁ_write_to_pipe__mutmut_24, 
        'xǁBridgeManagerǁ_write_to_pipe__mutmut_25': xǁBridgeManagerǁ_write_to_pipe__mutmut_25, 
        'xǁBridgeManagerǁ_write_to_pipe__mutmut_26': xǁBridgeManagerǁ_write_to_pipe__mutmut_26, 
        'xǁBridgeManagerǁ_write_to_pipe__mutmut_27': xǁBridgeManagerǁ_write_to_pipe__mutmut_27, 
        'xǁBridgeManagerǁ_write_to_pipe__mutmut_28': xǁBridgeManagerǁ_write_to_pipe__mutmut_28, 
        'xǁBridgeManagerǁ_write_to_pipe__mutmut_29': xǁBridgeManagerǁ_write_to_pipe__mutmut_29, 
        'xǁBridgeManagerǁ_write_to_pipe__mutmut_30': xǁBridgeManagerǁ_write_to_pipe__mutmut_30, 
        'xǁBridgeManagerǁ_write_to_pipe__mutmut_31': xǁBridgeManagerǁ_write_to_pipe__mutmut_31
    }
    
    def _write_to_pipe(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁBridgeManagerǁ_write_to_pipe__mutmut_orig"), object.__getattribute__(self, "xǁBridgeManagerǁ_write_to_pipe__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _write_to_pipe.__signature__ = _mutmut_signature(xǁBridgeManagerǁ_write_to_pipe__mutmut_orig)
    xǁBridgeManagerǁ_write_to_pipe__mutmut_orig.__name__ = 'xǁBridgeManagerǁ_write_to_pipe'
    
    def xǁBridgeManagerǁ_write_to_socket__mutmut_orig(self, message: ContextMessage) -> bool:
        """Write message to Unix socket with optional v2 protocol."""
        try:
            payload = message.to_json().encode('utf-8')
            
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = v2_encode(payload, compress=self.enable_compression)
            else:
                payload = payload + b'\n'  # Message delimiter for v1
            
            # Connect to socket
            client = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            client.connect(str(self.socket_path))
            
            # Send message
            client.sendall(payload)
            
            client.close()
            logger.debug(f"Message written to socket: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Socket write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_socket__mutmut_1(self, message: ContextMessage) -> bool:
        """Write message to Unix socket with optional v2 protocol."""
        try:
            payload = None
            
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = v2_encode(payload, compress=self.enable_compression)
            else:
                payload = payload + b'\n'  # Message delimiter for v1
            
            # Connect to socket
            client = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            client.connect(str(self.socket_path))
            
            # Send message
            client.sendall(payload)
            
            client.close()
            logger.debug(f"Message written to socket: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Socket write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_socket__mutmut_2(self, message: ContextMessage) -> bool:
        """Write message to Unix socket with optional v2 protocol."""
        try:
            payload = message.to_json().encode(None)
            
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = v2_encode(payload, compress=self.enable_compression)
            else:
                payload = payload + b'\n'  # Message delimiter for v1
            
            # Connect to socket
            client = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            client.connect(str(self.socket_path))
            
            # Send message
            client.sendall(payload)
            
            client.close()
            logger.debug(f"Message written to socket: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Socket write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_socket__mutmut_3(self, message: ContextMessage) -> bool:
        """Write message to Unix socket with optional v2 protocol."""
        try:
            payload = message.to_json().encode('XXutf-8XX')
            
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = v2_encode(payload, compress=self.enable_compression)
            else:
                payload = payload + b'\n'  # Message delimiter for v1
            
            # Connect to socket
            client = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            client.connect(str(self.socket_path))
            
            # Send message
            client.sendall(payload)
            
            client.close()
            logger.debug(f"Message written to socket: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Socket write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_socket__mutmut_4(self, message: ContextMessage) -> bool:
        """Write message to Unix socket with optional v2 protocol."""
        try:
            payload = message.to_json().encode('UTF-8')
            
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = v2_encode(payload, compress=self.enable_compression)
            else:
                payload = payload + b'\n'  # Message delimiter for v1
            
            # Connect to socket
            client = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            client.connect(str(self.socket_path))
            
            # Send message
            client.sendall(payload)
            
            client.close()
            logger.debug(f"Message written to socket: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Socket write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_socket__mutmut_5(self, message: ContextMessage) -> bool:
        """Write message to Unix socket with optional v2 protocol."""
        try:
            payload = message.to_json().encode('utf-8')
            
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 or HAS_PROTOCOL_V2:
                payload = v2_encode(payload, compress=self.enable_compression)
            else:
                payload = payload + b'\n'  # Message delimiter for v1
            
            # Connect to socket
            client = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            client.connect(str(self.socket_path))
            
            # Send message
            client.sendall(payload)
            
            client.close()
            logger.debug(f"Message written to socket: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Socket write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_socket__mutmut_6(self, message: ContextMessage) -> bool:
        """Write message to Unix socket with optional v2 protocol."""
        try:
            payload = message.to_json().encode('utf-8')
            
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = None
            else:
                payload = payload + b'\n'  # Message delimiter for v1
            
            # Connect to socket
            client = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            client.connect(str(self.socket_path))
            
            # Send message
            client.sendall(payload)
            
            client.close()
            logger.debug(f"Message written to socket: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Socket write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_socket__mutmut_7(self, message: ContextMessage) -> bool:
        """Write message to Unix socket with optional v2 protocol."""
        try:
            payload = message.to_json().encode('utf-8')
            
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = v2_encode(None, compress=self.enable_compression)
            else:
                payload = payload + b'\n'  # Message delimiter for v1
            
            # Connect to socket
            client = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            client.connect(str(self.socket_path))
            
            # Send message
            client.sendall(payload)
            
            client.close()
            logger.debug(f"Message written to socket: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Socket write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_socket__mutmut_8(self, message: ContextMessage) -> bool:
        """Write message to Unix socket with optional v2 protocol."""
        try:
            payload = message.to_json().encode('utf-8')
            
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = v2_encode(payload, compress=None)
            else:
                payload = payload + b'\n'  # Message delimiter for v1
            
            # Connect to socket
            client = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            client.connect(str(self.socket_path))
            
            # Send message
            client.sendall(payload)
            
            client.close()
            logger.debug(f"Message written to socket: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Socket write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_socket__mutmut_9(self, message: ContextMessage) -> bool:
        """Write message to Unix socket with optional v2 protocol."""
        try:
            payload = message.to_json().encode('utf-8')
            
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = v2_encode(compress=self.enable_compression)
            else:
                payload = payload + b'\n'  # Message delimiter for v1
            
            # Connect to socket
            client = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            client.connect(str(self.socket_path))
            
            # Send message
            client.sendall(payload)
            
            client.close()
            logger.debug(f"Message written to socket: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Socket write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_socket__mutmut_10(self, message: ContextMessage) -> bool:
        """Write message to Unix socket with optional v2 protocol."""
        try:
            payload = message.to_json().encode('utf-8')
            
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = v2_encode(payload, )
            else:
                payload = payload + b'\n'  # Message delimiter for v1
            
            # Connect to socket
            client = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            client.connect(str(self.socket_path))
            
            # Send message
            client.sendall(payload)
            
            client.close()
            logger.debug(f"Message written to socket: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Socket write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_socket__mutmut_11(self, message: ContextMessage) -> bool:
        """Write message to Unix socket with optional v2 protocol."""
        try:
            payload = message.to_json().encode('utf-8')
            
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = v2_encode(payload, compress=self.enable_compression)
            else:
                payload = None  # Message delimiter for v1
            
            # Connect to socket
            client = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            client.connect(str(self.socket_path))
            
            # Send message
            client.sendall(payload)
            
            client.close()
            logger.debug(f"Message written to socket: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Socket write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_socket__mutmut_12(self, message: ContextMessage) -> bool:
        """Write message to Unix socket with optional v2 protocol."""
        try:
            payload = message.to_json().encode('utf-8')
            
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = v2_encode(payload, compress=self.enable_compression)
            else:
                payload = payload - b'\n'  # Message delimiter for v1
            
            # Connect to socket
            client = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            client.connect(str(self.socket_path))
            
            # Send message
            client.sendall(payload)
            
            client.close()
            logger.debug(f"Message written to socket: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Socket write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_socket__mutmut_13(self, message: ContextMessage) -> bool:
        """Write message to Unix socket with optional v2 protocol."""
        try:
            payload = message.to_json().encode('utf-8')
            
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = v2_encode(payload, compress=self.enable_compression)
            else:
                payload = payload + b'XX\nXX'  # Message delimiter for v1
            
            # Connect to socket
            client = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            client.connect(str(self.socket_path))
            
            # Send message
            client.sendall(payload)
            
            client.close()
            logger.debug(f"Message written to socket: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Socket write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_socket__mutmut_14(self, message: ContextMessage) -> bool:
        """Write message to Unix socket with optional v2 protocol."""
        try:
            payload = message.to_json().encode('utf-8')
            
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = v2_encode(payload, compress=self.enable_compression)
            else:
                payload = payload + b'\n'  # Message delimiter for v1
            
            # Connect to socket
            client = None
            client.connect(str(self.socket_path))
            
            # Send message
            client.sendall(payload)
            
            client.close()
            logger.debug(f"Message written to socket: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Socket write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_socket__mutmut_15(self, message: ContextMessage) -> bool:
        """Write message to Unix socket with optional v2 protocol."""
        try:
            payload = message.to_json().encode('utf-8')
            
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = v2_encode(payload, compress=self.enable_compression)
            else:
                payload = payload + b'\n'  # Message delimiter for v1
            
            # Connect to socket
            client = socket.socket(None, socket.SOCK_STREAM)
            client.connect(str(self.socket_path))
            
            # Send message
            client.sendall(payload)
            
            client.close()
            logger.debug(f"Message written to socket: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Socket write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_socket__mutmut_16(self, message: ContextMessage) -> bool:
        """Write message to Unix socket with optional v2 protocol."""
        try:
            payload = message.to_json().encode('utf-8')
            
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = v2_encode(payload, compress=self.enable_compression)
            else:
                payload = payload + b'\n'  # Message delimiter for v1
            
            # Connect to socket
            client = socket.socket(socket.AF_UNIX, None)
            client.connect(str(self.socket_path))
            
            # Send message
            client.sendall(payload)
            
            client.close()
            logger.debug(f"Message written to socket: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Socket write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_socket__mutmut_17(self, message: ContextMessage) -> bool:
        """Write message to Unix socket with optional v2 protocol."""
        try:
            payload = message.to_json().encode('utf-8')
            
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = v2_encode(payload, compress=self.enable_compression)
            else:
                payload = payload + b'\n'  # Message delimiter for v1
            
            # Connect to socket
            client = socket.socket(socket.SOCK_STREAM)
            client.connect(str(self.socket_path))
            
            # Send message
            client.sendall(payload)
            
            client.close()
            logger.debug(f"Message written to socket: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Socket write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_socket__mutmut_18(self, message: ContextMessage) -> bool:
        """Write message to Unix socket with optional v2 protocol."""
        try:
            payload = message.to_json().encode('utf-8')
            
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = v2_encode(payload, compress=self.enable_compression)
            else:
                payload = payload + b'\n'  # Message delimiter for v1
            
            # Connect to socket
            client = socket.socket(socket.AF_UNIX, )
            client.connect(str(self.socket_path))
            
            # Send message
            client.sendall(payload)
            
            client.close()
            logger.debug(f"Message written to socket: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Socket write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_socket__mutmut_19(self, message: ContextMessage) -> bool:
        """Write message to Unix socket with optional v2 protocol."""
        try:
            payload = message.to_json().encode('utf-8')
            
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = v2_encode(payload, compress=self.enable_compression)
            else:
                payload = payload + b'\n'  # Message delimiter for v1
            
            # Connect to socket
            client = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            client.connect(None)
            
            # Send message
            client.sendall(payload)
            
            client.close()
            logger.debug(f"Message written to socket: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Socket write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_socket__mutmut_20(self, message: ContextMessage) -> bool:
        """Write message to Unix socket with optional v2 protocol."""
        try:
            payload = message.to_json().encode('utf-8')
            
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = v2_encode(payload, compress=self.enable_compression)
            else:
                payload = payload + b'\n'  # Message delimiter for v1
            
            # Connect to socket
            client = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            client.connect(str(None))
            
            # Send message
            client.sendall(payload)
            
            client.close()
            logger.debug(f"Message written to socket: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Socket write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_socket__mutmut_21(self, message: ContextMessage) -> bool:
        """Write message to Unix socket with optional v2 protocol."""
        try:
            payload = message.to_json().encode('utf-8')
            
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = v2_encode(payload, compress=self.enable_compression)
            else:
                payload = payload + b'\n'  # Message delimiter for v1
            
            # Connect to socket
            client = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            client.connect(str(self.socket_path))
            
            # Send message
            client.sendall(None)
            
            client.close()
            logger.debug(f"Message written to socket: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Socket write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_socket__mutmut_22(self, message: ContextMessage) -> bool:
        """Write message to Unix socket with optional v2 protocol."""
        try:
            payload = message.to_json().encode('utf-8')
            
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = v2_encode(payload, compress=self.enable_compression)
            else:
                payload = payload + b'\n'  # Message delimiter for v1
            
            # Connect to socket
            client = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            client.connect(str(self.socket_path))
            
            # Send message
            client.sendall(payload)
            
            client.close()
            logger.debug(None)
            return True
            
        except Exception as e:
            logger.error(f"Socket write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_socket__mutmut_23(self, message: ContextMessage) -> bool:
        """Write message to Unix socket with optional v2 protocol."""
        try:
            payload = message.to_json().encode('utf-8')
            
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = v2_encode(payload, compress=self.enable_compression)
            else:
                payload = payload + b'\n'  # Message delimiter for v1
            
            # Connect to socket
            client = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            client.connect(str(self.socket_path))
            
            # Send message
            client.sendall(payload)
            
            client.close()
            logger.debug(f"Message written to socket: {message.message_type}")
            return False
            
        except Exception as e:
            logger.error(f"Socket write error: {e}")
            return False
    
    def xǁBridgeManagerǁ_write_to_socket__mutmut_24(self, message: ContextMessage) -> bool:
        """Write message to Unix socket with optional v2 protocol."""
        try:
            payload = message.to_json().encode('utf-8')
            
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = v2_encode(payload, compress=self.enable_compression)
            else:
                payload = payload + b'\n'  # Message delimiter for v1
            
            # Connect to socket
            client = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            client.connect(str(self.socket_path))
            
            # Send message
            client.sendall(payload)
            
            client.close()
            logger.debug(f"Message written to socket: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(None)
            return False
    
    def xǁBridgeManagerǁ_write_to_socket__mutmut_25(self, message: ContextMessage) -> bool:
        """Write message to Unix socket with optional v2 protocol."""
        try:
            payload = message.to_json().encode('utf-8')
            
            # Use Protocol v2 with compression if enabled
            if self.use_protocol_v2 and HAS_PROTOCOL_V2:
                payload = v2_encode(payload, compress=self.enable_compression)
            else:
                payload = payload + b'\n'  # Message delimiter for v1
            
            # Connect to socket
            client = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            client.connect(str(self.socket_path))
            
            # Send message
            client.sendall(payload)
            
            client.close()
            logger.debug(f"Message written to socket: {message.message_type}")
            return True
            
        except Exception as e:
            logger.error(f"Socket write error: {e}")
            return True
    
    xǁBridgeManagerǁ_write_to_socket__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁBridgeManagerǁ_write_to_socket__mutmut_1': xǁBridgeManagerǁ_write_to_socket__mutmut_1, 
        'xǁBridgeManagerǁ_write_to_socket__mutmut_2': xǁBridgeManagerǁ_write_to_socket__mutmut_2, 
        'xǁBridgeManagerǁ_write_to_socket__mutmut_3': xǁBridgeManagerǁ_write_to_socket__mutmut_3, 
        'xǁBridgeManagerǁ_write_to_socket__mutmut_4': xǁBridgeManagerǁ_write_to_socket__mutmut_4, 
        'xǁBridgeManagerǁ_write_to_socket__mutmut_5': xǁBridgeManagerǁ_write_to_socket__mutmut_5, 
        'xǁBridgeManagerǁ_write_to_socket__mutmut_6': xǁBridgeManagerǁ_write_to_socket__mutmut_6, 
        'xǁBridgeManagerǁ_write_to_socket__mutmut_7': xǁBridgeManagerǁ_write_to_socket__mutmut_7, 
        'xǁBridgeManagerǁ_write_to_socket__mutmut_8': xǁBridgeManagerǁ_write_to_socket__mutmut_8, 
        'xǁBridgeManagerǁ_write_to_socket__mutmut_9': xǁBridgeManagerǁ_write_to_socket__mutmut_9, 
        'xǁBridgeManagerǁ_write_to_socket__mutmut_10': xǁBridgeManagerǁ_write_to_socket__mutmut_10, 
        'xǁBridgeManagerǁ_write_to_socket__mutmut_11': xǁBridgeManagerǁ_write_to_socket__mutmut_11, 
        'xǁBridgeManagerǁ_write_to_socket__mutmut_12': xǁBridgeManagerǁ_write_to_socket__mutmut_12, 
        'xǁBridgeManagerǁ_write_to_socket__mutmut_13': xǁBridgeManagerǁ_write_to_socket__mutmut_13, 
        'xǁBridgeManagerǁ_write_to_socket__mutmut_14': xǁBridgeManagerǁ_write_to_socket__mutmut_14, 
        'xǁBridgeManagerǁ_write_to_socket__mutmut_15': xǁBridgeManagerǁ_write_to_socket__mutmut_15, 
        'xǁBridgeManagerǁ_write_to_socket__mutmut_16': xǁBridgeManagerǁ_write_to_socket__mutmut_16, 
        'xǁBridgeManagerǁ_write_to_socket__mutmut_17': xǁBridgeManagerǁ_write_to_socket__mutmut_17, 
        'xǁBridgeManagerǁ_write_to_socket__mutmut_18': xǁBridgeManagerǁ_write_to_socket__mutmut_18, 
        'xǁBridgeManagerǁ_write_to_socket__mutmut_19': xǁBridgeManagerǁ_write_to_socket__mutmut_19, 
        'xǁBridgeManagerǁ_write_to_socket__mutmut_20': xǁBridgeManagerǁ_write_to_socket__mutmut_20, 
        'xǁBridgeManagerǁ_write_to_socket__mutmut_21': xǁBridgeManagerǁ_write_to_socket__mutmut_21, 
        'xǁBridgeManagerǁ_write_to_socket__mutmut_22': xǁBridgeManagerǁ_write_to_socket__mutmut_22, 
        'xǁBridgeManagerǁ_write_to_socket__mutmut_23': xǁBridgeManagerǁ_write_to_socket__mutmut_23, 
        'xǁBridgeManagerǁ_write_to_socket__mutmut_24': xǁBridgeManagerǁ_write_to_socket__mutmut_24, 
        'xǁBridgeManagerǁ_write_to_socket__mutmut_25': xǁBridgeManagerǁ_write_to_socket__mutmut_25
    }
    
    def _write_to_socket(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁBridgeManagerǁ_write_to_socket__mutmut_orig"), object.__getattribute__(self, "xǁBridgeManagerǁ_write_to_socket__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _write_to_socket.__signature__ = _mutmut_signature(xǁBridgeManagerǁ_write_to_socket__mutmut_orig)
    xǁBridgeManagerǁ_write_to_socket__mutmut_orig.__name__ = 'xǁBridgeManagerǁ_write_to_socket'
    
    # ==================== Protocol v2 Methods ====================
    
    def xǁBridgeManagerǁ_ensure_multi_client_bridge__mutmut_orig(self) -> bool:
        """Lazy initialization of multi-client bridge."""
        if not self.use_protocol_v2:
            return False
        if self._multi_client_bridge is None:
            self._multi_client_bridge = MultiClientBridge(max_clients=self._max_clients)
        return True
    
    # ==================== Protocol v2 Methods ====================
    
    def xǁBridgeManagerǁ_ensure_multi_client_bridge__mutmut_1(self) -> bool:
        """Lazy initialization of multi-client bridge."""
        if self.use_protocol_v2:
            return False
        if self._multi_client_bridge is None:
            self._multi_client_bridge = MultiClientBridge(max_clients=self._max_clients)
        return True
    
    # ==================== Protocol v2 Methods ====================
    
    def xǁBridgeManagerǁ_ensure_multi_client_bridge__mutmut_2(self) -> bool:
        """Lazy initialization of multi-client bridge."""
        if not self.use_protocol_v2:
            return True
        if self._multi_client_bridge is None:
            self._multi_client_bridge = MultiClientBridge(max_clients=self._max_clients)
        return True
    
    # ==================== Protocol v2 Methods ====================
    
    def xǁBridgeManagerǁ_ensure_multi_client_bridge__mutmut_3(self) -> bool:
        """Lazy initialization of multi-client bridge."""
        if not self.use_protocol_v2:
            return False
        if self._multi_client_bridge is not None:
            self._multi_client_bridge = MultiClientBridge(max_clients=self._max_clients)
        return True
    
    # ==================== Protocol v2 Methods ====================
    
    def xǁBridgeManagerǁ_ensure_multi_client_bridge__mutmut_4(self) -> bool:
        """Lazy initialization of multi-client bridge."""
        if not self.use_protocol_v2:
            return False
        if self._multi_client_bridge is None:
            self._multi_client_bridge = None
        return True
    
    # ==================== Protocol v2 Methods ====================
    
    def xǁBridgeManagerǁ_ensure_multi_client_bridge__mutmut_5(self) -> bool:
        """Lazy initialization of multi-client bridge."""
        if not self.use_protocol_v2:
            return False
        if self._multi_client_bridge is None:
            self._multi_client_bridge = MultiClientBridge(max_clients=None)
        return True
    
    # ==================== Protocol v2 Methods ====================
    
    def xǁBridgeManagerǁ_ensure_multi_client_bridge__mutmut_6(self) -> bool:
        """Lazy initialization of multi-client bridge."""
        if not self.use_protocol_v2:
            return False
        if self._multi_client_bridge is None:
            self._multi_client_bridge = MultiClientBridge(max_clients=self._max_clients)
        return False
    
    xǁBridgeManagerǁ_ensure_multi_client_bridge__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁBridgeManagerǁ_ensure_multi_client_bridge__mutmut_1': xǁBridgeManagerǁ_ensure_multi_client_bridge__mutmut_1, 
        'xǁBridgeManagerǁ_ensure_multi_client_bridge__mutmut_2': xǁBridgeManagerǁ_ensure_multi_client_bridge__mutmut_2, 
        'xǁBridgeManagerǁ_ensure_multi_client_bridge__mutmut_3': xǁBridgeManagerǁ_ensure_multi_client_bridge__mutmut_3, 
        'xǁBridgeManagerǁ_ensure_multi_client_bridge__mutmut_4': xǁBridgeManagerǁ_ensure_multi_client_bridge__mutmut_4, 
        'xǁBridgeManagerǁ_ensure_multi_client_bridge__mutmut_5': xǁBridgeManagerǁ_ensure_multi_client_bridge__mutmut_5, 
        'xǁBridgeManagerǁ_ensure_multi_client_bridge__mutmut_6': xǁBridgeManagerǁ_ensure_multi_client_bridge__mutmut_6
    }
    
    def _ensure_multi_client_bridge(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁBridgeManagerǁ_ensure_multi_client_bridge__mutmut_orig"), object.__getattribute__(self, "xǁBridgeManagerǁ_ensure_multi_client_bridge__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _ensure_multi_client_bridge.__signature__ = _mutmut_signature(xǁBridgeManagerǁ_ensure_multi_client_bridge__mutmut_orig)
    xǁBridgeManagerǁ_ensure_multi_client_bridge__mutmut_orig.__name__ = 'xǁBridgeManagerǁ_ensure_multi_client_bridge'
    
    def xǁBridgeManagerǁregister_client__mutmut_orig(self, client_id: str, socket_path: str, priority: int = 0) -> bool:
        """
        Register a client for multi-client bridge support (Protocol v2).
        
        Args:
            client_id: Unique client identifier
            socket_path: Path to client's socket
            priority: Client priority (higher = more important)
            
        Returns:
            True if registered successfully
        """
        if not self._ensure_multi_client_bridge():
            logger.warning("Multi-client not available (Protocol v2 not enabled)")
            return False
        
        result = self._multi_client_bridge.register_client(client_id, socket_path, priority)
        if result:
            self._audit_log("CLIENT_REGISTERED", {
                "client_id": client_id,
                "priority": priority,
            })
        return result
    
    def xǁBridgeManagerǁregister_client__mutmut_1(self, client_id: str, socket_path: str, priority: int = 1) -> bool:
        """
        Register a client for multi-client bridge support (Protocol v2).
        
        Args:
            client_id: Unique client identifier
            socket_path: Path to client's socket
            priority: Client priority (higher = more important)
            
        Returns:
            True if registered successfully
        """
        if not self._ensure_multi_client_bridge():
            logger.warning("Multi-client not available (Protocol v2 not enabled)")
            return False
        
        result = self._multi_client_bridge.register_client(client_id, socket_path, priority)
        if result:
            self._audit_log("CLIENT_REGISTERED", {
                "client_id": client_id,
                "priority": priority,
            })
        return result
    
    def xǁBridgeManagerǁregister_client__mutmut_2(self, client_id: str, socket_path: str, priority: int = 0) -> bool:
        """
        Register a client for multi-client bridge support (Protocol v2).
        
        Args:
            client_id: Unique client identifier
            socket_path: Path to client's socket
            priority: Client priority (higher = more important)
            
        Returns:
            True if registered successfully
        """
        if self._ensure_multi_client_bridge():
            logger.warning("Multi-client not available (Protocol v2 not enabled)")
            return False
        
        result = self._multi_client_bridge.register_client(client_id, socket_path, priority)
        if result:
            self._audit_log("CLIENT_REGISTERED", {
                "client_id": client_id,
                "priority": priority,
            })
        return result
    
    def xǁBridgeManagerǁregister_client__mutmut_3(self, client_id: str, socket_path: str, priority: int = 0) -> bool:
        """
        Register a client for multi-client bridge support (Protocol v2).
        
        Args:
            client_id: Unique client identifier
            socket_path: Path to client's socket
            priority: Client priority (higher = more important)
            
        Returns:
            True if registered successfully
        """
        if not self._ensure_multi_client_bridge():
            logger.warning(None)
            return False
        
        result = self._multi_client_bridge.register_client(client_id, socket_path, priority)
        if result:
            self._audit_log("CLIENT_REGISTERED", {
                "client_id": client_id,
                "priority": priority,
            })
        return result
    
    def xǁBridgeManagerǁregister_client__mutmut_4(self, client_id: str, socket_path: str, priority: int = 0) -> bool:
        """
        Register a client for multi-client bridge support (Protocol v2).
        
        Args:
            client_id: Unique client identifier
            socket_path: Path to client's socket
            priority: Client priority (higher = more important)
            
        Returns:
            True if registered successfully
        """
        if not self._ensure_multi_client_bridge():
            logger.warning("XXMulti-client not available (Protocol v2 not enabled)XX")
            return False
        
        result = self._multi_client_bridge.register_client(client_id, socket_path, priority)
        if result:
            self._audit_log("CLIENT_REGISTERED", {
                "client_id": client_id,
                "priority": priority,
            })
        return result
    
    def xǁBridgeManagerǁregister_client__mutmut_5(self, client_id: str, socket_path: str, priority: int = 0) -> bool:
        """
        Register a client for multi-client bridge support (Protocol v2).
        
        Args:
            client_id: Unique client identifier
            socket_path: Path to client's socket
            priority: Client priority (higher = more important)
            
        Returns:
            True if registered successfully
        """
        if not self._ensure_multi_client_bridge():
            logger.warning("multi-client not available (protocol v2 not enabled)")
            return False
        
        result = self._multi_client_bridge.register_client(client_id, socket_path, priority)
        if result:
            self._audit_log("CLIENT_REGISTERED", {
                "client_id": client_id,
                "priority": priority,
            })
        return result
    
    def xǁBridgeManagerǁregister_client__mutmut_6(self, client_id: str, socket_path: str, priority: int = 0) -> bool:
        """
        Register a client for multi-client bridge support (Protocol v2).
        
        Args:
            client_id: Unique client identifier
            socket_path: Path to client's socket
            priority: Client priority (higher = more important)
            
        Returns:
            True if registered successfully
        """
        if not self._ensure_multi_client_bridge():
            logger.warning("MULTI-CLIENT NOT AVAILABLE (PROTOCOL V2 NOT ENABLED)")
            return False
        
        result = self._multi_client_bridge.register_client(client_id, socket_path, priority)
        if result:
            self._audit_log("CLIENT_REGISTERED", {
                "client_id": client_id,
                "priority": priority,
            })
        return result
    
    def xǁBridgeManagerǁregister_client__mutmut_7(self, client_id: str, socket_path: str, priority: int = 0) -> bool:
        """
        Register a client for multi-client bridge support (Protocol v2).
        
        Args:
            client_id: Unique client identifier
            socket_path: Path to client's socket
            priority: Client priority (higher = more important)
            
        Returns:
            True if registered successfully
        """
        if not self._ensure_multi_client_bridge():
            logger.warning("Multi-client not available (Protocol v2 not enabled)")
            return True
        
        result = self._multi_client_bridge.register_client(client_id, socket_path, priority)
        if result:
            self._audit_log("CLIENT_REGISTERED", {
                "client_id": client_id,
                "priority": priority,
            })
        return result
    
    def xǁBridgeManagerǁregister_client__mutmut_8(self, client_id: str, socket_path: str, priority: int = 0) -> bool:
        """
        Register a client for multi-client bridge support (Protocol v2).
        
        Args:
            client_id: Unique client identifier
            socket_path: Path to client's socket
            priority: Client priority (higher = more important)
            
        Returns:
            True if registered successfully
        """
        if not self._ensure_multi_client_bridge():
            logger.warning("Multi-client not available (Protocol v2 not enabled)")
            return False
        
        result = None
        if result:
            self._audit_log("CLIENT_REGISTERED", {
                "client_id": client_id,
                "priority": priority,
            })
        return result
    
    def xǁBridgeManagerǁregister_client__mutmut_9(self, client_id: str, socket_path: str, priority: int = 0) -> bool:
        """
        Register a client for multi-client bridge support (Protocol v2).
        
        Args:
            client_id: Unique client identifier
            socket_path: Path to client's socket
            priority: Client priority (higher = more important)
            
        Returns:
            True if registered successfully
        """
        if not self._ensure_multi_client_bridge():
            logger.warning("Multi-client not available (Protocol v2 not enabled)")
            return False
        
        result = self._multi_client_bridge.register_client(None, socket_path, priority)
        if result:
            self._audit_log("CLIENT_REGISTERED", {
                "client_id": client_id,
                "priority": priority,
            })
        return result
    
    def xǁBridgeManagerǁregister_client__mutmut_10(self, client_id: str, socket_path: str, priority: int = 0) -> bool:
        """
        Register a client for multi-client bridge support (Protocol v2).
        
        Args:
            client_id: Unique client identifier
            socket_path: Path to client's socket
            priority: Client priority (higher = more important)
            
        Returns:
            True if registered successfully
        """
        if not self._ensure_multi_client_bridge():
            logger.warning("Multi-client not available (Protocol v2 not enabled)")
            return False
        
        result = self._multi_client_bridge.register_client(client_id, None, priority)
        if result:
            self._audit_log("CLIENT_REGISTERED", {
                "client_id": client_id,
                "priority": priority,
            })
        return result
    
    def xǁBridgeManagerǁregister_client__mutmut_11(self, client_id: str, socket_path: str, priority: int = 0) -> bool:
        """
        Register a client for multi-client bridge support (Protocol v2).
        
        Args:
            client_id: Unique client identifier
            socket_path: Path to client's socket
            priority: Client priority (higher = more important)
            
        Returns:
            True if registered successfully
        """
        if not self._ensure_multi_client_bridge():
            logger.warning("Multi-client not available (Protocol v2 not enabled)")
            return False
        
        result = self._multi_client_bridge.register_client(client_id, socket_path, None)
        if result:
            self._audit_log("CLIENT_REGISTERED", {
                "client_id": client_id,
                "priority": priority,
            })
        return result
    
    def xǁBridgeManagerǁregister_client__mutmut_12(self, client_id: str, socket_path: str, priority: int = 0) -> bool:
        """
        Register a client for multi-client bridge support (Protocol v2).
        
        Args:
            client_id: Unique client identifier
            socket_path: Path to client's socket
            priority: Client priority (higher = more important)
            
        Returns:
            True if registered successfully
        """
        if not self._ensure_multi_client_bridge():
            logger.warning("Multi-client not available (Protocol v2 not enabled)")
            return False
        
        result = self._multi_client_bridge.register_client(socket_path, priority)
        if result:
            self._audit_log("CLIENT_REGISTERED", {
                "client_id": client_id,
                "priority": priority,
            })
        return result
    
    def xǁBridgeManagerǁregister_client__mutmut_13(self, client_id: str, socket_path: str, priority: int = 0) -> bool:
        """
        Register a client for multi-client bridge support (Protocol v2).
        
        Args:
            client_id: Unique client identifier
            socket_path: Path to client's socket
            priority: Client priority (higher = more important)
            
        Returns:
            True if registered successfully
        """
        if not self._ensure_multi_client_bridge():
            logger.warning("Multi-client not available (Protocol v2 not enabled)")
            return False
        
        result = self._multi_client_bridge.register_client(client_id, priority)
        if result:
            self._audit_log("CLIENT_REGISTERED", {
                "client_id": client_id,
                "priority": priority,
            })
        return result
    
    def xǁBridgeManagerǁregister_client__mutmut_14(self, client_id: str, socket_path: str, priority: int = 0) -> bool:
        """
        Register a client for multi-client bridge support (Protocol v2).
        
        Args:
            client_id: Unique client identifier
            socket_path: Path to client's socket
            priority: Client priority (higher = more important)
            
        Returns:
            True if registered successfully
        """
        if not self._ensure_multi_client_bridge():
            logger.warning("Multi-client not available (Protocol v2 not enabled)")
            return False
        
        result = self._multi_client_bridge.register_client(client_id, socket_path, )
        if result:
            self._audit_log("CLIENT_REGISTERED", {
                "client_id": client_id,
                "priority": priority,
            })
        return result
    
    def xǁBridgeManagerǁregister_client__mutmut_15(self, client_id: str, socket_path: str, priority: int = 0) -> bool:
        """
        Register a client for multi-client bridge support (Protocol v2).
        
        Args:
            client_id: Unique client identifier
            socket_path: Path to client's socket
            priority: Client priority (higher = more important)
            
        Returns:
            True if registered successfully
        """
        if not self._ensure_multi_client_bridge():
            logger.warning("Multi-client not available (Protocol v2 not enabled)")
            return False
        
        result = self._multi_client_bridge.register_client(client_id, socket_path, priority)
        if result:
            self._audit_log(None, {
                "client_id": client_id,
                "priority": priority,
            })
        return result
    
    def xǁBridgeManagerǁregister_client__mutmut_16(self, client_id: str, socket_path: str, priority: int = 0) -> bool:
        """
        Register a client for multi-client bridge support (Protocol v2).
        
        Args:
            client_id: Unique client identifier
            socket_path: Path to client's socket
            priority: Client priority (higher = more important)
            
        Returns:
            True if registered successfully
        """
        if not self._ensure_multi_client_bridge():
            logger.warning("Multi-client not available (Protocol v2 not enabled)")
            return False
        
        result = self._multi_client_bridge.register_client(client_id, socket_path, priority)
        if result:
            self._audit_log("CLIENT_REGISTERED", None)
        return result
    
    def xǁBridgeManagerǁregister_client__mutmut_17(self, client_id: str, socket_path: str, priority: int = 0) -> bool:
        """
        Register a client for multi-client bridge support (Protocol v2).
        
        Args:
            client_id: Unique client identifier
            socket_path: Path to client's socket
            priority: Client priority (higher = more important)
            
        Returns:
            True if registered successfully
        """
        if not self._ensure_multi_client_bridge():
            logger.warning("Multi-client not available (Protocol v2 not enabled)")
            return False
        
        result = self._multi_client_bridge.register_client(client_id, socket_path, priority)
        if result:
            self._audit_log({
                "client_id": client_id,
                "priority": priority,
            })
        return result
    
    def xǁBridgeManagerǁregister_client__mutmut_18(self, client_id: str, socket_path: str, priority: int = 0) -> bool:
        """
        Register a client for multi-client bridge support (Protocol v2).
        
        Args:
            client_id: Unique client identifier
            socket_path: Path to client's socket
            priority: Client priority (higher = more important)
            
        Returns:
            True if registered successfully
        """
        if not self._ensure_multi_client_bridge():
            logger.warning("Multi-client not available (Protocol v2 not enabled)")
            return False
        
        result = self._multi_client_bridge.register_client(client_id, socket_path, priority)
        if result:
            self._audit_log("CLIENT_REGISTERED", )
        return result
    
    def xǁBridgeManagerǁregister_client__mutmut_19(self, client_id: str, socket_path: str, priority: int = 0) -> bool:
        """
        Register a client for multi-client bridge support (Protocol v2).
        
        Args:
            client_id: Unique client identifier
            socket_path: Path to client's socket
            priority: Client priority (higher = more important)
            
        Returns:
            True if registered successfully
        """
        if not self._ensure_multi_client_bridge():
            logger.warning("Multi-client not available (Protocol v2 not enabled)")
            return False
        
        result = self._multi_client_bridge.register_client(client_id, socket_path, priority)
        if result:
            self._audit_log("XXCLIENT_REGISTEREDXX", {
                "client_id": client_id,
                "priority": priority,
            })
        return result
    
    def xǁBridgeManagerǁregister_client__mutmut_20(self, client_id: str, socket_path: str, priority: int = 0) -> bool:
        """
        Register a client for multi-client bridge support (Protocol v2).
        
        Args:
            client_id: Unique client identifier
            socket_path: Path to client's socket
            priority: Client priority (higher = more important)
            
        Returns:
            True if registered successfully
        """
        if not self._ensure_multi_client_bridge():
            logger.warning("Multi-client not available (Protocol v2 not enabled)")
            return False
        
        result = self._multi_client_bridge.register_client(client_id, socket_path, priority)
        if result:
            self._audit_log("client_registered", {
                "client_id": client_id,
                "priority": priority,
            })
        return result
    
    def xǁBridgeManagerǁregister_client__mutmut_21(self, client_id: str, socket_path: str, priority: int = 0) -> bool:
        """
        Register a client for multi-client bridge support (Protocol v2).
        
        Args:
            client_id: Unique client identifier
            socket_path: Path to client's socket
            priority: Client priority (higher = more important)
            
        Returns:
            True if registered successfully
        """
        if not self._ensure_multi_client_bridge():
            logger.warning("Multi-client not available (Protocol v2 not enabled)")
            return False
        
        result = self._multi_client_bridge.register_client(client_id, socket_path, priority)
        if result:
            self._audit_log("CLIENT_REGISTERED", {
                "XXclient_idXX": client_id,
                "priority": priority,
            })
        return result
    
    def xǁBridgeManagerǁregister_client__mutmut_22(self, client_id: str, socket_path: str, priority: int = 0) -> bool:
        """
        Register a client for multi-client bridge support (Protocol v2).
        
        Args:
            client_id: Unique client identifier
            socket_path: Path to client's socket
            priority: Client priority (higher = more important)
            
        Returns:
            True if registered successfully
        """
        if not self._ensure_multi_client_bridge():
            logger.warning("Multi-client not available (Protocol v2 not enabled)")
            return False
        
        result = self._multi_client_bridge.register_client(client_id, socket_path, priority)
        if result:
            self._audit_log("CLIENT_REGISTERED", {
                "CLIENT_ID": client_id,
                "priority": priority,
            })
        return result
    
    def xǁBridgeManagerǁregister_client__mutmut_23(self, client_id: str, socket_path: str, priority: int = 0) -> bool:
        """
        Register a client for multi-client bridge support (Protocol v2).
        
        Args:
            client_id: Unique client identifier
            socket_path: Path to client's socket
            priority: Client priority (higher = more important)
            
        Returns:
            True if registered successfully
        """
        if not self._ensure_multi_client_bridge():
            logger.warning("Multi-client not available (Protocol v2 not enabled)")
            return False
        
        result = self._multi_client_bridge.register_client(client_id, socket_path, priority)
        if result:
            self._audit_log("CLIENT_REGISTERED", {
                "client_id": client_id,
                "XXpriorityXX": priority,
            })
        return result
    
    def xǁBridgeManagerǁregister_client__mutmut_24(self, client_id: str, socket_path: str, priority: int = 0) -> bool:
        """
        Register a client for multi-client bridge support (Protocol v2).
        
        Args:
            client_id: Unique client identifier
            socket_path: Path to client's socket
            priority: Client priority (higher = more important)
            
        Returns:
            True if registered successfully
        """
        if not self._ensure_multi_client_bridge():
            logger.warning("Multi-client not available (Protocol v2 not enabled)")
            return False
        
        result = self._multi_client_bridge.register_client(client_id, socket_path, priority)
        if result:
            self._audit_log("CLIENT_REGISTERED", {
                "client_id": client_id,
                "PRIORITY": priority,
            })
        return result
    
    xǁBridgeManagerǁregister_client__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁBridgeManagerǁregister_client__mutmut_1': xǁBridgeManagerǁregister_client__mutmut_1, 
        'xǁBridgeManagerǁregister_client__mutmut_2': xǁBridgeManagerǁregister_client__mutmut_2, 
        'xǁBridgeManagerǁregister_client__mutmut_3': xǁBridgeManagerǁregister_client__mutmut_3, 
        'xǁBridgeManagerǁregister_client__mutmut_4': xǁBridgeManagerǁregister_client__mutmut_4, 
        'xǁBridgeManagerǁregister_client__mutmut_5': xǁBridgeManagerǁregister_client__mutmut_5, 
        'xǁBridgeManagerǁregister_client__mutmut_6': xǁBridgeManagerǁregister_client__mutmut_6, 
        'xǁBridgeManagerǁregister_client__mutmut_7': xǁBridgeManagerǁregister_client__mutmut_7, 
        'xǁBridgeManagerǁregister_client__mutmut_8': xǁBridgeManagerǁregister_client__mutmut_8, 
        'xǁBridgeManagerǁregister_client__mutmut_9': xǁBridgeManagerǁregister_client__mutmut_9, 
        'xǁBridgeManagerǁregister_client__mutmut_10': xǁBridgeManagerǁregister_client__mutmut_10, 
        'xǁBridgeManagerǁregister_client__mutmut_11': xǁBridgeManagerǁregister_client__mutmut_11, 
        'xǁBridgeManagerǁregister_client__mutmut_12': xǁBridgeManagerǁregister_client__mutmut_12, 
        'xǁBridgeManagerǁregister_client__mutmut_13': xǁBridgeManagerǁregister_client__mutmut_13, 
        'xǁBridgeManagerǁregister_client__mutmut_14': xǁBridgeManagerǁregister_client__mutmut_14, 
        'xǁBridgeManagerǁregister_client__mutmut_15': xǁBridgeManagerǁregister_client__mutmut_15, 
        'xǁBridgeManagerǁregister_client__mutmut_16': xǁBridgeManagerǁregister_client__mutmut_16, 
        'xǁBridgeManagerǁregister_client__mutmut_17': xǁBridgeManagerǁregister_client__mutmut_17, 
        'xǁBridgeManagerǁregister_client__mutmut_18': xǁBridgeManagerǁregister_client__mutmut_18, 
        'xǁBridgeManagerǁregister_client__mutmut_19': xǁBridgeManagerǁregister_client__mutmut_19, 
        'xǁBridgeManagerǁregister_client__mutmut_20': xǁBridgeManagerǁregister_client__mutmut_20, 
        'xǁBridgeManagerǁregister_client__mutmut_21': xǁBridgeManagerǁregister_client__mutmut_21, 
        'xǁBridgeManagerǁregister_client__mutmut_22': xǁBridgeManagerǁregister_client__mutmut_22, 
        'xǁBridgeManagerǁregister_client__mutmut_23': xǁBridgeManagerǁregister_client__mutmut_23, 
        'xǁBridgeManagerǁregister_client__mutmut_24': xǁBridgeManagerǁregister_client__mutmut_24
    }
    
    def register_client(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁBridgeManagerǁregister_client__mutmut_orig"), object.__getattribute__(self, "xǁBridgeManagerǁregister_client__mutmut_mutants"), args, kwargs, self)
        return result 
    
    register_client.__signature__ = _mutmut_signature(xǁBridgeManagerǁregister_client__mutmut_orig)
    xǁBridgeManagerǁregister_client__mutmut_orig.__name__ = 'xǁBridgeManagerǁregister_client'
    
    def xǁBridgeManagerǁunregister_client__mutmut_orig(self, client_id: str) -> bool:
        """Unregister a client from the multi-client bridge."""
        if not self._multi_client_bridge:
            return False
        
        result = self._multi_client_bridge.unregister_client(client_id)
        if result:
            self._audit_log("CLIENT_UNREGISTERED", {"client_id": client_id})
        return result
    
    def xǁBridgeManagerǁunregister_client__mutmut_1(self, client_id: str) -> bool:
        """Unregister a client from the multi-client bridge."""
        if self._multi_client_bridge:
            return False
        
        result = self._multi_client_bridge.unregister_client(client_id)
        if result:
            self._audit_log("CLIENT_UNREGISTERED", {"client_id": client_id})
        return result
    
    def xǁBridgeManagerǁunregister_client__mutmut_2(self, client_id: str) -> bool:
        """Unregister a client from the multi-client bridge."""
        if not self._multi_client_bridge:
            return True
        
        result = self._multi_client_bridge.unregister_client(client_id)
        if result:
            self._audit_log("CLIENT_UNREGISTERED", {"client_id": client_id})
        return result
    
    def xǁBridgeManagerǁunregister_client__mutmut_3(self, client_id: str) -> bool:
        """Unregister a client from the multi-client bridge."""
        if not self._multi_client_bridge:
            return False
        
        result = None
        if result:
            self._audit_log("CLIENT_UNREGISTERED", {"client_id": client_id})
        return result
    
    def xǁBridgeManagerǁunregister_client__mutmut_4(self, client_id: str) -> bool:
        """Unregister a client from the multi-client bridge."""
        if not self._multi_client_bridge:
            return False
        
        result = self._multi_client_bridge.unregister_client(None)
        if result:
            self._audit_log("CLIENT_UNREGISTERED", {"client_id": client_id})
        return result
    
    def xǁBridgeManagerǁunregister_client__mutmut_5(self, client_id: str) -> bool:
        """Unregister a client from the multi-client bridge."""
        if not self._multi_client_bridge:
            return False
        
        result = self._multi_client_bridge.unregister_client(client_id)
        if result:
            self._audit_log(None, {"client_id": client_id})
        return result
    
    def xǁBridgeManagerǁunregister_client__mutmut_6(self, client_id: str) -> bool:
        """Unregister a client from the multi-client bridge."""
        if not self._multi_client_bridge:
            return False
        
        result = self._multi_client_bridge.unregister_client(client_id)
        if result:
            self._audit_log("CLIENT_UNREGISTERED", None)
        return result
    
    def xǁBridgeManagerǁunregister_client__mutmut_7(self, client_id: str) -> bool:
        """Unregister a client from the multi-client bridge."""
        if not self._multi_client_bridge:
            return False
        
        result = self._multi_client_bridge.unregister_client(client_id)
        if result:
            self._audit_log({"client_id": client_id})
        return result
    
    def xǁBridgeManagerǁunregister_client__mutmut_8(self, client_id: str) -> bool:
        """Unregister a client from the multi-client bridge."""
        if not self._multi_client_bridge:
            return False
        
        result = self._multi_client_bridge.unregister_client(client_id)
        if result:
            self._audit_log("CLIENT_UNREGISTERED", )
        return result
    
    def xǁBridgeManagerǁunregister_client__mutmut_9(self, client_id: str) -> bool:
        """Unregister a client from the multi-client bridge."""
        if not self._multi_client_bridge:
            return False
        
        result = self._multi_client_bridge.unregister_client(client_id)
        if result:
            self._audit_log("XXCLIENT_UNREGISTEREDXX", {"client_id": client_id})
        return result
    
    def xǁBridgeManagerǁunregister_client__mutmut_10(self, client_id: str) -> bool:
        """Unregister a client from the multi-client bridge."""
        if not self._multi_client_bridge:
            return False
        
        result = self._multi_client_bridge.unregister_client(client_id)
        if result:
            self._audit_log("client_unregistered", {"client_id": client_id})
        return result
    
    def xǁBridgeManagerǁunregister_client__mutmut_11(self, client_id: str) -> bool:
        """Unregister a client from the multi-client bridge."""
        if not self._multi_client_bridge:
            return False
        
        result = self._multi_client_bridge.unregister_client(client_id)
        if result:
            self._audit_log("CLIENT_UNREGISTERED", {"XXclient_idXX": client_id})
        return result
    
    def xǁBridgeManagerǁunregister_client__mutmut_12(self, client_id: str) -> bool:
        """Unregister a client from the multi-client bridge."""
        if not self._multi_client_bridge:
            return False
        
        result = self._multi_client_bridge.unregister_client(client_id)
        if result:
            self._audit_log("CLIENT_UNREGISTERED", {"CLIENT_ID": client_id})
        return result
    
    xǁBridgeManagerǁunregister_client__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁBridgeManagerǁunregister_client__mutmut_1': xǁBridgeManagerǁunregister_client__mutmut_1, 
        'xǁBridgeManagerǁunregister_client__mutmut_2': xǁBridgeManagerǁunregister_client__mutmut_2, 
        'xǁBridgeManagerǁunregister_client__mutmut_3': xǁBridgeManagerǁunregister_client__mutmut_3, 
        'xǁBridgeManagerǁunregister_client__mutmut_4': xǁBridgeManagerǁunregister_client__mutmut_4, 
        'xǁBridgeManagerǁunregister_client__mutmut_5': xǁBridgeManagerǁunregister_client__mutmut_5, 
        'xǁBridgeManagerǁunregister_client__mutmut_6': xǁBridgeManagerǁunregister_client__mutmut_6, 
        'xǁBridgeManagerǁunregister_client__mutmut_7': xǁBridgeManagerǁunregister_client__mutmut_7, 
        'xǁBridgeManagerǁunregister_client__mutmut_8': xǁBridgeManagerǁunregister_client__mutmut_8, 
        'xǁBridgeManagerǁunregister_client__mutmut_9': xǁBridgeManagerǁunregister_client__mutmut_9, 
        'xǁBridgeManagerǁunregister_client__mutmut_10': xǁBridgeManagerǁunregister_client__mutmut_10, 
        'xǁBridgeManagerǁunregister_client__mutmut_11': xǁBridgeManagerǁunregister_client__mutmut_11, 
        'xǁBridgeManagerǁunregister_client__mutmut_12': xǁBridgeManagerǁunregister_client__mutmut_12
    }
    
    def unregister_client(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁBridgeManagerǁunregister_client__mutmut_orig"), object.__getattribute__(self, "xǁBridgeManagerǁunregister_client__mutmut_mutants"), args, kwargs, self)
        return result 
    
    unregister_client.__signature__ = _mutmut_signature(xǁBridgeManagerǁunregister_client__mutmut_orig)
    xǁBridgeManagerǁunregister_client__mutmut_orig.__name__ = 'xǁBridgeManagerǁunregister_client'
    
    def xǁBridgeManagerǁget_bridge_stats__mutmut_orig(self) -> Dict[str, Any]:
        """Get bridge statistics including multi-client info."""
        stats = {
            "mode": self.mode.value,
            "protocol_v2": self.use_protocol_v2,
            "compression_enabled": self.enable_compression,
            "auth_required": self.require_auth,
        }
        
        if self._multi_client_bridge:
            stats["multi_client"] = self._multi_client_bridge.get_stats()
        
        return stats
    
    def xǁBridgeManagerǁget_bridge_stats__mutmut_1(self) -> Dict[str, Any]:
        """Get bridge statistics including multi-client info."""
        stats = None
        
        if self._multi_client_bridge:
            stats["multi_client"] = self._multi_client_bridge.get_stats()
        
        return stats
    
    def xǁBridgeManagerǁget_bridge_stats__mutmut_2(self) -> Dict[str, Any]:
        """Get bridge statistics including multi-client info."""
        stats = {
            "XXmodeXX": self.mode.value,
            "protocol_v2": self.use_protocol_v2,
            "compression_enabled": self.enable_compression,
            "auth_required": self.require_auth,
        }
        
        if self._multi_client_bridge:
            stats["multi_client"] = self._multi_client_bridge.get_stats()
        
        return stats
    
    def xǁBridgeManagerǁget_bridge_stats__mutmut_3(self) -> Dict[str, Any]:
        """Get bridge statistics including multi-client info."""
        stats = {
            "MODE": self.mode.value,
            "protocol_v2": self.use_protocol_v2,
            "compression_enabled": self.enable_compression,
            "auth_required": self.require_auth,
        }
        
        if self._multi_client_bridge:
            stats["multi_client"] = self._multi_client_bridge.get_stats()
        
        return stats
    
    def xǁBridgeManagerǁget_bridge_stats__mutmut_4(self) -> Dict[str, Any]:
        """Get bridge statistics including multi-client info."""
        stats = {
            "mode": self.mode.value,
            "XXprotocol_v2XX": self.use_protocol_v2,
            "compression_enabled": self.enable_compression,
            "auth_required": self.require_auth,
        }
        
        if self._multi_client_bridge:
            stats["multi_client"] = self._multi_client_bridge.get_stats()
        
        return stats
    
    def xǁBridgeManagerǁget_bridge_stats__mutmut_5(self) -> Dict[str, Any]:
        """Get bridge statistics including multi-client info."""
        stats = {
            "mode": self.mode.value,
            "PROTOCOL_V2": self.use_protocol_v2,
            "compression_enabled": self.enable_compression,
            "auth_required": self.require_auth,
        }
        
        if self._multi_client_bridge:
            stats["multi_client"] = self._multi_client_bridge.get_stats()
        
        return stats
    
    def xǁBridgeManagerǁget_bridge_stats__mutmut_6(self) -> Dict[str, Any]:
        """Get bridge statistics including multi-client info."""
        stats = {
            "mode": self.mode.value,
            "protocol_v2": self.use_protocol_v2,
            "XXcompression_enabledXX": self.enable_compression,
            "auth_required": self.require_auth,
        }
        
        if self._multi_client_bridge:
            stats["multi_client"] = self._multi_client_bridge.get_stats()
        
        return stats
    
    def xǁBridgeManagerǁget_bridge_stats__mutmut_7(self) -> Dict[str, Any]:
        """Get bridge statistics including multi-client info."""
        stats = {
            "mode": self.mode.value,
            "protocol_v2": self.use_protocol_v2,
            "COMPRESSION_ENABLED": self.enable_compression,
            "auth_required": self.require_auth,
        }
        
        if self._multi_client_bridge:
            stats["multi_client"] = self._multi_client_bridge.get_stats()
        
        return stats
    
    def xǁBridgeManagerǁget_bridge_stats__mutmut_8(self) -> Dict[str, Any]:
        """Get bridge statistics including multi-client info."""
        stats = {
            "mode": self.mode.value,
            "protocol_v2": self.use_protocol_v2,
            "compression_enabled": self.enable_compression,
            "XXauth_requiredXX": self.require_auth,
        }
        
        if self._multi_client_bridge:
            stats["multi_client"] = self._multi_client_bridge.get_stats()
        
        return stats
    
    def xǁBridgeManagerǁget_bridge_stats__mutmut_9(self) -> Dict[str, Any]:
        """Get bridge statistics including multi-client info."""
        stats = {
            "mode": self.mode.value,
            "protocol_v2": self.use_protocol_v2,
            "compression_enabled": self.enable_compression,
            "AUTH_REQUIRED": self.require_auth,
        }
        
        if self._multi_client_bridge:
            stats["multi_client"] = self._multi_client_bridge.get_stats()
        
        return stats
    
    def xǁBridgeManagerǁget_bridge_stats__mutmut_10(self) -> Dict[str, Any]:
        """Get bridge statistics including multi-client info."""
        stats = {
            "mode": self.mode.value,
            "protocol_v2": self.use_protocol_v2,
            "compression_enabled": self.enable_compression,
            "auth_required": self.require_auth,
        }
        
        if self._multi_client_bridge:
            stats["multi_client"] = None
        
        return stats
    
    def xǁBridgeManagerǁget_bridge_stats__mutmut_11(self) -> Dict[str, Any]:
        """Get bridge statistics including multi-client info."""
        stats = {
            "mode": self.mode.value,
            "protocol_v2": self.use_protocol_v2,
            "compression_enabled": self.enable_compression,
            "auth_required": self.require_auth,
        }
        
        if self._multi_client_bridge:
            stats["XXmulti_clientXX"] = self._multi_client_bridge.get_stats()
        
        return stats
    
    def xǁBridgeManagerǁget_bridge_stats__mutmut_12(self) -> Dict[str, Any]:
        """Get bridge statistics including multi-client info."""
        stats = {
            "mode": self.mode.value,
            "protocol_v2": self.use_protocol_v2,
            "compression_enabled": self.enable_compression,
            "auth_required": self.require_auth,
        }
        
        if self._multi_client_bridge:
            stats["MULTI_CLIENT"] = self._multi_client_bridge.get_stats()
        
        return stats
    
    xǁBridgeManagerǁget_bridge_stats__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁBridgeManagerǁget_bridge_stats__mutmut_1': xǁBridgeManagerǁget_bridge_stats__mutmut_1, 
        'xǁBridgeManagerǁget_bridge_stats__mutmut_2': xǁBridgeManagerǁget_bridge_stats__mutmut_2, 
        'xǁBridgeManagerǁget_bridge_stats__mutmut_3': xǁBridgeManagerǁget_bridge_stats__mutmut_3, 
        'xǁBridgeManagerǁget_bridge_stats__mutmut_4': xǁBridgeManagerǁget_bridge_stats__mutmut_4, 
        'xǁBridgeManagerǁget_bridge_stats__mutmut_5': xǁBridgeManagerǁget_bridge_stats__mutmut_5, 
        'xǁBridgeManagerǁget_bridge_stats__mutmut_6': xǁBridgeManagerǁget_bridge_stats__mutmut_6, 
        'xǁBridgeManagerǁget_bridge_stats__mutmut_7': xǁBridgeManagerǁget_bridge_stats__mutmut_7, 
        'xǁBridgeManagerǁget_bridge_stats__mutmut_8': xǁBridgeManagerǁget_bridge_stats__mutmut_8, 
        'xǁBridgeManagerǁget_bridge_stats__mutmut_9': xǁBridgeManagerǁget_bridge_stats__mutmut_9, 
        'xǁBridgeManagerǁget_bridge_stats__mutmut_10': xǁBridgeManagerǁget_bridge_stats__mutmut_10, 
        'xǁBridgeManagerǁget_bridge_stats__mutmut_11': xǁBridgeManagerǁget_bridge_stats__mutmut_11, 
        'xǁBridgeManagerǁget_bridge_stats__mutmut_12': xǁBridgeManagerǁget_bridge_stats__mutmut_12
    }
    
    def get_bridge_stats(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁBridgeManagerǁget_bridge_stats__mutmut_orig"), object.__getattribute__(self, "xǁBridgeManagerǁget_bridge_stats__mutmut_mutants"), args, kwargs, self)
        return result 
    
    get_bridge_stats.__signature__ = _mutmut_signature(xǁBridgeManagerǁget_bridge_stats__mutmut_orig)
    xǁBridgeManagerǁget_bridge_stats__mutmut_orig.__name__ = 'xǁBridgeManagerǁget_bridge_stats'
    
    def xǁBridgeManagerǁread_message__mutmut_orig(self, timeout: Optional[int] = None) -> Optional[ContextMessage]:
        """
        Read a message from the bridge with locking and audit logging (PS-02).
        
        Args:
            timeout: Maximum seconds to wait for message
            
        Returns:
            ContextMessage if read successful, None otherwise
        """
        try:
            with bridge_lock(self.lock_path, timeout=timeout or 5):
                if self.mode == BridgeMode.NAMED_PIPE:
                    message = self._read_from_pipe()
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    message = self._read_from_socket()
                else:
                    message = None
                
                if message:
                    # Verify authentication for received messages (PS-02)
                    if not self._verify_auth_token(message):
                        return None
                    
                    self._audit_log("MESSAGE_RECEIVED", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                    return message
                
                return None
        
        except TimeoutError as e:
            logger.warning(f"Bridge read timeout: {e}")
            self._audit_log("READ_TIMEOUT", {"error": str(e)})
            return None
        except Exception as e:
            logger.error(f"Bridge read error: {e}")
            self._audit_log("READ_ERROR", {"error": str(e)})
            return None
    
    def xǁBridgeManagerǁread_message__mutmut_1(self, timeout: Optional[int] = None) -> Optional[ContextMessage]:
        """
        Read a message from the bridge with locking and audit logging (PS-02).
        
        Args:
            timeout: Maximum seconds to wait for message
            
        Returns:
            ContextMessage if read successful, None otherwise
        """
        try:
            with bridge_lock(None, timeout=timeout or 5):
                if self.mode == BridgeMode.NAMED_PIPE:
                    message = self._read_from_pipe()
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    message = self._read_from_socket()
                else:
                    message = None
                
                if message:
                    # Verify authentication for received messages (PS-02)
                    if not self._verify_auth_token(message):
                        return None
                    
                    self._audit_log("MESSAGE_RECEIVED", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                    return message
                
                return None
        
        except TimeoutError as e:
            logger.warning(f"Bridge read timeout: {e}")
            self._audit_log("READ_TIMEOUT", {"error": str(e)})
            return None
        except Exception as e:
            logger.error(f"Bridge read error: {e}")
            self._audit_log("READ_ERROR", {"error": str(e)})
            return None
    
    def xǁBridgeManagerǁread_message__mutmut_2(self, timeout: Optional[int] = None) -> Optional[ContextMessage]:
        """
        Read a message from the bridge with locking and audit logging (PS-02).
        
        Args:
            timeout: Maximum seconds to wait for message
            
        Returns:
            ContextMessage if read successful, None otherwise
        """
        try:
            with bridge_lock(self.lock_path, timeout=None):
                if self.mode == BridgeMode.NAMED_PIPE:
                    message = self._read_from_pipe()
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    message = self._read_from_socket()
                else:
                    message = None
                
                if message:
                    # Verify authentication for received messages (PS-02)
                    if not self._verify_auth_token(message):
                        return None
                    
                    self._audit_log("MESSAGE_RECEIVED", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                    return message
                
                return None
        
        except TimeoutError as e:
            logger.warning(f"Bridge read timeout: {e}")
            self._audit_log("READ_TIMEOUT", {"error": str(e)})
            return None
        except Exception as e:
            logger.error(f"Bridge read error: {e}")
            self._audit_log("READ_ERROR", {"error": str(e)})
            return None
    
    def xǁBridgeManagerǁread_message__mutmut_3(self, timeout: Optional[int] = None) -> Optional[ContextMessage]:
        """
        Read a message from the bridge with locking and audit logging (PS-02).
        
        Args:
            timeout: Maximum seconds to wait for message
            
        Returns:
            ContextMessage if read successful, None otherwise
        """
        try:
            with bridge_lock(timeout=timeout or 5):
                if self.mode == BridgeMode.NAMED_PIPE:
                    message = self._read_from_pipe()
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    message = self._read_from_socket()
                else:
                    message = None
                
                if message:
                    # Verify authentication for received messages (PS-02)
                    if not self._verify_auth_token(message):
                        return None
                    
                    self._audit_log("MESSAGE_RECEIVED", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                    return message
                
                return None
        
        except TimeoutError as e:
            logger.warning(f"Bridge read timeout: {e}")
            self._audit_log("READ_TIMEOUT", {"error": str(e)})
            return None
        except Exception as e:
            logger.error(f"Bridge read error: {e}")
            self._audit_log("READ_ERROR", {"error": str(e)})
            return None
    
    def xǁBridgeManagerǁread_message__mutmut_4(self, timeout: Optional[int] = None) -> Optional[ContextMessage]:
        """
        Read a message from the bridge with locking and audit logging (PS-02).
        
        Args:
            timeout: Maximum seconds to wait for message
            
        Returns:
            ContextMessage if read successful, None otherwise
        """
        try:
            with bridge_lock(self.lock_path, ):
                if self.mode == BridgeMode.NAMED_PIPE:
                    message = self._read_from_pipe()
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    message = self._read_from_socket()
                else:
                    message = None
                
                if message:
                    # Verify authentication for received messages (PS-02)
                    if not self._verify_auth_token(message):
                        return None
                    
                    self._audit_log("MESSAGE_RECEIVED", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                    return message
                
                return None
        
        except TimeoutError as e:
            logger.warning(f"Bridge read timeout: {e}")
            self._audit_log("READ_TIMEOUT", {"error": str(e)})
            return None
        except Exception as e:
            logger.error(f"Bridge read error: {e}")
            self._audit_log("READ_ERROR", {"error": str(e)})
            return None
    
    def xǁBridgeManagerǁread_message__mutmut_5(self, timeout: Optional[int] = None) -> Optional[ContextMessage]:
        """
        Read a message from the bridge with locking and audit logging (PS-02).
        
        Args:
            timeout: Maximum seconds to wait for message
            
        Returns:
            ContextMessage if read successful, None otherwise
        """
        try:
            with bridge_lock(self.lock_path, timeout=timeout and 5):
                if self.mode == BridgeMode.NAMED_PIPE:
                    message = self._read_from_pipe()
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    message = self._read_from_socket()
                else:
                    message = None
                
                if message:
                    # Verify authentication for received messages (PS-02)
                    if not self._verify_auth_token(message):
                        return None
                    
                    self._audit_log("MESSAGE_RECEIVED", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                    return message
                
                return None
        
        except TimeoutError as e:
            logger.warning(f"Bridge read timeout: {e}")
            self._audit_log("READ_TIMEOUT", {"error": str(e)})
            return None
        except Exception as e:
            logger.error(f"Bridge read error: {e}")
            self._audit_log("READ_ERROR", {"error": str(e)})
            return None
    
    def xǁBridgeManagerǁread_message__mutmut_6(self, timeout: Optional[int] = None) -> Optional[ContextMessage]:
        """
        Read a message from the bridge with locking and audit logging (PS-02).
        
        Args:
            timeout: Maximum seconds to wait for message
            
        Returns:
            ContextMessage if read successful, None otherwise
        """
        try:
            with bridge_lock(self.lock_path, timeout=timeout or 6):
                if self.mode == BridgeMode.NAMED_PIPE:
                    message = self._read_from_pipe()
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    message = self._read_from_socket()
                else:
                    message = None
                
                if message:
                    # Verify authentication for received messages (PS-02)
                    if not self._verify_auth_token(message):
                        return None
                    
                    self._audit_log("MESSAGE_RECEIVED", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                    return message
                
                return None
        
        except TimeoutError as e:
            logger.warning(f"Bridge read timeout: {e}")
            self._audit_log("READ_TIMEOUT", {"error": str(e)})
            return None
        except Exception as e:
            logger.error(f"Bridge read error: {e}")
            self._audit_log("READ_ERROR", {"error": str(e)})
            return None
    
    def xǁBridgeManagerǁread_message__mutmut_7(self, timeout: Optional[int] = None) -> Optional[ContextMessage]:
        """
        Read a message from the bridge with locking and audit logging (PS-02).
        
        Args:
            timeout: Maximum seconds to wait for message
            
        Returns:
            ContextMessage if read successful, None otherwise
        """
        try:
            with bridge_lock(self.lock_path, timeout=timeout or 5):
                if self.mode != BridgeMode.NAMED_PIPE:
                    message = self._read_from_pipe()
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    message = self._read_from_socket()
                else:
                    message = None
                
                if message:
                    # Verify authentication for received messages (PS-02)
                    if not self._verify_auth_token(message):
                        return None
                    
                    self._audit_log("MESSAGE_RECEIVED", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                    return message
                
                return None
        
        except TimeoutError as e:
            logger.warning(f"Bridge read timeout: {e}")
            self._audit_log("READ_TIMEOUT", {"error": str(e)})
            return None
        except Exception as e:
            logger.error(f"Bridge read error: {e}")
            self._audit_log("READ_ERROR", {"error": str(e)})
            return None
    
    def xǁBridgeManagerǁread_message__mutmut_8(self, timeout: Optional[int] = None) -> Optional[ContextMessage]:
        """
        Read a message from the bridge with locking and audit logging (PS-02).
        
        Args:
            timeout: Maximum seconds to wait for message
            
        Returns:
            ContextMessage if read successful, None otherwise
        """
        try:
            with bridge_lock(self.lock_path, timeout=timeout or 5):
                if self.mode == BridgeMode.NAMED_PIPE:
                    message = None
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    message = self._read_from_socket()
                else:
                    message = None
                
                if message:
                    # Verify authentication for received messages (PS-02)
                    if not self._verify_auth_token(message):
                        return None
                    
                    self._audit_log("MESSAGE_RECEIVED", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                    return message
                
                return None
        
        except TimeoutError as e:
            logger.warning(f"Bridge read timeout: {e}")
            self._audit_log("READ_TIMEOUT", {"error": str(e)})
            return None
        except Exception as e:
            logger.error(f"Bridge read error: {e}")
            self._audit_log("READ_ERROR", {"error": str(e)})
            return None
    
    def xǁBridgeManagerǁread_message__mutmut_9(self, timeout: Optional[int] = None) -> Optional[ContextMessage]:
        """
        Read a message from the bridge with locking and audit logging (PS-02).
        
        Args:
            timeout: Maximum seconds to wait for message
            
        Returns:
            ContextMessage if read successful, None otherwise
        """
        try:
            with bridge_lock(self.lock_path, timeout=timeout or 5):
                if self.mode == BridgeMode.NAMED_PIPE:
                    message = self._read_from_pipe()
                elif self.mode != BridgeMode.UNIX_SOCKET:
                    message = self._read_from_socket()
                else:
                    message = None
                
                if message:
                    # Verify authentication for received messages (PS-02)
                    if not self._verify_auth_token(message):
                        return None
                    
                    self._audit_log("MESSAGE_RECEIVED", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                    return message
                
                return None
        
        except TimeoutError as e:
            logger.warning(f"Bridge read timeout: {e}")
            self._audit_log("READ_TIMEOUT", {"error": str(e)})
            return None
        except Exception as e:
            logger.error(f"Bridge read error: {e}")
            self._audit_log("READ_ERROR", {"error": str(e)})
            return None
    
    def xǁBridgeManagerǁread_message__mutmut_10(self, timeout: Optional[int] = None) -> Optional[ContextMessage]:
        """
        Read a message from the bridge with locking and audit logging (PS-02).
        
        Args:
            timeout: Maximum seconds to wait for message
            
        Returns:
            ContextMessage if read successful, None otherwise
        """
        try:
            with bridge_lock(self.lock_path, timeout=timeout or 5):
                if self.mode == BridgeMode.NAMED_PIPE:
                    message = self._read_from_pipe()
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    message = None
                else:
                    message = None
                
                if message:
                    # Verify authentication for received messages (PS-02)
                    if not self._verify_auth_token(message):
                        return None
                    
                    self._audit_log("MESSAGE_RECEIVED", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                    return message
                
                return None
        
        except TimeoutError as e:
            logger.warning(f"Bridge read timeout: {e}")
            self._audit_log("READ_TIMEOUT", {"error": str(e)})
            return None
        except Exception as e:
            logger.error(f"Bridge read error: {e}")
            self._audit_log("READ_ERROR", {"error": str(e)})
            return None
    
    def xǁBridgeManagerǁread_message__mutmut_11(self, timeout: Optional[int] = None) -> Optional[ContextMessage]:
        """
        Read a message from the bridge with locking and audit logging (PS-02).
        
        Args:
            timeout: Maximum seconds to wait for message
            
        Returns:
            ContextMessage if read successful, None otherwise
        """
        try:
            with bridge_lock(self.lock_path, timeout=timeout or 5):
                if self.mode == BridgeMode.NAMED_PIPE:
                    message = self._read_from_pipe()
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    message = self._read_from_socket()
                else:
                    message = ""
                
                if message:
                    # Verify authentication for received messages (PS-02)
                    if not self._verify_auth_token(message):
                        return None
                    
                    self._audit_log("MESSAGE_RECEIVED", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                    return message
                
                return None
        
        except TimeoutError as e:
            logger.warning(f"Bridge read timeout: {e}")
            self._audit_log("READ_TIMEOUT", {"error": str(e)})
            return None
        except Exception as e:
            logger.error(f"Bridge read error: {e}")
            self._audit_log("READ_ERROR", {"error": str(e)})
            return None
    
    def xǁBridgeManagerǁread_message__mutmut_12(self, timeout: Optional[int] = None) -> Optional[ContextMessage]:
        """
        Read a message from the bridge with locking and audit logging (PS-02).
        
        Args:
            timeout: Maximum seconds to wait for message
            
        Returns:
            ContextMessage if read successful, None otherwise
        """
        try:
            with bridge_lock(self.lock_path, timeout=timeout or 5):
                if self.mode == BridgeMode.NAMED_PIPE:
                    message = self._read_from_pipe()
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    message = self._read_from_socket()
                else:
                    message = None
                
                if message:
                    # Verify authentication for received messages (PS-02)
                    if self._verify_auth_token(message):
                        return None
                    
                    self._audit_log("MESSAGE_RECEIVED", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                    return message
                
                return None
        
        except TimeoutError as e:
            logger.warning(f"Bridge read timeout: {e}")
            self._audit_log("READ_TIMEOUT", {"error": str(e)})
            return None
        except Exception as e:
            logger.error(f"Bridge read error: {e}")
            self._audit_log("READ_ERROR", {"error": str(e)})
            return None
    
    def xǁBridgeManagerǁread_message__mutmut_13(self, timeout: Optional[int] = None) -> Optional[ContextMessage]:
        """
        Read a message from the bridge with locking and audit logging (PS-02).
        
        Args:
            timeout: Maximum seconds to wait for message
            
        Returns:
            ContextMessage if read successful, None otherwise
        """
        try:
            with bridge_lock(self.lock_path, timeout=timeout or 5):
                if self.mode == BridgeMode.NAMED_PIPE:
                    message = self._read_from_pipe()
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    message = self._read_from_socket()
                else:
                    message = None
                
                if message:
                    # Verify authentication for received messages (PS-02)
                    if not self._verify_auth_token(None):
                        return None
                    
                    self._audit_log("MESSAGE_RECEIVED", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                    return message
                
                return None
        
        except TimeoutError as e:
            logger.warning(f"Bridge read timeout: {e}")
            self._audit_log("READ_TIMEOUT", {"error": str(e)})
            return None
        except Exception as e:
            logger.error(f"Bridge read error: {e}")
            self._audit_log("READ_ERROR", {"error": str(e)})
            return None
    
    def xǁBridgeManagerǁread_message__mutmut_14(self, timeout: Optional[int] = None) -> Optional[ContextMessage]:
        """
        Read a message from the bridge with locking and audit logging (PS-02).
        
        Args:
            timeout: Maximum seconds to wait for message
            
        Returns:
            ContextMessage if read successful, None otherwise
        """
        try:
            with bridge_lock(self.lock_path, timeout=timeout or 5):
                if self.mode == BridgeMode.NAMED_PIPE:
                    message = self._read_from_pipe()
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    message = self._read_from_socket()
                else:
                    message = None
                
                if message:
                    # Verify authentication for received messages (PS-02)
                    if not self._verify_auth_token(message):
                        return None
                    
                    self._audit_log(None, {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                    return message
                
                return None
        
        except TimeoutError as e:
            logger.warning(f"Bridge read timeout: {e}")
            self._audit_log("READ_TIMEOUT", {"error": str(e)})
            return None
        except Exception as e:
            logger.error(f"Bridge read error: {e}")
            self._audit_log("READ_ERROR", {"error": str(e)})
            return None
    
    def xǁBridgeManagerǁread_message__mutmut_15(self, timeout: Optional[int] = None) -> Optional[ContextMessage]:
        """
        Read a message from the bridge with locking and audit logging (PS-02).
        
        Args:
            timeout: Maximum seconds to wait for message
            
        Returns:
            ContextMessage if read successful, None otherwise
        """
        try:
            with bridge_lock(self.lock_path, timeout=timeout or 5):
                if self.mode == BridgeMode.NAMED_PIPE:
                    message = self._read_from_pipe()
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    message = self._read_from_socket()
                else:
                    message = None
                
                if message:
                    # Verify authentication for received messages (PS-02)
                    if not self._verify_auth_token(message):
                        return None
                    
                    self._audit_log("MESSAGE_RECEIVED", None)
                    return message
                
                return None
        
        except TimeoutError as e:
            logger.warning(f"Bridge read timeout: {e}")
            self._audit_log("READ_TIMEOUT", {"error": str(e)})
            return None
        except Exception as e:
            logger.error(f"Bridge read error: {e}")
            self._audit_log("READ_ERROR", {"error": str(e)})
            return None
    
    def xǁBridgeManagerǁread_message__mutmut_16(self, timeout: Optional[int] = None) -> Optional[ContextMessage]:
        """
        Read a message from the bridge with locking and audit logging (PS-02).
        
        Args:
            timeout: Maximum seconds to wait for message
            
        Returns:
            ContextMessage if read successful, None otherwise
        """
        try:
            with bridge_lock(self.lock_path, timeout=timeout or 5):
                if self.mode == BridgeMode.NAMED_PIPE:
                    message = self._read_from_pipe()
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    message = self._read_from_socket()
                else:
                    message = None
                
                if message:
                    # Verify authentication for received messages (PS-02)
                    if not self._verify_auth_token(message):
                        return None
                    
                    self._audit_log({
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                    return message
                
                return None
        
        except TimeoutError as e:
            logger.warning(f"Bridge read timeout: {e}")
            self._audit_log("READ_TIMEOUT", {"error": str(e)})
            return None
        except Exception as e:
            logger.error(f"Bridge read error: {e}")
            self._audit_log("READ_ERROR", {"error": str(e)})
            return None
    
    def xǁBridgeManagerǁread_message__mutmut_17(self, timeout: Optional[int] = None) -> Optional[ContextMessage]:
        """
        Read a message from the bridge with locking and audit logging (PS-02).
        
        Args:
            timeout: Maximum seconds to wait for message
            
        Returns:
            ContextMessage if read successful, None otherwise
        """
        try:
            with bridge_lock(self.lock_path, timeout=timeout or 5):
                if self.mode == BridgeMode.NAMED_PIPE:
                    message = self._read_from_pipe()
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    message = self._read_from_socket()
                else:
                    message = None
                
                if message:
                    # Verify authentication for received messages (PS-02)
                    if not self._verify_auth_token(message):
                        return None
                    
                    self._audit_log("MESSAGE_RECEIVED", )
                    return message
                
                return None
        
        except TimeoutError as e:
            logger.warning(f"Bridge read timeout: {e}")
            self._audit_log("READ_TIMEOUT", {"error": str(e)})
            return None
        except Exception as e:
            logger.error(f"Bridge read error: {e}")
            self._audit_log("READ_ERROR", {"error": str(e)})
            return None
    
    def xǁBridgeManagerǁread_message__mutmut_18(self, timeout: Optional[int] = None) -> Optional[ContextMessage]:
        """
        Read a message from the bridge with locking and audit logging (PS-02).
        
        Args:
            timeout: Maximum seconds to wait for message
            
        Returns:
            ContextMessage if read successful, None otherwise
        """
        try:
            with bridge_lock(self.lock_path, timeout=timeout or 5):
                if self.mode == BridgeMode.NAMED_PIPE:
                    message = self._read_from_pipe()
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    message = self._read_from_socket()
                else:
                    message = None
                
                if message:
                    # Verify authentication for received messages (PS-02)
                    if not self._verify_auth_token(message):
                        return None
                    
                    self._audit_log("XXMESSAGE_RECEIVEDXX", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                    return message
                
                return None
        
        except TimeoutError as e:
            logger.warning(f"Bridge read timeout: {e}")
            self._audit_log("READ_TIMEOUT", {"error": str(e)})
            return None
        except Exception as e:
            logger.error(f"Bridge read error: {e}")
            self._audit_log("READ_ERROR", {"error": str(e)})
            return None
    
    def xǁBridgeManagerǁread_message__mutmut_19(self, timeout: Optional[int] = None) -> Optional[ContextMessage]:
        """
        Read a message from the bridge with locking and audit logging (PS-02).
        
        Args:
            timeout: Maximum seconds to wait for message
            
        Returns:
            ContextMessage if read successful, None otherwise
        """
        try:
            with bridge_lock(self.lock_path, timeout=timeout or 5):
                if self.mode == BridgeMode.NAMED_PIPE:
                    message = self._read_from_pipe()
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    message = self._read_from_socket()
                else:
                    message = None
                
                if message:
                    # Verify authentication for received messages (PS-02)
                    if not self._verify_auth_token(message):
                        return None
                    
                    self._audit_log("message_received", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                    return message
                
                return None
        
        except TimeoutError as e:
            logger.warning(f"Bridge read timeout: {e}")
            self._audit_log("READ_TIMEOUT", {"error": str(e)})
            return None
        except Exception as e:
            logger.error(f"Bridge read error: {e}")
            self._audit_log("READ_ERROR", {"error": str(e)})
            return None
    
    def xǁBridgeManagerǁread_message__mutmut_20(self, timeout: Optional[int] = None) -> Optional[ContextMessage]:
        """
        Read a message from the bridge with locking and audit logging (PS-02).
        
        Args:
            timeout: Maximum seconds to wait for message
            
        Returns:
            ContextMessage if read successful, None otherwise
        """
        try:
            with bridge_lock(self.lock_path, timeout=timeout or 5):
                if self.mode == BridgeMode.NAMED_PIPE:
                    message = self._read_from_pipe()
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    message = self._read_from_socket()
                else:
                    message = None
                
                if message:
                    # Verify authentication for received messages (PS-02)
                    if not self._verify_auth_token(message):
                        return None
                    
                    self._audit_log("MESSAGE_RECEIVED", {
                        "XXsourceXX": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                    return message
                
                return None
        
        except TimeoutError as e:
            logger.warning(f"Bridge read timeout: {e}")
            self._audit_log("READ_TIMEOUT", {"error": str(e)})
            return None
        except Exception as e:
            logger.error(f"Bridge read error: {e}")
            self._audit_log("READ_ERROR", {"error": str(e)})
            return None
    
    def xǁBridgeManagerǁread_message__mutmut_21(self, timeout: Optional[int] = None) -> Optional[ContextMessage]:
        """
        Read a message from the bridge with locking and audit logging (PS-02).
        
        Args:
            timeout: Maximum seconds to wait for message
            
        Returns:
            ContextMessage if read successful, None otherwise
        """
        try:
            with bridge_lock(self.lock_path, timeout=timeout or 5):
                if self.mode == BridgeMode.NAMED_PIPE:
                    message = self._read_from_pipe()
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    message = self._read_from_socket()
                else:
                    message = None
                
                if message:
                    # Verify authentication for received messages (PS-02)
                    if not self._verify_auth_token(message):
                        return None
                    
                    self._audit_log("MESSAGE_RECEIVED", {
                        "SOURCE": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                    return message
                
                return None
        
        except TimeoutError as e:
            logger.warning(f"Bridge read timeout: {e}")
            self._audit_log("READ_TIMEOUT", {"error": str(e)})
            return None
        except Exception as e:
            logger.error(f"Bridge read error: {e}")
            self._audit_log("READ_ERROR", {"error": str(e)})
            return None
    
    def xǁBridgeManagerǁread_message__mutmut_22(self, timeout: Optional[int] = None) -> Optional[ContextMessage]:
        """
        Read a message from the bridge with locking and audit logging (PS-02).
        
        Args:
            timeout: Maximum seconds to wait for message
            
        Returns:
            ContextMessage if read successful, None otherwise
        """
        try:
            with bridge_lock(self.lock_path, timeout=timeout or 5):
                if self.mode == BridgeMode.NAMED_PIPE:
                    message = self._read_from_pipe()
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    message = self._read_from_socket()
                else:
                    message = None
                
                if message:
                    # Verify authentication for received messages (PS-02)
                    if not self._verify_auth_token(message):
                        return None
                    
                    self._audit_log("MESSAGE_RECEIVED", {
                        "source": message.source,
                        "XXmessage_typeXX": message.message_type,
                        "mode": self.mode.value
                    })
                    return message
                
                return None
        
        except TimeoutError as e:
            logger.warning(f"Bridge read timeout: {e}")
            self._audit_log("READ_TIMEOUT", {"error": str(e)})
            return None
        except Exception as e:
            logger.error(f"Bridge read error: {e}")
            self._audit_log("READ_ERROR", {"error": str(e)})
            return None
    
    def xǁBridgeManagerǁread_message__mutmut_23(self, timeout: Optional[int] = None) -> Optional[ContextMessage]:
        """
        Read a message from the bridge with locking and audit logging (PS-02).
        
        Args:
            timeout: Maximum seconds to wait for message
            
        Returns:
            ContextMessage if read successful, None otherwise
        """
        try:
            with bridge_lock(self.lock_path, timeout=timeout or 5):
                if self.mode == BridgeMode.NAMED_PIPE:
                    message = self._read_from_pipe()
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    message = self._read_from_socket()
                else:
                    message = None
                
                if message:
                    # Verify authentication for received messages (PS-02)
                    if not self._verify_auth_token(message):
                        return None
                    
                    self._audit_log("MESSAGE_RECEIVED", {
                        "source": message.source,
                        "MESSAGE_TYPE": message.message_type,
                        "mode": self.mode.value
                    })
                    return message
                
                return None
        
        except TimeoutError as e:
            logger.warning(f"Bridge read timeout: {e}")
            self._audit_log("READ_TIMEOUT", {"error": str(e)})
            return None
        except Exception as e:
            logger.error(f"Bridge read error: {e}")
            self._audit_log("READ_ERROR", {"error": str(e)})
            return None
    
    def xǁBridgeManagerǁread_message__mutmut_24(self, timeout: Optional[int] = None) -> Optional[ContextMessage]:
        """
        Read a message from the bridge with locking and audit logging (PS-02).
        
        Args:
            timeout: Maximum seconds to wait for message
            
        Returns:
            ContextMessage if read successful, None otherwise
        """
        try:
            with bridge_lock(self.lock_path, timeout=timeout or 5):
                if self.mode == BridgeMode.NAMED_PIPE:
                    message = self._read_from_pipe()
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    message = self._read_from_socket()
                else:
                    message = None
                
                if message:
                    # Verify authentication for received messages (PS-02)
                    if not self._verify_auth_token(message):
                        return None
                    
                    self._audit_log("MESSAGE_RECEIVED", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "XXmodeXX": self.mode.value
                    })
                    return message
                
                return None
        
        except TimeoutError as e:
            logger.warning(f"Bridge read timeout: {e}")
            self._audit_log("READ_TIMEOUT", {"error": str(e)})
            return None
        except Exception as e:
            logger.error(f"Bridge read error: {e}")
            self._audit_log("READ_ERROR", {"error": str(e)})
            return None
    
    def xǁBridgeManagerǁread_message__mutmut_25(self, timeout: Optional[int] = None) -> Optional[ContextMessage]:
        """
        Read a message from the bridge with locking and audit logging (PS-02).
        
        Args:
            timeout: Maximum seconds to wait for message
            
        Returns:
            ContextMessage if read successful, None otherwise
        """
        try:
            with bridge_lock(self.lock_path, timeout=timeout or 5):
                if self.mode == BridgeMode.NAMED_PIPE:
                    message = self._read_from_pipe()
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    message = self._read_from_socket()
                else:
                    message = None
                
                if message:
                    # Verify authentication for received messages (PS-02)
                    if not self._verify_auth_token(message):
                        return None
                    
                    self._audit_log("MESSAGE_RECEIVED", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "MODE": self.mode.value
                    })
                    return message
                
                return None
        
        except TimeoutError as e:
            logger.warning(f"Bridge read timeout: {e}")
            self._audit_log("READ_TIMEOUT", {"error": str(e)})
            return None
        except Exception as e:
            logger.error(f"Bridge read error: {e}")
            self._audit_log("READ_ERROR", {"error": str(e)})
            return None
    
    def xǁBridgeManagerǁread_message__mutmut_26(self, timeout: Optional[int] = None) -> Optional[ContextMessage]:
        """
        Read a message from the bridge with locking and audit logging (PS-02).
        
        Args:
            timeout: Maximum seconds to wait for message
            
        Returns:
            ContextMessage if read successful, None otherwise
        """
        try:
            with bridge_lock(self.lock_path, timeout=timeout or 5):
                if self.mode == BridgeMode.NAMED_PIPE:
                    message = self._read_from_pipe()
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    message = self._read_from_socket()
                else:
                    message = None
                
                if message:
                    # Verify authentication for received messages (PS-02)
                    if not self._verify_auth_token(message):
                        return None
                    
                    self._audit_log("MESSAGE_RECEIVED", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                    return message
                
                return None
        
        except TimeoutError as e:
            logger.warning(None)
            self._audit_log("READ_TIMEOUT", {"error": str(e)})
            return None
        except Exception as e:
            logger.error(f"Bridge read error: {e}")
            self._audit_log("READ_ERROR", {"error": str(e)})
            return None
    
    def xǁBridgeManagerǁread_message__mutmut_27(self, timeout: Optional[int] = None) -> Optional[ContextMessage]:
        """
        Read a message from the bridge with locking and audit logging (PS-02).
        
        Args:
            timeout: Maximum seconds to wait for message
            
        Returns:
            ContextMessage if read successful, None otherwise
        """
        try:
            with bridge_lock(self.lock_path, timeout=timeout or 5):
                if self.mode == BridgeMode.NAMED_PIPE:
                    message = self._read_from_pipe()
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    message = self._read_from_socket()
                else:
                    message = None
                
                if message:
                    # Verify authentication for received messages (PS-02)
                    if not self._verify_auth_token(message):
                        return None
                    
                    self._audit_log("MESSAGE_RECEIVED", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                    return message
                
                return None
        
        except TimeoutError as e:
            logger.warning(f"Bridge read timeout: {e}")
            self._audit_log(None, {"error": str(e)})
            return None
        except Exception as e:
            logger.error(f"Bridge read error: {e}")
            self._audit_log("READ_ERROR", {"error": str(e)})
            return None
    
    def xǁBridgeManagerǁread_message__mutmut_28(self, timeout: Optional[int] = None) -> Optional[ContextMessage]:
        """
        Read a message from the bridge with locking and audit logging (PS-02).
        
        Args:
            timeout: Maximum seconds to wait for message
            
        Returns:
            ContextMessage if read successful, None otherwise
        """
        try:
            with bridge_lock(self.lock_path, timeout=timeout or 5):
                if self.mode == BridgeMode.NAMED_PIPE:
                    message = self._read_from_pipe()
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    message = self._read_from_socket()
                else:
                    message = None
                
                if message:
                    # Verify authentication for received messages (PS-02)
                    if not self._verify_auth_token(message):
                        return None
                    
                    self._audit_log("MESSAGE_RECEIVED", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                    return message
                
                return None
        
        except TimeoutError as e:
            logger.warning(f"Bridge read timeout: {e}")
            self._audit_log("READ_TIMEOUT", None)
            return None
        except Exception as e:
            logger.error(f"Bridge read error: {e}")
            self._audit_log("READ_ERROR", {"error": str(e)})
            return None
    
    def xǁBridgeManagerǁread_message__mutmut_29(self, timeout: Optional[int] = None) -> Optional[ContextMessage]:
        """
        Read a message from the bridge with locking and audit logging (PS-02).
        
        Args:
            timeout: Maximum seconds to wait for message
            
        Returns:
            ContextMessage if read successful, None otherwise
        """
        try:
            with bridge_lock(self.lock_path, timeout=timeout or 5):
                if self.mode == BridgeMode.NAMED_PIPE:
                    message = self._read_from_pipe()
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    message = self._read_from_socket()
                else:
                    message = None
                
                if message:
                    # Verify authentication for received messages (PS-02)
                    if not self._verify_auth_token(message):
                        return None
                    
                    self._audit_log("MESSAGE_RECEIVED", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                    return message
                
                return None
        
        except TimeoutError as e:
            logger.warning(f"Bridge read timeout: {e}")
            self._audit_log({"error": str(e)})
            return None
        except Exception as e:
            logger.error(f"Bridge read error: {e}")
            self._audit_log("READ_ERROR", {"error": str(e)})
            return None
    
    def xǁBridgeManagerǁread_message__mutmut_30(self, timeout: Optional[int] = None) -> Optional[ContextMessage]:
        """
        Read a message from the bridge with locking and audit logging (PS-02).
        
        Args:
            timeout: Maximum seconds to wait for message
            
        Returns:
            ContextMessage if read successful, None otherwise
        """
        try:
            with bridge_lock(self.lock_path, timeout=timeout or 5):
                if self.mode == BridgeMode.NAMED_PIPE:
                    message = self._read_from_pipe()
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    message = self._read_from_socket()
                else:
                    message = None
                
                if message:
                    # Verify authentication for received messages (PS-02)
                    if not self._verify_auth_token(message):
                        return None
                    
                    self._audit_log("MESSAGE_RECEIVED", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                    return message
                
                return None
        
        except TimeoutError as e:
            logger.warning(f"Bridge read timeout: {e}")
            self._audit_log("READ_TIMEOUT", )
            return None
        except Exception as e:
            logger.error(f"Bridge read error: {e}")
            self._audit_log("READ_ERROR", {"error": str(e)})
            return None
    
    def xǁBridgeManagerǁread_message__mutmut_31(self, timeout: Optional[int] = None) -> Optional[ContextMessage]:
        """
        Read a message from the bridge with locking and audit logging (PS-02).
        
        Args:
            timeout: Maximum seconds to wait for message
            
        Returns:
            ContextMessage if read successful, None otherwise
        """
        try:
            with bridge_lock(self.lock_path, timeout=timeout or 5):
                if self.mode == BridgeMode.NAMED_PIPE:
                    message = self._read_from_pipe()
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    message = self._read_from_socket()
                else:
                    message = None
                
                if message:
                    # Verify authentication for received messages (PS-02)
                    if not self._verify_auth_token(message):
                        return None
                    
                    self._audit_log("MESSAGE_RECEIVED", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                    return message
                
                return None
        
        except TimeoutError as e:
            logger.warning(f"Bridge read timeout: {e}")
            self._audit_log("XXREAD_TIMEOUTXX", {"error": str(e)})
            return None
        except Exception as e:
            logger.error(f"Bridge read error: {e}")
            self._audit_log("READ_ERROR", {"error": str(e)})
            return None
    
    def xǁBridgeManagerǁread_message__mutmut_32(self, timeout: Optional[int] = None) -> Optional[ContextMessage]:
        """
        Read a message from the bridge with locking and audit logging (PS-02).
        
        Args:
            timeout: Maximum seconds to wait for message
            
        Returns:
            ContextMessage if read successful, None otherwise
        """
        try:
            with bridge_lock(self.lock_path, timeout=timeout or 5):
                if self.mode == BridgeMode.NAMED_PIPE:
                    message = self._read_from_pipe()
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    message = self._read_from_socket()
                else:
                    message = None
                
                if message:
                    # Verify authentication for received messages (PS-02)
                    if not self._verify_auth_token(message):
                        return None
                    
                    self._audit_log("MESSAGE_RECEIVED", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                    return message
                
                return None
        
        except TimeoutError as e:
            logger.warning(f"Bridge read timeout: {e}")
            self._audit_log("read_timeout", {"error": str(e)})
            return None
        except Exception as e:
            logger.error(f"Bridge read error: {e}")
            self._audit_log("READ_ERROR", {"error": str(e)})
            return None
    
    def xǁBridgeManagerǁread_message__mutmut_33(self, timeout: Optional[int] = None) -> Optional[ContextMessage]:
        """
        Read a message from the bridge with locking and audit logging (PS-02).
        
        Args:
            timeout: Maximum seconds to wait for message
            
        Returns:
            ContextMessage if read successful, None otherwise
        """
        try:
            with bridge_lock(self.lock_path, timeout=timeout or 5):
                if self.mode == BridgeMode.NAMED_PIPE:
                    message = self._read_from_pipe()
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    message = self._read_from_socket()
                else:
                    message = None
                
                if message:
                    # Verify authentication for received messages (PS-02)
                    if not self._verify_auth_token(message):
                        return None
                    
                    self._audit_log("MESSAGE_RECEIVED", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                    return message
                
                return None
        
        except TimeoutError as e:
            logger.warning(f"Bridge read timeout: {e}")
            self._audit_log("READ_TIMEOUT", {"XXerrorXX": str(e)})
            return None
        except Exception as e:
            logger.error(f"Bridge read error: {e}")
            self._audit_log("READ_ERROR", {"error": str(e)})
            return None
    
    def xǁBridgeManagerǁread_message__mutmut_34(self, timeout: Optional[int] = None) -> Optional[ContextMessage]:
        """
        Read a message from the bridge with locking and audit logging (PS-02).
        
        Args:
            timeout: Maximum seconds to wait for message
            
        Returns:
            ContextMessage if read successful, None otherwise
        """
        try:
            with bridge_lock(self.lock_path, timeout=timeout or 5):
                if self.mode == BridgeMode.NAMED_PIPE:
                    message = self._read_from_pipe()
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    message = self._read_from_socket()
                else:
                    message = None
                
                if message:
                    # Verify authentication for received messages (PS-02)
                    if not self._verify_auth_token(message):
                        return None
                    
                    self._audit_log("MESSAGE_RECEIVED", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                    return message
                
                return None
        
        except TimeoutError as e:
            logger.warning(f"Bridge read timeout: {e}")
            self._audit_log("READ_TIMEOUT", {"ERROR": str(e)})
            return None
        except Exception as e:
            logger.error(f"Bridge read error: {e}")
            self._audit_log("READ_ERROR", {"error": str(e)})
            return None
    
    def xǁBridgeManagerǁread_message__mutmut_35(self, timeout: Optional[int] = None) -> Optional[ContextMessage]:
        """
        Read a message from the bridge with locking and audit logging (PS-02).
        
        Args:
            timeout: Maximum seconds to wait for message
            
        Returns:
            ContextMessage if read successful, None otherwise
        """
        try:
            with bridge_lock(self.lock_path, timeout=timeout or 5):
                if self.mode == BridgeMode.NAMED_PIPE:
                    message = self._read_from_pipe()
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    message = self._read_from_socket()
                else:
                    message = None
                
                if message:
                    # Verify authentication for received messages (PS-02)
                    if not self._verify_auth_token(message):
                        return None
                    
                    self._audit_log("MESSAGE_RECEIVED", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                    return message
                
                return None
        
        except TimeoutError as e:
            logger.warning(f"Bridge read timeout: {e}")
            self._audit_log("READ_TIMEOUT", {"error": str(None)})
            return None
        except Exception as e:
            logger.error(f"Bridge read error: {e}")
            self._audit_log("READ_ERROR", {"error": str(e)})
            return None
    
    def xǁBridgeManagerǁread_message__mutmut_36(self, timeout: Optional[int] = None) -> Optional[ContextMessage]:
        """
        Read a message from the bridge with locking and audit logging (PS-02).
        
        Args:
            timeout: Maximum seconds to wait for message
            
        Returns:
            ContextMessage if read successful, None otherwise
        """
        try:
            with bridge_lock(self.lock_path, timeout=timeout or 5):
                if self.mode == BridgeMode.NAMED_PIPE:
                    message = self._read_from_pipe()
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    message = self._read_from_socket()
                else:
                    message = None
                
                if message:
                    # Verify authentication for received messages (PS-02)
                    if not self._verify_auth_token(message):
                        return None
                    
                    self._audit_log("MESSAGE_RECEIVED", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                    return message
                
                return None
        
        except TimeoutError as e:
            logger.warning(f"Bridge read timeout: {e}")
            self._audit_log("READ_TIMEOUT", {"error": str(e)})
            return None
        except Exception as e:
            logger.error(None)
            self._audit_log("READ_ERROR", {"error": str(e)})
            return None
    
    def xǁBridgeManagerǁread_message__mutmut_37(self, timeout: Optional[int] = None) -> Optional[ContextMessage]:
        """
        Read a message from the bridge with locking and audit logging (PS-02).
        
        Args:
            timeout: Maximum seconds to wait for message
            
        Returns:
            ContextMessage if read successful, None otherwise
        """
        try:
            with bridge_lock(self.lock_path, timeout=timeout or 5):
                if self.mode == BridgeMode.NAMED_PIPE:
                    message = self._read_from_pipe()
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    message = self._read_from_socket()
                else:
                    message = None
                
                if message:
                    # Verify authentication for received messages (PS-02)
                    if not self._verify_auth_token(message):
                        return None
                    
                    self._audit_log("MESSAGE_RECEIVED", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                    return message
                
                return None
        
        except TimeoutError as e:
            logger.warning(f"Bridge read timeout: {e}")
            self._audit_log("READ_TIMEOUT", {"error": str(e)})
            return None
        except Exception as e:
            logger.error(f"Bridge read error: {e}")
            self._audit_log(None, {"error": str(e)})
            return None
    
    def xǁBridgeManagerǁread_message__mutmut_38(self, timeout: Optional[int] = None) -> Optional[ContextMessage]:
        """
        Read a message from the bridge with locking and audit logging (PS-02).
        
        Args:
            timeout: Maximum seconds to wait for message
            
        Returns:
            ContextMessage if read successful, None otherwise
        """
        try:
            with bridge_lock(self.lock_path, timeout=timeout or 5):
                if self.mode == BridgeMode.NAMED_PIPE:
                    message = self._read_from_pipe()
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    message = self._read_from_socket()
                else:
                    message = None
                
                if message:
                    # Verify authentication for received messages (PS-02)
                    if not self._verify_auth_token(message):
                        return None
                    
                    self._audit_log("MESSAGE_RECEIVED", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                    return message
                
                return None
        
        except TimeoutError as e:
            logger.warning(f"Bridge read timeout: {e}")
            self._audit_log("READ_TIMEOUT", {"error": str(e)})
            return None
        except Exception as e:
            logger.error(f"Bridge read error: {e}")
            self._audit_log("READ_ERROR", None)
            return None
    
    def xǁBridgeManagerǁread_message__mutmut_39(self, timeout: Optional[int] = None) -> Optional[ContextMessage]:
        """
        Read a message from the bridge with locking and audit logging (PS-02).
        
        Args:
            timeout: Maximum seconds to wait for message
            
        Returns:
            ContextMessage if read successful, None otherwise
        """
        try:
            with bridge_lock(self.lock_path, timeout=timeout or 5):
                if self.mode == BridgeMode.NAMED_PIPE:
                    message = self._read_from_pipe()
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    message = self._read_from_socket()
                else:
                    message = None
                
                if message:
                    # Verify authentication for received messages (PS-02)
                    if not self._verify_auth_token(message):
                        return None
                    
                    self._audit_log("MESSAGE_RECEIVED", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                    return message
                
                return None
        
        except TimeoutError as e:
            logger.warning(f"Bridge read timeout: {e}")
            self._audit_log("READ_TIMEOUT", {"error": str(e)})
            return None
        except Exception as e:
            logger.error(f"Bridge read error: {e}")
            self._audit_log({"error": str(e)})
            return None
    
    def xǁBridgeManagerǁread_message__mutmut_40(self, timeout: Optional[int] = None) -> Optional[ContextMessage]:
        """
        Read a message from the bridge with locking and audit logging (PS-02).
        
        Args:
            timeout: Maximum seconds to wait for message
            
        Returns:
            ContextMessage if read successful, None otherwise
        """
        try:
            with bridge_lock(self.lock_path, timeout=timeout or 5):
                if self.mode == BridgeMode.NAMED_PIPE:
                    message = self._read_from_pipe()
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    message = self._read_from_socket()
                else:
                    message = None
                
                if message:
                    # Verify authentication for received messages (PS-02)
                    if not self._verify_auth_token(message):
                        return None
                    
                    self._audit_log("MESSAGE_RECEIVED", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                    return message
                
                return None
        
        except TimeoutError as e:
            logger.warning(f"Bridge read timeout: {e}")
            self._audit_log("READ_TIMEOUT", {"error": str(e)})
            return None
        except Exception as e:
            logger.error(f"Bridge read error: {e}")
            self._audit_log("READ_ERROR", )
            return None
    
    def xǁBridgeManagerǁread_message__mutmut_41(self, timeout: Optional[int] = None) -> Optional[ContextMessage]:
        """
        Read a message from the bridge with locking and audit logging (PS-02).
        
        Args:
            timeout: Maximum seconds to wait for message
            
        Returns:
            ContextMessage if read successful, None otherwise
        """
        try:
            with bridge_lock(self.lock_path, timeout=timeout or 5):
                if self.mode == BridgeMode.NAMED_PIPE:
                    message = self._read_from_pipe()
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    message = self._read_from_socket()
                else:
                    message = None
                
                if message:
                    # Verify authentication for received messages (PS-02)
                    if not self._verify_auth_token(message):
                        return None
                    
                    self._audit_log("MESSAGE_RECEIVED", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                    return message
                
                return None
        
        except TimeoutError as e:
            logger.warning(f"Bridge read timeout: {e}")
            self._audit_log("READ_TIMEOUT", {"error": str(e)})
            return None
        except Exception as e:
            logger.error(f"Bridge read error: {e}")
            self._audit_log("XXREAD_ERRORXX", {"error": str(e)})
            return None
    
    def xǁBridgeManagerǁread_message__mutmut_42(self, timeout: Optional[int] = None) -> Optional[ContextMessage]:
        """
        Read a message from the bridge with locking and audit logging (PS-02).
        
        Args:
            timeout: Maximum seconds to wait for message
            
        Returns:
            ContextMessage if read successful, None otherwise
        """
        try:
            with bridge_lock(self.lock_path, timeout=timeout or 5):
                if self.mode == BridgeMode.NAMED_PIPE:
                    message = self._read_from_pipe()
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    message = self._read_from_socket()
                else:
                    message = None
                
                if message:
                    # Verify authentication for received messages (PS-02)
                    if not self._verify_auth_token(message):
                        return None
                    
                    self._audit_log("MESSAGE_RECEIVED", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                    return message
                
                return None
        
        except TimeoutError as e:
            logger.warning(f"Bridge read timeout: {e}")
            self._audit_log("READ_TIMEOUT", {"error": str(e)})
            return None
        except Exception as e:
            logger.error(f"Bridge read error: {e}")
            self._audit_log("read_error", {"error": str(e)})
            return None
    
    def xǁBridgeManagerǁread_message__mutmut_43(self, timeout: Optional[int] = None) -> Optional[ContextMessage]:
        """
        Read a message from the bridge with locking and audit logging (PS-02).
        
        Args:
            timeout: Maximum seconds to wait for message
            
        Returns:
            ContextMessage if read successful, None otherwise
        """
        try:
            with bridge_lock(self.lock_path, timeout=timeout or 5):
                if self.mode == BridgeMode.NAMED_PIPE:
                    message = self._read_from_pipe()
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    message = self._read_from_socket()
                else:
                    message = None
                
                if message:
                    # Verify authentication for received messages (PS-02)
                    if not self._verify_auth_token(message):
                        return None
                    
                    self._audit_log("MESSAGE_RECEIVED", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                    return message
                
                return None
        
        except TimeoutError as e:
            logger.warning(f"Bridge read timeout: {e}")
            self._audit_log("READ_TIMEOUT", {"error": str(e)})
            return None
        except Exception as e:
            logger.error(f"Bridge read error: {e}")
            self._audit_log("READ_ERROR", {"XXerrorXX": str(e)})
            return None
    
    def xǁBridgeManagerǁread_message__mutmut_44(self, timeout: Optional[int] = None) -> Optional[ContextMessage]:
        """
        Read a message from the bridge with locking and audit logging (PS-02).
        
        Args:
            timeout: Maximum seconds to wait for message
            
        Returns:
            ContextMessage if read successful, None otherwise
        """
        try:
            with bridge_lock(self.lock_path, timeout=timeout or 5):
                if self.mode == BridgeMode.NAMED_PIPE:
                    message = self._read_from_pipe()
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    message = self._read_from_socket()
                else:
                    message = None
                
                if message:
                    # Verify authentication for received messages (PS-02)
                    if not self._verify_auth_token(message):
                        return None
                    
                    self._audit_log("MESSAGE_RECEIVED", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                    return message
                
                return None
        
        except TimeoutError as e:
            logger.warning(f"Bridge read timeout: {e}")
            self._audit_log("READ_TIMEOUT", {"error": str(e)})
            return None
        except Exception as e:
            logger.error(f"Bridge read error: {e}")
            self._audit_log("READ_ERROR", {"ERROR": str(e)})
            return None
    
    def xǁBridgeManagerǁread_message__mutmut_45(self, timeout: Optional[int] = None) -> Optional[ContextMessage]:
        """
        Read a message from the bridge with locking and audit logging (PS-02).
        
        Args:
            timeout: Maximum seconds to wait for message
            
        Returns:
            ContextMessage if read successful, None otherwise
        """
        try:
            with bridge_lock(self.lock_path, timeout=timeout or 5):
                if self.mode == BridgeMode.NAMED_PIPE:
                    message = self._read_from_pipe()
                elif self.mode == BridgeMode.UNIX_SOCKET:
                    message = self._read_from_socket()
                else:
                    message = None
                
                if message:
                    # Verify authentication for received messages (PS-02)
                    if not self._verify_auth_token(message):
                        return None
                    
                    self._audit_log("MESSAGE_RECEIVED", {
                        "source": message.source,
                        "message_type": message.message_type,
                        "mode": self.mode.value
                    })
                    return message
                
                return None
        
        except TimeoutError as e:
            logger.warning(f"Bridge read timeout: {e}")
            self._audit_log("READ_TIMEOUT", {"error": str(e)})
            return None
        except Exception as e:
            logger.error(f"Bridge read error: {e}")
            self._audit_log("READ_ERROR", {"error": str(None)})
            return None
    
    xǁBridgeManagerǁread_message__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁBridgeManagerǁread_message__mutmut_1': xǁBridgeManagerǁread_message__mutmut_1, 
        'xǁBridgeManagerǁread_message__mutmut_2': xǁBridgeManagerǁread_message__mutmut_2, 
        'xǁBridgeManagerǁread_message__mutmut_3': xǁBridgeManagerǁread_message__mutmut_3, 
        'xǁBridgeManagerǁread_message__mutmut_4': xǁBridgeManagerǁread_message__mutmut_4, 
        'xǁBridgeManagerǁread_message__mutmut_5': xǁBridgeManagerǁread_message__mutmut_5, 
        'xǁBridgeManagerǁread_message__mutmut_6': xǁBridgeManagerǁread_message__mutmut_6, 
        'xǁBridgeManagerǁread_message__mutmut_7': xǁBridgeManagerǁread_message__mutmut_7, 
        'xǁBridgeManagerǁread_message__mutmut_8': xǁBridgeManagerǁread_message__mutmut_8, 
        'xǁBridgeManagerǁread_message__mutmut_9': xǁBridgeManagerǁread_message__mutmut_9, 
        'xǁBridgeManagerǁread_message__mutmut_10': xǁBridgeManagerǁread_message__mutmut_10, 
        'xǁBridgeManagerǁread_message__mutmut_11': xǁBridgeManagerǁread_message__mutmut_11, 
        'xǁBridgeManagerǁread_message__mutmut_12': xǁBridgeManagerǁread_message__mutmut_12, 
        'xǁBridgeManagerǁread_message__mutmut_13': xǁBridgeManagerǁread_message__mutmut_13, 
        'xǁBridgeManagerǁread_message__mutmut_14': xǁBridgeManagerǁread_message__mutmut_14, 
        'xǁBridgeManagerǁread_message__mutmut_15': xǁBridgeManagerǁread_message__mutmut_15, 
        'xǁBridgeManagerǁread_message__mutmut_16': xǁBridgeManagerǁread_message__mutmut_16, 
        'xǁBridgeManagerǁread_message__mutmut_17': xǁBridgeManagerǁread_message__mutmut_17, 
        'xǁBridgeManagerǁread_message__mutmut_18': xǁBridgeManagerǁread_message__mutmut_18, 
        'xǁBridgeManagerǁread_message__mutmut_19': xǁBridgeManagerǁread_message__mutmut_19, 
        'xǁBridgeManagerǁread_message__mutmut_20': xǁBridgeManagerǁread_message__mutmut_20, 
        'xǁBridgeManagerǁread_message__mutmut_21': xǁBridgeManagerǁread_message__mutmut_21, 
        'xǁBridgeManagerǁread_message__mutmut_22': xǁBridgeManagerǁread_message__mutmut_22, 
        'xǁBridgeManagerǁread_message__mutmut_23': xǁBridgeManagerǁread_message__mutmut_23, 
        'xǁBridgeManagerǁread_message__mutmut_24': xǁBridgeManagerǁread_message__mutmut_24, 
        'xǁBridgeManagerǁread_message__mutmut_25': xǁBridgeManagerǁread_message__mutmut_25, 
        'xǁBridgeManagerǁread_message__mutmut_26': xǁBridgeManagerǁread_message__mutmut_26, 
        'xǁBridgeManagerǁread_message__mutmut_27': xǁBridgeManagerǁread_message__mutmut_27, 
        'xǁBridgeManagerǁread_message__mutmut_28': xǁBridgeManagerǁread_message__mutmut_28, 
        'xǁBridgeManagerǁread_message__mutmut_29': xǁBridgeManagerǁread_message__mutmut_29, 
        'xǁBridgeManagerǁread_message__mutmut_30': xǁBridgeManagerǁread_message__mutmut_30, 
        'xǁBridgeManagerǁread_message__mutmut_31': xǁBridgeManagerǁread_message__mutmut_31, 
        'xǁBridgeManagerǁread_message__mutmut_32': xǁBridgeManagerǁread_message__mutmut_32, 
        'xǁBridgeManagerǁread_message__mutmut_33': xǁBridgeManagerǁread_message__mutmut_33, 
        'xǁBridgeManagerǁread_message__mutmut_34': xǁBridgeManagerǁread_message__mutmut_34, 
        'xǁBridgeManagerǁread_message__mutmut_35': xǁBridgeManagerǁread_message__mutmut_35, 
        'xǁBridgeManagerǁread_message__mutmut_36': xǁBridgeManagerǁread_message__mutmut_36, 
        'xǁBridgeManagerǁread_message__mutmut_37': xǁBridgeManagerǁread_message__mutmut_37, 
        'xǁBridgeManagerǁread_message__mutmut_38': xǁBridgeManagerǁread_message__mutmut_38, 
        'xǁBridgeManagerǁread_message__mutmut_39': xǁBridgeManagerǁread_message__mutmut_39, 
        'xǁBridgeManagerǁread_message__mutmut_40': xǁBridgeManagerǁread_message__mutmut_40, 
        'xǁBridgeManagerǁread_message__mutmut_41': xǁBridgeManagerǁread_message__mutmut_41, 
        'xǁBridgeManagerǁread_message__mutmut_42': xǁBridgeManagerǁread_message__mutmut_42, 
        'xǁBridgeManagerǁread_message__mutmut_43': xǁBridgeManagerǁread_message__mutmut_43, 
        'xǁBridgeManagerǁread_message__mutmut_44': xǁBridgeManagerǁread_message__mutmut_44, 
        'xǁBridgeManagerǁread_message__mutmut_45': xǁBridgeManagerǁread_message__mutmut_45
    }
    
    def read_message(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁBridgeManagerǁread_message__mutmut_orig"), object.__getattribute__(self, "xǁBridgeManagerǁread_message__mutmut_mutants"), args, kwargs, self)
        return result 
    
    read_message.__signature__ = _mutmut_signature(xǁBridgeManagerǁread_message__mutmut_orig)
    xǁBridgeManagerǁread_message__mutmut_orig.__name__ = 'xǁBridgeManagerǁread_message'
    
    def xǁBridgeManagerǁ_read_from_pipe__mutmut_orig(self) -> Optional[ContextMessage]:
        """Read message from named pipe with auto-detection of v1/v2 protocol."""
        try:
            # Always read in binary mode to detect protocol version
            with open(self.pipe_path, 'rb') as pipe:
                data = pipe.read()
                if not data:
                    return None
                
                # Auto-detect v2 protocol by magic bytes (regardless of settings)
                # Note: Using imported MAGIC_BYTES constant to maintain single source of truth
                # Dynamic length check makes this resilient to future protocol changes
                magic_len = len(MAGIC_BYTES)
                if HAS_PROTOCOL_V2 and len(data) >= magic_len and data[:magic_len] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                    logger.debug(f"Read v2 message (compressed={bool(header.flags & 1)})")
                else:
                    # Legacy v1 format
                    json_str = data.decode('utf-8').strip()
                
                if json_str:
                    message = ContextMessage.from_json(json_str)
                    logger.debug(f"Message read from pipe: {message.message_type}")
                    return message
            
            return None
            
        except Exception as e:
            logger.error(f"Pipe read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_pipe__mutmut_1(self) -> Optional[ContextMessage]:
        """Read message from named pipe with auto-detection of v1/v2 protocol."""
        try:
            # Always read in binary mode to detect protocol version
            with open(None, 'rb') as pipe:
                data = pipe.read()
                if not data:
                    return None
                
                # Auto-detect v2 protocol by magic bytes (regardless of settings)
                # Note: Using imported MAGIC_BYTES constant to maintain single source of truth
                # Dynamic length check makes this resilient to future protocol changes
                magic_len = len(MAGIC_BYTES)
                if HAS_PROTOCOL_V2 and len(data) >= magic_len and data[:magic_len] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                    logger.debug(f"Read v2 message (compressed={bool(header.flags & 1)})")
                else:
                    # Legacy v1 format
                    json_str = data.decode('utf-8').strip()
                
                if json_str:
                    message = ContextMessage.from_json(json_str)
                    logger.debug(f"Message read from pipe: {message.message_type}")
                    return message
            
            return None
            
        except Exception as e:
            logger.error(f"Pipe read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_pipe__mutmut_2(self) -> Optional[ContextMessage]:
        """Read message from named pipe with auto-detection of v1/v2 protocol."""
        try:
            # Always read in binary mode to detect protocol version
            with open(self.pipe_path, None) as pipe:
                data = pipe.read()
                if not data:
                    return None
                
                # Auto-detect v2 protocol by magic bytes (regardless of settings)
                # Note: Using imported MAGIC_BYTES constant to maintain single source of truth
                # Dynamic length check makes this resilient to future protocol changes
                magic_len = len(MAGIC_BYTES)
                if HAS_PROTOCOL_V2 and len(data) >= magic_len and data[:magic_len] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                    logger.debug(f"Read v2 message (compressed={bool(header.flags & 1)})")
                else:
                    # Legacy v1 format
                    json_str = data.decode('utf-8').strip()
                
                if json_str:
                    message = ContextMessage.from_json(json_str)
                    logger.debug(f"Message read from pipe: {message.message_type}")
                    return message
            
            return None
            
        except Exception as e:
            logger.error(f"Pipe read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_pipe__mutmut_3(self) -> Optional[ContextMessage]:
        """Read message from named pipe with auto-detection of v1/v2 protocol."""
        try:
            # Always read in binary mode to detect protocol version
            with open('rb') as pipe:
                data = pipe.read()
                if not data:
                    return None
                
                # Auto-detect v2 protocol by magic bytes (regardless of settings)
                # Note: Using imported MAGIC_BYTES constant to maintain single source of truth
                # Dynamic length check makes this resilient to future protocol changes
                magic_len = len(MAGIC_BYTES)
                if HAS_PROTOCOL_V2 and len(data) >= magic_len and data[:magic_len] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                    logger.debug(f"Read v2 message (compressed={bool(header.flags & 1)})")
                else:
                    # Legacy v1 format
                    json_str = data.decode('utf-8').strip()
                
                if json_str:
                    message = ContextMessage.from_json(json_str)
                    logger.debug(f"Message read from pipe: {message.message_type}")
                    return message
            
            return None
            
        except Exception as e:
            logger.error(f"Pipe read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_pipe__mutmut_4(self) -> Optional[ContextMessage]:
        """Read message from named pipe with auto-detection of v1/v2 protocol."""
        try:
            # Always read in binary mode to detect protocol version
            with open(self.pipe_path, ) as pipe:
                data = pipe.read()
                if not data:
                    return None
                
                # Auto-detect v2 protocol by magic bytes (regardless of settings)
                # Note: Using imported MAGIC_BYTES constant to maintain single source of truth
                # Dynamic length check makes this resilient to future protocol changes
                magic_len = len(MAGIC_BYTES)
                if HAS_PROTOCOL_V2 and len(data) >= magic_len and data[:magic_len] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                    logger.debug(f"Read v2 message (compressed={bool(header.flags & 1)})")
                else:
                    # Legacy v1 format
                    json_str = data.decode('utf-8').strip()
                
                if json_str:
                    message = ContextMessage.from_json(json_str)
                    logger.debug(f"Message read from pipe: {message.message_type}")
                    return message
            
            return None
            
        except Exception as e:
            logger.error(f"Pipe read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_pipe__mutmut_5(self) -> Optional[ContextMessage]:
        """Read message from named pipe with auto-detection of v1/v2 protocol."""
        try:
            # Always read in binary mode to detect protocol version
            with open(self.pipe_path, 'XXrbXX') as pipe:
                data = pipe.read()
                if not data:
                    return None
                
                # Auto-detect v2 protocol by magic bytes (regardless of settings)
                # Note: Using imported MAGIC_BYTES constant to maintain single source of truth
                # Dynamic length check makes this resilient to future protocol changes
                magic_len = len(MAGIC_BYTES)
                if HAS_PROTOCOL_V2 and len(data) >= magic_len and data[:magic_len] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                    logger.debug(f"Read v2 message (compressed={bool(header.flags & 1)})")
                else:
                    # Legacy v1 format
                    json_str = data.decode('utf-8').strip()
                
                if json_str:
                    message = ContextMessage.from_json(json_str)
                    logger.debug(f"Message read from pipe: {message.message_type}")
                    return message
            
            return None
            
        except Exception as e:
            logger.error(f"Pipe read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_pipe__mutmut_6(self) -> Optional[ContextMessage]:
        """Read message from named pipe with auto-detection of v1/v2 protocol."""
        try:
            # Always read in binary mode to detect protocol version
            with open(self.pipe_path, 'RB') as pipe:
                data = pipe.read()
                if not data:
                    return None
                
                # Auto-detect v2 protocol by magic bytes (regardless of settings)
                # Note: Using imported MAGIC_BYTES constant to maintain single source of truth
                # Dynamic length check makes this resilient to future protocol changes
                magic_len = len(MAGIC_BYTES)
                if HAS_PROTOCOL_V2 and len(data) >= magic_len and data[:magic_len] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                    logger.debug(f"Read v2 message (compressed={bool(header.flags & 1)})")
                else:
                    # Legacy v1 format
                    json_str = data.decode('utf-8').strip()
                
                if json_str:
                    message = ContextMessage.from_json(json_str)
                    logger.debug(f"Message read from pipe: {message.message_type}")
                    return message
            
            return None
            
        except Exception as e:
            logger.error(f"Pipe read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_pipe__mutmut_7(self) -> Optional[ContextMessage]:
        """Read message from named pipe with auto-detection of v1/v2 protocol."""
        try:
            # Always read in binary mode to detect protocol version
            with open(self.pipe_path, 'rb') as pipe:
                data = None
                if not data:
                    return None
                
                # Auto-detect v2 protocol by magic bytes (regardless of settings)
                # Note: Using imported MAGIC_BYTES constant to maintain single source of truth
                # Dynamic length check makes this resilient to future protocol changes
                magic_len = len(MAGIC_BYTES)
                if HAS_PROTOCOL_V2 and len(data) >= magic_len and data[:magic_len] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                    logger.debug(f"Read v2 message (compressed={bool(header.flags & 1)})")
                else:
                    # Legacy v1 format
                    json_str = data.decode('utf-8').strip()
                
                if json_str:
                    message = ContextMessage.from_json(json_str)
                    logger.debug(f"Message read from pipe: {message.message_type}")
                    return message
            
            return None
            
        except Exception as e:
            logger.error(f"Pipe read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_pipe__mutmut_8(self) -> Optional[ContextMessage]:
        """Read message from named pipe with auto-detection of v1/v2 protocol."""
        try:
            # Always read in binary mode to detect protocol version
            with open(self.pipe_path, 'rb') as pipe:
                data = pipe.read()
                if data:
                    return None
                
                # Auto-detect v2 protocol by magic bytes (regardless of settings)
                # Note: Using imported MAGIC_BYTES constant to maintain single source of truth
                # Dynamic length check makes this resilient to future protocol changes
                magic_len = len(MAGIC_BYTES)
                if HAS_PROTOCOL_V2 and len(data) >= magic_len and data[:magic_len] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                    logger.debug(f"Read v2 message (compressed={bool(header.flags & 1)})")
                else:
                    # Legacy v1 format
                    json_str = data.decode('utf-8').strip()
                
                if json_str:
                    message = ContextMessage.from_json(json_str)
                    logger.debug(f"Message read from pipe: {message.message_type}")
                    return message
            
            return None
            
        except Exception as e:
            logger.error(f"Pipe read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_pipe__mutmut_9(self) -> Optional[ContextMessage]:
        """Read message from named pipe with auto-detection of v1/v2 protocol."""
        try:
            # Always read in binary mode to detect protocol version
            with open(self.pipe_path, 'rb') as pipe:
                data = pipe.read()
                if not data:
                    return None
                
                # Auto-detect v2 protocol by magic bytes (regardless of settings)
                # Note: Using imported MAGIC_BYTES constant to maintain single source of truth
                # Dynamic length check makes this resilient to future protocol changes
                magic_len = None
                if HAS_PROTOCOL_V2 and len(data) >= magic_len and data[:magic_len] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                    logger.debug(f"Read v2 message (compressed={bool(header.flags & 1)})")
                else:
                    # Legacy v1 format
                    json_str = data.decode('utf-8').strip()
                
                if json_str:
                    message = ContextMessage.from_json(json_str)
                    logger.debug(f"Message read from pipe: {message.message_type}")
                    return message
            
            return None
            
        except Exception as e:
            logger.error(f"Pipe read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_pipe__mutmut_10(self) -> Optional[ContextMessage]:
        """Read message from named pipe with auto-detection of v1/v2 protocol."""
        try:
            # Always read in binary mode to detect protocol version
            with open(self.pipe_path, 'rb') as pipe:
                data = pipe.read()
                if not data:
                    return None
                
                # Auto-detect v2 protocol by magic bytes (regardless of settings)
                # Note: Using imported MAGIC_BYTES constant to maintain single source of truth
                # Dynamic length check makes this resilient to future protocol changes
                magic_len = len(MAGIC_BYTES)
                if HAS_PROTOCOL_V2 and len(data) >= magic_len or data[:magic_len] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                    logger.debug(f"Read v2 message (compressed={bool(header.flags & 1)})")
                else:
                    # Legacy v1 format
                    json_str = data.decode('utf-8').strip()
                
                if json_str:
                    message = ContextMessage.from_json(json_str)
                    logger.debug(f"Message read from pipe: {message.message_type}")
                    return message
            
            return None
            
        except Exception as e:
            logger.error(f"Pipe read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_pipe__mutmut_11(self) -> Optional[ContextMessage]:
        """Read message from named pipe with auto-detection of v1/v2 protocol."""
        try:
            # Always read in binary mode to detect protocol version
            with open(self.pipe_path, 'rb') as pipe:
                data = pipe.read()
                if not data:
                    return None
                
                # Auto-detect v2 protocol by magic bytes (regardless of settings)
                # Note: Using imported MAGIC_BYTES constant to maintain single source of truth
                # Dynamic length check makes this resilient to future protocol changes
                magic_len = len(MAGIC_BYTES)
                if HAS_PROTOCOL_V2 or len(data) >= magic_len and data[:magic_len] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                    logger.debug(f"Read v2 message (compressed={bool(header.flags & 1)})")
                else:
                    # Legacy v1 format
                    json_str = data.decode('utf-8').strip()
                
                if json_str:
                    message = ContextMessage.from_json(json_str)
                    logger.debug(f"Message read from pipe: {message.message_type}")
                    return message
            
            return None
            
        except Exception as e:
            logger.error(f"Pipe read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_pipe__mutmut_12(self) -> Optional[ContextMessage]:
        """Read message from named pipe with auto-detection of v1/v2 protocol."""
        try:
            # Always read in binary mode to detect protocol version
            with open(self.pipe_path, 'rb') as pipe:
                data = pipe.read()
                if not data:
                    return None
                
                # Auto-detect v2 protocol by magic bytes (regardless of settings)
                # Note: Using imported MAGIC_BYTES constant to maintain single source of truth
                # Dynamic length check makes this resilient to future protocol changes
                magic_len = len(MAGIC_BYTES)
                if HAS_PROTOCOL_V2 and len(data) > magic_len and data[:magic_len] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                    logger.debug(f"Read v2 message (compressed={bool(header.flags & 1)})")
                else:
                    # Legacy v1 format
                    json_str = data.decode('utf-8').strip()
                
                if json_str:
                    message = ContextMessage.from_json(json_str)
                    logger.debug(f"Message read from pipe: {message.message_type}")
                    return message
            
            return None
            
        except Exception as e:
            logger.error(f"Pipe read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_pipe__mutmut_13(self) -> Optional[ContextMessage]:
        """Read message from named pipe with auto-detection of v1/v2 protocol."""
        try:
            # Always read in binary mode to detect protocol version
            with open(self.pipe_path, 'rb') as pipe:
                data = pipe.read()
                if not data:
                    return None
                
                # Auto-detect v2 protocol by magic bytes (regardless of settings)
                # Note: Using imported MAGIC_BYTES constant to maintain single source of truth
                # Dynamic length check makes this resilient to future protocol changes
                magic_len = len(MAGIC_BYTES)
                if HAS_PROTOCOL_V2 and len(data) >= magic_len and data[:magic_len] != MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                    logger.debug(f"Read v2 message (compressed={bool(header.flags & 1)})")
                else:
                    # Legacy v1 format
                    json_str = data.decode('utf-8').strip()
                
                if json_str:
                    message = ContextMessage.from_json(json_str)
                    logger.debug(f"Message read from pipe: {message.message_type}")
                    return message
            
            return None
            
        except Exception as e:
            logger.error(f"Pipe read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_pipe__mutmut_14(self) -> Optional[ContextMessage]:
        """Read message from named pipe with auto-detection of v1/v2 protocol."""
        try:
            # Always read in binary mode to detect protocol version
            with open(self.pipe_path, 'rb') as pipe:
                data = pipe.read()
                if not data:
                    return None
                
                # Auto-detect v2 protocol by magic bytes (regardless of settings)
                # Note: Using imported MAGIC_BYTES constant to maintain single source of truth
                # Dynamic length check makes this resilient to future protocol changes
                magic_len = len(MAGIC_BYTES)
                if HAS_PROTOCOL_V2 and len(data) >= magic_len and data[:magic_len] == MAGIC_BYTES:
                    payload, header = None
                    json_str = payload.decode('utf-8')
                    logger.debug(f"Read v2 message (compressed={bool(header.flags & 1)})")
                else:
                    # Legacy v1 format
                    json_str = data.decode('utf-8').strip()
                
                if json_str:
                    message = ContextMessage.from_json(json_str)
                    logger.debug(f"Message read from pipe: {message.message_type}")
                    return message
            
            return None
            
        except Exception as e:
            logger.error(f"Pipe read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_pipe__mutmut_15(self) -> Optional[ContextMessage]:
        """Read message from named pipe with auto-detection of v1/v2 protocol."""
        try:
            # Always read in binary mode to detect protocol version
            with open(self.pipe_path, 'rb') as pipe:
                data = pipe.read()
                if not data:
                    return None
                
                # Auto-detect v2 protocol by magic bytes (regardless of settings)
                # Note: Using imported MAGIC_BYTES constant to maintain single source of truth
                # Dynamic length check makes this resilient to future protocol changes
                magic_len = len(MAGIC_BYTES)
                if HAS_PROTOCOL_V2 and len(data) >= magic_len and data[:magic_len] == MAGIC_BYTES:
                    payload, header = v2_decode(None)
                    json_str = payload.decode('utf-8')
                    logger.debug(f"Read v2 message (compressed={bool(header.flags & 1)})")
                else:
                    # Legacy v1 format
                    json_str = data.decode('utf-8').strip()
                
                if json_str:
                    message = ContextMessage.from_json(json_str)
                    logger.debug(f"Message read from pipe: {message.message_type}")
                    return message
            
            return None
            
        except Exception as e:
            logger.error(f"Pipe read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_pipe__mutmut_16(self) -> Optional[ContextMessage]:
        """Read message from named pipe with auto-detection of v1/v2 protocol."""
        try:
            # Always read in binary mode to detect protocol version
            with open(self.pipe_path, 'rb') as pipe:
                data = pipe.read()
                if not data:
                    return None
                
                # Auto-detect v2 protocol by magic bytes (regardless of settings)
                # Note: Using imported MAGIC_BYTES constant to maintain single source of truth
                # Dynamic length check makes this resilient to future protocol changes
                magic_len = len(MAGIC_BYTES)
                if HAS_PROTOCOL_V2 and len(data) >= magic_len and data[:magic_len] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = None
                    logger.debug(f"Read v2 message (compressed={bool(header.flags & 1)})")
                else:
                    # Legacy v1 format
                    json_str = data.decode('utf-8').strip()
                
                if json_str:
                    message = ContextMessage.from_json(json_str)
                    logger.debug(f"Message read from pipe: {message.message_type}")
                    return message
            
            return None
            
        except Exception as e:
            logger.error(f"Pipe read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_pipe__mutmut_17(self) -> Optional[ContextMessage]:
        """Read message from named pipe with auto-detection of v1/v2 protocol."""
        try:
            # Always read in binary mode to detect protocol version
            with open(self.pipe_path, 'rb') as pipe:
                data = pipe.read()
                if not data:
                    return None
                
                # Auto-detect v2 protocol by magic bytes (regardless of settings)
                # Note: Using imported MAGIC_BYTES constant to maintain single source of truth
                # Dynamic length check makes this resilient to future protocol changes
                magic_len = len(MAGIC_BYTES)
                if HAS_PROTOCOL_V2 and len(data) >= magic_len and data[:magic_len] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode(None)
                    logger.debug(f"Read v2 message (compressed={bool(header.flags & 1)})")
                else:
                    # Legacy v1 format
                    json_str = data.decode('utf-8').strip()
                
                if json_str:
                    message = ContextMessage.from_json(json_str)
                    logger.debug(f"Message read from pipe: {message.message_type}")
                    return message
            
            return None
            
        except Exception as e:
            logger.error(f"Pipe read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_pipe__mutmut_18(self) -> Optional[ContextMessage]:
        """Read message from named pipe with auto-detection of v1/v2 protocol."""
        try:
            # Always read in binary mode to detect protocol version
            with open(self.pipe_path, 'rb') as pipe:
                data = pipe.read()
                if not data:
                    return None
                
                # Auto-detect v2 protocol by magic bytes (regardless of settings)
                # Note: Using imported MAGIC_BYTES constant to maintain single source of truth
                # Dynamic length check makes this resilient to future protocol changes
                magic_len = len(MAGIC_BYTES)
                if HAS_PROTOCOL_V2 and len(data) >= magic_len and data[:magic_len] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('XXutf-8XX')
                    logger.debug(f"Read v2 message (compressed={bool(header.flags & 1)})")
                else:
                    # Legacy v1 format
                    json_str = data.decode('utf-8').strip()
                
                if json_str:
                    message = ContextMessage.from_json(json_str)
                    logger.debug(f"Message read from pipe: {message.message_type}")
                    return message
            
            return None
            
        except Exception as e:
            logger.error(f"Pipe read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_pipe__mutmut_19(self) -> Optional[ContextMessage]:
        """Read message from named pipe with auto-detection of v1/v2 protocol."""
        try:
            # Always read in binary mode to detect protocol version
            with open(self.pipe_path, 'rb') as pipe:
                data = pipe.read()
                if not data:
                    return None
                
                # Auto-detect v2 protocol by magic bytes (regardless of settings)
                # Note: Using imported MAGIC_BYTES constant to maintain single source of truth
                # Dynamic length check makes this resilient to future protocol changes
                magic_len = len(MAGIC_BYTES)
                if HAS_PROTOCOL_V2 and len(data) >= magic_len and data[:magic_len] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('UTF-8')
                    logger.debug(f"Read v2 message (compressed={bool(header.flags & 1)})")
                else:
                    # Legacy v1 format
                    json_str = data.decode('utf-8').strip()
                
                if json_str:
                    message = ContextMessage.from_json(json_str)
                    logger.debug(f"Message read from pipe: {message.message_type}")
                    return message
            
            return None
            
        except Exception as e:
            logger.error(f"Pipe read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_pipe__mutmut_20(self) -> Optional[ContextMessage]:
        """Read message from named pipe with auto-detection of v1/v2 protocol."""
        try:
            # Always read in binary mode to detect protocol version
            with open(self.pipe_path, 'rb') as pipe:
                data = pipe.read()
                if not data:
                    return None
                
                # Auto-detect v2 protocol by magic bytes (regardless of settings)
                # Note: Using imported MAGIC_BYTES constant to maintain single source of truth
                # Dynamic length check makes this resilient to future protocol changes
                magic_len = len(MAGIC_BYTES)
                if HAS_PROTOCOL_V2 and len(data) >= magic_len and data[:magic_len] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                    logger.debug(None)
                else:
                    # Legacy v1 format
                    json_str = data.decode('utf-8').strip()
                
                if json_str:
                    message = ContextMessage.from_json(json_str)
                    logger.debug(f"Message read from pipe: {message.message_type}")
                    return message
            
            return None
            
        except Exception as e:
            logger.error(f"Pipe read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_pipe__mutmut_21(self) -> Optional[ContextMessage]:
        """Read message from named pipe with auto-detection of v1/v2 protocol."""
        try:
            # Always read in binary mode to detect protocol version
            with open(self.pipe_path, 'rb') as pipe:
                data = pipe.read()
                if not data:
                    return None
                
                # Auto-detect v2 protocol by magic bytes (regardless of settings)
                # Note: Using imported MAGIC_BYTES constant to maintain single source of truth
                # Dynamic length check makes this resilient to future protocol changes
                magic_len = len(MAGIC_BYTES)
                if HAS_PROTOCOL_V2 and len(data) >= magic_len and data[:magic_len] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                    logger.debug(f"Read v2 message (compressed={bool(None)})")
                else:
                    # Legacy v1 format
                    json_str = data.decode('utf-8').strip()
                
                if json_str:
                    message = ContextMessage.from_json(json_str)
                    logger.debug(f"Message read from pipe: {message.message_type}")
                    return message
            
            return None
            
        except Exception as e:
            logger.error(f"Pipe read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_pipe__mutmut_22(self) -> Optional[ContextMessage]:
        """Read message from named pipe with auto-detection of v1/v2 protocol."""
        try:
            # Always read in binary mode to detect protocol version
            with open(self.pipe_path, 'rb') as pipe:
                data = pipe.read()
                if not data:
                    return None
                
                # Auto-detect v2 protocol by magic bytes (regardless of settings)
                # Note: Using imported MAGIC_BYTES constant to maintain single source of truth
                # Dynamic length check makes this resilient to future protocol changes
                magic_len = len(MAGIC_BYTES)
                if HAS_PROTOCOL_V2 and len(data) >= magic_len and data[:magic_len] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                    logger.debug(f"Read v2 message (compressed={bool(header.flags | 1)})")
                else:
                    # Legacy v1 format
                    json_str = data.decode('utf-8').strip()
                
                if json_str:
                    message = ContextMessage.from_json(json_str)
                    logger.debug(f"Message read from pipe: {message.message_type}")
                    return message
            
            return None
            
        except Exception as e:
            logger.error(f"Pipe read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_pipe__mutmut_23(self) -> Optional[ContextMessage]:
        """Read message from named pipe with auto-detection of v1/v2 protocol."""
        try:
            # Always read in binary mode to detect protocol version
            with open(self.pipe_path, 'rb') as pipe:
                data = pipe.read()
                if not data:
                    return None
                
                # Auto-detect v2 protocol by magic bytes (regardless of settings)
                # Note: Using imported MAGIC_BYTES constant to maintain single source of truth
                # Dynamic length check makes this resilient to future protocol changes
                magic_len = len(MAGIC_BYTES)
                if HAS_PROTOCOL_V2 and len(data) >= magic_len and data[:magic_len] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                    logger.debug(f"Read v2 message (compressed={bool(header.flags & 2)})")
                else:
                    # Legacy v1 format
                    json_str = data.decode('utf-8').strip()
                
                if json_str:
                    message = ContextMessage.from_json(json_str)
                    logger.debug(f"Message read from pipe: {message.message_type}")
                    return message
            
            return None
            
        except Exception as e:
            logger.error(f"Pipe read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_pipe__mutmut_24(self) -> Optional[ContextMessage]:
        """Read message from named pipe with auto-detection of v1/v2 protocol."""
        try:
            # Always read in binary mode to detect protocol version
            with open(self.pipe_path, 'rb') as pipe:
                data = pipe.read()
                if not data:
                    return None
                
                # Auto-detect v2 protocol by magic bytes (regardless of settings)
                # Note: Using imported MAGIC_BYTES constant to maintain single source of truth
                # Dynamic length check makes this resilient to future protocol changes
                magic_len = len(MAGIC_BYTES)
                if HAS_PROTOCOL_V2 and len(data) >= magic_len and data[:magic_len] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                    logger.debug(f"Read v2 message (compressed={bool(header.flags & 1)})")
                else:
                    # Legacy v1 format
                    json_str = None
                
                if json_str:
                    message = ContextMessage.from_json(json_str)
                    logger.debug(f"Message read from pipe: {message.message_type}")
                    return message
            
            return None
            
        except Exception as e:
            logger.error(f"Pipe read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_pipe__mutmut_25(self) -> Optional[ContextMessage]:
        """Read message from named pipe with auto-detection of v1/v2 protocol."""
        try:
            # Always read in binary mode to detect protocol version
            with open(self.pipe_path, 'rb') as pipe:
                data = pipe.read()
                if not data:
                    return None
                
                # Auto-detect v2 protocol by magic bytes (regardless of settings)
                # Note: Using imported MAGIC_BYTES constant to maintain single source of truth
                # Dynamic length check makes this resilient to future protocol changes
                magic_len = len(MAGIC_BYTES)
                if HAS_PROTOCOL_V2 and len(data) >= magic_len and data[:magic_len] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                    logger.debug(f"Read v2 message (compressed={bool(header.flags & 1)})")
                else:
                    # Legacy v1 format
                    json_str = data.decode(None).strip()
                
                if json_str:
                    message = ContextMessage.from_json(json_str)
                    logger.debug(f"Message read from pipe: {message.message_type}")
                    return message
            
            return None
            
        except Exception as e:
            logger.error(f"Pipe read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_pipe__mutmut_26(self) -> Optional[ContextMessage]:
        """Read message from named pipe with auto-detection of v1/v2 protocol."""
        try:
            # Always read in binary mode to detect protocol version
            with open(self.pipe_path, 'rb') as pipe:
                data = pipe.read()
                if not data:
                    return None
                
                # Auto-detect v2 protocol by magic bytes (regardless of settings)
                # Note: Using imported MAGIC_BYTES constant to maintain single source of truth
                # Dynamic length check makes this resilient to future protocol changes
                magic_len = len(MAGIC_BYTES)
                if HAS_PROTOCOL_V2 and len(data) >= magic_len and data[:magic_len] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                    logger.debug(f"Read v2 message (compressed={bool(header.flags & 1)})")
                else:
                    # Legacy v1 format
                    json_str = data.decode('XXutf-8XX').strip()
                
                if json_str:
                    message = ContextMessage.from_json(json_str)
                    logger.debug(f"Message read from pipe: {message.message_type}")
                    return message
            
            return None
            
        except Exception as e:
            logger.error(f"Pipe read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_pipe__mutmut_27(self) -> Optional[ContextMessage]:
        """Read message from named pipe with auto-detection of v1/v2 protocol."""
        try:
            # Always read in binary mode to detect protocol version
            with open(self.pipe_path, 'rb') as pipe:
                data = pipe.read()
                if not data:
                    return None
                
                # Auto-detect v2 protocol by magic bytes (regardless of settings)
                # Note: Using imported MAGIC_BYTES constant to maintain single source of truth
                # Dynamic length check makes this resilient to future protocol changes
                magic_len = len(MAGIC_BYTES)
                if HAS_PROTOCOL_V2 and len(data) >= magic_len and data[:magic_len] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                    logger.debug(f"Read v2 message (compressed={bool(header.flags & 1)})")
                else:
                    # Legacy v1 format
                    json_str = data.decode('UTF-8').strip()
                
                if json_str:
                    message = ContextMessage.from_json(json_str)
                    logger.debug(f"Message read from pipe: {message.message_type}")
                    return message
            
            return None
            
        except Exception as e:
            logger.error(f"Pipe read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_pipe__mutmut_28(self) -> Optional[ContextMessage]:
        """Read message from named pipe with auto-detection of v1/v2 protocol."""
        try:
            # Always read in binary mode to detect protocol version
            with open(self.pipe_path, 'rb') as pipe:
                data = pipe.read()
                if not data:
                    return None
                
                # Auto-detect v2 protocol by magic bytes (regardless of settings)
                # Note: Using imported MAGIC_BYTES constant to maintain single source of truth
                # Dynamic length check makes this resilient to future protocol changes
                magic_len = len(MAGIC_BYTES)
                if HAS_PROTOCOL_V2 and len(data) >= magic_len and data[:magic_len] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                    logger.debug(f"Read v2 message (compressed={bool(header.flags & 1)})")
                else:
                    # Legacy v1 format
                    json_str = data.decode('utf-8').strip()
                
                if json_str:
                    message = None
                    logger.debug(f"Message read from pipe: {message.message_type}")
                    return message
            
            return None
            
        except Exception as e:
            logger.error(f"Pipe read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_pipe__mutmut_29(self) -> Optional[ContextMessage]:
        """Read message from named pipe with auto-detection of v1/v2 protocol."""
        try:
            # Always read in binary mode to detect protocol version
            with open(self.pipe_path, 'rb') as pipe:
                data = pipe.read()
                if not data:
                    return None
                
                # Auto-detect v2 protocol by magic bytes (regardless of settings)
                # Note: Using imported MAGIC_BYTES constant to maintain single source of truth
                # Dynamic length check makes this resilient to future protocol changes
                magic_len = len(MAGIC_BYTES)
                if HAS_PROTOCOL_V2 and len(data) >= magic_len and data[:magic_len] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                    logger.debug(f"Read v2 message (compressed={bool(header.flags & 1)})")
                else:
                    # Legacy v1 format
                    json_str = data.decode('utf-8').strip()
                
                if json_str:
                    message = ContextMessage.from_json(None)
                    logger.debug(f"Message read from pipe: {message.message_type}")
                    return message
            
            return None
            
        except Exception as e:
            logger.error(f"Pipe read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_pipe__mutmut_30(self) -> Optional[ContextMessage]:
        """Read message from named pipe with auto-detection of v1/v2 protocol."""
        try:
            # Always read in binary mode to detect protocol version
            with open(self.pipe_path, 'rb') as pipe:
                data = pipe.read()
                if not data:
                    return None
                
                # Auto-detect v2 protocol by magic bytes (regardless of settings)
                # Note: Using imported MAGIC_BYTES constant to maintain single source of truth
                # Dynamic length check makes this resilient to future protocol changes
                magic_len = len(MAGIC_BYTES)
                if HAS_PROTOCOL_V2 and len(data) >= magic_len and data[:magic_len] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                    logger.debug(f"Read v2 message (compressed={bool(header.flags & 1)})")
                else:
                    # Legacy v1 format
                    json_str = data.decode('utf-8').strip()
                
                if json_str:
                    message = ContextMessage.from_json(json_str)
                    logger.debug(None)
                    return message
            
            return None
            
        except Exception as e:
            logger.error(f"Pipe read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_pipe__mutmut_31(self) -> Optional[ContextMessage]:
        """Read message from named pipe with auto-detection of v1/v2 protocol."""
        try:
            # Always read in binary mode to detect protocol version
            with open(self.pipe_path, 'rb') as pipe:
                data = pipe.read()
                if not data:
                    return None
                
                # Auto-detect v2 protocol by magic bytes (regardless of settings)
                # Note: Using imported MAGIC_BYTES constant to maintain single source of truth
                # Dynamic length check makes this resilient to future protocol changes
                magic_len = len(MAGIC_BYTES)
                if HAS_PROTOCOL_V2 and len(data) >= magic_len and data[:magic_len] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                    logger.debug(f"Read v2 message (compressed={bool(header.flags & 1)})")
                else:
                    # Legacy v1 format
                    json_str = data.decode('utf-8').strip()
                
                if json_str:
                    message = ContextMessage.from_json(json_str)
                    logger.debug(f"Message read from pipe: {message.message_type}")
                    return message
            
            return None
            
        except Exception as e:
            logger.error(None)
            return None
    
    xǁBridgeManagerǁ_read_from_pipe__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁBridgeManagerǁ_read_from_pipe__mutmut_1': xǁBridgeManagerǁ_read_from_pipe__mutmut_1, 
        'xǁBridgeManagerǁ_read_from_pipe__mutmut_2': xǁBridgeManagerǁ_read_from_pipe__mutmut_2, 
        'xǁBridgeManagerǁ_read_from_pipe__mutmut_3': xǁBridgeManagerǁ_read_from_pipe__mutmut_3, 
        'xǁBridgeManagerǁ_read_from_pipe__mutmut_4': xǁBridgeManagerǁ_read_from_pipe__mutmut_4, 
        'xǁBridgeManagerǁ_read_from_pipe__mutmut_5': xǁBridgeManagerǁ_read_from_pipe__mutmut_5, 
        'xǁBridgeManagerǁ_read_from_pipe__mutmut_6': xǁBridgeManagerǁ_read_from_pipe__mutmut_6, 
        'xǁBridgeManagerǁ_read_from_pipe__mutmut_7': xǁBridgeManagerǁ_read_from_pipe__mutmut_7, 
        'xǁBridgeManagerǁ_read_from_pipe__mutmut_8': xǁBridgeManagerǁ_read_from_pipe__mutmut_8, 
        'xǁBridgeManagerǁ_read_from_pipe__mutmut_9': xǁBridgeManagerǁ_read_from_pipe__mutmut_9, 
        'xǁBridgeManagerǁ_read_from_pipe__mutmut_10': xǁBridgeManagerǁ_read_from_pipe__mutmut_10, 
        'xǁBridgeManagerǁ_read_from_pipe__mutmut_11': xǁBridgeManagerǁ_read_from_pipe__mutmut_11, 
        'xǁBridgeManagerǁ_read_from_pipe__mutmut_12': xǁBridgeManagerǁ_read_from_pipe__mutmut_12, 
        'xǁBridgeManagerǁ_read_from_pipe__mutmut_13': xǁBridgeManagerǁ_read_from_pipe__mutmut_13, 
        'xǁBridgeManagerǁ_read_from_pipe__mutmut_14': xǁBridgeManagerǁ_read_from_pipe__mutmut_14, 
        'xǁBridgeManagerǁ_read_from_pipe__mutmut_15': xǁBridgeManagerǁ_read_from_pipe__mutmut_15, 
        'xǁBridgeManagerǁ_read_from_pipe__mutmut_16': xǁBridgeManagerǁ_read_from_pipe__mutmut_16, 
        'xǁBridgeManagerǁ_read_from_pipe__mutmut_17': xǁBridgeManagerǁ_read_from_pipe__mutmut_17, 
        'xǁBridgeManagerǁ_read_from_pipe__mutmut_18': xǁBridgeManagerǁ_read_from_pipe__mutmut_18, 
        'xǁBridgeManagerǁ_read_from_pipe__mutmut_19': xǁBridgeManagerǁ_read_from_pipe__mutmut_19, 
        'xǁBridgeManagerǁ_read_from_pipe__mutmut_20': xǁBridgeManagerǁ_read_from_pipe__mutmut_20, 
        'xǁBridgeManagerǁ_read_from_pipe__mutmut_21': xǁBridgeManagerǁ_read_from_pipe__mutmut_21, 
        'xǁBridgeManagerǁ_read_from_pipe__mutmut_22': xǁBridgeManagerǁ_read_from_pipe__mutmut_22, 
        'xǁBridgeManagerǁ_read_from_pipe__mutmut_23': xǁBridgeManagerǁ_read_from_pipe__mutmut_23, 
        'xǁBridgeManagerǁ_read_from_pipe__mutmut_24': xǁBridgeManagerǁ_read_from_pipe__mutmut_24, 
        'xǁBridgeManagerǁ_read_from_pipe__mutmut_25': xǁBridgeManagerǁ_read_from_pipe__mutmut_25, 
        'xǁBridgeManagerǁ_read_from_pipe__mutmut_26': xǁBridgeManagerǁ_read_from_pipe__mutmut_26, 
        'xǁBridgeManagerǁ_read_from_pipe__mutmut_27': xǁBridgeManagerǁ_read_from_pipe__mutmut_27, 
        'xǁBridgeManagerǁ_read_from_pipe__mutmut_28': xǁBridgeManagerǁ_read_from_pipe__mutmut_28, 
        'xǁBridgeManagerǁ_read_from_pipe__mutmut_29': xǁBridgeManagerǁ_read_from_pipe__mutmut_29, 
        'xǁBridgeManagerǁ_read_from_pipe__mutmut_30': xǁBridgeManagerǁ_read_from_pipe__mutmut_30, 
        'xǁBridgeManagerǁ_read_from_pipe__mutmut_31': xǁBridgeManagerǁ_read_from_pipe__mutmut_31
    }
    
    def _read_from_pipe(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁBridgeManagerǁ_read_from_pipe__mutmut_orig"), object.__getattribute__(self, "xǁBridgeManagerǁ_read_from_pipe__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _read_from_pipe.__signature__ = _mutmut_signature(xǁBridgeManagerǁ_read_from_pipe__mutmut_orig)
    xǁBridgeManagerǁ_read_from_pipe__mutmut_orig.__name__ = 'xǁBridgeManagerǁ_read_from_pipe'
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_orig(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_1(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = None
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_2(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(None, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_3(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, None)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_4(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_5(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, )
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_6(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(None)
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_7(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(None))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_8(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(None)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_9(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(2)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_10(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(None, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_11(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, None)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_12(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_13(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, )
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_14(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 385)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_15(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = None
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_16(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = None
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_17(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b'XXXX'
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_18(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = None
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_19(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = True
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_20(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = None
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_21(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 1
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_22(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while False:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_23(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = None
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_24(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(None)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_25(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4097)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_26(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_27(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    return
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_28(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data = chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_29(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data -= chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_30(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 or data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_31(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 or len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_32(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_33(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) > 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_34(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 15 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_35(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:5] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_36(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] != MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_37(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = None
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_38(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = False
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_39(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = None
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_40(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(None)
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_41(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:15])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_42(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = None
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_43(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 - header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_44(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 15 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_45(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 or v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_46(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len >= 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_47(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 1:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_48(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) > v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_49(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        return
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_50(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'XX\nXX' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_51(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' not in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_52(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    return
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_53(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 or data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_54(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 or len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_55(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) > 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_56(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 5 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_57(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:5] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_58(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] != MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_59(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = None
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_60(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(None)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_61(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = None
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_62(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode(None)
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_63(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('XXutf-8XX')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_64(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('UTF-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_65(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = None
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_66(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode(None).strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_67(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('XXutf-8XX').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_68(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('UTF-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_69(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = None
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_70(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(None)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_71(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(None)
                return message
            
            return None
            
        except Exception as e:
            logger.error(f"Socket read error: {e}")
            return None
    
    def xǁBridgeManagerǁ_read_from_socket__mutmut_72(self) -> Optional[ContextMessage]:
        """Read message from Unix socket with auto-detection of v1/v2 protocol."""
        try:
            # Create server socket
            server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
            server.bind(str(self.socket_path))
            server.listen(1)
            
            # Set permissions
            if self.owner_only:
                os.chmod(self.socket_path, 0o600)
            
            # Accept connection
            conn, _ = server.accept()
            
            # Receive message - auto-detect protocol
            data = b''
            is_v2 = False
            v2_expected_len = 0
            
            while True:
                chunk = conn.recv(4096)
                if not chunk:
                    break
                data += chunk
                
                # Auto-detect v2 protocol by magic bytes
                if not is_v2 and len(data) >= 14 and data[:4] == MAGIC_BYTES:
                    is_v2 = True
                    # Use ProtocolHeader for proper parsing
                    if HAS_PROTOCOL_V2:
                        from bridge_protocol_v2 import ProtocolHeader
                        header = ProtocolHeader.from_bytes(data[:14])
                        v2_expected_len = 14 + header.length
                
                # Check if we have complete message
                if is_v2 and v2_expected_len > 0:
                    if len(data) >= v2_expected_len:
                        break
                elif b'\n' in data:
                    break
            
            conn.close()
            server.close()
            
            # Parse message with auto-detected protocol
            if data:
                if HAS_PROTOCOL_V2 and len(data) >= 4 and data[:4] == MAGIC_BYTES:
                    payload, header = v2_decode(data)
                    json_str = payload.decode('utf-8')
                else:
                    json_str = data.decode('utf-8').strip()
                
                message = ContextMessage.from_json(json_str)
                logger.debug(f"Message read from socket: {message.message_type}")
                return message
            
            return None
            
        except Exception as e:
            logger.error(None)
            return None
    
    xǁBridgeManagerǁ_read_from_socket__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁBridgeManagerǁ_read_from_socket__mutmut_1': xǁBridgeManagerǁ_read_from_socket__mutmut_1, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_2': xǁBridgeManagerǁ_read_from_socket__mutmut_2, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_3': xǁBridgeManagerǁ_read_from_socket__mutmut_3, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_4': xǁBridgeManagerǁ_read_from_socket__mutmut_4, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_5': xǁBridgeManagerǁ_read_from_socket__mutmut_5, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_6': xǁBridgeManagerǁ_read_from_socket__mutmut_6, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_7': xǁBridgeManagerǁ_read_from_socket__mutmut_7, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_8': xǁBridgeManagerǁ_read_from_socket__mutmut_8, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_9': xǁBridgeManagerǁ_read_from_socket__mutmut_9, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_10': xǁBridgeManagerǁ_read_from_socket__mutmut_10, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_11': xǁBridgeManagerǁ_read_from_socket__mutmut_11, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_12': xǁBridgeManagerǁ_read_from_socket__mutmut_12, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_13': xǁBridgeManagerǁ_read_from_socket__mutmut_13, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_14': xǁBridgeManagerǁ_read_from_socket__mutmut_14, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_15': xǁBridgeManagerǁ_read_from_socket__mutmut_15, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_16': xǁBridgeManagerǁ_read_from_socket__mutmut_16, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_17': xǁBridgeManagerǁ_read_from_socket__mutmut_17, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_18': xǁBridgeManagerǁ_read_from_socket__mutmut_18, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_19': xǁBridgeManagerǁ_read_from_socket__mutmut_19, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_20': xǁBridgeManagerǁ_read_from_socket__mutmut_20, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_21': xǁBridgeManagerǁ_read_from_socket__mutmut_21, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_22': xǁBridgeManagerǁ_read_from_socket__mutmut_22, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_23': xǁBridgeManagerǁ_read_from_socket__mutmut_23, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_24': xǁBridgeManagerǁ_read_from_socket__mutmut_24, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_25': xǁBridgeManagerǁ_read_from_socket__mutmut_25, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_26': xǁBridgeManagerǁ_read_from_socket__mutmut_26, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_27': xǁBridgeManagerǁ_read_from_socket__mutmut_27, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_28': xǁBridgeManagerǁ_read_from_socket__mutmut_28, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_29': xǁBridgeManagerǁ_read_from_socket__mutmut_29, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_30': xǁBridgeManagerǁ_read_from_socket__mutmut_30, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_31': xǁBridgeManagerǁ_read_from_socket__mutmut_31, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_32': xǁBridgeManagerǁ_read_from_socket__mutmut_32, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_33': xǁBridgeManagerǁ_read_from_socket__mutmut_33, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_34': xǁBridgeManagerǁ_read_from_socket__mutmut_34, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_35': xǁBridgeManagerǁ_read_from_socket__mutmut_35, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_36': xǁBridgeManagerǁ_read_from_socket__mutmut_36, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_37': xǁBridgeManagerǁ_read_from_socket__mutmut_37, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_38': xǁBridgeManagerǁ_read_from_socket__mutmut_38, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_39': xǁBridgeManagerǁ_read_from_socket__mutmut_39, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_40': xǁBridgeManagerǁ_read_from_socket__mutmut_40, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_41': xǁBridgeManagerǁ_read_from_socket__mutmut_41, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_42': xǁBridgeManagerǁ_read_from_socket__mutmut_42, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_43': xǁBridgeManagerǁ_read_from_socket__mutmut_43, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_44': xǁBridgeManagerǁ_read_from_socket__mutmut_44, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_45': xǁBridgeManagerǁ_read_from_socket__mutmut_45, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_46': xǁBridgeManagerǁ_read_from_socket__mutmut_46, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_47': xǁBridgeManagerǁ_read_from_socket__mutmut_47, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_48': xǁBridgeManagerǁ_read_from_socket__mutmut_48, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_49': xǁBridgeManagerǁ_read_from_socket__mutmut_49, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_50': xǁBridgeManagerǁ_read_from_socket__mutmut_50, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_51': xǁBridgeManagerǁ_read_from_socket__mutmut_51, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_52': xǁBridgeManagerǁ_read_from_socket__mutmut_52, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_53': xǁBridgeManagerǁ_read_from_socket__mutmut_53, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_54': xǁBridgeManagerǁ_read_from_socket__mutmut_54, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_55': xǁBridgeManagerǁ_read_from_socket__mutmut_55, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_56': xǁBridgeManagerǁ_read_from_socket__mutmut_56, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_57': xǁBridgeManagerǁ_read_from_socket__mutmut_57, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_58': xǁBridgeManagerǁ_read_from_socket__mutmut_58, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_59': xǁBridgeManagerǁ_read_from_socket__mutmut_59, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_60': xǁBridgeManagerǁ_read_from_socket__mutmut_60, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_61': xǁBridgeManagerǁ_read_from_socket__mutmut_61, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_62': xǁBridgeManagerǁ_read_from_socket__mutmut_62, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_63': xǁBridgeManagerǁ_read_from_socket__mutmut_63, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_64': xǁBridgeManagerǁ_read_from_socket__mutmut_64, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_65': xǁBridgeManagerǁ_read_from_socket__mutmut_65, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_66': xǁBridgeManagerǁ_read_from_socket__mutmut_66, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_67': xǁBridgeManagerǁ_read_from_socket__mutmut_67, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_68': xǁBridgeManagerǁ_read_from_socket__mutmut_68, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_69': xǁBridgeManagerǁ_read_from_socket__mutmut_69, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_70': xǁBridgeManagerǁ_read_from_socket__mutmut_70, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_71': xǁBridgeManagerǁ_read_from_socket__mutmut_71, 
        'xǁBridgeManagerǁ_read_from_socket__mutmut_72': xǁBridgeManagerǁ_read_from_socket__mutmut_72
    }
    
    def _read_from_socket(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁBridgeManagerǁ_read_from_socket__mutmut_orig"), object.__getattribute__(self, "xǁBridgeManagerǁ_read_from_socket__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _read_from_socket.__signature__ = _mutmut_signature(xǁBridgeManagerǁ_read_from_socket__mutmut_orig)
    xǁBridgeManagerǁ_read_from_socket__mutmut_orig.__name__ = 'xǁBridgeManagerǁ_read_from_socket'
    
    def xǁBridgeManagerǁcleanup__mutmut_orig(self) -> None:
        """Clean up bridge resources and audit log final state (PS-02)."""
        try:
            self._audit_log("BRIDGE_CLEANUP", {
                "mode": self.mode.value,
                "bridge_dir": str(self.bridge_dir)
            })
            
            if self.mode == BridgeMode.NAMED_PIPE and self.pipe_path.exists():
                self.pipe_path.unlink()
            elif self.mode == BridgeMode.UNIX_SOCKET and self.socket_path.exists():
                self.socket_path.unlink()
            
            if self.lock_path.exists():
                self.lock_path.unlink()
            
            logger.info("Bridge cleaned up")
            
        except Exception as e:
            logger.error(f"Bridge cleanup error: {e}")
            self._audit_log("CLEANUP_ERROR", {"error": str(e)})
    
    def xǁBridgeManagerǁcleanup__mutmut_1(self) -> None:
        """Clean up bridge resources and audit log final state (PS-02)."""
        try:
            self._audit_log(None, {
                "mode": self.mode.value,
                "bridge_dir": str(self.bridge_dir)
            })
            
            if self.mode == BridgeMode.NAMED_PIPE and self.pipe_path.exists():
                self.pipe_path.unlink()
            elif self.mode == BridgeMode.UNIX_SOCKET and self.socket_path.exists():
                self.socket_path.unlink()
            
            if self.lock_path.exists():
                self.lock_path.unlink()
            
            logger.info("Bridge cleaned up")
            
        except Exception as e:
            logger.error(f"Bridge cleanup error: {e}")
            self._audit_log("CLEANUP_ERROR", {"error": str(e)})
    
    def xǁBridgeManagerǁcleanup__mutmut_2(self) -> None:
        """Clean up bridge resources and audit log final state (PS-02)."""
        try:
            self._audit_log("BRIDGE_CLEANUP", None)
            
            if self.mode == BridgeMode.NAMED_PIPE and self.pipe_path.exists():
                self.pipe_path.unlink()
            elif self.mode == BridgeMode.UNIX_SOCKET and self.socket_path.exists():
                self.socket_path.unlink()
            
            if self.lock_path.exists():
                self.lock_path.unlink()
            
            logger.info("Bridge cleaned up")
            
        except Exception as e:
            logger.error(f"Bridge cleanup error: {e}")
            self._audit_log("CLEANUP_ERROR", {"error": str(e)})
    
    def xǁBridgeManagerǁcleanup__mutmut_3(self) -> None:
        """Clean up bridge resources and audit log final state (PS-02)."""
        try:
            self._audit_log({
                "mode": self.mode.value,
                "bridge_dir": str(self.bridge_dir)
            })
            
            if self.mode == BridgeMode.NAMED_PIPE and self.pipe_path.exists():
                self.pipe_path.unlink()
            elif self.mode == BridgeMode.UNIX_SOCKET and self.socket_path.exists():
                self.socket_path.unlink()
            
            if self.lock_path.exists():
                self.lock_path.unlink()
            
            logger.info("Bridge cleaned up")
            
        except Exception as e:
            logger.error(f"Bridge cleanup error: {e}")
            self._audit_log("CLEANUP_ERROR", {"error": str(e)})
    
    def xǁBridgeManagerǁcleanup__mutmut_4(self) -> None:
        """Clean up bridge resources and audit log final state (PS-02)."""
        try:
            self._audit_log("BRIDGE_CLEANUP", )
            
            if self.mode == BridgeMode.NAMED_PIPE and self.pipe_path.exists():
                self.pipe_path.unlink()
            elif self.mode == BridgeMode.UNIX_SOCKET and self.socket_path.exists():
                self.socket_path.unlink()
            
            if self.lock_path.exists():
                self.lock_path.unlink()
            
            logger.info("Bridge cleaned up")
            
        except Exception as e:
            logger.error(f"Bridge cleanup error: {e}")
            self._audit_log("CLEANUP_ERROR", {"error": str(e)})
    
    def xǁBridgeManagerǁcleanup__mutmut_5(self) -> None:
        """Clean up bridge resources and audit log final state (PS-02)."""
        try:
            self._audit_log("XXBRIDGE_CLEANUPXX", {
                "mode": self.mode.value,
                "bridge_dir": str(self.bridge_dir)
            })
            
            if self.mode == BridgeMode.NAMED_PIPE and self.pipe_path.exists():
                self.pipe_path.unlink()
            elif self.mode == BridgeMode.UNIX_SOCKET and self.socket_path.exists():
                self.socket_path.unlink()
            
            if self.lock_path.exists():
                self.lock_path.unlink()
            
            logger.info("Bridge cleaned up")
            
        except Exception as e:
            logger.error(f"Bridge cleanup error: {e}")
            self._audit_log("CLEANUP_ERROR", {"error": str(e)})
    
    def xǁBridgeManagerǁcleanup__mutmut_6(self) -> None:
        """Clean up bridge resources and audit log final state (PS-02)."""
        try:
            self._audit_log("bridge_cleanup", {
                "mode": self.mode.value,
                "bridge_dir": str(self.bridge_dir)
            })
            
            if self.mode == BridgeMode.NAMED_PIPE and self.pipe_path.exists():
                self.pipe_path.unlink()
            elif self.mode == BridgeMode.UNIX_SOCKET and self.socket_path.exists():
                self.socket_path.unlink()
            
            if self.lock_path.exists():
                self.lock_path.unlink()
            
            logger.info("Bridge cleaned up")
            
        except Exception as e:
            logger.error(f"Bridge cleanup error: {e}")
            self._audit_log("CLEANUP_ERROR", {"error": str(e)})
    
    def xǁBridgeManagerǁcleanup__mutmut_7(self) -> None:
        """Clean up bridge resources and audit log final state (PS-02)."""
        try:
            self._audit_log("BRIDGE_CLEANUP", {
                "XXmodeXX": self.mode.value,
                "bridge_dir": str(self.bridge_dir)
            })
            
            if self.mode == BridgeMode.NAMED_PIPE and self.pipe_path.exists():
                self.pipe_path.unlink()
            elif self.mode == BridgeMode.UNIX_SOCKET and self.socket_path.exists():
                self.socket_path.unlink()
            
            if self.lock_path.exists():
                self.lock_path.unlink()
            
            logger.info("Bridge cleaned up")
            
        except Exception as e:
            logger.error(f"Bridge cleanup error: {e}")
            self._audit_log("CLEANUP_ERROR", {"error": str(e)})
    
    def xǁBridgeManagerǁcleanup__mutmut_8(self) -> None:
        """Clean up bridge resources and audit log final state (PS-02)."""
        try:
            self._audit_log("BRIDGE_CLEANUP", {
                "MODE": self.mode.value,
                "bridge_dir": str(self.bridge_dir)
            })
            
            if self.mode == BridgeMode.NAMED_PIPE and self.pipe_path.exists():
                self.pipe_path.unlink()
            elif self.mode == BridgeMode.UNIX_SOCKET and self.socket_path.exists():
                self.socket_path.unlink()
            
            if self.lock_path.exists():
                self.lock_path.unlink()
            
            logger.info("Bridge cleaned up")
            
        except Exception as e:
            logger.error(f"Bridge cleanup error: {e}")
            self._audit_log("CLEANUP_ERROR", {"error": str(e)})
    
    def xǁBridgeManagerǁcleanup__mutmut_9(self) -> None:
        """Clean up bridge resources and audit log final state (PS-02)."""
        try:
            self._audit_log("BRIDGE_CLEANUP", {
                "mode": self.mode.value,
                "XXbridge_dirXX": str(self.bridge_dir)
            })
            
            if self.mode == BridgeMode.NAMED_PIPE and self.pipe_path.exists():
                self.pipe_path.unlink()
            elif self.mode == BridgeMode.UNIX_SOCKET and self.socket_path.exists():
                self.socket_path.unlink()
            
            if self.lock_path.exists():
                self.lock_path.unlink()
            
            logger.info("Bridge cleaned up")
            
        except Exception as e:
            logger.error(f"Bridge cleanup error: {e}")
            self._audit_log("CLEANUP_ERROR", {"error": str(e)})
    
    def xǁBridgeManagerǁcleanup__mutmut_10(self) -> None:
        """Clean up bridge resources and audit log final state (PS-02)."""
        try:
            self._audit_log("BRIDGE_CLEANUP", {
                "mode": self.mode.value,
                "BRIDGE_DIR": str(self.bridge_dir)
            })
            
            if self.mode == BridgeMode.NAMED_PIPE and self.pipe_path.exists():
                self.pipe_path.unlink()
            elif self.mode == BridgeMode.UNIX_SOCKET and self.socket_path.exists():
                self.socket_path.unlink()
            
            if self.lock_path.exists():
                self.lock_path.unlink()
            
            logger.info("Bridge cleaned up")
            
        except Exception as e:
            logger.error(f"Bridge cleanup error: {e}")
            self._audit_log("CLEANUP_ERROR", {"error": str(e)})
    
    def xǁBridgeManagerǁcleanup__mutmut_11(self) -> None:
        """Clean up bridge resources and audit log final state (PS-02)."""
        try:
            self._audit_log("BRIDGE_CLEANUP", {
                "mode": self.mode.value,
                "bridge_dir": str(None)
            })
            
            if self.mode == BridgeMode.NAMED_PIPE and self.pipe_path.exists():
                self.pipe_path.unlink()
            elif self.mode == BridgeMode.UNIX_SOCKET and self.socket_path.exists():
                self.socket_path.unlink()
            
            if self.lock_path.exists():
                self.lock_path.unlink()
            
            logger.info("Bridge cleaned up")
            
        except Exception as e:
            logger.error(f"Bridge cleanup error: {e}")
            self._audit_log("CLEANUP_ERROR", {"error": str(e)})
    
    def xǁBridgeManagerǁcleanup__mutmut_12(self) -> None:
        """Clean up bridge resources and audit log final state (PS-02)."""
        try:
            self._audit_log("BRIDGE_CLEANUP", {
                "mode": self.mode.value,
                "bridge_dir": str(self.bridge_dir)
            })
            
            if self.mode == BridgeMode.NAMED_PIPE or self.pipe_path.exists():
                self.pipe_path.unlink()
            elif self.mode == BridgeMode.UNIX_SOCKET and self.socket_path.exists():
                self.socket_path.unlink()
            
            if self.lock_path.exists():
                self.lock_path.unlink()
            
            logger.info("Bridge cleaned up")
            
        except Exception as e:
            logger.error(f"Bridge cleanup error: {e}")
            self._audit_log("CLEANUP_ERROR", {"error": str(e)})
    
    def xǁBridgeManagerǁcleanup__mutmut_13(self) -> None:
        """Clean up bridge resources and audit log final state (PS-02)."""
        try:
            self._audit_log("BRIDGE_CLEANUP", {
                "mode": self.mode.value,
                "bridge_dir": str(self.bridge_dir)
            })
            
            if self.mode != BridgeMode.NAMED_PIPE and self.pipe_path.exists():
                self.pipe_path.unlink()
            elif self.mode == BridgeMode.UNIX_SOCKET and self.socket_path.exists():
                self.socket_path.unlink()
            
            if self.lock_path.exists():
                self.lock_path.unlink()
            
            logger.info("Bridge cleaned up")
            
        except Exception as e:
            logger.error(f"Bridge cleanup error: {e}")
            self._audit_log("CLEANUP_ERROR", {"error": str(e)})
    
    def xǁBridgeManagerǁcleanup__mutmut_14(self) -> None:
        """Clean up bridge resources and audit log final state (PS-02)."""
        try:
            self._audit_log("BRIDGE_CLEANUP", {
                "mode": self.mode.value,
                "bridge_dir": str(self.bridge_dir)
            })
            
            if self.mode == BridgeMode.NAMED_PIPE and self.pipe_path.exists():
                self.pipe_path.unlink()
            elif self.mode == BridgeMode.UNIX_SOCKET or self.socket_path.exists():
                self.socket_path.unlink()
            
            if self.lock_path.exists():
                self.lock_path.unlink()
            
            logger.info("Bridge cleaned up")
            
        except Exception as e:
            logger.error(f"Bridge cleanup error: {e}")
            self._audit_log("CLEANUP_ERROR", {"error": str(e)})
    
    def xǁBridgeManagerǁcleanup__mutmut_15(self) -> None:
        """Clean up bridge resources and audit log final state (PS-02)."""
        try:
            self._audit_log("BRIDGE_CLEANUP", {
                "mode": self.mode.value,
                "bridge_dir": str(self.bridge_dir)
            })
            
            if self.mode == BridgeMode.NAMED_PIPE and self.pipe_path.exists():
                self.pipe_path.unlink()
            elif self.mode != BridgeMode.UNIX_SOCKET and self.socket_path.exists():
                self.socket_path.unlink()
            
            if self.lock_path.exists():
                self.lock_path.unlink()
            
            logger.info("Bridge cleaned up")
            
        except Exception as e:
            logger.error(f"Bridge cleanup error: {e}")
            self._audit_log("CLEANUP_ERROR", {"error": str(e)})
    
    def xǁBridgeManagerǁcleanup__mutmut_16(self) -> None:
        """Clean up bridge resources and audit log final state (PS-02)."""
        try:
            self._audit_log("BRIDGE_CLEANUP", {
                "mode": self.mode.value,
                "bridge_dir": str(self.bridge_dir)
            })
            
            if self.mode == BridgeMode.NAMED_PIPE and self.pipe_path.exists():
                self.pipe_path.unlink()
            elif self.mode == BridgeMode.UNIX_SOCKET and self.socket_path.exists():
                self.socket_path.unlink()
            
            if self.lock_path.exists():
                self.lock_path.unlink()
            
            logger.info(None)
            
        except Exception as e:
            logger.error(f"Bridge cleanup error: {e}")
            self._audit_log("CLEANUP_ERROR", {"error": str(e)})
    
    def xǁBridgeManagerǁcleanup__mutmut_17(self) -> None:
        """Clean up bridge resources and audit log final state (PS-02)."""
        try:
            self._audit_log("BRIDGE_CLEANUP", {
                "mode": self.mode.value,
                "bridge_dir": str(self.bridge_dir)
            })
            
            if self.mode == BridgeMode.NAMED_PIPE and self.pipe_path.exists():
                self.pipe_path.unlink()
            elif self.mode == BridgeMode.UNIX_SOCKET and self.socket_path.exists():
                self.socket_path.unlink()
            
            if self.lock_path.exists():
                self.lock_path.unlink()
            
            logger.info("XXBridge cleaned upXX")
            
        except Exception as e:
            logger.error(f"Bridge cleanup error: {e}")
            self._audit_log("CLEANUP_ERROR", {"error": str(e)})
    
    def xǁBridgeManagerǁcleanup__mutmut_18(self) -> None:
        """Clean up bridge resources and audit log final state (PS-02)."""
        try:
            self._audit_log("BRIDGE_CLEANUP", {
                "mode": self.mode.value,
                "bridge_dir": str(self.bridge_dir)
            })
            
            if self.mode == BridgeMode.NAMED_PIPE and self.pipe_path.exists():
                self.pipe_path.unlink()
            elif self.mode == BridgeMode.UNIX_SOCKET and self.socket_path.exists():
                self.socket_path.unlink()
            
            if self.lock_path.exists():
                self.lock_path.unlink()
            
            logger.info("bridge cleaned up")
            
        except Exception as e:
            logger.error(f"Bridge cleanup error: {e}")
            self._audit_log("CLEANUP_ERROR", {"error": str(e)})
    
    def xǁBridgeManagerǁcleanup__mutmut_19(self) -> None:
        """Clean up bridge resources and audit log final state (PS-02)."""
        try:
            self._audit_log("BRIDGE_CLEANUP", {
                "mode": self.mode.value,
                "bridge_dir": str(self.bridge_dir)
            })
            
            if self.mode == BridgeMode.NAMED_PIPE and self.pipe_path.exists():
                self.pipe_path.unlink()
            elif self.mode == BridgeMode.UNIX_SOCKET and self.socket_path.exists():
                self.socket_path.unlink()
            
            if self.lock_path.exists():
                self.lock_path.unlink()
            
            logger.info("BRIDGE CLEANED UP")
            
        except Exception as e:
            logger.error(f"Bridge cleanup error: {e}")
            self._audit_log("CLEANUP_ERROR", {"error": str(e)})
    
    def xǁBridgeManagerǁcleanup__mutmut_20(self) -> None:
        """Clean up bridge resources and audit log final state (PS-02)."""
        try:
            self._audit_log("BRIDGE_CLEANUP", {
                "mode": self.mode.value,
                "bridge_dir": str(self.bridge_dir)
            })
            
            if self.mode == BridgeMode.NAMED_PIPE and self.pipe_path.exists():
                self.pipe_path.unlink()
            elif self.mode == BridgeMode.UNIX_SOCKET and self.socket_path.exists():
                self.socket_path.unlink()
            
            if self.lock_path.exists():
                self.lock_path.unlink()
            
            logger.info("Bridge cleaned up")
            
        except Exception as e:
            logger.error(None)
            self._audit_log("CLEANUP_ERROR", {"error": str(e)})
    
    def xǁBridgeManagerǁcleanup__mutmut_21(self) -> None:
        """Clean up bridge resources and audit log final state (PS-02)."""
        try:
            self._audit_log("BRIDGE_CLEANUP", {
                "mode": self.mode.value,
                "bridge_dir": str(self.bridge_dir)
            })
            
            if self.mode == BridgeMode.NAMED_PIPE and self.pipe_path.exists():
                self.pipe_path.unlink()
            elif self.mode == BridgeMode.UNIX_SOCKET and self.socket_path.exists():
                self.socket_path.unlink()
            
            if self.lock_path.exists():
                self.lock_path.unlink()
            
            logger.info("Bridge cleaned up")
            
        except Exception as e:
            logger.error(f"Bridge cleanup error: {e}")
            self._audit_log(None, {"error": str(e)})
    
    def xǁBridgeManagerǁcleanup__mutmut_22(self) -> None:
        """Clean up bridge resources and audit log final state (PS-02)."""
        try:
            self._audit_log("BRIDGE_CLEANUP", {
                "mode": self.mode.value,
                "bridge_dir": str(self.bridge_dir)
            })
            
            if self.mode == BridgeMode.NAMED_PIPE and self.pipe_path.exists():
                self.pipe_path.unlink()
            elif self.mode == BridgeMode.UNIX_SOCKET and self.socket_path.exists():
                self.socket_path.unlink()
            
            if self.lock_path.exists():
                self.lock_path.unlink()
            
            logger.info("Bridge cleaned up")
            
        except Exception as e:
            logger.error(f"Bridge cleanup error: {e}")
            self._audit_log("CLEANUP_ERROR", None)
    
    def xǁBridgeManagerǁcleanup__mutmut_23(self) -> None:
        """Clean up bridge resources and audit log final state (PS-02)."""
        try:
            self._audit_log("BRIDGE_CLEANUP", {
                "mode": self.mode.value,
                "bridge_dir": str(self.bridge_dir)
            })
            
            if self.mode == BridgeMode.NAMED_PIPE and self.pipe_path.exists():
                self.pipe_path.unlink()
            elif self.mode == BridgeMode.UNIX_SOCKET and self.socket_path.exists():
                self.socket_path.unlink()
            
            if self.lock_path.exists():
                self.lock_path.unlink()
            
            logger.info("Bridge cleaned up")
            
        except Exception as e:
            logger.error(f"Bridge cleanup error: {e}")
            self._audit_log({"error": str(e)})
    
    def xǁBridgeManagerǁcleanup__mutmut_24(self) -> None:
        """Clean up bridge resources and audit log final state (PS-02)."""
        try:
            self._audit_log("BRIDGE_CLEANUP", {
                "mode": self.mode.value,
                "bridge_dir": str(self.bridge_dir)
            })
            
            if self.mode == BridgeMode.NAMED_PIPE and self.pipe_path.exists():
                self.pipe_path.unlink()
            elif self.mode == BridgeMode.UNIX_SOCKET and self.socket_path.exists():
                self.socket_path.unlink()
            
            if self.lock_path.exists():
                self.lock_path.unlink()
            
            logger.info("Bridge cleaned up")
            
        except Exception as e:
            logger.error(f"Bridge cleanup error: {e}")
            self._audit_log("CLEANUP_ERROR", )
    
    def xǁBridgeManagerǁcleanup__mutmut_25(self) -> None:
        """Clean up bridge resources and audit log final state (PS-02)."""
        try:
            self._audit_log("BRIDGE_CLEANUP", {
                "mode": self.mode.value,
                "bridge_dir": str(self.bridge_dir)
            })
            
            if self.mode == BridgeMode.NAMED_PIPE and self.pipe_path.exists():
                self.pipe_path.unlink()
            elif self.mode == BridgeMode.UNIX_SOCKET and self.socket_path.exists():
                self.socket_path.unlink()
            
            if self.lock_path.exists():
                self.lock_path.unlink()
            
            logger.info("Bridge cleaned up")
            
        except Exception as e:
            logger.error(f"Bridge cleanup error: {e}")
            self._audit_log("XXCLEANUP_ERRORXX", {"error": str(e)})
    
    def xǁBridgeManagerǁcleanup__mutmut_26(self) -> None:
        """Clean up bridge resources and audit log final state (PS-02)."""
        try:
            self._audit_log("BRIDGE_CLEANUP", {
                "mode": self.mode.value,
                "bridge_dir": str(self.bridge_dir)
            })
            
            if self.mode == BridgeMode.NAMED_PIPE and self.pipe_path.exists():
                self.pipe_path.unlink()
            elif self.mode == BridgeMode.UNIX_SOCKET and self.socket_path.exists():
                self.socket_path.unlink()
            
            if self.lock_path.exists():
                self.lock_path.unlink()
            
            logger.info("Bridge cleaned up")
            
        except Exception as e:
            logger.error(f"Bridge cleanup error: {e}")
            self._audit_log("cleanup_error", {"error": str(e)})
    
    def xǁBridgeManagerǁcleanup__mutmut_27(self) -> None:
        """Clean up bridge resources and audit log final state (PS-02)."""
        try:
            self._audit_log("BRIDGE_CLEANUP", {
                "mode": self.mode.value,
                "bridge_dir": str(self.bridge_dir)
            })
            
            if self.mode == BridgeMode.NAMED_PIPE and self.pipe_path.exists():
                self.pipe_path.unlink()
            elif self.mode == BridgeMode.UNIX_SOCKET and self.socket_path.exists():
                self.socket_path.unlink()
            
            if self.lock_path.exists():
                self.lock_path.unlink()
            
            logger.info("Bridge cleaned up")
            
        except Exception as e:
            logger.error(f"Bridge cleanup error: {e}")
            self._audit_log("CLEANUP_ERROR", {"XXerrorXX": str(e)})
    
    def xǁBridgeManagerǁcleanup__mutmut_28(self) -> None:
        """Clean up bridge resources and audit log final state (PS-02)."""
        try:
            self._audit_log("BRIDGE_CLEANUP", {
                "mode": self.mode.value,
                "bridge_dir": str(self.bridge_dir)
            })
            
            if self.mode == BridgeMode.NAMED_PIPE and self.pipe_path.exists():
                self.pipe_path.unlink()
            elif self.mode == BridgeMode.UNIX_SOCKET and self.socket_path.exists():
                self.socket_path.unlink()
            
            if self.lock_path.exists():
                self.lock_path.unlink()
            
            logger.info("Bridge cleaned up")
            
        except Exception as e:
            logger.error(f"Bridge cleanup error: {e}")
            self._audit_log("CLEANUP_ERROR", {"ERROR": str(e)})
    
    def xǁBridgeManagerǁcleanup__mutmut_29(self) -> None:
        """Clean up bridge resources and audit log final state (PS-02)."""
        try:
            self._audit_log("BRIDGE_CLEANUP", {
                "mode": self.mode.value,
                "bridge_dir": str(self.bridge_dir)
            })
            
            if self.mode == BridgeMode.NAMED_PIPE and self.pipe_path.exists():
                self.pipe_path.unlink()
            elif self.mode == BridgeMode.UNIX_SOCKET and self.socket_path.exists():
                self.socket_path.unlink()
            
            if self.lock_path.exists():
                self.lock_path.unlink()
            
            logger.info("Bridge cleaned up")
            
        except Exception as e:
            logger.error(f"Bridge cleanup error: {e}")
            self._audit_log("CLEANUP_ERROR", {"error": str(None)})
    
    xǁBridgeManagerǁcleanup__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁBridgeManagerǁcleanup__mutmut_1': xǁBridgeManagerǁcleanup__mutmut_1, 
        'xǁBridgeManagerǁcleanup__mutmut_2': xǁBridgeManagerǁcleanup__mutmut_2, 
        'xǁBridgeManagerǁcleanup__mutmut_3': xǁBridgeManagerǁcleanup__mutmut_3, 
        'xǁBridgeManagerǁcleanup__mutmut_4': xǁBridgeManagerǁcleanup__mutmut_4, 
        'xǁBridgeManagerǁcleanup__mutmut_5': xǁBridgeManagerǁcleanup__mutmut_5, 
        'xǁBridgeManagerǁcleanup__mutmut_6': xǁBridgeManagerǁcleanup__mutmut_6, 
        'xǁBridgeManagerǁcleanup__mutmut_7': xǁBridgeManagerǁcleanup__mutmut_7, 
        'xǁBridgeManagerǁcleanup__mutmut_8': xǁBridgeManagerǁcleanup__mutmut_8, 
        'xǁBridgeManagerǁcleanup__mutmut_9': xǁBridgeManagerǁcleanup__mutmut_9, 
        'xǁBridgeManagerǁcleanup__mutmut_10': xǁBridgeManagerǁcleanup__mutmut_10, 
        'xǁBridgeManagerǁcleanup__mutmut_11': xǁBridgeManagerǁcleanup__mutmut_11, 
        'xǁBridgeManagerǁcleanup__mutmut_12': xǁBridgeManagerǁcleanup__mutmut_12, 
        'xǁBridgeManagerǁcleanup__mutmut_13': xǁBridgeManagerǁcleanup__mutmut_13, 
        'xǁBridgeManagerǁcleanup__mutmut_14': xǁBridgeManagerǁcleanup__mutmut_14, 
        'xǁBridgeManagerǁcleanup__mutmut_15': xǁBridgeManagerǁcleanup__mutmut_15, 
        'xǁBridgeManagerǁcleanup__mutmut_16': xǁBridgeManagerǁcleanup__mutmut_16, 
        'xǁBridgeManagerǁcleanup__mutmut_17': xǁBridgeManagerǁcleanup__mutmut_17, 
        'xǁBridgeManagerǁcleanup__mutmut_18': xǁBridgeManagerǁcleanup__mutmut_18, 
        'xǁBridgeManagerǁcleanup__mutmut_19': xǁBridgeManagerǁcleanup__mutmut_19, 
        'xǁBridgeManagerǁcleanup__mutmut_20': xǁBridgeManagerǁcleanup__mutmut_20, 
        'xǁBridgeManagerǁcleanup__mutmut_21': xǁBridgeManagerǁcleanup__mutmut_21, 
        'xǁBridgeManagerǁcleanup__mutmut_22': xǁBridgeManagerǁcleanup__mutmut_22, 
        'xǁBridgeManagerǁcleanup__mutmut_23': xǁBridgeManagerǁcleanup__mutmut_23, 
        'xǁBridgeManagerǁcleanup__mutmut_24': xǁBridgeManagerǁcleanup__mutmut_24, 
        'xǁBridgeManagerǁcleanup__mutmut_25': xǁBridgeManagerǁcleanup__mutmut_25, 
        'xǁBridgeManagerǁcleanup__mutmut_26': xǁBridgeManagerǁcleanup__mutmut_26, 
        'xǁBridgeManagerǁcleanup__mutmut_27': xǁBridgeManagerǁcleanup__mutmut_27, 
        'xǁBridgeManagerǁcleanup__mutmut_28': xǁBridgeManagerǁcleanup__mutmut_28, 
        'xǁBridgeManagerǁcleanup__mutmut_29': xǁBridgeManagerǁcleanup__mutmut_29
    }
    
    def cleanup(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁBridgeManagerǁcleanup__mutmut_orig"), object.__getattribute__(self, "xǁBridgeManagerǁcleanup__mutmut_mutants"), args, kwargs, self)
        return result 
    
    cleanup.__signature__ = _mutmut_signature(xǁBridgeManagerǁcleanup__mutmut_orig)
    xǁBridgeManagerǁcleanup__mutmut_orig.__name__ = 'xǁBridgeManagerǁcleanup'


# Convenience functions for cognitive brain integration

def x_share_context_with_copilot__mutmut_orig(context: Dict[str, Any], bridge: Optional[BridgeManager] = None) -> bool:
    """
    Share cognitive context with Copilot watcher via secure bridge.
    
    Args:
        context: Context data to share
        bridge: Optional bridge instance (creates default if not provided)
        
    Returns:
        True if context shared successfully
    """
    if bridge is None:
        bridge = BridgeManager()
    
    # Get auth token from environment for authentication (PS-02)
    auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
    
    message = ContextMessage(
        timestamp=datetime.now(UTC).isoformat(),
        source="cognitive_brain",
        message_type="context_update",
        context=context,
        metadata={"version": "1.0"},
        auth_token=auth_token
    )
    
    return bridge.write_message(message)


# Convenience functions for cognitive brain integration

def x_share_context_with_copilot__mutmut_1(context: Dict[str, Any], bridge: Optional[BridgeManager] = None) -> bool:
    """
    Share cognitive context with Copilot watcher via secure bridge.
    
    Args:
        context: Context data to share
        bridge: Optional bridge instance (creates default if not provided)
        
    Returns:
        True if context shared successfully
    """
    if bridge is not None:
        bridge = BridgeManager()
    
    # Get auth token from environment for authentication (PS-02)
    auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
    
    message = ContextMessage(
        timestamp=datetime.now(UTC).isoformat(),
        source="cognitive_brain",
        message_type="context_update",
        context=context,
        metadata={"version": "1.0"},
        auth_token=auth_token
    )
    
    return bridge.write_message(message)


# Convenience functions for cognitive brain integration

def x_share_context_with_copilot__mutmut_2(context: Dict[str, Any], bridge: Optional[BridgeManager] = None) -> bool:
    """
    Share cognitive context with Copilot watcher via secure bridge.
    
    Args:
        context: Context data to share
        bridge: Optional bridge instance (creates default if not provided)
        
    Returns:
        True if context shared successfully
    """
    if bridge is None:
        bridge = None
    
    # Get auth token from environment for authentication (PS-02)
    auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
    
    message = ContextMessage(
        timestamp=datetime.now(UTC).isoformat(),
        source="cognitive_brain",
        message_type="context_update",
        context=context,
        metadata={"version": "1.0"},
        auth_token=auth_token
    )
    
    return bridge.write_message(message)


# Convenience functions for cognitive brain integration

def x_share_context_with_copilot__mutmut_3(context: Dict[str, Any], bridge: Optional[BridgeManager] = None) -> bool:
    """
    Share cognitive context with Copilot watcher via secure bridge.
    
    Args:
        context: Context data to share
        bridge: Optional bridge instance (creates default if not provided)
        
    Returns:
        True if context shared successfully
    """
    if bridge is None:
        bridge = BridgeManager()
    
    # Get auth token from environment for authentication (PS-02)
    auth_token = None
    
    message = ContextMessage(
        timestamp=datetime.now(UTC).isoformat(),
        source="cognitive_brain",
        message_type="context_update",
        context=context,
        metadata={"version": "1.0"},
        auth_token=auth_token
    )
    
    return bridge.write_message(message)


# Convenience functions for cognitive brain integration

def x_share_context_with_copilot__mutmut_4(context: Dict[str, Any], bridge: Optional[BridgeManager] = None) -> bool:
    """
    Share cognitive context with Copilot watcher via secure bridge.
    
    Args:
        context: Context data to share
        bridge: Optional bridge instance (creates default if not provided)
        
    Returns:
        True if context shared successfully
    """
    if bridge is None:
        bridge = BridgeManager()
    
    # Get auth token from environment for authentication (PS-02)
    auth_token = os.getenv(None)
    
    message = ContextMessage(
        timestamp=datetime.now(UTC).isoformat(),
        source="cognitive_brain",
        message_type="context_update",
        context=context,
        metadata={"version": "1.0"},
        auth_token=auth_token
    )
    
    return bridge.write_message(message)


# Convenience functions for cognitive brain integration

def x_share_context_with_copilot__mutmut_5(context: Dict[str, Any], bridge: Optional[BridgeManager] = None) -> bool:
    """
    Share cognitive context with Copilot watcher via secure bridge.
    
    Args:
        context: Context data to share
        bridge: Optional bridge instance (creates default if not provided)
        
    Returns:
        True if context shared successfully
    """
    if bridge is None:
        bridge = BridgeManager()
    
    # Get auth token from environment for authentication (PS-02)
    auth_token = os.getenv("XXCODEX_BRIDGE_TOKENXX")
    
    message = ContextMessage(
        timestamp=datetime.now(UTC).isoformat(),
        source="cognitive_brain",
        message_type="context_update",
        context=context,
        metadata={"version": "1.0"},
        auth_token=auth_token
    )
    
    return bridge.write_message(message)


# Convenience functions for cognitive brain integration

def x_share_context_with_copilot__mutmut_6(context: Dict[str, Any], bridge: Optional[BridgeManager] = None) -> bool:
    """
    Share cognitive context with Copilot watcher via secure bridge.
    
    Args:
        context: Context data to share
        bridge: Optional bridge instance (creates default if not provided)
        
    Returns:
        True if context shared successfully
    """
    if bridge is None:
        bridge = BridgeManager()
    
    # Get auth token from environment for authentication (PS-02)
    auth_token = os.getenv("codex_bridge_token")
    
    message = ContextMessage(
        timestamp=datetime.now(UTC).isoformat(),
        source="cognitive_brain",
        message_type="context_update",
        context=context,
        metadata={"version": "1.0"},
        auth_token=auth_token
    )
    
    return bridge.write_message(message)


# Convenience functions for cognitive brain integration

def x_share_context_with_copilot__mutmut_7(context: Dict[str, Any], bridge: Optional[BridgeManager] = None) -> bool:
    """
    Share cognitive context with Copilot watcher via secure bridge.
    
    Args:
        context: Context data to share
        bridge: Optional bridge instance (creates default if not provided)
        
    Returns:
        True if context shared successfully
    """
    if bridge is None:
        bridge = BridgeManager()
    
    # Get auth token from environment for authentication (PS-02)
    auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
    
    message = None
    
    return bridge.write_message(message)


# Convenience functions for cognitive brain integration

def x_share_context_with_copilot__mutmut_8(context: Dict[str, Any], bridge: Optional[BridgeManager] = None) -> bool:
    """
    Share cognitive context with Copilot watcher via secure bridge.
    
    Args:
        context: Context data to share
        bridge: Optional bridge instance (creates default if not provided)
        
    Returns:
        True if context shared successfully
    """
    if bridge is None:
        bridge = BridgeManager()
    
    # Get auth token from environment for authentication (PS-02)
    auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
    
    message = ContextMessage(
        timestamp=None,
        source="cognitive_brain",
        message_type="context_update",
        context=context,
        metadata={"version": "1.0"},
        auth_token=auth_token
    )
    
    return bridge.write_message(message)


# Convenience functions for cognitive brain integration

def x_share_context_with_copilot__mutmut_9(context: Dict[str, Any], bridge: Optional[BridgeManager] = None) -> bool:
    """
    Share cognitive context with Copilot watcher via secure bridge.
    
    Args:
        context: Context data to share
        bridge: Optional bridge instance (creates default if not provided)
        
    Returns:
        True if context shared successfully
    """
    if bridge is None:
        bridge = BridgeManager()
    
    # Get auth token from environment for authentication (PS-02)
    auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
    
    message = ContextMessage(
        timestamp=datetime.now(UTC).isoformat(),
        source=None,
        message_type="context_update",
        context=context,
        metadata={"version": "1.0"},
        auth_token=auth_token
    )
    
    return bridge.write_message(message)


# Convenience functions for cognitive brain integration

def x_share_context_with_copilot__mutmut_10(context: Dict[str, Any], bridge: Optional[BridgeManager] = None) -> bool:
    """
    Share cognitive context with Copilot watcher via secure bridge.
    
    Args:
        context: Context data to share
        bridge: Optional bridge instance (creates default if not provided)
        
    Returns:
        True if context shared successfully
    """
    if bridge is None:
        bridge = BridgeManager()
    
    # Get auth token from environment for authentication (PS-02)
    auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
    
    message = ContextMessage(
        timestamp=datetime.now(UTC).isoformat(),
        source="cognitive_brain",
        message_type=None,
        context=context,
        metadata={"version": "1.0"},
        auth_token=auth_token
    )
    
    return bridge.write_message(message)


# Convenience functions for cognitive brain integration

def x_share_context_with_copilot__mutmut_11(context: Dict[str, Any], bridge: Optional[BridgeManager] = None) -> bool:
    """
    Share cognitive context with Copilot watcher via secure bridge.
    
    Args:
        context: Context data to share
        bridge: Optional bridge instance (creates default if not provided)
        
    Returns:
        True if context shared successfully
    """
    if bridge is None:
        bridge = BridgeManager()
    
    # Get auth token from environment for authentication (PS-02)
    auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
    
    message = ContextMessage(
        timestamp=datetime.now(UTC).isoformat(),
        source="cognitive_brain",
        message_type="context_update",
        context=None,
        metadata={"version": "1.0"},
        auth_token=auth_token
    )
    
    return bridge.write_message(message)


# Convenience functions for cognitive brain integration

def x_share_context_with_copilot__mutmut_12(context: Dict[str, Any], bridge: Optional[BridgeManager] = None) -> bool:
    """
    Share cognitive context with Copilot watcher via secure bridge.
    
    Args:
        context: Context data to share
        bridge: Optional bridge instance (creates default if not provided)
        
    Returns:
        True if context shared successfully
    """
    if bridge is None:
        bridge = BridgeManager()
    
    # Get auth token from environment for authentication (PS-02)
    auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
    
    message = ContextMessage(
        timestamp=datetime.now(UTC).isoformat(),
        source="cognitive_brain",
        message_type="context_update",
        context=context,
        metadata=None,
        auth_token=auth_token
    )
    
    return bridge.write_message(message)


# Convenience functions for cognitive brain integration

def x_share_context_with_copilot__mutmut_13(context: Dict[str, Any], bridge: Optional[BridgeManager] = None) -> bool:
    """
    Share cognitive context with Copilot watcher via secure bridge.
    
    Args:
        context: Context data to share
        bridge: Optional bridge instance (creates default if not provided)
        
    Returns:
        True if context shared successfully
    """
    if bridge is None:
        bridge = BridgeManager()
    
    # Get auth token from environment for authentication (PS-02)
    auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
    
    message = ContextMessage(
        timestamp=datetime.now(UTC).isoformat(),
        source="cognitive_brain",
        message_type="context_update",
        context=context,
        metadata={"version": "1.0"},
        auth_token=None
    )
    
    return bridge.write_message(message)


# Convenience functions for cognitive brain integration

def x_share_context_with_copilot__mutmut_14(context: Dict[str, Any], bridge: Optional[BridgeManager] = None) -> bool:
    """
    Share cognitive context with Copilot watcher via secure bridge.
    
    Args:
        context: Context data to share
        bridge: Optional bridge instance (creates default if not provided)
        
    Returns:
        True if context shared successfully
    """
    if bridge is None:
        bridge = BridgeManager()
    
    # Get auth token from environment for authentication (PS-02)
    auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
    
    message = ContextMessage(
        source="cognitive_brain",
        message_type="context_update",
        context=context,
        metadata={"version": "1.0"},
        auth_token=auth_token
    )
    
    return bridge.write_message(message)


# Convenience functions for cognitive brain integration

def x_share_context_with_copilot__mutmut_15(context: Dict[str, Any], bridge: Optional[BridgeManager] = None) -> bool:
    """
    Share cognitive context with Copilot watcher via secure bridge.
    
    Args:
        context: Context data to share
        bridge: Optional bridge instance (creates default if not provided)
        
    Returns:
        True if context shared successfully
    """
    if bridge is None:
        bridge = BridgeManager()
    
    # Get auth token from environment for authentication (PS-02)
    auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
    
    message = ContextMessage(
        timestamp=datetime.now(UTC).isoformat(),
        message_type="context_update",
        context=context,
        metadata={"version": "1.0"},
        auth_token=auth_token
    )
    
    return bridge.write_message(message)


# Convenience functions for cognitive brain integration

def x_share_context_with_copilot__mutmut_16(context: Dict[str, Any], bridge: Optional[BridgeManager] = None) -> bool:
    """
    Share cognitive context with Copilot watcher via secure bridge.
    
    Args:
        context: Context data to share
        bridge: Optional bridge instance (creates default if not provided)
        
    Returns:
        True if context shared successfully
    """
    if bridge is None:
        bridge = BridgeManager()
    
    # Get auth token from environment for authentication (PS-02)
    auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
    
    message = ContextMessage(
        timestamp=datetime.now(UTC).isoformat(),
        source="cognitive_brain",
        context=context,
        metadata={"version": "1.0"},
        auth_token=auth_token
    )
    
    return bridge.write_message(message)


# Convenience functions for cognitive brain integration

def x_share_context_with_copilot__mutmut_17(context: Dict[str, Any], bridge: Optional[BridgeManager] = None) -> bool:
    """
    Share cognitive context with Copilot watcher via secure bridge.
    
    Args:
        context: Context data to share
        bridge: Optional bridge instance (creates default if not provided)
        
    Returns:
        True if context shared successfully
    """
    if bridge is None:
        bridge = BridgeManager()
    
    # Get auth token from environment for authentication (PS-02)
    auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
    
    message = ContextMessage(
        timestamp=datetime.now(UTC).isoformat(),
        source="cognitive_brain",
        message_type="context_update",
        metadata={"version": "1.0"},
        auth_token=auth_token
    )
    
    return bridge.write_message(message)


# Convenience functions for cognitive brain integration

def x_share_context_with_copilot__mutmut_18(context: Dict[str, Any], bridge: Optional[BridgeManager] = None) -> bool:
    """
    Share cognitive context with Copilot watcher via secure bridge.
    
    Args:
        context: Context data to share
        bridge: Optional bridge instance (creates default if not provided)
        
    Returns:
        True if context shared successfully
    """
    if bridge is None:
        bridge = BridgeManager()
    
    # Get auth token from environment for authentication (PS-02)
    auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
    
    message = ContextMessage(
        timestamp=datetime.now(UTC).isoformat(),
        source="cognitive_brain",
        message_type="context_update",
        context=context,
        auth_token=auth_token
    )
    
    return bridge.write_message(message)


# Convenience functions for cognitive brain integration

def x_share_context_with_copilot__mutmut_19(context: Dict[str, Any], bridge: Optional[BridgeManager] = None) -> bool:
    """
    Share cognitive context with Copilot watcher via secure bridge.
    
    Args:
        context: Context data to share
        bridge: Optional bridge instance (creates default if not provided)
        
    Returns:
        True if context shared successfully
    """
    if bridge is None:
        bridge = BridgeManager()
    
    # Get auth token from environment for authentication (PS-02)
    auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
    
    message = ContextMessage(
        timestamp=datetime.now(UTC).isoformat(),
        source="cognitive_brain",
        message_type="context_update",
        context=context,
        metadata={"version": "1.0"},
        )
    
    return bridge.write_message(message)


# Convenience functions for cognitive brain integration

def x_share_context_with_copilot__mutmut_20(context: Dict[str, Any], bridge: Optional[BridgeManager] = None) -> bool:
    """
    Share cognitive context with Copilot watcher via secure bridge.
    
    Args:
        context: Context data to share
        bridge: Optional bridge instance (creates default if not provided)
        
    Returns:
        True if context shared successfully
    """
    if bridge is None:
        bridge = BridgeManager()
    
    # Get auth token from environment for authentication (PS-02)
    auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
    
    message = ContextMessage(
        timestamp=datetime.now(None).isoformat(),
        source="cognitive_brain",
        message_type="context_update",
        context=context,
        metadata={"version": "1.0"},
        auth_token=auth_token
    )
    
    return bridge.write_message(message)


# Convenience functions for cognitive brain integration

def x_share_context_with_copilot__mutmut_21(context: Dict[str, Any], bridge: Optional[BridgeManager] = None) -> bool:
    """
    Share cognitive context with Copilot watcher via secure bridge.
    
    Args:
        context: Context data to share
        bridge: Optional bridge instance (creates default if not provided)
        
    Returns:
        True if context shared successfully
    """
    if bridge is None:
        bridge = BridgeManager()
    
    # Get auth token from environment for authentication (PS-02)
    auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
    
    message = ContextMessage(
        timestamp=datetime.now(UTC).isoformat(),
        source="XXcognitive_brainXX",
        message_type="context_update",
        context=context,
        metadata={"version": "1.0"},
        auth_token=auth_token
    )
    
    return bridge.write_message(message)


# Convenience functions for cognitive brain integration

def x_share_context_with_copilot__mutmut_22(context: Dict[str, Any], bridge: Optional[BridgeManager] = None) -> bool:
    """
    Share cognitive context with Copilot watcher via secure bridge.
    
    Args:
        context: Context data to share
        bridge: Optional bridge instance (creates default if not provided)
        
    Returns:
        True if context shared successfully
    """
    if bridge is None:
        bridge = BridgeManager()
    
    # Get auth token from environment for authentication (PS-02)
    auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
    
    message = ContextMessage(
        timestamp=datetime.now(UTC).isoformat(),
        source="COGNITIVE_BRAIN",
        message_type="context_update",
        context=context,
        metadata={"version": "1.0"},
        auth_token=auth_token
    )
    
    return bridge.write_message(message)


# Convenience functions for cognitive brain integration

def x_share_context_with_copilot__mutmut_23(context: Dict[str, Any], bridge: Optional[BridgeManager] = None) -> bool:
    """
    Share cognitive context with Copilot watcher via secure bridge.
    
    Args:
        context: Context data to share
        bridge: Optional bridge instance (creates default if not provided)
        
    Returns:
        True if context shared successfully
    """
    if bridge is None:
        bridge = BridgeManager()
    
    # Get auth token from environment for authentication (PS-02)
    auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
    
    message = ContextMessage(
        timestamp=datetime.now(UTC).isoformat(),
        source="cognitive_brain",
        message_type="XXcontext_updateXX",
        context=context,
        metadata={"version": "1.0"},
        auth_token=auth_token
    )
    
    return bridge.write_message(message)


# Convenience functions for cognitive brain integration

def x_share_context_with_copilot__mutmut_24(context: Dict[str, Any], bridge: Optional[BridgeManager] = None) -> bool:
    """
    Share cognitive context with Copilot watcher via secure bridge.
    
    Args:
        context: Context data to share
        bridge: Optional bridge instance (creates default if not provided)
        
    Returns:
        True if context shared successfully
    """
    if bridge is None:
        bridge = BridgeManager()
    
    # Get auth token from environment for authentication (PS-02)
    auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
    
    message = ContextMessage(
        timestamp=datetime.now(UTC).isoformat(),
        source="cognitive_brain",
        message_type="CONTEXT_UPDATE",
        context=context,
        metadata={"version": "1.0"},
        auth_token=auth_token
    )
    
    return bridge.write_message(message)


# Convenience functions for cognitive brain integration

def x_share_context_with_copilot__mutmut_25(context: Dict[str, Any], bridge: Optional[BridgeManager] = None) -> bool:
    """
    Share cognitive context with Copilot watcher via secure bridge.
    
    Args:
        context: Context data to share
        bridge: Optional bridge instance (creates default if not provided)
        
    Returns:
        True if context shared successfully
    """
    if bridge is None:
        bridge = BridgeManager()
    
    # Get auth token from environment for authentication (PS-02)
    auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
    
    message = ContextMessage(
        timestamp=datetime.now(UTC).isoformat(),
        source="cognitive_brain",
        message_type="context_update",
        context=context,
        metadata={"XXversionXX": "1.0"},
        auth_token=auth_token
    )
    
    return bridge.write_message(message)


# Convenience functions for cognitive brain integration

def x_share_context_with_copilot__mutmut_26(context: Dict[str, Any], bridge: Optional[BridgeManager] = None) -> bool:
    """
    Share cognitive context with Copilot watcher via secure bridge.
    
    Args:
        context: Context data to share
        bridge: Optional bridge instance (creates default if not provided)
        
    Returns:
        True if context shared successfully
    """
    if bridge is None:
        bridge = BridgeManager()
    
    # Get auth token from environment for authentication (PS-02)
    auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
    
    message = ContextMessage(
        timestamp=datetime.now(UTC).isoformat(),
        source="cognitive_brain",
        message_type="context_update",
        context=context,
        metadata={"VERSION": "1.0"},
        auth_token=auth_token
    )
    
    return bridge.write_message(message)


# Convenience functions for cognitive brain integration

def x_share_context_with_copilot__mutmut_27(context: Dict[str, Any], bridge: Optional[BridgeManager] = None) -> bool:
    """
    Share cognitive context with Copilot watcher via secure bridge.
    
    Args:
        context: Context data to share
        bridge: Optional bridge instance (creates default if not provided)
        
    Returns:
        True if context shared successfully
    """
    if bridge is None:
        bridge = BridgeManager()
    
    # Get auth token from environment for authentication (PS-02)
    auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
    
    message = ContextMessage(
        timestamp=datetime.now(UTC).isoformat(),
        source="cognitive_brain",
        message_type="context_update",
        context=context,
        metadata={"version": "XX1.0XX"},
        auth_token=auth_token
    )
    
    return bridge.write_message(message)


# Convenience functions for cognitive brain integration

def x_share_context_with_copilot__mutmut_28(context: Dict[str, Any], bridge: Optional[BridgeManager] = None) -> bool:
    """
    Share cognitive context with Copilot watcher via secure bridge.
    
    Args:
        context: Context data to share
        bridge: Optional bridge instance (creates default if not provided)
        
    Returns:
        True if context shared successfully
    """
    if bridge is None:
        bridge = BridgeManager()
    
    # Get auth token from environment for authentication (PS-02)
    auth_token = os.getenv("CODEX_BRIDGE_TOKEN")
    
    message = ContextMessage(
        timestamp=datetime.now(UTC).isoformat(),
        source="cognitive_brain",
        message_type="context_update",
        context=context,
        metadata={"version": "1.0"},
        auth_token=auth_token
    )
    
    return bridge.write_message(None)

x_share_context_with_copilot__mutmut_mutants : ClassVar[MutantDict] = {
'x_share_context_with_copilot__mutmut_1': x_share_context_with_copilot__mutmut_1, 
    'x_share_context_with_copilot__mutmut_2': x_share_context_with_copilot__mutmut_2, 
    'x_share_context_with_copilot__mutmut_3': x_share_context_with_copilot__mutmut_3, 
    'x_share_context_with_copilot__mutmut_4': x_share_context_with_copilot__mutmut_4, 
    'x_share_context_with_copilot__mutmut_5': x_share_context_with_copilot__mutmut_5, 
    'x_share_context_with_copilot__mutmut_6': x_share_context_with_copilot__mutmut_6, 
    'x_share_context_with_copilot__mutmut_7': x_share_context_with_copilot__mutmut_7, 
    'x_share_context_with_copilot__mutmut_8': x_share_context_with_copilot__mutmut_8, 
    'x_share_context_with_copilot__mutmut_9': x_share_context_with_copilot__mutmut_9, 
    'x_share_context_with_copilot__mutmut_10': x_share_context_with_copilot__mutmut_10, 
    'x_share_context_with_copilot__mutmut_11': x_share_context_with_copilot__mutmut_11, 
    'x_share_context_with_copilot__mutmut_12': x_share_context_with_copilot__mutmut_12, 
    'x_share_context_with_copilot__mutmut_13': x_share_context_with_copilot__mutmut_13, 
    'x_share_context_with_copilot__mutmut_14': x_share_context_with_copilot__mutmut_14, 
    'x_share_context_with_copilot__mutmut_15': x_share_context_with_copilot__mutmut_15, 
    'x_share_context_with_copilot__mutmut_16': x_share_context_with_copilot__mutmut_16, 
    'x_share_context_with_copilot__mutmut_17': x_share_context_with_copilot__mutmut_17, 
    'x_share_context_with_copilot__mutmut_18': x_share_context_with_copilot__mutmut_18, 
    'x_share_context_with_copilot__mutmut_19': x_share_context_with_copilot__mutmut_19, 
    'x_share_context_with_copilot__mutmut_20': x_share_context_with_copilot__mutmut_20, 
    'x_share_context_with_copilot__mutmut_21': x_share_context_with_copilot__mutmut_21, 
    'x_share_context_with_copilot__mutmut_22': x_share_context_with_copilot__mutmut_22, 
    'x_share_context_with_copilot__mutmut_23': x_share_context_with_copilot__mutmut_23, 
    'x_share_context_with_copilot__mutmut_24': x_share_context_with_copilot__mutmut_24, 
    'x_share_context_with_copilot__mutmut_25': x_share_context_with_copilot__mutmut_25, 
    'x_share_context_with_copilot__mutmut_26': x_share_context_with_copilot__mutmut_26, 
    'x_share_context_with_copilot__mutmut_27': x_share_context_with_copilot__mutmut_27, 
    'x_share_context_with_copilot__mutmut_28': x_share_context_with_copilot__mutmut_28
}

def share_context_with_copilot(*args, **kwargs):
    result = _mutmut_trampoline(x_share_context_with_copilot__mutmut_orig, x_share_context_with_copilot__mutmut_mutants, args, kwargs)
    return result 

share_context_with_copilot.__signature__ = _mutmut_signature(x_share_context_with_copilot__mutmut_orig)
x_share_context_with_copilot__mutmut_orig.__name__ = 'x_share_context_with_copilot'
