"""Extended trainer with evaluation, gradient accumulation, and checkpointing."""

from __future__ import annotations

import contextlib
import inspect
import json
import logging
logger = logging.getLogger(__name__)
import time
from collections.abc import Callable, Mapping, MutableMapping, Sequence
from dataclasses import dataclass, field
from pathlib import Path
from typing import Any

try:  # pragma: no cover - optional torch guard for import-time failures
    from torch.cuda.amp import GradScaler, autocast
    from torch.utils.data import DataLoader

    import torch
    from torch import nn
except Exception:  # pragma: no cover - propagate a consistent runtime error lazily
    torch = None  # type: ignore[assignment]
    nn = Any  # type: ignore[assignment]
    GradScaler = None  # type: ignore[assignment]
    autocast = None  # type: ignore[assignment]
    DataLoader = Any  # type: ignore[assignment]

if torch is not None:  # pragma: no cover - typing bridge
    TensorType = torch.Tensor
    OptimizerType = torch.optim.Optimizer
    DataLoaderType = DataLoader
else:  # pragma: no cover - fallback types
    TensorType = Any
    OptimizerType = Any
    DataLoaderType = Any

from codex_ml.utils.repro import set_seed as _set_seed
from logging_utils import (
    LoggingConfig,
    LoggingSession,
    log_metrics,
    setup_logging,
    shutdown_logging,
)
from metrics import append_ndjson

from .checkpointing import load_checkpoint
from .simple_trainer import SimpleTrainer

if torch is not None:
    try:
        _TORCH_SUPPORTS_WEIGHTS_ONLY = "weights_only" in inspect.signature(torch.load).parameters
    except (TypeError, ValueError):  # pragma: no cover - torch may bypass inspect
        _TORCH_SUPPORTS_WEIGHTS_ONLY = False
    _TORCH_LOAD_FN = getattr(torch, "load", None)
else:
    _TORCH_SUPPORTS_WEIGHTS_ONLY = False
    _TORCH_LOAD_FN = None
from inspect import signature as _mutmut_signature
from typing import Annotated
from typing import Callable
from typing import ClassVar


MutantDict = Annotated[dict[str, Callable], "Mutant"]


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg = None):
    """Forward call to original or mutated function, depending on the environment"""
    import os
    mutant_under_test = os.environ['MUTANT_UNDER_TEST']
    if mutant_under_test == 'fail':
        from mutmut.__main__ import MutmutProgrammaticFailException
        raise MutmutProgrammaticFailException('Failed programmatically')      
    elif mutant_under_test == 'stats':
        from mutmut.__main__ import record_trampoline_hit
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__)
        result = orig(*call_args, **call_kwargs)
        return result
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_'
    if not mutant_under_test.startswith(prefix):
        result = orig(*call_args, **call_kwargs)
        return result
    mutant_name = mutant_under_test.rpartition('.')[-1]
    if self_arg is not None:
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs)
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs)
    return result


def x__load_checkpoint_payload__mutmut_orig(path: Path, *, map_location: Any) -> Mapping[str, Any]:
    if torch is None or _TORCH_LOAD_FN is None:
        raise RuntimeError("torch is required to load checkpoints")
    kwargs: dict[str, Any] = {}
    if map_location is not None:
        kwargs["map_location"] = map_location
    if _TORCH_SUPPORTS_WEIGHTS_ONLY:
        kwargs["weights_only"] = False
    try:
        result = _TORCH_LOAD_FN(path, **kwargs)
    except TypeError as exc:
        logger.debug(f"TypeError: {exc}")
        if _TORCH_SUPPORTS_WEIGHTS_ONLY and "weights_only" in str(exc):
            kwargs.pop("weights_only", None)
            result = _TORCH_LOAD_FN(path, **kwargs)
        else:
            raise
    if not isinstance(result, Mapping):
        return {}
    return result


def x__load_checkpoint_payload__mutmut_1(path: Path, *, map_location: Any) -> Mapping[str, Any]:
    if torch is None and _TORCH_LOAD_FN is None:
        raise RuntimeError("torch is required to load checkpoints")
    kwargs: dict[str, Any] = {}
    if map_location is not None:
        kwargs["map_location"] = map_location
    if _TORCH_SUPPORTS_WEIGHTS_ONLY:
        kwargs["weights_only"] = False
    try:
        result = _TORCH_LOAD_FN(path, **kwargs)
    except TypeError as exc:
        logger.debug(f"TypeError: {exc}")
        if _TORCH_SUPPORTS_WEIGHTS_ONLY and "weights_only" in str(exc):
            kwargs.pop("weights_only", None)
            result = _TORCH_LOAD_FN(path, **kwargs)
        else:
            raise
    if not isinstance(result, Mapping):
        return {}
    return result


def x__load_checkpoint_payload__mutmut_2(path: Path, *, map_location: Any) -> Mapping[str, Any]:
    if torch is not None or _TORCH_LOAD_FN is None:
        raise RuntimeError("torch is required to load checkpoints")
    kwargs: dict[str, Any] = {}
    if map_location is not None:
        kwargs["map_location"] = map_location
    if _TORCH_SUPPORTS_WEIGHTS_ONLY:
        kwargs["weights_only"] = False
    try:
        result = _TORCH_LOAD_FN(path, **kwargs)
    except TypeError as exc:
        logger.debug(f"TypeError: {exc}")
        if _TORCH_SUPPORTS_WEIGHTS_ONLY and "weights_only" in str(exc):
            kwargs.pop("weights_only", None)
            result = _TORCH_LOAD_FN(path, **kwargs)
        else:
            raise
    if not isinstance(result, Mapping):
        return {}
    return result


def x__load_checkpoint_payload__mutmut_3(path: Path, *, map_location: Any) -> Mapping[str, Any]:
    if torch is None or _TORCH_LOAD_FN is not None:
        raise RuntimeError("torch is required to load checkpoints")
    kwargs: dict[str, Any] = {}
    if map_location is not None:
        kwargs["map_location"] = map_location
    if _TORCH_SUPPORTS_WEIGHTS_ONLY:
        kwargs["weights_only"] = False
    try:
        result = _TORCH_LOAD_FN(path, **kwargs)
    except TypeError as exc:
        logger.debug(f"TypeError: {exc}")
        if _TORCH_SUPPORTS_WEIGHTS_ONLY and "weights_only" in str(exc):
            kwargs.pop("weights_only", None)
            result = _TORCH_LOAD_FN(path, **kwargs)
        else:
            raise
    if not isinstance(result, Mapping):
        return {}
    return result


def x__load_checkpoint_payload__mutmut_4(path: Path, *, map_location: Any) -> Mapping[str, Any]:
    if torch is None or _TORCH_LOAD_FN is None:
        raise RuntimeError(None)
    kwargs: dict[str, Any] = {}
    if map_location is not None:
        kwargs["map_location"] = map_location
    if _TORCH_SUPPORTS_WEIGHTS_ONLY:
        kwargs["weights_only"] = False
    try:
        result = _TORCH_LOAD_FN(path, **kwargs)
    except TypeError as exc:
        logger.debug(f"TypeError: {exc}")
        if _TORCH_SUPPORTS_WEIGHTS_ONLY and "weights_only" in str(exc):
            kwargs.pop("weights_only", None)
            result = _TORCH_LOAD_FN(path, **kwargs)
        else:
            raise
    if not isinstance(result, Mapping):
        return {}
    return result


def x__load_checkpoint_payload__mutmut_5(path: Path, *, map_location: Any) -> Mapping[str, Any]:
    if torch is None or _TORCH_LOAD_FN is None:
        raise RuntimeError("XXtorch is required to load checkpointsXX")
    kwargs: dict[str, Any] = {}
    if map_location is not None:
        kwargs["map_location"] = map_location
    if _TORCH_SUPPORTS_WEIGHTS_ONLY:
        kwargs["weights_only"] = False
    try:
        result = _TORCH_LOAD_FN(path, **kwargs)
    except TypeError as exc:
        logger.debug(f"TypeError: {exc}")
        if _TORCH_SUPPORTS_WEIGHTS_ONLY and "weights_only" in str(exc):
            kwargs.pop("weights_only", None)
            result = _TORCH_LOAD_FN(path, **kwargs)
        else:
            raise
    if not isinstance(result, Mapping):
        return {}
    return result


def x__load_checkpoint_payload__mutmut_6(path: Path, *, map_location: Any) -> Mapping[str, Any]:
    if torch is None or _TORCH_LOAD_FN is None:
        raise RuntimeError("TORCH IS REQUIRED TO LOAD CHECKPOINTS")
    kwargs: dict[str, Any] = {}
    if map_location is not None:
        kwargs["map_location"] = map_location
    if _TORCH_SUPPORTS_WEIGHTS_ONLY:
        kwargs["weights_only"] = False
    try:
        result = _TORCH_LOAD_FN(path, **kwargs)
    except TypeError as exc:
        logger.debug(f"TypeError: {exc}")
        if _TORCH_SUPPORTS_WEIGHTS_ONLY and "weights_only" in str(exc):
            kwargs.pop("weights_only", None)
            result = _TORCH_LOAD_FN(path, **kwargs)
        else:
            raise
    if not isinstance(result, Mapping):
        return {}
    return result


def x__load_checkpoint_payload__mutmut_7(path: Path, *, map_location: Any) -> Mapping[str, Any]:
    if torch is None or _TORCH_LOAD_FN is None:
        raise RuntimeError("torch is required to load checkpoints")
    kwargs: dict[str, Any] = None
    if map_location is not None:
        kwargs["map_location"] = map_location
    if _TORCH_SUPPORTS_WEIGHTS_ONLY:
        kwargs["weights_only"] = False
    try:
        result = _TORCH_LOAD_FN(path, **kwargs)
    except TypeError as exc:
        logger.debug(f"TypeError: {exc}")
        if _TORCH_SUPPORTS_WEIGHTS_ONLY and "weights_only" in str(exc):
            kwargs.pop("weights_only", None)
            result = _TORCH_LOAD_FN(path, **kwargs)
        else:
            raise
    if not isinstance(result, Mapping):
        return {}
    return result


def x__load_checkpoint_payload__mutmut_8(path: Path, *, map_location: Any) -> Mapping[str, Any]:
    if torch is None or _TORCH_LOAD_FN is None:
        raise RuntimeError("torch is required to load checkpoints")
    kwargs: dict[str, Any] = {}
    if map_location is None:
        kwargs["map_location"] = map_location
    if _TORCH_SUPPORTS_WEIGHTS_ONLY:
        kwargs["weights_only"] = False
    try:
        result = _TORCH_LOAD_FN(path, **kwargs)
    except TypeError as exc:
        logger.debug(f"TypeError: {exc}")
        if _TORCH_SUPPORTS_WEIGHTS_ONLY and "weights_only" in str(exc):
            kwargs.pop("weights_only", None)
            result = _TORCH_LOAD_FN(path, **kwargs)
        else:
            raise
    if not isinstance(result, Mapping):
        return {}
    return result


def x__load_checkpoint_payload__mutmut_9(path: Path, *, map_location: Any) -> Mapping[str, Any]:
    if torch is None or _TORCH_LOAD_FN is None:
        raise RuntimeError("torch is required to load checkpoints")
    kwargs: dict[str, Any] = {}
    if map_location is not None:
        kwargs["map_location"] = None
    if _TORCH_SUPPORTS_WEIGHTS_ONLY:
        kwargs["weights_only"] = False
    try:
        result = _TORCH_LOAD_FN(path, **kwargs)
    except TypeError as exc:
        logger.debug(f"TypeError: {exc}")
        if _TORCH_SUPPORTS_WEIGHTS_ONLY and "weights_only" in str(exc):
            kwargs.pop("weights_only", None)
            result = _TORCH_LOAD_FN(path, **kwargs)
        else:
            raise
    if not isinstance(result, Mapping):
        return {}
    return result


def x__load_checkpoint_payload__mutmut_10(path: Path, *, map_location: Any) -> Mapping[str, Any]:
    if torch is None or _TORCH_LOAD_FN is None:
        raise RuntimeError("torch is required to load checkpoints")
    kwargs: dict[str, Any] = {}
    if map_location is not None:
        kwargs["XXmap_locationXX"] = map_location
    if _TORCH_SUPPORTS_WEIGHTS_ONLY:
        kwargs["weights_only"] = False
    try:
        result = _TORCH_LOAD_FN(path, **kwargs)
    except TypeError as exc:
        logger.debug(f"TypeError: {exc}")
        if _TORCH_SUPPORTS_WEIGHTS_ONLY and "weights_only" in str(exc):
            kwargs.pop("weights_only", None)
            result = _TORCH_LOAD_FN(path, **kwargs)
        else:
            raise
    if not isinstance(result, Mapping):
        return {}
    return result


def x__load_checkpoint_payload__mutmut_11(path: Path, *, map_location: Any) -> Mapping[str, Any]:
    if torch is None or _TORCH_LOAD_FN is None:
        raise RuntimeError("torch is required to load checkpoints")
    kwargs: dict[str, Any] = {}
    if map_location is not None:
        kwargs["MAP_LOCATION"] = map_location
    if _TORCH_SUPPORTS_WEIGHTS_ONLY:
        kwargs["weights_only"] = False
    try:
        result = _TORCH_LOAD_FN(path, **kwargs)
    except TypeError as exc:
        logger.debug(f"TypeError: {exc}")
        if _TORCH_SUPPORTS_WEIGHTS_ONLY and "weights_only" in str(exc):
            kwargs.pop("weights_only", None)
            result = _TORCH_LOAD_FN(path, **kwargs)
        else:
            raise
    if not isinstance(result, Mapping):
        return {}
    return result


def x__load_checkpoint_payload__mutmut_12(path: Path, *, map_location: Any) -> Mapping[str, Any]:
    if torch is None or _TORCH_LOAD_FN is None:
        raise RuntimeError("torch is required to load checkpoints")
    kwargs: dict[str, Any] = {}
    if map_location is not None:
        kwargs["map_location"] = map_location
    if _TORCH_SUPPORTS_WEIGHTS_ONLY:
        kwargs["weights_only"] = None
    try:
        result = _TORCH_LOAD_FN(path, **kwargs)
    except TypeError as exc:
        logger.debug(f"TypeError: {exc}")
        if _TORCH_SUPPORTS_WEIGHTS_ONLY and "weights_only" in str(exc):
            kwargs.pop("weights_only", None)
            result = _TORCH_LOAD_FN(path, **kwargs)
        else:
            raise
    if not isinstance(result, Mapping):
        return {}
    return result


def x__load_checkpoint_payload__mutmut_13(path: Path, *, map_location: Any) -> Mapping[str, Any]:
    if torch is None or _TORCH_LOAD_FN is None:
        raise RuntimeError("torch is required to load checkpoints")
    kwargs: dict[str, Any] = {}
    if map_location is not None:
        kwargs["map_location"] = map_location
    if _TORCH_SUPPORTS_WEIGHTS_ONLY:
        kwargs["XXweights_onlyXX"] = False
    try:
        result = _TORCH_LOAD_FN(path, **kwargs)
    except TypeError as exc:
        logger.debug(f"TypeError: {exc}")
        if _TORCH_SUPPORTS_WEIGHTS_ONLY and "weights_only" in str(exc):
            kwargs.pop("weights_only", None)
            result = _TORCH_LOAD_FN(path, **kwargs)
        else:
            raise
    if not isinstance(result, Mapping):
        return {}
    return result


def x__load_checkpoint_payload__mutmut_14(path: Path, *, map_location: Any) -> Mapping[str, Any]:
    if torch is None or _TORCH_LOAD_FN is None:
        raise RuntimeError("torch is required to load checkpoints")
    kwargs: dict[str, Any] = {}
    if map_location is not None:
        kwargs["map_location"] = map_location
    if _TORCH_SUPPORTS_WEIGHTS_ONLY:
        kwargs["WEIGHTS_ONLY"] = False
    try:
        result = _TORCH_LOAD_FN(path, **kwargs)
    except TypeError as exc:
        logger.debug(f"TypeError: {exc}")
        if _TORCH_SUPPORTS_WEIGHTS_ONLY and "weights_only" in str(exc):
            kwargs.pop("weights_only", None)
            result = _TORCH_LOAD_FN(path, **kwargs)
        else:
            raise
    if not isinstance(result, Mapping):
        return {}
    return result


def x__load_checkpoint_payload__mutmut_15(path: Path, *, map_location: Any) -> Mapping[str, Any]:
    if torch is None or _TORCH_LOAD_FN is None:
        raise RuntimeError("torch is required to load checkpoints")
    kwargs: dict[str, Any] = {}
    if map_location is not None:
        kwargs["map_location"] = map_location
    if _TORCH_SUPPORTS_WEIGHTS_ONLY:
        kwargs["weights_only"] = True
    try:
        result = _TORCH_LOAD_FN(path, **kwargs)
    except TypeError as exc:
        logger.debug(f"TypeError: {exc}")
        if _TORCH_SUPPORTS_WEIGHTS_ONLY and "weights_only" in str(exc):
            kwargs.pop("weights_only", None)
            result = _TORCH_LOAD_FN(path, **kwargs)
        else:
            raise
    if not isinstance(result, Mapping):
        return {}
    return result


def x__load_checkpoint_payload__mutmut_16(path: Path, *, map_location: Any) -> Mapping[str, Any]:
    if torch is None or _TORCH_LOAD_FN is None:
        raise RuntimeError("torch is required to load checkpoints")
    kwargs: dict[str, Any] = {}
    if map_location is not None:
        kwargs["map_location"] = map_location
    if _TORCH_SUPPORTS_WEIGHTS_ONLY:
        kwargs["weights_only"] = False
    try:
        result = None
    except TypeError as exc:
        logger.debug(f"TypeError: {exc}")
        if _TORCH_SUPPORTS_WEIGHTS_ONLY and "weights_only" in str(exc):
            kwargs.pop("weights_only", None)
            result = _TORCH_LOAD_FN(path, **kwargs)
        else:
            raise
    if not isinstance(result, Mapping):
        return {}
    return result


def x__load_checkpoint_payload__mutmut_17(path: Path, *, map_location: Any) -> Mapping[str, Any]:
    if torch is None or _TORCH_LOAD_FN is None:
        raise RuntimeError("torch is required to load checkpoints")
    kwargs: dict[str, Any] = {}
    if map_location is not None:
        kwargs["map_location"] = map_location
    if _TORCH_SUPPORTS_WEIGHTS_ONLY:
        kwargs["weights_only"] = False
    try:
        result = _TORCH_LOAD_FN(None, **kwargs)
    except TypeError as exc:
        logger.debug(f"TypeError: {exc}")
        if _TORCH_SUPPORTS_WEIGHTS_ONLY and "weights_only" in str(exc):
            kwargs.pop("weights_only", None)
            result = _TORCH_LOAD_FN(path, **kwargs)
        else:
            raise
    if not isinstance(result, Mapping):
        return {}
    return result


def x__load_checkpoint_payload__mutmut_18(path: Path, *, map_location: Any) -> Mapping[str, Any]:
    if torch is None or _TORCH_LOAD_FN is None:
        raise RuntimeError("torch is required to load checkpoints")
    kwargs: dict[str, Any] = {}
    if map_location is not None:
        kwargs["map_location"] = map_location
    if _TORCH_SUPPORTS_WEIGHTS_ONLY:
        kwargs["weights_only"] = False
    try:
        result = _TORCH_LOAD_FN(**kwargs)
    except TypeError as exc:
        logger.debug(f"TypeError: {exc}")
        if _TORCH_SUPPORTS_WEIGHTS_ONLY and "weights_only" in str(exc):
            kwargs.pop("weights_only", None)
            result = _TORCH_LOAD_FN(path, **kwargs)
        else:
            raise
    if not isinstance(result, Mapping):
        return {}
    return result


def x__load_checkpoint_payload__mutmut_19(path: Path, *, map_location: Any) -> Mapping[str, Any]:
    if torch is None or _TORCH_LOAD_FN is None:
        raise RuntimeError("torch is required to load checkpoints")
    kwargs: dict[str, Any] = {}
    if map_location is not None:
        kwargs["map_location"] = map_location
    if _TORCH_SUPPORTS_WEIGHTS_ONLY:
        kwargs["weights_only"] = False
    try:
        result = _TORCH_LOAD_FN(path, )
    except TypeError as exc:
        logger.debug(f"TypeError: {exc}")
        if _TORCH_SUPPORTS_WEIGHTS_ONLY and "weights_only" in str(exc):
            kwargs.pop("weights_only", None)
            result = _TORCH_LOAD_FN(path, **kwargs)
        else:
            raise
    if not isinstance(result, Mapping):
        return {}
    return result


def x__load_checkpoint_payload__mutmut_20(path: Path, *, map_location: Any) -> Mapping[str, Any]:
    if torch is None or _TORCH_LOAD_FN is None:
        raise RuntimeError("torch is required to load checkpoints")
    kwargs: dict[str, Any] = {}
    if map_location is not None:
        kwargs["map_location"] = map_location
    if _TORCH_SUPPORTS_WEIGHTS_ONLY:
        kwargs["weights_only"] = False
    try:
        result = _TORCH_LOAD_FN(path, **kwargs)
    except TypeError as exc:
        logger.debug(None)
        if _TORCH_SUPPORTS_WEIGHTS_ONLY and "weights_only" in str(exc):
            kwargs.pop("weights_only", None)
            result = _TORCH_LOAD_FN(path, **kwargs)
        else:
            raise
    if not isinstance(result, Mapping):
        return {}
    return result


def x__load_checkpoint_payload__mutmut_21(path: Path, *, map_location: Any) -> Mapping[str, Any]:
    if torch is None or _TORCH_LOAD_FN is None:
        raise RuntimeError("torch is required to load checkpoints")
    kwargs: dict[str, Any] = {}
    if map_location is not None:
        kwargs["map_location"] = map_location
    if _TORCH_SUPPORTS_WEIGHTS_ONLY:
        kwargs["weights_only"] = False
    try:
        result = _TORCH_LOAD_FN(path, **kwargs)
    except TypeError as exc:
        logger.debug(f"TypeError: {exc}")
        if _TORCH_SUPPORTS_WEIGHTS_ONLY or "weights_only" in str(exc):
            kwargs.pop("weights_only", None)
            result = _TORCH_LOAD_FN(path, **kwargs)
        else:
            raise
    if not isinstance(result, Mapping):
        return {}
    return result


def x__load_checkpoint_payload__mutmut_22(path: Path, *, map_location: Any) -> Mapping[str, Any]:
    if torch is None or _TORCH_LOAD_FN is None:
        raise RuntimeError("torch is required to load checkpoints")
    kwargs: dict[str, Any] = {}
    if map_location is not None:
        kwargs["map_location"] = map_location
    if _TORCH_SUPPORTS_WEIGHTS_ONLY:
        kwargs["weights_only"] = False
    try:
        result = _TORCH_LOAD_FN(path, **kwargs)
    except TypeError as exc:
        logger.debug(f"TypeError: {exc}")
        if _TORCH_SUPPORTS_WEIGHTS_ONLY and "XXweights_onlyXX" in str(exc):
            kwargs.pop("weights_only", None)
            result = _TORCH_LOAD_FN(path, **kwargs)
        else:
            raise
    if not isinstance(result, Mapping):
        return {}
    return result


def x__load_checkpoint_payload__mutmut_23(path: Path, *, map_location: Any) -> Mapping[str, Any]:
    if torch is None or _TORCH_LOAD_FN is None:
        raise RuntimeError("torch is required to load checkpoints")
    kwargs: dict[str, Any] = {}
    if map_location is not None:
        kwargs["map_location"] = map_location
    if _TORCH_SUPPORTS_WEIGHTS_ONLY:
        kwargs["weights_only"] = False
    try:
        result = _TORCH_LOAD_FN(path, **kwargs)
    except TypeError as exc:
        logger.debug(f"TypeError: {exc}")
        if _TORCH_SUPPORTS_WEIGHTS_ONLY and "WEIGHTS_ONLY" in str(exc):
            kwargs.pop("weights_only", None)
            result = _TORCH_LOAD_FN(path, **kwargs)
        else:
            raise
    if not isinstance(result, Mapping):
        return {}
    return result


def x__load_checkpoint_payload__mutmut_24(path: Path, *, map_location: Any) -> Mapping[str, Any]:
    if torch is None or _TORCH_LOAD_FN is None:
        raise RuntimeError("torch is required to load checkpoints")
    kwargs: dict[str, Any] = {}
    if map_location is not None:
        kwargs["map_location"] = map_location
    if _TORCH_SUPPORTS_WEIGHTS_ONLY:
        kwargs["weights_only"] = False
    try:
        result = _TORCH_LOAD_FN(path, **kwargs)
    except TypeError as exc:
        logger.debug(f"TypeError: {exc}")
        if _TORCH_SUPPORTS_WEIGHTS_ONLY and "weights_only" not in str(exc):
            kwargs.pop("weights_only", None)
            result = _TORCH_LOAD_FN(path, **kwargs)
        else:
            raise
    if not isinstance(result, Mapping):
        return {}
    return result


def x__load_checkpoint_payload__mutmut_25(path: Path, *, map_location: Any) -> Mapping[str, Any]:
    if torch is None or _TORCH_LOAD_FN is None:
        raise RuntimeError("torch is required to load checkpoints")
    kwargs: dict[str, Any] = {}
    if map_location is not None:
        kwargs["map_location"] = map_location
    if _TORCH_SUPPORTS_WEIGHTS_ONLY:
        kwargs["weights_only"] = False
    try:
        result = _TORCH_LOAD_FN(path, **kwargs)
    except TypeError as exc:
        logger.debug(f"TypeError: {exc}")
        if _TORCH_SUPPORTS_WEIGHTS_ONLY and "weights_only" in str(None):
            kwargs.pop("weights_only", None)
            result = _TORCH_LOAD_FN(path, **kwargs)
        else:
            raise
    if not isinstance(result, Mapping):
        return {}
    return result


def x__load_checkpoint_payload__mutmut_26(path: Path, *, map_location: Any) -> Mapping[str, Any]:
    if torch is None or _TORCH_LOAD_FN is None:
        raise RuntimeError("torch is required to load checkpoints")
    kwargs: dict[str, Any] = {}
    if map_location is not None:
        kwargs["map_location"] = map_location
    if _TORCH_SUPPORTS_WEIGHTS_ONLY:
        kwargs["weights_only"] = False
    try:
        result = _TORCH_LOAD_FN(path, **kwargs)
    except TypeError as exc:
        logger.debug(f"TypeError: {exc}")
        if _TORCH_SUPPORTS_WEIGHTS_ONLY and "weights_only" in str(exc):
            kwargs.pop(None, None)
            result = _TORCH_LOAD_FN(path, **kwargs)
        else:
            raise
    if not isinstance(result, Mapping):
        return {}
    return result


def x__load_checkpoint_payload__mutmut_27(path: Path, *, map_location: Any) -> Mapping[str, Any]:
    if torch is None or _TORCH_LOAD_FN is None:
        raise RuntimeError("torch is required to load checkpoints")
    kwargs: dict[str, Any] = {}
    if map_location is not None:
        kwargs["map_location"] = map_location
    if _TORCH_SUPPORTS_WEIGHTS_ONLY:
        kwargs["weights_only"] = False
    try:
        result = _TORCH_LOAD_FN(path, **kwargs)
    except TypeError as exc:
        logger.debug(f"TypeError: {exc}")
        if _TORCH_SUPPORTS_WEIGHTS_ONLY and "weights_only" in str(exc):
            kwargs.pop(None)
            result = _TORCH_LOAD_FN(path, **kwargs)
        else:
            raise
    if not isinstance(result, Mapping):
        return {}
    return result


def x__load_checkpoint_payload__mutmut_28(path: Path, *, map_location: Any) -> Mapping[str, Any]:
    if torch is None or _TORCH_LOAD_FN is None:
        raise RuntimeError("torch is required to load checkpoints")
    kwargs: dict[str, Any] = {}
    if map_location is not None:
        kwargs["map_location"] = map_location
    if _TORCH_SUPPORTS_WEIGHTS_ONLY:
        kwargs["weights_only"] = False
    try:
        result = _TORCH_LOAD_FN(path, **kwargs)
    except TypeError as exc:
        logger.debug(f"TypeError: {exc}")
        if _TORCH_SUPPORTS_WEIGHTS_ONLY and "weights_only" in str(exc):
            kwargs.pop("weights_only", )
            result = _TORCH_LOAD_FN(path, **kwargs)
        else:
            raise
    if not isinstance(result, Mapping):
        return {}
    return result


def x__load_checkpoint_payload__mutmut_29(path: Path, *, map_location: Any) -> Mapping[str, Any]:
    if torch is None or _TORCH_LOAD_FN is None:
        raise RuntimeError("torch is required to load checkpoints")
    kwargs: dict[str, Any] = {}
    if map_location is not None:
        kwargs["map_location"] = map_location
    if _TORCH_SUPPORTS_WEIGHTS_ONLY:
        kwargs["weights_only"] = False
    try:
        result = _TORCH_LOAD_FN(path, **kwargs)
    except TypeError as exc:
        logger.debug(f"TypeError: {exc}")
        if _TORCH_SUPPORTS_WEIGHTS_ONLY and "weights_only" in str(exc):
            kwargs.pop("XXweights_onlyXX", None)
            result = _TORCH_LOAD_FN(path, **kwargs)
        else:
            raise
    if not isinstance(result, Mapping):
        return {}
    return result


def x__load_checkpoint_payload__mutmut_30(path: Path, *, map_location: Any) -> Mapping[str, Any]:
    if torch is None or _TORCH_LOAD_FN is None:
        raise RuntimeError("torch is required to load checkpoints")
    kwargs: dict[str, Any] = {}
    if map_location is not None:
        kwargs["map_location"] = map_location
    if _TORCH_SUPPORTS_WEIGHTS_ONLY:
        kwargs["weights_only"] = False
    try:
        result = _TORCH_LOAD_FN(path, **kwargs)
    except TypeError as exc:
        logger.debug(f"TypeError: {exc}")
        if _TORCH_SUPPORTS_WEIGHTS_ONLY and "weights_only" in str(exc):
            kwargs.pop("WEIGHTS_ONLY", None)
            result = _TORCH_LOAD_FN(path, **kwargs)
        else:
            raise
    if not isinstance(result, Mapping):
        return {}
    return result


def x__load_checkpoint_payload__mutmut_31(path: Path, *, map_location: Any) -> Mapping[str, Any]:
    if torch is None or _TORCH_LOAD_FN is None:
        raise RuntimeError("torch is required to load checkpoints")
    kwargs: dict[str, Any] = {}
    if map_location is not None:
        kwargs["map_location"] = map_location
    if _TORCH_SUPPORTS_WEIGHTS_ONLY:
        kwargs["weights_only"] = False
    try:
        result = _TORCH_LOAD_FN(path, **kwargs)
    except TypeError as exc:
        logger.debug(f"TypeError: {exc}")
        if _TORCH_SUPPORTS_WEIGHTS_ONLY and "weights_only" in str(exc):
            kwargs.pop("weights_only", None)
            result = None
        else:
            raise
    if not isinstance(result, Mapping):
        return {}
    return result


def x__load_checkpoint_payload__mutmut_32(path: Path, *, map_location: Any) -> Mapping[str, Any]:
    if torch is None or _TORCH_LOAD_FN is None:
        raise RuntimeError("torch is required to load checkpoints")
    kwargs: dict[str, Any] = {}
    if map_location is not None:
        kwargs["map_location"] = map_location
    if _TORCH_SUPPORTS_WEIGHTS_ONLY:
        kwargs["weights_only"] = False
    try:
        result = _TORCH_LOAD_FN(path, **kwargs)
    except TypeError as exc:
        logger.debug(f"TypeError: {exc}")
        if _TORCH_SUPPORTS_WEIGHTS_ONLY and "weights_only" in str(exc):
            kwargs.pop("weights_only", None)
            result = _TORCH_LOAD_FN(None, **kwargs)
        else:
            raise
    if not isinstance(result, Mapping):
        return {}
    return result


def x__load_checkpoint_payload__mutmut_33(path: Path, *, map_location: Any) -> Mapping[str, Any]:
    if torch is None or _TORCH_LOAD_FN is None:
        raise RuntimeError("torch is required to load checkpoints")
    kwargs: dict[str, Any] = {}
    if map_location is not None:
        kwargs["map_location"] = map_location
    if _TORCH_SUPPORTS_WEIGHTS_ONLY:
        kwargs["weights_only"] = False
    try:
        result = _TORCH_LOAD_FN(path, **kwargs)
    except TypeError as exc:
        logger.debug(f"TypeError: {exc}")
        if _TORCH_SUPPORTS_WEIGHTS_ONLY and "weights_only" in str(exc):
            kwargs.pop("weights_only", None)
            result = _TORCH_LOAD_FN(**kwargs)
        else:
            raise
    if not isinstance(result, Mapping):
        return {}
    return result


def x__load_checkpoint_payload__mutmut_34(path: Path, *, map_location: Any) -> Mapping[str, Any]:
    if torch is None or _TORCH_LOAD_FN is None:
        raise RuntimeError("torch is required to load checkpoints")
    kwargs: dict[str, Any] = {}
    if map_location is not None:
        kwargs["map_location"] = map_location
    if _TORCH_SUPPORTS_WEIGHTS_ONLY:
        kwargs["weights_only"] = False
    try:
        result = _TORCH_LOAD_FN(path, **kwargs)
    except TypeError as exc:
        logger.debug(f"TypeError: {exc}")
        if _TORCH_SUPPORTS_WEIGHTS_ONLY and "weights_only" in str(exc):
            kwargs.pop("weights_only", None)
            result = _TORCH_LOAD_FN(path, )
        else:
            raise
    if not isinstance(result, Mapping):
        return {}
    return result


def x__load_checkpoint_payload__mutmut_35(path: Path, *, map_location: Any) -> Mapping[str, Any]:
    if torch is None or _TORCH_LOAD_FN is None:
        raise RuntimeError("torch is required to load checkpoints")
    kwargs: dict[str, Any] = {}
    if map_location is not None:
        kwargs["map_location"] = map_location
    if _TORCH_SUPPORTS_WEIGHTS_ONLY:
        kwargs["weights_only"] = False
    try:
        result = _TORCH_LOAD_FN(path, **kwargs)
    except TypeError as exc:
        logger.debug(f"TypeError: {exc}")
        if _TORCH_SUPPORTS_WEIGHTS_ONLY and "weights_only" in str(exc):
            kwargs.pop("weights_only", None)
            result = _TORCH_LOAD_FN(path, **kwargs)
        else:
            raise
    if isinstance(result, Mapping):
        return {}
    return result

x__load_checkpoint_payload__mutmut_mutants : ClassVar[MutantDict] = {
'x__load_checkpoint_payload__mutmut_1': x__load_checkpoint_payload__mutmut_1, 
    'x__load_checkpoint_payload__mutmut_2': x__load_checkpoint_payload__mutmut_2, 
    'x__load_checkpoint_payload__mutmut_3': x__load_checkpoint_payload__mutmut_3, 
    'x__load_checkpoint_payload__mutmut_4': x__load_checkpoint_payload__mutmut_4, 
    'x__load_checkpoint_payload__mutmut_5': x__load_checkpoint_payload__mutmut_5, 
    'x__load_checkpoint_payload__mutmut_6': x__load_checkpoint_payload__mutmut_6, 
    'x__load_checkpoint_payload__mutmut_7': x__load_checkpoint_payload__mutmut_7, 
    'x__load_checkpoint_payload__mutmut_8': x__load_checkpoint_payload__mutmut_8, 
    'x__load_checkpoint_payload__mutmut_9': x__load_checkpoint_payload__mutmut_9, 
    'x__load_checkpoint_payload__mutmut_10': x__load_checkpoint_payload__mutmut_10, 
    'x__load_checkpoint_payload__mutmut_11': x__load_checkpoint_payload__mutmut_11, 
    'x__load_checkpoint_payload__mutmut_12': x__load_checkpoint_payload__mutmut_12, 
    'x__load_checkpoint_payload__mutmut_13': x__load_checkpoint_payload__mutmut_13, 
    'x__load_checkpoint_payload__mutmut_14': x__load_checkpoint_payload__mutmut_14, 
    'x__load_checkpoint_payload__mutmut_15': x__load_checkpoint_payload__mutmut_15, 
    'x__load_checkpoint_payload__mutmut_16': x__load_checkpoint_payload__mutmut_16, 
    'x__load_checkpoint_payload__mutmut_17': x__load_checkpoint_payload__mutmut_17, 
    'x__load_checkpoint_payload__mutmut_18': x__load_checkpoint_payload__mutmut_18, 
    'x__load_checkpoint_payload__mutmut_19': x__load_checkpoint_payload__mutmut_19, 
    'x__load_checkpoint_payload__mutmut_20': x__load_checkpoint_payload__mutmut_20, 
    'x__load_checkpoint_payload__mutmut_21': x__load_checkpoint_payload__mutmut_21, 
    'x__load_checkpoint_payload__mutmut_22': x__load_checkpoint_payload__mutmut_22, 
    'x__load_checkpoint_payload__mutmut_23': x__load_checkpoint_payload__mutmut_23, 
    'x__load_checkpoint_payload__mutmut_24': x__load_checkpoint_payload__mutmut_24, 
    'x__load_checkpoint_payload__mutmut_25': x__load_checkpoint_payload__mutmut_25, 
    'x__load_checkpoint_payload__mutmut_26': x__load_checkpoint_payload__mutmut_26, 
    'x__load_checkpoint_payload__mutmut_27': x__load_checkpoint_payload__mutmut_27, 
    'x__load_checkpoint_payload__mutmut_28': x__load_checkpoint_payload__mutmut_28, 
    'x__load_checkpoint_payload__mutmut_29': x__load_checkpoint_payload__mutmut_29, 
    'x__load_checkpoint_payload__mutmut_30': x__load_checkpoint_payload__mutmut_30, 
    'x__load_checkpoint_payload__mutmut_31': x__load_checkpoint_payload__mutmut_31, 
    'x__load_checkpoint_payload__mutmut_32': x__load_checkpoint_payload__mutmut_32, 
    'x__load_checkpoint_payload__mutmut_33': x__load_checkpoint_payload__mutmut_33, 
    'x__load_checkpoint_payload__mutmut_34': x__load_checkpoint_payload__mutmut_34, 
    'x__load_checkpoint_payload__mutmut_35': x__load_checkpoint_payload__mutmut_35
}

def _load_checkpoint_payload(*args, **kwargs):
    result = _mutmut_trampoline(x__load_checkpoint_payload__mutmut_orig, x__load_checkpoint_payload__mutmut_mutants, args, kwargs)
    return result 

_load_checkpoint_payload.__signature__ = _mutmut_signature(x__load_checkpoint_payload__mutmut_orig)
x__load_checkpoint_payload__mutmut_orig.__name__ = 'x__load_checkpoint_payload'


LOGGER = logging.getLogger(__name__)


_CHECKPOINT_POINTER_VERSION = "1.0"


MetricFn = Callable[[TensorType, TensorType], float]
LossFn = Callable[[TensorType, TensorType], TensorType]


@dataclass(slots=True, init=False)
class CheckpointConfig:
    directory: str
    best_k: int = 1
    monitor: str = "val_loss"
    mode: str = "min"  # either "min" or "max"
    save_optimizer: bool = True

    def __init__(
        self,
        directory: str,
        best_k: int = 1,
        monitor: str = "val_loss",
        mode: str | None = None,
        save_optimizer: bool = True,
        *,
        keep_best_k: int | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        effective_best_k = best_k
        if keep_best_k is not None:
            if best_k != 1 and keep_best_k != best_k:
                raise ValueError("Conflicting best_k/keep_best_k values; please specify only one")
            effective_best_k = keep_best_k

        resolved_mode = mode or "min"
        if maximize_metric is not None:
            desired_mode = "max" if maximize_metric else "min"
            if mode is not None and resolved_mode.lower() not in {"min", "max"}:
                raise ValueError("mode must be 'min' or 'max' when used with maximize_metric")
            if mode is not None and resolved_mode.lower() != desired_mode:
                raise ValueError("Conflicting mode/maximize_metric values; please specify only one")
            resolved_mode = desired_mode

        object.__setattr__(self, "directory", directory)
        object.__setattr__(self, "best_k", int(effective_best_k))
        object.__setattr__(self, "monitor", monitor)
        object.__setattr__(self, "mode", resolved_mode)
        object.__setattr__(self, "save_optimizer", bool(save_optimizer))
        self.__post_init__()

    def __post_init__(self) -> None:
        if self.best_k < 1:
            raise ValueError("best_k must be >= 1")
        normalised_mode = self.mode.lower()
        if normalised_mode not in {"min", "max"}:
            raise ValueError("mode must be 'min' or 'max'")
        object.__setattr__(self, "mode", normalised_mode)

    def path_for_epoch(self, epoch: int) -> Path:
        return Path(self.directory) / f"epoch_{epoch}.pt"


@dataclass(slots=True)
class TrainerConfig:
    epochs: int = 1
    gradient_accumulation_steps: int = 1
    mixed_precision: bool = False
    max_grad_norm: float | None = None
    log_every_n_steps: int = 0
    logging: LoggingConfig = field(default_factory=LoggingConfig)
    checkpoint: CheckpointConfig | None = None
    seed: int | None = None
    metrics_ndjson_path: str | None = None


# NOTE: Public compatibility alias expected by trainer API consumers and tests.
TrainerLoggingConfig = LoggingConfig


@dataclass(slots=True)
class TrainingState:
    epoch: int = 0
    global_step: int = 0
    best_metric: float | None = None


class Trainer:
    """Extended training loop wrapper that builds on :class:`SimpleTrainer`."""

    def xǁTrainerǁ__init____mutmut_orig(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_1(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None and autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_2(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None and GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_3(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is not None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_4(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is not None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_5(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is not None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_6(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError(None)
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_7(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("XXtorch is required for the extended trainerXX")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_8(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("TORCH IS REQUIRED FOR THE EXTENDED TRAINER")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_9(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None or trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_10(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_11(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_12(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError(None)

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_13(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("XXPass only one of 'config' or 'trainer_config'XX")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_14(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_15(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("PASS ONLY ONE OF 'CONFIG' OR 'TRAINER_CONFIG'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_16(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = ""
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_17(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = None
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_18(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_19(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = None
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_20(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = None
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_21(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_22(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                None
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_23(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "XXconfig/trainer_config must be a TrainerConfig or mapping when providedXX"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_24(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a trainerconfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_25(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "CONFIG/TRAINER_CONFIG MUST BE A TRAINERCONFIG OR MAPPING WHEN PROVIDED"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_26(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = None

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_27(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config and TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_28(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_29(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = None

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_30(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(None)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_31(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_32(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = None
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_33(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = None
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_34(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError(None)

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_35(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("XXlogging_config must be a LoggingConfig or mapping when providedXX")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_36(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a loggingconfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_37(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("LOGGING_CONFIG MUST BE A LOGGINGCONFIG OR MAPPING WHEN PROVIDED")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_38(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_39(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = None
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_40(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.upper()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_41(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_42(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"XXminXX", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_43(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"MIN", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_44(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "XXmaxXX"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_45(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "MAX"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_46(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError(None)
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_47(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("XXmetric_mode must be 'min' or 'max'XX")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_48(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("METRIC_MODE MUST BE 'MIN' OR 'MAX'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_49(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = ""

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_50(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_51(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = None
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_52(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(None)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_53(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(None)
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_54(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError(None) from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_55(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("XXTrainerConfig.seed must be an intXX") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_56(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("trainerconfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_57(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TRAINERCONFIG.SEED MUST BE AN INT") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_58(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(None)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_59(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = None

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_60(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_61(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_62(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    None
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_63(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "XXPass checkpoint settings via either config.checkpoint or theXX"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_64(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_65(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "PASS CHECKPOINT SETTINGS VIA EITHER CONFIG.CHECKPOINT OR THE"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_66(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    "XX checkpoint_config argument, not bothXX"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_67(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " CHECKPOINT_CONFIG ARGUMENT, NOT BOTH"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_68(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = None
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_69(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = None
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_70(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    None
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_71(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "XXcheckpoint_config must be a CheckpointConfig or mapping when providedXX"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_72(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a checkpointconfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_73(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "CHECKPOINT_CONFIG MUST BE A CHECKPOINTCONFIG OR MAPPING WHEN PROVIDED"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_74(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None and normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_75(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None and keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_76(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_77(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_78(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_79(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is not None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_80(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError(None)
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_81(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("XXcheckpoint_dir is required when configuring checkpoint optionsXX")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_82(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("CHECKPOINT_DIR IS REQUIRED WHEN CONFIGURING CHECKPOINT OPTIONS")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_83(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = None

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_84(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=None,
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_85(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=None,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_86(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=None,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_87(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=None,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_88(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_89(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_90(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_91(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_92(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(None),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_93(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps <= 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_94(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 2:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_95(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError(None)
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_96(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("XXgradient_accumulation_steps must be >= 1XX")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_97(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("GRADIENT_ACCUMULATION_STEPS MUST BE >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_98(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = None
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_99(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=None, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_100(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=None, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_101(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=None)
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_102(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_103(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_104(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, )
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_105(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device and "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_106(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "XXcpuXX")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_107(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "CPU")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_108(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = None
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_109(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = None
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_110(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = None
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_111(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn and self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_112(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = None
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_113(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = None
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_114(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = None
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_115(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=None)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_116(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = None
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_117(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = None
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_118(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = None
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_119(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = None
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_120(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(None)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_121(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = None
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_122(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_123(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(None) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_124(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = ""

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_125(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_126(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = None
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_127(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(None)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_128(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=None, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_129(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=None)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_130(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_131(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, )
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_132(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=False, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_133(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=False)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_134(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(None)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_135(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = None
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_136(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(None)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_137(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = None
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_138(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = True
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_139(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_140(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning(None, exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_141(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", None)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_142(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning(exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_143(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", )
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_144(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("XXAuto-resume skipped due to error: %sXX", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_145(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_146(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("AUTO-RESUME SKIPPED DUE TO ERROR: %S", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_147(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = None
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_148(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = False
            if not resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_149(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if resumed:
                self._resume_from_latest_checkpoint(cfg.checkpoint)

    def xǁTrainerǁ__init____mutmut_150(
        self,
        model: nn.Module,
        optimizer: OptimizerType,
        train_loader: DataLoaderType,
        *,
        val_loader: DataLoaderType | None = None,
        loss_fn: LossFn | None = None,
        metric_fn: MetricFn | None = None,
        config: TrainerConfig | Mapping[str, Any] | None = None,
        trainer_config: TrainerConfig | Mapping[str, Any] | None = None,
        checkpoint_config: CheckpointConfig | Mapping[str, Any] | None = None,
        device: str | None = None,
        gradient_accumulation_steps: int | None = None,
        checkpoint_dir: str | Path | None = None,
        keep_best_k: int | None = None,
        logging_config: LoggingConfig | Mapping[str, Any] | None = None,
        metric_mode: str | None = None,
        maximize_metric: bool | None = None,
    ) -> None:
        if torch is None or GradScaler is None or autocast is None:
            raise RuntimeError("torch is required for the extended trainer")
        if config is not None and trainer_config is not None:
            raise TypeError("Pass only one of 'config' or 'trainer_config'")

        resolved_config: TrainerConfig | None = None
        selected = trainer_config if trainer_config is not None else config
        if isinstance(selected, TrainerConfig):
            resolved_config = selected
        elif isinstance(selected, Mapping):
            resolved_config = TrainerConfig(**selected)
        elif selected is not None:
            raise TypeError(
                "config/trainer_config must be a TrainerConfig or mapping when provided"
            )

        cfg = resolved_config or TrainerConfig()

        if gradient_accumulation_steps is not None:
            cfg.gradient_accumulation_steps = int(gradient_accumulation_steps)

        if logging_config is not None:
            if isinstance(logging_config, LoggingConfig):
                cfg.logging = logging_config
            elif isinstance(logging_config, Mapping):
                cfg.logging = LoggingConfig(**logging_config)
            else:
                raise TypeError("logging_config must be a LoggingConfig or mapping when provided")

        if metric_mode is not None:
            normalized_mode = metric_mode.lower()
            if normalized_mode not in {"min", "max"}:
                raise ValueError("metric_mode must be 'min' or 'max'")
        else:
            normalized_mode = None

        if cfg.seed is not None:
            try:
                resolved_seed = int(cfg.seed)
            except (TypeError, ValueError) as exc:
                logger.debug(f"Exception: {exc}")
                raise ValueError("TrainerConfig.seed must be an int") from exc
            _set_seed(resolved_seed)
            cfg.seed = resolved_seed

        if checkpoint_config is not None:
            if cfg.checkpoint is not None:
                raise TypeError(
                    "Pass checkpoint settings via either config.checkpoint or the"
                    " checkpoint_config argument, not both"
                )
            if isinstance(checkpoint_config, CheckpointConfig):
                cfg.checkpoint = checkpoint_config
            elif isinstance(checkpoint_config, Mapping):
                cfg.checkpoint = CheckpointConfig(**checkpoint_config)
            else:
                raise TypeError(
                    "checkpoint_config must be a CheckpointConfig or mapping when provided"
                )
        elif checkpoint_dir is not None or keep_best_k is not None or normalized_mode is not None:
            if checkpoint_dir is None:
                raise TypeError("checkpoint_dir is required when configuring checkpoint options")
            cfg.checkpoint = CheckpointConfig(
                directory=str(checkpoint_dir),
                keep_best_k=keep_best_k,
                mode=normalized_mode,
                maximize_metric=maximize_metric,
            )

        if cfg.gradient_accumulation_steps < 1:
            raise ValueError("gradient_accumulation_steps must be >= 1")
        self.simple = SimpleTrainer(model=model, optimizer=optimizer, device=device or "cpu")
        self.train_loader = train_loader
        self.val_loader = val_loader
        self.loss_fn = loss_fn or self._default_loss
        self.metric_fn = metric_fn
        self.config = cfg
        self.scaler = GradScaler(enabled=cfg.mixed_precision)
        self.state = TrainingState()
        self.history: list[Mapping[str, float]] = []
        self._checkpoints: list[tuple[float, Path, Path]] = []
        self._logging_session: LoggingSession = setup_logging(cfg.logging)
        metrics_path = cfg.metrics_ndjson_path
        self._metrics_path: Path | None = Path(metrics_path) if metrics_path else None
        self._resume_metadata: Mapping[str, Any] | None = None

        if cfg.checkpoint is not None:
            checkpoint_dir = Path(cfg.checkpoint.directory)
            checkpoint_dir.mkdir(parents=True, exist_ok=True)
            self._hydrate_existing_checkpoints(checkpoint_dir)
            latest = self._find_latest_checkpoint(checkpoint_dir)
            resumed = False
            if latest is not None:
                try:
                    self._load_checkpoint(*latest)
                except Exception as exc:  # pragma: no cover - resume is best-effort
                    LOGGER.warning("Auto-resume skipped due to error: %s", exc)
                else:
                    resumed = True
            if not resumed:
                self._resume_from_latest_checkpoint(None)
    
    xǁTrainerǁ__init____mutmut_mutants : ClassVar[MutantDict] = {
    'xǁTrainerǁ__init____mutmut_1': xǁTrainerǁ__init____mutmut_1, 
        'xǁTrainerǁ__init____mutmut_2': xǁTrainerǁ__init____mutmut_2, 
        'xǁTrainerǁ__init____mutmut_3': xǁTrainerǁ__init____mutmut_3, 
        'xǁTrainerǁ__init____mutmut_4': xǁTrainerǁ__init____mutmut_4, 
        'xǁTrainerǁ__init____mutmut_5': xǁTrainerǁ__init____mutmut_5, 
        'xǁTrainerǁ__init____mutmut_6': xǁTrainerǁ__init____mutmut_6, 
        'xǁTrainerǁ__init____mutmut_7': xǁTrainerǁ__init____mutmut_7, 
        'xǁTrainerǁ__init____mutmut_8': xǁTrainerǁ__init____mutmut_8, 
        'xǁTrainerǁ__init____mutmut_9': xǁTrainerǁ__init____mutmut_9, 
        'xǁTrainerǁ__init____mutmut_10': xǁTrainerǁ__init____mutmut_10, 
        'xǁTrainerǁ__init____mutmut_11': xǁTrainerǁ__init____mutmut_11, 
        'xǁTrainerǁ__init____mutmut_12': xǁTrainerǁ__init____mutmut_12, 
        'xǁTrainerǁ__init____mutmut_13': xǁTrainerǁ__init____mutmut_13, 
        'xǁTrainerǁ__init____mutmut_14': xǁTrainerǁ__init____mutmut_14, 
        'xǁTrainerǁ__init____mutmut_15': xǁTrainerǁ__init____mutmut_15, 
        'xǁTrainerǁ__init____mutmut_16': xǁTrainerǁ__init____mutmut_16, 
        'xǁTrainerǁ__init____mutmut_17': xǁTrainerǁ__init____mutmut_17, 
        'xǁTrainerǁ__init____mutmut_18': xǁTrainerǁ__init____mutmut_18, 
        'xǁTrainerǁ__init____mutmut_19': xǁTrainerǁ__init____mutmut_19, 
        'xǁTrainerǁ__init____mutmut_20': xǁTrainerǁ__init____mutmut_20, 
        'xǁTrainerǁ__init____mutmut_21': xǁTrainerǁ__init____mutmut_21, 
        'xǁTrainerǁ__init____mutmut_22': xǁTrainerǁ__init____mutmut_22, 
        'xǁTrainerǁ__init____mutmut_23': xǁTrainerǁ__init____mutmut_23, 
        'xǁTrainerǁ__init____mutmut_24': xǁTrainerǁ__init____mutmut_24, 
        'xǁTrainerǁ__init____mutmut_25': xǁTrainerǁ__init____mutmut_25, 
        'xǁTrainerǁ__init____mutmut_26': xǁTrainerǁ__init____mutmut_26, 
        'xǁTrainerǁ__init____mutmut_27': xǁTrainerǁ__init____mutmut_27, 
        'xǁTrainerǁ__init____mutmut_28': xǁTrainerǁ__init____mutmut_28, 
        'xǁTrainerǁ__init____mutmut_29': xǁTrainerǁ__init____mutmut_29, 
        'xǁTrainerǁ__init____mutmut_30': xǁTrainerǁ__init____mutmut_30, 
        'xǁTrainerǁ__init____mutmut_31': xǁTrainerǁ__init____mutmut_31, 
        'xǁTrainerǁ__init____mutmut_32': xǁTrainerǁ__init____mutmut_32, 
        'xǁTrainerǁ__init____mutmut_33': xǁTrainerǁ__init____mutmut_33, 
        'xǁTrainerǁ__init____mutmut_34': xǁTrainerǁ__init____mutmut_34, 
        'xǁTrainerǁ__init____mutmut_35': xǁTrainerǁ__init____mutmut_35, 
        'xǁTrainerǁ__init____mutmut_36': xǁTrainerǁ__init____mutmut_36, 
        'xǁTrainerǁ__init____mutmut_37': xǁTrainerǁ__init____mutmut_37, 
        'xǁTrainerǁ__init____mutmut_38': xǁTrainerǁ__init____mutmut_38, 
        'xǁTrainerǁ__init____mutmut_39': xǁTrainerǁ__init____mutmut_39, 
        'xǁTrainerǁ__init____mutmut_40': xǁTrainerǁ__init____mutmut_40, 
        'xǁTrainerǁ__init____mutmut_41': xǁTrainerǁ__init____mutmut_41, 
        'xǁTrainerǁ__init____mutmut_42': xǁTrainerǁ__init____mutmut_42, 
        'xǁTrainerǁ__init____mutmut_43': xǁTrainerǁ__init____mutmut_43, 
        'xǁTrainerǁ__init____mutmut_44': xǁTrainerǁ__init____mutmut_44, 
        'xǁTrainerǁ__init____mutmut_45': xǁTrainerǁ__init____mutmut_45, 
        'xǁTrainerǁ__init____mutmut_46': xǁTrainerǁ__init____mutmut_46, 
        'xǁTrainerǁ__init____mutmut_47': xǁTrainerǁ__init____mutmut_47, 
        'xǁTrainerǁ__init____mutmut_48': xǁTrainerǁ__init____mutmut_48, 
        'xǁTrainerǁ__init____mutmut_49': xǁTrainerǁ__init____mutmut_49, 
        'xǁTrainerǁ__init____mutmut_50': xǁTrainerǁ__init____mutmut_50, 
        'xǁTrainerǁ__init____mutmut_51': xǁTrainerǁ__init____mutmut_51, 
        'xǁTrainerǁ__init____mutmut_52': xǁTrainerǁ__init____mutmut_52, 
        'xǁTrainerǁ__init____mutmut_53': xǁTrainerǁ__init____mutmut_53, 
        'xǁTrainerǁ__init____mutmut_54': xǁTrainerǁ__init____mutmut_54, 
        'xǁTrainerǁ__init____mutmut_55': xǁTrainerǁ__init____mutmut_55, 
        'xǁTrainerǁ__init____mutmut_56': xǁTrainerǁ__init____mutmut_56, 
        'xǁTrainerǁ__init____mutmut_57': xǁTrainerǁ__init____mutmut_57, 
        'xǁTrainerǁ__init____mutmut_58': xǁTrainerǁ__init____mutmut_58, 
        'xǁTrainerǁ__init____mutmut_59': xǁTrainerǁ__init____mutmut_59, 
        'xǁTrainerǁ__init____mutmut_60': xǁTrainerǁ__init____mutmut_60, 
        'xǁTrainerǁ__init____mutmut_61': xǁTrainerǁ__init____mutmut_61, 
        'xǁTrainerǁ__init____mutmut_62': xǁTrainerǁ__init____mutmut_62, 
        'xǁTrainerǁ__init____mutmut_63': xǁTrainerǁ__init____mutmut_63, 
        'xǁTrainerǁ__init____mutmut_64': xǁTrainerǁ__init____mutmut_64, 
        'xǁTrainerǁ__init____mutmut_65': xǁTrainerǁ__init____mutmut_65, 
        'xǁTrainerǁ__init____mutmut_66': xǁTrainerǁ__init____mutmut_66, 
        'xǁTrainerǁ__init____mutmut_67': xǁTrainerǁ__init____mutmut_67, 
        'xǁTrainerǁ__init____mutmut_68': xǁTrainerǁ__init____mutmut_68, 
        'xǁTrainerǁ__init____mutmut_69': xǁTrainerǁ__init____mutmut_69, 
        'xǁTrainerǁ__init____mutmut_70': xǁTrainerǁ__init____mutmut_70, 
        'xǁTrainerǁ__init____mutmut_71': xǁTrainerǁ__init____mutmut_71, 
        'xǁTrainerǁ__init____mutmut_72': xǁTrainerǁ__init____mutmut_72, 
        'xǁTrainerǁ__init____mutmut_73': xǁTrainerǁ__init____mutmut_73, 
        'xǁTrainerǁ__init____mutmut_74': xǁTrainerǁ__init____mutmut_74, 
        'xǁTrainerǁ__init____mutmut_75': xǁTrainerǁ__init____mutmut_75, 
        'xǁTrainerǁ__init____mutmut_76': xǁTrainerǁ__init____mutmut_76, 
        'xǁTrainerǁ__init____mutmut_77': xǁTrainerǁ__init____mutmut_77, 
        'xǁTrainerǁ__init____mutmut_78': xǁTrainerǁ__init____mutmut_78, 
        'xǁTrainerǁ__init____mutmut_79': xǁTrainerǁ__init____mutmut_79, 
        'xǁTrainerǁ__init____mutmut_80': xǁTrainerǁ__init____mutmut_80, 
        'xǁTrainerǁ__init____mutmut_81': xǁTrainerǁ__init____mutmut_81, 
        'xǁTrainerǁ__init____mutmut_82': xǁTrainerǁ__init____mutmut_82, 
        'xǁTrainerǁ__init____mutmut_83': xǁTrainerǁ__init____mutmut_83, 
        'xǁTrainerǁ__init____mutmut_84': xǁTrainerǁ__init____mutmut_84, 
        'xǁTrainerǁ__init____mutmut_85': xǁTrainerǁ__init____mutmut_85, 
        'xǁTrainerǁ__init____mutmut_86': xǁTrainerǁ__init____mutmut_86, 
        'xǁTrainerǁ__init____mutmut_87': xǁTrainerǁ__init____mutmut_87, 
        'xǁTrainerǁ__init____mutmut_88': xǁTrainerǁ__init____mutmut_88, 
        'xǁTrainerǁ__init____mutmut_89': xǁTrainerǁ__init____mutmut_89, 
        'xǁTrainerǁ__init____mutmut_90': xǁTrainerǁ__init____mutmut_90, 
        'xǁTrainerǁ__init____mutmut_91': xǁTrainerǁ__init____mutmut_91, 
        'xǁTrainerǁ__init____mutmut_92': xǁTrainerǁ__init____mutmut_92, 
        'xǁTrainerǁ__init____mutmut_93': xǁTrainerǁ__init____mutmut_93, 
        'xǁTrainerǁ__init____mutmut_94': xǁTrainerǁ__init____mutmut_94, 
        'xǁTrainerǁ__init____mutmut_95': xǁTrainerǁ__init____mutmut_95, 
        'xǁTrainerǁ__init____mutmut_96': xǁTrainerǁ__init____mutmut_96, 
        'xǁTrainerǁ__init____mutmut_97': xǁTrainerǁ__init____mutmut_97, 
        'xǁTrainerǁ__init____mutmut_98': xǁTrainerǁ__init____mutmut_98, 
        'xǁTrainerǁ__init____mutmut_99': xǁTrainerǁ__init____mutmut_99, 
        'xǁTrainerǁ__init____mutmut_100': xǁTrainerǁ__init____mutmut_100, 
        'xǁTrainerǁ__init____mutmut_101': xǁTrainerǁ__init____mutmut_101, 
        'xǁTrainerǁ__init____mutmut_102': xǁTrainerǁ__init____mutmut_102, 
        'xǁTrainerǁ__init____mutmut_103': xǁTrainerǁ__init____mutmut_103, 
        'xǁTrainerǁ__init____mutmut_104': xǁTrainerǁ__init____mutmut_104, 
        'xǁTrainerǁ__init____mutmut_105': xǁTrainerǁ__init____mutmut_105, 
        'xǁTrainerǁ__init____mutmut_106': xǁTrainerǁ__init____mutmut_106, 
        'xǁTrainerǁ__init____mutmut_107': xǁTrainerǁ__init____mutmut_107, 
        'xǁTrainerǁ__init____mutmut_108': xǁTrainerǁ__init____mutmut_108, 
        'xǁTrainerǁ__init____mutmut_109': xǁTrainerǁ__init____mutmut_109, 
        'xǁTrainerǁ__init____mutmut_110': xǁTrainerǁ__init____mutmut_110, 
        'xǁTrainerǁ__init____mutmut_111': xǁTrainerǁ__init____mutmut_111, 
        'xǁTrainerǁ__init____mutmut_112': xǁTrainerǁ__init____mutmut_112, 
        'xǁTrainerǁ__init____mutmut_113': xǁTrainerǁ__init____mutmut_113, 
        'xǁTrainerǁ__init____mutmut_114': xǁTrainerǁ__init____mutmut_114, 
        'xǁTrainerǁ__init____mutmut_115': xǁTrainerǁ__init____mutmut_115, 
        'xǁTrainerǁ__init____mutmut_116': xǁTrainerǁ__init____mutmut_116, 
        'xǁTrainerǁ__init____mutmut_117': xǁTrainerǁ__init____mutmut_117, 
        'xǁTrainerǁ__init____mutmut_118': xǁTrainerǁ__init____mutmut_118, 
        'xǁTrainerǁ__init____mutmut_119': xǁTrainerǁ__init____mutmut_119, 
        'xǁTrainerǁ__init____mutmut_120': xǁTrainerǁ__init____mutmut_120, 
        'xǁTrainerǁ__init____mutmut_121': xǁTrainerǁ__init____mutmut_121, 
        'xǁTrainerǁ__init____mutmut_122': xǁTrainerǁ__init____mutmut_122, 
        'xǁTrainerǁ__init____mutmut_123': xǁTrainerǁ__init____mutmut_123, 
        'xǁTrainerǁ__init____mutmut_124': xǁTrainerǁ__init____mutmut_124, 
        'xǁTrainerǁ__init____mutmut_125': xǁTrainerǁ__init____mutmut_125, 
        'xǁTrainerǁ__init____mutmut_126': xǁTrainerǁ__init____mutmut_126, 
        'xǁTrainerǁ__init____mutmut_127': xǁTrainerǁ__init____mutmut_127, 
        'xǁTrainerǁ__init____mutmut_128': xǁTrainerǁ__init____mutmut_128, 
        'xǁTrainerǁ__init____mutmut_129': xǁTrainerǁ__init____mutmut_129, 
        'xǁTrainerǁ__init____mutmut_130': xǁTrainerǁ__init____mutmut_130, 
        'xǁTrainerǁ__init____mutmut_131': xǁTrainerǁ__init____mutmut_131, 
        'xǁTrainerǁ__init____mutmut_132': xǁTrainerǁ__init____mutmut_132, 
        'xǁTrainerǁ__init____mutmut_133': xǁTrainerǁ__init____mutmut_133, 
        'xǁTrainerǁ__init____mutmut_134': xǁTrainerǁ__init____mutmut_134, 
        'xǁTrainerǁ__init____mutmut_135': xǁTrainerǁ__init____mutmut_135, 
        'xǁTrainerǁ__init____mutmut_136': xǁTrainerǁ__init____mutmut_136, 
        'xǁTrainerǁ__init____mutmut_137': xǁTrainerǁ__init____mutmut_137, 
        'xǁTrainerǁ__init____mutmut_138': xǁTrainerǁ__init____mutmut_138, 
        'xǁTrainerǁ__init____mutmut_139': xǁTrainerǁ__init____mutmut_139, 
        'xǁTrainerǁ__init____mutmut_140': xǁTrainerǁ__init____mutmut_140, 
        'xǁTrainerǁ__init____mutmut_141': xǁTrainerǁ__init____mutmut_141, 
        'xǁTrainerǁ__init____mutmut_142': xǁTrainerǁ__init____mutmut_142, 
        'xǁTrainerǁ__init____mutmut_143': xǁTrainerǁ__init____mutmut_143, 
        'xǁTrainerǁ__init____mutmut_144': xǁTrainerǁ__init____mutmut_144, 
        'xǁTrainerǁ__init____mutmut_145': xǁTrainerǁ__init____mutmut_145, 
        'xǁTrainerǁ__init____mutmut_146': xǁTrainerǁ__init____mutmut_146, 
        'xǁTrainerǁ__init____mutmut_147': xǁTrainerǁ__init____mutmut_147, 
        'xǁTrainerǁ__init____mutmut_148': xǁTrainerǁ__init____mutmut_148, 
        'xǁTrainerǁ__init____mutmut_149': xǁTrainerǁ__init____mutmut_149, 
        'xǁTrainerǁ__init____mutmut_150': xǁTrainerǁ__init____mutmut_150
    }
    
    def __init__(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁTrainerǁ__init____mutmut_orig"), object.__getattribute__(self, "xǁTrainerǁ__init____mutmut_mutants"), args, kwargs, self)
        return result 
    
    __init__.__signature__ = _mutmut_signature(xǁTrainerǁ__init____mutmut_orig)
    xǁTrainerǁ__init____mutmut_orig.__name__ = 'xǁTrainerǁ__init__'

    @property
    def device(self) -> str:
        return self.simple.device

    def xǁTrainerǁ_prepare_batch__mutmut_orig(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_1(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = None
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_2(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(None)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_3(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "XXlabelsXX" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_4(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "LABELS" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_5(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_6(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError(None)
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_7(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("XXBatch mapping must include a 'labels' tensorXX")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_8(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_9(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("BATCH MAPPING MUST INCLUDE A 'LABELS' TENSOR")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_10(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = None
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_11(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop(None)
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_12(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("XXlabelsXX")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_13(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("LABELS")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_14(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = None
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_15(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(None)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_16(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = None
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_17(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = None
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_18(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(None) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_19(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(None, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_20(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, None) else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_21(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr("to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_22(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, ) else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_23(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "XXtoXX") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_24(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "TO") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_25(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) or len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_26(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) != 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_27(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 3:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_28(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = None
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_29(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = None
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_30(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(None)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_31(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = None
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_32(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["XXlabelsXX"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_33(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["LABELS"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_34(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(None)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_35(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(None, "to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_36(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, None):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_37(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr("to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_38(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, ):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_39(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "XXtoXX"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_40(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "TO"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_41(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = None
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_42(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(None)
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_43(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr(None, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_44(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, None):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_45(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr("to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_46(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, ):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_47(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "XXtoXX"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_48(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "TO"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_49(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = None
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_50(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = labels.to(None)
            return inputs, labels
        raise TypeError("Unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_51(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError(None)

    def xǁTrainerǁ_prepare_batch__mutmut_52(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("XXUnsupported batch type; expected mapping or (inputs, labels) tupleXX")

    def xǁTrainerǁ_prepare_batch__mutmut_53(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("unsupported batch type; expected mapping or (inputs, labels) tuple")

    def xǁTrainerǁ_prepare_batch__mutmut_54(
        self, batch: Sequence | Mapping[str, torch.Tensor]
    ) -> tuple[Any, torch.Tensor]:
        if isinstance(batch, Mapping):
            mapping = dict(batch)
            if "labels" not in mapping:
                raise ValueError("Batch mapping must include a 'labels' tensor")
            labels = mapping.pop("labels")
            labels = labels.to(self.device)
            inputs: MutableMapping[str, Any] = {}
            for key, value in mapping.items():
                inputs[key] = value.to(self.device) if hasattr(value, "to") else value
            return inputs, labels
        if isinstance(batch, Sequence) and len(batch) == 2:
            inputs, labels = batch
            if isinstance(inputs, Mapping):
                merged = dict(inputs)
                merged["labels"] = labels
                return self._prepare_batch(merged)
            if hasattr(inputs, "to"):
                inputs = inputs.to(self.device)
            if hasattr(labels, "to"):
                labels = labels.to(self.device)
            return inputs, labels
        raise TypeError("UNSUPPORTED BATCH TYPE; EXPECTED MAPPING OR (INPUTS, LABELS) TUPLE")
    
    xǁTrainerǁ_prepare_batch__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁTrainerǁ_prepare_batch__mutmut_1': xǁTrainerǁ_prepare_batch__mutmut_1, 
        'xǁTrainerǁ_prepare_batch__mutmut_2': xǁTrainerǁ_prepare_batch__mutmut_2, 
        'xǁTrainerǁ_prepare_batch__mutmut_3': xǁTrainerǁ_prepare_batch__mutmut_3, 
        'xǁTrainerǁ_prepare_batch__mutmut_4': xǁTrainerǁ_prepare_batch__mutmut_4, 
        'xǁTrainerǁ_prepare_batch__mutmut_5': xǁTrainerǁ_prepare_batch__mutmut_5, 
        'xǁTrainerǁ_prepare_batch__mutmut_6': xǁTrainerǁ_prepare_batch__mutmut_6, 
        'xǁTrainerǁ_prepare_batch__mutmut_7': xǁTrainerǁ_prepare_batch__mutmut_7, 
        'xǁTrainerǁ_prepare_batch__mutmut_8': xǁTrainerǁ_prepare_batch__mutmut_8, 
        'xǁTrainerǁ_prepare_batch__mutmut_9': xǁTrainerǁ_prepare_batch__mutmut_9, 
        'xǁTrainerǁ_prepare_batch__mutmut_10': xǁTrainerǁ_prepare_batch__mutmut_10, 
        'xǁTrainerǁ_prepare_batch__mutmut_11': xǁTrainerǁ_prepare_batch__mutmut_11, 
        'xǁTrainerǁ_prepare_batch__mutmut_12': xǁTrainerǁ_prepare_batch__mutmut_12, 
        'xǁTrainerǁ_prepare_batch__mutmut_13': xǁTrainerǁ_prepare_batch__mutmut_13, 
        'xǁTrainerǁ_prepare_batch__mutmut_14': xǁTrainerǁ_prepare_batch__mutmut_14, 
        'xǁTrainerǁ_prepare_batch__mutmut_15': xǁTrainerǁ_prepare_batch__mutmut_15, 
        'xǁTrainerǁ_prepare_batch__mutmut_16': xǁTrainerǁ_prepare_batch__mutmut_16, 
        'xǁTrainerǁ_prepare_batch__mutmut_17': xǁTrainerǁ_prepare_batch__mutmut_17, 
        'xǁTrainerǁ_prepare_batch__mutmut_18': xǁTrainerǁ_prepare_batch__mutmut_18, 
        'xǁTrainerǁ_prepare_batch__mutmut_19': xǁTrainerǁ_prepare_batch__mutmut_19, 
        'xǁTrainerǁ_prepare_batch__mutmut_20': xǁTrainerǁ_prepare_batch__mutmut_20, 
        'xǁTrainerǁ_prepare_batch__mutmut_21': xǁTrainerǁ_prepare_batch__mutmut_21, 
        'xǁTrainerǁ_prepare_batch__mutmut_22': xǁTrainerǁ_prepare_batch__mutmut_22, 
        'xǁTrainerǁ_prepare_batch__mutmut_23': xǁTrainerǁ_prepare_batch__mutmut_23, 
        'xǁTrainerǁ_prepare_batch__mutmut_24': xǁTrainerǁ_prepare_batch__mutmut_24, 
        'xǁTrainerǁ_prepare_batch__mutmut_25': xǁTrainerǁ_prepare_batch__mutmut_25, 
        'xǁTrainerǁ_prepare_batch__mutmut_26': xǁTrainerǁ_prepare_batch__mutmut_26, 
        'xǁTrainerǁ_prepare_batch__mutmut_27': xǁTrainerǁ_prepare_batch__mutmut_27, 
        'xǁTrainerǁ_prepare_batch__mutmut_28': xǁTrainerǁ_prepare_batch__mutmut_28, 
        'xǁTrainerǁ_prepare_batch__mutmut_29': xǁTrainerǁ_prepare_batch__mutmut_29, 
        'xǁTrainerǁ_prepare_batch__mutmut_30': xǁTrainerǁ_prepare_batch__mutmut_30, 
        'xǁTrainerǁ_prepare_batch__mutmut_31': xǁTrainerǁ_prepare_batch__mutmut_31, 
        'xǁTrainerǁ_prepare_batch__mutmut_32': xǁTrainerǁ_prepare_batch__mutmut_32, 
        'xǁTrainerǁ_prepare_batch__mutmut_33': xǁTrainerǁ_prepare_batch__mutmut_33, 
        'xǁTrainerǁ_prepare_batch__mutmut_34': xǁTrainerǁ_prepare_batch__mutmut_34, 
        'xǁTrainerǁ_prepare_batch__mutmut_35': xǁTrainerǁ_prepare_batch__mutmut_35, 
        'xǁTrainerǁ_prepare_batch__mutmut_36': xǁTrainerǁ_prepare_batch__mutmut_36, 
        'xǁTrainerǁ_prepare_batch__mutmut_37': xǁTrainerǁ_prepare_batch__mutmut_37, 
        'xǁTrainerǁ_prepare_batch__mutmut_38': xǁTrainerǁ_prepare_batch__mutmut_38, 
        'xǁTrainerǁ_prepare_batch__mutmut_39': xǁTrainerǁ_prepare_batch__mutmut_39, 
        'xǁTrainerǁ_prepare_batch__mutmut_40': xǁTrainerǁ_prepare_batch__mutmut_40, 
        'xǁTrainerǁ_prepare_batch__mutmut_41': xǁTrainerǁ_prepare_batch__mutmut_41, 
        'xǁTrainerǁ_prepare_batch__mutmut_42': xǁTrainerǁ_prepare_batch__mutmut_42, 
        'xǁTrainerǁ_prepare_batch__mutmut_43': xǁTrainerǁ_prepare_batch__mutmut_43, 
        'xǁTrainerǁ_prepare_batch__mutmut_44': xǁTrainerǁ_prepare_batch__mutmut_44, 
        'xǁTrainerǁ_prepare_batch__mutmut_45': xǁTrainerǁ_prepare_batch__mutmut_45, 
        'xǁTrainerǁ_prepare_batch__mutmut_46': xǁTrainerǁ_prepare_batch__mutmut_46, 
        'xǁTrainerǁ_prepare_batch__mutmut_47': xǁTrainerǁ_prepare_batch__mutmut_47, 
        'xǁTrainerǁ_prepare_batch__mutmut_48': xǁTrainerǁ_prepare_batch__mutmut_48, 
        'xǁTrainerǁ_prepare_batch__mutmut_49': xǁTrainerǁ_prepare_batch__mutmut_49, 
        'xǁTrainerǁ_prepare_batch__mutmut_50': xǁTrainerǁ_prepare_batch__mutmut_50, 
        'xǁTrainerǁ_prepare_batch__mutmut_51': xǁTrainerǁ_prepare_batch__mutmut_51, 
        'xǁTrainerǁ_prepare_batch__mutmut_52': xǁTrainerǁ_prepare_batch__mutmut_52, 
        'xǁTrainerǁ_prepare_batch__mutmut_53': xǁTrainerǁ_prepare_batch__mutmut_53, 
        'xǁTrainerǁ_prepare_batch__mutmut_54': xǁTrainerǁ_prepare_batch__mutmut_54
    }
    
    def _prepare_batch(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁTrainerǁ_prepare_batch__mutmut_orig"), object.__getattribute__(self, "xǁTrainerǁ_prepare_batch__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _prepare_batch.__signature__ = _mutmut_signature(xǁTrainerǁ_prepare_batch__mutmut_orig)
    xǁTrainerǁ_prepare_batch__mutmut_orig.__name__ = 'xǁTrainerǁ_prepare_batch'

    def xǁTrainerǁ_zero_grad__mutmut_orig(self) -> None:
        try:
            self.simple.optimizer.zero_grad(set_to_none=True)
        except TypeError:
            self.simple.optimizer.zero_grad()

    def xǁTrainerǁ_zero_grad__mutmut_1(self) -> None:
        try:
            self.simple.optimizer.zero_grad(set_to_none=None)
        except TypeError:
            self.simple.optimizer.zero_grad()

    def xǁTrainerǁ_zero_grad__mutmut_2(self) -> None:
        try:
            self.simple.optimizer.zero_grad(set_to_none=False)
        except TypeError:
            self.simple.optimizer.zero_grad()
    
    xǁTrainerǁ_zero_grad__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁTrainerǁ_zero_grad__mutmut_1': xǁTrainerǁ_zero_grad__mutmut_1, 
        'xǁTrainerǁ_zero_grad__mutmut_2': xǁTrainerǁ_zero_grad__mutmut_2
    }
    
    def _zero_grad(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁTrainerǁ_zero_grad__mutmut_orig"), object.__getattribute__(self, "xǁTrainerǁ_zero_grad__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _zero_grad.__signature__ = _mutmut_signature(xǁTrainerǁ_zero_grad__mutmut_orig)
    xǁTrainerǁ_zero_grad__mutmut_orig.__name__ = 'xǁTrainerǁ_zero_grad'

    def xǁTrainerǁ_forward__mutmut_orig(self, inputs: Any) -> torch.Tensor:
        if isinstance(inputs, Mapping):
            return self.simple.model(**inputs)  # type: ignore[arg-type]
        return self.simple.model(inputs)

    def xǁTrainerǁ_forward__mutmut_1(self, inputs: Any) -> torch.Tensor:
        if isinstance(inputs, Mapping):
            return self.simple.model(**inputs)  # type: ignore[arg-type]
        return self.simple.model(None)
    
    xǁTrainerǁ_forward__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁTrainerǁ_forward__mutmut_1': xǁTrainerǁ_forward__mutmut_1
    }
    
    def _forward(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁTrainerǁ_forward__mutmut_orig"), object.__getattribute__(self, "xǁTrainerǁ_forward__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _forward.__signature__ = _mutmut_signature(xǁTrainerǁ_forward__mutmut_orig)
    xǁTrainerǁ_forward__mutmut_orig.__name__ = 'xǁTrainerǁ_forward'

    def xǁTrainerǁ_latest_checkpoint_path__mutmut_orig(self, directory: str | Path) -> Path | None:
        try:
            candidates = sorted(
                (p for p in Path(directory).glob("epoch*-metric*.pt") if p.is_file()),
                key=lambda p: p.stat().st_mtime,
                reverse=True,
            )
        except Exception:  # pragma: no cover - directory read failure
            return None
        return candidates[0] if candidates else None

    def xǁTrainerǁ_latest_checkpoint_path__mutmut_1(self, directory: str | Path) -> Path | None:
        try:
            candidates = None
        except Exception:  # pragma: no cover - directory read failure
            return None
        return candidates[0] if candidates else None

    def xǁTrainerǁ_latest_checkpoint_path__mutmut_2(self, directory: str | Path) -> Path | None:
        try:
            candidates = sorted(
                None,
                key=lambda p: p.stat().st_mtime,
                reverse=True,
            )
        except Exception:  # pragma: no cover - directory read failure
            return None
        return candidates[0] if candidates else None

    def xǁTrainerǁ_latest_checkpoint_path__mutmut_3(self, directory: str | Path) -> Path | None:
        try:
            candidates = sorted(
                (p for p in Path(directory).glob("epoch*-metric*.pt") if p.is_file()),
                key=None,
                reverse=True,
            )
        except Exception:  # pragma: no cover - directory read failure
            return None
        return candidates[0] if candidates else None

    def xǁTrainerǁ_latest_checkpoint_path__mutmut_4(self, directory: str | Path) -> Path | None:
        try:
            candidates = sorted(
                (p for p in Path(directory).glob("epoch*-metric*.pt") if p.is_file()),
                key=lambda p: p.stat().st_mtime,
                reverse=None,
            )
        except Exception:  # pragma: no cover - directory read failure
            return None
        return candidates[0] if candidates else None

    def xǁTrainerǁ_latest_checkpoint_path__mutmut_5(self, directory: str | Path) -> Path | None:
        try:
            candidates = sorted(
                key=lambda p: p.stat().st_mtime,
                reverse=True,
            )
        except Exception:  # pragma: no cover - directory read failure
            return None
        return candidates[0] if candidates else None

    def xǁTrainerǁ_latest_checkpoint_path__mutmut_6(self, directory: str | Path) -> Path | None:
        try:
            candidates = sorted(
                (p for p in Path(directory).glob("epoch*-metric*.pt") if p.is_file()),
                reverse=True,
            )
        except Exception:  # pragma: no cover - directory read failure
            return None
        return candidates[0] if candidates else None

    def xǁTrainerǁ_latest_checkpoint_path__mutmut_7(self, directory: str | Path) -> Path | None:
        try:
            candidates = sorted(
                (p for p in Path(directory).glob("epoch*-metric*.pt") if p.is_file()),
                key=lambda p: p.stat().st_mtime,
                )
        except Exception:  # pragma: no cover - directory read failure
            return None
        return candidates[0] if candidates else None

    def xǁTrainerǁ_latest_checkpoint_path__mutmut_8(self, directory: str | Path) -> Path | None:
        try:
            candidates = sorted(
                (p for p in Path(directory).glob(None) if p.is_file()),
                key=lambda p: p.stat().st_mtime,
                reverse=True,
            )
        except Exception:  # pragma: no cover - directory read failure
            return None
        return candidates[0] if candidates else None

    def xǁTrainerǁ_latest_checkpoint_path__mutmut_9(self, directory: str | Path) -> Path | None:
        try:
            candidates = sorted(
                (p for p in Path(None).glob("epoch*-metric*.pt") if p.is_file()),
                key=lambda p: p.stat().st_mtime,
                reverse=True,
            )
        except Exception:  # pragma: no cover - directory read failure
            return None
        return candidates[0] if candidates else None

    def xǁTrainerǁ_latest_checkpoint_path__mutmut_10(self, directory: str | Path) -> Path | None:
        try:
            candidates = sorted(
                (p for p in Path(directory).glob("XXepoch*-metric*.ptXX") if p.is_file()),
                key=lambda p: p.stat().st_mtime,
                reverse=True,
            )
        except Exception:  # pragma: no cover - directory read failure
            return None
        return candidates[0] if candidates else None

    def xǁTrainerǁ_latest_checkpoint_path__mutmut_11(self, directory: str | Path) -> Path | None:
        try:
            candidates = sorted(
                (p for p in Path(directory).glob("EPOCH*-METRIC*.PT") if p.is_file()),
                key=lambda p: p.stat().st_mtime,
                reverse=True,
            )
        except Exception:  # pragma: no cover - directory read failure
            return None
        return candidates[0] if candidates else None

    def xǁTrainerǁ_latest_checkpoint_path__mutmut_12(self, directory: str | Path) -> Path | None:
        try:
            candidates = sorted(
                (p for p in Path(directory).glob("epoch*-metric*.pt") if p.is_file()),
                key=lambda p: None,
                reverse=True,
            )
        except Exception:  # pragma: no cover - directory read failure
            return None
        return candidates[0] if candidates else None

    def xǁTrainerǁ_latest_checkpoint_path__mutmut_13(self, directory: str | Path) -> Path | None:
        try:
            candidates = sorted(
                (p for p in Path(directory).glob("epoch*-metric*.pt") if p.is_file()),
                key=lambda p: p.stat().st_mtime,
                reverse=False,
            )
        except Exception:  # pragma: no cover - directory read failure
            return None
        return candidates[0] if candidates else None

    def xǁTrainerǁ_latest_checkpoint_path__mutmut_14(self, directory: str | Path) -> Path | None:
        try:
            candidates = sorted(
                (p for p in Path(directory).glob("epoch*-metric*.pt") if p.is_file()),
                key=lambda p: p.stat().st_mtime,
                reverse=True,
            )
        except Exception:  # pragma: no cover - directory read failure
            return None
        return candidates[1] if candidates else None
    
    xǁTrainerǁ_latest_checkpoint_path__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁTrainerǁ_latest_checkpoint_path__mutmut_1': xǁTrainerǁ_latest_checkpoint_path__mutmut_1, 
        'xǁTrainerǁ_latest_checkpoint_path__mutmut_2': xǁTrainerǁ_latest_checkpoint_path__mutmut_2, 
        'xǁTrainerǁ_latest_checkpoint_path__mutmut_3': xǁTrainerǁ_latest_checkpoint_path__mutmut_3, 
        'xǁTrainerǁ_latest_checkpoint_path__mutmut_4': xǁTrainerǁ_latest_checkpoint_path__mutmut_4, 
        'xǁTrainerǁ_latest_checkpoint_path__mutmut_5': xǁTrainerǁ_latest_checkpoint_path__mutmut_5, 
        'xǁTrainerǁ_latest_checkpoint_path__mutmut_6': xǁTrainerǁ_latest_checkpoint_path__mutmut_6, 
        'xǁTrainerǁ_latest_checkpoint_path__mutmut_7': xǁTrainerǁ_latest_checkpoint_path__mutmut_7, 
        'xǁTrainerǁ_latest_checkpoint_path__mutmut_8': xǁTrainerǁ_latest_checkpoint_path__mutmut_8, 
        'xǁTrainerǁ_latest_checkpoint_path__mutmut_9': xǁTrainerǁ_latest_checkpoint_path__mutmut_9, 
        'xǁTrainerǁ_latest_checkpoint_path__mutmut_10': xǁTrainerǁ_latest_checkpoint_path__mutmut_10, 
        'xǁTrainerǁ_latest_checkpoint_path__mutmut_11': xǁTrainerǁ_latest_checkpoint_path__mutmut_11, 
        'xǁTrainerǁ_latest_checkpoint_path__mutmut_12': xǁTrainerǁ_latest_checkpoint_path__mutmut_12, 
        'xǁTrainerǁ_latest_checkpoint_path__mutmut_13': xǁTrainerǁ_latest_checkpoint_path__mutmut_13, 
        'xǁTrainerǁ_latest_checkpoint_path__mutmut_14': xǁTrainerǁ_latest_checkpoint_path__mutmut_14
    }
    
    def _latest_checkpoint_path(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁTrainerǁ_latest_checkpoint_path__mutmut_orig"), object.__getattribute__(self, "xǁTrainerǁ_latest_checkpoint_path__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _latest_checkpoint_path.__signature__ = _mutmut_signature(xǁTrainerǁ_latest_checkpoint_path__mutmut_orig)
    xǁTrainerǁ_latest_checkpoint_path__mutmut_orig.__name__ = 'xǁTrainerǁ_latest_checkpoint_path'

    def xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_orig(self, cfg: CheckpointConfig) -> None:
        latest = self._latest_checkpoint_path(cfg.directory)
        if latest is None:
            return
        try:
            epoch, metric = load_checkpoint(latest, self.simple.model, self.simple.optimizer)
        except Exception as exc:  # pragma: no cover - robustness guard
            LOGGER.warning("Failed to auto-resume from %s: %s", latest, exc)
            return
        self.state.epoch = int(epoch)
        self.state.best_metric = float(metric)
        LOGGER.info(
            "Auto-resumed training from checkpoint %s (epoch=%s, metric=%s)",
            latest.name,
            epoch,
            metric,
        )

    def xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_1(self, cfg: CheckpointConfig) -> None:
        latest = None
        if latest is None:
            return
        try:
            epoch, metric = load_checkpoint(latest, self.simple.model, self.simple.optimizer)
        except Exception as exc:  # pragma: no cover - robustness guard
            LOGGER.warning("Failed to auto-resume from %s: %s", latest, exc)
            return
        self.state.epoch = int(epoch)
        self.state.best_metric = float(metric)
        LOGGER.info(
            "Auto-resumed training from checkpoint %s (epoch=%s, metric=%s)",
            latest.name,
            epoch,
            metric,
        )

    def xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_2(self, cfg: CheckpointConfig) -> None:
        latest = self._latest_checkpoint_path(None)
        if latest is None:
            return
        try:
            epoch, metric = load_checkpoint(latest, self.simple.model, self.simple.optimizer)
        except Exception as exc:  # pragma: no cover - robustness guard
            LOGGER.warning("Failed to auto-resume from %s: %s", latest, exc)
            return
        self.state.epoch = int(epoch)
        self.state.best_metric = float(metric)
        LOGGER.info(
            "Auto-resumed training from checkpoint %s (epoch=%s, metric=%s)",
            latest.name,
            epoch,
            metric,
        )

    def xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_3(self, cfg: CheckpointConfig) -> None:
        latest = self._latest_checkpoint_path(cfg.directory)
        if latest is not None:
            return
        try:
            epoch, metric = load_checkpoint(latest, self.simple.model, self.simple.optimizer)
        except Exception as exc:  # pragma: no cover - robustness guard
            LOGGER.warning("Failed to auto-resume from %s: %s", latest, exc)
            return
        self.state.epoch = int(epoch)
        self.state.best_metric = float(metric)
        LOGGER.info(
            "Auto-resumed training from checkpoint %s (epoch=%s, metric=%s)",
            latest.name,
            epoch,
            metric,
        )

    def xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_4(self, cfg: CheckpointConfig) -> None:
        latest = self._latest_checkpoint_path(cfg.directory)
        if latest is None:
            return
        try:
            epoch, metric = None
        except Exception as exc:  # pragma: no cover - robustness guard
            LOGGER.warning("Failed to auto-resume from %s: %s", latest, exc)
            return
        self.state.epoch = int(epoch)
        self.state.best_metric = float(metric)
        LOGGER.info(
            "Auto-resumed training from checkpoint %s (epoch=%s, metric=%s)",
            latest.name,
            epoch,
            metric,
        )

    def xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_5(self, cfg: CheckpointConfig) -> None:
        latest = self._latest_checkpoint_path(cfg.directory)
        if latest is None:
            return
        try:
            epoch, metric = load_checkpoint(None, self.simple.model, self.simple.optimizer)
        except Exception as exc:  # pragma: no cover - robustness guard
            LOGGER.warning("Failed to auto-resume from %s: %s", latest, exc)
            return
        self.state.epoch = int(epoch)
        self.state.best_metric = float(metric)
        LOGGER.info(
            "Auto-resumed training from checkpoint %s (epoch=%s, metric=%s)",
            latest.name,
            epoch,
            metric,
        )

    def xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_6(self, cfg: CheckpointConfig) -> None:
        latest = self._latest_checkpoint_path(cfg.directory)
        if latest is None:
            return
        try:
            epoch, metric = load_checkpoint(latest, None, self.simple.optimizer)
        except Exception as exc:  # pragma: no cover - robustness guard
            LOGGER.warning("Failed to auto-resume from %s: %s", latest, exc)
            return
        self.state.epoch = int(epoch)
        self.state.best_metric = float(metric)
        LOGGER.info(
            "Auto-resumed training from checkpoint %s (epoch=%s, metric=%s)",
            latest.name,
            epoch,
            metric,
        )

    def xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_7(self, cfg: CheckpointConfig) -> None:
        latest = self._latest_checkpoint_path(cfg.directory)
        if latest is None:
            return
        try:
            epoch, metric = load_checkpoint(latest, self.simple.model, None)
        except Exception as exc:  # pragma: no cover - robustness guard
            LOGGER.warning("Failed to auto-resume from %s: %s", latest, exc)
            return
        self.state.epoch = int(epoch)
        self.state.best_metric = float(metric)
        LOGGER.info(
            "Auto-resumed training from checkpoint %s (epoch=%s, metric=%s)",
            latest.name,
            epoch,
            metric,
        )

    def xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_8(self, cfg: CheckpointConfig) -> None:
        latest = self._latest_checkpoint_path(cfg.directory)
        if latest is None:
            return
        try:
            epoch, metric = load_checkpoint(self.simple.model, self.simple.optimizer)
        except Exception as exc:  # pragma: no cover - robustness guard
            LOGGER.warning("Failed to auto-resume from %s: %s", latest, exc)
            return
        self.state.epoch = int(epoch)
        self.state.best_metric = float(metric)
        LOGGER.info(
            "Auto-resumed training from checkpoint %s (epoch=%s, metric=%s)",
            latest.name,
            epoch,
            metric,
        )

    def xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_9(self, cfg: CheckpointConfig) -> None:
        latest = self._latest_checkpoint_path(cfg.directory)
        if latest is None:
            return
        try:
            epoch, metric = load_checkpoint(latest, self.simple.optimizer)
        except Exception as exc:  # pragma: no cover - robustness guard
            LOGGER.warning("Failed to auto-resume from %s: %s", latest, exc)
            return
        self.state.epoch = int(epoch)
        self.state.best_metric = float(metric)
        LOGGER.info(
            "Auto-resumed training from checkpoint %s (epoch=%s, metric=%s)",
            latest.name,
            epoch,
            metric,
        )

    def xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_10(self, cfg: CheckpointConfig) -> None:
        latest = self._latest_checkpoint_path(cfg.directory)
        if latest is None:
            return
        try:
            epoch, metric = load_checkpoint(latest, self.simple.model, )
        except Exception as exc:  # pragma: no cover - robustness guard
            LOGGER.warning("Failed to auto-resume from %s: %s", latest, exc)
            return
        self.state.epoch = int(epoch)
        self.state.best_metric = float(metric)
        LOGGER.info(
            "Auto-resumed training from checkpoint %s (epoch=%s, metric=%s)",
            latest.name,
            epoch,
            metric,
        )

    def xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_11(self, cfg: CheckpointConfig) -> None:
        latest = self._latest_checkpoint_path(cfg.directory)
        if latest is None:
            return
        try:
            epoch, metric = load_checkpoint(latest, self.simple.model, self.simple.optimizer)
        except Exception as exc:  # pragma: no cover - robustness guard
            LOGGER.warning(None, latest, exc)
            return
        self.state.epoch = int(epoch)
        self.state.best_metric = float(metric)
        LOGGER.info(
            "Auto-resumed training from checkpoint %s (epoch=%s, metric=%s)",
            latest.name,
            epoch,
            metric,
        )

    def xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_12(self, cfg: CheckpointConfig) -> None:
        latest = self._latest_checkpoint_path(cfg.directory)
        if latest is None:
            return
        try:
            epoch, metric = load_checkpoint(latest, self.simple.model, self.simple.optimizer)
        except Exception as exc:  # pragma: no cover - robustness guard
            LOGGER.warning("Failed to auto-resume from %s: %s", None, exc)
            return
        self.state.epoch = int(epoch)
        self.state.best_metric = float(metric)
        LOGGER.info(
            "Auto-resumed training from checkpoint %s (epoch=%s, metric=%s)",
            latest.name,
            epoch,
            metric,
        )

    def xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_13(self, cfg: CheckpointConfig) -> None:
        latest = self._latest_checkpoint_path(cfg.directory)
        if latest is None:
            return
        try:
            epoch, metric = load_checkpoint(latest, self.simple.model, self.simple.optimizer)
        except Exception as exc:  # pragma: no cover - robustness guard
            LOGGER.warning("Failed to auto-resume from %s: %s", latest, None)
            return
        self.state.epoch = int(epoch)
        self.state.best_metric = float(metric)
        LOGGER.info(
            "Auto-resumed training from checkpoint %s (epoch=%s, metric=%s)",
            latest.name,
            epoch,
            metric,
        )

    def xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_14(self, cfg: CheckpointConfig) -> None:
        latest = self._latest_checkpoint_path(cfg.directory)
        if latest is None:
            return
        try:
            epoch, metric = load_checkpoint(latest, self.simple.model, self.simple.optimizer)
        except Exception as exc:  # pragma: no cover - robustness guard
            LOGGER.warning(latest, exc)
            return
        self.state.epoch = int(epoch)
        self.state.best_metric = float(metric)
        LOGGER.info(
            "Auto-resumed training from checkpoint %s (epoch=%s, metric=%s)",
            latest.name,
            epoch,
            metric,
        )

    def xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_15(self, cfg: CheckpointConfig) -> None:
        latest = self._latest_checkpoint_path(cfg.directory)
        if latest is None:
            return
        try:
            epoch, metric = load_checkpoint(latest, self.simple.model, self.simple.optimizer)
        except Exception as exc:  # pragma: no cover - robustness guard
            LOGGER.warning("Failed to auto-resume from %s: %s", exc)
            return
        self.state.epoch = int(epoch)
        self.state.best_metric = float(metric)
        LOGGER.info(
            "Auto-resumed training from checkpoint %s (epoch=%s, metric=%s)",
            latest.name,
            epoch,
            metric,
        )

    def xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_16(self, cfg: CheckpointConfig) -> None:
        latest = self._latest_checkpoint_path(cfg.directory)
        if latest is None:
            return
        try:
            epoch, metric = load_checkpoint(latest, self.simple.model, self.simple.optimizer)
        except Exception as exc:  # pragma: no cover - robustness guard
            LOGGER.warning("Failed to auto-resume from %s: %s", latest, )
            return
        self.state.epoch = int(epoch)
        self.state.best_metric = float(metric)
        LOGGER.info(
            "Auto-resumed training from checkpoint %s (epoch=%s, metric=%s)",
            latest.name,
            epoch,
            metric,
        )

    def xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_17(self, cfg: CheckpointConfig) -> None:
        latest = self._latest_checkpoint_path(cfg.directory)
        if latest is None:
            return
        try:
            epoch, metric = load_checkpoint(latest, self.simple.model, self.simple.optimizer)
        except Exception as exc:  # pragma: no cover - robustness guard
            LOGGER.warning("XXFailed to auto-resume from %s: %sXX", latest, exc)
            return
        self.state.epoch = int(epoch)
        self.state.best_metric = float(metric)
        LOGGER.info(
            "Auto-resumed training from checkpoint %s (epoch=%s, metric=%s)",
            latest.name,
            epoch,
            metric,
        )

    def xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_18(self, cfg: CheckpointConfig) -> None:
        latest = self._latest_checkpoint_path(cfg.directory)
        if latest is None:
            return
        try:
            epoch, metric = load_checkpoint(latest, self.simple.model, self.simple.optimizer)
        except Exception as exc:  # pragma: no cover - robustness guard
            LOGGER.warning("failed to auto-resume from %s: %s", latest, exc)
            return
        self.state.epoch = int(epoch)
        self.state.best_metric = float(metric)
        LOGGER.info(
            "Auto-resumed training from checkpoint %s (epoch=%s, metric=%s)",
            latest.name,
            epoch,
            metric,
        )

    def xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_19(self, cfg: CheckpointConfig) -> None:
        latest = self._latest_checkpoint_path(cfg.directory)
        if latest is None:
            return
        try:
            epoch, metric = load_checkpoint(latest, self.simple.model, self.simple.optimizer)
        except Exception as exc:  # pragma: no cover - robustness guard
            LOGGER.warning("FAILED TO AUTO-RESUME FROM %S: %S", latest, exc)
            return
        self.state.epoch = int(epoch)
        self.state.best_metric = float(metric)
        LOGGER.info(
            "Auto-resumed training from checkpoint %s (epoch=%s, metric=%s)",
            latest.name,
            epoch,
            metric,
        )

    def xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_20(self, cfg: CheckpointConfig) -> None:
        latest = self._latest_checkpoint_path(cfg.directory)
        if latest is None:
            return
        try:
            epoch, metric = load_checkpoint(latest, self.simple.model, self.simple.optimizer)
        except Exception as exc:  # pragma: no cover - robustness guard
            LOGGER.warning("Failed to auto-resume from %s: %s", latest, exc)
            return
        self.state.epoch = None
        self.state.best_metric = float(metric)
        LOGGER.info(
            "Auto-resumed training from checkpoint %s (epoch=%s, metric=%s)",
            latest.name,
            epoch,
            metric,
        )

    def xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_21(self, cfg: CheckpointConfig) -> None:
        latest = self._latest_checkpoint_path(cfg.directory)
        if latest is None:
            return
        try:
            epoch, metric = load_checkpoint(latest, self.simple.model, self.simple.optimizer)
        except Exception as exc:  # pragma: no cover - robustness guard
            LOGGER.warning("Failed to auto-resume from %s: %s", latest, exc)
            return
        self.state.epoch = int(None)
        self.state.best_metric = float(metric)
        LOGGER.info(
            "Auto-resumed training from checkpoint %s (epoch=%s, metric=%s)",
            latest.name,
            epoch,
            metric,
        )

    def xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_22(self, cfg: CheckpointConfig) -> None:
        latest = self._latest_checkpoint_path(cfg.directory)
        if latest is None:
            return
        try:
            epoch, metric = load_checkpoint(latest, self.simple.model, self.simple.optimizer)
        except Exception as exc:  # pragma: no cover - robustness guard
            LOGGER.warning("Failed to auto-resume from %s: %s", latest, exc)
            return
        self.state.epoch = int(epoch)
        self.state.best_metric = None
        LOGGER.info(
            "Auto-resumed training from checkpoint %s (epoch=%s, metric=%s)",
            latest.name,
            epoch,
            metric,
        )

    def xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_23(self, cfg: CheckpointConfig) -> None:
        latest = self._latest_checkpoint_path(cfg.directory)
        if latest is None:
            return
        try:
            epoch, metric = load_checkpoint(latest, self.simple.model, self.simple.optimizer)
        except Exception as exc:  # pragma: no cover - robustness guard
            LOGGER.warning("Failed to auto-resume from %s: %s", latest, exc)
            return
        self.state.epoch = int(epoch)
        self.state.best_metric = float(None)
        LOGGER.info(
            "Auto-resumed training from checkpoint %s (epoch=%s, metric=%s)",
            latest.name,
            epoch,
            metric,
        )

    def xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_24(self, cfg: CheckpointConfig) -> None:
        latest = self._latest_checkpoint_path(cfg.directory)
        if latest is None:
            return
        try:
            epoch, metric = load_checkpoint(latest, self.simple.model, self.simple.optimizer)
        except Exception as exc:  # pragma: no cover - robustness guard
            LOGGER.warning("Failed to auto-resume from %s: %s", latest, exc)
            return
        self.state.epoch = int(epoch)
        self.state.best_metric = float(metric)
        LOGGER.info(
            None,
            latest.name,
            epoch,
            metric,
        )

    def xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_25(self, cfg: CheckpointConfig) -> None:
        latest = self._latest_checkpoint_path(cfg.directory)
        if latest is None:
            return
        try:
            epoch, metric = load_checkpoint(latest, self.simple.model, self.simple.optimizer)
        except Exception as exc:  # pragma: no cover - robustness guard
            LOGGER.warning("Failed to auto-resume from %s: %s", latest, exc)
            return
        self.state.epoch = int(epoch)
        self.state.best_metric = float(metric)
        LOGGER.info(
            "Auto-resumed training from checkpoint %s (epoch=%s, metric=%s)",
            None,
            epoch,
            metric,
        )

    def xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_26(self, cfg: CheckpointConfig) -> None:
        latest = self._latest_checkpoint_path(cfg.directory)
        if latest is None:
            return
        try:
            epoch, metric = load_checkpoint(latest, self.simple.model, self.simple.optimizer)
        except Exception as exc:  # pragma: no cover - robustness guard
            LOGGER.warning("Failed to auto-resume from %s: %s", latest, exc)
            return
        self.state.epoch = int(epoch)
        self.state.best_metric = float(metric)
        LOGGER.info(
            "Auto-resumed training from checkpoint %s (epoch=%s, metric=%s)",
            latest.name,
            None,
            metric,
        )

    def xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_27(self, cfg: CheckpointConfig) -> None:
        latest = self._latest_checkpoint_path(cfg.directory)
        if latest is None:
            return
        try:
            epoch, metric = load_checkpoint(latest, self.simple.model, self.simple.optimizer)
        except Exception as exc:  # pragma: no cover - robustness guard
            LOGGER.warning("Failed to auto-resume from %s: %s", latest, exc)
            return
        self.state.epoch = int(epoch)
        self.state.best_metric = float(metric)
        LOGGER.info(
            "Auto-resumed training from checkpoint %s (epoch=%s, metric=%s)",
            latest.name,
            epoch,
            None,
        )

    def xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_28(self, cfg: CheckpointConfig) -> None:
        latest = self._latest_checkpoint_path(cfg.directory)
        if latest is None:
            return
        try:
            epoch, metric = load_checkpoint(latest, self.simple.model, self.simple.optimizer)
        except Exception as exc:  # pragma: no cover - robustness guard
            LOGGER.warning("Failed to auto-resume from %s: %s", latest, exc)
            return
        self.state.epoch = int(epoch)
        self.state.best_metric = float(metric)
        LOGGER.info(
            latest.name,
            epoch,
            metric,
        )

    def xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_29(self, cfg: CheckpointConfig) -> None:
        latest = self._latest_checkpoint_path(cfg.directory)
        if latest is None:
            return
        try:
            epoch, metric = load_checkpoint(latest, self.simple.model, self.simple.optimizer)
        except Exception as exc:  # pragma: no cover - robustness guard
            LOGGER.warning("Failed to auto-resume from %s: %s", latest, exc)
            return
        self.state.epoch = int(epoch)
        self.state.best_metric = float(metric)
        LOGGER.info(
            "Auto-resumed training from checkpoint %s (epoch=%s, metric=%s)",
            epoch,
            metric,
        )

    def xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_30(self, cfg: CheckpointConfig) -> None:
        latest = self._latest_checkpoint_path(cfg.directory)
        if latest is None:
            return
        try:
            epoch, metric = load_checkpoint(latest, self.simple.model, self.simple.optimizer)
        except Exception as exc:  # pragma: no cover - robustness guard
            LOGGER.warning("Failed to auto-resume from %s: %s", latest, exc)
            return
        self.state.epoch = int(epoch)
        self.state.best_metric = float(metric)
        LOGGER.info(
            "Auto-resumed training from checkpoint %s (epoch=%s, metric=%s)",
            latest.name,
            metric,
        )

    def xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_31(self, cfg: CheckpointConfig) -> None:
        latest = self._latest_checkpoint_path(cfg.directory)
        if latest is None:
            return
        try:
            epoch, metric = load_checkpoint(latest, self.simple.model, self.simple.optimizer)
        except Exception as exc:  # pragma: no cover - robustness guard
            LOGGER.warning("Failed to auto-resume from %s: %s", latest, exc)
            return
        self.state.epoch = int(epoch)
        self.state.best_metric = float(metric)
        LOGGER.info(
            "Auto-resumed training from checkpoint %s (epoch=%s, metric=%s)",
            latest.name,
            epoch,
            )

    def xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_32(self, cfg: CheckpointConfig) -> None:
        latest = self._latest_checkpoint_path(cfg.directory)
        if latest is None:
            return
        try:
            epoch, metric = load_checkpoint(latest, self.simple.model, self.simple.optimizer)
        except Exception as exc:  # pragma: no cover - robustness guard
            LOGGER.warning("Failed to auto-resume from %s: %s", latest, exc)
            return
        self.state.epoch = int(epoch)
        self.state.best_metric = float(metric)
        LOGGER.info(
            "XXAuto-resumed training from checkpoint %s (epoch=%s, metric=%s)XX",
            latest.name,
            epoch,
            metric,
        )

    def xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_33(self, cfg: CheckpointConfig) -> None:
        latest = self._latest_checkpoint_path(cfg.directory)
        if latest is None:
            return
        try:
            epoch, metric = load_checkpoint(latest, self.simple.model, self.simple.optimizer)
        except Exception as exc:  # pragma: no cover - robustness guard
            LOGGER.warning("Failed to auto-resume from %s: %s", latest, exc)
            return
        self.state.epoch = int(epoch)
        self.state.best_metric = float(metric)
        LOGGER.info(
            "auto-resumed training from checkpoint %s (epoch=%s, metric=%s)",
            latest.name,
            epoch,
            metric,
        )

    def xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_34(self, cfg: CheckpointConfig) -> None:
        latest = self._latest_checkpoint_path(cfg.directory)
        if latest is None:
            return
        try:
            epoch, metric = load_checkpoint(latest, self.simple.model, self.simple.optimizer)
        except Exception as exc:  # pragma: no cover - robustness guard
            LOGGER.warning("Failed to auto-resume from %s: %s", latest, exc)
            return
        self.state.epoch = int(epoch)
        self.state.best_metric = float(metric)
        LOGGER.info(
            "AUTO-RESUMED TRAINING FROM CHECKPOINT %S (EPOCH=%S, METRIC=%S)",
            latest.name,
            epoch,
            metric,
        )
    
    xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_1': xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_1, 
        'xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_2': xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_2, 
        'xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_3': xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_3, 
        'xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_4': xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_4, 
        'xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_5': xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_5, 
        'xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_6': xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_6, 
        'xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_7': xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_7, 
        'xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_8': xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_8, 
        'xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_9': xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_9, 
        'xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_10': xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_10, 
        'xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_11': xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_11, 
        'xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_12': xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_12, 
        'xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_13': xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_13, 
        'xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_14': xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_14, 
        'xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_15': xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_15, 
        'xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_16': xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_16, 
        'xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_17': xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_17, 
        'xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_18': xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_18, 
        'xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_19': xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_19, 
        'xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_20': xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_20, 
        'xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_21': xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_21, 
        'xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_22': xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_22, 
        'xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_23': xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_23, 
        'xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_24': xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_24, 
        'xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_25': xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_25, 
        'xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_26': xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_26, 
        'xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_27': xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_27, 
        'xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_28': xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_28, 
        'xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_29': xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_29, 
        'xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_30': xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_30, 
        'xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_31': xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_31, 
        'xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_32': xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_32, 
        'xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_33': xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_33, 
        'xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_34': xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_34
    }
    
    def _resume_from_latest_checkpoint(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_orig"), object.__getattribute__(self, "xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _resume_from_latest_checkpoint.__signature__ = _mutmut_signature(xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_orig)
    xǁTrainerǁ_resume_from_latest_checkpoint__mutmut_orig.__name__ = 'xǁTrainerǁ_resume_from_latest_checkpoint'

    def xǁTrainerǁ_default_loss__mutmut_orig(self, outputs: torch.Tensor, labels: torch.Tensor) -> torch.Tensor:
        logits = getattr(outputs, "logits", outputs)
        return torch.nn.functional.cross_entropy(logits, labels)

    def xǁTrainerǁ_default_loss__mutmut_1(self, outputs: torch.Tensor, labels: torch.Tensor) -> torch.Tensor:
        logits = None
        return torch.nn.functional.cross_entropy(logits, labels)

    def xǁTrainerǁ_default_loss__mutmut_2(self, outputs: torch.Tensor, labels: torch.Tensor) -> torch.Tensor:
        logits = getattr(None, "logits", outputs)
        return torch.nn.functional.cross_entropy(logits, labels)

    def xǁTrainerǁ_default_loss__mutmut_3(self, outputs: torch.Tensor, labels: torch.Tensor) -> torch.Tensor:
        logits = getattr(outputs, None, outputs)
        return torch.nn.functional.cross_entropy(logits, labels)

    def xǁTrainerǁ_default_loss__mutmut_4(self, outputs: torch.Tensor, labels: torch.Tensor) -> torch.Tensor:
        logits = getattr(outputs, "logits", None)
        return torch.nn.functional.cross_entropy(logits, labels)

    def xǁTrainerǁ_default_loss__mutmut_5(self, outputs: torch.Tensor, labels: torch.Tensor) -> torch.Tensor:
        logits = getattr("logits", outputs)
        return torch.nn.functional.cross_entropy(logits, labels)

    def xǁTrainerǁ_default_loss__mutmut_6(self, outputs: torch.Tensor, labels: torch.Tensor) -> torch.Tensor:
        logits = getattr(outputs, outputs)
        return torch.nn.functional.cross_entropy(logits, labels)

    def xǁTrainerǁ_default_loss__mutmut_7(self, outputs: torch.Tensor, labels: torch.Tensor) -> torch.Tensor:
        logits = getattr(outputs, "logits", )
        return torch.nn.functional.cross_entropy(logits, labels)

    def xǁTrainerǁ_default_loss__mutmut_8(self, outputs: torch.Tensor, labels: torch.Tensor) -> torch.Tensor:
        logits = getattr(outputs, "XXlogitsXX", outputs)
        return torch.nn.functional.cross_entropy(logits, labels)

    def xǁTrainerǁ_default_loss__mutmut_9(self, outputs: torch.Tensor, labels: torch.Tensor) -> torch.Tensor:
        logits = getattr(outputs, "LOGITS", outputs)
        return torch.nn.functional.cross_entropy(logits, labels)

    def xǁTrainerǁ_default_loss__mutmut_10(self, outputs: torch.Tensor, labels: torch.Tensor) -> torch.Tensor:
        logits = getattr(outputs, "logits", outputs)
        return torch.nn.functional.cross_entropy(None, labels)

    def xǁTrainerǁ_default_loss__mutmut_11(self, outputs: torch.Tensor, labels: torch.Tensor) -> torch.Tensor:
        logits = getattr(outputs, "logits", outputs)
        return torch.nn.functional.cross_entropy(logits, None)

    def xǁTrainerǁ_default_loss__mutmut_12(self, outputs: torch.Tensor, labels: torch.Tensor) -> torch.Tensor:
        logits = getattr(outputs, "logits", outputs)
        return torch.nn.functional.cross_entropy(labels)

    def xǁTrainerǁ_default_loss__mutmut_13(self, outputs: torch.Tensor, labels: torch.Tensor) -> torch.Tensor:
        logits = getattr(outputs, "logits", outputs)
        return torch.nn.functional.cross_entropy(logits, )
    
    xǁTrainerǁ_default_loss__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁTrainerǁ_default_loss__mutmut_1': xǁTrainerǁ_default_loss__mutmut_1, 
        'xǁTrainerǁ_default_loss__mutmut_2': xǁTrainerǁ_default_loss__mutmut_2, 
        'xǁTrainerǁ_default_loss__mutmut_3': xǁTrainerǁ_default_loss__mutmut_3, 
        'xǁTrainerǁ_default_loss__mutmut_4': xǁTrainerǁ_default_loss__mutmut_4, 
        'xǁTrainerǁ_default_loss__mutmut_5': xǁTrainerǁ_default_loss__mutmut_5, 
        'xǁTrainerǁ_default_loss__mutmut_6': xǁTrainerǁ_default_loss__mutmut_6, 
        'xǁTrainerǁ_default_loss__mutmut_7': xǁTrainerǁ_default_loss__mutmut_7, 
        'xǁTrainerǁ_default_loss__mutmut_8': xǁTrainerǁ_default_loss__mutmut_8, 
        'xǁTrainerǁ_default_loss__mutmut_9': xǁTrainerǁ_default_loss__mutmut_9, 
        'xǁTrainerǁ_default_loss__mutmut_10': xǁTrainerǁ_default_loss__mutmut_10, 
        'xǁTrainerǁ_default_loss__mutmut_11': xǁTrainerǁ_default_loss__mutmut_11, 
        'xǁTrainerǁ_default_loss__mutmut_12': xǁTrainerǁ_default_loss__mutmut_12, 
        'xǁTrainerǁ_default_loss__mutmut_13': xǁTrainerǁ_default_loss__mutmut_13
    }
    
    def _default_loss(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁTrainerǁ_default_loss__mutmut_orig"), object.__getattribute__(self, "xǁTrainerǁ_default_loss__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _default_loss.__signature__ = _mutmut_signature(xǁTrainerǁ_default_loss__mutmut_orig)
    xǁTrainerǁ_default_loss__mutmut_orig.__name__ = 'xǁTrainerǁ_default_loss'

    def xǁTrainerǁ_compute_metrics__mutmut_orig(
        self,
        outputs: torch.Tensor,
        labels: torch.Tensor,
        *,
        include_loss: bool = True,
    ) -> MutableMapping[str, float]:
        metrics: MutableMapping[str, float] = {}
        if include_loss:
            loss = self.loss_fn(outputs, labels)
            metrics["val_loss"] = float(loss.detach().cpu().item())
        if self.metric_fn is not None:
            try:
                metrics["val_metric"] = float(self.metric_fn(outputs, labels))
            except Exception as exc:  # pragma: no cover - metric robustness guard
                LOGGER.debug("Metric function failed: %s", exc)
        return metrics

    def xǁTrainerǁ_compute_metrics__mutmut_1(
        self,
        outputs: torch.Tensor,
        labels: torch.Tensor,
        *,
        include_loss: bool = False,
    ) -> MutableMapping[str, float]:
        metrics: MutableMapping[str, float] = {}
        if include_loss:
            loss = self.loss_fn(outputs, labels)
            metrics["val_loss"] = float(loss.detach().cpu().item())
        if self.metric_fn is not None:
            try:
                metrics["val_metric"] = float(self.metric_fn(outputs, labels))
            except Exception as exc:  # pragma: no cover - metric robustness guard
                LOGGER.debug("Metric function failed: %s", exc)
        return metrics

    def xǁTrainerǁ_compute_metrics__mutmut_2(
        self,
        outputs: torch.Tensor,
        labels: torch.Tensor,
        *,
        include_loss: bool = True,
    ) -> MutableMapping[str, float]:
        metrics: MutableMapping[str, float] = None
        if include_loss:
            loss = self.loss_fn(outputs, labels)
            metrics["val_loss"] = float(loss.detach().cpu().item())
        if self.metric_fn is not None:
            try:
                metrics["val_metric"] = float(self.metric_fn(outputs, labels))
            except Exception as exc:  # pragma: no cover - metric robustness guard
                LOGGER.debug("Metric function failed: %s", exc)
        return metrics

    def xǁTrainerǁ_compute_metrics__mutmut_3(
        self,
        outputs: torch.Tensor,
        labels: torch.Tensor,
        *,
        include_loss: bool = True,
    ) -> MutableMapping[str, float]:
        metrics: MutableMapping[str, float] = {}
        if include_loss:
            loss = None
            metrics["val_loss"] = float(loss.detach().cpu().item())
        if self.metric_fn is not None:
            try:
                metrics["val_metric"] = float(self.metric_fn(outputs, labels))
            except Exception as exc:  # pragma: no cover - metric robustness guard
                LOGGER.debug("Metric function failed: %s", exc)
        return metrics

    def xǁTrainerǁ_compute_metrics__mutmut_4(
        self,
        outputs: torch.Tensor,
        labels: torch.Tensor,
        *,
        include_loss: bool = True,
    ) -> MutableMapping[str, float]:
        metrics: MutableMapping[str, float] = {}
        if include_loss:
            loss = self.loss_fn(None, labels)
            metrics["val_loss"] = float(loss.detach().cpu().item())
        if self.metric_fn is not None:
            try:
                metrics["val_metric"] = float(self.metric_fn(outputs, labels))
            except Exception as exc:  # pragma: no cover - metric robustness guard
                LOGGER.debug("Metric function failed: %s", exc)
        return metrics

    def xǁTrainerǁ_compute_metrics__mutmut_5(
        self,
        outputs: torch.Tensor,
        labels: torch.Tensor,
        *,
        include_loss: bool = True,
    ) -> MutableMapping[str, float]:
        metrics: MutableMapping[str, float] = {}
        if include_loss:
            loss = self.loss_fn(outputs, None)
            metrics["val_loss"] = float(loss.detach().cpu().item())
        if self.metric_fn is not None:
            try:
                metrics["val_metric"] = float(self.metric_fn(outputs, labels))
            except Exception as exc:  # pragma: no cover - metric robustness guard
                LOGGER.debug("Metric function failed: %s", exc)
        return metrics

    def xǁTrainerǁ_compute_metrics__mutmut_6(
        self,
        outputs: torch.Tensor,
        labels: torch.Tensor,
        *,
        include_loss: bool = True,
    ) -> MutableMapping[str, float]:
        metrics: MutableMapping[str, float] = {}
        if include_loss:
            loss = self.loss_fn(labels)
            metrics["val_loss"] = float(loss.detach().cpu().item())
        if self.metric_fn is not None:
            try:
                metrics["val_metric"] = float(self.metric_fn(outputs, labels))
            except Exception as exc:  # pragma: no cover - metric robustness guard
                LOGGER.debug("Metric function failed: %s", exc)
        return metrics

    def xǁTrainerǁ_compute_metrics__mutmut_7(
        self,
        outputs: torch.Tensor,
        labels: torch.Tensor,
        *,
        include_loss: bool = True,
    ) -> MutableMapping[str, float]:
        metrics: MutableMapping[str, float] = {}
        if include_loss:
            loss = self.loss_fn(outputs, )
            metrics["val_loss"] = float(loss.detach().cpu().item())
        if self.metric_fn is not None:
            try:
                metrics["val_metric"] = float(self.metric_fn(outputs, labels))
            except Exception as exc:  # pragma: no cover - metric robustness guard
                LOGGER.debug("Metric function failed: %s", exc)
        return metrics

    def xǁTrainerǁ_compute_metrics__mutmut_8(
        self,
        outputs: torch.Tensor,
        labels: torch.Tensor,
        *,
        include_loss: bool = True,
    ) -> MutableMapping[str, float]:
        metrics: MutableMapping[str, float] = {}
        if include_loss:
            loss = self.loss_fn(outputs, labels)
            metrics["val_loss"] = None
        if self.metric_fn is not None:
            try:
                metrics["val_metric"] = float(self.metric_fn(outputs, labels))
            except Exception as exc:  # pragma: no cover - metric robustness guard
                LOGGER.debug("Metric function failed: %s", exc)
        return metrics

    def xǁTrainerǁ_compute_metrics__mutmut_9(
        self,
        outputs: torch.Tensor,
        labels: torch.Tensor,
        *,
        include_loss: bool = True,
    ) -> MutableMapping[str, float]:
        metrics: MutableMapping[str, float] = {}
        if include_loss:
            loss = self.loss_fn(outputs, labels)
            metrics["XXval_lossXX"] = float(loss.detach().cpu().item())
        if self.metric_fn is not None:
            try:
                metrics["val_metric"] = float(self.metric_fn(outputs, labels))
            except Exception as exc:  # pragma: no cover - metric robustness guard
                LOGGER.debug("Metric function failed: %s", exc)
        return metrics

    def xǁTrainerǁ_compute_metrics__mutmut_10(
        self,
        outputs: torch.Tensor,
        labels: torch.Tensor,
        *,
        include_loss: bool = True,
    ) -> MutableMapping[str, float]:
        metrics: MutableMapping[str, float] = {}
        if include_loss:
            loss = self.loss_fn(outputs, labels)
            metrics["VAL_LOSS"] = float(loss.detach().cpu().item())
        if self.metric_fn is not None:
            try:
                metrics["val_metric"] = float(self.metric_fn(outputs, labels))
            except Exception as exc:  # pragma: no cover - metric robustness guard
                LOGGER.debug("Metric function failed: %s", exc)
        return metrics

    def xǁTrainerǁ_compute_metrics__mutmut_11(
        self,
        outputs: torch.Tensor,
        labels: torch.Tensor,
        *,
        include_loss: bool = True,
    ) -> MutableMapping[str, float]:
        metrics: MutableMapping[str, float] = {}
        if include_loss:
            loss = self.loss_fn(outputs, labels)
            metrics["val_loss"] = float(None)
        if self.metric_fn is not None:
            try:
                metrics["val_metric"] = float(self.metric_fn(outputs, labels))
            except Exception as exc:  # pragma: no cover - metric robustness guard
                LOGGER.debug("Metric function failed: %s", exc)
        return metrics

    def xǁTrainerǁ_compute_metrics__mutmut_12(
        self,
        outputs: torch.Tensor,
        labels: torch.Tensor,
        *,
        include_loss: bool = True,
    ) -> MutableMapping[str, float]:
        metrics: MutableMapping[str, float] = {}
        if include_loss:
            loss = self.loss_fn(outputs, labels)
            metrics["val_loss"] = float(loss.detach().cpu().item())
        if self.metric_fn is None:
            try:
                metrics["val_metric"] = float(self.metric_fn(outputs, labels))
            except Exception as exc:  # pragma: no cover - metric robustness guard
                LOGGER.debug("Metric function failed: %s", exc)
        return metrics

    def xǁTrainerǁ_compute_metrics__mutmut_13(
        self,
        outputs: torch.Tensor,
        labels: torch.Tensor,
        *,
        include_loss: bool = True,
    ) -> MutableMapping[str, float]:
        metrics: MutableMapping[str, float] = {}
        if include_loss:
            loss = self.loss_fn(outputs, labels)
            metrics["val_loss"] = float(loss.detach().cpu().item())
        if self.metric_fn is not None:
            try:
                metrics["val_metric"] = None
            except Exception as exc:  # pragma: no cover - metric robustness guard
                LOGGER.debug("Metric function failed: %s", exc)
        return metrics

    def xǁTrainerǁ_compute_metrics__mutmut_14(
        self,
        outputs: torch.Tensor,
        labels: torch.Tensor,
        *,
        include_loss: bool = True,
    ) -> MutableMapping[str, float]:
        metrics: MutableMapping[str, float] = {}
        if include_loss:
            loss = self.loss_fn(outputs, labels)
            metrics["val_loss"] = float(loss.detach().cpu().item())
        if self.metric_fn is not None:
            try:
                metrics["XXval_metricXX"] = float(self.metric_fn(outputs, labels))
            except Exception as exc:  # pragma: no cover - metric robustness guard
                LOGGER.debug("Metric function failed: %s", exc)
        return metrics

    def xǁTrainerǁ_compute_metrics__mutmut_15(
        self,
        outputs: torch.Tensor,
        labels: torch.Tensor,
        *,
        include_loss: bool = True,
    ) -> MutableMapping[str, float]:
        metrics: MutableMapping[str, float] = {}
        if include_loss:
            loss = self.loss_fn(outputs, labels)
            metrics["val_loss"] = float(loss.detach().cpu().item())
        if self.metric_fn is not None:
            try:
                metrics["VAL_METRIC"] = float(self.metric_fn(outputs, labels))
            except Exception as exc:  # pragma: no cover - metric robustness guard
                LOGGER.debug("Metric function failed: %s", exc)
        return metrics

    def xǁTrainerǁ_compute_metrics__mutmut_16(
        self,
        outputs: torch.Tensor,
        labels: torch.Tensor,
        *,
        include_loss: bool = True,
    ) -> MutableMapping[str, float]:
        metrics: MutableMapping[str, float] = {}
        if include_loss:
            loss = self.loss_fn(outputs, labels)
            metrics["val_loss"] = float(loss.detach().cpu().item())
        if self.metric_fn is not None:
            try:
                metrics["val_metric"] = float(None)
            except Exception as exc:  # pragma: no cover - metric robustness guard
                LOGGER.debug("Metric function failed: %s", exc)
        return metrics

    def xǁTrainerǁ_compute_metrics__mutmut_17(
        self,
        outputs: torch.Tensor,
        labels: torch.Tensor,
        *,
        include_loss: bool = True,
    ) -> MutableMapping[str, float]:
        metrics: MutableMapping[str, float] = {}
        if include_loss:
            loss = self.loss_fn(outputs, labels)
            metrics["val_loss"] = float(loss.detach().cpu().item())
        if self.metric_fn is not None:
            try:
                metrics["val_metric"] = float(self.metric_fn(None, labels))
            except Exception as exc:  # pragma: no cover - metric robustness guard
                LOGGER.debug("Metric function failed: %s", exc)
        return metrics

    def xǁTrainerǁ_compute_metrics__mutmut_18(
        self,
        outputs: torch.Tensor,
        labels: torch.Tensor,
        *,
        include_loss: bool = True,
    ) -> MutableMapping[str, float]:
        metrics: MutableMapping[str, float] = {}
        if include_loss:
            loss = self.loss_fn(outputs, labels)
            metrics["val_loss"] = float(loss.detach().cpu().item())
        if self.metric_fn is not None:
            try:
                metrics["val_metric"] = float(self.metric_fn(outputs, None))
            except Exception as exc:  # pragma: no cover - metric robustness guard
                LOGGER.debug("Metric function failed: %s", exc)
        return metrics

    def xǁTrainerǁ_compute_metrics__mutmut_19(
        self,
        outputs: torch.Tensor,
        labels: torch.Tensor,
        *,
        include_loss: bool = True,
    ) -> MutableMapping[str, float]:
        metrics: MutableMapping[str, float] = {}
        if include_loss:
            loss = self.loss_fn(outputs, labels)
            metrics["val_loss"] = float(loss.detach().cpu().item())
        if self.metric_fn is not None:
            try:
                metrics["val_metric"] = float(self.metric_fn(labels))
            except Exception as exc:  # pragma: no cover - metric robustness guard
                LOGGER.debug("Metric function failed: %s", exc)
        return metrics

    def xǁTrainerǁ_compute_metrics__mutmut_20(
        self,
        outputs: torch.Tensor,
        labels: torch.Tensor,
        *,
        include_loss: bool = True,
    ) -> MutableMapping[str, float]:
        metrics: MutableMapping[str, float] = {}
        if include_loss:
            loss = self.loss_fn(outputs, labels)
            metrics["val_loss"] = float(loss.detach().cpu().item())
        if self.metric_fn is not None:
            try:
                metrics["val_metric"] = float(self.metric_fn(outputs, ))
            except Exception as exc:  # pragma: no cover - metric robustness guard
                LOGGER.debug("Metric function failed: %s", exc)
        return metrics

    def xǁTrainerǁ_compute_metrics__mutmut_21(
        self,
        outputs: torch.Tensor,
        labels: torch.Tensor,
        *,
        include_loss: bool = True,
    ) -> MutableMapping[str, float]:
        metrics: MutableMapping[str, float] = {}
        if include_loss:
            loss = self.loss_fn(outputs, labels)
            metrics["val_loss"] = float(loss.detach().cpu().item())
        if self.metric_fn is not None:
            try:
                metrics["val_metric"] = float(self.metric_fn(outputs, labels))
            except Exception as exc:  # pragma: no cover - metric robustness guard
                LOGGER.debug(None, exc)
        return metrics

    def xǁTrainerǁ_compute_metrics__mutmut_22(
        self,
        outputs: torch.Tensor,
        labels: torch.Tensor,
        *,
        include_loss: bool = True,
    ) -> MutableMapping[str, float]:
        metrics: MutableMapping[str, float] = {}
        if include_loss:
            loss = self.loss_fn(outputs, labels)
            metrics["val_loss"] = float(loss.detach().cpu().item())
        if self.metric_fn is not None:
            try:
                metrics["val_metric"] = float(self.metric_fn(outputs, labels))
            except Exception as exc:  # pragma: no cover - metric robustness guard
                LOGGER.debug("Metric function failed: %s", None)
        return metrics

    def xǁTrainerǁ_compute_metrics__mutmut_23(
        self,
        outputs: torch.Tensor,
        labels: torch.Tensor,
        *,
        include_loss: bool = True,
    ) -> MutableMapping[str, float]:
        metrics: MutableMapping[str, float] = {}
        if include_loss:
            loss = self.loss_fn(outputs, labels)
            metrics["val_loss"] = float(loss.detach().cpu().item())
        if self.metric_fn is not None:
            try:
                metrics["val_metric"] = float(self.metric_fn(outputs, labels))
            except Exception as exc:  # pragma: no cover - metric robustness guard
                LOGGER.debug(exc)
        return metrics

    def xǁTrainerǁ_compute_metrics__mutmut_24(
        self,
        outputs: torch.Tensor,
        labels: torch.Tensor,
        *,
        include_loss: bool = True,
    ) -> MutableMapping[str, float]:
        metrics: MutableMapping[str, float] = {}
        if include_loss:
            loss = self.loss_fn(outputs, labels)
            metrics["val_loss"] = float(loss.detach().cpu().item())
        if self.metric_fn is not None:
            try:
                metrics["val_metric"] = float(self.metric_fn(outputs, labels))
            except Exception as exc:  # pragma: no cover - metric robustness guard
                LOGGER.debug("Metric function failed: %s", )
        return metrics

    def xǁTrainerǁ_compute_metrics__mutmut_25(
        self,
        outputs: torch.Tensor,
        labels: torch.Tensor,
        *,
        include_loss: bool = True,
    ) -> MutableMapping[str, float]:
        metrics: MutableMapping[str, float] = {}
        if include_loss:
            loss = self.loss_fn(outputs, labels)
            metrics["val_loss"] = float(loss.detach().cpu().item())
        if self.metric_fn is not None:
            try:
                metrics["val_metric"] = float(self.metric_fn(outputs, labels))
            except Exception as exc:  # pragma: no cover - metric robustness guard
                LOGGER.debug("XXMetric function failed: %sXX", exc)
        return metrics

    def xǁTrainerǁ_compute_metrics__mutmut_26(
        self,
        outputs: torch.Tensor,
        labels: torch.Tensor,
        *,
        include_loss: bool = True,
    ) -> MutableMapping[str, float]:
        metrics: MutableMapping[str, float] = {}
        if include_loss:
            loss = self.loss_fn(outputs, labels)
            metrics["val_loss"] = float(loss.detach().cpu().item())
        if self.metric_fn is not None:
            try:
                metrics["val_metric"] = float(self.metric_fn(outputs, labels))
            except Exception as exc:  # pragma: no cover - metric robustness guard
                LOGGER.debug("metric function failed: %s", exc)
        return metrics

    def xǁTrainerǁ_compute_metrics__mutmut_27(
        self,
        outputs: torch.Tensor,
        labels: torch.Tensor,
        *,
        include_loss: bool = True,
    ) -> MutableMapping[str, float]:
        metrics: MutableMapping[str, float] = {}
        if include_loss:
            loss = self.loss_fn(outputs, labels)
            metrics["val_loss"] = float(loss.detach().cpu().item())
        if self.metric_fn is not None:
            try:
                metrics["val_metric"] = float(self.metric_fn(outputs, labels))
            except Exception as exc:  # pragma: no cover - metric robustness guard
                LOGGER.debug("METRIC FUNCTION FAILED: %S", exc)
        return metrics
    
    xǁTrainerǁ_compute_metrics__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁTrainerǁ_compute_metrics__mutmut_1': xǁTrainerǁ_compute_metrics__mutmut_1, 
        'xǁTrainerǁ_compute_metrics__mutmut_2': xǁTrainerǁ_compute_metrics__mutmut_2, 
        'xǁTrainerǁ_compute_metrics__mutmut_3': xǁTrainerǁ_compute_metrics__mutmut_3, 
        'xǁTrainerǁ_compute_metrics__mutmut_4': xǁTrainerǁ_compute_metrics__mutmut_4, 
        'xǁTrainerǁ_compute_metrics__mutmut_5': xǁTrainerǁ_compute_metrics__mutmut_5, 
        'xǁTrainerǁ_compute_metrics__mutmut_6': xǁTrainerǁ_compute_metrics__mutmut_6, 
        'xǁTrainerǁ_compute_metrics__mutmut_7': xǁTrainerǁ_compute_metrics__mutmut_7, 
        'xǁTrainerǁ_compute_metrics__mutmut_8': xǁTrainerǁ_compute_metrics__mutmut_8, 
        'xǁTrainerǁ_compute_metrics__mutmut_9': xǁTrainerǁ_compute_metrics__mutmut_9, 
        'xǁTrainerǁ_compute_metrics__mutmut_10': xǁTrainerǁ_compute_metrics__mutmut_10, 
        'xǁTrainerǁ_compute_metrics__mutmut_11': xǁTrainerǁ_compute_metrics__mutmut_11, 
        'xǁTrainerǁ_compute_metrics__mutmut_12': xǁTrainerǁ_compute_metrics__mutmut_12, 
        'xǁTrainerǁ_compute_metrics__mutmut_13': xǁTrainerǁ_compute_metrics__mutmut_13, 
        'xǁTrainerǁ_compute_metrics__mutmut_14': xǁTrainerǁ_compute_metrics__mutmut_14, 
        'xǁTrainerǁ_compute_metrics__mutmut_15': xǁTrainerǁ_compute_metrics__mutmut_15, 
        'xǁTrainerǁ_compute_metrics__mutmut_16': xǁTrainerǁ_compute_metrics__mutmut_16, 
        'xǁTrainerǁ_compute_metrics__mutmut_17': xǁTrainerǁ_compute_metrics__mutmut_17, 
        'xǁTrainerǁ_compute_metrics__mutmut_18': xǁTrainerǁ_compute_metrics__mutmut_18, 
        'xǁTrainerǁ_compute_metrics__mutmut_19': xǁTrainerǁ_compute_metrics__mutmut_19, 
        'xǁTrainerǁ_compute_metrics__mutmut_20': xǁTrainerǁ_compute_metrics__mutmut_20, 
        'xǁTrainerǁ_compute_metrics__mutmut_21': xǁTrainerǁ_compute_metrics__mutmut_21, 
        'xǁTrainerǁ_compute_metrics__mutmut_22': xǁTrainerǁ_compute_metrics__mutmut_22, 
        'xǁTrainerǁ_compute_metrics__mutmut_23': xǁTrainerǁ_compute_metrics__mutmut_23, 
        'xǁTrainerǁ_compute_metrics__mutmut_24': xǁTrainerǁ_compute_metrics__mutmut_24, 
        'xǁTrainerǁ_compute_metrics__mutmut_25': xǁTrainerǁ_compute_metrics__mutmut_25, 
        'xǁTrainerǁ_compute_metrics__mutmut_26': xǁTrainerǁ_compute_metrics__mutmut_26, 
        'xǁTrainerǁ_compute_metrics__mutmut_27': xǁTrainerǁ_compute_metrics__mutmut_27
    }
    
    def _compute_metrics(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁTrainerǁ_compute_metrics__mutmut_orig"), object.__getattribute__(self, "xǁTrainerǁ_compute_metrics__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _compute_metrics.__signature__ = _mutmut_signature(xǁTrainerǁ_compute_metrics__mutmut_orig)
    xǁTrainerǁ_compute_metrics__mutmut_orig.__name__ = 'xǁTrainerǁ_compute_metrics'

    def xǁTrainerǁ_should_replace__mutmut_orig(self, new_metric: float) -> bool:
        if self.config.checkpoint is None:
            return False
        if self.state.best_metric is None:
            return True
        mode = self.config.checkpoint.mode.lower()
        if mode not in {"min", "max"}:
            raise ValueError("checkpoint.mode must be 'min' or 'max'")
        if mode == "min":
            return new_metric < self.state.best_metric
        return new_metric > self.state.best_metric

    def xǁTrainerǁ_should_replace__mutmut_1(self, new_metric: float) -> bool:
        if self.config.checkpoint is not None:
            return False
        if self.state.best_metric is None:
            return True
        mode = self.config.checkpoint.mode.lower()
        if mode not in {"min", "max"}:
            raise ValueError("checkpoint.mode must be 'min' or 'max'")
        if mode == "min":
            return new_metric < self.state.best_metric
        return new_metric > self.state.best_metric

    def xǁTrainerǁ_should_replace__mutmut_2(self, new_metric: float) -> bool:
        if self.config.checkpoint is None:
            return True
        if self.state.best_metric is None:
            return True
        mode = self.config.checkpoint.mode.lower()
        if mode not in {"min", "max"}:
            raise ValueError("checkpoint.mode must be 'min' or 'max'")
        if mode == "min":
            return new_metric < self.state.best_metric
        return new_metric > self.state.best_metric

    def xǁTrainerǁ_should_replace__mutmut_3(self, new_metric: float) -> bool:
        if self.config.checkpoint is None:
            return False
        if self.state.best_metric is not None:
            return True
        mode = self.config.checkpoint.mode.lower()
        if mode not in {"min", "max"}:
            raise ValueError("checkpoint.mode must be 'min' or 'max'")
        if mode == "min":
            return new_metric < self.state.best_metric
        return new_metric > self.state.best_metric

    def xǁTrainerǁ_should_replace__mutmut_4(self, new_metric: float) -> bool:
        if self.config.checkpoint is None:
            return False
        if self.state.best_metric is None:
            return False
        mode = self.config.checkpoint.mode.lower()
        if mode not in {"min", "max"}:
            raise ValueError("checkpoint.mode must be 'min' or 'max'")
        if mode == "min":
            return new_metric < self.state.best_metric
        return new_metric > self.state.best_metric

    def xǁTrainerǁ_should_replace__mutmut_5(self, new_metric: float) -> bool:
        if self.config.checkpoint is None:
            return False
        if self.state.best_metric is None:
            return True
        mode = None
        if mode not in {"min", "max"}:
            raise ValueError("checkpoint.mode must be 'min' or 'max'")
        if mode == "min":
            return new_metric < self.state.best_metric
        return new_metric > self.state.best_metric

    def xǁTrainerǁ_should_replace__mutmut_6(self, new_metric: float) -> bool:
        if self.config.checkpoint is None:
            return False
        if self.state.best_metric is None:
            return True
        mode = self.config.checkpoint.mode.upper()
        if mode not in {"min", "max"}:
            raise ValueError("checkpoint.mode must be 'min' or 'max'")
        if mode == "min":
            return new_metric < self.state.best_metric
        return new_metric > self.state.best_metric

    def xǁTrainerǁ_should_replace__mutmut_7(self, new_metric: float) -> bool:
        if self.config.checkpoint is None:
            return False
        if self.state.best_metric is None:
            return True
        mode = self.config.checkpoint.mode.lower()
        if mode in {"min", "max"}:
            raise ValueError("checkpoint.mode must be 'min' or 'max'")
        if mode == "min":
            return new_metric < self.state.best_metric
        return new_metric > self.state.best_metric

    def xǁTrainerǁ_should_replace__mutmut_8(self, new_metric: float) -> bool:
        if self.config.checkpoint is None:
            return False
        if self.state.best_metric is None:
            return True
        mode = self.config.checkpoint.mode.lower()
        if mode not in {"XXminXX", "max"}:
            raise ValueError("checkpoint.mode must be 'min' or 'max'")
        if mode == "min":
            return new_metric < self.state.best_metric
        return new_metric > self.state.best_metric

    def xǁTrainerǁ_should_replace__mutmut_9(self, new_metric: float) -> bool:
        if self.config.checkpoint is None:
            return False
        if self.state.best_metric is None:
            return True
        mode = self.config.checkpoint.mode.lower()
        if mode not in {"MIN", "max"}:
            raise ValueError("checkpoint.mode must be 'min' or 'max'")
        if mode == "min":
            return new_metric < self.state.best_metric
        return new_metric > self.state.best_metric

    def xǁTrainerǁ_should_replace__mutmut_10(self, new_metric: float) -> bool:
        if self.config.checkpoint is None:
            return False
        if self.state.best_metric is None:
            return True
        mode = self.config.checkpoint.mode.lower()
        if mode not in {"min", "XXmaxXX"}:
            raise ValueError("checkpoint.mode must be 'min' or 'max'")
        if mode == "min":
            return new_metric < self.state.best_metric
        return new_metric > self.state.best_metric

    def xǁTrainerǁ_should_replace__mutmut_11(self, new_metric: float) -> bool:
        if self.config.checkpoint is None:
            return False
        if self.state.best_metric is None:
            return True
        mode = self.config.checkpoint.mode.lower()
        if mode not in {"min", "MAX"}:
            raise ValueError("checkpoint.mode must be 'min' or 'max'")
        if mode == "min":
            return new_metric < self.state.best_metric
        return new_metric > self.state.best_metric

    def xǁTrainerǁ_should_replace__mutmut_12(self, new_metric: float) -> bool:
        if self.config.checkpoint is None:
            return False
        if self.state.best_metric is None:
            return True
        mode = self.config.checkpoint.mode.lower()
        if mode not in {"min", "max"}:
            raise ValueError(None)
        if mode == "min":
            return new_metric < self.state.best_metric
        return new_metric > self.state.best_metric

    def xǁTrainerǁ_should_replace__mutmut_13(self, new_metric: float) -> bool:
        if self.config.checkpoint is None:
            return False
        if self.state.best_metric is None:
            return True
        mode = self.config.checkpoint.mode.lower()
        if mode not in {"min", "max"}:
            raise ValueError("XXcheckpoint.mode must be 'min' or 'max'XX")
        if mode == "min":
            return new_metric < self.state.best_metric
        return new_metric > self.state.best_metric

    def xǁTrainerǁ_should_replace__mutmut_14(self, new_metric: float) -> bool:
        if self.config.checkpoint is None:
            return False
        if self.state.best_metric is None:
            return True
        mode = self.config.checkpoint.mode.lower()
        if mode not in {"min", "max"}:
            raise ValueError("CHECKPOINT.MODE MUST BE 'MIN' OR 'MAX'")
        if mode == "min":
            return new_metric < self.state.best_metric
        return new_metric > self.state.best_metric

    def xǁTrainerǁ_should_replace__mutmut_15(self, new_metric: float) -> bool:
        if self.config.checkpoint is None:
            return False
        if self.state.best_metric is None:
            return True
        mode = self.config.checkpoint.mode.lower()
        if mode not in {"min", "max"}:
            raise ValueError("checkpoint.mode must be 'min' or 'max'")
        if mode != "min":
            return new_metric < self.state.best_metric
        return new_metric > self.state.best_metric

    def xǁTrainerǁ_should_replace__mutmut_16(self, new_metric: float) -> bool:
        if self.config.checkpoint is None:
            return False
        if self.state.best_metric is None:
            return True
        mode = self.config.checkpoint.mode.lower()
        if mode not in {"min", "max"}:
            raise ValueError("checkpoint.mode must be 'min' or 'max'")
        if mode == "XXminXX":
            return new_metric < self.state.best_metric
        return new_metric > self.state.best_metric

    def xǁTrainerǁ_should_replace__mutmut_17(self, new_metric: float) -> bool:
        if self.config.checkpoint is None:
            return False
        if self.state.best_metric is None:
            return True
        mode = self.config.checkpoint.mode.lower()
        if mode not in {"min", "max"}:
            raise ValueError("checkpoint.mode must be 'min' or 'max'")
        if mode == "MIN":
            return new_metric < self.state.best_metric
        return new_metric > self.state.best_metric

    def xǁTrainerǁ_should_replace__mutmut_18(self, new_metric: float) -> bool:
        if self.config.checkpoint is None:
            return False
        if self.state.best_metric is None:
            return True
        mode = self.config.checkpoint.mode.lower()
        if mode not in {"min", "max"}:
            raise ValueError("checkpoint.mode must be 'min' or 'max'")
        if mode == "min":
            return new_metric <= self.state.best_metric
        return new_metric > self.state.best_metric

    def xǁTrainerǁ_should_replace__mutmut_19(self, new_metric: float) -> bool:
        if self.config.checkpoint is None:
            return False
        if self.state.best_metric is None:
            return True
        mode = self.config.checkpoint.mode.lower()
        if mode not in {"min", "max"}:
            raise ValueError("checkpoint.mode must be 'min' or 'max'")
        if mode == "min":
            return new_metric < self.state.best_metric
        return new_metric >= self.state.best_metric
    
    xǁTrainerǁ_should_replace__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁTrainerǁ_should_replace__mutmut_1': xǁTrainerǁ_should_replace__mutmut_1, 
        'xǁTrainerǁ_should_replace__mutmut_2': xǁTrainerǁ_should_replace__mutmut_2, 
        'xǁTrainerǁ_should_replace__mutmut_3': xǁTrainerǁ_should_replace__mutmut_3, 
        'xǁTrainerǁ_should_replace__mutmut_4': xǁTrainerǁ_should_replace__mutmut_4, 
        'xǁTrainerǁ_should_replace__mutmut_5': xǁTrainerǁ_should_replace__mutmut_5, 
        'xǁTrainerǁ_should_replace__mutmut_6': xǁTrainerǁ_should_replace__mutmut_6, 
        'xǁTrainerǁ_should_replace__mutmut_7': xǁTrainerǁ_should_replace__mutmut_7, 
        'xǁTrainerǁ_should_replace__mutmut_8': xǁTrainerǁ_should_replace__mutmut_8, 
        'xǁTrainerǁ_should_replace__mutmut_9': xǁTrainerǁ_should_replace__mutmut_9, 
        'xǁTrainerǁ_should_replace__mutmut_10': xǁTrainerǁ_should_replace__mutmut_10, 
        'xǁTrainerǁ_should_replace__mutmut_11': xǁTrainerǁ_should_replace__mutmut_11, 
        'xǁTrainerǁ_should_replace__mutmut_12': xǁTrainerǁ_should_replace__mutmut_12, 
        'xǁTrainerǁ_should_replace__mutmut_13': xǁTrainerǁ_should_replace__mutmut_13, 
        'xǁTrainerǁ_should_replace__mutmut_14': xǁTrainerǁ_should_replace__mutmut_14, 
        'xǁTrainerǁ_should_replace__mutmut_15': xǁTrainerǁ_should_replace__mutmut_15, 
        'xǁTrainerǁ_should_replace__mutmut_16': xǁTrainerǁ_should_replace__mutmut_16, 
        'xǁTrainerǁ_should_replace__mutmut_17': xǁTrainerǁ_should_replace__mutmut_17, 
        'xǁTrainerǁ_should_replace__mutmut_18': xǁTrainerǁ_should_replace__mutmut_18, 
        'xǁTrainerǁ_should_replace__mutmut_19': xǁTrainerǁ_should_replace__mutmut_19
    }
    
    def _should_replace(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁTrainerǁ_should_replace__mutmut_orig"), object.__getattribute__(self, "xǁTrainerǁ_should_replace__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _should_replace.__signature__ = _mutmut_signature(xǁTrainerǁ_should_replace__mutmut_orig)
    xǁTrainerǁ_should_replace__mutmut_orig.__name__ = 'xǁTrainerǁ_should_replace'

    def xǁTrainerǁ_monitor_value__mutmut_orig(self, metrics: Mapping[str, float]) -> float | None:
        monitor_key = (
            self.config.checkpoint.monitor if self.config.checkpoint else None
        ) or "val_loss"
        return metrics.get(monitor_key)

    def xǁTrainerǁ_monitor_value__mutmut_1(self, metrics: Mapping[str, float]) -> float | None:
        monitor_key = None
        return metrics.get(monitor_key)

    def xǁTrainerǁ_monitor_value__mutmut_2(self, metrics: Mapping[str, float]) -> float | None:
        monitor_key = (
            self.config.checkpoint.monitor if self.config.checkpoint else None
        ) and "val_loss"
        return metrics.get(monitor_key)

    def xǁTrainerǁ_monitor_value__mutmut_3(self, metrics: Mapping[str, float]) -> float | None:
        monitor_key = (
            self.config.checkpoint.monitor if self.config.checkpoint else None
        ) or "XXval_lossXX"
        return metrics.get(monitor_key)

    def xǁTrainerǁ_monitor_value__mutmut_4(self, metrics: Mapping[str, float]) -> float | None:
        monitor_key = (
            self.config.checkpoint.monitor if self.config.checkpoint else None
        ) or "VAL_LOSS"
        return metrics.get(monitor_key)

    def xǁTrainerǁ_monitor_value__mutmut_5(self, metrics: Mapping[str, float]) -> float | None:
        monitor_key = (
            self.config.checkpoint.monitor if self.config.checkpoint else None
        ) or "val_loss"
        return metrics.get(None)
    
    xǁTrainerǁ_monitor_value__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁTrainerǁ_monitor_value__mutmut_1': xǁTrainerǁ_monitor_value__mutmut_1, 
        'xǁTrainerǁ_monitor_value__mutmut_2': xǁTrainerǁ_monitor_value__mutmut_2, 
        'xǁTrainerǁ_monitor_value__mutmut_3': xǁTrainerǁ_monitor_value__mutmut_3, 
        'xǁTrainerǁ_monitor_value__mutmut_4': xǁTrainerǁ_monitor_value__mutmut_4, 
        'xǁTrainerǁ_monitor_value__mutmut_5': xǁTrainerǁ_monitor_value__mutmut_5
    }
    
    def _monitor_value(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁTrainerǁ_monitor_value__mutmut_orig"), object.__getattribute__(self, "xǁTrainerǁ_monitor_value__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _monitor_value.__signature__ = _mutmut_signature(xǁTrainerǁ_monitor_value__mutmut_orig)
    xǁTrainerǁ_monitor_value__mutmut_orig.__name__ = 'xǁTrainerǁ_monitor_value'

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_orig(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_1(self, directory: Path) -> None:
        cfg = None
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_2(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is not None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_3(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_4(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(None):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_5(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob(None)):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_6(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("XXepoch_*.jsonXX")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_7(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("EPOCH_*.JSON")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_8(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = None
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_9(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(None)
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_10(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding=None))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_11(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="XXutf-8XX"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_12(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="UTF-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_13(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(None)
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_14(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug(None, meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_15(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", None, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_16(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, None)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_17(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug(meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_18(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_19(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, )
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_20(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("XXSkipping checkpoint metadata %s: %sXX", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_21(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_22(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("SKIPPING CHECKPOINT METADATA %S: %S", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_23(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                break
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_24(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = None
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_25(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get(None)
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_26(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("XXmonitorXX")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_27(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("MONITOR")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_28(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is not None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_29(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                break
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_30(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = None
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_31(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(None)
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_32(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix("XX.ptXX")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_33(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".PT")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_34(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_35(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                break
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_36(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = None
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_37(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(None)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_38(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug(None, exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_39(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=None)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_40(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug(exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_41(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", )
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_42(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("XXException caught, continuingXX", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_43(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_44(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("EXCEPTION CAUGHT, CONTINUING", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_45(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=False)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_46(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                break
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_47(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append(None)
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_48(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_49(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(None)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_50(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = None
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_51(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.upper() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_52(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() != "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_53(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "XXmaxXX"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_54(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "MAX"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_55(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=None, reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_56(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=None)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_57(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_58(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], )
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_59(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: None, reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_60(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[1], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_61(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = None

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_62(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[1][0]

    def xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_63(self, directory: Path) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        entries: list[tuple[float, Path, Path]] = []
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            monitor_value = data.get("monitor")
            if monitor_value is None:
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            try:
                monitor_float = float(monitor_value)
            except (TypeError, ValueError):
                logger.debug("Exception caught, continuing", exc_info=True)
                continue
            entries.append((monitor_float, checkpoint_path, meta_path))
        if not entries:
            return
        self._checkpoints.extend(entries)
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        self.state.best_metric = self._checkpoints[0][1]
    
    xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_1': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_1, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_2': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_2, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_3': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_3, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_4': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_4, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_5': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_5, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_6': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_6, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_7': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_7, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_8': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_8, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_9': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_9, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_10': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_10, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_11': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_11, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_12': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_12, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_13': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_13, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_14': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_14, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_15': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_15, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_16': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_16, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_17': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_17, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_18': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_18, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_19': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_19, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_20': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_20, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_21': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_21, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_22': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_22, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_23': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_23, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_24': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_24, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_25': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_25, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_26': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_26, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_27': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_27, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_28': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_28, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_29': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_29, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_30': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_30, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_31': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_31, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_32': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_32, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_33': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_33, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_34': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_34, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_35': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_35, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_36': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_36, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_37': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_37, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_38': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_38, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_39': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_39, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_40': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_40, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_41': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_41, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_42': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_42, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_43': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_43, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_44': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_44, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_45': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_45, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_46': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_46, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_47': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_47, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_48': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_48, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_49': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_49, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_50': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_50, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_51': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_51, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_52': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_52, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_53': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_53, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_54': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_54, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_55': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_55, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_56': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_56, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_57': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_57, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_58': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_58, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_59': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_59, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_60': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_60, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_61': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_61, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_62': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_62, 
        'xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_63': xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_63
    }
    
    def _hydrate_existing_checkpoints(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_orig"), object.__getattribute__(self, "xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _hydrate_existing_checkpoints.__signature__ = _mutmut_signature(xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_orig)
    xǁTrainerǁ_hydrate_existing_checkpoints__mutmut_orig.__name__ = 'xǁTrainerǁ_hydrate_existing_checkpoints'

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_orig(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_1(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = None
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_2(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory * "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_3(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "XXlatest.jsonXX"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_4(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "LATEST.JSON"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_5(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = None
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_6(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(None)
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_7(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding=None))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_8(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="XXutf-8XX"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_9(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="UTF-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_10(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning(None, exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_11(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=None)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_12(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning(exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_13(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", )
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_14(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("XXException occurredXX", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_15(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_16(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("EXCEPTION OCCURRED", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_17(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=False)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_18(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning(None, exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_19(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=None)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_20(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning(exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_21(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", )
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_22(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("XXException occurredXX", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_23(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_24(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("EXCEPTION OCCURRED", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_25(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=False)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_26(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = None
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_27(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = None
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_28(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get(None)
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_29(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("XXpathXX")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_30(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("PATH")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_31(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = None
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_32(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory * str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_33(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(None)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_34(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = ""
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_35(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(None):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_36(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob(None)):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_37(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("XXepoch_*.jsonXX")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_38(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("EPOCH_*.JSON")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_39(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = None
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_40(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(None)
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_41(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding=None))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_42(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="XXutf-8XX"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_43(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="UTF-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_44(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(None)
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_45(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug(None, meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_46(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", None, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_47(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, None)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_48(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug(meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_49(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_50(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, )
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_51(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("XXSkipping checkpoint metadata %s: %sXX", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_52(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_53(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("SKIPPING CHECKPOINT METADATA %S: %S", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_54(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                break
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_55(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = None
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_56(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(None)
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_57(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix("XX.ptXX")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_58(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".PT")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_59(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_60(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                break
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_61(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = None
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_62(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get(None, 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_63(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", None)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_64(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get(0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_65(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", )
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_66(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("XXepochXX", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_67(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("EPOCH", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_68(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 1)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_69(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = None
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_70(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(None)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_71(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = None
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_72(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 1
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_73(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None and epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_74(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is not None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_75(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int >= int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_76(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(None):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_77(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get(None, 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_78(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", None)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_79(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get(0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_80(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", )):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_81(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[2].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_82(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("XXepochXX", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_83(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("EPOCH", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_84(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 1)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_85(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = None
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_86(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(None)
                enriched.setdefault("path", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_87(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault(None, checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_88(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", None)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_89(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault(checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_90(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", )
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_91(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("XXpathXX", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_92(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("PATH", checkpoint_path.name)
                latest = (checkpoint_path, enriched)
        return latest

    def xǁTrainerǁ_find_latest_checkpoint__mutmut_93(self, directory: Path) -> tuple[Path, Mapping[str, Any]] | None:
        pointer = directory / "latest.json"
        if pointer.exists():
            try:
                payload = json.loads(pointer.read_text(encoding="utf-8"))
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                payload = {}
            path_hint = payload.get("path")
            if path_hint:
                candidate = directory / str(path_hint)
                if candidate.exists():
                    return candidate, payload
        latest: tuple[Path, Mapping[str, Any]] | None = None
        for meta_path in sorted(directory.glob("epoch_*.json")):
            try:
                data = json.loads(meta_path.read_text(encoding="utf-8"))
            except Exception as exc:
                logger.debug(f"Exception: {exc}")
                LOGGER.debug("Skipping checkpoint metadata %s: %s", meta_path, exc)
                continue
            checkpoint_path = meta_path.with_suffix(".pt")
            if not checkpoint_path.exists():
                continue
            epoch = data.get("epoch", 0)
            try:
                epoch_int = int(epoch)
            except (TypeError, ValueError):
                epoch_int = 0
            if latest is None or epoch_int > int(latest[1].get("epoch", 0)):
                enriched = dict(data)
                enriched.setdefault("path", checkpoint_path.name)
                latest = None
        return latest
    
    xǁTrainerǁ_find_latest_checkpoint__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁTrainerǁ_find_latest_checkpoint__mutmut_1': xǁTrainerǁ_find_latest_checkpoint__mutmut_1, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_2': xǁTrainerǁ_find_latest_checkpoint__mutmut_2, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_3': xǁTrainerǁ_find_latest_checkpoint__mutmut_3, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_4': xǁTrainerǁ_find_latest_checkpoint__mutmut_4, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_5': xǁTrainerǁ_find_latest_checkpoint__mutmut_5, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_6': xǁTrainerǁ_find_latest_checkpoint__mutmut_6, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_7': xǁTrainerǁ_find_latest_checkpoint__mutmut_7, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_8': xǁTrainerǁ_find_latest_checkpoint__mutmut_8, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_9': xǁTrainerǁ_find_latest_checkpoint__mutmut_9, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_10': xǁTrainerǁ_find_latest_checkpoint__mutmut_10, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_11': xǁTrainerǁ_find_latest_checkpoint__mutmut_11, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_12': xǁTrainerǁ_find_latest_checkpoint__mutmut_12, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_13': xǁTrainerǁ_find_latest_checkpoint__mutmut_13, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_14': xǁTrainerǁ_find_latest_checkpoint__mutmut_14, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_15': xǁTrainerǁ_find_latest_checkpoint__mutmut_15, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_16': xǁTrainerǁ_find_latest_checkpoint__mutmut_16, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_17': xǁTrainerǁ_find_latest_checkpoint__mutmut_17, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_18': xǁTrainerǁ_find_latest_checkpoint__mutmut_18, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_19': xǁTrainerǁ_find_latest_checkpoint__mutmut_19, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_20': xǁTrainerǁ_find_latest_checkpoint__mutmut_20, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_21': xǁTrainerǁ_find_latest_checkpoint__mutmut_21, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_22': xǁTrainerǁ_find_latest_checkpoint__mutmut_22, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_23': xǁTrainerǁ_find_latest_checkpoint__mutmut_23, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_24': xǁTrainerǁ_find_latest_checkpoint__mutmut_24, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_25': xǁTrainerǁ_find_latest_checkpoint__mutmut_25, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_26': xǁTrainerǁ_find_latest_checkpoint__mutmut_26, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_27': xǁTrainerǁ_find_latest_checkpoint__mutmut_27, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_28': xǁTrainerǁ_find_latest_checkpoint__mutmut_28, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_29': xǁTrainerǁ_find_latest_checkpoint__mutmut_29, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_30': xǁTrainerǁ_find_latest_checkpoint__mutmut_30, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_31': xǁTrainerǁ_find_latest_checkpoint__mutmut_31, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_32': xǁTrainerǁ_find_latest_checkpoint__mutmut_32, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_33': xǁTrainerǁ_find_latest_checkpoint__mutmut_33, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_34': xǁTrainerǁ_find_latest_checkpoint__mutmut_34, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_35': xǁTrainerǁ_find_latest_checkpoint__mutmut_35, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_36': xǁTrainerǁ_find_latest_checkpoint__mutmut_36, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_37': xǁTrainerǁ_find_latest_checkpoint__mutmut_37, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_38': xǁTrainerǁ_find_latest_checkpoint__mutmut_38, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_39': xǁTrainerǁ_find_latest_checkpoint__mutmut_39, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_40': xǁTrainerǁ_find_latest_checkpoint__mutmut_40, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_41': xǁTrainerǁ_find_latest_checkpoint__mutmut_41, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_42': xǁTrainerǁ_find_latest_checkpoint__mutmut_42, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_43': xǁTrainerǁ_find_latest_checkpoint__mutmut_43, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_44': xǁTrainerǁ_find_latest_checkpoint__mutmut_44, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_45': xǁTrainerǁ_find_latest_checkpoint__mutmut_45, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_46': xǁTrainerǁ_find_latest_checkpoint__mutmut_46, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_47': xǁTrainerǁ_find_latest_checkpoint__mutmut_47, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_48': xǁTrainerǁ_find_latest_checkpoint__mutmut_48, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_49': xǁTrainerǁ_find_latest_checkpoint__mutmut_49, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_50': xǁTrainerǁ_find_latest_checkpoint__mutmut_50, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_51': xǁTrainerǁ_find_latest_checkpoint__mutmut_51, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_52': xǁTrainerǁ_find_latest_checkpoint__mutmut_52, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_53': xǁTrainerǁ_find_latest_checkpoint__mutmut_53, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_54': xǁTrainerǁ_find_latest_checkpoint__mutmut_54, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_55': xǁTrainerǁ_find_latest_checkpoint__mutmut_55, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_56': xǁTrainerǁ_find_latest_checkpoint__mutmut_56, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_57': xǁTrainerǁ_find_latest_checkpoint__mutmut_57, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_58': xǁTrainerǁ_find_latest_checkpoint__mutmut_58, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_59': xǁTrainerǁ_find_latest_checkpoint__mutmut_59, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_60': xǁTrainerǁ_find_latest_checkpoint__mutmut_60, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_61': xǁTrainerǁ_find_latest_checkpoint__mutmut_61, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_62': xǁTrainerǁ_find_latest_checkpoint__mutmut_62, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_63': xǁTrainerǁ_find_latest_checkpoint__mutmut_63, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_64': xǁTrainerǁ_find_latest_checkpoint__mutmut_64, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_65': xǁTrainerǁ_find_latest_checkpoint__mutmut_65, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_66': xǁTrainerǁ_find_latest_checkpoint__mutmut_66, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_67': xǁTrainerǁ_find_latest_checkpoint__mutmut_67, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_68': xǁTrainerǁ_find_latest_checkpoint__mutmut_68, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_69': xǁTrainerǁ_find_latest_checkpoint__mutmut_69, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_70': xǁTrainerǁ_find_latest_checkpoint__mutmut_70, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_71': xǁTrainerǁ_find_latest_checkpoint__mutmut_71, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_72': xǁTrainerǁ_find_latest_checkpoint__mutmut_72, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_73': xǁTrainerǁ_find_latest_checkpoint__mutmut_73, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_74': xǁTrainerǁ_find_latest_checkpoint__mutmut_74, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_75': xǁTrainerǁ_find_latest_checkpoint__mutmut_75, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_76': xǁTrainerǁ_find_latest_checkpoint__mutmut_76, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_77': xǁTrainerǁ_find_latest_checkpoint__mutmut_77, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_78': xǁTrainerǁ_find_latest_checkpoint__mutmut_78, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_79': xǁTrainerǁ_find_latest_checkpoint__mutmut_79, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_80': xǁTrainerǁ_find_latest_checkpoint__mutmut_80, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_81': xǁTrainerǁ_find_latest_checkpoint__mutmut_81, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_82': xǁTrainerǁ_find_latest_checkpoint__mutmut_82, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_83': xǁTrainerǁ_find_latest_checkpoint__mutmut_83, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_84': xǁTrainerǁ_find_latest_checkpoint__mutmut_84, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_85': xǁTrainerǁ_find_latest_checkpoint__mutmut_85, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_86': xǁTrainerǁ_find_latest_checkpoint__mutmut_86, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_87': xǁTrainerǁ_find_latest_checkpoint__mutmut_87, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_88': xǁTrainerǁ_find_latest_checkpoint__mutmut_88, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_89': xǁTrainerǁ_find_latest_checkpoint__mutmut_89, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_90': xǁTrainerǁ_find_latest_checkpoint__mutmut_90, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_91': xǁTrainerǁ_find_latest_checkpoint__mutmut_91, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_92': xǁTrainerǁ_find_latest_checkpoint__mutmut_92, 
        'xǁTrainerǁ_find_latest_checkpoint__mutmut_93': xǁTrainerǁ_find_latest_checkpoint__mutmut_93
    }
    
    def _find_latest_checkpoint(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁTrainerǁ_find_latest_checkpoint__mutmut_orig"), object.__getattribute__(self, "xǁTrainerǁ_find_latest_checkpoint__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _find_latest_checkpoint.__signature__ = _mutmut_signature(xǁTrainerǁ_find_latest_checkpoint__mutmut_orig)
    xǁTrainerǁ_find_latest_checkpoint__mutmut_orig.__name__ = 'xǁTrainerǁ_find_latest_checkpoint'

    def xǁTrainerǁ_load_checkpoint__mutmut_orig(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_1(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = None
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_2(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(None, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_3(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=None)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_4(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_5(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, )
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_6(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = None
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_7(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") and payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_8(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get(None) or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_9(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("XXmodel_stateXX") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_10(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("MODEL_STATE") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_11(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get(None)
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_12(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("XXmodelXX")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_13(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("MODEL")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_14(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(None)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_15(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = None
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_16(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") and payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_17(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get(None) or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_18(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("XXoptimizer_stateXX") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_19(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("OPTIMIZER_STATE") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_20(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get(None)
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_21(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("XXoptimizerXX")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_22(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("OPTIMIZER")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_23(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None or self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_24(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state or self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_25(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_26(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(None):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_27(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(None)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_28(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = None
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_29(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") and payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_30(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get(None) or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_31(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("XXepochXX") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_32(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("EPOCH") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_33(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get(None, 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_34(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", None)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_35(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get(0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_36(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", )
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_37(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("XXepochXX", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_38(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("EPOCH", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_39(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 1)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_40(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = None
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_41(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") and payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_42(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get(None) or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_43(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("XXglobal_stepXX") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_44(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("GLOBAL_STEP") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_45(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get(None, 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_46(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", None)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_47(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get(0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_48(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", )
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_49(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("XXglobal_stepXX", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_50(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("GLOBAL_STEP", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_51(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 1)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_52(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = None
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_53(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") and payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_54(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get(None) or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_55(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("XXmonitorXX") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_56(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("MONITOR") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_57(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get(None)
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_58(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("XXmonitorXX")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_59(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("MONITOR")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_60(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = None
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_61(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(None)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_62(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = None
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_63(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 1
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_64(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = None
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_65(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(None)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_66(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = None
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_67(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 1
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_68(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_69(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(None, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_70(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, None):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_71(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_72(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_73(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = None
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_74(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(None)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_75(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = None
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_76(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            None,
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_77(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            None,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_78(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            None,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_79(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            None,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_80(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_81(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_82(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_83(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "Resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            )

    def xǁTrainerǁ_load_checkpoint__mutmut_84(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "XXResumed trainer state from %s (epoch=%s, global_step=%s)XX",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_85(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "resumed trainer state from %s (epoch=%s, global_step=%s)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )

    def xǁTrainerǁ_load_checkpoint__mutmut_86(self, checkpoint_path: Path, pointer: Mapping[str, Any]) -> None:
        payload = _load_checkpoint_payload(checkpoint_path, map_location=self.device)
        model_state = payload.get("model_state") or payload.get("model")
        if isinstance(model_state, Mapping):
            self.simple.model.load_state_dict(model_state)
        optimizer_state = payload.get("optimizer_state") or payload.get("optimizer")
        if (
            optimizer_state
            and self.config.checkpoint is not None
            and self.config.checkpoint.save_optimizer
        ):
            with contextlib.suppress(Exception):
                self.simple.optimizer.load_state_dict(optimizer_state)

        epoch = pointer.get("epoch") or payload.get("epoch", 0)
        global_step = pointer.get("global_step") or payload.get("global_step", 0)
        monitor = pointer.get("monitor") or payload.get("monitor")
        try:
            self.state.epoch = int(epoch)
        except (TypeError, ValueError):
            self.state.epoch = 0
        try:
            self.state.global_step = int(global_step)
        except (TypeError, ValueError):
            self.state.global_step = 0
        if monitor is not None:
            with contextlib.suppress(TypeError, ValueError):
                self.state.best_metric = float(monitor)
        self._resume_metadata = pointer
        LOGGER.info(
            "RESUMED TRAINER STATE FROM %S (EPOCH=%S, GLOBAL_STEP=%S)",
            checkpoint_path,
            self.state.epoch,
            self.state.global_step,
        )
    
    xǁTrainerǁ_load_checkpoint__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁTrainerǁ_load_checkpoint__mutmut_1': xǁTrainerǁ_load_checkpoint__mutmut_1, 
        'xǁTrainerǁ_load_checkpoint__mutmut_2': xǁTrainerǁ_load_checkpoint__mutmut_2, 
        'xǁTrainerǁ_load_checkpoint__mutmut_3': xǁTrainerǁ_load_checkpoint__mutmut_3, 
        'xǁTrainerǁ_load_checkpoint__mutmut_4': xǁTrainerǁ_load_checkpoint__mutmut_4, 
        'xǁTrainerǁ_load_checkpoint__mutmut_5': xǁTrainerǁ_load_checkpoint__mutmut_5, 
        'xǁTrainerǁ_load_checkpoint__mutmut_6': xǁTrainerǁ_load_checkpoint__mutmut_6, 
        'xǁTrainerǁ_load_checkpoint__mutmut_7': xǁTrainerǁ_load_checkpoint__mutmut_7, 
        'xǁTrainerǁ_load_checkpoint__mutmut_8': xǁTrainerǁ_load_checkpoint__mutmut_8, 
        'xǁTrainerǁ_load_checkpoint__mutmut_9': xǁTrainerǁ_load_checkpoint__mutmut_9, 
        'xǁTrainerǁ_load_checkpoint__mutmut_10': xǁTrainerǁ_load_checkpoint__mutmut_10, 
        'xǁTrainerǁ_load_checkpoint__mutmut_11': xǁTrainerǁ_load_checkpoint__mutmut_11, 
        'xǁTrainerǁ_load_checkpoint__mutmut_12': xǁTrainerǁ_load_checkpoint__mutmut_12, 
        'xǁTrainerǁ_load_checkpoint__mutmut_13': xǁTrainerǁ_load_checkpoint__mutmut_13, 
        'xǁTrainerǁ_load_checkpoint__mutmut_14': xǁTrainerǁ_load_checkpoint__mutmut_14, 
        'xǁTrainerǁ_load_checkpoint__mutmut_15': xǁTrainerǁ_load_checkpoint__mutmut_15, 
        'xǁTrainerǁ_load_checkpoint__mutmut_16': xǁTrainerǁ_load_checkpoint__mutmut_16, 
        'xǁTrainerǁ_load_checkpoint__mutmut_17': xǁTrainerǁ_load_checkpoint__mutmut_17, 
        'xǁTrainerǁ_load_checkpoint__mutmut_18': xǁTrainerǁ_load_checkpoint__mutmut_18, 
        'xǁTrainerǁ_load_checkpoint__mutmut_19': xǁTrainerǁ_load_checkpoint__mutmut_19, 
        'xǁTrainerǁ_load_checkpoint__mutmut_20': xǁTrainerǁ_load_checkpoint__mutmut_20, 
        'xǁTrainerǁ_load_checkpoint__mutmut_21': xǁTrainerǁ_load_checkpoint__mutmut_21, 
        'xǁTrainerǁ_load_checkpoint__mutmut_22': xǁTrainerǁ_load_checkpoint__mutmut_22, 
        'xǁTrainerǁ_load_checkpoint__mutmut_23': xǁTrainerǁ_load_checkpoint__mutmut_23, 
        'xǁTrainerǁ_load_checkpoint__mutmut_24': xǁTrainerǁ_load_checkpoint__mutmut_24, 
        'xǁTrainerǁ_load_checkpoint__mutmut_25': xǁTrainerǁ_load_checkpoint__mutmut_25, 
        'xǁTrainerǁ_load_checkpoint__mutmut_26': xǁTrainerǁ_load_checkpoint__mutmut_26, 
        'xǁTrainerǁ_load_checkpoint__mutmut_27': xǁTrainerǁ_load_checkpoint__mutmut_27, 
        'xǁTrainerǁ_load_checkpoint__mutmut_28': xǁTrainerǁ_load_checkpoint__mutmut_28, 
        'xǁTrainerǁ_load_checkpoint__mutmut_29': xǁTrainerǁ_load_checkpoint__mutmut_29, 
        'xǁTrainerǁ_load_checkpoint__mutmut_30': xǁTrainerǁ_load_checkpoint__mutmut_30, 
        'xǁTrainerǁ_load_checkpoint__mutmut_31': xǁTrainerǁ_load_checkpoint__mutmut_31, 
        'xǁTrainerǁ_load_checkpoint__mutmut_32': xǁTrainerǁ_load_checkpoint__mutmut_32, 
        'xǁTrainerǁ_load_checkpoint__mutmut_33': xǁTrainerǁ_load_checkpoint__mutmut_33, 
        'xǁTrainerǁ_load_checkpoint__mutmut_34': xǁTrainerǁ_load_checkpoint__mutmut_34, 
        'xǁTrainerǁ_load_checkpoint__mutmut_35': xǁTrainerǁ_load_checkpoint__mutmut_35, 
        'xǁTrainerǁ_load_checkpoint__mutmut_36': xǁTrainerǁ_load_checkpoint__mutmut_36, 
        'xǁTrainerǁ_load_checkpoint__mutmut_37': xǁTrainerǁ_load_checkpoint__mutmut_37, 
        'xǁTrainerǁ_load_checkpoint__mutmut_38': xǁTrainerǁ_load_checkpoint__mutmut_38, 
        'xǁTrainerǁ_load_checkpoint__mutmut_39': xǁTrainerǁ_load_checkpoint__mutmut_39, 
        'xǁTrainerǁ_load_checkpoint__mutmut_40': xǁTrainerǁ_load_checkpoint__mutmut_40, 
        'xǁTrainerǁ_load_checkpoint__mutmut_41': xǁTrainerǁ_load_checkpoint__mutmut_41, 
        'xǁTrainerǁ_load_checkpoint__mutmut_42': xǁTrainerǁ_load_checkpoint__mutmut_42, 
        'xǁTrainerǁ_load_checkpoint__mutmut_43': xǁTrainerǁ_load_checkpoint__mutmut_43, 
        'xǁTrainerǁ_load_checkpoint__mutmut_44': xǁTrainerǁ_load_checkpoint__mutmut_44, 
        'xǁTrainerǁ_load_checkpoint__mutmut_45': xǁTrainerǁ_load_checkpoint__mutmut_45, 
        'xǁTrainerǁ_load_checkpoint__mutmut_46': xǁTrainerǁ_load_checkpoint__mutmut_46, 
        'xǁTrainerǁ_load_checkpoint__mutmut_47': xǁTrainerǁ_load_checkpoint__mutmut_47, 
        'xǁTrainerǁ_load_checkpoint__mutmut_48': xǁTrainerǁ_load_checkpoint__mutmut_48, 
        'xǁTrainerǁ_load_checkpoint__mutmut_49': xǁTrainerǁ_load_checkpoint__mutmut_49, 
        'xǁTrainerǁ_load_checkpoint__mutmut_50': xǁTrainerǁ_load_checkpoint__mutmut_50, 
        'xǁTrainerǁ_load_checkpoint__mutmut_51': xǁTrainerǁ_load_checkpoint__mutmut_51, 
        'xǁTrainerǁ_load_checkpoint__mutmut_52': xǁTrainerǁ_load_checkpoint__mutmut_52, 
        'xǁTrainerǁ_load_checkpoint__mutmut_53': xǁTrainerǁ_load_checkpoint__mutmut_53, 
        'xǁTrainerǁ_load_checkpoint__mutmut_54': xǁTrainerǁ_load_checkpoint__mutmut_54, 
        'xǁTrainerǁ_load_checkpoint__mutmut_55': xǁTrainerǁ_load_checkpoint__mutmut_55, 
        'xǁTrainerǁ_load_checkpoint__mutmut_56': xǁTrainerǁ_load_checkpoint__mutmut_56, 
        'xǁTrainerǁ_load_checkpoint__mutmut_57': xǁTrainerǁ_load_checkpoint__mutmut_57, 
        'xǁTrainerǁ_load_checkpoint__mutmut_58': xǁTrainerǁ_load_checkpoint__mutmut_58, 
        'xǁTrainerǁ_load_checkpoint__mutmut_59': xǁTrainerǁ_load_checkpoint__mutmut_59, 
        'xǁTrainerǁ_load_checkpoint__mutmut_60': xǁTrainerǁ_load_checkpoint__mutmut_60, 
        'xǁTrainerǁ_load_checkpoint__mutmut_61': xǁTrainerǁ_load_checkpoint__mutmut_61, 
        'xǁTrainerǁ_load_checkpoint__mutmut_62': xǁTrainerǁ_load_checkpoint__mutmut_62, 
        'xǁTrainerǁ_load_checkpoint__mutmut_63': xǁTrainerǁ_load_checkpoint__mutmut_63, 
        'xǁTrainerǁ_load_checkpoint__mutmut_64': xǁTrainerǁ_load_checkpoint__mutmut_64, 
        'xǁTrainerǁ_load_checkpoint__mutmut_65': xǁTrainerǁ_load_checkpoint__mutmut_65, 
        'xǁTrainerǁ_load_checkpoint__mutmut_66': xǁTrainerǁ_load_checkpoint__mutmut_66, 
        'xǁTrainerǁ_load_checkpoint__mutmut_67': xǁTrainerǁ_load_checkpoint__mutmut_67, 
        'xǁTrainerǁ_load_checkpoint__mutmut_68': xǁTrainerǁ_load_checkpoint__mutmut_68, 
        'xǁTrainerǁ_load_checkpoint__mutmut_69': xǁTrainerǁ_load_checkpoint__mutmut_69, 
        'xǁTrainerǁ_load_checkpoint__mutmut_70': xǁTrainerǁ_load_checkpoint__mutmut_70, 
        'xǁTrainerǁ_load_checkpoint__mutmut_71': xǁTrainerǁ_load_checkpoint__mutmut_71, 
        'xǁTrainerǁ_load_checkpoint__mutmut_72': xǁTrainerǁ_load_checkpoint__mutmut_72, 
        'xǁTrainerǁ_load_checkpoint__mutmut_73': xǁTrainerǁ_load_checkpoint__mutmut_73, 
        'xǁTrainerǁ_load_checkpoint__mutmut_74': xǁTrainerǁ_load_checkpoint__mutmut_74, 
        'xǁTrainerǁ_load_checkpoint__mutmut_75': xǁTrainerǁ_load_checkpoint__mutmut_75, 
        'xǁTrainerǁ_load_checkpoint__mutmut_76': xǁTrainerǁ_load_checkpoint__mutmut_76, 
        'xǁTrainerǁ_load_checkpoint__mutmut_77': xǁTrainerǁ_load_checkpoint__mutmut_77, 
        'xǁTrainerǁ_load_checkpoint__mutmut_78': xǁTrainerǁ_load_checkpoint__mutmut_78, 
        'xǁTrainerǁ_load_checkpoint__mutmut_79': xǁTrainerǁ_load_checkpoint__mutmut_79, 
        'xǁTrainerǁ_load_checkpoint__mutmut_80': xǁTrainerǁ_load_checkpoint__mutmut_80, 
        'xǁTrainerǁ_load_checkpoint__mutmut_81': xǁTrainerǁ_load_checkpoint__mutmut_81, 
        'xǁTrainerǁ_load_checkpoint__mutmut_82': xǁTrainerǁ_load_checkpoint__mutmut_82, 
        'xǁTrainerǁ_load_checkpoint__mutmut_83': xǁTrainerǁ_load_checkpoint__mutmut_83, 
        'xǁTrainerǁ_load_checkpoint__mutmut_84': xǁTrainerǁ_load_checkpoint__mutmut_84, 
        'xǁTrainerǁ_load_checkpoint__mutmut_85': xǁTrainerǁ_load_checkpoint__mutmut_85, 
        'xǁTrainerǁ_load_checkpoint__mutmut_86': xǁTrainerǁ_load_checkpoint__mutmut_86
    }
    
    def _load_checkpoint(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁTrainerǁ_load_checkpoint__mutmut_orig"), object.__getattribute__(self, "xǁTrainerǁ_load_checkpoint__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _load_checkpoint.__signature__ = _mutmut_signature(xǁTrainerǁ_load_checkpoint__mutmut_orig)
    xǁTrainerǁ_load_checkpoint__mutmut_orig.__name__ = 'xǁTrainerǁ_load_checkpoint'

    def xǁTrainerǁ_prune_checkpoints__mutmut_orig(self) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        while len(self._checkpoints) > cfg.best_k:
            _, ckpt_path, meta_path = self._checkpoints.pop(-1)
            for path in (ckpt_path, meta_path):
                try:
                    path.unlink(missing_ok=True)
                except Exception as exc:  # pragma: no cover - retention guard
                    LOGGER.debug("Failed to remove checkpoint '%s': %s", path, exc)

    def xǁTrainerǁ_prune_checkpoints__mutmut_1(self) -> None:
        cfg = None
        if cfg is None:
            return
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        while len(self._checkpoints) > cfg.best_k:
            _, ckpt_path, meta_path = self._checkpoints.pop(-1)
            for path in (ckpt_path, meta_path):
                try:
                    path.unlink(missing_ok=True)
                except Exception as exc:  # pragma: no cover - retention guard
                    LOGGER.debug("Failed to remove checkpoint '%s': %s", path, exc)

    def xǁTrainerǁ_prune_checkpoints__mutmut_2(self) -> None:
        cfg = self.config.checkpoint
        if cfg is not None:
            return
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        while len(self._checkpoints) > cfg.best_k:
            _, ckpt_path, meta_path = self._checkpoints.pop(-1)
            for path in (ckpt_path, meta_path):
                try:
                    path.unlink(missing_ok=True)
                except Exception as exc:  # pragma: no cover - retention guard
                    LOGGER.debug("Failed to remove checkpoint '%s': %s", path, exc)

    def xǁTrainerǁ_prune_checkpoints__mutmut_3(self) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        reverse = None
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        while len(self._checkpoints) > cfg.best_k:
            _, ckpt_path, meta_path = self._checkpoints.pop(-1)
            for path in (ckpt_path, meta_path):
                try:
                    path.unlink(missing_ok=True)
                except Exception as exc:  # pragma: no cover - retention guard
                    LOGGER.debug("Failed to remove checkpoint '%s': %s", path, exc)

    def xǁTrainerǁ_prune_checkpoints__mutmut_4(self) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        reverse = cfg.mode.upper() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        while len(self._checkpoints) > cfg.best_k:
            _, ckpt_path, meta_path = self._checkpoints.pop(-1)
            for path in (ckpt_path, meta_path):
                try:
                    path.unlink(missing_ok=True)
                except Exception as exc:  # pragma: no cover - retention guard
                    LOGGER.debug("Failed to remove checkpoint '%s': %s", path, exc)

    def xǁTrainerǁ_prune_checkpoints__mutmut_5(self) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        reverse = cfg.mode.lower() != "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        while len(self._checkpoints) > cfg.best_k:
            _, ckpt_path, meta_path = self._checkpoints.pop(-1)
            for path in (ckpt_path, meta_path):
                try:
                    path.unlink(missing_ok=True)
                except Exception as exc:  # pragma: no cover - retention guard
                    LOGGER.debug("Failed to remove checkpoint '%s': %s", path, exc)

    def xǁTrainerǁ_prune_checkpoints__mutmut_6(self) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        reverse = cfg.mode.lower() == "XXmaxXX"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        while len(self._checkpoints) > cfg.best_k:
            _, ckpt_path, meta_path = self._checkpoints.pop(-1)
            for path in (ckpt_path, meta_path):
                try:
                    path.unlink(missing_ok=True)
                except Exception as exc:  # pragma: no cover - retention guard
                    LOGGER.debug("Failed to remove checkpoint '%s': %s", path, exc)

    def xǁTrainerǁ_prune_checkpoints__mutmut_7(self) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        reverse = cfg.mode.lower() == "MAX"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        while len(self._checkpoints) > cfg.best_k:
            _, ckpt_path, meta_path = self._checkpoints.pop(-1)
            for path in (ckpt_path, meta_path):
                try:
                    path.unlink(missing_ok=True)
                except Exception as exc:  # pragma: no cover - retention guard
                    LOGGER.debug("Failed to remove checkpoint '%s': %s", path, exc)

    def xǁTrainerǁ_prune_checkpoints__mutmut_8(self) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=None, reverse=reverse)
        while len(self._checkpoints) > cfg.best_k:
            _, ckpt_path, meta_path = self._checkpoints.pop(-1)
            for path in (ckpt_path, meta_path):
                try:
                    path.unlink(missing_ok=True)
                except Exception as exc:  # pragma: no cover - retention guard
                    LOGGER.debug("Failed to remove checkpoint '%s': %s", path, exc)

    def xǁTrainerǁ_prune_checkpoints__mutmut_9(self) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=None)
        while len(self._checkpoints) > cfg.best_k:
            _, ckpt_path, meta_path = self._checkpoints.pop(-1)
            for path in (ckpt_path, meta_path):
                try:
                    path.unlink(missing_ok=True)
                except Exception as exc:  # pragma: no cover - retention guard
                    LOGGER.debug("Failed to remove checkpoint '%s': %s", path, exc)

    def xǁTrainerǁ_prune_checkpoints__mutmut_10(self) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(reverse=reverse)
        while len(self._checkpoints) > cfg.best_k:
            _, ckpt_path, meta_path = self._checkpoints.pop(-1)
            for path in (ckpt_path, meta_path):
                try:
                    path.unlink(missing_ok=True)
                except Exception as exc:  # pragma: no cover - retention guard
                    LOGGER.debug("Failed to remove checkpoint '%s': %s", path, exc)

    def xǁTrainerǁ_prune_checkpoints__mutmut_11(self) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], )
        while len(self._checkpoints) > cfg.best_k:
            _, ckpt_path, meta_path = self._checkpoints.pop(-1)
            for path in (ckpt_path, meta_path):
                try:
                    path.unlink(missing_ok=True)
                except Exception as exc:  # pragma: no cover - retention guard
                    LOGGER.debug("Failed to remove checkpoint '%s': %s", path, exc)

    def xǁTrainerǁ_prune_checkpoints__mutmut_12(self) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: None, reverse=reverse)
        while len(self._checkpoints) > cfg.best_k:
            _, ckpt_path, meta_path = self._checkpoints.pop(-1)
            for path in (ckpt_path, meta_path):
                try:
                    path.unlink(missing_ok=True)
                except Exception as exc:  # pragma: no cover - retention guard
                    LOGGER.debug("Failed to remove checkpoint '%s': %s", path, exc)

    def xǁTrainerǁ_prune_checkpoints__mutmut_13(self) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[1], reverse=reverse)
        while len(self._checkpoints) > cfg.best_k:
            _, ckpt_path, meta_path = self._checkpoints.pop(-1)
            for path in (ckpt_path, meta_path):
                try:
                    path.unlink(missing_ok=True)
                except Exception as exc:  # pragma: no cover - retention guard
                    LOGGER.debug("Failed to remove checkpoint '%s': %s", path, exc)

    def xǁTrainerǁ_prune_checkpoints__mutmut_14(self) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        while len(self._checkpoints) >= cfg.best_k:
            _, ckpt_path, meta_path = self._checkpoints.pop(-1)
            for path in (ckpt_path, meta_path):
                try:
                    path.unlink(missing_ok=True)
                except Exception as exc:  # pragma: no cover - retention guard
                    LOGGER.debug("Failed to remove checkpoint '%s': %s", path, exc)

    def xǁTrainerǁ_prune_checkpoints__mutmut_15(self) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        while len(self._checkpoints) > cfg.best_k:
            _, ckpt_path, meta_path = None
            for path in (ckpt_path, meta_path):
                try:
                    path.unlink(missing_ok=True)
                except Exception as exc:  # pragma: no cover - retention guard
                    LOGGER.debug("Failed to remove checkpoint '%s': %s", path, exc)

    def xǁTrainerǁ_prune_checkpoints__mutmut_16(self) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        while len(self._checkpoints) > cfg.best_k:
            _, ckpt_path, meta_path = self._checkpoints.pop(None)
            for path in (ckpt_path, meta_path):
                try:
                    path.unlink(missing_ok=True)
                except Exception as exc:  # pragma: no cover - retention guard
                    LOGGER.debug("Failed to remove checkpoint '%s': %s", path, exc)

    def xǁTrainerǁ_prune_checkpoints__mutmut_17(self) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        while len(self._checkpoints) > cfg.best_k:
            _, ckpt_path, meta_path = self._checkpoints.pop(+1)
            for path in (ckpt_path, meta_path):
                try:
                    path.unlink(missing_ok=True)
                except Exception as exc:  # pragma: no cover - retention guard
                    LOGGER.debug("Failed to remove checkpoint '%s': %s", path, exc)

    def xǁTrainerǁ_prune_checkpoints__mutmut_18(self) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        while len(self._checkpoints) > cfg.best_k:
            _, ckpt_path, meta_path = self._checkpoints.pop(-2)
            for path in (ckpt_path, meta_path):
                try:
                    path.unlink(missing_ok=True)
                except Exception as exc:  # pragma: no cover - retention guard
                    LOGGER.debug("Failed to remove checkpoint '%s': %s", path, exc)

    def xǁTrainerǁ_prune_checkpoints__mutmut_19(self) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        while len(self._checkpoints) > cfg.best_k:
            _, ckpt_path, meta_path = self._checkpoints.pop(-1)
            for path in (ckpt_path, meta_path):
                try:
                    path.unlink(missing_ok=None)
                except Exception as exc:  # pragma: no cover - retention guard
                    LOGGER.debug("Failed to remove checkpoint '%s': %s", path, exc)

    def xǁTrainerǁ_prune_checkpoints__mutmut_20(self) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        while len(self._checkpoints) > cfg.best_k:
            _, ckpt_path, meta_path = self._checkpoints.pop(-1)
            for path in (ckpt_path, meta_path):
                try:
                    path.unlink(missing_ok=False)
                except Exception as exc:  # pragma: no cover - retention guard
                    LOGGER.debug("Failed to remove checkpoint '%s': %s", path, exc)

    def xǁTrainerǁ_prune_checkpoints__mutmut_21(self) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        while len(self._checkpoints) > cfg.best_k:
            _, ckpt_path, meta_path = self._checkpoints.pop(-1)
            for path in (ckpt_path, meta_path):
                try:
                    path.unlink(missing_ok=True)
                except Exception as exc:  # pragma: no cover - retention guard
                    LOGGER.debug(None, path, exc)

    def xǁTrainerǁ_prune_checkpoints__mutmut_22(self) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        while len(self._checkpoints) > cfg.best_k:
            _, ckpt_path, meta_path = self._checkpoints.pop(-1)
            for path in (ckpt_path, meta_path):
                try:
                    path.unlink(missing_ok=True)
                except Exception as exc:  # pragma: no cover - retention guard
                    LOGGER.debug("Failed to remove checkpoint '%s': %s", None, exc)

    def xǁTrainerǁ_prune_checkpoints__mutmut_23(self) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        while len(self._checkpoints) > cfg.best_k:
            _, ckpt_path, meta_path = self._checkpoints.pop(-1)
            for path in (ckpt_path, meta_path):
                try:
                    path.unlink(missing_ok=True)
                except Exception as exc:  # pragma: no cover - retention guard
                    LOGGER.debug("Failed to remove checkpoint '%s': %s", path, None)

    def xǁTrainerǁ_prune_checkpoints__mutmut_24(self) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        while len(self._checkpoints) > cfg.best_k:
            _, ckpt_path, meta_path = self._checkpoints.pop(-1)
            for path in (ckpt_path, meta_path):
                try:
                    path.unlink(missing_ok=True)
                except Exception as exc:  # pragma: no cover - retention guard
                    LOGGER.debug(path, exc)

    def xǁTrainerǁ_prune_checkpoints__mutmut_25(self) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        while len(self._checkpoints) > cfg.best_k:
            _, ckpt_path, meta_path = self._checkpoints.pop(-1)
            for path in (ckpt_path, meta_path):
                try:
                    path.unlink(missing_ok=True)
                except Exception as exc:  # pragma: no cover - retention guard
                    LOGGER.debug("Failed to remove checkpoint '%s': %s", exc)

    def xǁTrainerǁ_prune_checkpoints__mutmut_26(self) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        while len(self._checkpoints) > cfg.best_k:
            _, ckpt_path, meta_path = self._checkpoints.pop(-1)
            for path in (ckpt_path, meta_path):
                try:
                    path.unlink(missing_ok=True)
                except Exception as exc:  # pragma: no cover - retention guard
                    LOGGER.debug("Failed to remove checkpoint '%s': %s", path, )

    def xǁTrainerǁ_prune_checkpoints__mutmut_27(self) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        while len(self._checkpoints) > cfg.best_k:
            _, ckpt_path, meta_path = self._checkpoints.pop(-1)
            for path in (ckpt_path, meta_path):
                try:
                    path.unlink(missing_ok=True)
                except Exception as exc:  # pragma: no cover - retention guard
                    LOGGER.debug("XXFailed to remove checkpoint '%s': %sXX", path, exc)

    def xǁTrainerǁ_prune_checkpoints__mutmut_28(self) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        while len(self._checkpoints) > cfg.best_k:
            _, ckpt_path, meta_path = self._checkpoints.pop(-1)
            for path in (ckpt_path, meta_path):
                try:
                    path.unlink(missing_ok=True)
                except Exception as exc:  # pragma: no cover - retention guard
                    LOGGER.debug("failed to remove checkpoint '%s': %s", path, exc)

    def xǁTrainerǁ_prune_checkpoints__mutmut_29(self) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        reverse = cfg.mode.lower() == "max"
        self._checkpoints.sort(key=lambda item: item[0], reverse=reverse)
        while len(self._checkpoints) > cfg.best_k:
            _, ckpt_path, meta_path = self._checkpoints.pop(-1)
            for path in (ckpt_path, meta_path):
                try:
                    path.unlink(missing_ok=True)
                except Exception as exc:  # pragma: no cover - retention guard
                    LOGGER.debug("FAILED TO REMOVE CHECKPOINT '%S': %S", path, exc)
    
    xǁTrainerǁ_prune_checkpoints__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁTrainerǁ_prune_checkpoints__mutmut_1': xǁTrainerǁ_prune_checkpoints__mutmut_1, 
        'xǁTrainerǁ_prune_checkpoints__mutmut_2': xǁTrainerǁ_prune_checkpoints__mutmut_2, 
        'xǁTrainerǁ_prune_checkpoints__mutmut_3': xǁTrainerǁ_prune_checkpoints__mutmut_3, 
        'xǁTrainerǁ_prune_checkpoints__mutmut_4': xǁTrainerǁ_prune_checkpoints__mutmut_4, 
        'xǁTrainerǁ_prune_checkpoints__mutmut_5': xǁTrainerǁ_prune_checkpoints__mutmut_5, 
        'xǁTrainerǁ_prune_checkpoints__mutmut_6': xǁTrainerǁ_prune_checkpoints__mutmut_6, 
        'xǁTrainerǁ_prune_checkpoints__mutmut_7': xǁTrainerǁ_prune_checkpoints__mutmut_7, 
        'xǁTrainerǁ_prune_checkpoints__mutmut_8': xǁTrainerǁ_prune_checkpoints__mutmut_8, 
        'xǁTrainerǁ_prune_checkpoints__mutmut_9': xǁTrainerǁ_prune_checkpoints__mutmut_9, 
        'xǁTrainerǁ_prune_checkpoints__mutmut_10': xǁTrainerǁ_prune_checkpoints__mutmut_10, 
        'xǁTrainerǁ_prune_checkpoints__mutmut_11': xǁTrainerǁ_prune_checkpoints__mutmut_11, 
        'xǁTrainerǁ_prune_checkpoints__mutmut_12': xǁTrainerǁ_prune_checkpoints__mutmut_12, 
        'xǁTrainerǁ_prune_checkpoints__mutmut_13': xǁTrainerǁ_prune_checkpoints__mutmut_13, 
        'xǁTrainerǁ_prune_checkpoints__mutmut_14': xǁTrainerǁ_prune_checkpoints__mutmut_14, 
        'xǁTrainerǁ_prune_checkpoints__mutmut_15': xǁTrainerǁ_prune_checkpoints__mutmut_15, 
        'xǁTrainerǁ_prune_checkpoints__mutmut_16': xǁTrainerǁ_prune_checkpoints__mutmut_16, 
        'xǁTrainerǁ_prune_checkpoints__mutmut_17': xǁTrainerǁ_prune_checkpoints__mutmut_17, 
        'xǁTrainerǁ_prune_checkpoints__mutmut_18': xǁTrainerǁ_prune_checkpoints__mutmut_18, 
        'xǁTrainerǁ_prune_checkpoints__mutmut_19': xǁTrainerǁ_prune_checkpoints__mutmut_19, 
        'xǁTrainerǁ_prune_checkpoints__mutmut_20': xǁTrainerǁ_prune_checkpoints__mutmut_20, 
        'xǁTrainerǁ_prune_checkpoints__mutmut_21': xǁTrainerǁ_prune_checkpoints__mutmut_21, 
        'xǁTrainerǁ_prune_checkpoints__mutmut_22': xǁTrainerǁ_prune_checkpoints__mutmut_22, 
        'xǁTrainerǁ_prune_checkpoints__mutmut_23': xǁTrainerǁ_prune_checkpoints__mutmut_23, 
        'xǁTrainerǁ_prune_checkpoints__mutmut_24': xǁTrainerǁ_prune_checkpoints__mutmut_24, 
        'xǁTrainerǁ_prune_checkpoints__mutmut_25': xǁTrainerǁ_prune_checkpoints__mutmut_25, 
        'xǁTrainerǁ_prune_checkpoints__mutmut_26': xǁTrainerǁ_prune_checkpoints__mutmut_26, 
        'xǁTrainerǁ_prune_checkpoints__mutmut_27': xǁTrainerǁ_prune_checkpoints__mutmut_27, 
        'xǁTrainerǁ_prune_checkpoints__mutmut_28': xǁTrainerǁ_prune_checkpoints__mutmut_28, 
        'xǁTrainerǁ_prune_checkpoints__mutmut_29': xǁTrainerǁ_prune_checkpoints__mutmut_29
    }
    
    def _prune_checkpoints(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁTrainerǁ_prune_checkpoints__mutmut_orig"), object.__getattribute__(self, "xǁTrainerǁ_prune_checkpoints__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _prune_checkpoints.__signature__ = _mutmut_signature(xǁTrainerǁ_prune_checkpoints__mutmut_orig)
    xǁTrainerǁ_prune_checkpoints__mutmut_orig.__name__ = 'xǁTrainerǁ_prune_checkpoints'

    def xǁTrainerǁ_save_checkpoint__mutmut_orig(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_1(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = None
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_2(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is not None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_3(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = None
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_4(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(None)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_5(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is not None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_6(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug(None, cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_7(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", None)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_8(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug(cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_9(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", )
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_10(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("XXSkipping checkpoint save; monitor '%s' missingXX", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_11(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_12(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("SKIPPING CHECKPOINT SAVE; MONITOR '%S' MISSING", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_13(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = None
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_14(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(None)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_15(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = None
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_16(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(None)
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_17(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix("XX.jsonXX")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_18(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".JSON")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_19(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = None
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_20(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "XXepochXX": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_21(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "EPOCH": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_22(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "XXglobal_stepXX": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_23(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "GLOBAL_STEP": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_24(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "XXmetricsXX": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_25(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "METRICS": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_26(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(None),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_27(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "XXmonitorXX": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_28(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "MONITOR": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_29(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = None
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_30(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["XXschema_versionXX"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_31(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["SCHEMA_VERSION"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_32(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = None
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_33(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["XXhas_optimizer_stateXX"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_34(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["HAS_OPTIMIZER_STATE"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_35(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = None
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_36(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "XXmodel_stateXX": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_37(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "MODEL_STATE": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_38(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = None
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_39(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["XXoptimizer_stateXX"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_40(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["OPTIMIZER_STATE"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_41(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(None, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_42(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, None)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_43(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_44(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, )
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_45(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(None, encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_46(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding=None)
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_47(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_48(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), )
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_49(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(None, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_50(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=None), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_51(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_52(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, ), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_53(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=3), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_54(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="XXutf-8XX")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_55(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="UTF-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_56(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append(None)
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_57(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(None):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_58(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = None
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_59(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = None
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_60(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "XXschema_versionXX": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_61(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "SCHEMA_VERSION": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_62(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "XXepochXX": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_63(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "EPOCH": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_64(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "XXpathXX": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_65(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "PATH": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_66(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "XXglobal_stepXX": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_67(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "GLOBAL_STEP": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_68(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "XXmonitorXX": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_69(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "MONITOR": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_70(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "XXupdated_atXX": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_71(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "UPDATED_AT": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_72(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = None
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_73(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent * "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_74(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "XXlatest.jsonXX"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_75(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "LATEST.JSON"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_76(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(None, encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_77(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding=None)
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_78(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_79(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), )
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_80(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(None, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_81(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=None), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_82(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_83(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, ), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_84(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=3), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_85(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="XXutf-8XX")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_86(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="UTF-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_87(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning(None, checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_88(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", None, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_89(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, None)

    def xǁTrainerǁ_save_checkpoint__mutmut_90(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning(checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_91(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_92(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("Failed to persist checkpoint '%s': %s", checkpoint_path, )

    def xǁTrainerǁ_save_checkpoint__mutmut_93(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("XXFailed to persist checkpoint '%s': %sXX", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_94(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("failed to persist checkpoint '%s': %s", checkpoint_path, exc)

    def xǁTrainerǁ_save_checkpoint__mutmut_95(self, epoch: int, metrics: Mapping[str, float]) -> None:
        cfg = self.config.checkpoint
        if cfg is None:
            return
        monitor_value = self._monitor_value(metrics)
        if monitor_value is None:
            LOGGER.debug("Skipping checkpoint save; monitor '%s' missing", cfg.monitor)
            return

        checkpoint_path = cfg.path_for_epoch(epoch)
        metadata_path = checkpoint_path.with_suffix(".json")
        payload = {
            "epoch": epoch,
            "global_step": self.state.global_step,
            "metrics": dict(metrics),
            "monitor": monitor_value,
        }
        payload["schema_version"] = _CHECKPOINT_POINTER_VERSION
        payload["has_optimizer_state"] = cfg.save_optimizer
        checkpoint: dict[str, Any] = {
            "model_state": self.simple.model.state_dict(),
            **payload,
        }
        if cfg.save_optimizer:
            checkpoint["optimizer_state"] = self.simple.optimizer.state_dict()
        try:
            torch.save(checkpoint, checkpoint_path)
            metadata_path.write_text(json.dumps(payload, indent=2), encoding="utf-8")
            self._checkpoints.append((monitor_value, checkpoint_path, metadata_path))
            self._prune_checkpoints()
            if self._should_replace(monitor_value):
                self.state.best_metric = monitor_value
            pointer_payload = {
                "schema_version": _CHECKPOINT_POINTER_VERSION,
                "epoch": epoch,
                "path": checkpoint_path.name,
                "global_step": self.state.global_step,
                "monitor": monitor_value,
                "updated_at": time.time(),
            }
            pointer_path = checkpoint_path.parent / "latest.json"
            pointer_path.write_text(json.dumps(pointer_payload, indent=2), encoding="utf-8")
        except Exception as exc:  # pragma: no cover - persistence guard
            LOGGER.warning("FAILED TO PERSIST CHECKPOINT '%S': %S", checkpoint_path, exc)
    
    xǁTrainerǁ_save_checkpoint__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁTrainerǁ_save_checkpoint__mutmut_1': xǁTrainerǁ_save_checkpoint__mutmut_1, 
        'xǁTrainerǁ_save_checkpoint__mutmut_2': xǁTrainerǁ_save_checkpoint__mutmut_2, 
        'xǁTrainerǁ_save_checkpoint__mutmut_3': xǁTrainerǁ_save_checkpoint__mutmut_3, 
        'xǁTrainerǁ_save_checkpoint__mutmut_4': xǁTrainerǁ_save_checkpoint__mutmut_4, 
        'xǁTrainerǁ_save_checkpoint__mutmut_5': xǁTrainerǁ_save_checkpoint__mutmut_5, 
        'xǁTrainerǁ_save_checkpoint__mutmut_6': xǁTrainerǁ_save_checkpoint__mutmut_6, 
        'xǁTrainerǁ_save_checkpoint__mutmut_7': xǁTrainerǁ_save_checkpoint__mutmut_7, 
        'xǁTrainerǁ_save_checkpoint__mutmut_8': xǁTrainerǁ_save_checkpoint__mutmut_8, 
        'xǁTrainerǁ_save_checkpoint__mutmut_9': xǁTrainerǁ_save_checkpoint__mutmut_9, 
        'xǁTrainerǁ_save_checkpoint__mutmut_10': xǁTrainerǁ_save_checkpoint__mutmut_10, 
        'xǁTrainerǁ_save_checkpoint__mutmut_11': xǁTrainerǁ_save_checkpoint__mutmut_11, 
        'xǁTrainerǁ_save_checkpoint__mutmut_12': xǁTrainerǁ_save_checkpoint__mutmut_12, 
        'xǁTrainerǁ_save_checkpoint__mutmut_13': xǁTrainerǁ_save_checkpoint__mutmut_13, 
        'xǁTrainerǁ_save_checkpoint__mutmut_14': xǁTrainerǁ_save_checkpoint__mutmut_14, 
        'xǁTrainerǁ_save_checkpoint__mutmut_15': xǁTrainerǁ_save_checkpoint__mutmut_15, 
        'xǁTrainerǁ_save_checkpoint__mutmut_16': xǁTrainerǁ_save_checkpoint__mutmut_16, 
        'xǁTrainerǁ_save_checkpoint__mutmut_17': xǁTrainerǁ_save_checkpoint__mutmut_17, 
        'xǁTrainerǁ_save_checkpoint__mutmut_18': xǁTrainerǁ_save_checkpoint__mutmut_18, 
        'xǁTrainerǁ_save_checkpoint__mutmut_19': xǁTrainerǁ_save_checkpoint__mutmut_19, 
        'xǁTrainerǁ_save_checkpoint__mutmut_20': xǁTrainerǁ_save_checkpoint__mutmut_20, 
        'xǁTrainerǁ_save_checkpoint__mutmut_21': xǁTrainerǁ_save_checkpoint__mutmut_21, 
        'xǁTrainerǁ_save_checkpoint__mutmut_22': xǁTrainerǁ_save_checkpoint__mutmut_22, 
        'xǁTrainerǁ_save_checkpoint__mutmut_23': xǁTrainerǁ_save_checkpoint__mutmut_23, 
        'xǁTrainerǁ_save_checkpoint__mutmut_24': xǁTrainerǁ_save_checkpoint__mutmut_24, 
        'xǁTrainerǁ_save_checkpoint__mutmut_25': xǁTrainerǁ_save_checkpoint__mutmut_25, 
        'xǁTrainerǁ_save_checkpoint__mutmut_26': xǁTrainerǁ_save_checkpoint__mutmut_26, 
        'xǁTrainerǁ_save_checkpoint__mutmut_27': xǁTrainerǁ_save_checkpoint__mutmut_27, 
        'xǁTrainerǁ_save_checkpoint__mutmut_28': xǁTrainerǁ_save_checkpoint__mutmut_28, 
        'xǁTrainerǁ_save_checkpoint__mutmut_29': xǁTrainerǁ_save_checkpoint__mutmut_29, 
        'xǁTrainerǁ_save_checkpoint__mutmut_30': xǁTrainerǁ_save_checkpoint__mutmut_30, 
        'xǁTrainerǁ_save_checkpoint__mutmut_31': xǁTrainerǁ_save_checkpoint__mutmut_31, 
        'xǁTrainerǁ_save_checkpoint__mutmut_32': xǁTrainerǁ_save_checkpoint__mutmut_32, 
        'xǁTrainerǁ_save_checkpoint__mutmut_33': xǁTrainerǁ_save_checkpoint__mutmut_33, 
        'xǁTrainerǁ_save_checkpoint__mutmut_34': xǁTrainerǁ_save_checkpoint__mutmut_34, 
        'xǁTrainerǁ_save_checkpoint__mutmut_35': xǁTrainerǁ_save_checkpoint__mutmut_35, 
        'xǁTrainerǁ_save_checkpoint__mutmut_36': xǁTrainerǁ_save_checkpoint__mutmut_36, 
        'xǁTrainerǁ_save_checkpoint__mutmut_37': xǁTrainerǁ_save_checkpoint__mutmut_37, 
        'xǁTrainerǁ_save_checkpoint__mutmut_38': xǁTrainerǁ_save_checkpoint__mutmut_38, 
        'xǁTrainerǁ_save_checkpoint__mutmut_39': xǁTrainerǁ_save_checkpoint__mutmut_39, 
        'xǁTrainerǁ_save_checkpoint__mutmut_40': xǁTrainerǁ_save_checkpoint__mutmut_40, 
        'xǁTrainerǁ_save_checkpoint__mutmut_41': xǁTrainerǁ_save_checkpoint__mutmut_41, 
        'xǁTrainerǁ_save_checkpoint__mutmut_42': xǁTrainerǁ_save_checkpoint__mutmut_42, 
        'xǁTrainerǁ_save_checkpoint__mutmut_43': xǁTrainerǁ_save_checkpoint__mutmut_43, 
        'xǁTrainerǁ_save_checkpoint__mutmut_44': xǁTrainerǁ_save_checkpoint__mutmut_44, 
        'xǁTrainerǁ_save_checkpoint__mutmut_45': xǁTrainerǁ_save_checkpoint__mutmut_45, 
        'xǁTrainerǁ_save_checkpoint__mutmut_46': xǁTrainerǁ_save_checkpoint__mutmut_46, 
        'xǁTrainerǁ_save_checkpoint__mutmut_47': xǁTrainerǁ_save_checkpoint__mutmut_47, 
        'xǁTrainerǁ_save_checkpoint__mutmut_48': xǁTrainerǁ_save_checkpoint__mutmut_48, 
        'xǁTrainerǁ_save_checkpoint__mutmut_49': xǁTrainerǁ_save_checkpoint__mutmut_49, 
        'xǁTrainerǁ_save_checkpoint__mutmut_50': xǁTrainerǁ_save_checkpoint__mutmut_50, 
        'xǁTrainerǁ_save_checkpoint__mutmut_51': xǁTrainerǁ_save_checkpoint__mutmut_51, 
        'xǁTrainerǁ_save_checkpoint__mutmut_52': xǁTrainerǁ_save_checkpoint__mutmut_52, 
        'xǁTrainerǁ_save_checkpoint__mutmut_53': xǁTrainerǁ_save_checkpoint__mutmut_53, 
        'xǁTrainerǁ_save_checkpoint__mutmut_54': xǁTrainerǁ_save_checkpoint__mutmut_54, 
        'xǁTrainerǁ_save_checkpoint__mutmut_55': xǁTrainerǁ_save_checkpoint__mutmut_55, 
        'xǁTrainerǁ_save_checkpoint__mutmut_56': xǁTrainerǁ_save_checkpoint__mutmut_56, 
        'xǁTrainerǁ_save_checkpoint__mutmut_57': xǁTrainerǁ_save_checkpoint__mutmut_57, 
        'xǁTrainerǁ_save_checkpoint__mutmut_58': xǁTrainerǁ_save_checkpoint__mutmut_58, 
        'xǁTrainerǁ_save_checkpoint__mutmut_59': xǁTrainerǁ_save_checkpoint__mutmut_59, 
        'xǁTrainerǁ_save_checkpoint__mutmut_60': xǁTrainerǁ_save_checkpoint__mutmut_60, 
        'xǁTrainerǁ_save_checkpoint__mutmut_61': xǁTrainerǁ_save_checkpoint__mutmut_61, 
        'xǁTrainerǁ_save_checkpoint__mutmut_62': xǁTrainerǁ_save_checkpoint__mutmut_62, 
        'xǁTrainerǁ_save_checkpoint__mutmut_63': xǁTrainerǁ_save_checkpoint__mutmut_63, 
        'xǁTrainerǁ_save_checkpoint__mutmut_64': xǁTrainerǁ_save_checkpoint__mutmut_64, 
        'xǁTrainerǁ_save_checkpoint__mutmut_65': xǁTrainerǁ_save_checkpoint__mutmut_65, 
        'xǁTrainerǁ_save_checkpoint__mutmut_66': xǁTrainerǁ_save_checkpoint__mutmut_66, 
        'xǁTrainerǁ_save_checkpoint__mutmut_67': xǁTrainerǁ_save_checkpoint__mutmut_67, 
        'xǁTrainerǁ_save_checkpoint__mutmut_68': xǁTrainerǁ_save_checkpoint__mutmut_68, 
        'xǁTrainerǁ_save_checkpoint__mutmut_69': xǁTrainerǁ_save_checkpoint__mutmut_69, 
        'xǁTrainerǁ_save_checkpoint__mutmut_70': xǁTrainerǁ_save_checkpoint__mutmut_70, 
        'xǁTrainerǁ_save_checkpoint__mutmut_71': xǁTrainerǁ_save_checkpoint__mutmut_71, 
        'xǁTrainerǁ_save_checkpoint__mutmut_72': xǁTrainerǁ_save_checkpoint__mutmut_72, 
        'xǁTrainerǁ_save_checkpoint__mutmut_73': xǁTrainerǁ_save_checkpoint__mutmut_73, 
        'xǁTrainerǁ_save_checkpoint__mutmut_74': xǁTrainerǁ_save_checkpoint__mutmut_74, 
        'xǁTrainerǁ_save_checkpoint__mutmut_75': xǁTrainerǁ_save_checkpoint__mutmut_75, 
        'xǁTrainerǁ_save_checkpoint__mutmut_76': xǁTrainerǁ_save_checkpoint__mutmut_76, 
        'xǁTrainerǁ_save_checkpoint__mutmut_77': xǁTrainerǁ_save_checkpoint__mutmut_77, 
        'xǁTrainerǁ_save_checkpoint__mutmut_78': xǁTrainerǁ_save_checkpoint__mutmut_78, 
        'xǁTrainerǁ_save_checkpoint__mutmut_79': xǁTrainerǁ_save_checkpoint__mutmut_79, 
        'xǁTrainerǁ_save_checkpoint__mutmut_80': xǁTrainerǁ_save_checkpoint__mutmut_80, 
        'xǁTrainerǁ_save_checkpoint__mutmut_81': xǁTrainerǁ_save_checkpoint__mutmut_81, 
        'xǁTrainerǁ_save_checkpoint__mutmut_82': xǁTrainerǁ_save_checkpoint__mutmut_82, 
        'xǁTrainerǁ_save_checkpoint__mutmut_83': xǁTrainerǁ_save_checkpoint__mutmut_83, 
        'xǁTrainerǁ_save_checkpoint__mutmut_84': xǁTrainerǁ_save_checkpoint__mutmut_84, 
        'xǁTrainerǁ_save_checkpoint__mutmut_85': xǁTrainerǁ_save_checkpoint__mutmut_85, 
        'xǁTrainerǁ_save_checkpoint__mutmut_86': xǁTrainerǁ_save_checkpoint__mutmut_86, 
        'xǁTrainerǁ_save_checkpoint__mutmut_87': xǁTrainerǁ_save_checkpoint__mutmut_87, 
        'xǁTrainerǁ_save_checkpoint__mutmut_88': xǁTrainerǁ_save_checkpoint__mutmut_88, 
        'xǁTrainerǁ_save_checkpoint__mutmut_89': xǁTrainerǁ_save_checkpoint__mutmut_89, 
        'xǁTrainerǁ_save_checkpoint__mutmut_90': xǁTrainerǁ_save_checkpoint__mutmut_90, 
        'xǁTrainerǁ_save_checkpoint__mutmut_91': xǁTrainerǁ_save_checkpoint__mutmut_91, 
        'xǁTrainerǁ_save_checkpoint__mutmut_92': xǁTrainerǁ_save_checkpoint__mutmut_92, 
        'xǁTrainerǁ_save_checkpoint__mutmut_93': xǁTrainerǁ_save_checkpoint__mutmut_93, 
        'xǁTrainerǁ_save_checkpoint__mutmut_94': xǁTrainerǁ_save_checkpoint__mutmut_94, 
        'xǁTrainerǁ_save_checkpoint__mutmut_95': xǁTrainerǁ_save_checkpoint__mutmut_95
    }
    
    def _save_checkpoint(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁTrainerǁ_save_checkpoint__mutmut_orig"), object.__getattribute__(self, "xǁTrainerǁ_save_checkpoint__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _save_checkpoint.__signature__ = _mutmut_signature(xǁTrainerǁ_save_checkpoint__mutmut_orig)
    xǁTrainerǁ_save_checkpoint__mutmut_orig.__name__ = 'xǁTrainerǁ_save_checkpoint'

    def xǁTrainerǁevaluate__mutmut_orig(self) -> Mapping[str, float]:
        if self.val_loader is None:
            raise RuntimeError("Validation loader is not configured")
        self.simple.model.eval()
        losses: list[float] = []
        metrics: list[MutableMapping[str, float]] = []
        with torch.no_grad():
            for batch in self.val_loader:
                inputs, labels = self._prepare_batch(batch)
                outputs = self._forward(inputs)
                batch_metrics = self._compute_metrics(outputs, labels)
                losses.append(batch_metrics.get("val_loss", 0.0))
                metrics.append(batch_metrics)
        self.simple.model.train()
        aggregated: MutableMapping[str, float] = {}
        if losses:
            aggregated["val_loss"] = float(sum(losses) / len(losses))
        keys = {key for metric in metrics for key in metric}
        for key in keys:
            values = [metric[key] for metric in metrics if key in metric]
            if values:
                aggregated[key] = float(sum(values) / len(values))
        return aggregated

    def xǁTrainerǁevaluate__mutmut_1(self) -> Mapping[str, float]:
        if self.val_loader is not None:
            raise RuntimeError("Validation loader is not configured")
        self.simple.model.eval()
        losses: list[float] = []
        metrics: list[MutableMapping[str, float]] = []
        with torch.no_grad():
            for batch in self.val_loader:
                inputs, labels = self._prepare_batch(batch)
                outputs = self._forward(inputs)
                batch_metrics = self._compute_metrics(outputs, labels)
                losses.append(batch_metrics.get("val_loss", 0.0))
                metrics.append(batch_metrics)
        self.simple.model.train()
        aggregated: MutableMapping[str, float] = {}
        if losses:
            aggregated["val_loss"] = float(sum(losses) / len(losses))
        keys = {key for metric in metrics for key in metric}
        for key in keys:
            values = [metric[key] for metric in metrics if key in metric]
            if values:
                aggregated[key] = float(sum(values) / len(values))
        return aggregated

    def xǁTrainerǁevaluate__mutmut_2(self) -> Mapping[str, float]:
        if self.val_loader is None:
            raise RuntimeError(None)
        self.simple.model.eval()
        losses: list[float] = []
        metrics: list[MutableMapping[str, float]] = []
        with torch.no_grad():
            for batch in self.val_loader:
                inputs, labels = self._prepare_batch(batch)
                outputs = self._forward(inputs)
                batch_metrics = self._compute_metrics(outputs, labels)
                losses.append(batch_metrics.get("val_loss", 0.0))
                metrics.append(batch_metrics)
        self.simple.model.train()
        aggregated: MutableMapping[str, float] = {}
        if losses:
            aggregated["val_loss"] = float(sum(losses) / len(losses))
        keys = {key for metric in metrics for key in metric}
        for key in keys:
            values = [metric[key] for metric in metrics if key in metric]
            if values:
                aggregated[key] = float(sum(values) / len(values))
        return aggregated

    def xǁTrainerǁevaluate__mutmut_3(self) -> Mapping[str, float]:
        if self.val_loader is None:
            raise RuntimeError("XXValidation loader is not configuredXX")
        self.simple.model.eval()
        losses: list[float] = []
        metrics: list[MutableMapping[str, float]] = []
        with torch.no_grad():
            for batch in self.val_loader:
                inputs, labels = self._prepare_batch(batch)
                outputs = self._forward(inputs)
                batch_metrics = self._compute_metrics(outputs, labels)
                losses.append(batch_metrics.get("val_loss", 0.0))
                metrics.append(batch_metrics)
        self.simple.model.train()
        aggregated: MutableMapping[str, float] = {}
        if losses:
            aggregated["val_loss"] = float(sum(losses) / len(losses))
        keys = {key for metric in metrics for key in metric}
        for key in keys:
            values = [metric[key] for metric in metrics if key in metric]
            if values:
                aggregated[key] = float(sum(values) / len(values))
        return aggregated

    def xǁTrainerǁevaluate__mutmut_4(self) -> Mapping[str, float]:
        if self.val_loader is None:
            raise RuntimeError("validation loader is not configured")
        self.simple.model.eval()
        losses: list[float] = []
        metrics: list[MutableMapping[str, float]] = []
        with torch.no_grad():
            for batch in self.val_loader:
                inputs, labels = self._prepare_batch(batch)
                outputs = self._forward(inputs)
                batch_metrics = self._compute_metrics(outputs, labels)
                losses.append(batch_metrics.get("val_loss", 0.0))
                metrics.append(batch_metrics)
        self.simple.model.train()
        aggregated: MutableMapping[str, float] = {}
        if losses:
            aggregated["val_loss"] = float(sum(losses) / len(losses))
        keys = {key for metric in metrics for key in metric}
        for key in keys:
            values = [metric[key] for metric in metrics if key in metric]
            if values:
                aggregated[key] = float(sum(values) / len(values))
        return aggregated

    def xǁTrainerǁevaluate__mutmut_5(self) -> Mapping[str, float]:
        if self.val_loader is None:
            raise RuntimeError("VALIDATION LOADER IS NOT CONFIGURED")
        self.simple.model.eval()
        losses: list[float] = []
        metrics: list[MutableMapping[str, float]] = []
        with torch.no_grad():
            for batch in self.val_loader:
                inputs, labels = self._prepare_batch(batch)
                outputs = self._forward(inputs)
                batch_metrics = self._compute_metrics(outputs, labels)
                losses.append(batch_metrics.get("val_loss", 0.0))
                metrics.append(batch_metrics)
        self.simple.model.train()
        aggregated: MutableMapping[str, float] = {}
        if losses:
            aggregated["val_loss"] = float(sum(losses) / len(losses))
        keys = {key for metric in metrics for key in metric}
        for key in keys:
            values = [metric[key] for metric in metrics if key in metric]
            if values:
                aggregated[key] = float(sum(values) / len(values))
        return aggregated

    def xǁTrainerǁevaluate__mutmut_6(self) -> Mapping[str, float]:
        if self.val_loader is None:
            raise RuntimeError("Validation loader is not configured")
        self.simple.model.eval()
        losses: list[float] = None
        metrics: list[MutableMapping[str, float]] = []
        with torch.no_grad():
            for batch in self.val_loader:
                inputs, labels = self._prepare_batch(batch)
                outputs = self._forward(inputs)
                batch_metrics = self._compute_metrics(outputs, labels)
                losses.append(batch_metrics.get("val_loss", 0.0))
                metrics.append(batch_metrics)
        self.simple.model.train()
        aggregated: MutableMapping[str, float] = {}
        if losses:
            aggregated["val_loss"] = float(sum(losses) / len(losses))
        keys = {key for metric in metrics for key in metric}
        for key in keys:
            values = [metric[key] for metric in metrics if key in metric]
            if values:
                aggregated[key] = float(sum(values) / len(values))
        return aggregated

    def xǁTrainerǁevaluate__mutmut_7(self) -> Mapping[str, float]:
        if self.val_loader is None:
            raise RuntimeError("Validation loader is not configured")
        self.simple.model.eval()
        losses: list[float] = []
        metrics: list[MutableMapping[str, float]] = None
        with torch.no_grad():
            for batch in self.val_loader:
                inputs, labels = self._prepare_batch(batch)
                outputs = self._forward(inputs)
                batch_metrics = self._compute_metrics(outputs, labels)
                losses.append(batch_metrics.get("val_loss", 0.0))
                metrics.append(batch_metrics)
        self.simple.model.train()
        aggregated: MutableMapping[str, float] = {}
        if losses:
            aggregated["val_loss"] = float(sum(losses) / len(losses))
        keys = {key for metric in metrics for key in metric}
        for key in keys:
            values = [metric[key] for metric in metrics if key in metric]
            if values:
                aggregated[key] = float(sum(values) / len(values))
        return aggregated

    def xǁTrainerǁevaluate__mutmut_8(self) -> Mapping[str, float]:
        if self.val_loader is None:
            raise RuntimeError("Validation loader is not configured")
        self.simple.model.eval()
        losses: list[float] = []
        metrics: list[MutableMapping[str, float]] = []
        with torch.no_grad():
            for batch in self.val_loader:
                inputs, labels = None
                outputs = self._forward(inputs)
                batch_metrics = self._compute_metrics(outputs, labels)
                losses.append(batch_metrics.get("val_loss", 0.0))
                metrics.append(batch_metrics)
        self.simple.model.train()
        aggregated: MutableMapping[str, float] = {}
        if losses:
            aggregated["val_loss"] = float(sum(losses) / len(losses))
        keys = {key for metric in metrics for key in metric}
        for key in keys:
            values = [metric[key] for metric in metrics if key in metric]
            if values:
                aggregated[key] = float(sum(values) / len(values))
        return aggregated

    def xǁTrainerǁevaluate__mutmut_9(self) -> Mapping[str, float]:
        if self.val_loader is None:
            raise RuntimeError("Validation loader is not configured")
        self.simple.model.eval()
        losses: list[float] = []
        metrics: list[MutableMapping[str, float]] = []
        with torch.no_grad():
            for batch in self.val_loader:
                inputs, labels = self._prepare_batch(None)
                outputs = self._forward(inputs)
                batch_metrics = self._compute_metrics(outputs, labels)
                losses.append(batch_metrics.get("val_loss", 0.0))
                metrics.append(batch_metrics)
        self.simple.model.train()
        aggregated: MutableMapping[str, float] = {}
        if losses:
            aggregated["val_loss"] = float(sum(losses) / len(losses))
        keys = {key for metric in metrics for key in metric}
        for key in keys:
            values = [metric[key] for metric in metrics if key in metric]
            if values:
                aggregated[key] = float(sum(values) / len(values))
        return aggregated

    def xǁTrainerǁevaluate__mutmut_10(self) -> Mapping[str, float]:
        if self.val_loader is None:
            raise RuntimeError("Validation loader is not configured")
        self.simple.model.eval()
        losses: list[float] = []
        metrics: list[MutableMapping[str, float]] = []
        with torch.no_grad():
            for batch in self.val_loader:
                inputs, labels = self._prepare_batch(batch)
                outputs = None
                batch_metrics = self._compute_metrics(outputs, labels)
                losses.append(batch_metrics.get("val_loss", 0.0))
                metrics.append(batch_metrics)
        self.simple.model.train()
        aggregated: MutableMapping[str, float] = {}
        if losses:
            aggregated["val_loss"] = float(sum(losses) / len(losses))
        keys = {key for metric in metrics for key in metric}
        for key in keys:
            values = [metric[key] for metric in metrics if key in metric]
            if values:
                aggregated[key] = float(sum(values) / len(values))
        return aggregated

    def xǁTrainerǁevaluate__mutmut_11(self) -> Mapping[str, float]:
        if self.val_loader is None:
            raise RuntimeError("Validation loader is not configured")
        self.simple.model.eval()
        losses: list[float] = []
        metrics: list[MutableMapping[str, float]] = []
        with torch.no_grad():
            for batch in self.val_loader:
                inputs, labels = self._prepare_batch(batch)
                outputs = self._forward(None)
                batch_metrics = self._compute_metrics(outputs, labels)
                losses.append(batch_metrics.get("val_loss", 0.0))
                metrics.append(batch_metrics)
        self.simple.model.train()
        aggregated: MutableMapping[str, float] = {}
        if losses:
            aggregated["val_loss"] = float(sum(losses) / len(losses))
        keys = {key for metric in metrics for key in metric}
        for key in keys:
            values = [metric[key] for metric in metrics if key in metric]
            if values:
                aggregated[key] = float(sum(values) / len(values))
        return aggregated

    def xǁTrainerǁevaluate__mutmut_12(self) -> Mapping[str, float]:
        if self.val_loader is None:
            raise RuntimeError("Validation loader is not configured")
        self.simple.model.eval()
        losses: list[float] = []
        metrics: list[MutableMapping[str, float]] = []
        with torch.no_grad():
            for batch in self.val_loader:
                inputs, labels = self._prepare_batch(batch)
                outputs = self._forward(inputs)
                batch_metrics = None
                losses.append(batch_metrics.get("val_loss", 0.0))
                metrics.append(batch_metrics)
        self.simple.model.train()
        aggregated: MutableMapping[str, float] = {}
        if losses:
            aggregated["val_loss"] = float(sum(losses) / len(losses))
        keys = {key for metric in metrics for key in metric}
        for key in keys:
            values = [metric[key] for metric in metrics if key in metric]
            if values:
                aggregated[key] = float(sum(values) / len(values))
        return aggregated

    def xǁTrainerǁevaluate__mutmut_13(self) -> Mapping[str, float]:
        if self.val_loader is None:
            raise RuntimeError("Validation loader is not configured")
        self.simple.model.eval()
        losses: list[float] = []
        metrics: list[MutableMapping[str, float]] = []
        with torch.no_grad():
            for batch in self.val_loader:
                inputs, labels = self._prepare_batch(batch)
                outputs = self._forward(inputs)
                batch_metrics = self._compute_metrics(None, labels)
                losses.append(batch_metrics.get("val_loss", 0.0))
                metrics.append(batch_metrics)
        self.simple.model.train()
        aggregated: MutableMapping[str, float] = {}
        if losses:
            aggregated["val_loss"] = float(sum(losses) / len(losses))
        keys = {key for metric in metrics for key in metric}
        for key in keys:
            values = [metric[key] for metric in metrics if key in metric]
            if values:
                aggregated[key] = float(sum(values) / len(values))
        return aggregated

    def xǁTrainerǁevaluate__mutmut_14(self) -> Mapping[str, float]:
        if self.val_loader is None:
            raise RuntimeError("Validation loader is not configured")
        self.simple.model.eval()
        losses: list[float] = []
        metrics: list[MutableMapping[str, float]] = []
        with torch.no_grad():
            for batch in self.val_loader:
                inputs, labels = self._prepare_batch(batch)
                outputs = self._forward(inputs)
                batch_metrics = self._compute_metrics(outputs, None)
                losses.append(batch_metrics.get("val_loss", 0.0))
                metrics.append(batch_metrics)
        self.simple.model.train()
        aggregated: MutableMapping[str, float] = {}
        if losses:
            aggregated["val_loss"] = float(sum(losses) / len(losses))
        keys = {key for metric in metrics for key in metric}
        for key in keys:
            values = [metric[key] for metric in metrics if key in metric]
            if values:
                aggregated[key] = float(sum(values) / len(values))
        return aggregated

    def xǁTrainerǁevaluate__mutmut_15(self) -> Mapping[str, float]:
        if self.val_loader is None:
            raise RuntimeError("Validation loader is not configured")
        self.simple.model.eval()
        losses: list[float] = []
        metrics: list[MutableMapping[str, float]] = []
        with torch.no_grad():
            for batch in self.val_loader:
                inputs, labels = self._prepare_batch(batch)
                outputs = self._forward(inputs)
                batch_metrics = self._compute_metrics(labels)
                losses.append(batch_metrics.get("val_loss", 0.0))
                metrics.append(batch_metrics)
        self.simple.model.train()
        aggregated: MutableMapping[str, float] = {}
        if losses:
            aggregated["val_loss"] = float(sum(losses) / len(losses))
        keys = {key for metric in metrics for key in metric}
        for key in keys:
            values = [metric[key] for metric in metrics if key in metric]
            if values:
                aggregated[key] = float(sum(values) / len(values))
        return aggregated

    def xǁTrainerǁevaluate__mutmut_16(self) -> Mapping[str, float]:
        if self.val_loader is None:
            raise RuntimeError("Validation loader is not configured")
        self.simple.model.eval()
        losses: list[float] = []
        metrics: list[MutableMapping[str, float]] = []
        with torch.no_grad():
            for batch in self.val_loader:
                inputs, labels = self._prepare_batch(batch)
                outputs = self._forward(inputs)
                batch_metrics = self._compute_metrics(outputs, )
                losses.append(batch_metrics.get("val_loss", 0.0))
                metrics.append(batch_metrics)
        self.simple.model.train()
        aggregated: MutableMapping[str, float] = {}
        if losses:
            aggregated["val_loss"] = float(sum(losses) / len(losses))
        keys = {key for metric in metrics for key in metric}
        for key in keys:
            values = [metric[key] for metric in metrics if key in metric]
            if values:
                aggregated[key] = float(sum(values) / len(values))
        return aggregated

    def xǁTrainerǁevaluate__mutmut_17(self) -> Mapping[str, float]:
        if self.val_loader is None:
            raise RuntimeError("Validation loader is not configured")
        self.simple.model.eval()
        losses: list[float] = []
        metrics: list[MutableMapping[str, float]] = []
        with torch.no_grad():
            for batch in self.val_loader:
                inputs, labels = self._prepare_batch(batch)
                outputs = self._forward(inputs)
                batch_metrics = self._compute_metrics(outputs, labels)
                losses.append(None)
                metrics.append(batch_metrics)
        self.simple.model.train()
        aggregated: MutableMapping[str, float] = {}
        if losses:
            aggregated["val_loss"] = float(sum(losses) / len(losses))
        keys = {key for metric in metrics for key in metric}
        for key in keys:
            values = [metric[key] for metric in metrics if key in metric]
            if values:
                aggregated[key] = float(sum(values) / len(values))
        return aggregated

    def xǁTrainerǁevaluate__mutmut_18(self) -> Mapping[str, float]:
        if self.val_loader is None:
            raise RuntimeError("Validation loader is not configured")
        self.simple.model.eval()
        losses: list[float] = []
        metrics: list[MutableMapping[str, float]] = []
        with torch.no_grad():
            for batch in self.val_loader:
                inputs, labels = self._prepare_batch(batch)
                outputs = self._forward(inputs)
                batch_metrics = self._compute_metrics(outputs, labels)
                losses.append(batch_metrics.get(None, 0.0))
                metrics.append(batch_metrics)
        self.simple.model.train()
        aggregated: MutableMapping[str, float] = {}
        if losses:
            aggregated["val_loss"] = float(sum(losses) / len(losses))
        keys = {key for metric in metrics for key in metric}
        for key in keys:
            values = [metric[key] for metric in metrics if key in metric]
            if values:
                aggregated[key] = float(sum(values) / len(values))
        return aggregated

    def xǁTrainerǁevaluate__mutmut_19(self) -> Mapping[str, float]:
        if self.val_loader is None:
            raise RuntimeError("Validation loader is not configured")
        self.simple.model.eval()
        losses: list[float] = []
        metrics: list[MutableMapping[str, float]] = []
        with torch.no_grad():
            for batch in self.val_loader:
                inputs, labels = self._prepare_batch(batch)
                outputs = self._forward(inputs)
                batch_metrics = self._compute_metrics(outputs, labels)
                losses.append(batch_metrics.get("val_loss", None))
                metrics.append(batch_metrics)
        self.simple.model.train()
        aggregated: MutableMapping[str, float] = {}
        if losses:
            aggregated["val_loss"] = float(sum(losses) / len(losses))
        keys = {key for metric in metrics for key in metric}
        for key in keys:
            values = [metric[key] for metric in metrics if key in metric]
            if values:
                aggregated[key] = float(sum(values) / len(values))
        return aggregated

    def xǁTrainerǁevaluate__mutmut_20(self) -> Mapping[str, float]:
        if self.val_loader is None:
            raise RuntimeError("Validation loader is not configured")
        self.simple.model.eval()
        losses: list[float] = []
        metrics: list[MutableMapping[str, float]] = []
        with torch.no_grad():
            for batch in self.val_loader:
                inputs, labels = self._prepare_batch(batch)
                outputs = self._forward(inputs)
                batch_metrics = self._compute_metrics(outputs, labels)
                losses.append(batch_metrics.get(0.0))
                metrics.append(batch_metrics)
        self.simple.model.train()
        aggregated: MutableMapping[str, float] = {}
        if losses:
            aggregated["val_loss"] = float(sum(losses) / len(losses))
        keys = {key for metric in metrics for key in metric}
        for key in keys:
            values = [metric[key] for metric in metrics if key in metric]
            if values:
                aggregated[key] = float(sum(values) / len(values))
        return aggregated

    def xǁTrainerǁevaluate__mutmut_21(self) -> Mapping[str, float]:
        if self.val_loader is None:
            raise RuntimeError("Validation loader is not configured")
        self.simple.model.eval()
        losses: list[float] = []
        metrics: list[MutableMapping[str, float]] = []
        with torch.no_grad():
            for batch in self.val_loader:
                inputs, labels = self._prepare_batch(batch)
                outputs = self._forward(inputs)
                batch_metrics = self._compute_metrics(outputs, labels)
                losses.append(batch_metrics.get("val_loss", ))
                metrics.append(batch_metrics)
        self.simple.model.train()
        aggregated: MutableMapping[str, float] = {}
        if losses:
            aggregated["val_loss"] = float(sum(losses) / len(losses))
        keys = {key for metric in metrics for key in metric}
        for key in keys:
            values = [metric[key] for metric in metrics if key in metric]
            if values:
                aggregated[key] = float(sum(values) / len(values))
        return aggregated

    def xǁTrainerǁevaluate__mutmut_22(self) -> Mapping[str, float]:
        if self.val_loader is None:
            raise RuntimeError("Validation loader is not configured")
        self.simple.model.eval()
        losses: list[float] = []
        metrics: list[MutableMapping[str, float]] = []
        with torch.no_grad():
            for batch in self.val_loader:
                inputs, labels = self._prepare_batch(batch)
                outputs = self._forward(inputs)
                batch_metrics = self._compute_metrics(outputs, labels)
                losses.append(batch_metrics.get("XXval_lossXX", 0.0))
                metrics.append(batch_metrics)
        self.simple.model.train()
        aggregated: MutableMapping[str, float] = {}
        if losses:
            aggregated["val_loss"] = float(sum(losses) / len(losses))
        keys = {key for metric in metrics for key in metric}
        for key in keys:
            values = [metric[key] for metric in metrics if key in metric]
            if values:
                aggregated[key] = float(sum(values) / len(values))
        return aggregated

    def xǁTrainerǁevaluate__mutmut_23(self) -> Mapping[str, float]:
        if self.val_loader is None:
            raise RuntimeError("Validation loader is not configured")
        self.simple.model.eval()
        losses: list[float] = []
        metrics: list[MutableMapping[str, float]] = []
        with torch.no_grad():
            for batch in self.val_loader:
                inputs, labels = self._prepare_batch(batch)
                outputs = self._forward(inputs)
                batch_metrics = self._compute_metrics(outputs, labels)
                losses.append(batch_metrics.get("VAL_LOSS", 0.0))
                metrics.append(batch_metrics)
        self.simple.model.train()
        aggregated: MutableMapping[str, float] = {}
        if losses:
            aggregated["val_loss"] = float(sum(losses) / len(losses))
        keys = {key for metric in metrics for key in metric}
        for key in keys:
            values = [metric[key] for metric in metrics if key in metric]
            if values:
                aggregated[key] = float(sum(values) / len(values))
        return aggregated

    def xǁTrainerǁevaluate__mutmut_24(self) -> Mapping[str, float]:
        if self.val_loader is None:
            raise RuntimeError("Validation loader is not configured")
        self.simple.model.eval()
        losses: list[float] = []
        metrics: list[MutableMapping[str, float]] = []
        with torch.no_grad():
            for batch in self.val_loader:
                inputs, labels = self._prepare_batch(batch)
                outputs = self._forward(inputs)
                batch_metrics = self._compute_metrics(outputs, labels)
                losses.append(batch_metrics.get("val_loss", 1.0))
                metrics.append(batch_metrics)
        self.simple.model.train()
        aggregated: MutableMapping[str, float] = {}
        if losses:
            aggregated["val_loss"] = float(sum(losses) / len(losses))
        keys = {key for metric in metrics for key in metric}
        for key in keys:
            values = [metric[key] for metric in metrics if key in metric]
            if values:
                aggregated[key] = float(sum(values) / len(values))
        return aggregated

    def xǁTrainerǁevaluate__mutmut_25(self) -> Mapping[str, float]:
        if self.val_loader is None:
            raise RuntimeError("Validation loader is not configured")
        self.simple.model.eval()
        losses: list[float] = []
        metrics: list[MutableMapping[str, float]] = []
        with torch.no_grad():
            for batch in self.val_loader:
                inputs, labels = self._prepare_batch(batch)
                outputs = self._forward(inputs)
                batch_metrics = self._compute_metrics(outputs, labels)
                losses.append(batch_metrics.get("val_loss", 0.0))
                metrics.append(None)
        self.simple.model.train()
        aggregated: MutableMapping[str, float] = {}
        if losses:
            aggregated["val_loss"] = float(sum(losses) / len(losses))
        keys = {key for metric in metrics for key in metric}
        for key in keys:
            values = [metric[key] for metric in metrics if key in metric]
            if values:
                aggregated[key] = float(sum(values) / len(values))
        return aggregated

    def xǁTrainerǁevaluate__mutmut_26(self) -> Mapping[str, float]:
        if self.val_loader is None:
            raise RuntimeError("Validation loader is not configured")
        self.simple.model.eval()
        losses: list[float] = []
        metrics: list[MutableMapping[str, float]] = []
        with torch.no_grad():
            for batch in self.val_loader:
                inputs, labels = self._prepare_batch(batch)
                outputs = self._forward(inputs)
                batch_metrics = self._compute_metrics(outputs, labels)
                losses.append(batch_metrics.get("val_loss", 0.0))
                metrics.append(batch_metrics)
        self.simple.model.train()
        aggregated: MutableMapping[str, float] = None
        if losses:
            aggregated["val_loss"] = float(sum(losses) / len(losses))
        keys = {key for metric in metrics for key in metric}
        for key in keys:
            values = [metric[key] for metric in metrics if key in metric]
            if values:
                aggregated[key] = float(sum(values) / len(values))
        return aggregated

    def xǁTrainerǁevaluate__mutmut_27(self) -> Mapping[str, float]:
        if self.val_loader is None:
            raise RuntimeError("Validation loader is not configured")
        self.simple.model.eval()
        losses: list[float] = []
        metrics: list[MutableMapping[str, float]] = []
        with torch.no_grad():
            for batch in self.val_loader:
                inputs, labels = self._prepare_batch(batch)
                outputs = self._forward(inputs)
                batch_metrics = self._compute_metrics(outputs, labels)
                losses.append(batch_metrics.get("val_loss", 0.0))
                metrics.append(batch_metrics)
        self.simple.model.train()
        aggregated: MutableMapping[str, float] = {}
        if losses:
            aggregated["val_loss"] = None
        keys = {key for metric in metrics for key in metric}
        for key in keys:
            values = [metric[key] for metric in metrics if key in metric]
            if values:
                aggregated[key] = float(sum(values) / len(values))
        return aggregated

    def xǁTrainerǁevaluate__mutmut_28(self) -> Mapping[str, float]:
        if self.val_loader is None:
            raise RuntimeError("Validation loader is not configured")
        self.simple.model.eval()
        losses: list[float] = []
        metrics: list[MutableMapping[str, float]] = []
        with torch.no_grad():
            for batch in self.val_loader:
                inputs, labels = self._prepare_batch(batch)
                outputs = self._forward(inputs)
                batch_metrics = self._compute_metrics(outputs, labels)
                losses.append(batch_metrics.get("val_loss", 0.0))
                metrics.append(batch_metrics)
        self.simple.model.train()
        aggregated: MutableMapping[str, float] = {}
        if losses:
            aggregated["XXval_lossXX"] = float(sum(losses) / len(losses))
        keys = {key for metric in metrics for key in metric}
        for key in keys:
            values = [metric[key] for metric in metrics if key in metric]
            if values:
                aggregated[key] = float(sum(values) / len(values))
        return aggregated

    def xǁTrainerǁevaluate__mutmut_29(self) -> Mapping[str, float]:
        if self.val_loader is None:
            raise RuntimeError("Validation loader is not configured")
        self.simple.model.eval()
        losses: list[float] = []
        metrics: list[MutableMapping[str, float]] = []
        with torch.no_grad():
            for batch in self.val_loader:
                inputs, labels = self._prepare_batch(batch)
                outputs = self._forward(inputs)
                batch_metrics = self._compute_metrics(outputs, labels)
                losses.append(batch_metrics.get("val_loss", 0.0))
                metrics.append(batch_metrics)
        self.simple.model.train()
        aggregated: MutableMapping[str, float] = {}
        if losses:
            aggregated["VAL_LOSS"] = float(sum(losses) / len(losses))
        keys = {key for metric in metrics for key in metric}
        for key in keys:
            values = [metric[key] for metric in metrics if key in metric]
            if values:
                aggregated[key] = float(sum(values) / len(values))
        return aggregated

    def xǁTrainerǁevaluate__mutmut_30(self) -> Mapping[str, float]:
        if self.val_loader is None:
            raise RuntimeError("Validation loader is not configured")
        self.simple.model.eval()
        losses: list[float] = []
        metrics: list[MutableMapping[str, float]] = []
        with torch.no_grad():
            for batch in self.val_loader:
                inputs, labels = self._prepare_batch(batch)
                outputs = self._forward(inputs)
                batch_metrics = self._compute_metrics(outputs, labels)
                losses.append(batch_metrics.get("val_loss", 0.0))
                metrics.append(batch_metrics)
        self.simple.model.train()
        aggregated: MutableMapping[str, float] = {}
        if losses:
            aggregated["val_loss"] = float(None)
        keys = {key for metric in metrics for key in metric}
        for key in keys:
            values = [metric[key] for metric in metrics if key in metric]
            if values:
                aggregated[key] = float(sum(values) / len(values))
        return aggregated

    def xǁTrainerǁevaluate__mutmut_31(self) -> Mapping[str, float]:
        if self.val_loader is None:
            raise RuntimeError("Validation loader is not configured")
        self.simple.model.eval()
        losses: list[float] = []
        metrics: list[MutableMapping[str, float]] = []
        with torch.no_grad():
            for batch in self.val_loader:
                inputs, labels = self._prepare_batch(batch)
                outputs = self._forward(inputs)
                batch_metrics = self._compute_metrics(outputs, labels)
                losses.append(batch_metrics.get("val_loss", 0.0))
                metrics.append(batch_metrics)
        self.simple.model.train()
        aggregated: MutableMapping[str, float] = {}
        if losses:
            aggregated["val_loss"] = float(sum(losses) * len(losses))
        keys = {key for metric in metrics for key in metric}
        for key in keys:
            values = [metric[key] for metric in metrics if key in metric]
            if values:
                aggregated[key] = float(sum(values) / len(values))
        return aggregated

    def xǁTrainerǁevaluate__mutmut_32(self) -> Mapping[str, float]:
        if self.val_loader is None:
            raise RuntimeError("Validation loader is not configured")
        self.simple.model.eval()
        losses: list[float] = []
        metrics: list[MutableMapping[str, float]] = []
        with torch.no_grad():
            for batch in self.val_loader:
                inputs, labels = self._prepare_batch(batch)
                outputs = self._forward(inputs)
                batch_metrics = self._compute_metrics(outputs, labels)
                losses.append(batch_metrics.get("val_loss", 0.0))
                metrics.append(batch_metrics)
        self.simple.model.train()
        aggregated: MutableMapping[str, float] = {}
        if losses:
            aggregated["val_loss"] = float(sum(None) / len(losses))
        keys = {key for metric in metrics for key in metric}
        for key in keys:
            values = [metric[key] for metric in metrics if key in metric]
            if values:
                aggregated[key] = float(sum(values) / len(values))
        return aggregated

    def xǁTrainerǁevaluate__mutmut_33(self) -> Mapping[str, float]:
        if self.val_loader is None:
            raise RuntimeError("Validation loader is not configured")
        self.simple.model.eval()
        losses: list[float] = []
        metrics: list[MutableMapping[str, float]] = []
        with torch.no_grad():
            for batch in self.val_loader:
                inputs, labels = self._prepare_batch(batch)
                outputs = self._forward(inputs)
                batch_metrics = self._compute_metrics(outputs, labels)
                losses.append(batch_metrics.get("val_loss", 0.0))
                metrics.append(batch_metrics)
        self.simple.model.train()
        aggregated: MutableMapping[str, float] = {}
        if losses:
            aggregated["val_loss"] = float(sum(losses) / len(losses))
        keys = None
        for key in keys:
            values = [metric[key] for metric in metrics if key in metric]
            if values:
                aggregated[key] = float(sum(values) / len(values))
        return aggregated

    def xǁTrainerǁevaluate__mutmut_34(self) -> Mapping[str, float]:
        if self.val_loader is None:
            raise RuntimeError("Validation loader is not configured")
        self.simple.model.eval()
        losses: list[float] = []
        metrics: list[MutableMapping[str, float]] = []
        with torch.no_grad():
            for batch in self.val_loader:
                inputs, labels = self._prepare_batch(batch)
                outputs = self._forward(inputs)
                batch_metrics = self._compute_metrics(outputs, labels)
                losses.append(batch_metrics.get("val_loss", 0.0))
                metrics.append(batch_metrics)
        self.simple.model.train()
        aggregated: MutableMapping[str, float] = {}
        if losses:
            aggregated["val_loss"] = float(sum(losses) / len(losses))
        keys = {key for metric in metrics for key in metric}
        for key in keys:
            values = None
            if values:
                aggregated[key] = float(sum(values) / len(values))
        return aggregated

    def xǁTrainerǁevaluate__mutmut_35(self) -> Mapping[str, float]:
        if self.val_loader is None:
            raise RuntimeError("Validation loader is not configured")
        self.simple.model.eval()
        losses: list[float] = []
        metrics: list[MutableMapping[str, float]] = []
        with torch.no_grad():
            for batch in self.val_loader:
                inputs, labels = self._prepare_batch(batch)
                outputs = self._forward(inputs)
                batch_metrics = self._compute_metrics(outputs, labels)
                losses.append(batch_metrics.get("val_loss", 0.0))
                metrics.append(batch_metrics)
        self.simple.model.train()
        aggregated: MutableMapping[str, float] = {}
        if losses:
            aggregated["val_loss"] = float(sum(losses) / len(losses))
        keys = {key for metric in metrics for key in metric}
        for key in keys:
            values = [metric[key] for metric in metrics if key not in metric]
            if values:
                aggregated[key] = float(sum(values) / len(values))
        return aggregated

    def xǁTrainerǁevaluate__mutmut_36(self) -> Mapping[str, float]:
        if self.val_loader is None:
            raise RuntimeError("Validation loader is not configured")
        self.simple.model.eval()
        losses: list[float] = []
        metrics: list[MutableMapping[str, float]] = []
        with torch.no_grad():
            for batch in self.val_loader:
                inputs, labels = self._prepare_batch(batch)
                outputs = self._forward(inputs)
                batch_metrics = self._compute_metrics(outputs, labels)
                losses.append(batch_metrics.get("val_loss", 0.0))
                metrics.append(batch_metrics)
        self.simple.model.train()
        aggregated: MutableMapping[str, float] = {}
        if losses:
            aggregated["val_loss"] = float(sum(losses) / len(losses))
        keys = {key for metric in metrics for key in metric}
        for key in keys:
            values = [metric[key] for metric in metrics if key in metric]
            if values:
                aggregated[key] = None
        return aggregated

    def xǁTrainerǁevaluate__mutmut_37(self) -> Mapping[str, float]:
        if self.val_loader is None:
            raise RuntimeError("Validation loader is not configured")
        self.simple.model.eval()
        losses: list[float] = []
        metrics: list[MutableMapping[str, float]] = []
        with torch.no_grad():
            for batch in self.val_loader:
                inputs, labels = self._prepare_batch(batch)
                outputs = self._forward(inputs)
                batch_metrics = self._compute_metrics(outputs, labels)
                losses.append(batch_metrics.get("val_loss", 0.0))
                metrics.append(batch_metrics)
        self.simple.model.train()
        aggregated: MutableMapping[str, float] = {}
        if losses:
            aggregated["val_loss"] = float(sum(losses) / len(losses))
        keys = {key for metric in metrics for key in metric}
        for key in keys:
            values = [metric[key] for metric in metrics if key in metric]
            if values:
                aggregated[key] = float(None)
        return aggregated

    def xǁTrainerǁevaluate__mutmut_38(self) -> Mapping[str, float]:
        if self.val_loader is None:
            raise RuntimeError("Validation loader is not configured")
        self.simple.model.eval()
        losses: list[float] = []
        metrics: list[MutableMapping[str, float]] = []
        with torch.no_grad():
            for batch in self.val_loader:
                inputs, labels = self._prepare_batch(batch)
                outputs = self._forward(inputs)
                batch_metrics = self._compute_metrics(outputs, labels)
                losses.append(batch_metrics.get("val_loss", 0.0))
                metrics.append(batch_metrics)
        self.simple.model.train()
        aggregated: MutableMapping[str, float] = {}
        if losses:
            aggregated["val_loss"] = float(sum(losses) / len(losses))
        keys = {key for metric in metrics for key in metric}
        for key in keys:
            values = [metric[key] for metric in metrics if key in metric]
            if values:
                aggregated[key] = float(sum(values) * len(values))
        return aggregated

    def xǁTrainerǁevaluate__mutmut_39(self) -> Mapping[str, float]:
        if self.val_loader is None:
            raise RuntimeError("Validation loader is not configured")
        self.simple.model.eval()
        losses: list[float] = []
        metrics: list[MutableMapping[str, float]] = []
        with torch.no_grad():
            for batch in self.val_loader:
                inputs, labels = self._prepare_batch(batch)
                outputs = self._forward(inputs)
                batch_metrics = self._compute_metrics(outputs, labels)
                losses.append(batch_metrics.get("val_loss", 0.0))
                metrics.append(batch_metrics)
        self.simple.model.train()
        aggregated: MutableMapping[str, float] = {}
        if losses:
            aggregated["val_loss"] = float(sum(losses) / len(losses))
        keys = {key for metric in metrics for key in metric}
        for key in keys:
            values = [metric[key] for metric in metrics if key in metric]
            if values:
                aggregated[key] = float(sum(None) / len(values))
        return aggregated
    
    xǁTrainerǁevaluate__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁTrainerǁevaluate__mutmut_1': xǁTrainerǁevaluate__mutmut_1, 
        'xǁTrainerǁevaluate__mutmut_2': xǁTrainerǁevaluate__mutmut_2, 
        'xǁTrainerǁevaluate__mutmut_3': xǁTrainerǁevaluate__mutmut_3, 
        'xǁTrainerǁevaluate__mutmut_4': xǁTrainerǁevaluate__mutmut_4, 
        'xǁTrainerǁevaluate__mutmut_5': xǁTrainerǁevaluate__mutmut_5, 
        'xǁTrainerǁevaluate__mutmut_6': xǁTrainerǁevaluate__mutmut_6, 
        'xǁTrainerǁevaluate__mutmut_7': xǁTrainerǁevaluate__mutmut_7, 
        'xǁTrainerǁevaluate__mutmut_8': xǁTrainerǁevaluate__mutmut_8, 
        'xǁTrainerǁevaluate__mutmut_9': xǁTrainerǁevaluate__mutmut_9, 
        'xǁTrainerǁevaluate__mutmut_10': xǁTrainerǁevaluate__mutmut_10, 
        'xǁTrainerǁevaluate__mutmut_11': xǁTrainerǁevaluate__mutmut_11, 
        'xǁTrainerǁevaluate__mutmut_12': xǁTrainerǁevaluate__mutmut_12, 
        'xǁTrainerǁevaluate__mutmut_13': xǁTrainerǁevaluate__mutmut_13, 
        'xǁTrainerǁevaluate__mutmut_14': xǁTrainerǁevaluate__mutmut_14, 
        'xǁTrainerǁevaluate__mutmut_15': xǁTrainerǁevaluate__mutmut_15, 
        'xǁTrainerǁevaluate__mutmut_16': xǁTrainerǁevaluate__mutmut_16, 
        'xǁTrainerǁevaluate__mutmut_17': xǁTrainerǁevaluate__mutmut_17, 
        'xǁTrainerǁevaluate__mutmut_18': xǁTrainerǁevaluate__mutmut_18, 
        'xǁTrainerǁevaluate__mutmut_19': xǁTrainerǁevaluate__mutmut_19, 
        'xǁTrainerǁevaluate__mutmut_20': xǁTrainerǁevaluate__mutmut_20, 
        'xǁTrainerǁevaluate__mutmut_21': xǁTrainerǁevaluate__mutmut_21, 
        'xǁTrainerǁevaluate__mutmut_22': xǁTrainerǁevaluate__mutmut_22, 
        'xǁTrainerǁevaluate__mutmut_23': xǁTrainerǁevaluate__mutmut_23, 
        'xǁTrainerǁevaluate__mutmut_24': xǁTrainerǁevaluate__mutmut_24, 
        'xǁTrainerǁevaluate__mutmut_25': xǁTrainerǁevaluate__mutmut_25, 
        'xǁTrainerǁevaluate__mutmut_26': xǁTrainerǁevaluate__mutmut_26, 
        'xǁTrainerǁevaluate__mutmut_27': xǁTrainerǁevaluate__mutmut_27, 
        'xǁTrainerǁevaluate__mutmut_28': xǁTrainerǁevaluate__mutmut_28, 
        'xǁTrainerǁevaluate__mutmut_29': xǁTrainerǁevaluate__mutmut_29, 
        'xǁTrainerǁevaluate__mutmut_30': xǁTrainerǁevaluate__mutmut_30, 
        'xǁTrainerǁevaluate__mutmut_31': xǁTrainerǁevaluate__mutmut_31, 
        'xǁTrainerǁevaluate__mutmut_32': xǁTrainerǁevaluate__mutmut_32, 
        'xǁTrainerǁevaluate__mutmut_33': xǁTrainerǁevaluate__mutmut_33, 
        'xǁTrainerǁevaluate__mutmut_34': xǁTrainerǁevaluate__mutmut_34, 
        'xǁTrainerǁevaluate__mutmut_35': xǁTrainerǁevaluate__mutmut_35, 
        'xǁTrainerǁevaluate__mutmut_36': xǁTrainerǁevaluate__mutmut_36, 
        'xǁTrainerǁevaluate__mutmut_37': xǁTrainerǁevaluate__mutmut_37, 
        'xǁTrainerǁevaluate__mutmut_38': xǁTrainerǁevaluate__mutmut_38, 
        'xǁTrainerǁevaluate__mutmut_39': xǁTrainerǁevaluate__mutmut_39
    }
    
    def evaluate(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁTrainerǁevaluate__mutmut_orig"), object.__getattribute__(self, "xǁTrainerǁevaluate__mutmut_mutants"), args, kwargs, self)
        return result 
    
    evaluate.__signature__ = _mutmut_signature(xǁTrainerǁevaluate__mutmut_orig)
    xǁTrainerǁevaluate__mutmut_orig.__name__ = 'xǁTrainerǁevaluate'

    def xǁTrainerǁtrain__mutmut_orig(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_1(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = None
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_2(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_3(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = None
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_4(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(None)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_5(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = None
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_6(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = None
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_7(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(None, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_8(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, None)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_9(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_10(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, )
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_11(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(1, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_12(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = None

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_13(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch - 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_14(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 2

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_15(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                None,
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_16(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                None,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_17(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                None,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_18(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                None,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_19(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_20(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_21(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_22(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_23(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "XXResuming training loop from epoch %s (next epoch=%s, target=%s)XX",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_24(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_25(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "RESUMING TRAINING LOOP FROM EPOCH %S (NEXT EPOCH=%S, TARGET=%S)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_26(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch >= cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_27(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                None,
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_28(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                None,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_29(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                None,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_30(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_31(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_32(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_33(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "XXSkipping training loop; start_epoch=%s exceeds configured epochs=%sXX",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_34(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_35(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "SKIPPING TRAINING LOOP; START_EPOCH=%S EXCEEDS CONFIGURED EPOCHS=%S",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_36(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[+1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_37(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-2] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_38(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(None, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_39(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, None):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_40(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_41(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, ):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_42(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs - 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_43(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 2):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_44(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = None
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_45(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = None
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_46(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 1.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_47(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = None
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_48(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 1
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_49(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(None, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_50(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=None):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_51(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_52(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, ):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_53(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=2):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_54(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = None
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_55(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(None)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_56(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=None):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_57(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = None
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_58(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(None)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_59(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = None
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_60(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(None, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_61(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, None)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_62(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_63(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, )
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_64(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = None
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_65(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(None)
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_66(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss = loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_67(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss -= loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_68(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches = 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_69(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches -= 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_70(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 2
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_71(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = None
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_72(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss * grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_73(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(None).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_74(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = None
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_75(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 and step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_76(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step / grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_77(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps != 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_78(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 1 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_79(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step != len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_80(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_81(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(None)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_82(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            None, cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_83(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), None
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_84(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_85(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_86(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(None)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_87(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step = 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_88(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step -= 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_89(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 2

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_90(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step or (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_91(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps or should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_92(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step / cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_93(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps != 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_94(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 1)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_95(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        None,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_96(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        None,
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_97(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        None,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_98(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_99(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_100(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_101(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"XXtrain_lossXX": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_102(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"TRAIN_LOSS": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_103(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss * max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_104(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(None, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_105(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, None)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_106(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_107(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, )},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_108(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(2, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_109(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = None
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_110(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss * max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_111(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(None, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_112(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, None)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_113(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_114(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, )
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_115(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(2, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_116(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = None

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_117(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"XXtrain_lossXX": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_118(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"TRAIN_LOSS": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_119(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(None)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_120(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_121(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = None
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_122(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(None)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_123(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning(None, epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_124(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", None, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_125(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, None)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_126(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning(epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_127(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_128(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, )

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_129(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("XXValidation failed at epoch %s: %sXX", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_130(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_131(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("VALIDATION FAILED AT EPOCH %S: %S", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_132(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(None)
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_133(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(None))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_134(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(None, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_135(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, None, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_136(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, None)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_137(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_138(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_139(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, )
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_140(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_141(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = None
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_142(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"XXepochXX": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_143(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"EPOCH": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_144(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "XXglobal_stepXX": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_145(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "GLOBAL_STEP": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_146(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update(None)
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_147(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(None) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_148(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(None, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_149(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, None)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_150(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_151(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, )
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_152(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug(None, exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_153(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", None)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_154(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug(exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_155(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", )
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_156(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("XXFailed to write metrics NDJSON: %sXX", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_157(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("failed to write metrics ndjson: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_158(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("FAILED TO WRITE METRICS NDJSON: %S", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_159(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(None, epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_160(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, None)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_161(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch_metrics)

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_162(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, )

        return self.history[-1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_163(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[+1] if self.history else {}

    def xǁTrainerǁtrain__mutmut_164(self, *, epochs: int | None = None) -> list[Mapping[str, float]]:
        cfg = self.config
        if epochs is not None:
            cfg.epochs = int(epochs)
        grad_steps = cfg.gradient_accumulation_steps
        completed_epoch = max(0, self.state.epoch)
        start_epoch = completed_epoch + 1

        if completed_epoch:
            LOGGER.info(
                "Resuming training loop from epoch %s (next epoch=%s, target=%s)",
                completed_epoch,
                start_epoch,
                cfg.epochs,
            )

        if start_epoch > cfg.epochs:
            LOGGER.info(
                "Skipping training loop; start_epoch=%s exceeds configured epochs=%s",
                start_epoch,
                cfg.epochs,
            )
            return self.history[-1] if self.history else {}

        for epoch in range(start_epoch, cfg.epochs + 1):
            self.state.epoch = epoch
            running_loss = 0.0
            num_batches = 0
            self._zero_grad()

            for step, batch in enumerate(self.train_loader, start=1):
                inputs, labels = self._prepare_batch(batch)
                with autocast(enabled=cfg.mixed_precision):
                    outputs = self._forward(inputs)
                    loss = self.loss_fn(outputs, labels)
                loss_value = float(loss.detach().cpu().item())
                running_loss += loss_value
                num_batches += 1
                scaled_loss = loss / grad_steps
                self.scaler.scale(scaled_loss).backward()

                should_step = step % grad_steps == 0 or step == len(self.train_loader)
                if should_step:
                    if cfg.max_grad_norm is not None:
                        self.scaler.unscale_(self.simple.optimizer)
                        torch.nn.utils.clip_grad_norm_(
                            self.simple.model.parameters(), cfg.max_grad_norm
                        )
                    self.scaler.step(self.simple.optimizer)
                    self.scaler.update()
                    self._zero_grad()
                    self.state.global_step += 1

                if (
                    cfg.log_every_n_steps
                    and should_step
                    and (self.state.global_step % cfg.log_every_n_steps == 0)
                ):
                    log_metrics(
                        self._logging_session,
                        {"train_loss": running_loss / max(1, num_batches)},
                        self.state.global_step,
                    )

            avg_loss = running_loss / max(1, num_batches)
            epoch_metrics: MutableMapping[str, float] = {"train_loss": float(avg_loss)}

            if self.val_loader is not None:
                try:
                    eval_metrics = self.evaluate()
                    epoch_metrics.update(eval_metrics)
                except Exception as exc:  # pragma: no cover - evaluation robustness
                    LOGGER.warning("Validation failed at epoch %s: %s", epoch, exc)

            self.history.append(dict(epoch_metrics))
            log_metrics(self._logging_session, epoch_metrics, epoch)
            if self._metrics_path is not None:
                try:
                    record = {"epoch": epoch, "global_step": self.state.global_step}
                    record.update({k: float(v) for k, v in epoch_metrics.items()})
                    append_ndjson(record, self._metrics_path)
                except Exception as exc:  # pragma: no cover - diagnostics only
                    LOGGER.debug("Failed to write metrics NDJSON: %s", exc)
            self._save_checkpoint(epoch, epoch_metrics)

        return self.history[-2] if self.history else {}
    
    xǁTrainerǁtrain__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁTrainerǁtrain__mutmut_1': xǁTrainerǁtrain__mutmut_1, 
        'xǁTrainerǁtrain__mutmut_2': xǁTrainerǁtrain__mutmut_2, 
        'xǁTrainerǁtrain__mutmut_3': xǁTrainerǁtrain__mutmut_3, 
        'xǁTrainerǁtrain__mutmut_4': xǁTrainerǁtrain__mutmut_4, 
        'xǁTrainerǁtrain__mutmut_5': xǁTrainerǁtrain__mutmut_5, 
        'xǁTrainerǁtrain__mutmut_6': xǁTrainerǁtrain__mutmut_6, 
        'xǁTrainerǁtrain__mutmut_7': xǁTrainerǁtrain__mutmut_7, 
        'xǁTrainerǁtrain__mutmut_8': xǁTrainerǁtrain__mutmut_8, 
        'xǁTrainerǁtrain__mutmut_9': xǁTrainerǁtrain__mutmut_9, 
        'xǁTrainerǁtrain__mutmut_10': xǁTrainerǁtrain__mutmut_10, 
        'xǁTrainerǁtrain__mutmut_11': xǁTrainerǁtrain__mutmut_11, 
        'xǁTrainerǁtrain__mutmut_12': xǁTrainerǁtrain__mutmut_12, 
        'xǁTrainerǁtrain__mutmut_13': xǁTrainerǁtrain__mutmut_13, 
        'xǁTrainerǁtrain__mutmut_14': xǁTrainerǁtrain__mutmut_14, 
        'xǁTrainerǁtrain__mutmut_15': xǁTrainerǁtrain__mutmut_15, 
        'xǁTrainerǁtrain__mutmut_16': xǁTrainerǁtrain__mutmut_16, 
        'xǁTrainerǁtrain__mutmut_17': xǁTrainerǁtrain__mutmut_17, 
        'xǁTrainerǁtrain__mutmut_18': xǁTrainerǁtrain__mutmut_18, 
        'xǁTrainerǁtrain__mutmut_19': xǁTrainerǁtrain__mutmut_19, 
        'xǁTrainerǁtrain__mutmut_20': xǁTrainerǁtrain__mutmut_20, 
        'xǁTrainerǁtrain__mutmut_21': xǁTrainerǁtrain__mutmut_21, 
        'xǁTrainerǁtrain__mutmut_22': xǁTrainerǁtrain__mutmut_22, 
        'xǁTrainerǁtrain__mutmut_23': xǁTrainerǁtrain__mutmut_23, 
        'xǁTrainerǁtrain__mutmut_24': xǁTrainerǁtrain__mutmut_24, 
        'xǁTrainerǁtrain__mutmut_25': xǁTrainerǁtrain__mutmut_25, 
        'xǁTrainerǁtrain__mutmut_26': xǁTrainerǁtrain__mutmut_26, 
        'xǁTrainerǁtrain__mutmut_27': xǁTrainerǁtrain__mutmut_27, 
        'xǁTrainerǁtrain__mutmut_28': xǁTrainerǁtrain__mutmut_28, 
        'xǁTrainerǁtrain__mutmut_29': xǁTrainerǁtrain__mutmut_29, 
        'xǁTrainerǁtrain__mutmut_30': xǁTrainerǁtrain__mutmut_30, 
        'xǁTrainerǁtrain__mutmut_31': xǁTrainerǁtrain__mutmut_31, 
        'xǁTrainerǁtrain__mutmut_32': xǁTrainerǁtrain__mutmut_32, 
        'xǁTrainerǁtrain__mutmut_33': xǁTrainerǁtrain__mutmut_33, 
        'xǁTrainerǁtrain__mutmut_34': xǁTrainerǁtrain__mutmut_34, 
        'xǁTrainerǁtrain__mutmut_35': xǁTrainerǁtrain__mutmut_35, 
        'xǁTrainerǁtrain__mutmut_36': xǁTrainerǁtrain__mutmut_36, 
        'xǁTrainerǁtrain__mutmut_37': xǁTrainerǁtrain__mutmut_37, 
        'xǁTrainerǁtrain__mutmut_38': xǁTrainerǁtrain__mutmut_38, 
        'xǁTrainerǁtrain__mutmut_39': xǁTrainerǁtrain__mutmut_39, 
        'xǁTrainerǁtrain__mutmut_40': xǁTrainerǁtrain__mutmut_40, 
        'xǁTrainerǁtrain__mutmut_41': xǁTrainerǁtrain__mutmut_41, 
        'xǁTrainerǁtrain__mutmut_42': xǁTrainerǁtrain__mutmut_42, 
        'xǁTrainerǁtrain__mutmut_43': xǁTrainerǁtrain__mutmut_43, 
        'xǁTrainerǁtrain__mutmut_44': xǁTrainerǁtrain__mutmut_44, 
        'xǁTrainerǁtrain__mutmut_45': xǁTrainerǁtrain__mutmut_45, 
        'xǁTrainerǁtrain__mutmut_46': xǁTrainerǁtrain__mutmut_46, 
        'xǁTrainerǁtrain__mutmut_47': xǁTrainerǁtrain__mutmut_47, 
        'xǁTrainerǁtrain__mutmut_48': xǁTrainerǁtrain__mutmut_48, 
        'xǁTrainerǁtrain__mutmut_49': xǁTrainerǁtrain__mutmut_49, 
        'xǁTrainerǁtrain__mutmut_50': xǁTrainerǁtrain__mutmut_50, 
        'xǁTrainerǁtrain__mutmut_51': xǁTrainerǁtrain__mutmut_51, 
        'xǁTrainerǁtrain__mutmut_52': xǁTrainerǁtrain__mutmut_52, 
        'xǁTrainerǁtrain__mutmut_53': xǁTrainerǁtrain__mutmut_53, 
        'xǁTrainerǁtrain__mutmut_54': xǁTrainerǁtrain__mutmut_54, 
        'xǁTrainerǁtrain__mutmut_55': xǁTrainerǁtrain__mutmut_55, 
        'xǁTrainerǁtrain__mutmut_56': xǁTrainerǁtrain__mutmut_56, 
        'xǁTrainerǁtrain__mutmut_57': xǁTrainerǁtrain__mutmut_57, 
        'xǁTrainerǁtrain__mutmut_58': xǁTrainerǁtrain__mutmut_58, 
        'xǁTrainerǁtrain__mutmut_59': xǁTrainerǁtrain__mutmut_59, 
        'xǁTrainerǁtrain__mutmut_60': xǁTrainerǁtrain__mutmut_60, 
        'xǁTrainerǁtrain__mutmut_61': xǁTrainerǁtrain__mutmut_61, 
        'xǁTrainerǁtrain__mutmut_62': xǁTrainerǁtrain__mutmut_62, 
        'xǁTrainerǁtrain__mutmut_63': xǁTrainerǁtrain__mutmut_63, 
        'xǁTrainerǁtrain__mutmut_64': xǁTrainerǁtrain__mutmut_64, 
        'xǁTrainerǁtrain__mutmut_65': xǁTrainerǁtrain__mutmut_65, 
        'xǁTrainerǁtrain__mutmut_66': xǁTrainerǁtrain__mutmut_66, 
        'xǁTrainerǁtrain__mutmut_67': xǁTrainerǁtrain__mutmut_67, 
        'xǁTrainerǁtrain__mutmut_68': xǁTrainerǁtrain__mutmut_68, 
        'xǁTrainerǁtrain__mutmut_69': xǁTrainerǁtrain__mutmut_69, 
        'xǁTrainerǁtrain__mutmut_70': xǁTrainerǁtrain__mutmut_70, 
        'xǁTrainerǁtrain__mutmut_71': xǁTrainerǁtrain__mutmut_71, 
        'xǁTrainerǁtrain__mutmut_72': xǁTrainerǁtrain__mutmut_72, 
        'xǁTrainerǁtrain__mutmut_73': xǁTrainerǁtrain__mutmut_73, 
        'xǁTrainerǁtrain__mutmut_74': xǁTrainerǁtrain__mutmut_74, 
        'xǁTrainerǁtrain__mutmut_75': xǁTrainerǁtrain__mutmut_75, 
        'xǁTrainerǁtrain__mutmut_76': xǁTrainerǁtrain__mutmut_76, 
        'xǁTrainerǁtrain__mutmut_77': xǁTrainerǁtrain__mutmut_77, 
        'xǁTrainerǁtrain__mutmut_78': xǁTrainerǁtrain__mutmut_78, 
        'xǁTrainerǁtrain__mutmut_79': xǁTrainerǁtrain__mutmut_79, 
        'xǁTrainerǁtrain__mutmut_80': xǁTrainerǁtrain__mutmut_80, 
        'xǁTrainerǁtrain__mutmut_81': xǁTrainerǁtrain__mutmut_81, 
        'xǁTrainerǁtrain__mutmut_82': xǁTrainerǁtrain__mutmut_82, 
        'xǁTrainerǁtrain__mutmut_83': xǁTrainerǁtrain__mutmut_83, 
        'xǁTrainerǁtrain__mutmut_84': xǁTrainerǁtrain__mutmut_84, 
        'xǁTrainerǁtrain__mutmut_85': xǁTrainerǁtrain__mutmut_85, 
        'xǁTrainerǁtrain__mutmut_86': xǁTrainerǁtrain__mutmut_86, 
        'xǁTrainerǁtrain__mutmut_87': xǁTrainerǁtrain__mutmut_87, 
        'xǁTrainerǁtrain__mutmut_88': xǁTrainerǁtrain__mutmut_88, 
        'xǁTrainerǁtrain__mutmut_89': xǁTrainerǁtrain__mutmut_89, 
        'xǁTrainerǁtrain__mutmut_90': xǁTrainerǁtrain__mutmut_90, 
        'xǁTrainerǁtrain__mutmut_91': xǁTrainerǁtrain__mutmut_91, 
        'xǁTrainerǁtrain__mutmut_92': xǁTrainerǁtrain__mutmut_92, 
        'xǁTrainerǁtrain__mutmut_93': xǁTrainerǁtrain__mutmut_93, 
        'xǁTrainerǁtrain__mutmut_94': xǁTrainerǁtrain__mutmut_94, 
        'xǁTrainerǁtrain__mutmut_95': xǁTrainerǁtrain__mutmut_95, 
        'xǁTrainerǁtrain__mutmut_96': xǁTrainerǁtrain__mutmut_96, 
        'xǁTrainerǁtrain__mutmut_97': xǁTrainerǁtrain__mutmut_97, 
        'xǁTrainerǁtrain__mutmut_98': xǁTrainerǁtrain__mutmut_98, 
        'xǁTrainerǁtrain__mutmut_99': xǁTrainerǁtrain__mutmut_99, 
        'xǁTrainerǁtrain__mutmut_100': xǁTrainerǁtrain__mutmut_100, 
        'xǁTrainerǁtrain__mutmut_101': xǁTrainerǁtrain__mutmut_101, 
        'xǁTrainerǁtrain__mutmut_102': xǁTrainerǁtrain__mutmut_102, 
        'xǁTrainerǁtrain__mutmut_103': xǁTrainerǁtrain__mutmut_103, 
        'xǁTrainerǁtrain__mutmut_104': xǁTrainerǁtrain__mutmut_104, 
        'xǁTrainerǁtrain__mutmut_105': xǁTrainerǁtrain__mutmut_105, 
        'xǁTrainerǁtrain__mutmut_106': xǁTrainerǁtrain__mutmut_106, 
        'xǁTrainerǁtrain__mutmut_107': xǁTrainerǁtrain__mutmut_107, 
        'xǁTrainerǁtrain__mutmut_108': xǁTrainerǁtrain__mutmut_108, 
        'xǁTrainerǁtrain__mutmut_109': xǁTrainerǁtrain__mutmut_109, 
        'xǁTrainerǁtrain__mutmut_110': xǁTrainerǁtrain__mutmut_110, 
        'xǁTrainerǁtrain__mutmut_111': xǁTrainerǁtrain__mutmut_111, 
        'xǁTrainerǁtrain__mutmut_112': xǁTrainerǁtrain__mutmut_112, 
        'xǁTrainerǁtrain__mutmut_113': xǁTrainerǁtrain__mutmut_113, 
        'xǁTrainerǁtrain__mutmut_114': xǁTrainerǁtrain__mutmut_114, 
        'xǁTrainerǁtrain__mutmut_115': xǁTrainerǁtrain__mutmut_115, 
        'xǁTrainerǁtrain__mutmut_116': xǁTrainerǁtrain__mutmut_116, 
        'xǁTrainerǁtrain__mutmut_117': xǁTrainerǁtrain__mutmut_117, 
        'xǁTrainerǁtrain__mutmut_118': xǁTrainerǁtrain__mutmut_118, 
        'xǁTrainerǁtrain__mutmut_119': xǁTrainerǁtrain__mutmut_119, 
        'xǁTrainerǁtrain__mutmut_120': xǁTrainerǁtrain__mutmut_120, 
        'xǁTrainerǁtrain__mutmut_121': xǁTrainerǁtrain__mutmut_121, 
        'xǁTrainerǁtrain__mutmut_122': xǁTrainerǁtrain__mutmut_122, 
        'xǁTrainerǁtrain__mutmut_123': xǁTrainerǁtrain__mutmut_123, 
        'xǁTrainerǁtrain__mutmut_124': xǁTrainerǁtrain__mutmut_124, 
        'xǁTrainerǁtrain__mutmut_125': xǁTrainerǁtrain__mutmut_125, 
        'xǁTrainerǁtrain__mutmut_126': xǁTrainerǁtrain__mutmut_126, 
        'xǁTrainerǁtrain__mutmut_127': xǁTrainerǁtrain__mutmut_127, 
        'xǁTrainerǁtrain__mutmut_128': xǁTrainerǁtrain__mutmut_128, 
        'xǁTrainerǁtrain__mutmut_129': xǁTrainerǁtrain__mutmut_129, 
        'xǁTrainerǁtrain__mutmut_130': xǁTrainerǁtrain__mutmut_130, 
        'xǁTrainerǁtrain__mutmut_131': xǁTrainerǁtrain__mutmut_131, 
        'xǁTrainerǁtrain__mutmut_132': xǁTrainerǁtrain__mutmut_132, 
        'xǁTrainerǁtrain__mutmut_133': xǁTrainerǁtrain__mutmut_133, 
        'xǁTrainerǁtrain__mutmut_134': xǁTrainerǁtrain__mutmut_134, 
        'xǁTrainerǁtrain__mutmut_135': xǁTrainerǁtrain__mutmut_135, 
        'xǁTrainerǁtrain__mutmut_136': xǁTrainerǁtrain__mutmut_136, 
        'xǁTrainerǁtrain__mutmut_137': xǁTrainerǁtrain__mutmut_137, 
        'xǁTrainerǁtrain__mutmut_138': xǁTrainerǁtrain__mutmut_138, 
        'xǁTrainerǁtrain__mutmut_139': xǁTrainerǁtrain__mutmut_139, 
        'xǁTrainerǁtrain__mutmut_140': xǁTrainerǁtrain__mutmut_140, 
        'xǁTrainerǁtrain__mutmut_141': xǁTrainerǁtrain__mutmut_141, 
        'xǁTrainerǁtrain__mutmut_142': xǁTrainerǁtrain__mutmut_142, 
        'xǁTrainerǁtrain__mutmut_143': xǁTrainerǁtrain__mutmut_143, 
        'xǁTrainerǁtrain__mutmut_144': xǁTrainerǁtrain__mutmut_144, 
        'xǁTrainerǁtrain__mutmut_145': xǁTrainerǁtrain__mutmut_145, 
        'xǁTrainerǁtrain__mutmut_146': xǁTrainerǁtrain__mutmut_146, 
        'xǁTrainerǁtrain__mutmut_147': xǁTrainerǁtrain__mutmut_147, 
        'xǁTrainerǁtrain__mutmut_148': xǁTrainerǁtrain__mutmut_148, 
        'xǁTrainerǁtrain__mutmut_149': xǁTrainerǁtrain__mutmut_149, 
        'xǁTrainerǁtrain__mutmut_150': xǁTrainerǁtrain__mutmut_150, 
        'xǁTrainerǁtrain__mutmut_151': xǁTrainerǁtrain__mutmut_151, 
        'xǁTrainerǁtrain__mutmut_152': xǁTrainerǁtrain__mutmut_152, 
        'xǁTrainerǁtrain__mutmut_153': xǁTrainerǁtrain__mutmut_153, 
        'xǁTrainerǁtrain__mutmut_154': xǁTrainerǁtrain__mutmut_154, 
        'xǁTrainerǁtrain__mutmut_155': xǁTrainerǁtrain__mutmut_155, 
        'xǁTrainerǁtrain__mutmut_156': xǁTrainerǁtrain__mutmut_156, 
        'xǁTrainerǁtrain__mutmut_157': xǁTrainerǁtrain__mutmut_157, 
        'xǁTrainerǁtrain__mutmut_158': xǁTrainerǁtrain__mutmut_158, 
        'xǁTrainerǁtrain__mutmut_159': xǁTrainerǁtrain__mutmut_159, 
        'xǁTrainerǁtrain__mutmut_160': xǁTrainerǁtrain__mutmut_160, 
        'xǁTrainerǁtrain__mutmut_161': xǁTrainerǁtrain__mutmut_161, 
        'xǁTrainerǁtrain__mutmut_162': xǁTrainerǁtrain__mutmut_162, 
        'xǁTrainerǁtrain__mutmut_163': xǁTrainerǁtrain__mutmut_163, 
        'xǁTrainerǁtrain__mutmut_164': xǁTrainerǁtrain__mutmut_164
    }
    
    def train(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁTrainerǁtrain__mutmut_orig"), object.__getattribute__(self, "xǁTrainerǁtrain__mutmut_mutants"), args, kwargs, self)
        return result 
    
    train.__signature__ = _mutmut_signature(xǁTrainerǁtrain__mutmut_orig)
    xǁTrainerǁtrain__mutmut_orig.__name__ = 'xǁTrainerǁtrain'

    def xǁTrainerǁclose__mutmut_orig(self) -> None:
        shutdown_logging(self._logging_session)

    def xǁTrainerǁclose__mutmut_1(self) -> None:
        shutdown_logging(None)
    
    xǁTrainerǁclose__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁTrainerǁclose__mutmut_1': xǁTrainerǁclose__mutmut_1
    }
    
    def close(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁTrainerǁclose__mutmut_orig"), object.__getattribute__(self, "xǁTrainerǁclose__mutmut_mutants"), args, kwargs, self)
        return result 
    
    close.__signature__ = _mutmut_signature(xǁTrainerǁclose__mutmut_orig)
    xǁTrainerǁclose__mutmut_orig.__name__ = 'xǁTrainerǁclose'

    def xǁTrainerǁ__del____mutmut_orig(self) -> None:  # pragma: no cover - defensive cleanup
        with contextlib.suppress(Exception):
            self.close()

    def xǁTrainerǁ__del____mutmut_1(self) -> None:  # pragma: no cover - defensive cleanup
        with contextlib.suppress(None):
            self.close()
    
    xǁTrainerǁ__del____mutmut_mutants : ClassVar[MutantDict] = {
    'xǁTrainerǁ__del____mutmut_1': xǁTrainerǁ__del____mutmut_1
    }
    
    def __del__(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁTrainerǁ__del____mutmut_orig"), object.__getattribute__(self, "xǁTrainerǁ__del____mutmut_mutants"), args, kwargs, self)
        return result 
    
    __del__.__signature__ = _mutmut_signature(xǁTrainerǁ__del____mutmut_orig)
    xǁTrainerǁ__del____mutmut_orig.__name__ = 'xǁTrainerǁ__del__'


ExtendedTrainer = Trainer

__all__ = [
    "CheckpointConfig",
    "ExtendedTrainer",
    "Trainer",
    "TrainerConfig",
]
