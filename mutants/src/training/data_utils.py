"""
Data Utils Module

This module provides functionality for data utils.

Usage:
    from training.data_utils import ...

Classes:
    [To be documented]

Functions:
    [To be documented]

Author: Codex Team
"""

from __future__ import annotations
import logging
logger = logging.getLogger(__name__)

import hashlib
import json
import os
from collections.abc import Iterable, Iterator, Mapping, Sequence
from dataclasses import dataclass
from pathlib import Path
from typing import TYPE_CHECKING, Any, TypeVar, cast

try:  # Optional dependency handling
    import numpy as np
    import numpy.typing as npt
except ModuleNotFoundError:  # pragma: no cover - lightweight environments
    np = None  # type: ignore[assignment]
    npt = Any  # type: ignore[assignment]

try:
    import torch
except ModuleNotFoundError:  # pragma: no cover - torch optional
    torch = None  # type: ignore[assignment]
else:
    if not hasattr(torch, "Tensor"):
        torch = None  # type: ignore[assignment]

try:  # pragma: no cover - fcntl unavailable on Windows
    import fcntl  # type: ignore[attr-defined]
except ImportError:  # pragma: no cover - platform-specific fallback
    fcntl = None  # type: ignore[assignment]

if TYPE_CHECKING:  # pragma: no cover - typing only
    if torch is not None:
        Tensor = torch.Tensor  # type: ignore[attr-defined]
    else:
        Tensor = Any  # type: ignore[misc]
else:  # pragma: no cover - runtime alias
    if torch is not None:
        Tensor = torch.Tensor  # type: ignore[attr-defined]
    else:
        Tensor = Any  # type: ignore[assignment]

T = TypeVar("T")
from inspect import signature as _mutmut_signature
from typing import Annotated
from typing import Callable
from typing import ClassVar


MutantDict = Annotated[dict[str, Callable], "Mutant"]


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg = None):
    """Forward call to original or mutated function, depending on the environment"""
    import os
    mutant_under_test = os.environ['MUTANT_UNDER_TEST']
    if mutant_under_test == 'fail':
        from mutmut.__main__ import MutmutProgrammaticFailException
        raise MutmutProgrammaticFailException('Failed programmatically')      
    elif mutant_under_test == 'stats':
        from mutmut.__main__ import record_trampoline_hit
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__)
        result = orig(*call_args, **call_kwargs)
        return result
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_'
    if not mutant_under_test.startswith(prefix):
        result = orig(*call_args, **call_kwargs)
        return result
    mutant_name = mutant_under_test.rpartition('.')[-1]
    if self_arg is not None:
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs)
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs)
    return result


def x__require_torch__mutmut_orig() -> None:
    if torch is None:
        raise ModuleNotFoundError("torch is required for this operation")


def x__require_torch__mutmut_1() -> None:
    if torch is not None:
        raise ModuleNotFoundError("torch is required for this operation")


def x__require_torch__mutmut_2() -> None:
    if torch is None:
        raise ModuleNotFoundError(None)


def x__require_torch__mutmut_3() -> None:
    if torch is None:
        raise ModuleNotFoundError("XXtorch is required for this operationXX")


def x__require_torch__mutmut_4() -> None:
    if torch is None:
        raise ModuleNotFoundError("TORCH IS REQUIRED FOR THIS OPERATION")

x__require_torch__mutmut_mutants : ClassVar[MutantDict] = {
'x__require_torch__mutmut_1': x__require_torch__mutmut_1, 
    'x__require_torch__mutmut_2': x__require_torch__mutmut_2, 
    'x__require_torch__mutmut_3': x__require_torch__mutmut_3, 
    'x__require_torch__mutmut_4': x__require_torch__mutmut_4
}

def _require_torch(*args, **kwargs):
    result = _mutmut_trampoline(x__require_torch__mutmut_orig, x__require_torch__mutmut_mutants, args, kwargs)
    return result 

_require_torch.__signature__ = _mutmut_signature(x__require_torch__mutmut_orig)
x__require_torch__mutmut_orig.__name__ = 'x__require_torch'


# Optional deterministic shuffler import with robust fallback
try:  # pragma: no cover - optional import from ingestion utilities
    from ingestion import deterministic_shuffle  # type: ignore
except Exception:  # pragma: no cover - fallback
    import random

    def deterministic_shuffle(seq: Iterable[Any], seed: int) -> list[Any]:
        items = list(seq)
        rnd = random.Random(seed)  # nosec B311 - deterministic shuffle fallback
        rnd.shuffle(items)
        return items


def x__stable_checksum_of_seq_repr__mutmut_orig(seq: Sequence[Any]) -> str:
    """
    Compute a stable checksum for a sequence based on the repr of its items.
    Uses UTF-8 encoded concatenation of item reprs with SHA-256.
    """
    return hashlib.sha256("".join(map(repr, seq)).encode("utf-8")).hexdigest()


def x__stable_checksum_of_seq_repr__mutmut_1(seq: Sequence[Any]) -> str:
    """
    Compute a stable checksum for a sequence based on the repr of its items.
    Uses UTF-8 encoded concatenation of item reprs with SHA-256.
    """
    return hashlib.sha256(None).hexdigest()


def x__stable_checksum_of_seq_repr__mutmut_2(seq: Sequence[Any]) -> str:
    """
    Compute a stable checksum for a sequence based on the repr of its items.
    Uses UTF-8 encoded concatenation of item reprs with SHA-256.
    """
    return hashlib.sha256("".join(map(repr, seq)).encode(None)).hexdigest()


def x__stable_checksum_of_seq_repr__mutmut_3(seq: Sequence[Any]) -> str:
    """
    Compute a stable checksum for a sequence based on the repr of its items.
    Uses UTF-8 encoded concatenation of item reprs with SHA-256.
    """
    return hashlib.sha256("".join(None).encode("utf-8")).hexdigest()


def x__stable_checksum_of_seq_repr__mutmut_4(seq: Sequence[Any]) -> str:
    """
    Compute a stable checksum for a sequence based on the repr of its items.
    Uses UTF-8 encoded concatenation of item reprs with SHA-256.
    """
    return hashlib.sha256("XXXX".join(map(repr, seq)).encode("utf-8")).hexdigest()


def x__stable_checksum_of_seq_repr__mutmut_5(seq: Sequence[Any]) -> str:
    """
    Compute a stable checksum for a sequence based on the repr of its items.
    Uses UTF-8 encoded concatenation of item reprs with SHA-256.
    """
    return hashlib.sha256("".join(map(None, seq)).encode("utf-8")).hexdigest()


def x__stable_checksum_of_seq_repr__mutmut_6(seq: Sequence[Any]) -> str:
    """
    Compute a stable checksum for a sequence based on the repr of its items.
    Uses UTF-8 encoded concatenation of item reprs with SHA-256.
    """
    return hashlib.sha256("".join(map(repr, None)).encode("utf-8")).hexdigest()


def x__stable_checksum_of_seq_repr__mutmut_7(seq: Sequence[Any]) -> str:
    """
    Compute a stable checksum for a sequence based on the repr of its items.
    Uses UTF-8 encoded concatenation of item reprs with SHA-256.
    """
    return hashlib.sha256("".join(map(seq)).encode("utf-8")).hexdigest()


def x__stable_checksum_of_seq_repr__mutmut_8(seq: Sequence[Any]) -> str:
    """
    Compute a stable checksum for a sequence based on the repr of its items.
    Uses UTF-8 encoded concatenation of item reprs with SHA-256.
    """
    return hashlib.sha256("".join(map(repr, )).encode("utf-8")).hexdigest()


def x__stable_checksum_of_seq_repr__mutmut_9(seq: Sequence[Any]) -> str:
    """
    Compute a stable checksum for a sequence based on the repr of its items.
    Uses UTF-8 encoded concatenation of item reprs with SHA-256.
    """
    return hashlib.sha256("".join(map(repr, seq)).encode("XXutf-8XX")).hexdigest()


def x__stable_checksum_of_seq_repr__mutmut_10(seq: Sequence[Any]) -> str:
    """
    Compute a stable checksum for a sequence based on the repr of its items.
    Uses UTF-8 encoded concatenation of item reprs with SHA-256.
    """
    return hashlib.sha256("".join(map(repr, seq)).encode("UTF-8")).hexdigest()

x__stable_checksum_of_seq_repr__mutmut_mutants : ClassVar[MutantDict] = {
'x__stable_checksum_of_seq_repr__mutmut_1': x__stable_checksum_of_seq_repr__mutmut_1, 
    'x__stable_checksum_of_seq_repr__mutmut_2': x__stable_checksum_of_seq_repr__mutmut_2, 
    'x__stable_checksum_of_seq_repr__mutmut_3': x__stable_checksum_of_seq_repr__mutmut_3, 
    'x__stable_checksum_of_seq_repr__mutmut_4': x__stable_checksum_of_seq_repr__mutmut_4, 
    'x__stable_checksum_of_seq_repr__mutmut_5': x__stable_checksum_of_seq_repr__mutmut_5, 
    'x__stable_checksum_of_seq_repr__mutmut_6': x__stable_checksum_of_seq_repr__mutmut_6, 
    'x__stable_checksum_of_seq_repr__mutmut_7': x__stable_checksum_of_seq_repr__mutmut_7, 
    'x__stable_checksum_of_seq_repr__mutmut_8': x__stable_checksum_of_seq_repr__mutmut_8, 
    'x__stable_checksum_of_seq_repr__mutmut_9': x__stable_checksum_of_seq_repr__mutmut_9, 
    'x__stable_checksum_of_seq_repr__mutmut_10': x__stable_checksum_of_seq_repr__mutmut_10
}

def _stable_checksum_of_seq_repr(*args, **kwargs):
    result = _mutmut_trampoline(x__stable_checksum_of_seq_repr__mutmut_orig, x__stable_checksum_of_seq_repr__mutmut_mutants, args, kwargs)
    return result 

_stable_checksum_of_seq_repr.__signature__ = _mutmut_signature(x__stable_checksum_of_seq_repr__mutmut_orig)
x__stable_checksum_of_seq_repr__mutmut_orig.__name__ = 'x__stable_checksum_of_seq_repr'


def x_split_dataset__mutmut_orig(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_1(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 1.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_2(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 43,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_3(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_4(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 1.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_5(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 < float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_6(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(None) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_7(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) < 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_8(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 2.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_9(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError(None)

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_10(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("XXtrain_ratio must be within [0.0, 1.0]XX")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_11(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("TRAIN_RATIO MUST BE WITHIN [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_12(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = None
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_13(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(None) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_14(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(None)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_15(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = None
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_16(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n != 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_17(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 1:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_18(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = None

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_19(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(None)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_20(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = ""
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_21(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = ""
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_22(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_23(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = None
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_24(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(None)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_25(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = None
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_26(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(None)
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_27(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding=None))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_28(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="XXutf-8XX"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_29(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="UTF-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_30(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = None
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_31(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(None)
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_32(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get(None, train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_33(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", None))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_34(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get(train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_35(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", ))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_36(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("XXtrain_ratioXX", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_37(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("TRAIN_RATIO", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_38(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = None
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_39(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(None)
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_40(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get(None, seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_41(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", None))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_42(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get(seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_43(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", ))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_44(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("XXseedXX", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_45(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("SEED", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_46(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = None
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_47(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(None)
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_48(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get(None, -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_49(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", None))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_50(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get(-1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_51(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", ))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_52(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("XXlengthXX", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_53(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("LENGTH", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_54(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", +1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_55(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -2))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_56(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = None
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_57(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") and data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_58(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get(None) or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_59(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("XXchecksumXX") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_60(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("CHECKSUM") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_61(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get(None)
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_62(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("XXdata_hashXX")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_63(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("DATA_HASH")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_64(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list) or isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_65(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum or isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_66(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed) or cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_67(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12 or cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_68(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n or abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_69(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict) or cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_70(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len != n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_71(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(None) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_72(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio + float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_73(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(None)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_74(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) <= 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_75(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1.000000000001
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_76(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed != int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_77(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(None)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_78(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum != checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_79(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = None
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_80(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(None) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_81(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["XXtrain_idxXX"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_82(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["TRAIN_IDX"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_83(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = None
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_84(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(None) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_85(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["XXval_idxXX"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_86(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["VAL_IDX"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_87(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning(None, exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_88(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=None)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_89(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning(exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_90(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", )
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_91(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("XXException occurredXX", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_92(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_93(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("EXCEPTION OCCURRED", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_94(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=False)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_95(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning(None, exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_96(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=None)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_97(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning(exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_98(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", )
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_99(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("XXException occurredXX", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_100(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_101(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("EXCEPTION OCCURRED", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_102(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=False)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_103(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = ""

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_104(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None and cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_105(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is not None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_106(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is not None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_107(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = None
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_108(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(None)
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_109(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(None))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_110(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = None
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_111(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(None, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_112(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, None)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_113(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_114(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, )
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_115(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = None
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_116(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(None)
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_117(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n / float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_118(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(None))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_119(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = None
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_120(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = None

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_121(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = None
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_122(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = None

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_123(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_124(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = None
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_125(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(None)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_126(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=None, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_127(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=None)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_128(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_129(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, )
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_130(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=False, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_131(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=False)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_132(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                None,
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_133(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding=None,
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_134(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_135(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_136(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    None,
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_137(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=None,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_138(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_139(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_140(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "XXlengthXX": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_141(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "LENGTH": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_142(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "XXseedXX": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_143(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "SEED": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_144(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(None),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_145(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "XXtrain_ratioXX": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_146(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "TRAIN_RATIO": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_147(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(None),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_148(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "XXchecksumXX": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_149(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "CHECKSUM": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_150(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "XXdata_hashXX": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_151(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "DATA_HASH": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_152(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "XXtrain_idxXX": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_153(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "TRAIN_IDX": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_154(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "XXval_idxXX": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_155(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "VAL_IDX": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_156(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=3,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_157(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="XXutf-8XX",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_158(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="UTF-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_159(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning(None, exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_160(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=None)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_161(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning(exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_162(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", )
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_163(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("XXException occurredXX", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_164(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_165(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("EXCEPTION OCCURRED", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_166(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=False)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_167(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning(None, exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_168(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=None)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_169(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning(exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_170(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", )
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_171(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("XXException occurredXX", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_172(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("exception occurred", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_173(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("EXCEPTION OCCURRED", exc_info=True)
            # Best-effort cache; ignore failures
            pass

    return train, val


def x_split_dataset__mutmut_174(
    items: Sequence[T] | Mapping[Any, T],
    train_ratio: float = 0.9,
    seed: int = 42,
    cache_path: str | Path | None = None,
) -> tuple[list[T], list[T]]:
    """Split items deterministically into train and validation lists.

    Supports sequences or mappings; mappings are split over their values.
    Optionally caches split indices on disk for reproducible reuse.

    Parameters
    ----------
    items : Sequence[T] | Mapping[Any, T]
        Collection of items to split.
    train_ratio : float, default=0.9
        Fraction of items to allocate to the training set. Must be in [0, 1].
    seed : int, default=42
        Seed for deterministic shuffling.
    cache_path : str | Path | None, default=None
        When provided, cache split indices to JSON at this path and reuse on subsequent runs.
        The cache stores indices, not values, to remain agnostic to item serializability.

    Returns
    -------
    (List[T], List[T])
        Train and validation lists.
    """
    if not 0.0 <= float(train_ratio) <= 1.0:
        raise ValueError("train_ratio must be within [0.0, 1.0]")

    seq: list[T] = list(items.values()) if isinstance(items, Mapping) else list(items)
    n = len(seq)
    if n == 0:
        return [], []

    # Stable checksum based on repr of items
    checksum = _stable_checksum_of_seq_repr(seq)

    # Try to reuse cached indices when compatible
    cached_train_idx: list[int] | None = None
    cached_val_idx: list[int] | None = None
    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_ratio = float(data.get("train_ratio", train_ratio))
                cached_seed = int(data.get("seed", seed))
                cached_len = int(data.get("length", -1))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if (
                    isinstance(data, dict)
                    and cached_len == n
                    and abs(cached_ratio - float(train_ratio)) < 1e-12
                    and cached_seed == int(seed)
                    and cached_sum == checksum
                    and isinstance(data.get("train_idx"), list)
                    and isinstance(data.get("val_idx"), list)
                ):
                    cached_train_idx = [int(i) for i in data["train_idx"]]
                    cached_val_idx = [int(i) for i in data["val_idx"]]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Ignore malformed cache and proceed to recompute
                cached_train_idx = cached_val_idx = None

    if cached_train_idx is None or cached_val_idx is None:
        # Fresh deterministic indices
        indices = list(range(n))
        indices = deterministic_shuffle(indices, seed)
        split = int(n * float(train_ratio))
        train_idx, val_idx = indices[:split], indices[split:]
    else:
        train_idx, val_idx = cached_train_idx, cached_val_idx

    train = [seq[i] for i in train_idx]
    val = [seq[i] for i in val_idx]

    # Persist indices cache if requested
    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "length": n,
                        "seed": int(seed),
                        "train_ratio": float(train_ratio),
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                        "train_idx": train_idx,
                        "val_idx": val_idx,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=False)
            # Best-effort cache; ignore failures
            pass

    return train, val

x_split_dataset__mutmut_mutants : ClassVar[MutantDict] = {
'x_split_dataset__mutmut_1': x_split_dataset__mutmut_1, 
    'x_split_dataset__mutmut_2': x_split_dataset__mutmut_2, 
    'x_split_dataset__mutmut_3': x_split_dataset__mutmut_3, 
    'x_split_dataset__mutmut_4': x_split_dataset__mutmut_4, 
    'x_split_dataset__mutmut_5': x_split_dataset__mutmut_5, 
    'x_split_dataset__mutmut_6': x_split_dataset__mutmut_6, 
    'x_split_dataset__mutmut_7': x_split_dataset__mutmut_7, 
    'x_split_dataset__mutmut_8': x_split_dataset__mutmut_8, 
    'x_split_dataset__mutmut_9': x_split_dataset__mutmut_9, 
    'x_split_dataset__mutmut_10': x_split_dataset__mutmut_10, 
    'x_split_dataset__mutmut_11': x_split_dataset__mutmut_11, 
    'x_split_dataset__mutmut_12': x_split_dataset__mutmut_12, 
    'x_split_dataset__mutmut_13': x_split_dataset__mutmut_13, 
    'x_split_dataset__mutmut_14': x_split_dataset__mutmut_14, 
    'x_split_dataset__mutmut_15': x_split_dataset__mutmut_15, 
    'x_split_dataset__mutmut_16': x_split_dataset__mutmut_16, 
    'x_split_dataset__mutmut_17': x_split_dataset__mutmut_17, 
    'x_split_dataset__mutmut_18': x_split_dataset__mutmut_18, 
    'x_split_dataset__mutmut_19': x_split_dataset__mutmut_19, 
    'x_split_dataset__mutmut_20': x_split_dataset__mutmut_20, 
    'x_split_dataset__mutmut_21': x_split_dataset__mutmut_21, 
    'x_split_dataset__mutmut_22': x_split_dataset__mutmut_22, 
    'x_split_dataset__mutmut_23': x_split_dataset__mutmut_23, 
    'x_split_dataset__mutmut_24': x_split_dataset__mutmut_24, 
    'x_split_dataset__mutmut_25': x_split_dataset__mutmut_25, 
    'x_split_dataset__mutmut_26': x_split_dataset__mutmut_26, 
    'x_split_dataset__mutmut_27': x_split_dataset__mutmut_27, 
    'x_split_dataset__mutmut_28': x_split_dataset__mutmut_28, 
    'x_split_dataset__mutmut_29': x_split_dataset__mutmut_29, 
    'x_split_dataset__mutmut_30': x_split_dataset__mutmut_30, 
    'x_split_dataset__mutmut_31': x_split_dataset__mutmut_31, 
    'x_split_dataset__mutmut_168': x_split_dataset__mutmut_168, 
    'x_split_dataset__mutmut_169': x_split_dataset__mutmut_169, 
    'x_split_dataset__mutmut_170': x_split_dataset__mutmut_170, 
    'x_split_dataset__mutmut_171': x_split_dataset__mutmut_171, 
    'x_split_dataset__mutmut_172': x_split_dataset__mutmut_172, 
    'x_split_dataset__mutmut_173': x_split_dataset__mutmut_173, 
    'x_split_dataset__mutmut_174': x_split_dataset__mutmut_174
}

def split_dataset(*args, **kwargs):
    result = _mutmut_trampoline(x_split_dataset__mutmut_orig, x_split_dataset__mutmut_mutants, args, kwargs)
    return result 

split_dataset.__signature__ = _mutmut_signature(x_split_dataset__mutmut_orig)
x_split_dataset__mutmut_orig.__name__ = 'x_split_dataset'


def x_split_texts__mutmut_orig(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_1(
    texts: Iterable[str],
    train_ratio: float = 1.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_2(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 1,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_3(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = None
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_4(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(None)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_5(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = None

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_6(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256(None).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_7(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode(None)).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_8(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(None).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_9(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("XXXX".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_10(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("XXutf-8XX")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_11(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("UTF-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_12(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_13(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = None
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_14(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(None)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_15(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = None
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_16(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(None)
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_17(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding=None))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_18(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="XXutf-8XX"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_19(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="UTF-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_20(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = None
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_21(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") and data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_22(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get(None) or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_23(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("XXchecksumXX") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_24(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("CHECKSUM") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_25(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get(None)
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_26(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("XXdata_hashXX")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_27(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("DATA_HASH")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_28(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum != checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_29(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(None), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_30(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["XXtrainXX"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_31(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["TRAIN"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_32(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(None)
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_33(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["XXvalXX"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_34(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["VAL"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_35(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning(None, exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_36(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=None)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_37(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning(exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_38(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", )
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_39(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("XXException occurredXX", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_40(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_41(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("EXCEPTION OCCURRED", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_42(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=False)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_43(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning(None, exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_44(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=None)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_45(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning(exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_46(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", )
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_47(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("XXException occurredXX", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_48(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_49(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("EXCEPTION OCCURRED", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_50(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=False)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_51(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = None
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_52(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(None, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_53(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, None)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_54(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_55(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, )
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_56(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = None
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_57(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(None)
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_58(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) / float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_59(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(None))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_60(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = None

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_61(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_62(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = None
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_63(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(None)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_64(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=None, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_65(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=None)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_66(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_67(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, )
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_68(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=False, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_69(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=False)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_70(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                None,
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_71(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding=None,
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_72(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_73(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_74(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    None,
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_75(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=None,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_76(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_77(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_78(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "XXtrainXX": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_79(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "TRAIN": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_80(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "XXvalXX": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_81(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "VAL": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_82(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "XXchecksumXX": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_83(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "CHECKSUM": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_84(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "XXdata_hashXX": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_85(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "DATA_HASH": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_86(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=3,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_87(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="XXutf-8XX",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_88(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="UTF-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_89(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning(None, exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_90(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=None)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_91(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning(exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_92(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", )
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_93(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("XXException occurredXX", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_94(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_95(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("EXCEPTION OCCURRED", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_96(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=False)
            logger.warning("Exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_97(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning(None, exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_98(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=None)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_99(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning(exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_100(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", )
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_101(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("XXException occurredXX", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_102(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("exception occurred", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_103(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("EXCEPTION OCCURRED", exc_info=True)
            # Best-effort caching; ignore failures
            pass

    return train, val


def x_split_texts__mutmut_104(
    texts: Iterable[str],
    train_ratio: float = 0.9,
    seed: int = 0,
    cache_path: str | None = None,
) -> tuple[list[str], list[str]]:
    """Deterministically split texts into train/val lists.

    When cache_path is provided the split is cached on disk to allow
    reproducible reuse across runs.

    Parameters
    ----------
    texts : Iterable[str]
        Input text samples.
    train_ratio : float, default=0.9
        Fraction of samples placed in the training set.
    seed : int, default=0
        Shuffle seed.
    cache_path : str | None, default=None
        JSON file to persist/reuse the concrete text split.

    Returns
    -------
    (List[str], List[str])
        Train and validation text lists.
    """
    items = list(texts)
    checksum = hashlib.sha256("".join(items).encode("utf-8")).hexdigest()

    if cache_path is not None:
        p = Path(cache_path)
        if p.exists():
            try:
                data = json.loads(p.read_text(encoding="utf-8"))
                cached_sum = data.get("checksum") or data.get("data_hash")
                if cached_sum == checksum:
                    # defensive cast and copies
                    return list(data["train"]), list(data["val"])
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # fall through to recompute
                pass

    shuffled = deterministic_shuffle(items, seed)
    split = int(len(shuffled) * float(train_ratio))
    train, val = shuffled[:split], shuffled[split:]

    if cache_path is not None:
        try:
            p = Path(cache_path)
            p.parent.mkdir(parents=True, exist_ok=True)
            p.write_text(
                json.dumps(
                    {
                        "train": train,
                        "val": val,
                        # Write both keys for backward/forward compatibility
                        "checksum": checksum,
                        "data_hash": checksum,
                    },
                    indent=2,
                ),
                encoding="utf-8",
            )
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=False)
            # Best-effort caching; ignore failures
            pass

    return train, val

x_split_texts__mutmut_mutants : ClassVar[MutantDict] = {
'x_split_texts__mutmut_1': x_split_texts__mutmut_1, 
    'x_split_texts__mutmut_2': x_split_texts__mutmut_2, 
    'x_split_texts__mutmut_3': x_split_texts__mutmut_3, 
    'x_split_texts__mutmut_4': x_split_texts__mutmut_4, 
    'x_split_texts__mutmut_5': x_split_texts__mutmut_5, 
    'x_split_texts__mutmut_6': x_split_texts__mutmut_6, 
    'x_split_texts__mutmut_7': x_split_texts__mutmut_7, 
    'x_split_texts__mutmut_8': x_split_texts__mutmut_8, 
    'x_split_texts__mutmut_9': x_split_texts__mutmut_9, 
    'x_split_texts__mutmut_10': x_split_texts__mutmut_10, 
    'x_split_texts__mutmut_11': x_split_texts__mutmut_11, 
    'x_split_texts__mutmut_12': x_split_texts__mutmut_12, 
    'x_split_texts__mutmut_13': x_split_texts__mutmut_13, 
    'x_split_texts__mutmut_14': x_split_texts__mutmut_14, 
    'x_split_texts__mutmut_15': x_split_texts__mutmut_15, 
    'x_split_texts__mutmut_16': x_split_texts__mutmut_16, 
    'x_split_texts__mutmut_17': x_split_texts__mutmut_17, 
    'x_split_texts__mutmut_18': x_split_texts__mutmut_18, 
    'x_split_texts__mutmut_19': x_split_texts__mutmut_19, 
    'x_split_texts__mutmut_20': x_split_texts__mutmut_20, 
    'x_split_texts__mutmut_21': x_split_texts__mutmut_21, 
    'x_split_texts__mutmut_22': x_split_texts__mutmut_22, 
    'x_split_texts__mutmut_23': x_split_texts__mutmut_23, 
    'x_split_texts__mutmut_24': x_split_texts__mutmut_24, 
    'x_split_texts__mutmut_25': x_split_texts__mutmut_25, 
    'x_split_texts__mutmut_26': x_split_texts__mutmut_26, 
    'x_split_texts__mutmut_27': x_split_texts__mutmut_27, 
    'x_split_texts__mutmut_28': x_split_texts__mutmut_28, 
    'x_split_texts__mutmut_29': x_split_texts__mutmut_29, 
    'x_split_texts__mutmut_30': x_split_texts__mutmut_30, 
    'x_split_texts__mutmut_31': x_split_texts__mutmut_31, 
    'x_split_texts__mutmut_32': x_split_texts__mutmut_32, 
    'x_split_texts__mutmut_33': x_split_texts__mutmut_33, 
    'x_split_texts__mutmut_34': x_split_texts__mutmut_34, 
    'x_split_texts__mutmut_35': x_split_texts__mutmut_35, 
    'x_split_texts__mutmut_36': x_split_texts__mutmut_36, 
    'x_split_texts__mutmut_37': x_split_texts__mutmut_37, 
    'x_split_texts__mutmut_86': x_split_texts__mutmut_86, 
    'x_split_texts__mutmut_87': x_split_texts__mutmut_87, 
    'x_split_texts__mutmut_88': x_split_texts__mutmut_88, 
    'x_split_texts__mutmut_89': x_split_texts__mutmut_89, 
    'x_split_texts__mutmut_90': x_split_texts__mutmut_90, 
    'x_split_texts__mutmut_91': x_split_texts__mutmut_91, 
    'x_split_texts__mutmut_92': x_split_texts__mutmut_92, 
    'x_split_texts__mutmut_93': x_split_texts__mutmut_93, 
    'x_split_texts__mutmut_94': x_split_texts__mutmut_94, 
    'x_split_texts__mutmut_95': x_split_texts__mutmut_95, 
    'x_split_texts__mutmut_96': x_split_texts__mutmut_96, 
    'x_split_texts__mutmut_97': x_split_texts__mutmut_97, 
    'x_split_texts__mutmut_98': x_split_texts__mutmut_98, 
    'x_split_texts__mutmut_99': x_split_texts__mutmut_99, 
    'x_split_texts__mutmut_100': x_split_texts__mutmut_100, 
    'x_split_texts__mutmut_101': x_split_texts__mutmut_101, 
    'x_split_texts__mutmut_102': x_split_texts__mutmut_102, 
    'x_split_texts__mutmut_103': x_split_texts__mutmut_103, 
    'x_split_texts__mutmut_104': x_split_texts__mutmut_104
}

def split_texts(*args, **kwargs):
    result = _mutmut_trampoline(x_split_texts__mutmut_orig, x_split_texts__mutmut_mutants, args, kwargs)
    return result 

split_texts.__signature__ = _mutmut_signature(x_split_texts__mutmut_orig)
x_split_texts__mutmut_orig.__name__ = 'x_split_texts'


@dataclass
class TextDataset:
    """Minimal text dataset producing next-token labels."""

    texts: list[str]
    tokenizer: Any
    block_size: int = 128

    def __post_init__(self) -> None:
        _require_torch()
        # Prepare tensor examples eagerly
        self.examples: list[dict[str, Tensor]] = []
        for txt in self.texts:
            try:
                # Support both HF-style tokenizer dict-call and simple encode function
                if hasattr(self.tokenizer, "encode"):
                    ids = self.tokenizer.encode(txt)
                else:
                    out = self.tokenizer(txt)
                    # Expecting a mapping with 'input_ids'
                    ids = out["input_ids"]  # type: ignore[index]
            except Exception:
                logger.warning("Exception occurred", exc_info=True)
                logger.warning("Exception occurred", exc_info=True)
                # Skip samples that fail tokenization
                continue

            # Truncate to block_size + next-token label
            ids = ids[: self.block_size + 1]
            if len(ids) < 2:
                continue
            input_ids = ids[:-1]
            labels = ids[1:]
            attn = [1] * len(input_ids)
            self.examples.append(
                {
                    "input_ids": torch.tensor(input_ids, dtype=torch.int64),
                    "labels": torch.tensor(labels, dtype=torch.int64),
                    "attention_mask": torch.tensor(attn, dtype=torch.int64),
                }
            )

    def __len__(self) -> int:
        return len(self.examples)

    def __getitem__(self, idx: int) -> dict[str, Tensor]:
        ex = self.examples[idx]
        # Return clones to avoid unwanted in-place mutations
        return {k: v.clone() for k, v in ex.items()}


def x__to_numpy__mutmut_orig(value: Any) -> npt.NDArray[np.generic]:
    """Convert tensors/arrays to a CPU NumPy array in a type-aware manner."""

    if np is None:
        raise ModuleNotFoundError("numpy is required for dataset caching utilities")

    if torch is not None and isinstance(value, torch.Tensor):
        tensor_any = cast(Any, value)
        array = tensor_any.detach().cpu().numpy()
    else:
        array = np.asarray(value)
    return cast(npt.NDArray[np.generic], array)


def x__to_numpy__mutmut_1(value: Any) -> npt.NDArray[np.generic]:
    """Convert tensors/arrays to a CPU NumPy array in a type-aware manner."""

    if np is not None:
        raise ModuleNotFoundError("numpy is required for dataset caching utilities")

    if torch is not None and isinstance(value, torch.Tensor):
        tensor_any = cast(Any, value)
        array = tensor_any.detach().cpu().numpy()
    else:
        array = np.asarray(value)
    return cast(npt.NDArray[np.generic], array)


def x__to_numpy__mutmut_2(value: Any) -> npt.NDArray[np.generic]:
    """Convert tensors/arrays to a CPU NumPy array in a type-aware manner."""

    if np is None:
        raise ModuleNotFoundError(None)

    if torch is not None and isinstance(value, torch.Tensor):
        tensor_any = cast(Any, value)
        array = tensor_any.detach().cpu().numpy()
    else:
        array = np.asarray(value)
    return cast(npt.NDArray[np.generic], array)


def x__to_numpy__mutmut_3(value: Any) -> npt.NDArray[np.generic]:
    """Convert tensors/arrays to a CPU NumPy array in a type-aware manner."""

    if np is None:
        raise ModuleNotFoundError("XXnumpy is required for dataset caching utilitiesXX")

    if torch is not None and isinstance(value, torch.Tensor):
        tensor_any = cast(Any, value)
        array = tensor_any.detach().cpu().numpy()
    else:
        array = np.asarray(value)
    return cast(npt.NDArray[np.generic], array)


def x__to_numpy__mutmut_4(value: Any) -> npt.NDArray[np.generic]:
    """Convert tensors/arrays to a CPU NumPy array in a type-aware manner."""

    if np is None:
        raise ModuleNotFoundError("NUMPY IS REQUIRED FOR DATASET CACHING UTILITIES")

    if torch is not None and isinstance(value, torch.Tensor):
        tensor_any = cast(Any, value)
        array = tensor_any.detach().cpu().numpy()
    else:
        array = np.asarray(value)
    return cast(npt.NDArray[np.generic], array)


def x__to_numpy__mutmut_5(value: Any) -> npt.NDArray[np.generic]:
    """Convert tensors/arrays to a CPU NumPy array in a type-aware manner."""

    if np is None:
        raise ModuleNotFoundError("numpy is required for dataset caching utilities")

    if torch is not None or isinstance(value, torch.Tensor):
        tensor_any = cast(Any, value)
        array = tensor_any.detach().cpu().numpy()
    else:
        array = np.asarray(value)
    return cast(npt.NDArray[np.generic], array)


def x__to_numpy__mutmut_6(value: Any) -> npt.NDArray[np.generic]:
    """Convert tensors/arrays to a CPU NumPy array in a type-aware manner."""

    if np is None:
        raise ModuleNotFoundError("numpy is required for dataset caching utilities")

    if torch is None and isinstance(value, torch.Tensor):
        tensor_any = cast(Any, value)
        array = tensor_any.detach().cpu().numpy()
    else:
        array = np.asarray(value)
    return cast(npt.NDArray[np.generic], array)


def x__to_numpy__mutmut_7(value: Any) -> npt.NDArray[np.generic]:
    """Convert tensors/arrays to a CPU NumPy array in a type-aware manner."""

    if np is None:
        raise ModuleNotFoundError("numpy is required for dataset caching utilities")

    if torch is not None and isinstance(value, torch.Tensor):
        tensor_any = None
        array = tensor_any.detach().cpu().numpy()
    else:
        array = np.asarray(value)
    return cast(npt.NDArray[np.generic], array)


def x__to_numpy__mutmut_8(value: Any) -> npt.NDArray[np.generic]:
    """Convert tensors/arrays to a CPU NumPy array in a type-aware manner."""

    if np is None:
        raise ModuleNotFoundError("numpy is required for dataset caching utilities")

    if torch is not None and isinstance(value, torch.Tensor):
        tensor_any = cast(None, value)
        array = tensor_any.detach().cpu().numpy()
    else:
        array = np.asarray(value)
    return cast(npt.NDArray[np.generic], array)


def x__to_numpy__mutmut_9(value: Any) -> npt.NDArray[np.generic]:
    """Convert tensors/arrays to a CPU NumPy array in a type-aware manner."""

    if np is None:
        raise ModuleNotFoundError("numpy is required for dataset caching utilities")

    if torch is not None and isinstance(value, torch.Tensor):
        tensor_any = cast(Any, None)
        array = tensor_any.detach().cpu().numpy()
    else:
        array = np.asarray(value)
    return cast(npt.NDArray[np.generic], array)


def x__to_numpy__mutmut_10(value: Any) -> npt.NDArray[np.generic]:
    """Convert tensors/arrays to a CPU NumPy array in a type-aware manner."""

    if np is None:
        raise ModuleNotFoundError("numpy is required for dataset caching utilities")

    if torch is not None and isinstance(value, torch.Tensor):
        tensor_any = cast(value)
        array = tensor_any.detach().cpu().numpy()
    else:
        array = np.asarray(value)
    return cast(npt.NDArray[np.generic], array)


def x__to_numpy__mutmut_11(value: Any) -> npt.NDArray[np.generic]:
    """Convert tensors/arrays to a CPU NumPy array in a type-aware manner."""

    if np is None:
        raise ModuleNotFoundError("numpy is required for dataset caching utilities")

    if torch is not None and isinstance(value, torch.Tensor):
        tensor_any = cast(Any, )
        array = tensor_any.detach().cpu().numpy()
    else:
        array = np.asarray(value)
    return cast(npt.NDArray[np.generic], array)


def x__to_numpy__mutmut_12(value: Any) -> npt.NDArray[np.generic]:
    """Convert tensors/arrays to a CPU NumPy array in a type-aware manner."""

    if np is None:
        raise ModuleNotFoundError("numpy is required for dataset caching utilities")

    if torch is not None and isinstance(value, torch.Tensor):
        tensor_any = cast(Any, value)
        array = None
    else:
        array = np.asarray(value)
    return cast(npt.NDArray[np.generic], array)


def x__to_numpy__mutmut_13(value: Any) -> npt.NDArray[np.generic]:
    """Convert tensors/arrays to a CPU NumPy array in a type-aware manner."""

    if np is None:
        raise ModuleNotFoundError("numpy is required for dataset caching utilities")

    if torch is not None and isinstance(value, torch.Tensor):
        tensor_any = cast(Any, value)
        array = tensor_any.detach().cpu().numpy()
    else:
        array = None
    return cast(npt.NDArray[np.generic], array)


def x__to_numpy__mutmut_14(value: Any) -> npt.NDArray[np.generic]:
    """Convert tensors/arrays to a CPU NumPy array in a type-aware manner."""

    if np is None:
        raise ModuleNotFoundError("numpy is required for dataset caching utilities")

    if torch is not None and isinstance(value, torch.Tensor):
        tensor_any = cast(Any, value)
        array = tensor_any.detach().cpu().numpy()
    else:
        array = np.asarray(None)
    return cast(npt.NDArray[np.generic], array)


def x__to_numpy__mutmut_15(value: Any) -> npt.NDArray[np.generic]:
    """Convert tensors/arrays to a CPU NumPy array in a type-aware manner."""

    if np is None:
        raise ModuleNotFoundError("numpy is required for dataset caching utilities")

    if torch is not None and isinstance(value, torch.Tensor):
        tensor_any = cast(Any, value)
        array = tensor_any.detach().cpu().numpy()
    else:
        array = np.asarray(value)
    return cast(None, array)


def x__to_numpy__mutmut_16(value: Any) -> npt.NDArray[np.generic]:
    """Convert tensors/arrays to a CPU NumPy array in a type-aware manner."""

    if np is None:
        raise ModuleNotFoundError("numpy is required for dataset caching utilities")

    if torch is not None and isinstance(value, torch.Tensor):
        tensor_any = cast(Any, value)
        array = tensor_any.detach().cpu().numpy()
    else:
        array = np.asarray(value)
    return cast(npt.NDArray[np.generic], None)


def x__to_numpy__mutmut_17(value: Any) -> npt.NDArray[np.generic]:
    """Convert tensors/arrays to a CPU NumPy array in a type-aware manner."""

    if np is None:
        raise ModuleNotFoundError("numpy is required for dataset caching utilities")

    if torch is not None and isinstance(value, torch.Tensor):
        tensor_any = cast(Any, value)
        array = tensor_any.detach().cpu().numpy()
    else:
        array = np.asarray(value)
    return cast(array)


def x__to_numpy__mutmut_18(value: Any) -> npt.NDArray[np.generic]:
    """Convert tensors/arrays to a CPU NumPy array in a type-aware manner."""

    if np is None:
        raise ModuleNotFoundError("numpy is required for dataset caching utilities")

    if torch is not None and isinstance(value, torch.Tensor):
        tensor_any = cast(Any, value)
        array = tensor_any.detach().cpu().numpy()
    else:
        array = np.asarray(value)
    return cast(npt.NDArray[np.generic], )

x__to_numpy__mutmut_mutants : ClassVar[MutantDict] = {
'x__to_numpy__mutmut_1': x__to_numpy__mutmut_1, 
    'x__to_numpy__mutmut_2': x__to_numpy__mutmut_2, 
    'x__to_numpy__mutmut_3': x__to_numpy__mutmut_3, 
    'x__to_numpy__mutmut_4': x__to_numpy__mutmut_4, 
    'x__to_numpy__mutmut_5': x__to_numpy__mutmut_5, 
    'x__to_numpy__mutmut_6': x__to_numpy__mutmut_6, 
    'x__to_numpy__mutmut_7': x__to_numpy__mutmut_7, 
    'x__to_numpy__mutmut_8': x__to_numpy__mutmut_8, 
    'x__to_numpy__mutmut_9': x__to_numpy__mutmut_9, 
    'x__to_numpy__mutmut_10': x__to_numpy__mutmut_10, 
    'x__to_numpy__mutmut_11': x__to_numpy__mutmut_11, 
    'x__to_numpy__mutmut_12': x__to_numpy__mutmut_12, 
    'x__to_numpy__mutmut_13': x__to_numpy__mutmut_13, 
    'x__to_numpy__mutmut_14': x__to_numpy__mutmut_14, 
    'x__to_numpy__mutmut_15': x__to_numpy__mutmut_15, 
    'x__to_numpy__mutmut_16': x__to_numpy__mutmut_16, 
    'x__to_numpy__mutmut_17': x__to_numpy__mutmut_17, 
    'x__to_numpy__mutmut_18': x__to_numpy__mutmut_18
}

def _to_numpy(*args, **kwargs):
    result = _mutmut_trampoline(x__to_numpy__mutmut_orig, x__to_numpy__mutmut_mutants, args, kwargs)
    return result 

_to_numpy.__signature__ = _mutmut_signature(x__to_numpy__mutmut_orig)
x__to_numpy__mutmut_orig.__name__ = 'x__to_numpy'


def x_cache_dataset__mutmut_orig(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_1(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is not None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_2(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError(None)

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_3(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("XXnumpy is required to cache tokenised datasetsXX")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_4(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("NUMPY IS REQUIRED TO CACHE TOKENISED DATASETS")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_5(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = None
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_6(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(None)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_7(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=None, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_8(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=None)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_9(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_10(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, )
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_11(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=False, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_12(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=False)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_13(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(None):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_14(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = None
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_15(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None or isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_16(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_17(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = None
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_18(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(None, value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_19(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], None)
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_20(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_21(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], )
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_22(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = None
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_23(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(None)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_24(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = None
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_25(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path * f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_26(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is not None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_27(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(None, allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_28(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=None, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_29(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_30(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_31(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, )
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_32(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(None), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_33(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=True, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_34(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                break

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_35(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = None
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_36(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(None)
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_37(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix - ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_38(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + "XX.lockXX")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_39(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".LOCK")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_40(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=None, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_41(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=None)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_42(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_43(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, )
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_44(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=False, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_45(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=False)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_46(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(None, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_47(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, None, encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_48(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding=None) as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_49(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open("w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_50(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_51(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", ) as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_52(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "XXwXX", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_53(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "W", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_54(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="XXutf-8XX") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_55(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="UTF-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_56(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = None
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_57(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(None, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_58(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, None)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_59(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_60(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, )
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_61(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(None, allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_62(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=None, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_63(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_64(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_65(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, )
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_66(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(None), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_67(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=True, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_68(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(None)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_69(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(None, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_70(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, None)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_71(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_72(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, )
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_73(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(None)
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_74(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(None, exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_75(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=None)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_76(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_77(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", )  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_78(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=False)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_79(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning(None, exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_80(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=None)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_81(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning(exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_82(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", )
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_83(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("XXException occurredXX", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_84(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_85(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("EXCEPTION OCCURRED", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_86(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=False)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_87(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning(None, exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_88(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=None)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_89(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning(exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_90(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", )
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_91(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("XXException occurredXX", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_92(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_93(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("EXCEPTION OCCURRED", exc_info=True)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_94(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=False)
            # Skip samples that fail to serialize
            continue


def x_cache_dataset__mutmut_95(
    ds: Iterable[Mapping[str, Tensor | np.ndarray | Any]],
    cache_dir: str | Path,
) -> None:
    """Cache tokenised dataset ds under cache_dir as .npz shards.

    Each sample in ds is expected to be a mapping from string keys to
    torch.Tensor, numpy.ndarray or array-like entries.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to cache tokenised datasets")

    path = Path(cache_dir)
    path.mkdir(parents=True, exist_ok=True)
    for i, sample in enumerate(ds):
        try:
            arrs: dict[str, npt.NDArray[Any]] = {}
            for key, value in sample.items():
                if torch is not None and isinstance(value, torch.Tensor):
                    arrs[key] = cast(npt.NDArray[Any], value.detach().cpu().numpy())
                else:
                    arrs[key] = np.asarray(value)
            target = path / f"{i}.npz"
            if fcntl is None:
                np.savez(str(target), allow_pickle=False, **arrs)
                continue

            lock_path = target.with_suffix(target.suffix + ".lock")
            lock_path.parent.mkdir(parents=True, exist_ok=True)
            with open(lock_path, "w", encoding="utf-8") as lock_file:
                fd = lock_file.fileno()
                try:
                    fcntl.flock(fd, fcntl.LOCK_EX)
                    np.savez(str(target), allow_pickle=False, **arrs)
                    lock_file.flush()
                    os.fsync(fd)
                finally:
                    try:
                        fcntl.flock(fd, fcntl.LOCK_UN)
                    except Exception as e:
                        logger.debug(f"Exception: {e}")
                        logger.warning(f"Exception: {e}", exc_info=True)  # File lock release failed; continue cleanup
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip samples that fail to serialize
            break

x_cache_dataset__mutmut_mutants : ClassVar[MutantDict] = {
'x_cache_dataset__mutmut_1': x_cache_dataset__mutmut_1, 
    'x_cache_dataset__mutmut_2': x_cache_dataset__mutmut_2, 
    'x_cache_dataset__mutmut_3': x_cache_dataset__mutmut_3, 
    'x_cache_dataset__mutmut_4': x_cache_dataset__mutmut_4, 
    'x_cache_dataset__mutmut_5': x_cache_dataset__mutmut_5, 
    'x_cache_dataset__mutmut_6': x_cache_dataset__mutmut_6, 
    'x_cache_dataset__mutmut_7': x_cache_dataset__mutmut_7, 
    'x_cache_dataset__mutmut_8': x_cache_dataset__mutmut_8, 
    'x_cache_dataset__mutmut_9': x_cache_dataset__mutmut_9, 
    'x_cache_dataset__mutmut_10': x_cache_dataset__mutmut_10, 
    'x_cache_dataset__mutmut_11': x_cache_dataset__mutmut_11, 
    'x_cache_dataset__mutmut_12': x_cache_dataset__mutmut_12, 
    'x_cache_dataset__mutmut_13': x_cache_dataset__mutmut_13, 
    'x_cache_dataset__mutmut_14': x_cache_dataset__mutmut_14, 
    'x_cache_dataset__mutmut_15': x_cache_dataset__mutmut_15, 
    'x_cache_dataset__mutmut_16': x_cache_dataset__mutmut_16, 
    'x_cache_dataset__mutmut_17': x_cache_dataset__mutmut_17, 
    'x_cache_dataset__mutmut_18': x_cache_dataset__mutmut_18, 
    'x_cache_dataset__mutmut_19': x_cache_dataset__mutmut_19, 
    'x_cache_dataset__mutmut_20': x_cache_dataset__mutmut_20, 
    'x_cache_dataset__mutmut_21': x_cache_dataset__mutmut_21, 
    'x_cache_dataset__mutmut_22': x_cache_dataset__mutmut_22, 
    'x_cache_dataset__mutmut_23': x_cache_dataset__mutmut_23, 
    'x_cache_dataset__mutmut_24': x_cache_dataset__mutmut_24, 
    'x_cache_dataset__mutmut_25': x_cache_dataset__mutmut_25, 
    'x_cache_dataset__mutmut_26': x_cache_dataset__mutmut_26, 
    'x_cache_dataset__mutmut_27': x_cache_dataset__mutmut_27, 
    'x_cache_dataset__mutmut_28': x_cache_dataset__mutmut_28, 
    'x_cache_dataset__mutmut_29': x_cache_dataset__mutmut_29, 
    'x_cache_dataset__mutmut_30': x_cache_dataset__mutmut_30, 
    'x_cache_dataset__mutmut_31': x_cache_dataset__mutmut_31, 
    'x_cache_dataset__mutmut_32': x_cache_dataset__mutmut_32, 
    'x_cache_dataset__mutmut_33': x_cache_dataset__mutmut_33, 
    'x_cache_dataset__mutmut_34': x_cache_dataset__mutmut_34, 
    'x_cache_dataset__mutmut_35': x_cache_dataset__mutmut_35, 
    'x_cache_dataset__mutmut_36': x_cache_dataset__mutmut_36, 
    'x_cache_dataset__mutmut_37': x_cache_dataset__mutmut_37, 
    'x_cache_dataset__mutmut_38': x_cache_dataset__mutmut_38, 
    'x_cache_dataset__mutmut_39': x_cache_dataset__mutmut_39, 
    'x_cache_dataset__mutmut_40': x_cache_dataset__mutmut_40, 
    'x_cache_dataset__mutmut_41': x_cache_dataset__mutmut_41, 
    'x_cache_dataset__mutmut_42': x_cache_dataset__mutmut_42, 
    'x_cache_dataset__mutmut_43': x_cache_dataset__mutmut_43, 
    'x_cache_dataset__mutmut_44': x_cache_dataset__mutmut_44, 
    'x_cache_dataset__mutmut_45': x_cache_dataset__mutmut_45, 
    'x_cache_dataset__mutmut_46': x_cache_dataset__mutmut_46, 
    'x_cache_dataset__mutmut_47': x_cache_dataset__mutmut_47, 
    'x_cache_dataset__mutmut_48': x_cache_dataset__mutmut_48, 
    'x_cache_dataset__mutmut_49': x_cache_dataset__mutmut_49, 
    'x_cache_dataset__mutmut_50': x_cache_dataset__mutmut_50, 
    'x_cache_dataset__mutmut_51': x_cache_dataset__mutmut_51, 
    'x_cache_dataset__mutmut_52': x_cache_dataset__mutmut_52, 
    'x_cache_dataset__mutmut_53': x_cache_dataset__mutmut_53, 
    'x_cache_dataset__mutmut_54': x_cache_dataset__mutmut_54, 
    'x_cache_dataset__mutmut_55': x_cache_dataset__mutmut_55, 
    'x_cache_dataset__mutmut_56': x_cache_dataset__mutmut_56, 
    'x_cache_dataset__mutmut_57': x_cache_dataset__mutmut_57, 
    'x_cache_dataset__mutmut_58': x_cache_dataset__mutmut_58, 
    'x_cache_dataset__mutmut_59': x_cache_dataset__mutmut_59, 
    'x_cache_dataset__mutmut_60': x_cache_dataset__mutmut_60, 
    'x_cache_dataset__mutmut_61': x_cache_dataset__mutmut_61, 
    'x_cache_dataset__mutmut_62': x_cache_dataset__mutmut_62, 
    'x_cache_dataset__mutmut_63': x_cache_dataset__mutmut_63, 
    'x_cache_dataset__mutmut_64': x_cache_dataset__mutmut_64, 
    'x_cache_dataset__mutmut_65': x_cache_dataset__mutmut_65, 
    'x_cache_dataset__mutmut_66': x_cache_dataset__mutmut_66, 
    'x_cache_dataset__mutmut_67': x_cache_dataset__mutmut_67, 
    'x_cache_dataset__mutmut_68': x_cache_dataset__mutmut_68, 
    'x_cache_dataset__mutmut_69': x_cache_dataset__mutmut_69, 
    'x_cache_dataset__mutmut_70': x_cache_dataset__mutmut_70, 
    'x_cache_dataset__mutmut_71': x_cache_dataset__mutmut_71, 
    'x_cache_dataset__mutmut_72': x_cache_dataset__mutmut_72, 
    'x_cache_dataset__mutmut_73': x_cache_dataset__mutmut_73, 
    'x_cache_dataset__mutmut_74': x_cache_dataset__mutmut_74, 
    'x_cache_dataset__mutmut_75': x_cache_dataset__mutmut_75, 
    'x_cache_dataset__mutmut_76': x_cache_dataset__mutmut_76, 
    'x_cache_dataset__mutmut_77': x_cache_dataset__mutmut_77, 
    'x_cache_dataset__mutmut_78': x_cache_dataset__mutmut_78, 
    'x_cache_dataset__mutmut_79': x_cache_dataset__mutmut_79, 
    'x_cache_dataset__mutmut_80': x_cache_dataset__mutmut_80, 
    'x_cache_dataset__mutmut_81': x_cache_dataset__mutmut_81, 
    'x_cache_dataset__mutmut_82': x_cache_dataset__mutmut_82, 
    'x_cache_dataset__mutmut_83': x_cache_dataset__mutmut_83, 
    'x_cache_dataset__mutmut_84': x_cache_dataset__mutmut_84, 
    'x_cache_dataset__mutmut_85': x_cache_dataset__mutmut_85, 
    'x_cache_dataset__mutmut_86': x_cache_dataset__mutmut_86, 
    'x_cache_dataset__mutmut_87': x_cache_dataset__mutmut_87, 
    'x_cache_dataset__mutmut_88': x_cache_dataset__mutmut_88, 
    'x_cache_dataset__mutmut_89': x_cache_dataset__mutmut_89, 
    'x_cache_dataset__mutmut_90': x_cache_dataset__mutmut_90, 
    'x_cache_dataset__mutmut_91': x_cache_dataset__mutmut_91, 
    'x_cache_dataset__mutmut_92': x_cache_dataset__mutmut_92, 
    'x_cache_dataset__mutmut_93': x_cache_dataset__mutmut_93, 
    'x_cache_dataset__mutmut_94': x_cache_dataset__mutmut_94, 
    'x_cache_dataset__mutmut_95': x_cache_dataset__mutmut_95
}

def cache_dataset(*args, **kwargs):
    result = _mutmut_trampoline(x_cache_dataset__mutmut_orig, x_cache_dataset__mutmut_mutants, args, kwargs)
    return result 

cache_dataset.__signature__ = _mutmut_signature(x_cache_dataset__mutmut_orig)
x_cache_dataset__mutmut_orig.__name__ = 'x_cache_dataset'


def x_load_cached__mutmut_orig(cache_dir: str | Path) -> Iterator[dict[str, Tensor]]:
    """Yield cached samples stored by cache_dataset.

    Loads .npz files from cache_dir and yields tensors keyed by their original names.
    Corrupted files are skipped gracefully.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to load cached datasets")

    path = Path(cache_dir)
    for npz in sorted(path.glob("*.npz")):
        try:
            data = np.load(npz)
            _require_torch()
            yield {k: torch.tensor(data[k]) for k in data.files}
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip unreadable/corrupted shards
            continue


def x_load_cached__mutmut_1(cache_dir: str | Path) -> Iterator[dict[str, Tensor]]:
    """Yield cached samples stored by cache_dataset.

    Loads .npz files from cache_dir and yields tensors keyed by their original names.
    Corrupted files are skipped gracefully.
    """
    if np is not None:
        raise ModuleNotFoundError("numpy is required to load cached datasets")

    path = Path(cache_dir)
    for npz in sorted(path.glob("*.npz")):
        try:
            data = np.load(npz)
            _require_torch()
            yield {k: torch.tensor(data[k]) for k in data.files}
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip unreadable/corrupted shards
            continue


def x_load_cached__mutmut_2(cache_dir: str | Path) -> Iterator[dict[str, Tensor]]:
    """Yield cached samples stored by cache_dataset.

    Loads .npz files from cache_dir and yields tensors keyed by their original names.
    Corrupted files are skipped gracefully.
    """
    if np is None:
        raise ModuleNotFoundError(None)

    path = Path(cache_dir)
    for npz in sorted(path.glob("*.npz")):
        try:
            data = np.load(npz)
            _require_torch()
            yield {k: torch.tensor(data[k]) for k in data.files}
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip unreadable/corrupted shards
            continue


def x_load_cached__mutmut_3(cache_dir: str | Path) -> Iterator[dict[str, Tensor]]:
    """Yield cached samples stored by cache_dataset.

    Loads .npz files from cache_dir and yields tensors keyed by their original names.
    Corrupted files are skipped gracefully.
    """
    if np is None:
        raise ModuleNotFoundError("XXnumpy is required to load cached datasetsXX")

    path = Path(cache_dir)
    for npz in sorted(path.glob("*.npz")):
        try:
            data = np.load(npz)
            _require_torch()
            yield {k: torch.tensor(data[k]) for k in data.files}
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip unreadable/corrupted shards
            continue


def x_load_cached__mutmut_4(cache_dir: str | Path) -> Iterator[dict[str, Tensor]]:
    """Yield cached samples stored by cache_dataset.

    Loads .npz files from cache_dir and yields tensors keyed by their original names.
    Corrupted files are skipped gracefully.
    """
    if np is None:
        raise ModuleNotFoundError("NUMPY IS REQUIRED TO LOAD CACHED DATASETS")

    path = Path(cache_dir)
    for npz in sorted(path.glob("*.npz")):
        try:
            data = np.load(npz)
            _require_torch()
            yield {k: torch.tensor(data[k]) for k in data.files}
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip unreadable/corrupted shards
            continue


def x_load_cached__mutmut_5(cache_dir: str | Path) -> Iterator[dict[str, Tensor]]:
    """Yield cached samples stored by cache_dataset.

    Loads .npz files from cache_dir and yields tensors keyed by their original names.
    Corrupted files are skipped gracefully.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to load cached datasets")

    path = None
    for npz in sorted(path.glob("*.npz")):
        try:
            data = np.load(npz)
            _require_torch()
            yield {k: torch.tensor(data[k]) for k in data.files}
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip unreadable/corrupted shards
            continue


def x_load_cached__mutmut_6(cache_dir: str | Path) -> Iterator[dict[str, Tensor]]:
    """Yield cached samples stored by cache_dataset.

    Loads .npz files from cache_dir and yields tensors keyed by their original names.
    Corrupted files are skipped gracefully.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to load cached datasets")

    path = Path(None)
    for npz in sorted(path.glob("*.npz")):
        try:
            data = np.load(npz)
            _require_torch()
            yield {k: torch.tensor(data[k]) for k in data.files}
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip unreadable/corrupted shards
            continue


def x_load_cached__mutmut_7(cache_dir: str | Path) -> Iterator[dict[str, Tensor]]:
    """Yield cached samples stored by cache_dataset.

    Loads .npz files from cache_dir and yields tensors keyed by their original names.
    Corrupted files are skipped gracefully.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to load cached datasets")

    path = Path(cache_dir)
    for npz in sorted(None):
        try:
            data = np.load(npz)
            _require_torch()
            yield {k: torch.tensor(data[k]) for k in data.files}
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip unreadable/corrupted shards
            continue


def x_load_cached__mutmut_8(cache_dir: str | Path) -> Iterator[dict[str, Tensor]]:
    """Yield cached samples stored by cache_dataset.

    Loads .npz files from cache_dir and yields tensors keyed by their original names.
    Corrupted files are skipped gracefully.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to load cached datasets")

    path = Path(cache_dir)
    for npz in sorted(path.glob(None)):
        try:
            data = np.load(npz)
            _require_torch()
            yield {k: torch.tensor(data[k]) for k in data.files}
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip unreadable/corrupted shards
            continue


def x_load_cached__mutmut_9(cache_dir: str | Path) -> Iterator[dict[str, Tensor]]:
    """Yield cached samples stored by cache_dataset.

    Loads .npz files from cache_dir and yields tensors keyed by their original names.
    Corrupted files are skipped gracefully.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to load cached datasets")

    path = Path(cache_dir)
    for npz in sorted(path.glob("XX*.npzXX")):
        try:
            data = np.load(npz)
            _require_torch()
            yield {k: torch.tensor(data[k]) for k in data.files}
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip unreadable/corrupted shards
            continue


def x_load_cached__mutmut_10(cache_dir: str | Path) -> Iterator[dict[str, Tensor]]:
    """Yield cached samples stored by cache_dataset.

    Loads .npz files from cache_dir and yields tensors keyed by their original names.
    Corrupted files are skipped gracefully.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to load cached datasets")

    path = Path(cache_dir)
    for npz in sorted(path.glob("*.NPZ")):
        try:
            data = np.load(npz)
            _require_torch()
            yield {k: torch.tensor(data[k]) for k in data.files}
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip unreadable/corrupted shards
            continue


def x_load_cached__mutmut_11(cache_dir: str | Path) -> Iterator[dict[str, Tensor]]:
    """Yield cached samples stored by cache_dataset.

    Loads .npz files from cache_dir and yields tensors keyed by their original names.
    Corrupted files are skipped gracefully.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to load cached datasets")

    path = Path(cache_dir)
    for npz in sorted(path.glob("*.npz")):
        try:
            data = None
            _require_torch()
            yield {k: torch.tensor(data[k]) for k in data.files}
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip unreadable/corrupted shards
            continue


def x_load_cached__mutmut_12(cache_dir: str | Path) -> Iterator[dict[str, Tensor]]:
    """Yield cached samples stored by cache_dataset.

    Loads .npz files from cache_dir and yields tensors keyed by their original names.
    Corrupted files are skipped gracefully.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to load cached datasets")

    path = Path(cache_dir)
    for npz in sorted(path.glob("*.npz")):
        try:
            data = np.load(None)
            _require_torch()
            yield {k: torch.tensor(data[k]) for k in data.files}
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip unreadable/corrupted shards
            continue


def x_load_cached__mutmut_13(cache_dir: str | Path) -> Iterator[dict[str, Tensor]]:
    """Yield cached samples stored by cache_dataset.

    Loads .npz files from cache_dir and yields tensors keyed by their original names.
    Corrupted files are skipped gracefully.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to load cached datasets")

    path = Path(cache_dir)
    for npz in sorted(path.glob("*.npz")):
        try:
            data = np.load(npz)
            _require_torch()
            yield {k: torch.tensor(None) for k in data.files}
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip unreadable/corrupted shards
            continue


def x_load_cached__mutmut_14(cache_dir: str | Path) -> Iterator[dict[str, Tensor]]:
    """Yield cached samples stored by cache_dataset.

    Loads .npz files from cache_dir and yields tensors keyed by their original names.
    Corrupted files are skipped gracefully.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to load cached datasets")

    path = Path(cache_dir)
    for npz in sorted(path.glob("*.npz")):
        try:
            data = np.load(npz)
            _require_torch()
            yield {k: torch.tensor(data[k]) for k in data.files}
        except Exception:
            logger.warning(None, exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip unreadable/corrupted shards
            continue


def x_load_cached__mutmut_15(cache_dir: str | Path) -> Iterator[dict[str, Tensor]]:
    """Yield cached samples stored by cache_dataset.

    Loads .npz files from cache_dir and yields tensors keyed by their original names.
    Corrupted files are skipped gracefully.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to load cached datasets")

    path = Path(cache_dir)
    for npz in sorted(path.glob("*.npz")):
        try:
            data = np.load(npz)
            _require_torch()
            yield {k: torch.tensor(data[k]) for k in data.files}
        except Exception:
            logger.warning("Exception occurred", exc_info=None)
            logger.warning("Exception occurred", exc_info=True)
            # Skip unreadable/corrupted shards
            continue


def x_load_cached__mutmut_16(cache_dir: str | Path) -> Iterator[dict[str, Tensor]]:
    """Yield cached samples stored by cache_dataset.

    Loads .npz files from cache_dir and yields tensors keyed by their original names.
    Corrupted files are skipped gracefully.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to load cached datasets")

    path = Path(cache_dir)
    for npz in sorted(path.glob("*.npz")):
        try:
            data = np.load(npz)
            _require_torch()
            yield {k: torch.tensor(data[k]) for k in data.files}
        except Exception:
            logger.warning(exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip unreadable/corrupted shards
            continue


def x_load_cached__mutmut_17(cache_dir: str | Path) -> Iterator[dict[str, Tensor]]:
    """Yield cached samples stored by cache_dataset.

    Loads .npz files from cache_dir and yields tensors keyed by their original names.
    Corrupted files are skipped gracefully.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to load cached datasets")

    path = Path(cache_dir)
    for npz in sorted(path.glob("*.npz")):
        try:
            data = np.load(npz)
            _require_torch()
            yield {k: torch.tensor(data[k]) for k in data.files}
        except Exception:
            logger.warning("Exception occurred", )
            logger.warning("Exception occurred", exc_info=True)
            # Skip unreadable/corrupted shards
            continue


def x_load_cached__mutmut_18(cache_dir: str | Path) -> Iterator[dict[str, Tensor]]:
    """Yield cached samples stored by cache_dataset.

    Loads .npz files from cache_dir and yields tensors keyed by their original names.
    Corrupted files are skipped gracefully.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to load cached datasets")

    path = Path(cache_dir)
    for npz in sorted(path.glob("*.npz")):
        try:
            data = np.load(npz)
            _require_torch()
            yield {k: torch.tensor(data[k]) for k in data.files}
        except Exception:
            logger.warning("XXException occurredXX", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip unreadable/corrupted shards
            continue


def x_load_cached__mutmut_19(cache_dir: str | Path) -> Iterator[dict[str, Tensor]]:
    """Yield cached samples stored by cache_dataset.

    Loads .npz files from cache_dir and yields tensors keyed by their original names.
    Corrupted files are skipped gracefully.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to load cached datasets")

    path = Path(cache_dir)
    for npz in sorted(path.glob("*.npz")):
        try:
            data = np.load(npz)
            _require_torch()
            yield {k: torch.tensor(data[k]) for k in data.files}
        except Exception:
            logger.warning("exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip unreadable/corrupted shards
            continue


def x_load_cached__mutmut_20(cache_dir: str | Path) -> Iterator[dict[str, Tensor]]:
    """Yield cached samples stored by cache_dataset.

    Loads .npz files from cache_dir and yields tensors keyed by their original names.
    Corrupted files are skipped gracefully.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to load cached datasets")

    path = Path(cache_dir)
    for npz in sorted(path.glob("*.npz")):
        try:
            data = np.load(npz)
            _require_torch()
            yield {k: torch.tensor(data[k]) for k in data.files}
        except Exception:
            logger.warning("EXCEPTION OCCURRED", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip unreadable/corrupted shards
            continue


def x_load_cached__mutmut_21(cache_dir: str | Path) -> Iterator[dict[str, Tensor]]:
    """Yield cached samples stored by cache_dataset.

    Loads .npz files from cache_dir and yields tensors keyed by their original names.
    Corrupted files are skipped gracefully.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to load cached datasets")

    path = Path(cache_dir)
    for npz in sorted(path.glob("*.npz")):
        try:
            data = np.load(npz)
            _require_torch()
            yield {k: torch.tensor(data[k]) for k in data.files}
        except Exception:
            logger.warning("Exception occurred", exc_info=False)
            logger.warning("Exception occurred", exc_info=True)
            # Skip unreadable/corrupted shards
            continue


def x_load_cached__mutmut_22(cache_dir: str | Path) -> Iterator[dict[str, Tensor]]:
    """Yield cached samples stored by cache_dataset.

    Loads .npz files from cache_dir and yields tensors keyed by their original names.
    Corrupted files are skipped gracefully.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to load cached datasets")

    path = Path(cache_dir)
    for npz in sorted(path.glob("*.npz")):
        try:
            data = np.load(npz)
            _require_torch()
            yield {k: torch.tensor(data[k]) for k in data.files}
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning(None, exc_info=True)
            # Skip unreadable/corrupted shards
            continue


def x_load_cached__mutmut_23(cache_dir: str | Path) -> Iterator[dict[str, Tensor]]:
    """Yield cached samples stored by cache_dataset.

    Loads .npz files from cache_dir and yields tensors keyed by their original names.
    Corrupted files are skipped gracefully.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to load cached datasets")

    path = Path(cache_dir)
    for npz in sorted(path.glob("*.npz")):
        try:
            data = np.load(npz)
            _require_torch()
            yield {k: torch.tensor(data[k]) for k in data.files}
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=None)
            # Skip unreadable/corrupted shards
            continue


def x_load_cached__mutmut_24(cache_dir: str | Path) -> Iterator[dict[str, Tensor]]:
    """Yield cached samples stored by cache_dataset.

    Loads .npz files from cache_dir and yields tensors keyed by their original names.
    Corrupted files are skipped gracefully.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to load cached datasets")

    path = Path(cache_dir)
    for npz in sorted(path.glob("*.npz")):
        try:
            data = np.load(npz)
            _require_torch()
            yield {k: torch.tensor(data[k]) for k in data.files}
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning(exc_info=True)
            # Skip unreadable/corrupted shards
            continue


def x_load_cached__mutmut_25(cache_dir: str | Path) -> Iterator[dict[str, Tensor]]:
    """Yield cached samples stored by cache_dataset.

    Loads .npz files from cache_dir and yields tensors keyed by their original names.
    Corrupted files are skipped gracefully.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to load cached datasets")

    path = Path(cache_dir)
    for npz in sorted(path.glob("*.npz")):
        try:
            data = np.load(npz)
            _require_torch()
            yield {k: torch.tensor(data[k]) for k in data.files}
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", )
            # Skip unreadable/corrupted shards
            continue


def x_load_cached__mutmut_26(cache_dir: str | Path) -> Iterator[dict[str, Tensor]]:
    """Yield cached samples stored by cache_dataset.

    Loads .npz files from cache_dir and yields tensors keyed by their original names.
    Corrupted files are skipped gracefully.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to load cached datasets")

    path = Path(cache_dir)
    for npz in sorted(path.glob("*.npz")):
        try:
            data = np.load(npz)
            _require_torch()
            yield {k: torch.tensor(data[k]) for k in data.files}
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("XXException occurredXX", exc_info=True)
            # Skip unreadable/corrupted shards
            continue


def x_load_cached__mutmut_27(cache_dir: str | Path) -> Iterator[dict[str, Tensor]]:
    """Yield cached samples stored by cache_dataset.

    Loads .npz files from cache_dir and yields tensors keyed by their original names.
    Corrupted files are skipped gracefully.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to load cached datasets")

    path = Path(cache_dir)
    for npz in sorted(path.glob("*.npz")):
        try:
            data = np.load(npz)
            _require_torch()
            yield {k: torch.tensor(data[k]) for k in data.files}
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("exception occurred", exc_info=True)
            # Skip unreadable/corrupted shards
            continue


def x_load_cached__mutmut_28(cache_dir: str | Path) -> Iterator[dict[str, Tensor]]:
    """Yield cached samples stored by cache_dataset.

    Loads .npz files from cache_dir and yields tensors keyed by their original names.
    Corrupted files are skipped gracefully.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to load cached datasets")

    path = Path(cache_dir)
    for npz in sorted(path.glob("*.npz")):
        try:
            data = np.load(npz)
            _require_torch()
            yield {k: torch.tensor(data[k]) for k in data.files}
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("EXCEPTION OCCURRED", exc_info=True)
            # Skip unreadable/corrupted shards
            continue


def x_load_cached__mutmut_29(cache_dir: str | Path) -> Iterator[dict[str, Tensor]]:
    """Yield cached samples stored by cache_dataset.

    Loads .npz files from cache_dir and yields tensors keyed by their original names.
    Corrupted files are skipped gracefully.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to load cached datasets")

    path = Path(cache_dir)
    for npz in sorted(path.glob("*.npz")):
        try:
            data = np.load(npz)
            _require_torch()
            yield {k: torch.tensor(data[k]) for k in data.files}
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=False)
            # Skip unreadable/corrupted shards
            continue


def x_load_cached__mutmut_30(cache_dir: str | Path) -> Iterator[dict[str, Tensor]]:
    """Yield cached samples stored by cache_dataset.

    Loads .npz files from cache_dir and yields tensors keyed by their original names.
    Corrupted files are skipped gracefully.
    """
    if np is None:
        raise ModuleNotFoundError("numpy is required to load cached datasets")

    path = Path(cache_dir)
    for npz in sorted(path.glob("*.npz")):
        try:
            data = np.load(npz)
            _require_torch()
            yield {k: torch.tensor(data[k]) for k in data.files}
        except Exception:
            logger.warning("Exception occurred", exc_info=True)
            logger.warning("Exception occurred", exc_info=True)
            # Skip unreadable/corrupted shards
            break

x_load_cached__mutmut_mutants : ClassVar[MutantDict] = {
'x_load_cached__mutmut_1': x_load_cached__mutmut_1, 
    'x_load_cached__mutmut_2': x_load_cached__mutmut_2, 
    'x_load_cached__mutmut_3': x_load_cached__mutmut_3, 
    'x_load_cached__mutmut_4': x_load_cached__mutmut_4, 
    'x_load_cached__mutmut_5': x_load_cached__mutmut_5, 
    'x_load_cached__mutmut_6': x_load_cached__mutmut_6, 
    'x_load_cached__mutmut_7': x_load_cached__mutmut_7, 
    'x_load_cached__mutmut_8': x_load_cached__mutmut_8, 
    'x_load_cached__mutmut_9': x_load_cached__mutmut_9, 
    'x_load_cached__mutmut_10': x_load_cached__mutmut_10, 
    'x_load_cached__mutmut_11': x_load_cached__mutmut_11, 
    'x_load_cached__mutmut_12': x_load_cached__mutmut_12, 
    'x_load_cached__mutmut_13': x_load_cached__mutmut_13, 
    'x_load_cached__mutmut_14': x_load_cached__mutmut_14, 
    'x_load_cached__mutmut_15': x_load_cached__mutmut_15, 
    'x_load_cached__mutmut_16': x_load_cached__mutmut_16, 
    'x_load_cached__mutmut_17': x_load_cached__mutmut_17, 
    'x_load_cached__mutmut_18': x_load_cached__mutmut_18, 
    'x_load_cached__mutmut_19': x_load_cached__mutmut_19, 
    'x_load_cached__mutmut_20': x_load_cached__mutmut_20, 
    'x_load_cached__mutmut_21': x_load_cached__mutmut_21, 
    'x_load_cached__mutmut_22': x_load_cached__mutmut_22, 
    'x_load_cached__mutmut_23': x_load_cached__mutmut_23, 
    'x_load_cached__mutmut_24': x_load_cached__mutmut_24, 
    'x_load_cached__mutmut_25': x_load_cached__mutmut_25, 
    'x_load_cached__mutmut_26': x_load_cached__mutmut_26, 
    'x_load_cached__mutmut_27': x_load_cached__mutmut_27, 
    'x_load_cached__mutmut_28': x_load_cached__mutmut_28, 
    'x_load_cached__mutmut_29': x_load_cached__mutmut_29, 
    'x_load_cached__mutmut_30': x_load_cached__mutmut_30
}

def load_cached(*args, **kwargs):
    result = _mutmut_trampoline(x_load_cached__mutmut_orig, x_load_cached__mutmut_mutants, args, kwargs)
    return result 

load_cached.__signature__ = _mutmut_signature(x_load_cached__mutmut_orig)
x_load_cached__mutmut_orig.__name__ = 'x_load_cached'


__all__ = [
    "deterministic_shuffle",
    "split_dataset",
    "split_texts",
    "TextDataset",
    "cache_dataset",
    "load_cached",
]
