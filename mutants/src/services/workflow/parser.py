"""YAML parser for GitHub Actions workflows.

Handles YAML parsing with edge cases like anchors, aliases, and multi-document files.
Extracts workflow metadata including triggers, inputs, jobs, and dependencies.
"""

import logging
from datetime import datetime, timezone
from pathlib import Path
from typing import Any, Optional

import yaml

from .types import (
    InputType,
    TriggerType,
    WorkflowInput,
    WorkflowJob,
    WorkflowMetadata,
    WorkflowTrigger,
)

logger = logging.getLogger(__name__)
from inspect import signature as _mutmut_signature
from typing import Annotated
from typing import Callable
from typing import ClassVar


MutantDict = Annotated[dict[str, Callable], "Mutant"]


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg = None):
    """Forward call to original or mutated function, depending on the environment"""
    import os
    mutant_under_test = os.environ['MUTANT_UNDER_TEST']
    if mutant_under_test == 'fail':
        from mutmut.__main__ import MutmutProgrammaticFailException
        raise MutmutProgrammaticFailException('Failed programmatically')      
    elif mutant_under_test == 'stats':
        from mutmut.__main__ import record_trampoline_hit
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__)
        result = orig(*call_args, **call_kwargs)
        return result
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_'
    if not mutant_under_test.startswith(prefix):
        result = orig(*call_args, **call_kwargs)
        return result
    mutant_name = mutant_under_test.rpartition('.')[-1]
    if self_arg is not None:
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs)
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs)
    return result


class WorkflowParser:
    """Parser for GitHub Actions workflow YAML files.

    Handles:
    - Standard YAML parsing
    - YAML anchors and aliases
    - Multi-document YAML files
    - Malformed YAML with graceful degradation
    - Extraction of triggers, inputs, jobs, dependencies
    """

    def xǁWorkflowParserǁ__init____mutmut_orig(self):
        """Initialize the parser."""
        self._cache: dict[Path, WorkflowMetadata] = {}

    def xǁWorkflowParserǁ__init____mutmut_1(self):
        """Initialize the parser."""
        self._cache: dict[Path, WorkflowMetadata] = None
    
    xǁWorkflowParserǁ__init____mutmut_mutants : ClassVar[MutantDict] = {
    'xǁWorkflowParserǁ__init____mutmut_1': xǁWorkflowParserǁ__init____mutmut_1
    }
    
    def __init__(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁWorkflowParserǁ__init____mutmut_orig"), object.__getattribute__(self, "xǁWorkflowParserǁ__init____mutmut_mutants"), args, kwargs, self)
        return result 
    
    __init__.__signature__ = _mutmut_signature(xǁWorkflowParserǁ__init____mutmut_orig)
    xǁWorkflowParserǁ__init____mutmut_orig.__name__ = 'xǁWorkflowParserǁ__init__'

    def xǁWorkflowParserǁparse_file__mutmut_orig(self, file_path: Path, use_cache: bool = True) -> Optional[WorkflowMetadata]:
        """Parse a workflow file.

        Args:
            file_path: Path to the workflow YAML file.
            use_cache: Whether to use cached results.

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        if use_cache and file_path in self._cache:
            return self._cache[file_path]

        if not file_path.exists():
            logger.warning(f"Workflow file not found: {file_path}")
            return None

        try:
            with open(file_path, "r", encoding="utf-8") as f:
                content = f.read()

            metadata = self.parse_content(content, file_path)
            if metadata and use_cache:
                self._cache[file_path] = metadata
            return metadata
        except FileNotFoundError as e:
            logger.debug(f"FileNotFoundError: {e}")
            logger.warning(f"FileNotFoundError: {e}", exc_info=True)
            logger.error(f"Workflow file not found: {file_path}")
            return None
        except PermissionError as e:
            logger.debug(f"PermissionError: {e}")
            logger.warning(f"PermissionError: {e}", exc_info=True)
            logger.error(f"Permission denied reading workflow: {file_path}")
            return None
        except UnicodeDecodeError as e:
            logger.debug(f"UnicodeDecodeError: {e}")
            logger.error(f"Invalid UTF-8 encoding in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Failed to parse workflow {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_file__mutmut_1(self, file_path: Path, use_cache: bool = False) -> Optional[WorkflowMetadata]:
        """Parse a workflow file.

        Args:
            file_path: Path to the workflow YAML file.
            use_cache: Whether to use cached results.

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        if use_cache and file_path in self._cache:
            return self._cache[file_path]

        if not file_path.exists():
            logger.warning(f"Workflow file not found: {file_path}")
            return None

        try:
            with open(file_path, "r", encoding="utf-8") as f:
                content = f.read()

            metadata = self.parse_content(content, file_path)
            if metadata and use_cache:
                self._cache[file_path] = metadata
            return metadata
        except FileNotFoundError as e:
            logger.debug(f"FileNotFoundError: {e}")
            logger.warning(f"FileNotFoundError: {e}", exc_info=True)
            logger.error(f"Workflow file not found: {file_path}")
            return None
        except PermissionError as e:
            logger.debug(f"PermissionError: {e}")
            logger.warning(f"PermissionError: {e}", exc_info=True)
            logger.error(f"Permission denied reading workflow: {file_path}")
            return None
        except UnicodeDecodeError as e:
            logger.debug(f"UnicodeDecodeError: {e}")
            logger.error(f"Invalid UTF-8 encoding in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Failed to parse workflow {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_file__mutmut_2(self, file_path: Path, use_cache: bool = True) -> Optional[WorkflowMetadata]:
        """Parse a workflow file.

        Args:
            file_path: Path to the workflow YAML file.
            use_cache: Whether to use cached results.

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        if use_cache or file_path in self._cache:
            return self._cache[file_path]

        if not file_path.exists():
            logger.warning(f"Workflow file not found: {file_path}")
            return None

        try:
            with open(file_path, "r", encoding="utf-8") as f:
                content = f.read()

            metadata = self.parse_content(content, file_path)
            if metadata and use_cache:
                self._cache[file_path] = metadata
            return metadata
        except FileNotFoundError as e:
            logger.debug(f"FileNotFoundError: {e}")
            logger.warning(f"FileNotFoundError: {e}", exc_info=True)
            logger.error(f"Workflow file not found: {file_path}")
            return None
        except PermissionError as e:
            logger.debug(f"PermissionError: {e}")
            logger.warning(f"PermissionError: {e}", exc_info=True)
            logger.error(f"Permission denied reading workflow: {file_path}")
            return None
        except UnicodeDecodeError as e:
            logger.debug(f"UnicodeDecodeError: {e}")
            logger.error(f"Invalid UTF-8 encoding in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Failed to parse workflow {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_file__mutmut_3(self, file_path: Path, use_cache: bool = True) -> Optional[WorkflowMetadata]:
        """Parse a workflow file.

        Args:
            file_path: Path to the workflow YAML file.
            use_cache: Whether to use cached results.

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        if use_cache and file_path not in self._cache:
            return self._cache[file_path]

        if not file_path.exists():
            logger.warning(f"Workflow file not found: {file_path}")
            return None

        try:
            with open(file_path, "r", encoding="utf-8") as f:
                content = f.read()

            metadata = self.parse_content(content, file_path)
            if metadata and use_cache:
                self._cache[file_path] = metadata
            return metadata
        except FileNotFoundError as e:
            logger.debug(f"FileNotFoundError: {e}")
            logger.warning(f"FileNotFoundError: {e}", exc_info=True)
            logger.error(f"Workflow file not found: {file_path}")
            return None
        except PermissionError as e:
            logger.debug(f"PermissionError: {e}")
            logger.warning(f"PermissionError: {e}", exc_info=True)
            logger.error(f"Permission denied reading workflow: {file_path}")
            return None
        except UnicodeDecodeError as e:
            logger.debug(f"UnicodeDecodeError: {e}")
            logger.error(f"Invalid UTF-8 encoding in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Failed to parse workflow {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_file__mutmut_4(self, file_path: Path, use_cache: bool = True) -> Optional[WorkflowMetadata]:
        """Parse a workflow file.

        Args:
            file_path: Path to the workflow YAML file.
            use_cache: Whether to use cached results.

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        if use_cache and file_path in self._cache:
            return self._cache[file_path]

        if file_path.exists():
            logger.warning(f"Workflow file not found: {file_path}")
            return None

        try:
            with open(file_path, "r", encoding="utf-8") as f:
                content = f.read()

            metadata = self.parse_content(content, file_path)
            if metadata and use_cache:
                self._cache[file_path] = metadata
            return metadata
        except FileNotFoundError as e:
            logger.debug(f"FileNotFoundError: {e}")
            logger.warning(f"FileNotFoundError: {e}", exc_info=True)
            logger.error(f"Workflow file not found: {file_path}")
            return None
        except PermissionError as e:
            logger.debug(f"PermissionError: {e}")
            logger.warning(f"PermissionError: {e}", exc_info=True)
            logger.error(f"Permission denied reading workflow: {file_path}")
            return None
        except UnicodeDecodeError as e:
            logger.debug(f"UnicodeDecodeError: {e}")
            logger.error(f"Invalid UTF-8 encoding in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Failed to parse workflow {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_file__mutmut_5(self, file_path: Path, use_cache: bool = True) -> Optional[WorkflowMetadata]:
        """Parse a workflow file.

        Args:
            file_path: Path to the workflow YAML file.
            use_cache: Whether to use cached results.

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        if use_cache and file_path in self._cache:
            return self._cache[file_path]

        if not file_path.exists():
            logger.warning(None)
            return None

        try:
            with open(file_path, "r", encoding="utf-8") as f:
                content = f.read()

            metadata = self.parse_content(content, file_path)
            if metadata and use_cache:
                self._cache[file_path] = metadata
            return metadata
        except FileNotFoundError as e:
            logger.debug(f"FileNotFoundError: {e}")
            logger.warning(f"FileNotFoundError: {e}", exc_info=True)
            logger.error(f"Workflow file not found: {file_path}")
            return None
        except PermissionError as e:
            logger.debug(f"PermissionError: {e}")
            logger.warning(f"PermissionError: {e}", exc_info=True)
            logger.error(f"Permission denied reading workflow: {file_path}")
            return None
        except UnicodeDecodeError as e:
            logger.debug(f"UnicodeDecodeError: {e}")
            logger.error(f"Invalid UTF-8 encoding in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Failed to parse workflow {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_file__mutmut_6(self, file_path: Path, use_cache: bool = True) -> Optional[WorkflowMetadata]:
        """Parse a workflow file.

        Args:
            file_path: Path to the workflow YAML file.
            use_cache: Whether to use cached results.

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        if use_cache and file_path in self._cache:
            return self._cache[file_path]

        if not file_path.exists():
            logger.warning(f"Workflow file not found: {file_path}")
            return None

        try:
            with open(None, "r", encoding="utf-8") as f:
                content = f.read()

            metadata = self.parse_content(content, file_path)
            if metadata and use_cache:
                self._cache[file_path] = metadata
            return metadata
        except FileNotFoundError as e:
            logger.debug(f"FileNotFoundError: {e}")
            logger.warning(f"FileNotFoundError: {e}", exc_info=True)
            logger.error(f"Workflow file not found: {file_path}")
            return None
        except PermissionError as e:
            logger.debug(f"PermissionError: {e}")
            logger.warning(f"PermissionError: {e}", exc_info=True)
            logger.error(f"Permission denied reading workflow: {file_path}")
            return None
        except UnicodeDecodeError as e:
            logger.debug(f"UnicodeDecodeError: {e}")
            logger.error(f"Invalid UTF-8 encoding in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Failed to parse workflow {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_file__mutmut_7(self, file_path: Path, use_cache: bool = True) -> Optional[WorkflowMetadata]:
        """Parse a workflow file.

        Args:
            file_path: Path to the workflow YAML file.
            use_cache: Whether to use cached results.

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        if use_cache and file_path in self._cache:
            return self._cache[file_path]

        if not file_path.exists():
            logger.warning(f"Workflow file not found: {file_path}")
            return None

        try:
            with open(file_path, None, encoding="utf-8") as f:
                content = f.read()

            metadata = self.parse_content(content, file_path)
            if metadata and use_cache:
                self._cache[file_path] = metadata
            return metadata
        except FileNotFoundError as e:
            logger.debug(f"FileNotFoundError: {e}")
            logger.warning(f"FileNotFoundError: {e}", exc_info=True)
            logger.error(f"Workflow file not found: {file_path}")
            return None
        except PermissionError as e:
            logger.debug(f"PermissionError: {e}")
            logger.warning(f"PermissionError: {e}", exc_info=True)
            logger.error(f"Permission denied reading workflow: {file_path}")
            return None
        except UnicodeDecodeError as e:
            logger.debug(f"UnicodeDecodeError: {e}")
            logger.error(f"Invalid UTF-8 encoding in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Failed to parse workflow {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_file__mutmut_8(self, file_path: Path, use_cache: bool = True) -> Optional[WorkflowMetadata]:
        """Parse a workflow file.

        Args:
            file_path: Path to the workflow YAML file.
            use_cache: Whether to use cached results.

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        if use_cache and file_path in self._cache:
            return self._cache[file_path]

        if not file_path.exists():
            logger.warning(f"Workflow file not found: {file_path}")
            return None

        try:
            with open(file_path, "r", encoding=None) as f:
                content = f.read()

            metadata = self.parse_content(content, file_path)
            if metadata and use_cache:
                self._cache[file_path] = metadata
            return metadata
        except FileNotFoundError as e:
            logger.debug(f"FileNotFoundError: {e}")
            logger.warning(f"FileNotFoundError: {e}", exc_info=True)
            logger.error(f"Workflow file not found: {file_path}")
            return None
        except PermissionError as e:
            logger.debug(f"PermissionError: {e}")
            logger.warning(f"PermissionError: {e}", exc_info=True)
            logger.error(f"Permission denied reading workflow: {file_path}")
            return None
        except UnicodeDecodeError as e:
            logger.debug(f"UnicodeDecodeError: {e}")
            logger.error(f"Invalid UTF-8 encoding in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Failed to parse workflow {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_file__mutmut_9(self, file_path: Path, use_cache: bool = True) -> Optional[WorkflowMetadata]:
        """Parse a workflow file.

        Args:
            file_path: Path to the workflow YAML file.
            use_cache: Whether to use cached results.

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        if use_cache and file_path in self._cache:
            return self._cache[file_path]

        if not file_path.exists():
            logger.warning(f"Workflow file not found: {file_path}")
            return None

        try:
            with open("r", encoding="utf-8") as f:
                content = f.read()

            metadata = self.parse_content(content, file_path)
            if metadata and use_cache:
                self._cache[file_path] = metadata
            return metadata
        except FileNotFoundError as e:
            logger.debug(f"FileNotFoundError: {e}")
            logger.warning(f"FileNotFoundError: {e}", exc_info=True)
            logger.error(f"Workflow file not found: {file_path}")
            return None
        except PermissionError as e:
            logger.debug(f"PermissionError: {e}")
            logger.warning(f"PermissionError: {e}", exc_info=True)
            logger.error(f"Permission denied reading workflow: {file_path}")
            return None
        except UnicodeDecodeError as e:
            logger.debug(f"UnicodeDecodeError: {e}")
            logger.error(f"Invalid UTF-8 encoding in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Failed to parse workflow {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_file__mutmut_10(self, file_path: Path, use_cache: bool = True) -> Optional[WorkflowMetadata]:
        """Parse a workflow file.

        Args:
            file_path: Path to the workflow YAML file.
            use_cache: Whether to use cached results.

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        if use_cache and file_path in self._cache:
            return self._cache[file_path]

        if not file_path.exists():
            logger.warning(f"Workflow file not found: {file_path}")
            return None

        try:
            with open(file_path, encoding="utf-8") as f:
                content = f.read()

            metadata = self.parse_content(content, file_path)
            if metadata and use_cache:
                self._cache[file_path] = metadata
            return metadata
        except FileNotFoundError as e:
            logger.debug(f"FileNotFoundError: {e}")
            logger.warning(f"FileNotFoundError: {e}", exc_info=True)
            logger.error(f"Workflow file not found: {file_path}")
            return None
        except PermissionError as e:
            logger.debug(f"PermissionError: {e}")
            logger.warning(f"PermissionError: {e}", exc_info=True)
            logger.error(f"Permission denied reading workflow: {file_path}")
            return None
        except UnicodeDecodeError as e:
            logger.debug(f"UnicodeDecodeError: {e}")
            logger.error(f"Invalid UTF-8 encoding in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Failed to parse workflow {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_file__mutmut_11(self, file_path: Path, use_cache: bool = True) -> Optional[WorkflowMetadata]:
        """Parse a workflow file.

        Args:
            file_path: Path to the workflow YAML file.
            use_cache: Whether to use cached results.

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        if use_cache and file_path in self._cache:
            return self._cache[file_path]

        if not file_path.exists():
            logger.warning(f"Workflow file not found: {file_path}")
            return None

        try:
            with open(file_path, "r", ) as f:
                content = f.read()

            metadata = self.parse_content(content, file_path)
            if metadata and use_cache:
                self._cache[file_path] = metadata
            return metadata
        except FileNotFoundError as e:
            logger.debug(f"FileNotFoundError: {e}")
            logger.warning(f"FileNotFoundError: {e}", exc_info=True)
            logger.error(f"Workflow file not found: {file_path}")
            return None
        except PermissionError as e:
            logger.debug(f"PermissionError: {e}")
            logger.warning(f"PermissionError: {e}", exc_info=True)
            logger.error(f"Permission denied reading workflow: {file_path}")
            return None
        except UnicodeDecodeError as e:
            logger.debug(f"UnicodeDecodeError: {e}")
            logger.error(f"Invalid UTF-8 encoding in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Failed to parse workflow {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_file__mutmut_12(self, file_path: Path, use_cache: bool = True) -> Optional[WorkflowMetadata]:
        """Parse a workflow file.

        Args:
            file_path: Path to the workflow YAML file.
            use_cache: Whether to use cached results.

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        if use_cache and file_path in self._cache:
            return self._cache[file_path]

        if not file_path.exists():
            logger.warning(f"Workflow file not found: {file_path}")
            return None

        try:
            with open(file_path, "XXrXX", encoding="utf-8") as f:
                content = f.read()

            metadata = self.parse_content(content, file_path)
            if metadata and use_cache:
                self._cache[file_path] = metadata
            return metadata
        except FileNotFoundError as e:
            logger.debug(f"FileNotFoundError: {e}")
            logger.warning(f"FileNotFoundError: {e}", exc_info=True)
            logger.error(f"Workflow file not found: {file_path}")
            return None
        except PermissionError as e:
            logger.debug(f"PermissionError: {e}")
            logger.warning(f"PermissionError: {e}", exc_info=True)
            logger.error(f"Permission denied reading workflow: {file_path}")
            return None
        except UnicodeDecodeError as e:
            logger.debug(f"UnicodeDecodeError: {e}")
            logger.error(f"Invalid UTF-8 encoding in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Failed to parse workflow {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_file__mutmut_13(self, file_path: Path, use_cache: bool = True) -> Optional[WorkflowMetadata]:
        """Parse a workflow file.

        Args:
            file_path: Path to the workflow YAML file.
            use_cache: Whether to use cached results.

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        if use_cache and file_path in self._cache:
            return self._cache[file_path]

        if not file_path.exists():
            logger.warning(f"Workflow file not found: {file_path}")
            return None

        try:
            with open(file_path, "R", encoding="utf-8") as f:
                content = f.read()

            metadata = self.parse_content(content, file_path)
            if metadata and use_cache:
                self._cache[file_path] = metadata
            return metadata
        except FileNotFoundError as e:
            logger.debug(f"FileNotFoundError: {e}")
            logger.warning(f"FileNotFoundError: {e}", exc_info=True)
            logger.error(f"Workflow file not found: {file_path}")
            return None
        except PermissionError as e:
            logger.debug(f"PermissionError: {e}")
            logger.warning(f"PermissionError: {e}", exc_info=True)
            logger.error(f"Permission denied reading workflow: {file_path}")
            return None
        except UnicodeDecodeError as e:
            logger.debug(f"UnicodeDecodeError: {e}")
            logger.error(f"Invalid UTF-8 encoding in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Failed to parse workflow {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_file__mutmut_14(self, file_path: Path, use_cache: bool = True) -> Optional[WorkflowMetadata]:
        """Parse a workflow file.

        Args:
            file_path: Path to the workflow YAML file.
            use_cache: Whether to use cached results.

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        if use_cache and file_path in self._cache:
            return self._cache[file_path]

        if not file_path.exists():
            logger.warning(f"Workflow file not found: {file_path}")
            return None

        try:
            with open(file_path, "r", encoding="XXutf-8XX") as f:
                content = f.read()

            metadata = self.parse_content(content, file_path)
            if metadata and use_cache:
                self._cache[file_path] = metadata
            return metadata
        except FileNotFoundError as e:
            logger.debug(f"FileNotFoundError: {e}")
            logger.warning(f"FileNotFoundError: {e}", exc_info=True)
            logger.error(f"Workflow file not found: {file_path}")
            return None
        except PermissionError as e:
            logger.debug(f"PermissionError: {e}")
            logger.warning(f"PermissionError: {e}", exc_info=True)
            logger.error(f"Permission denied reading workflow: {file_path}")
            return None
        except UnicodeDecodeError as e:
            logger.debug(f"UnicodeDecodeError: {e}")
            logger.error(f"Invalid UTF-8 encoding in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Failed to parse workflow {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_file__mutmut_15(self, file_path: Path, use_cache: bool = True) -> Optional[WorkflowMetadata]:
        """Parse a workflow file.

        Args:
            file_path: Path to the workflow YAML file.
            use_cache: Whether to use cached results.

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        if use_cache and file_path in self._cache:
            return self._cache[file_path]

        if not file_path.exists():
            logger.warning(f"Workflow file not found: {file_path}")
            return None

        try:
            with open(file_path, "r", encoding="UTF-8") as f:
                content = f.read()

            metadata = self.parse_content(content, file_path)
            if metadata and use_cache:
                self._cache[file_path] = metadata
            return metadata
        except FileNotFoundError as e:
            logger.debug(f"FileNotFoundError: {e}")
            logger.warning(f"FileNotFoundError: {e}", exc_info=True)
            logger.error(f"Workflow file not found: {file_path}")
            return None
        except PermissionError as e:
            logger.debug(f"PermissionError: {e}")
            logger.warning(f"PermissionError: {e}", exc_info=True)
            logger.error(f"Permission denied reading workflow: {file_path}")
            return None
        except UnicodeDecodeError as e:
            logger.debug(f"UnicodeDecodeError: {e}")
            logger.error(f"Invalid UTF-8 encoding in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Failed to parse workflow {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_file__mutmut_16(self, file_path: Path, use_cache: bool = True) -> Optional[WorkflowMetadata]:
        """Parse a workflow file.

        Args:
            file_path: Path to the workflow YAML file.
            use_cache: Whether to use cached results.

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        if use_cache and file_path in self._cache:
            return self._cache[file_path]

        if not file_path.exists():
            logger.warning(f"Workflow file not found: {file_path}")
            return None

        try:
            with open(file_path, "r", encoding="utf-8") as f:
                content = None

            metadata = self.parse_content(content, file_path)
            if metadata and use_cache:
                self._cache[file_path] = metadata
            return metadata
        except FileNotFoundError as e:
            logger.debug(f"FileNotFoundError: {e}")
            logger.warning(f"FileNotFoundError: {e}", exc_info=True)
            logger.error(f"Workflow file not found: {file_path}")
            return None
        except PermissionError as e:
            logger.debug(f"PermissionError: {e}")
            logger.warning(f"PermissionError: {e}", exc_info=True)
            logger.error(f"Permission denied reading workflow: {file_path}")
            return None
        except UnicodeDecodeError as e:
            logger.debug(f"UnicodeDecodeError: {e}")
            logger.error(f"Invalid UTF-8 encoding in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Failed to parse workflow {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_file__mutmut_17(self, file_path: Path, use_cache: bool = True) -> Optional[WorkflowMetadata]:
        """Parse a workflow file.

        Args:
            file_path: Path to the workflow YAML file.
            use_cache: Whether to use cached results.

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        if use_cache and file_path in self._cache:
            return self._cache[file_path]

        if not file_path.exists():
            logger.warning(f"Workflow file not found: {file_path}")
            return None

        try:
            with open(file_path, "r", encoding="utf-8") as f:
                content = f.read()

            metadata = None
            if metadata and use_cache:
                self._cache[file_path] = metadata
            return metadata
        except FileNotFoundError as e:
            logger.debug(f"FileNotFoundError: {e}")
            logger.warning(f"FileNotFoundError: {e}", exc_info=True)
            logger.error(f"Workflow file not found: {file_path}")
            return None
        except PermissionError as e:
            logger.debug(f"PermissionError: {e}")
            logger.warning(f"PermissionError: {e}", exc_info=True)
            logger.error(f"Permission denied reading workflow: {file_path}")
            return None
        except UnicodeDecodeError as e:
            logger.debug(f"UnicodeDecodeError: {e}")
            logger.error(f"Invalid UTF-8 encoding in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Failed to parse workflow {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_file__mutmut_18(self, file_path: Path, use_cache: bool = True) -> Optional[WorkflowMetadata]:
        """Parse a workflow file.

        Args:
            file_path: Path to the workflow YAML file.
            use_cache: Whether to use cached results.

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        if use_cache and file_path in self._cache:
            return self._cache[file_path]

        if not file_path.exists():
            logger.warning(f"Workflow file not found: {file_path}")
            return None

        try:
            with open(file_path, "r", encoding="utf-8") as f:
                content = f.read()

            metadata = self.parse_content(None, file_path)
            if metadata and use_cache:
                self._cache[file_path] = metadata
            return metadata
        except FileNotFoundError as e:
            logger.debug(f"FileNotFoundError: {e}")
            logger.warning(f"FileNotFoundError: {e}", exc_info=True)
            logger.error(f"Workflow file not found: {file_path}")
            return None
        except PermissionError as e:
            logger.debug(f"PermissionError: {e}")
            logger.warning(f"PermissionError: {e}", exc_info=True)
            logger.error(f"Permission denied reading workflow: {file_path}")
            return None
        except UnicodeDecodeError as e:
            logger.debug(f"UnicodeDecodeError: {e}")
            logger.error(f"Invalid UTF-8 encoding in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Failed to parse workflow {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_file__mutmut_19(self, file_path: Path, use_cache: bool = True) -> Optional[WorkflowMetadata]:
        """Parse a workflow file.

        Args:
            file_path: Path to the workflow YAML file.
            use_cache: Whether to use cached results.

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        if use_cache and file_path in self._cache:
            return self._cache[file_path]

        if not file_path.exists():
            logger.warning(f"Workflow file not found: {file_path}")
            return None

        try:
            with open(file_path, "r", encoding="utf-8") as f:
                content = f.read()

            metadata = self.parse_content(content, None)
            if metadata and use_cache:
                self._cache[file_path] = metadata
            return metadata
        except FileNotFoundError as e:
            logger.debug(f"FileNotFoundError: {e}")
            logger.warning(f"FileNotFoundError: {e}", exc_info=True)
            logger.error(f"Workflow file not found: {file_path}")
            return None
        except PermissionError as e:
            logger.debug(f"PermissionError: {e}")
            logger.warning(f"PermissionError: {e}", exc_info=True)
            logger.error(f"Permission denied reading workflow: {file_path}")
            return None
        except UnicodeDecodeError as e:
            logger.debug(f"UnicodeDecodeError: {e}")
            logger.error(f"Invalid UTF-8 encoding in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Failed to parse workflow {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_file__mutmut_20(self, file_path: Path, use_cache: bool = True) -> Optional[WorkflowMetadata]:
        """Parse a workflow file.

        Args:
            file_path: Path to the workflow YAML file.
            use_cache: Whether to use cached results.

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        if use_cache and file_path in self._cache:
            return self._cache[file_path]

        if not file_path.exists():
            logger.warning(f"Workflow file not found: {file_path}")
            return None

        try:
            with open(file_path, "r", encoding="utf-8") as f:
                content = f.read()

            metadata = self.parse_content(file_path)
            if metadata and use_cache:
                self._cache[file_path] = metadata
            return metadata
        except FileNotFoundError as e:
            logger.debug(f"FileNotFoundError: {e}")
            logger.warning(f"FileNotFoundError: {e}", exc_info=True)
            logger.error(f"Workflow file not found: {file_path}")
            return None
        except PermissionError as e:
            logger.debug(f"PermissionError: {e}")
            logger.warning(f"PermissionError: {e}", exc_info=True)
            logger.error(f"Permission denied reading workflow: {file_path}")
            return None
        except UnicodeDecodeError as e:
            logger.debug(f"UnicodeDecodeError: {e}")
            logger.error(f"Invalid UTF-8 encoding in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Failed to parse workflow {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_file__mutmut_21(self, file_path: Path, use_cache: bool = True) -> Optional[WorkflowMetadata]:
        """Parse a workflow file.

        Args:
            file_path: Path to the workflow YAML file.
            use_cache: Whether to use cached results.

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        if use_cache and file_path in self._cache:
            return self._cache[file_path]

        if not file_path.exists():
            logger.warning(f"Workflow file not found: {file_path}")
            return None

        try:
            with open(file_path, "r", encoding="utf-8") as f:
                content = f.read()

            metadata = self.parse_content(content, )
            if metadata and use_cache:
                self._cache[file_path] = metadata
            return metadata
        except FileNotFoundError as e:
            logger.debug(f"FileNotFoundError: {e}")
            logger.warning(f"FileNotFoundError: {e}", exc_info=True)
            logger.error(f"Workflow file not found: {file_path}")
            return None
        except PermissionError as e:
            logger.debug(f"PermissionError: {e}")
            logger.warning(f"PermissionError: {e}", exc_info=True)
            logger.error(f"Permission denied reading workflow: {file_path}")
            return None
        except UnicodeDecodeError as e:
            logger.debug(f"UnicodeDecodeError: {e}")
            logger.error(f"Invalid UTF-8 encoding in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Failed to parse workflow {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_file__mutmut_22(self, file_path: Path, use_cache: bool = True) -> Optional[WorkflowMetadata]:
        """Parse a workflow file.

        Args:
            file_path: Path to the workflow YAML file.
            use_cache: Whether to use cached results.

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        if use_cache and file_path in self._cache:
            return self._cache[file_path]

        if not file_path.exists():
            logger.warning(f"Workflow file not found: {file_path}")
            return None

        try:
            with open(file_path, "r", encoding="utf-8") as f:
                content = f.read()

            metadata = self.parse_content(content, file_path)
            if metadata or use_cache:
                self._cache[file_path] = metadata
            return metadata
        except FileNotFoundError as e:
            logger.debug(f"FileNotFoundError: {e}")
            logger.warning(f"FileNotFoundError: {e}", exc_info=True)
            logger.error(f"Workflow file not found: {file_path}")
            return None
        except PermissionError as e:
            logger.debug(f"PermissionError: {e}")
            logger.warning(f"PermissionError: {e}", exc_info=True)
            logger.error(f"Permission denied reading workflow: {file_path}")
            return None
        except UnicodeDecodeError as e:
            logger.debug(f"UnicodeDecodeError: {e}")
            logger.error(f"Invalid UTF-8 encoding in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Failed to parse workflow {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_file__mutmut_23(self, file_path: Path, use_cache: bool = True) -> Optional[WorkflowMetadata]:
        """Parse a workflow file.

        Args:
            file_path: Path to the workflow YAML file.
            use_cache: Whether to use cached results.

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        if use_cache and file_path in self._cache:
            return self._cache[file_path]

        if not file_path.exists():
            logger.warning(f"Workflow file not found: {file_path}")
            return None

        try:
            with open(file_path, "r", encoding="utf-8") as f:
                content = f.read()

            metadata = self.parse_content(content, file_path)
            if metadata and use_cache:
                self._cache[file_path] = None
            return metadata
        except FileNotFoundError as e:
            logger.debug(f"FileNotFoundError: {e}")
            logger.warning(f"FileNotFoundError: {e}", exc_info=True)
            logger.error(f"Workflow file not found: {file_path}")
            return None
        except PermissionError as e:
            logger.debug(f"PermissionError: {e}")
            logger.warning(f"PermissionError: {e}", exc_info=True)
            logger.error(f"Permission denied reading workflow: {file_path}")
            return None
        except UnicodeDecodeError as e:
            logger.debug(f"UnicodeDecodeError: {e}")
            logger.error(f"Invalid UTF-8 encoding in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Failed to parse workflow {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_file__mutmut_24(self, file_path: Path, use_cache: bool = True) -> Optional[WorkflowMetadata]:
        """Parse a workflow file.

        Args:
            file_path: Path to the workflow YAML file.
            use_cache: Whether to use cached results.

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        if use_cache and file_path in self._cache:
            return self._cache[file_path]

        if not file_path.exists():
            logger.warning(f"Workflow file not found: {file_path}")
            return None

        try:
            with open(file_path, "r", encoding="utf-8") as f:
                content = f.read()

            metadata = self.parse_content(content, file_path)
            if metadata and use_cache:
                self._cache[file_path] = metadata
            return metadata
        except FileNotFoundError as e:
            logger.debug(None)
            logger.warning(f"FileNotFoundError: {e}", exc_info=True)
            logger.error(f"Workflow file not found: {file_path}")
            return None
        except PermissionError as e:
            logger.debug(f"PermissionError: {e}")
            logger.warning(f"PermissionError: {e}", exc_info=True)
            logger.error(f"Permission denied reading workflow: {file_path}")
            return None
        except UnicodeDecodeError as e:
            logger.debug(f"UnicodeDecodeError: {e}")
            logger.error(f"Invalid UTF-8 encoding in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Failed to parse workflow {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_file__mutmut_25(self, file_path: Path, use_cache: bool = True) -> Optional[WorkflowMetadata]:
        """Parse a workflow file.

        Args:
            file_path: Path to the workflow YAML file.
            use_cache: Whether to use cached results.

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        if use_cache and file_path in self._cache:
            return self._cache[file_path]

        if not file_path.exists():
            logger.warning(f"Workflow file not found: {file_path}")
            return None

        try:
            with open(file_path, "r", encoding="utf-8") as f:
                content = f.read()

            metadata = self.parse_content(content, file_path)
            if metadata and use_cache:
                self._cache[file_path] = metadata
            return metadata
        except FileNotFoundError as e:
            logger.debug(f"FileNotFoundError: {e}")
            logger.warning(None, exc_info=True)
            logger.error(f"Workflow file not found: {file_path}")
            return None
        except PermissionError as e:
            logger.debug(f"PermissionError: {e}")
            logger.warning(f"PermissionError: {e}", exc_info=True)
            logger.error(f"Permission denied reading workflow: {file_path}")
            return None
        except UnicodeDecodeError as e:
            logger.debug(f"UnicodeDecodeError: {e}")
            logger.error(f"Invalid UTF-8 encoding in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Failed to parse workflow {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_file__mutmut_26(self, file_path: Path, use_cache: bool = True) -> Optional[WorkflowMetadata]:
        """Parse a workflow file.

        Args:
            file_path: Path to the workflow YAML file.
            use_cache: Whether to use cached results.

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        if use_cache and file_path in self._cache:
            return self._cache[file_path]

        if not file_path.exists():
            logger.warning(f"Workflow file not found: {file_path}")
            return None

        try:
            with open(file_path, "r", encoding="utf-8") as f:
                content = f.read()

            metadata = self.parse_content(content, file_path)
            if metadata and use_cache:
                self._cache[file_path] = metadata
            return metadata
        except FileNotFoundError as e:
            logger.debug(f"FileNotFoundError: {e}")
            logger.warning(f"FileNotFoundError: {e}", exc_info=None)
            logger.error(f"Workflow file not found: {file_path}")
            return None
        except PermissionError as e:
            logger.debug(f"PermissionError: {e}")
            logger.warning(f"PermissionError: {e}", exc_info=True)
            logger.error(f"Permission denied reading workflow: {file_path}")
            return None
        except UnicodeDecodeError as e:
            logger.debug(f"UnicodeDecodeError: {e}")
            logger.error(f"Invalid UTF-8 encoding in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Failed to parse workflow {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_file__mutmut_27(self, file_path: Path, use_cache: bool = True) -> Optional[WorkflowMetadata]:
        """Parse a workflow file.

        Args:
            file_path: Path to the workflow YAML file.
            use_cache: Whether to use cached results.

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        if use_cache and file_path in self._cache:
            return self._cache[file_path]

        if not file_path.exists():
            logger.warning(f"Workflow file not found: {file_path}")
            return None

        try:
            with open(file_path, "r", encoding="utf-8") as f:
                content = f.read()

            metadata = self.parse_content(content, file_path)
            if metadata and use_cache:
                self._cache[file_path] = metadata
            return metadata
        except FileNotFoundError as e:
            logger.debug(f"FileNotFoundError: {e}")
            logger.warning(exc_info=True)
            logger.error(f"Workflow file not found: {file_path}")
            return None
        except PermissionError as e:
            logger.debug(f"PermissionError: {e}")
            logger.warning(f"PermissionError: {e}", exc_info=True)
            logger.error(f"Permission denied reading workflow: {file_path}")
            return None
        except UnicodeDecodeError as e:
            logger.debug(f"UnicodeDecodeError: {e}")
            logger.error(f"Invalid UTF-8 encoding in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Failed to parse workflow {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_file__mutmut_28(self, file_path: Path, use_cache: bool = True) -> Optional[WorkflowMetadata]:
        """Parse a workflow file.

        Args:
            file_path: Path to the workflow YAML file.
            use_cache: Whether to use cached results.

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        if use_cache and file_path in self._cache:
            return self._cache[file_path]

        if not file_path.exists():
            logger.warning(f"Workflow file not found: {file_path}")
            return None

        try:
            with open(file_path, "r", encoding="utf-8") as f:
                content = f.read()

            metadata = self.parse_content(content, file_path)
            if metadata and use_cache:
                self._cache[file_path] = metadata
            return metadata
        except FileNotFoundError as e:
            logger.debug(f"FileNotFoundError: {e}")
            logger.warning(f"FileNotFoundError: {e}", )
            logger.error(f"Workflow file not found: {file_path}")
            return None
        except PermissionError as e:
            logger.debug(f"PermissionError: {e}")
            logger.warning(f"PermissionError: {e}", exc_info=True)
            logger.error(f"Permission denied reading workflow: {file_path}")
            return None
        except UnicodeDecodeError as e:
            logger.debug(f"UnicodeDecodeError: {e}")
            logger.error(f"Invalid UTF-8 encoding in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Failed to parse workflow {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_file__mutmut_29(self, file_path: Path, use_cache: bool = True) -> Optional[WorkflowMetadata]:
        """Parse a workflow file.

        Args:
            file_path: Path to the workflow YAML file.
            use_cache: Whether to use cached results.

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        if use_cache and file_path in self._cache:
            return self._cache[file_path]

        if not file_path.exists():
            logger.warning(f"Workflow file not found: {file_path}")
            return None

        try:
            with open(file_path, "r", encoding="utf-8") as f:
                content = f.read()

            metadata = self.parse_content(content, file_path)
            if metadata and use_cache:
                self._cache[file_path] = metadata
            return metadata
        except FileNotFoundError as e:
            logger.debug(f"FileNotFoundError: {e}")
            logger.warning(f"FileNotFoundError: {e}", exc_info=False)
            logger.error(f"Workflow file not found: {file_path}")
            return None
        except PermissionError as e:
            logger.debug(f"PermissionError: {e}")
            logger.warning(f"PermissionError: {e}", exc_info=True)
            logger.error(f"Permission denied reading workflow: {file_path}")
            return None
        except UnicodeDecodeError as e:
            logger.debug(f"UnicodeDecodeError: {e}")
            logger.error(f"Invalid UTF-8 encoding in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Failed to parse workflow {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_file__mutmut_30(self, file_path: Path, use_cache: bool = True) -> Optional[WorkflowMetadata]:
        """Parse a workflow file.

        Args:
            file_path: Path to the workflow YAML file.
            use_cache: Whether to use cached results.

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        if use_cache and file_path in self._cache:
            return self._cache[file_path]

        if not file_path.exists():
            logger.warning(f"Workflow file not found: {file_path}")
            return None

        try:
            with open(file_path, "r", encoding="utf-8") as f:
                content = f.read()

            metadata = self.parse_content(content, file_path)
            if metadata and use_cache:
                self._cache[file_path] = metadata
            return metadata
        except FileNotFoundError as e:
            logger.debug(f"FileNotFoundError: {e}")
            logger.warning(f"FileNotFoundError: {e}", exc_info=True)
            logger.error(None)
            return None
        except PermissionError as e:
            logger.debug(f"PermissionError: {e}")
            logger.warning(f"PermissionError: {e}", exc_info=True)
            logger.error(f"Permission denied reading workflow: {file_path}")
            return None
        except UnicodeDecodeError as e:
            logger.debug(f"UnicodeDecodeError: {e}")
            logger.error(f"Invalid UTF-8 encoding in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Failed to parse workflow {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_file__mutmut_31(self, file_path: Path, use_cache: bool = True) -> Optional[WorkflowMetadata]:
        """Parse a workflow file.

        Args:
            file_path: Path to the workflow YAML file.
            use_cache: Whether to use cached results.

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        if use_cache and file_path in self._cache:
            return self._cache[file_path]

        if not file_path.exists():
            logger.warning(f"Workflow file not found: {file_path}")
            return None

        try:
            with open(file_path, "r", encoding="utf-8") as f:
                content = f.read()

            metadata = self.parse_content(content, file_path)
            if metadata and use_cache:
                self._cache[file_path] = metadata
            return metadata
        except FileNotFoundError as e:
            logger.debug(f"FileNotFoundError: {e}")
            logger.warning(f"FileNotFoundError: {e}", exc_info=True)
            logger.error(f"Workflow file not found: {file_path}")
            return None
        except PermissionError as e:
            logger.debug(None)
            logger.warning(f"PermissionError: {e}", exc_info=True)
            logger.error(f"Permission denied reading workflow: {file_path}")
            return None
        except UnicodeDecodeError as e:
            logger.debug(f"UnicodeDecodeError: {e}")
            logger.error(f"Invalid UTF-8 encoding in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Failed to parse workflow {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_file__mutmut_32(self, file_path: Path, use_cache: bool = True) -> Optional[WorkflowMetadata]:
        """Parse a workflow file.

        Args:
            file_path: Path to the workflow YAML file.
            use_cache: Whether to use cached results.

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        if use_cache and file_path in self._cache:
            return self._cache[file_path]

        if not file_path.exists():
            logger.warning(f"Workflow file not found: {file_path}")
            return None

        try:
            with open(file_path, "r", encoding="utf-8") as f:
                content = f.read()

            metadata = self.parse_content(content, file_path)
            if metadata and use_cache:
                self._cache[file_path] = metadata
            return metadata
        except FileNotFoundError as e:
            logger.debug(f"FileNotFoundError: {e}")
            logger.warning(f"FileNotFoundError: {e}", exc_info=True)
            logger.error(f"Workflow file not found: {file_path}")
            return None
        except PermissionError as e:
            logger.debug(f"PermissionError: {e}")
            logger.warning(None, exc_info=True)
            logger.error(f"Permission denied reading workflow: {file_path}")
            return None
        except UnicodeDecodeError as e:
            logger.debug(f"UnicodeDecodeError: {e}")
            logger.error(f"Invalid UTF-8 encoding in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Failed to parse workflow {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_file__mutmut_33(self, file_path: Path, use_cache: bool = True) -> Optional[WorkflowMetadata]:
        """Parse a workflow file.

        Args:
            file_path: Path to the workflow YAML file.
            use_cache: Whether to use cached results.

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        if use_cache and file_path in self._cache:
            return self._cache[file_path]

        if not file_path.exists():
            logger.warning(f"Workflow file not found: {file_path}")
            return None

        try:
            with open(file_path, "r", encoding="utf-8") as f:
                content = f.read()

            metadata = self.parse_content(content, file_path)
            if metadata and use_cache:
                self._cache[file_path] = metadata
            return metadata
        except FileNotFoundError as e:
            logger.debug(f"FileNotFoundError: {e}")
            logger.warning(f"FileNotFoundError: {e}", exc_info=True)
            logger.error(f"Workflow file not found: {file_path}")
            return None
        except PermissionError as e:
            logger.debug(f"PermissionError: {e}")
            logger.warning(f"PermissionError: {e}", exc_info=None)
            logger.error(f"Permission denied reading workflow: {file_path}")
            return None
        except UnicodeDecodeError as e:
            logger.debug(f"UnicodeDecodeError: {e}")
            logger.error(f"Invalid UTF-8 encoding in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Failed to parse workflow {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_file__mutmut_34(self, file_path: Path, use_cache: bool = True) -> Optional[WorkflowMetadata]:
        """Parse a workflow file.

        Args:
            file_path: Path to the workflow YAML file.
            use_cache: Whether to use cached results.

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        if use_cache and file_path in self._cache:
            return self._cache[file_path]

        if not file_path.exists():
            logger.warning(f"Workflow file not found: {file_path}")
            return None

        try:
            with open(file_path, "r", encoding="utf-8") as f:
                content = f.read()

            metadata = self.parse_content(content, file_path)
            if metadata and use_cache:
                self._cache[file_path] = metadata
            return metadata
        except FileNotFoundError as e:
            logger.debug(f"FileNotFoundError: {e}")
            logger.warning(f"FileNotFoundError: {e}", exc_info=True)
            logger.error(f"Workflow file not found: {file_path}")
            return None
        except PermissionError as e:
            logger.debug(f"PermissionError: {e}")
            logger.warning(exc_info=True)
            logger.error(f"Permission denied reading workflow: {file_path}")
            return None
        except UnicodeDecodeError as e:
            logger.debug(f"UnicodeDecodeError: {e}")
            logger.error(f"Invalid UTF-8 encoding in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Failed to parse workflow {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_file__mutmut_35(self, file_path: Path, use_cache: bool = True) -> Optional[WorkflowMetadata]:
        """Parse a workflow file.

        Args:
            file_path: Path to the workflow YAML file.
            use_cache: Whether to use cached results.

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        if use_cache and file_path in self._cache:
            return self._cache[file_path]

        if not file_path.exists():
            logger.warning(f"Workflow file not found: {file_path}")
            return None

        try:
            with open(file_path, "r", encoding="utf-8") as f:
                content = f.read()

            metadata = self.parse_content(content, file_path)
            if metadata and use_cache:
                self._cache[file_path] = metadata
            return metadata
        except FileNotFoundError as e:
            logger.debug(f"FileNotFoundError: {e}")
            logger.warning(f"FileNotFoundError: {e}", exc_info=True)
            logger.error(f"Workflow file not found: {file_path}")
            return None
        except PermissionError as e:
            logger.debug(f"PermissionError: {e}")
            logger.warning(f"PermissionError: {e}", )
            logger.error(f"Permission denied reading workflow: {file_path}")
            return None
        except UnicodeDecodeError as e:
            logger.debug(f"UnicodeDecodeError: {e}")
            logger.error(f"Invalid UTF-8 encoding in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Failed to parse workflow {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_file__mutmut_36(self, file_path: Path, use_cache: bool = True) -> Optional[WorkflowMetadata]:
        """Parse a workflow file.

        Args:
            file_path: Path to the workflow YAML file.
            use_cache: Whether to use cached results.

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        if use_cache and file_path in self._cache:
            return self._cache[file_path]

        if not file_path.exists():
            logger.warning(f"Workflow file not found: {file_path}")
            return None

        try:
            with open(file_path, "r", encoding="utf-8") as f:
                content = f.read()

            metadata = self.parse_content(content, file_path)
            if metadata and use_cache:
                self._cache[file_path] = metadata
            return metadata
        except FileNotFoundError as e:
            logger.debug(f"FileNotFoundError: {e}")
            logger.warning(f"FileNotFoundError: {e}", exc_info=True)
            logger.error(f"Workflow file not found: {file_path}")
            return None
        except PermissionError as e:
            logger.debug(f"PermissionError: {e}")
            logger.warning(f"PermissionError: {e}", exc_info=False)
            logger.error(f"Permission denied reading workflow: {file_path}")
            return None
        except UnicodeDecodeError as e:
            logger.debug(f"UnicodeDecodeError: {e}")
            logger.error(f"Invalid UTF-8 encoding in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Failed to parse workflow {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_file__mutmut_37(self, file_path: Path, use_cache: bool = True) -> Optional[WorkflowMetadata]:
        """Parse a workflow file.

        Args:
            file_path: Path to the workflow YAML file.
            use_cache: Whether to use cached results.

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        if use_cache and file_path in self._cache:
            return self._cache[file_path]

        if not file_path.exists():
            logger.warning(f"Workflow file not found: {file_path}")
            return None

        try:
            with open(file_path, "r", encoding="utf-8") as f:
                content = f.read()

            metadata = self.parse_content(content, file_path)
            if metadata and use_cache:
                self._cache[file_path] = metadata
            return metadata
        except FileNotFoundError as e:
            logger.debug(f"FileNotFoundError: {e}")
            logger.warning(f"FileNotFoundError: {e}", exc_info=True)
            logger.error(f"Workflow file not found: {file_path}")
            return None
        except PermissionError as e:
            logger.debug(f"PermissionError: {e}")
            logger.warning(f"PermissionError: {e}", exc_info=True)
            logger.error(None)
            return None
        except UnicodeDecodeError as e:
            logger.debug(f"UnicodeDecodeError: {e}")
            logger.error(f"Invalid UTF-8 encoding in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Failed to parse workflow {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_file__mutmut_38(self, file_path: Path, use_cache: bool = True) -> Optional[WorkflowMetadata]:
        """Parse a workflow file.

        Args:
            file_path: Path to the workflow YAML file.
            use_cache: Whether to use cached results.

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        if use_cache and file_path in self._cache:
            return self._cache[file_path]

        if not file_path.exists():
            logger.warning(f"Workflow file not found: {file_path}")
            return None

        try:
            with open(file_path, "r", encoding="utf-8") as f:
                content = f.read()

            metadata = self.parse_content(content, file_path)
            if metadata and use_cache:
                self._cache[file_path] = metadata
            return metadata
        except FileNotFoundError as e:
            logger.debug(f"FileNotFoundError: {e}")
            logger.warning(f"FileNotFoundError: {e}", exc_info=True)
            logger.error(f"Workflow file not found: {file_path}")
            return None
        except PermissionError as e:
            logger.debug(f"PermissionError: {e}")
            logger.warning(f"PermissionError: {e}", exc_info=True)
            logger.error(f"Permission denied reading workflow: {file_path}")
            return None
        except UnicodeDecodeError as e:
            logger.debug(None)
            logger.error(f"Invalid UTF-8 encoding in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Failed to parse workflow {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_file__mutmut_39(self, file_path: Path, use_cache: bool = True) -> Optional[WorkflowMetadata]:
        """Parse a workflow file.

        Args:
            file_path: Path to the workflow YAML file.
            use_cache: Whether to use cached results.

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        if use_cache and file_path in self._cache:
            return self._cache[file_path]

        if not file_path.exists():
            logger.warning(f"Workflow file not found: {file_path}")
            return None

        try:
            with open(file_path, "r", encoding="utf-8") as f:
                content = f.read()

            metadata = self.parse_content(content, file_path)
            if metadata and use_cache:
                self._cache[file_path] = metadata
            return metadata
        except FileNotFoundError as e:
            logger.debug(f"FileNotFoundError: {e}")
            logger.warning(f"FileNotFoundError: {e}", exc_info=True)
            logger.error(f"Workflow file not found: {file_path}")
            return None
        except PermissionError as e:
            logger.debug(f"PermissionError: {e}")
            logger.warning(f"PermissionError: {e}", exc_info=True)
            logger.error(f"Permission denied reading workflow: {file_path}")
            return None
        except UnicodeDecodeError as e:
            logger.debug(f"UnicodeDecodeError: {e}")
            logger.error(None)
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Failed to parse workflow {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_file__mutmut_40(self, file_path: Path, use_cache: bool = True) -> Optional[WorkflowMetadata]:
        """Parse a workflow file.

        Args:
            file_path: Path to the workflow YAML file.
            use_cache: Whether to use cached results.

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        if use_cache and file_path in self._cache:
            return self._cache[file_path]

        if not file_path.exists():
            logger.warning(f"Workflow file not found: {file_path}")
            return None

        try:
            with open(file_path, "r", encoding="utf-8") as f:
                content = f.read()

            metadata = self.parse_content(content, file_path)
            if metadata and use_cache:
                self._cache[file_path] = metadata
            return metadata
        except FileNotFoundError as e:
            logger.debug(f"FileNotFoundError: {e}")
            logger.warning(f"FileNotFoundError: {e}", exc_info=True)
            logger.error(f"Workflow file not found: {file_path}")
            return None
        except PermissionError as e:
            logger.debug(f"PermissionError: {e}")
            logger.warning(f"PermissionError: {e}", exc_info=True)
            logger.error(f"Permission denied reading workflow: {file_path}")
            return None
        except UnicodeDecodeError as e:
            logger.debug(f"UnicodeDecodeError: {e}")
            logger.error(f"Invalid UTF-8 encoding in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(None)
            logger.error(f"Failed to parse workflow {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_file__mutmut_41(self, file_path: Path, use_cache: bool = True) -> Optional[WorkflowMetadata]:
        """Parse a workflow file.

        Args:
            file_path: Path to the workflow YAML file.
            use_cache: Whether to use cached results.

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        if use_cache and file_path in self._cache:
            return self._cache[file_path]

        if not file_path.exists():
            logger.warning(f"Workflow file not found: {file_path}")
            return None

        try:
            with open(file_path, "r", encoding="utf-8") as f:
                content = f.read()

            metadata = self.parse_content(content, file_path)
            if metadata and use_cache:
                self._cache[file_path] = metadata
            return metadata
        except FileNotFoundError as e:
            logger.debug(f"FileNotFoundError: {e}")
            logger.warning(f"FileNotFoundError: {e}", exc_info=True)
            logger.error(f"Workflow file not found: {file_path}")
            return None
        except PermissionError as e:
            logger.debug(f"PermissionError: {e}")
            logger.warning(f"PermissionError: {e}", exc_info=True)
            logger.error(f"Permission denied reading workflow: {file_path}")
            return None
        except UnicodeDecodeError as e:
            logger.debug(f"UnicodeDecodeError: {e}")
            logger.error(f"Invalid UTF-8 encoding in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(None, exc_info=True)
            return None

    def xǁWorkflowParserǁparse_file__mutmut_42(self, file_path: Path, use_cache: bool = True) -> Optional[WorkflowMetadata]:
        """Parse a workflow file.

        Args:
            file_path: Path to the workflow YAML file.
            use_cache: Whether to use cached results.

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        if use_cache and file_path in self._cache:
            return self._cache[file_path]

        if not file_path.exists():
            logger.warning(f"Workflow file not found: {file_path}")
            return None

        try:
            with open(file_path, "r", encoding="utf-8") as f:
                content = f.read()

            metadata = self.parse_content(content, file_path)
            if metadata and use_cache:
                self._cache[file_path] = metadata
            return metadata
        except FileNotFoundError as e:
            logger.debug(f"FileNotFoundError: {e}")
            logger.warning(f"FileNotFoundError: {e}", exc_info=True)
            logger.error(f"Workflow file not found: {file_path}")
            return None
        except PermissionError as e:
            logger.debug(f"PermissionError: {e}")
            logger.warning(f"PermissionError: {e}", exc_info=True)
            logger.error(f"Permission denied reading workflow: {file_path}")
            return None
        except UnicodeDecodeError as e:
            logger.debug(f"UnicodeDecodeError: {e}")
            logger.error(f"Invalid UTF-8 encoding in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Failed to parse workflow {file_path}: {e}", exc_info=None)
            return None

    def xǁWorkflowParserǁparse_file__mutmut_43(self, file_path: Path, use_cache: bool = True) -> Optional[WorkflowMetadata]:
        """Parse a workflow file.

        Args:
            file_path: Path to the workflow YAML file.
            use_cache: Whether to use cached results.

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        if use_cache and file_path in self._cache:
            return self._cache[file_path]

        if not file_path.exists():
            logger.warning(f"Workflow file not found: {file_path}")
            return None

        try:
            with open(file_path, "r", encoding="utf-8") as f:
                content = f.read()

            metadata = self.parse_content(content, file_path)
            if metadata and use_cache:
                self._cache[file_path] = metadata
            return metadata
        except FileNotFoundError as e:
            logger.debug(f"FileNotFoundError: {e}")
            logger.warning(f"FileNotFoundError: {e}", exc_info=True)
            logger.error(f"Workflow file not found: {file_path}")
            return None
        except PermissionError as e:
            logger.debug(f"PermissionError: {e}")
            logger.warning(f"PermissionError: {e}", exc_info=True)
            logger.error(f"Permission denied reading workflow: {file_path}")
            return None
        except UnicodeDecodeError as e:
            logger.debug(f"UnicodeDecodeError: {e}")
            logger.error(f"Invalid UTF-8 encoding in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(exc_info=True)
            return None

    def xǁWorkflowParserǁparse_file__mutmut_44(self, file_path: Path, use_cache: bool = True) -> Optional[WorkflowMetadata]:
        """Parse a workflow file.

        Args:
            file_path: Path to the workflow YAML file.
            use_cache: Whether to use cached results.

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        if use_cache and file_path in self._cache:
            return self._cache[file_path]

        if not file_path.exists():
            logger.warning(f"Workflow file not found: {file_path}")
            return None

        try:
            with open(file_path, "r", encoding="utf-8") as f:
                content = f.read()

            metadata = self.parse_content(content, file_path)
            if metadata and use_cache:
                self._cache[file_path] = metadata
            return metadata
        except FileNotFoundError as e:
            logger.debug(f"FileNotFoundError: {e}")
            logger.warning(f"FileNotFoundError: {e}", exc_info=True)
            logger.error(f"Workflow file not found: {file_path}")
            return None
        except PermissionError as e:
            logger.debug(f"PermissionError: {e}")
            logger.warning(f"PermissionError: {e}", exc_info=True)
            logger.error(f"Permission denied reading workflow: {file_path}")
            return None
        except UnicodeDecodeError as e:
            logger.debug(f"UnicodeDecodeError: {e}")
            logger.error(f"Invalid UTF-8 encoding in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Failed to parse workflow {file_path}: {e}", )
            return None

    def xǁWorkflowParserǁparse_file__mutmut_45(self, file_path: Path, use_cache: bool = True) -> Optional[WorkflowMetadata]:
        """Parse a workflow file.

        Args:
            file_path: Path to the workflow YAML file.
            use_cache: Whether to use cached results.

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        if use_cache and file_path in self._cache:
            return self._cache[file_path]

        if not file_path.exists():
            logger.warning(f"Workflow file not found: {file_path}")
            return None

        try:
            with open(file_path, "r", encoding="utf-8") as f:
                content = f.read()

            metadata = self.parse_content(content, file_path)
            if metadata and use_cache:
                self._cache[file_path] = metadata
            return metadata
        except FileNotFoundError as e:
            logger.debug(f"FileNotFoundError: {e}")
            logger.warning(f"FileNotFoundError: {e}", exc_info=True)
            logger.error(f"Workflow file not found: {file_path}")
            return None
        except PermissionError as e:
            logger.debug(f"PermissionError: {e}")
            logger.warning(f"PermissionError: {e}", exc_info=True)
            logger.error(f"Permission denied reading workflow: {file_path}")
            return None
        except UnicodeDecodeError as e:
            logger.debug(f"UnicodeDecodeError: {e}")
            logger.error(f"Invalid UTF-8 encoding in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Failed to parse workflow {file_path}: {e}", exc_info=False)
            return None
    
    xǁWorkflowParserǁparse_file__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁWorkflowParserǁparse_file__mutmut_1': xǁWorkflowParserǁparse_file__mutmut_1, 
        'xǁWorkflowParserǁparse_file__mutmut_2': xǁWorkflowParserǁparse_file__mutmut_2, 
        'xǁWorkflowParserǁparse_file__mutmut_3': xǁWorkflowParserǁparse_file__mutmut_3, 
        'xǁWorkflowParserǁparse_file__mutmut_4': xǁWorkflowParserǁparse_file__mutmut_4, 
        'xǁWorkflowParserǁparse_file__mutmut_5': xǁWorkflowParserǁparse_file__mutmut_5, 
        'xǁWorkflowParserǁparse_file__mutmut_6': xǁWorkflowParserǁparse_file__mutmut_6, 
        'xǁWorkflowParserǁparse_file__mutmut_7': xǁWorkflowParserǁparse_file__mutmut_7, 
        'xǁWorkflowParserǁparse_file__mutmut_8': xǁWorkflowParserǁparse_file__mutmut_8, 
        'xǁWorkflowParserǁparse_file__mutmut_9': xǁWorkflowParserǁparse_file__mutmut_9, 
        'xǁWorkflowParserǁparse_file__mutmut_10': xǁWorkflowParserǁparse_file__mutmut_10, 
        'xǁWorkflowParserǁparse_file__mutmut_11': xǁWorkflowParserǁparse_file__mutmut_11, 
        'xǁWorkflowParserǁparse_file__mutmut_12': xǁWorkflowParserǁparse_file__mutmut_12, 
        'xǁWorkflowParserǁparse_file__mutmut_13': xǁWorkflowParserǁparse_file__mutmut_13, 
        'xǁWorkflowParserǁparse_file__mutmut_14': xǁWorkflowParserǁparse_file__mutmut_14, 
        'xǁWorkflowParserǁparse_file__mutmut_15': xǁWorkflowParserǁparse_file__mutmut_15, 
        'xǁWorkflowParserǁparse_file__mutmut_16': xǁWorkflowParserǁparse_file__mutmut_16, 
        'xǁWorkflowParserǁparse_file__mutmut_17': xǁWorkflowParserǁparse_file__mutmut_17, 
        'xǁWorkflowParserǁparse_file__mutmut_18': xǁWorkflowParserǁparse_file__mutmut_18, 
        'xǁWorkflowParserǁparse_file__mutmut_19': xǁWorkflowParserǁparse_file__mutmut_19, 
        'xǁWorkflowParserǁparse_file__mutmut_20': xǁWorkflowParserǁparse_file__mutmut_20, 
        'xǁWorkflowParserǁparse_file__mutmut_21': xǁWorkflowParserǁparse_file__mutmut_21, 
        'xǁWorkflowParserǁparse_file__mutmut_22': xǁWorkflowParserǁparse_file__mutmut_22, 
        'xǁWorkflowParserǁparse_file__mutmut_23': xǁWorkflowParserǁparse_file__mutmut_23, 
        'xǁWorkflowParserǁparse_file__mutmut_24': xǁWorkflowParserǁparse_file__mutmut_24, 
        'xǁWorkflowParserǁparse_file__mutmut_25': xǁWorkflowParserǁparse_file__mutmut_25, 
        'xǁWorkflowParserǁparse_file__mutmut_26': xǁWorkflowParserǁparse_file__mutmut_26, 
        'xǁWorkflowParserǁparse_file__mutmut_27': xǁWorkflowParserǁparse_file__mutmut_27, 
        'xǁWorkflowParserǁparse_file__mutmut_28': xǁWorkflowParserǁparse_file__mutmut_28, 
        'xǁWorkflowParserǁparse_file__mutmut_29': xǁWorkflowParserǁparse_file__mutmut_29, 
        'xǁWorkflowParserǁparse_file__mutmut_30': xǁWorkflowParserǁparse_file__mutmut_30, 
        'xǁWorkflowParserǁparse_file__mutmut_31': xǁWorkflowParserǁparse_file__mutmut_31, 
        'xǁWorkflowParserǁparse_file__mutmut_32': xǁWorkflowParserǁparse_file__mutmut_32, 
        'xǁWorkflowParserǁparse_file__mutmut_33': xǁWorkflowParserǁparse_file__mutmut_33, 
        'xǁWorkflowParserǁparse_file__mutmut_34': xǁWorkflowParserǁparse_file__mutmut_34, 
        'xǁWorkflowParserǁparse_file__mutmut_35': xǁWorkflowParserǁparse_file__mutmut_35, 
        'xǁWorkflowParserǁparse_file__mutmut_36': xǁWorkflowParserǁparse_file__mutmut_36, 
        'xǁWorkflowParserǁparse_file__mutmut_37': xǁWorkflowParserǁparse_file__mutmut_37, 
        'xǁWorkflowParserǁparse_file__mutmut_38': xǁWorkflowParserǁparse_file__mutmut_38, 
        'xǁWorkflowParserǁparse_file__mutmut_39': xǁWorkflowParserǁparse_file__mutmut_39, 
        'xǁWorkflowParserǁparse_file__mutmut_40': xǁWorkflowParserǁparse_file__mutmut_40, 
        'xǁWorkflowParserǁparse_file__mutmut_41': xǁWorkflowParserǁparse_file__mutmut_41, 
        'xǁWorkflowParserǁparse_file__mutmut_42': xǁWorkflowParserǁparse_file__mutmut_42, 
        'xǁWorkflowParserǁparse_file__mutmut_43': xǁWorkflowParserǁparse_file__mutmut_43, 
        'xǁWorkflowParserǁparse_file__mutmut_44': xǁWorkflowParserǁparse_file__mutmut_44, 
        'xǁWorkflowParserǁparse_file__mutmut_45': xǁWorkflowParserǁparse_file__mutmut_45
    }
    
    def parse_file(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁWorkflowParserǁparse_file__mutmut_orig"), object.__getattribute__(self, "xǁWorkflowParserǁparse_file__mutmut_mutants"), args, kwargs, self)
        return result 
    
    parse_file.__signature__ = _mutmut_signature(xǁWorkflowParserǁparse_file__mutmut_orig)
    xǁWorkflowParserǁparse_file__mutmut_orig.__name__ = 'xǁWorkflowParserǁparse_file'

    def xǁWorkflowParserǁparse_content__mutmut_orig(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_1(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = None
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_2(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(None)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_3(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data and not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_4(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_5(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_6(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(None)
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_7(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = None

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_8(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get(None, file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_9(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", None)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_10(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get(file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_11(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", )

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_12(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("XXnameXX", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_13(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("NAME", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_14(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = None
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_15(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) and {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_16(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") and data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_17(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get(None) or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_18(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("XXonXX") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_19(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("ON") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_20(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(None) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_21(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(False) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_22(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = None

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_23(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(None)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_24(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = None

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_25(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(None)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_26(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = None

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_27(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(None)

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_28(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get(None, {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_29(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", None))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_30(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get({}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_31(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", ))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_32(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("XXjobsXX", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_33(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("JOBS", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_34(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = None
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_35(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get(None, {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_36(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", None)
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_37(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get({})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_38(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", )
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_39(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("XXpermissionsXX", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_40(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("PERMISSIONS", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_41(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = None

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_42(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"XXdefaultXX": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_43(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"DEFAULT": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_44(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = None
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_45(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get(None, {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_46(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", None)
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_47(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get({})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_48(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", )
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_49(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("XXenvXX", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_50(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("ENV", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_51(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_52(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = None

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_53(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = None

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_54(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get(None)

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_55(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("XXconcurrencyXX")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_56(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("CONCURRENCY")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_57(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = None
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_58(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(None)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_59(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type != TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_60(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = None

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_61(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(None)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_62(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type != TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_63(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = ""
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_64(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = None

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_65(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(None, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_66(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=None)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_67(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_68(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, )

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_69(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=None,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_70(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=None,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_71(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=None,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_72(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=None,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_73(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=None,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_74(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=None,  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_75(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=None,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_76(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=None,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_77(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=None,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_78(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=None,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_79(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=None,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_80(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=None,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_81(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_82(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_83(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_84(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_85(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_86(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_87(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_88(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_89(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_90(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_91(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_92(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_93(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(None)
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_94(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(None)
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_95(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:201]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_96(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(None)
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_97(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(None)
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_98(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(None)
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_99(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(None)
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_100(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(None)
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_101(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(None, exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_102(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=None)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_103(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(exc_info=True)
            return None

    def xǁWorkflowParserǁparse_content__mutmut_104(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", )
            return None

    def xǁWorkflowParserǁparse_content__mutmut_105(self, content: str, file_path: Path) -> Optional[WorkflowMetadata]:
        """Parse workflow YAML content.

        Args:
            content: YAML content as string.
            file_path: Path to the workflow file (for metadata).

        Returns:
            Parsed workflow metadata, or None if parsing failed.
        """
        try:
            # Parse YAML with safe loader (supports anchors/aliases)
            data = yaml.safe_load(content)
            if not data or not isinstance(data, dict):
                logger.warning(f"Invalid workflow structure in {file_path}")
                return None

            # Extract basic metadata
            name = data.get("name", file_path.stem)

            # Parse triggers - 'on' is a YAML boolean keyword, so it might be under True
            on_config = data.get("on") or data.get(True) or {}
            triggers = self._parse_triggers(on_config)

            # Parse inputs (for workflow_dispatch)
            inputs = self._parse_inputs(on_config)

            # Parse jobs
            jobs = self._parse_jobs(data.get("jobs", {}))

            # Extract additional metadata
            permissions = data.get("permissions", {})
            if isinstance(permissions, str):
                permissions = {"default": permissions}

            env = data.get("env", {})
            if not isinstance(env, dict):
                env = {}

            concurrency = data.get("concurrency")

            # Determine workflow capabilities
            is_triggerable = any(t.type == TriggerType.WORKFLOW_DISPATCH for t in triggers)
            is_reusable = any(t.type == TriggerType.WORKFLOW_CALL for t in triggers)

            # Get file modification time
            last_modified = None
            if file_path.exists():
                last_modified = datetime.fromtimestamp(file_path.stat().st_mtime, tz=timezone.utc)

            return WorkflowMetadata(
                name=name,
                file_path=file_path,
                triggers=triggers,
                inputs=inputs,
                jobs=jobs,
                dependencies=[],  # Dependencies calculated separately
                permissions=permissions,
                env=env,
                concurrency=concurrency,
                is_reusable=is_reusable,
                is_triggerable=is_triggerable,
                last_modified=last_modified,
            )
        except yaml.YAMLError as e:
            logger.error(f"YAML parsing error in {file_path}: {e}")
            logger.debug(f"Problematic content near error: {content[:200]}...")
            return None
        except KeyError as e:
            logger.debug(f"KeyError: {e}")
            logger.error(f"Missing required field in {file_path}: {e}")
            return None
        except ValueError as e:
            logger.debug(f"ValueError: {e}")
            logger.error(f"Invalid value in {file_path}: {e}")
            return None
        except Exception as e:
            logger.debug(f"Exception: {e}")
            logger.error(f"Unexpected error parsing {file_path}: {e}", exc_info=False)
            return None
    
    xǁWorkflowParserǁparse_content__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁWorkflowParserǁparse_content__mutmut_1': xǁWorkflowParserǁparse_content__mutmut_1, 
        'xǁWorkflowParserǁparse_content__mutmut_2': xǁWorkflowParserǁparse_content__mutmut_2, 
        'xǁWorkflowParserǁparse_content__mutmut_3': xǁWorkflowParserǁparse_content__mutmut_3, 
        'xǁWorkflowParserǁparse_content__mutmut_4': xǁWorkflowParserǁparse_content__mutmut_4, 
        'xǁWorkflowParserǁparse_content__mutmut_5': xǁWorkflowParserǁparse_content__mutmut_5, 
        'xǁWorkflowParserǁparse_content__mutmut_6': xǁWorkflowParserǁparse_content__mutmut_6, 
        'xǁWorkflowParserǁparse_content__mutmut_7': xǁWorkflowParserǁparse_content__mutmut_7, 
        'xǁWorkflowParserǁparse_content__mutmut_8': xǁWorkflowParserǁparse_content__mutmut_8, 
        'xǁWorkflowParserǁparse_content__mutmut_9': xǁWorkflowParserǁparse_content__mutmut_9, 
        'xǁWorkflowParserǁparse_content__mutmut_10': xǁWorkflowParserǁparse_content__mutmut_10, 
        'xǁWorkflowParserǁparse_content__mutmut_11': xǁWorkflowParserǁparse_content__mutmut_11, 
        'xǁWorkflowParserǁparse_content__mutmut_12': xǁWorkflowParserǁparse_content__mutmut_12, 
        'xǁWorkflowParserǁparse_content__mutmut_13': xǁWorkflowParserǁparse_content__mutmut_13, 
        'xǁWorkflowParserǁparse_content__mutmut_14': xǁWorkflowParserǁparse_content__mutmut_14, 
        'xǁWorkflowParserǁparse_content__mutmut_15': xǁWorkflowParserǁparse_content__mutmut_15, 
        'xǁWorkflowParserǁparse_content__mutmut_16': xǁWorkflowParserǁparse_content__mutmut_16, 
        'xǁWorkflowParserǁparse_content__mutmut_17': xǁWorkflowParserǁparse_content__mutmut_17, 
        'xǁWorkflowParserǁparse_content__mutmut_18': xǁWorkflowParserǁparse_content__mutmut_18, 
        'xǁWorkflowParserǁparse_content__mutmut_19': xǁWorkflowParserǁparse_content__mutmut_19, 
        'xǁWorkflowParserǁparse_content__mutmut_20': xǁWorkflowParserǁparse_content__mutmut_20, 
        'xǁWorkflowParserǁparse_content__mutmut_21': xǁWorkflowParserǁparse_content__mutmut_21, 
        'xǁWorkflowParserǁparse_content__mutmut_22': xǁWorkflowParserǁparse_content__mutmut_22, 
        'xǁWorkflowParserǁparse_content__mutmut_23': xǁWorkflowParserǁparse_content__mutmut_23, 
        'xǁWorkflowParserǁparse_content__mutmut_24': xǁWorkflowParserǁparse_content__mutmut_24, 
        'xǁWorkflowParserǁparse_content__mutmut_25': xǁWorkflowParserǁparse_content__mutmut_25, 
        'xǁWorkflowParserǁparse_content__mutmut_26': xǁWorkflowParserǁparse_content__mutmut_26, 
        'xǁWorkflowParserǁparse_content__mutmut_27': xǁWorkflowParserǁparse_content__mutmut_27, 
        'xǁWorkflowParserǁparse_content__mutmut_28': xǁWorkflowParserǁparse_content__mutmut_28, 
        'xǁWorkflowParserǁparse_content__mutmut_29': xǁWorkflowParserǁparse_content__mutmut_29, 
        'xǁWorkflowParserǁparse_content__mutmut_30': xǁWorkflowParserǁparse_content__mutmut_30, 
        'xǁWorkflowParserǁparse_content__mutmut_31': xǁWorkflowParserǁparse_content__mutmut_31, 
        'xǁWorkflowParserǁparse_content__mutmut_32': xǁWorkflowParserǁparse_content__mutmut_32, 
        'xǁWorkflowParserǁparse_content__mutmut_33': xǁWorkflowParserǁparse_content__mutmut_33, 
        'xǁWorkflowParserǁparse_content__mutmut_34': xǁWorkflowParserǁparse_content__mutmut_34, 
        'xǁWorkflowParserǁparse_content__mutmut_35': xǁWorkflowParserǁparse_content__mutmut_35, 
        'xǁWorkflowParserǁparse_content__mutmut_36': xǁWorkflowParserǁparse_content__mutmut_36, 
        'xǁWorkflowParserǁparse_content__mutmut_37': xǁWorkflowParserǁparse_content__mutmut_37, 
        'xǁWorkflowParserǁparse_content__mutmut_38': xǁWorkflowParserǁparse_content__mutmut_38, 
        'xǁWorkflowParserǁparse_content__mutmut_39': xǁWorkflowParserǁparse_content__mutmut_39, 
        'xǁWorkflowParserǁparse_content__mutmut_40': xǁWorkflowParserǁparse_content__mutmut_40, 
        'xǁWorkflowParserǁparse_content__mutmut_41': xǁWorkflowParserǁparse_content__mutmut_41, 
        'xǁWorkflowParserǁparse_content__mutmut_42': xǁWorkflowParserǁparse_content__mutmut_42, 
        'xǁWorkflowParserǁparse_content__mutmut_43': xǁWorkflowParserǁparse_content__mutmut_43, 
        'xǁWorkflowParserǁparse_content__mutmut_44': xǁWorkflowParserǁparse_content__mutmut_44, 
        'xǁWorkflowParserǁparse_content__mutmut_45': xǁWorkflowParserǁparse_content__mutmut_45, 
        'xǁWorkflowParserǁparse_content__mutmut_46': xǁWorkflowParserǁparse_content__mutmut_46, 
        'xǁWorkflowParserǁparse_content__mutmut_47': xǁWorkflowParserǁparse_content__mutmut_47, 
        'xǁWorkflowParserǁparse_content__mutmut_48': xǁWorkflowParserǁparse_content__mutmut_48, 
        'xǁWorkflowParserǁparse_content__mutmut_49': xǁWorkflowParserǁparse_content__mutmut_49, 
        'xǁWorkflowParserǁparse_content__mutmut_50': xǁWorkflowParserǁparse_content__mutmut_50, 
        'xǁWorkflowParserǁparse_content__mutmut_51': xǁWorkflowParserǁparse_content__mutmut_51, 
        'xǁWorkflowParserǁparse_content__mutmut_52': xǁWorkflowParserǁparse_content__mutmut_52, 
        'xǁWorkflowParserǁparse_content__mutmut_53': xǁWorkflowParserǁparse_content__mutmut_53, 
        'xǁWorkflowParserǁparse_content__mutmut_54': xǁWorkflowParserǁparse_content__mutmut_54, 
        'xǁWorkflowParserǁparse_content__mutmut_55': xǁWorkflowParserǁparse_content__mutmut_55, 
        'xǁWorkflowParserǁparse_content__mutmut_56': xǁWorkflowParserǁparse_content__mutmut_56, 
        'xǁWorkflowParserǁparse_content__mutmut_57': xǁWorkflowParserǁparse_content__mutmut_57, 
        'xǁWorkflowParserǁparse_content__mutmut_58': xǁWorkflowParserǁparse_content__mutmut_58, 
        'xǁWorkflowParserǁparse_content__mutmut_59': xǁWorkflowParserǁparse_content__mutmut_59, 
        'xǁWorkflowParserǁparse_content__mutmut_60': xǁWorkflowParserǁparse_content__mutmut_60, 
        'xǁWorkflowParserǁparse_content__mutmut_61': xǁWorkflowParserǁparse_content__mutmut_61, 
        'xǁWorkflowParserǁparse_content__mutmut_62': xǁWorkflowParserǁparse_content__mutmut_62, 
        'xǁWorkflowParserǁparse_content__mutmut_63': xǁWorkflowParserǁparse_content__mutmut_63, 
        'xǁWorkflowParserǁparse_content__mutmut_64': xǁWorkflowParserǁparse_content__mutmut_64, 
        'xǁWorkflowParserǁparse_content__mutmut_65': xǁWorkflowParserǁparse_content__mutmut_65, 
        'xǁWorkflowParserǁparse_content__mutmut_66': xǁWorkflowParserǁparse_content__mutmut_66, 
        'xǁWorkflowParserǁparse_content__mutmut_67': xǁWorkflowParserǁparse_content__mutmut_67, 
        'xǁWorkflowParserǁparse_content__mutmut_68': xǁWorkflowParserǁparse_content__mutmut_68, 
        'xǁWorkflowParserǁparse_content__mutmut_69': xǁWorkflowParserǁparse_content__mutmut_69, 
        'xǁWorkflowParserǁparse_content__mutmut_70': xǁWorkflowParserǁparse_content__mutmut_70, 
        'xǁWorkflowParserǁparse_content__mutmut_71': xǁWorkflowParserǁparse_content__mutmut_71, 
        'xǁWorkflowParserǁparse_content__mutmut_72': xǁWorkflowParserǁparse_content__mutmut_72, 
        'xǁWorkflowParserǁparse_content__mutmut_73': xǁWorkflowParserǁparse_content__mutmut_73, 
        'xǁWorkflowParserǁparse_content__mutmut_74': xǁWorkflowParserǁparse_content__mutmut_74, 
        'xǁWorkflowParserǁparse_content__mutmut_75': xǁWorkflowParserǁparse_content__mutmut_75, 
        'xǁWorkflowParserǁparse_content__mutmut_76': xǁWorkflowParserǁparse_content__mutmut_76, 
        'xǁWorkflowParserǁparse_content__mutmut_77': xǁWorkflowParserǁparse_content__mutmut_77, 
        'xǁWorkflowParserǁparse_content__mutmut_78': xǁWorkflowParserǁparse_content__mutmut_78, 
        'xǁWorkflowParserǁparse_content__mutmut_79': xǁWorkflowParserǁparse_content__mutmut_79, 
        'xǁWorkflowParserǁparse_content__mutmut_80': xǁWorkflowParserǁparse_content__mutmut_80, 
        'xǁWorkflowParserǁparse_content__mutmut_81': xǁWorkflowParserǁparse_content__mutmut_81, 
        'xǁWorkflowParserǁparse_content__mutmut_82': xǁWorkflowParserǁparse_content__mutmut_82, 
        'xǁWorkflowParserǁparse_content__mutmut_83': xǁWorkflowParserǁparse_content__mutmut_83, 
        'xǁWorkflowParserǁparse_content__mutmut_84': xǁWorkflowParserǁparse_content__mutmut_84, 
        'xǁWorkflowParserǁparse_content__mutmut_85': xǁWorkflowParserǁparse_content__mutmut_85, 
        'xǁWorkflowParserǁparse_content__mutmut_86': xǁWorkflowParserǁparse_content__mutmut_86, 
        'xǁWorkflowParserǁparse_content__mutmut_87': xǁWorkflowParserǁparse_content__mutmut_87, 
        'xǁWorkflowParserǁparse_content__mutmut_88': xǁWorkflowParserǁparse_content__mutmut_88, 
        'xǁWorkflowParserǁparse_content__mutmut_89': xǁWorkflowParserǁparse_content__mutmut_89, 
        'xǁWorkflowParserǁparse_content__mutmut_90': xǁWorkflowParserǁparse_content__mutmut_90, 
        'xǁWorkflowParserǁparse_content__mutmut_91': xǁWorkflowParserǁparse_content__mutmut_91, 
        'xǁWorkflowParserǁparse_content__mutmut_92': xǁWorkflowParserǁparse_content__mutmut_92, 
        'xǁWorkflowParserǁparse_content__mutmut_93': xǁWorkflowParserǁparse_content__mutmut_93, 
        'xǁWorkflowParserǁparse_content__mutmut_94': xǁWorkflowParserǁparse_content__mutmut_94, 
        'xǁWorkflowParserǁparse_content__mutmut_95': xǁWorkflowParserǁparse_content__mutmut_95, 
        'xǁWorkflowParserǁparse_content__mutmut_96': xǁWorkflowParserǁparse_content__mutmut_96, 
        'xǁWorkflowParserǁparse_content__mutmut_97': xǁWorkflowParserǁparse_content__mutmut_97, 
        'xǁWorkflowParserǁparse_content__mutmut_98': xǁWorkflowParserǁparse_content__mutmut_98, 
        'xǁWorkflowParserǁparse_content__mutmut_99': xǁWorkflowParserǁparse_content__mutmut_99, 
        'xǁWorkflowParserǁparse_content__mutmut_100': xǁWorkflowParserǁparse_content__mutmut_100, 
        'xǁWorkflowParserǁparse_content__mutmut_101': xǁWorkflowParserǁparse_content__mutmut_101, 
        'xǁWorkflowParserǁparse_content__mutmut_102': xǁWorkflowParserǁparse_content__mutmut_102, 
        'xǁWorkflowParserǁparse_content__mutmut_103': xǁWorkflowParserǁparse_content__mutmut_103, 
        'xǁWorkflowParserǁparse_content__mutmut_104': xǁWorkflowParserǁparse_content__mutmut_104, 
        'xǁWorkflowParserǁparse_content__mutmut_105': xǁWorkflowParserǁparse_content__mutmut_105
    }
    
    def parse_content(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁWorkflowParserǁparse_content__mutmut_orig"), object.__getattribute__(self, "xǁWorkflowParserǁparse_content__mutmut_mutants"), args, kwargs, self)
        return result 
    
    parse_content.__signature__ = _mutmut_signature(xǁWorkflowParserǁparse_content__mutmut_orig)
    xǁWorkflowParserǁparse_content__mutmut_orig.__name__ = 'xǁWorkflowParserǁparse_content'

    def xǁWorkflowParserǁ_parse_triggers__mutmut_orig(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_1(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = None

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_2(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_3(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = None
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_4(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(None)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_5(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                None
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_6(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=None,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_7(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_8(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_9(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_10(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_11(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_12(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_13(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = None
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_14(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(None)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_15(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    None
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_16(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=None,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_17(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_18(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_19(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_20(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_21(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_22(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_23(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = None

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_24(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(None)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_25(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = ""
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_26(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = ""
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_27(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = ""
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_28(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = ""
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_29(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = ""

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_30(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name != "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_31(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "XXscheduleXX":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_32(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "SCHEDULE":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_33(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = None
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_34(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get(None)
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_35(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("XXcronXX")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_36(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("CRON")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_37(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) or "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_38(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "XXcronXX" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_39(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "CRON" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_40(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" not in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_41(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) or "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_42(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "XXcronXX" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_43(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "CRON" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_44(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" not in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_45(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = None
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_46(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["XXcronXX"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_47(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["CRON"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_48(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = None
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_49(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get(None, [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_50(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", None)
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_51(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get([])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_52(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", )
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_53(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("XXbranchesXX", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_54(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("BRANCHES", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_55(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = None

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_56(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = None
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_57(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get(None, [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_58(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", None)
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_59(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get([])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_60(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", )
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_61(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("XXpathsXX", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_62(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("PATHS", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_63(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = None

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_64(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = None
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_65(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get(None, [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_66(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", None)
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_67(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get([])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_68(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", )
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_69(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("XXtypesXX", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_70(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("TYPES", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_71(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = None

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_72(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name not in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_73(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("XXworkflow_runXX", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_74(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("WORKFLOW_RUN", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_75(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "XXworkflow_callXX"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_76(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "WORKFLOW_CALL"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_77(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = None
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_78(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get(None, [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_79(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", None)
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_80(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get([])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_81(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", )
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_82(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("XXworkflowsXX", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_83(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("WORKFLOWS", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_84(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = None

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_85(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    None
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_86(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=None,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_87(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_88(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_89(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_90(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=None,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_91(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=None,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_92(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_93(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_94(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_95(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        schedule_cron=schedule_cron,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_96(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        workflows=workflows,
                    )
                )

        return triggers

    def xǁWorkflowParserǁ_parse_triggers__mutmut_97(self, on_config: Any) -> list[WorkflowTrigger]:
        """Parse trigger configuration.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            list of parsed triggers.
        """
        triggers: list[WorkflowTrigger] = []

        if not on_config:
            return triggers

        # Handle string format (single trigger)
        if isinstance(on_config, str):
            trigger_type = self._get_trigger_type(on_config)
            triggers.append(
                WorkflowTrigger(
                    type=trigger_type,
                    branches=None,
                    paths=None,
                    types=None,
                    schedule_cron=None,
                    workflows=None,
                )
            )
            return triggers

        # Handle list format
        if isinstance(on_config, list):
            for trigger_name in on_config:
                trigger_type = self._get_trigger_type(trigger_name)
                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=None,
                        paths=None,
                        types=None,
                        schedule_cron=None,
                        workflows=None,
                    )
                )
            return triggers

        # Handle dict format (with filters)
        if isinstance(on_config, dict):
            for trigger_name, config in on_config.items():
                trigger_type = self._get_trigger_type(trigger_name)

                # Parse trigger config
                branches = None
                paths = None
                types = None
                schedule_cron = None
                workflows = None

                # Schedule trigger - handle list of cron schedules
                if trigger_name == "schedule":
                    if isinstance(config, list):
                        schedule_cron = [
                            item.get("cron")
                            for item in config
                            if isinstance(item, dict) and "cron" in item
                        ]
                    elif isinstance(config, dict) and "cron" in config:
                        schedule_cron = [config["cron"]]
                elif isinstance(config, dict):
                    # Parse other trigger configurations
                    branches = config.get("branches", [])
                    if isinstance(branches, str):
                        branches = [branches]

                    paths = config.get("paths", [])
                    if isinstance(paths, str):
                        paths = [paths]

                    types = config.get("types", [])
                    if isinstance(types, str):
                        types = [types]

                    # Workflow dependencies
                    if trigger_name in ("workflow_run", "workflow_call"):
                        workflows = config.get("workflows", [])
                        if isinstance(workflows, str):
                            workflows = [workflows]

                triggers.append(
                    WorkflowTrigger(
                        type=trigger_type,
                        branches=branches if branches else None,
                        paths=paths if paths else None,
                        types=types if types else None,
                        schedule_cron=schedule_cron,
                        )
                )

        return triggers
    
    xǁWorkflowParserǁ_parse_triggers__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁWorkflowParserǁ_parse_triggers__mutmut_1': xǁWorkflowParserǁ_parse_triggers__mutmut_1, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_2': xǁWorkflowParserǁ_parse_triggers__mutmut_2, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_3': xǁWorkflowParserǁ_parse_triggers__mutmut_3, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_4': xǁWorkflowParserǁ_parse_triggers__mutmut_4, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_5': xǁWorkflowParserǁ_parse_triggers__mutmut_5, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_6': xǁWorkflowParserǁ_parse_triggers__mutmut_6, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_7': xǁWorkflowParserǁ_parse_triggers__mutmut_7, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_8': xǁWorkflowParserǁ_parse_triggers__mutmut_8, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_9': xǁWorkflowParserǁ_parse_triggers__mutmut_9, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_10': xǁWorkflowParserǁ_parse_triggers__mutmut_10, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_11': xǁWorkflowParserǁ_parse_triggers__mutmut_11, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_12': xǁWorkflowParserǁ_parse_triggers__mutmut_12, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_13': xǁWorkflowParserǁ_parse_triggers__mutmut_13, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_14': xǁWorkflowParserǁ_parse_triggers__mutmut_14, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_15': xǁWorkflowParserǁ_parse_triggers__mutmut_15, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_16': xǁWorkflowParserǁ_parse_triggers__mutmut_16, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_17': xǁWorkflowParserǁ_parse_triggers__mutmut_17, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_18': xǁWorkflowParserǁ_parse_triggers__mutmut_18, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_19': xǁWorkflowParserǁ_parse_triggers__mutmut_19, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_20': xǁWorkflowParserǁ_parse_triggers__mutmut_20, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_21': xǁWorkflowParserǁ_parse_triggers__mutmut_21, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_22': xǁWorkflowParserǁ_parse_triggers__mutmut_22, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_23': xǁWorkflowParserǁ_parse_triggers__mutmut_23, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_24': xǁWorkflowParserǁ_parse_triggers__mutmut_24, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_25': xǁWorkflowParserǁ_parse_triggers__mutmut_25, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_26': xǁWorkflowParserǁ_parse_triggers__mutmut_26, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_27': xǁWorkflowParserǁ_parse_triggers__mutmut_27, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_28': xǁWorkflowParserǁ_parse_triggers__mutmut_28, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_29': xǁWorkflowParserǁ_parse_triggers__mutmut_29, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_30': xǁWorkflowParserǁ_parse_triggers__mutmut_30, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_31': xǁWorkflowParserǁ_parse_triggers__mutmut_31, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_32': xǁWorkflowParserǁ_parse_triggers__mutmut_32, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_33': xǁWorkflowParserǁ_parse_triggers__mutmut_33, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_34': xǁWorkflowParserǁ_parse_triggers__mutmut_34, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_35': xǁWorkflowParserǁ_parse_triggers__mutmut_35, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_36': xǁWorkflowParserǁ_parse_triggers__mutmut_36, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_37': xǁWorkflowParserǁ_parse_triggers__mutmut_37, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_38': xǁWorkflowParserǁ_parse_triggers__mutmut_38, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_39': xǁWorkflowParserǁ_parse_triggers__mutmut_39, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_40': xǁWorkflowParserǁ_parse_triggers__mutmut_40, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_41': xǁWorkflowParserǁ_parse_triggers__mutmut_41, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_42': xǁWorkflowParserǁ_parse_triggers__mutmut_42, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_43': xǁWorkflowParserǁ_parse_triggers__mutmut_43, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_44': xǁWorkflowParserǁ_parse_triggers__mutmut_44, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_45': xǁWorkflowParserǁ_parse_triggers__mutmut_45, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_46': xǁWorkflowParserǁ_parse_triggers__mutmut_46, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_47': xǁWorkflowParserǁ_parse_triggers__mutmut_47, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_48': xǁWorkflowParserǁ_parse_triggers__mutmut_48, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_49': xǁWorkflowParserǁ_parse_triggers__mutmut_49, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_50': xǁWorkflowParserǁ_parse_triggers__mutmut_50, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_51': xǁWorkflowParserǁ_parse_triggers__mutmut_51, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_52': xǁWorkflowParserǁ_parse_triggers__mutmut_52, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_53': xǁWorkflowParserǁ_parse_triggers__mutmut_53, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_54': xǁWorkflowParserǁ_parse_triggers__mutmut_54, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_55': xǁWorkflowParserǁ_parse_triggers__mutmut_55, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_56': xǁWorkflowParserǁ_parse_triggers__mutmut_56, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_57': xǁWorkflowParserǁ_parse_triggers__mutmut_57, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_58': xǁWorkflowParserǁ_parse_triggers__mutmut_58, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_59': xǁWorkflowParserǁ_parse_triggers__mutmut_59, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_60': xǁWorkflowParserǁ_parse_triggers__mutmut_60, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_61': xǁWorkflowParserǁ_parse_triggers__mutmut_61, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_62': xǁWorkflowParserǁ_parse_triggers__mutmut_62, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_63': xǁWorkflowParserǁ_parse_triggers__mutmut_63, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_64': xǁWorkflowParserǁ_parse_triggers__mutmut_64, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_65': xǁWorkflowParserǁ_parse_triggers__mutmut_65, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_66': xǁWorkflowParserǁ_parse_triggers__mutmut_66, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_67': xǁWorkflowParserǁ_parse_triggers__mutmut_67, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_68': xǁWorkflowParserǁ_parse_triggers__mutmut_68, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_69': xǁWorkflowParserǁ_parse_triggers__mutmut_69, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_70': xǁWorkflowParserǁ_parse_triggers__mutmut_70, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_71': xǁWorkflowParserǁ_parse_triggers__mutmut_71, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_72': xǁWorkflowParserǁ_parse_triggers__mutmut_72, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_73': xǁWorkflowParserǁ_parse_triggers__mutmut_73, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_74': xǁWorkflowParserǁ_parse_triggers__mutmut_74, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_75': xǁWorkflowParserǁ_parse_triggers__mutmut_75, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_76': xǁWorkflowParserǁ_parse_triggers__mutmut_76, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_77': xǁWorkflowParserǁ_parse_triggers__mutmut_77, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_78': xǁWorkflowParserǁ_parse_triggers__mutmut_78, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_79': xǁWorkflowParserǁ_parse_triggers__mutmut_79, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_80': xǁWorkflowParserǁ_parse_triggers__mutmut_80, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_81': xǁWorkflowParserǁ_parse_triggers__mutmut_81, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_82': xǁWorkflowParserǁ_parse_triggers__mutmut_82, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_83': xǁWorkflowParserǁ_parse_triggers__mutmut_83, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_84': xǁWorkflowParserǁ_parse_triggers__mutmut_84, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_85': xǁWorkflowParserǁ_parse_triggers__mutmut_85, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_86': xǁWorkflowParserǁ_parse_triggers__mutmut_86, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_87': xǁWorkflowParserǁ_parse_triggers__mutmut_87, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_88': xǁWorkflowParserǁ_parse_triggers__mutmut_88, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_89': xǁWorkflowParserǁ_parse_triggers__mutmut_89, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_90': xǁWorkflowParserǁ_parse_triggers__mutmut_90, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_91': xǁWorkflowParserǁ_parse_triggers__mutmut_91, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_92': xǁWorkflowParserǁ_parse_triggers__mutmut_92, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_93': xǁWorkflowParserǁ_parse_triggers__mutmut_93, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_94': xǁWorkflowParserǁ_parse_triggers__mutmut_94, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_95': xǁWorkflowParserǁ_parse_triggers__mutmut_95, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_96': xǁWorkflowParserǁ_parse_triggers__mutmut_96, 
        'xǁWorkflowParserǁ_parse_triggers__mutmut_97': xǁWorkflowParserǁ_parse_triggers__mutmut_97
    }
    
    def _parse_triggers(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁWorkflowParserǁ_parse_triggers__mutmut_orig"), object.__getattribute__(self, "xǁWorkflowParserǁ_parse_triggers__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _parse_triggers.__signature__ = _mutmut_signature(xǁWorkflowParserǁ_parse_triggers__mutmut_orig)
    xǁWorkflowParserǁ_parse_triggers__mutmut_orig.__name__ = 'xǁWorkflowParserǁ_parse_triggers'

    def xǁWorkflowParserǁ_parse_inputs__mutmut_orig(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_1(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = None

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_2(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_3(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = None
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_4(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get(None, {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_5(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", None)
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_6(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get({})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_7(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", )
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_8(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("XXworkflow_dispatchXX", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_9(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("WORKFLOW_DISPATCH", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_10(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_11(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = None
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_12(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get(None, {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_13(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", None)
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_14(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get({})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_15(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", )
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_16(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("XXinputsXX", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_17(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("INPUTS", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_18(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_19(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_20(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                break

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_21(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = None
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_22(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").upper()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_23(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get(None, "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_24(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", None).lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_25(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_26(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", ).lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_27(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("XXtypeXX", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_28(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("TYPE", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_29(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "XXstringXX").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_30(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "STRING").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_31(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = None
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_32(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(None)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_33(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(None)
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_34(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(None, exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_35(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=None)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_36(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_37(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", )
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_38(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=False)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_39(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = None

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_40(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = None
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_41(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get(None)
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_42(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("XXoptionsXX")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_43(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("OPTIONS")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_44(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options or not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_45(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_46(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = ""

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_47(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = None

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_48(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get(None)

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_49(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("XXdefaultXX")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_50(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("DEFAULT")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_51(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = None

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_52(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=None,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_53(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=None,
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_54(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=None,
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_55(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=None,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_56(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=None,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_57(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=None,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_58(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_59(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_60(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_61(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_62(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_63(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_64(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get(None),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_65(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("XXdescriptionXX"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_66(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("DESCRIPTION"),
                required=input_config.get("required", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_67(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get(None, False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_68(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", None),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_69(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get(False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_70(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", ),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_71(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("XXrequiredXX", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_72(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("REQUIRED", False),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs

    def xǁWorkflowParserǁ_parse_inputs__mutmut_73(self, on_config: Any) -> dict[str, WorkflowInput]:
        """Parse workflow_dispatch inputs.

        Args:
            on_config: The 'on' section of the workflow.

        Returns:
            Dictionary of input name to WorkflowInput.
        """
        inputs: dict[str, WorkflowInput] = {}

        if not isinstance(on_config, dict):
            return inputs

        workflow_dispatch = on_config.get("workflow_dispatch", {})
        if not isinstance(workflow_dispatch, dict):
            return inputs

        input_defs = workflow_dispatch.get("inputs", {})
        if not isinstance(input_defs, dict):
            return inputs

        for input_name, input_config in input_defs.items():
            if not isinstance(input_config, dict):
                continue

            # Parse input type
            input_type_str = input_config.get("type", "string").lower()
            try:
                input_type = InputType(input_type_str)
            except ValueError as e:
                logger.debug(f"ValueError: {e}")
                logger.warning(f"ValueError: {e}", exc_info=True)
                input_type = InputType.STRING

            # Parse options (for choice type)
            options = input_config.get("options")
            if options and not isinstance(options, list):
                options = None

            # Parse default value
            default = input_config.get("default")

            inputs[input_name] = WorkflowInput(
                name=input_name,
                description=input_config.get("description"),
                required=input_config.get("required", True),
                type=input_type,
                default=default,
                options=options,
            )

        return inputs
    
    xǁWorkflowParserǁ_parse_inputs__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁWorkflowParserǁ_parse_inputs__mutmut_1': xǁWorkflowParserǁ_parse_inputs__mutmut_1, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_2': xǁWorkflowParserǁ_parse_inputs__mutmut_2, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_3': xǁWorkflowParserǁ_parse_inputs__mutmut_3, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_4': xǁWorkflowParserǁ_parse_inputs__mutmut_4, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_5': xǁWorkflowParserǁ_parse_inputs__mutmut_5, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_6': xǁWorkflowParserǁ_parse_inputs__mutmut_6, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_7': xǁWorkflowParserǁ_parse_inputs__mutmut_7, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_8': xǁWorkflowParserǁ_parse_inputs__mutmut_8, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_9': xǁWorkflowParserǁ_parse_inputs__mutmut_9, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_10': xǁWorkflowParserǁ_parse_inputs__mutmut_10, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_11': xǁWorkflowParserǁ_parse_inputs__mutmut_11, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_12': xǁWorkflowParserǁ_parse_inputs__mutmut_12, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_13': xǁWorkflowParserǁ_parse_inputs__mutmut_13, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_14': xǁWorkflowParserǁ_parse_inputs__mutmut_14, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_15': xǁWorkflowParserǁ_parse_inputs__mutmut_15, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_16': xǁWorkflowParserǁ_parse_inputs__mutmut_16, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_17': xǁWorkflowParserǁ_parse_inputs__mutmut_17, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_18': xǁWorkflowParserǁ_parse_inputs__mutmut_18, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_19': xǁWorkflowParserǁ_parse_inputs__mutmut_19, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_20': xǁWorkflowParserǁ_parse_inputs__mutmut_20, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_21': xǁWorkflowParserǁ_parse_inputs__mutmut_21, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_22': xǁWorkflowParserǁ_parse_inputs__mutmut_22, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_23': xǁWorkflowParserǁ_parse_inputs__mutmut_23, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_24': xǁWorkflowParserǁ_parse_inputs__mutmut_24, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_25': xǁWorkflowParserǁ_parse_inputs__mutmut_25, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_26': xǁWorkflowParserǁ_parse_inputs__mutmut_26, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_27': xǁWorkflowParserǁ_parse_inputs__mutmut_27, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_28': xǁWorkflowParserǁ_parse_inputs__mutmut_28, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_29': xǁWorkflowParserǁ_parse_inputs__mutmut_29, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_30': xǁWorkflowParserǁ_parse_inputs__mutmut_30, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_31': xǁWorkflowParserǁ_parse_inputs__mutmut_31, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_32': xǁWorkflowParserǁ_parse_inputs__mutmut_32, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_33': xǁWorkflowParserǁ_parse_inputs__mutmut_33, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_34': xǁWorkflowParserǁ_parse_inputs__mutmut_34, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_35': xǁWorkflowParserǁ_parse_inputs__mutmut_35, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_36': xǁWorkflowParserǁ_parse_inputs__mutmut_36, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_37': xǁWorkflowParserǁ_parse_inputs__mutmut_37, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_38': xǁWorkflowParserǁ_parse_inputs__mutmut_38, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_39': xǁWorkflowParserǁ_parse_inputs__mutmut_39, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_40': xǁWorkflowParserǁ_parse_inputs__mutmut_40, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_41': xǁWorkflowParserǁ_parse_inputs__mutmut_41, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_42': xǁWorkflowParserǁ_parse_inputs__mutmut_42, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_43': xǁWorkflowParserǁ_parse_inputs__mutmut_43, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_44': xǁWorkflowParserǁ_parse_inputs__mutmut_44, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_45': xǁWorkflowParserǁ_parse_inputs__mutmut_45, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_46': xǁWorkflowParserǁ_parse_inputs__mutmut_46, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_47': xǁWorkflowParserǁ_parse_inputs__mutmut_47, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_48': xǁWorkflowParserǁ_parse_inputs__mutmut_48, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_49': xǁWorkflowParserǁ_parse_inputs__mutmut_49, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_50': xǁWorkflowParserǁ_parse_inputs__mutmut_50, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_51': xǁWorkflowParserǁ_parse_inputs__mutmut_51, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_52': xǁWorkflowParserǁ_parse_inputs__mutmut_52, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_53': xǁWorkflowParserǁ_parse_inputs__mutmut_53, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_54': xǁWorkflowParserǁ_parse_inputs__mutmut_54, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_55': xǁWorkflowParserǁ_parse_inputs__mutmut_55, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_56': xǁWorkflowParserǁ_parse_inputs__mutmut_56, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_57': xǁWorkflowParserǁ_parse_inputs__mutmut_57, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_58': xǁWorkflowParserǁ_parse_inputs__mutmut_58, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_59': xǁWorkflowParserǁ_parse_inputs__mutmut_59, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_60': xǁWorkflowParserǁ_parse_inputs__mutmut_60, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_61': xǁWorkflowParserǁ_parse_inputs__mutmut_61, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_62': xǁWorkflowParserǁ_parse_inputs__mutmut_62, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_63': xǁWorkflowParserǁ_parse_inputs__mutmut_63, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_64': xǁWorkflowParserǁ_parse_inputs__mutmut_64, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_65': xǁWorkflowParserǁ_parse_inputs__mutmut_65, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_66': xǁWorkflowParserǁ_parse_inputs__mutmut_66, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_67': xǁWorkflowParserǁ_parse_inputs__mutmut_67, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_68': xǁWorkflowParserǁ_parse_inputs__mutmut_68, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_69': xǁWorkflowParserǁ_parse_inputs__mutmut_69, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_70': xǁWorkflowParserǁ_parse_inputs__mutmut_70, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_71': xǁWorkflowParserǁ_parse_inputs__mutmut_71, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_72': xǁWorkflowParserǁ_parse_inputs__mutmut_72, 
        'xǁWorkflowParserǁ_parse_inputs__mutmut_73': xǁWorkflowParserǁ_parse_inputs__mutmut_73
    }
    
    def _parse_inputs(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁWorkflowParserǁ_parse_inputs__mutmut_orig"), object.__getattribute__(self, "xǁWorkflowParserǁ_parse_inputs__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _parse_inputs.__signature__ = _mutmut_signature(xǁWorkflowParserǁ_parse_inputs__mutmut_orig)
    xǁWorkflowParserǁ_parse_inputs__mutmut_orig.__name__ = 'xǁWorkflowParserǁ_parse_inputs'

    def xǁWorkflowParserǁ_parse_jobs__mutmut_orig(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_1(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = None

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_2(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_3(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_4(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                break

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_5(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = None

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_6(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get(None, "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_7(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", None)

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_8(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_9(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", )

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_10(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("XXruns-onXX", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_11(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("RUNS-ON", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_12(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "XXubuntu-latestXX")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_13(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "UBUNTU-LATEST")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_14(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = None
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_15(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get(None)
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_16(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("XXneedsXX")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_17(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("NEEDS")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_18(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = None
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_19(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_20(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = ""

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_21(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = None
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_22(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get(None, [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_23(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", None)
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_24(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get([])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_25(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", )
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_26(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("XXstepsXX", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_27(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("STEPS", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_28(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = None

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_29(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 1

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_30(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = None

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_31(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get(None)

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_32(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("XXtimeout-minutesXX")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_33(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("TIMEOUT-MINUTES")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_34(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = None

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_35(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get(None)

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_36(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("XXusesXX")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_37(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("USES")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_38(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = None

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_39(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=None,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_40(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=None,
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_41(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=None,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_42(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=None,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_43(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=None,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_44(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=None,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_45(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=None,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_46(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_47(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_48(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_49(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_50(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_51(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_52(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_53(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_54(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get(None, job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_55(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", None),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_56(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get(job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_57(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", ),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_58(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("XXnameXX", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_59(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("NAME", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_60(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"XXifXX": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_61(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"IF": job_config.get("if")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_62(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get(None)},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_63(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("XXifXX")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs

    def xǁWorkflowParserǁ_parse_jobs__mutmut_64(self, jobs_config: dict[str, Any]) -> dict[str, WorkflowJob]:
        """Parse job definitions.

        Args:
            jobs_config: The 'jobs' section of the workflow.

        Returns:
            Dictionary of job ID to WorkflowJob.
        """
        jobs = {}

        if not isinstance(jobs_config, dict):
            return jobs

        for job_id, job_config in jobs_config.items():
            if not isinstance(job_config, dict):
                continue

            # Parse runs-on
            runs_on = job_config.get("runs-on", "ubuntu-latest")

            # Parse needs (job dependencies)
            needs = job_config.get("needs")
            if needs:
                if isinstance(needs, str):
                    needs = [needs]
                elif not isinstance(needs, list):
                    needs = None

            # Count steps
            steps = job_config.get("steps", [])
            step_count = len(steps) if isinstance(steps, list) else 0

            # Parse timeout
            timeout_minutes = job_config.get("timeout-minutes")

            # Check if it's a reusable workflow call
            uses = job_config.get("uses")

            jobs[job_id] = WorkflowJob(
                id=job_id,
                name=job_config.get("name", job_id),
                runs_on=runs_on,
                needs=needs,
                **{"if": job_config.get("IF")},  # Use alias directly
                steps=step_count,
                timeout_minutes=timeout_minutes,
                uses=uses,
            )

        return jobs
    
    xǁWorkflowParserǁ_parse_jobs__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁWorkflowParserǁ_parse_jobs__mutmut_1': xǁWorkflowParserǁ_parse_jobs__mutmut_1, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_2': xǁWorkflowParserǁ_parse_jobs__mutmut_2, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_3': xǁWorkflowParserǁ_parse_jobs__mutmut_3, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_4': xǁWorkflowParserǁ_parse_jobs__mutmut_4, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_5': xǁWorkflowParserǁ_parse_jobs__mutmut_5, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_6': xǁWorkflowParserǁ_parse_jobs__mutmut_6, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_7': xǁWorkflowParserǁ_parse_jobs__mutmut_7, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_8': xǁWorkflowParserǁ_parse_jobs__mutmut_8, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_9': xǁWorkflowParserǁ_parse_jobs__mutmut_9, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_10': xǁWorkflowParserǁ_parse_jobs__mutmut_10, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_11': xǁWorkflowParserǁ_parse_jobs__mutmut_11, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_12': xǁWorkflowParserǁ_parse_jobs__mutmut_12, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_13': xǁWorkflowParserǁ_parse_jobs__mutmut_13, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_14': xǁWorkflowParserǁ_parse_jobs__mutmut_14, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_15': xǁWorkflowParserǁ_parse_jobs__mutmut_15, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_16': xǁWorkflowParserǁ_parse_jobs__mutmut_16, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_17': xǁWorkflowParserǁ_parse_jobs__mutmut_17, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_18': xǁWorkflowParserǁ_parse_jobs__mutmut_18, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_19': xǁWorkflowParserǁ_parse_jobs__mutmut_19, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_20': xǁWorkflowParserǁ_parse_jobs__mutmut_20, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_21': xǁWorkflowParserǁ_parse_jobs__mutmut_21, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_22': xǁWorkflowParserǁ_parse_jobs__mutmut_22, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_23': xǁWorkflowParserǁ_parse_jobs__mutmut_23, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_24': xǁWorkflowParserǁ_parse_jobs__mutmut_24, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_25': xǁWorkflowParserǁ_parse_jobs__mutmut_25, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_26': xǁWorkflowParserǁ_parse_jobs__mutmut_26, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_27': xǁWorkflowParserǁ_parse_jobs__mutmut_27, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_28': xǁWorkflowParserǁ_parse_jobs__mutmut_28, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_29': xǁWorkflowParserǁ_parse_jobs__mutmut_29, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_30': xǁWorkflowParserǁ_parse_jobs__mutmut_30, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_31': xǁWorkflowParserǁ_parse_jobs__mutmut_31, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_32': xǁWorkflowParserǁ_parse_jobs__mutmut_32, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_33': xǁWorkflowParserǁ_parse_jobs__mutmut_33, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_34': xǁWorkflowParserǁ_parse_jobs__mutmut_34, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_35': xǁWorkflowParserǁ_parse_jobs__mutmut_35, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_36': xǁWorkflowParserǁ_parse_jobs__mutmut_36, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_37': xǁWorkflowParserǁ_parse_jobs__mutmut_37, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_38': xǁWorkflowParserǁ_parse_jobs__mutmut_38, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_39': xǁWorkflowParserǁ_parse_jobs__mutmut_39, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_40': xǁWorkflowParserǁ_parse_jobs__mutmut_40, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_41': xǁWorkflowParserǁ_parse_jobs__mutmut_41, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_42': xǁWorkflowParserǁ_parse_jobs__mutmut_42, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_43': xǁWorkflowParserǁ_parse_jobs__mutmut_43, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_44': xǁWorkflowParserǁ_parse_jobs__mutmut_44, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_45': xǁWorkflowParserǁ_parse_jobs__mutmut_45, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_46': xǁWorkflowParserǁ_parse_jobs__mutmut_46, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_47': xǁWorkflowParserǁ_parse_jobs__mutmut_47, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_48': xǁWorkflowParserǁ_parse_jobs__mutmut_48, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_49': xǁWorkflowParserǁ_parse_jobs__mutmut_49, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_50': xǁWorkflowParserǁ_parse_jobs__mutmut_50, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_51': xǁWorkflowParserǁ_parse_jobs__mutmut_51, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_52': xǁWorkflowParserǁ_parse_jobs__mutmut_52, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_53': xǁWorkflowParserǁ_parse_jobs__mutmut_53, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_54': xǁWorkflowParserǁ_parse_jobs__mutmut_54, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_55': xǁWorkflowParserǁ_parse_jobs__mutmut_55, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_56': xǁWorkflowParserǁ_parse_jobs__mutmut_56, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_57': xǁWorkflowParserǁ_parse_jobs__mutmut_57, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_58': xǁWorkflowParserǁ_parse_jobs__mutmut_58, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_59': xǁWorkflowParserǁ_parse_jobs__mutmut_59, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_60': xǁWorkflowParserǁ_parse_jobs__mutmut_60, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_61': xǁWorkflowParserǁ_parse_jobs__mutmut_61, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_62': xǁWorkflowParserǁ_parse_jobs__mutmut_62, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_63': xǁWorkflowParserǁ_parse_jobs__mutmut_63, 
        'xǁWorkflowParserǁ_parse_jobs__mutmut_64': xǁWorkflowParserǁ_parse_jobs__mutmut_64
    }
    
    def _parse_jobs(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁWorkflowParserǁ_parse_jobs__mutmut_orig"), object.__getattribute__(self, "xǁWorkflowParserǁ_parse_jobs__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _parse_jobs.__signature__ = _mutmut_signature(xǁWorkflowParserǁ_parse_jobs__mutmut_orig)
    xǁWorkflowParserǁ_parse_jobs__mutmut_orig.__name__ = 'xǁWorkflowParserǁ_parse_jobs'

    def xǁWorkflowParserǁ_get_trigger_type__mutmut_orig(self, trigger_name: str) -> TriggerType:
        """Map trigger name to TriggerType enum.

        Args:
            trigger_name: Trigger name from YAML.

        Returns:
            Corresponding TriggerType.
        """
        trigger_map = {
            "workflow_dispatch": TriggerType.WORKFLOW_DISPATCH,
            "push": TriggerType.PUSH,
            "pull_request": TriggerType.PULL_REQUEST,
            "schedule": TriggerType.SCHEDULE,
            "workflow_call": TriggerType.WORKFLOW_CALL,
            "workflow_run": TriggerType.WORKFLOW_RUN,
            "repository_dispatch": TriggerType.REPOSITORY_DISPATCH,
            "release": TriggerType.RELEASE,
            "create": TriggerType.CREATE,
            "delete": TriggerType.DELETE,
            "fork": TriggerType.FORK,
            "issues": TriggerType.ISSUES,
            "issue_comment": TriggerType.ISSUE_COMMENT,
            "pull_request_target": TriggerType.PULL_REQUEST_TARGET,
            "pull_request_review": TriggerType.PULL_REQUEST_REVIEW,
            "pull_request_review_comment": TriggerType.PULL_REQUEST_REVIEW_COMMENT,
            "registry_package": TriggerType.REGISTRY_PACKAGE,
            "watch": TriggerType.WATCH,
        }
        return trigger_map.get(trigger_name, TriggerType.OTHER)

    def xǁWorkflowParserǁ_get_trigger_type__mutmut_1(self, trigger_name: str) -> TriggerType:
        """Map trigger name to TriggerType enum.

        Args:
            trigger_name: Trigger name from YAML.

        Returns:
            Corresponding TriggerType.
        """
        trigger_map = None
        return trigger_map.get(trigger_name, TriggerType.OTHER)

    def xǁWorkflowParserǁ_get_trigger_type__mutmut_2(self, trigger_name: str) -> TriggerType:
        """Map trigger name to TriggerType enum.

        Args:
            trigger_name: Trigger name from YAML.

        Returns:
            Corresponding TriggerType.
        """
        trigger_map = {
            "XXworkflow_dispatchXX": TriggerType.WORKFLOW_DISPATCH,
            "push": TriggerType.PUSH,
            "pull_request": TriggerType.PULL_REQUEST,
            "schedule": TriggerType.SCHEDULE,
            "workflow_call": TriggerType.WORKFLOW_CALL,
            "workflow_run": TriggerType.WORKFLOW_RUN,
            "repository_dispatch": TriggerType.REPOSITORY_DISPATCH,
            "release": TriggerType.RELEASE,
            "create": TriggerType.CREATE,
            "delete": TriggerType.DELETE,
            "fork": TriggerType.FORK,
            "issues": TriggerType.ISSUES,
            "issue_comment": TriggerType.ISSUE_COMMENT,
            "pull_request_target": TriggerType.PULL_REQUEST_TARGET,
            "pull_request_review": TriggerType.PULL_REQUEST_REVIEW,
            "pull_request_review_comment": TriggerType.PULL_REQUEST_REVIEW_COMMENT,
            "registry_package": TriggerType.REGISTRY_PACKAGE,
            "watch": TriggerType.WATCH,
        }
        return trigger_map.get(trigger_name, TriggerType.OTHER)

    def xǁWorkflowParserǁ_get_trigger_type__mutmut_3(self, trigger_name: str) -> TriggerType:
        """Map trigger name to TriggerType enum.

        Args:
            trigger_name: Trigger name from YAML.

        Returns:
            Corresponding TriggerType.
        """
        trigger_map = {
            "WORKFLOW_DISPATCH": TriggerType.WORKFLOW_DISPATCH,
            "push": TriggerType.PUSH,
            "pull_request": TriggerType.PULL_REQUEST,
            "schedule": TriggerType.SCHEDULE,
            "workflow_call": TriggerType.WORKFLOW_CALL,
            "workflow_run": TriggerType.WORKFLOW_RUN,
            "repository_dispatch": TriggerType.REPOSITORY_DISPATCH,
            "release": TriggerType.RELEASE,
            "create": TriggerType.CREATE,
            "delete": TriggerType.DELETE,
            "fork": TriggerType.FORK,
            "issues": TriggerType.ISSUES,
            "issue_comment": TriggerType.ISSUE_COMMENT,
            "pull_request_target": TriggerType.PULL_REQUEST_TARGET,
            "pull_request_review": TriggerType.PULL_REQUEST_REVIEW,
            "pull_request_review_comment": TriggerType.PULL_REQUEST_REVIEW_COMMENT,
            "registry_package": TriggerType.REGISTRY_PACKAGE,
            "watch": TriggerType.WATCH,
        }
        return trigger_map.get(trigger_name, TriggerType.OTHER)

    def xǁWorkflowParserǁ_get_trigger_type__mutmut_4(self, trigger_name: str) -> TriggerType:
        """Map trigger name to TriggerType enum.

        Args:
            trigger_name: Trigger name from YAML.

        Returns:
            Corresponding TriggerType.
        """
        trigger_map = {
            "workflow_dispatch": TriggerType.WORKFLOW_DISPATCH,
            "XXpushXX": TriggerType.PUSH,
            "pull_request": TriggerType.PULL_REQUEST,
            "schedule": TriggerType.SCHEDULE,
            "workflow_call": TriggerType.WORKFLOW_CALL,
            "workflow_run": TriggerType.WORKFLOW_RUN,
            "repository_dispatch": TriggerType.REPOSITORY_DISPATCH,
            "release": TriggerType.RELEASE,
            "create": TriggerType.CREATE,
            "delete": TriggerType.DELETE,
            "fork": TriggerType.FORK,
            "issues": TriggerType.ISSUES,
            "issue_comment": TriggerType.ISSUE_COMMENT,
            "pull_request_target": TriggerType.PULL_REQUEST_TARGET,
            "pull_request_review": TriggerType.PULL_REQUEST_REVIEW,
            "pull_request_review_comment": TriggerType.PULL_REQUEST_REVIEW_COMMENT,
            "registry_package": TriggerType.REGISTRY_PACKAGE,
            "watch": TriggerType.WATCH,
        }
        return trigger_map.get(trigger_name, TriggerType.OTHER)

    def xǁWorkflowParserǁ_get_trigger_type__mutmut_5(self, trigger_name: str) -> TriggerType:
        """Map trigger name to TriggerType enum.

        Args:
            trigger_name: Trigger name from YAML.

        Returns:
            Corresponding TriggerType.
        """
        trigger_map = {
            "workflow_dispatch": TriggerType.WORKFLOW_DISPATCH,
            "PUSH": TriggerType.PUSH,
            "pull_request": TriggerType.PULL_REQUEST,
            "schedule": TriggerType.SCHEDULE,
            "workflow_call": TriggerType.WORKFLOW_CALL,
            "workflow_run": TriggerType.WORKFLOW_RUN,
            "repository_dispatch": TriggerType.REPOSITORY_DISPATCH,
            "release": TriggerType.RELEASE,
            "create": TriggerType.CREATE,
            "delete": TriggerType.DELETE,
            "fork": TriggerType.FORK,
            "issues": TriggerType.ISSUES,
            "issue_comment": TriggerType.ISSUE_COMMENT,
            "pull_request_target": TriggerType.PULL_REQUEST_TARGET,
            "pull_request_review": TriggerType.PULL_REQUEST_REVIEW,
            "pull_request_review_comment": TriggerType.PULL_REQUEST_REVIEW_COMMENT,
            "registry_package": TriggerType.REGISTRY_PACKAGE,
            "watch": TriggerType.WATCH,
        }
        return trigger_map.get(trigger_name, TriggerType.OTHER)

    def xǁWorkflowParserǁ_get_trigger_type__mutmut_6(self, trigger_name: str) -> TriggerType:
        """Map trigger name to TriggerType enum.

        Args:
            trigger_name: Trigger name from YAML.

        Returns:
            Corresponding TriggerType.
        """
        trigger_map = {
            "workflow_dispatch": TriggerType.WORKFLOW_DISPATCH,
            "push": TriggerType.PUSH,
            "XXpull_requestXX": TriggerType.PULL_REQUEST,
            "schedule": TriggerType.SCHEDULE,
            "workflow_call": TriggerType.WORKFLOW_CALL,
            "workflow_run": TriggerType.WORKFLOW_RUN,
            "repository_dispatch": TriggerType.REPOSITORY_DISPATCH,
            "release": TriggerType.RELEASE,
            "create": TriggerType.CREATE,
            "delete": TriggerType.DELETE,
            "fork": TriggerType.FORK,
            "issues": TriggerType.ISSUES,
            "issue_comment": TriggerType.ISSUE_COMMENT,
            "pull_request_target": TriggerType.PULL_REQUEST_TARGET,
            "pull_request_review": TriggerType.PULL_REQUEST_REVIEW,
            "pull_request_review_comment": TriggerType.PULL_REQUEST_REVIEW_COMMENT,
            "registry_package": TriggerType.REGISTRY_PACKAGE,
            "watch": TriggerType.WATCH,
        }
        return trigger_map.get(trigger_name, TriggerType.OTHER)

    def xǁWorkflowParserǁ_get_trigger_type__mutmut_7(self, trigger_name: str) -> TriggerType:
        """Map trigger name to TriggerType enum.

        Args:
            trigger_name: Trigger name from YAML.

        Returns:
            Corresponding TriggerType.
        """
        trigger_map = {
            "workflow_dispatch": TriggerType.WORKFLOW_DISPATCH,
            "push": TriggerType.PUSH,
            "PULL_REQUEST": TriggerType.PULL_REQUEST,
            "schedule": TriggerType.SCHEDULE,
            "workflow_call": TriggerType.WORKFLOW_CALL,
            "workflow_run": TriggerType.WORKFLOW_RUN,
            "repository_dispatch": TriggerType.REPOSITORY_DISPATCH,
            "release": TriggerType.RELEASE,
            "create": TriggerType.CREATE,
            "delete": TriggerType.DELETE,
            "fork": TriggerType.FORK,
            "issues": TriggerType.ISSUES,
            "issue_comment": TriggerType.ISSUE_COMMENT,
            "pull_request_target": TriggerType.PULL_REQUEST_TARGET,
            "pull_request_review": TriggerType.PULL_REQUEST_REVIEW,
            "pull_request_review_comment": TriggerType.PULL_REQUEST_REVIEW_COMMENT,
            "registry_package": TriggerType.REGISTRY_PACKAGE,
            "watch": TriggerType.WATCH,
        }
        return trigger_map.get(trigger_name, TriggerType.OTHER)

    def xǁWorkflowParserǁ_get_trigger_type__mutmut_8(self, trigger_name: str) -> TriggerType:
        """Map trigger name to TriggerType enum.

        Args:
            trigger_name: Trigger name from YAML.

        Returns:
            Corresponding TriggerType.
        """
        trigger_map = {
            "workflow_dispatch": TriggerType.WORKFLOW_DISPATCH,
            "push": TriggerType.PUSH,
            "pull_request": TriggerType.PULL_REQUEST,
            "XXscheduleXX": TriggerType.SCHEDULE,
            "workflow_call": TriggerType.WORKFLOW_CALL,
            "workflow_run": TriggerType.WORKFLOW_RUN,
            "repository_dispatch": TriggerType.REPOSITORY_DISPATCH,
            "release": TriggerType.RELEASE,
            "create": TriggerType.CREATE,
            "delete": TriggerType.DELETE,
            "fork": TriggerType.FORK,
            "issues": TriggerType.ISSUES,
            "issue_comment": TriggerType.ISSUE_COMMENT,
            "pull_request_target": TriggerType.PULL_REQUEST_TARGET,
            "pull_request_review": TriggerType.PULL_REQUEST_REVIEW,
            "pull_request_review_comment": TriggerType.PULL_REQUEST_REVIEW_COMMENT,
            "registry_package": TriggerType.REGISTRY_PACKAGE,
            "watch": TriggerType.WATCH,
        }
        return trigger_map.get(trigger_name, TriggerType.OTHER)

    def xǁWorkflowParserǁ_get_trigger_type__mutmut_9(self, trigger_name: str) -> TriggerType:
        """Map trigger name to TriggerType enum.

        Args:
            trigger_name: Trigger name from YAML.

        Returns:
            Corresponding TriggerType.
        """
        trigger_map = {
            "workflow_dispatch": TriggerType.WORKFLOW_DISPATCH,
            "push": TriggerType.PUSH,
            "pull_request": TriggerType.PULL_REQUEST,
            "SCHEDULE": TriggerType.SCHEDULE,
            "workflow_call": TriggerType.WORKFLOW_CALL,
            "workflow_run": TriggerType.WORKFLOW_RUN,
            "repository_dispatch": TriggerType.REPOSITORY_DISPATCH,
            "release": TriggerType.RELEASE,
            "create": TriggerType.CREATE,
            "delete": TriggerType.DELETE,
            "fork": TriggerType.FORK,
            "issues": TriggerType.ISSUES,
            "issue_comment": TriggerType.ISSUE_COMMENT,
            "pull_request_target": TriggerType.PULL_REQUEST_TARGET,
            "pull_request_review": TriggerType.PULL_REQUEST_REVIEW,
            "pull_request_review_comment": TriggerType.PULL_REQUEST_REVIEW_COMMENT,
            "registry_package": TriggerType.REGISTRY_PACKAGE,
            "watch": TriggerType.WATCH,
        }
        return trigger_map.get(trigger_name, TriggerType.OTHER)

    def xǁWorkflowParserǁ_get_trigger_type__mutmut_10(self, trigger_name: str) -> TriggerType:
        """Map trigger name to TriggerType enum.

        Args:
            trigger_name: Trigger name from YAML.

        Returns:
            Corresponding TriggerType.
        """
        trigger_map = {
            "workflow_dispatch": TriggerType.WORKFLOW_DISPATCH,
            "push": TriggerType.PUSH,
            "pull_request": TriggerType.PULL_REQUEST,
            "schedule": TriggerType.SCHEDULE,
            "XXworkflow_callXX": TriggerType.WORKFLOW_CALL,
            "workflow_run": TriggerType.WORKFLOW_RUN,
            "repository_dispatch": TriggerType.REPOSITORY_DISPATCH,
            "release": TriggerType.RELEASE,
            "create": TriggerType.CREATE,
            "delete": TriggerType.DELETE,
            "fork": TriggerType.FORK,
            "issues": TriggerType.ISSUES,
            "issue_comment": TriggerType.ISSUE_COMMENT,
            "pull_request_target": TriggerType.PULL_REQUEST_TARGET,
            "pull_request_review": TriggerType.PULL_REQUEST_REVIEW,
            "pull_request_review_comment": TriggerType.PULL_REQUEST_REVIEW_COMMENT,
            "registry_package": TriggerType.REGISTRY_PACKAGE,
            "watch": TriggerType.WATCH,
        }
        return trigger_map.get(trigger_name, TriggerType.OTHER)

    def xǁWorkflowParserǁ_get_trigger_type__mutmut_11(self, trigger_name: str) -> TriggerType:
        """Map trigger name to TriggerType enum.

        Args:
            trigger_name: Trigger name from YAML.

        Returns:
            Corresponding TriggerType.
        """
        trigger_map = {
            "workflow_dispatch": TriggerType.WORKFLOW_DISPATCH,
            "push": TriggerType.PUSH,
            "pull_request": TriggerType.PULL_REQUEST,
            "schedule": TriggerType.SCHEDULE,
            "WORKFLOW_CALL": TriggerType.WORKFLOW_CALL,
            "workflow_run": TriggerType.WORKFLOW_RUN,
            "repository_dispatch": TriggerType.REPOSITORY_DISPATCH,
            "release": TriggerType.RELEASE,
            "create": TriggerType.CREATE,
            "delete": TriggerType.DELETE,
            "fork": TriggerType.FORK,
            "issues": TriggerType.ISSUES,
            "issue_comment": TriggerType.ISSUE_COMMENT,
            "pull_request_target": TriggerType.PULL_REQUEST_TARGET,
            "pull_request_review": TriggerType.PULL_REQUEST_REVIEW,
            "pull_request_review_comment": TriggerType.PULL_REQUEST_REVIEW_COMMENT,
            "registry_package": TriggerType.REGISTRY_PACKAGE,
            "watch": TriggerType.WATCH,
        }
        return trigger_map.get(trigger_name, TriggerType.OTHER)

    def xǁWorkflowParserǁ_get_trigger_type__mutmut_12(self, trigger_name: str) -> TriggerType:
        """Map trigger name to TriggerType enum.

        Args:
            trigger_name: Trigger name from YAML.

        Returns:
            Corresponding TriggerType.
        """
        trigger_map = {
            "workflow_dispatch": TriggerType.WORKFLOW_DISPATCH,
            "push": TriggerType.PUSH,
            "pull_request": TriggerType.PULL_REQUEST,
            "schedule": TriggerType.SCHEDULE,
            "workflow_call": TriggerType.WORKFLOW_CALL,
            "XXworkflow_runXX": TriggerType.WORKFLOW_RUN,
            "repository_dispatch": TriggerType.REPOSITORY_DISPATCH,
            "release": TriggerType.RELEASE,
            "create": TriggerType.CREATE,
            "delete": TriggerType.DELETE,
            "fork": TriggerType.FORK,
            "issues": TriggerType.ISSUES,
            "issue_comment": TriggerType.ISSUE_COMMENT,
            "pull_request_target": TriggerType.PULL_REQUEST_TARGET,
            "pull_request_review": TriggerType.PULL_REQUEST_REVIEW,
            "pull_request_review_comment": TriggerType.PULL_REQUEST_REVIEW_COMMENT,
            "registry_package": TriggerType.REGISTRY_PACKAGE,
            "watch": TriggerType.WATCH,
        }
        return trigger_map.get(trigger_name, TriggerType.OTHER)

    def xǁWorkflowParserǁ_get_trigger_type__mutmut_13(self, trigger_name: str) -> TriggerType:
        """Map trigger name to TriggerType enum.

        Args:
            trigger_name: Trigger name from YAML.

        Returns:
            Corresponding TriggerType.
        """
        trigger_map = {
            "workflow_dispatch": TriggerType.WORKFLOW_DISPATCH,
            "push": TriggerType.PUSH,
            "pull_request": TriggerType.PULL_REQUEST,
            "schedule": TriggerType.SCHEDULE,
            "workflow_call": TriggerType.WORKFLOW_CALL,
            "WORKFLOW_RUN": TriggerType.WORKFLOW_RUN,
            "repository_dispatch": TriggerType.REPOSITORY_DISPATCH,
            "release": TriggerType.RELEASE,
            "create": TriggerType.CREATE,
            "delete": TriggerType.DELETE,
            "fork": TriggerType.FORK,
            "issues": TriggerType.ISSUES,
            "issue_comment": TriggerType.ISSUE_COMMENT,
            "pull_request_target": TriggerType.PULL_REQUEST_TARGET,
            "pull_request_review": TriggerType.PULL_REQUEST_REVIEW,
            "pull_request_review_comment": TriggerType.PULL_REQUEST_REVIEW_COMMENT,
            "registry_package": TriggerType.REGISTRY_PACKAGE,
            "watch": TriggerType.WATCH,
        }
        return trigger_map.get(trigger_name, TriggerType.OTHER)

    def xǁWorkflowParserǁ_get_trigger_type__mutmut_14(self, trigger_name: str) -> TriggerType:
        """Map trigger name to TriggerType enum.

        Args:
            trigger_name: Trigger name from YAML.

        Returns:
            Corresponding TriggerType.
        """
        trigger_map = {
            "workflow_dispatch": TriggerType.WORKFLOW_DISPATCH,
            "push": TriggerType.PUSH,
            "pull_request": TriggerType.PULL_REQUEST,
            "schedule": TriggerType.SCHEDULE,
            "workflow_call": TriggerType.WORKFLOW_CALL,
            "workflow_run": TriggerType.WORKFLOW_RUN,
            "XXrepository_dispatchXX": TriggerType.REPOSITORY_DISPATCH,
            "release": TriggerType.RELEASE,
            "create": TriggerType.CREATE,
            "delete": TriggerType.DELETE,
            "fork": TriggerType.FORK,
            "issues": TriggerType.ISSUES,
            "issue_comment": TriggerType.ISSUE_COMMENT,
            "pull_request_target": TriggerType.PULL_REQUEST_TARGET,
            "pull_request_review": TriggerType.PULL_REQUEST_REVIEW,
            "pull_request_review_comment": TriggerType.PULL_REQUEST_REVIEW_COMMENT,
            "registry_package": TriggerType.REGISTRY_PACKAGE,
            "watch": TriggerType.WATCH,
        }
        return trigger_map.get(trigger_name, TriggerType.OTHER)

    def xǁWorkflowParserǁ_get_trigger_type__mutmut_15(self, trigger_name: str) -> TriggerType:
        """Map trigger name to TriggerType enum.

        Args:
            trigger_name: Trigger name from YAML.

        Returns:
            Corresponding TriggerType.
        """
        trigger_map = {
            "workflow_dispatch": TriggerType.WORKFLOW_DISPATCH,
            "push": TriggerType.PUSH,
            "pull_request": TriggerType.PULL_REQUEST,
            "schedule": TriggerType.SCHEDULE,
            "workflow_call": TriggerType.WORKFLOW_CALL,
            "workflow_run": TriggerType.WORKFLOW_RUN,
            "REPOSITORY_DISPATCH": TriggerType.REPOSITORY_DISPATCH,
            "release": TriggerType.RELEASE,
            "create": TriggerType.CREATE,
            "delete": TriggerType.DELETE,
            "fork": TriggerType.FORK,
            "issues": TriggerType.ISSUES,
            "issue_comment": TriggerType.ISSUE_COMMENT,
            "pull_request_target": TriggerType.PULL_REQUEST_TARGET,
            "pull_request_review": TriggerType.PULL_REQUEST_REVIEW,
            "pull_request_review_comment": TriggerType.PULL_REQUEST_REVIEW_COMMENT,
            "registry_package": TriggerType.REGISTRY_PACKAGE,
            "watch": TriggerType.WATCH,
        }
        return trigger_map.get(trigger_name, TriggerType.OTHER)

    def xǁWorkflowParserǁ_get_trigger_type__mutmut_16(self, trigger_name: str) -> TriggerType:
        """Map trigger name to TriggerType enum.

        Args:
            trigger_name: Trigger name from YAML.

        Returns:
            Corresponding TriggerType.
        """
        trigger_map = {
            "workflow_dispatch": TriggerType.WORKFLOW_DISPATCH,
            "push": TriggerType.PUSH,
            "pull_request": TriggerType.PULL_REQUEST,
            "schedule": TriggerType.SCHEDULE,
            "workflow_call": TriggerType.WORKFLOW_CALL,
            "workflow_run": TriggerType.WORKFLOW_RUN,
            "repository_dispatch": TriggerType.REPOSITORY_DISPATCH,
            "XXreleaseXX": TriggerType.RELEASE,
            "create": TriggerType.CREATE,
            "delete": TriggerType.DELETE,
            "fork": TriggerType.FORK,
            "issues": TriggerType.ISSUES,
            "issue_comment": TriggerType.ISSUE_COMMENT,
            "pull_request_target": TriggerType.PULL_REQUEST_TARGET,
            "pull_request_review": TriggerType.PULL_REQUEST_REVIEW,
            "pull_request_review_comment": TriggerType.PULL_REQUEST_REVIEW_COMMENT,
            "registry_package": TriggerType.REGISTRY_PACKAGE,
            "watch": TriggerType.WATCH,
        }
        return trigger_map.get(trigger_name, TriggerType.OTHER)

    def xǁWorkflowParserǁ_get_trigger_type__mutmut_17(self, trigger_name: str) -> TriggerType:
        """Map trigger name to TriggerType enum.

        Args:
            trigger_name: Trigger name from YAML.

        Returns:
            Corresponding TriggerType.
        """
        trigger_map = {
            "workflow_dispatch": TriggerType.WORKFLOW_DISPATCH,
            "push": TriggerType.PUSH,
            "pull_request": TriggerType.PULL_REQUEST,
            "schedule": TriggerType.SCHEDULE,
            "workflow_call": TriggerType.WORKFLOW_CALL,
            "workflow_run": TriggerType.WORKFLOW_RUN,
            "repository_dispatch": TriggerType.REPOSITORY_DISPATCH,
            "RELEASE": TriggerType.RELEASE,
            "create": TriggerType.CREATE,
            "delete": TriggerType.DELETE,
            "fork": TriggerType.FORK,
            "issues": TriggerType.ISSUES,
            "issue_comment": TriggerType.ISSUE_COMMENT,
            "pull_request_target": TriggerType.PULL_REQUEST_TARGET,
            "pull_request_review": TriggerType.PULL_REQUEST_REVIEW,
            "pull_request_review_comment": TriggerType.PULL_REQUEST_REVIEW_COMMENT,
            "registry_package": TriggerType.REGISTRY_PACKAGE,
            "watch": TriggerType.WATCH,
        }
        return trigger_map.get(trigger_name, TriggerType.OTHER)

    def xǁWorkflowParserǁ_get_trigger_type__mutmut_18(self, trigger_name: str) -> TriggerType:
        """Map trigger name to TriggerType enum.

        Args:
            trigger_name: Trigger name from YAML.

        Returns:
            Corresponding TriggerType.
        """
        trigger_map = {
            "workflow_dispatch": TriggerType.WORKFLOW_DISPATCH,
            "push": TriggerType.PUSH,
            "pull_request": TriggerType.PULL_REQUEST,
            "schedule": TriggerType.SCHEDULE,
            "workflow_call": TriggerType.WORKFLOW_CALL,
            "workflow_run": TriggerType.WORKFLOW_RUN,
            "repository_dispatch": TriggerType.REPOSITORY_DISPATCH,
            "release": TriggerType.RELEASE,
            "XXcreateXX": TriggerType.CREATE,
            "delete": TriggerType.DELETE,
            "fork": TriggerType.FORK,
            "issues": TriggerType.ISSUES,
            "issue_comment": TriggerType.ISSUE_COMMENT,
            "pull_request_target": TriggerType.PULL_REQUEST_TARGET,
            "pull_request_review": TriggerType.PULL_REQUEST_REVIEW,
            "pull_request_review_comment": TriggerType.PULL_REQUEST_REVIEW_COMMENT,
            "registry_package": TriggerType.REGISTRY_PACKAGE,
            "watch": TriggerType.WATCH,
        }
        return trigger_map.get(trigger_name, TriggerType.OTHER)

    def xǁWorkflowParserǁ_get_trigger_type__mutmut_19(self, trigger_name: str) -> TriggerType:
        """Map trigger name to TriggerType enum.

        Args:
            trigger_name: Trigger name from YAML.

        Returns:
            Corresponding TriggerType.
        """
        trigger_map = {
            "workflow_dispatch": TriggerType.WORKFLOW_DISPATCH,
            "push": TriggerType.PUSH,
            "pull_request": TriggerType.PULL_REQUEST,
            "schedule": TriggerType.SCHEDULE,
            "workflow_call": TriggerType.WORKFLOW_CALL,
            "workflow_run": TriggerType.WORKFLOW_RUN,
            "repository_dispatch": TriggerType.REPOSITORY_DISPATCH,
            "release": TriggerType.RELEASE,
            "CREATE": TriggerType.CREATE,
            "delete": TriggerType.DELETE,
            "fork": TriggerType.FORK,
            "issues": TriggerType.ISSUES,
            "issue_comment": TriggerType.ISSUE_COMMENT,
            "pull_request_target": TriggerType.PULL_REQUEST_TARGET,
            "pull_request_review": TriggerType.PULL_REQUEST_REVIEW,
            "pull_request_review_comment": TriggerType.PULL_REQUEST_REVIEW_COMMENT,
            "registry_package": TriggerType.REGISTRY_PACKAGE,
            "watch": TriggerType.WATCH,
        }
        return trigger_map.get(trigger_name, TriggerType.OTHER)

    def xǁWorkflowParserǁ_get_trigger_type__mutmut_20(self, trigger_name: str) -> TriggerType:
        """Map trigger name to TriggerType enum.

        Args:
            trigger_name: Trigger name from YAML.

        Returns:
            Corresponding TriggerType.
        """
        trigger_map = {
            "workflow_dispatch": TriggerType.WORKFLOW_DISPATCH,
            "push": TriggerType.PUSH,
            "pull_request": TriggerType.PULL_REQUEST,
            "schedule": TriggerType.SCHEDULE,
            "workflow_call": TriggerType.WORKFLOW_CALL,
            "workflow_run": TriggerType.WORKFLOW_RUN,
            "repository_dispatch": TriggerType.REPOSITORY_DISPATCH,
            "release": TriggerType.RELEASE,
            "create": TriggerType.CREATE,
            "XXdeleteXX": TriggerType.DELETE,
            "fork": TriggerType.FORK,
            "issues": TriggerType.ISSUES,
            "issue_comment": TriggerType.ISSUE_COMMENT,
            "pull_request_target": TriggerType.PULL_REQUEST_TARGET,
            "pull_request_review": TriggerType.PULL_REQUEST_REVIEW,
            "pull_request_review_comment": TriggerType.PULL_REQUEST_REVIEW_COMMENT,
            "registry_package": TriggerType.REGISTRY_PACKAGE,
            "watch": TriggerType.WATCH,
        }
        return trigger_map.get(trigger_name, TriggerType.OTHER)

    def xǁWorkflowParserǁ_get_trigger_type__mutmut_21(self, trigger_name: str) -> TriggerType:
        """Map trigger name to TriggerType enum.

        Args:
            trigger_name: Trigger name from YAML.

        Returns:
            Corresponding TriggerType.
        """
        trigger_map = {
            "workflow_dispatch": TriggerType.WORKFLOW_DISPATCH,
            "push": TriggerType.PUSH,
            "pull_request": TriggerType.PULL_REQUEST,
            "schedule": TriggerType.SCHEDULE,
            "workflow_call": TriggerType.WORKFLOW_CALL,
            "workflow_run": TriggerType.WORKFLOW_RUN,
            "repository_dispatch": TriggerType.REPOSITORY_DISPATCH,
            "release": TriggerType.RELEASE,
            "create": TriggerType.CREATE,
            "DELETE": TriggerType.DELETE,
            "fork": TriggerType.FORK,
            "issues": TriggerType.ISSUES,
            "issue_comment": TriggerType.ISSUE_COMMENT,
            "pull_request_target": TriggerType.PULL_REQUEST_TARGET,
            "pull_request_review": TriggerType.PULL_REQUEST_REVIEW,
            "pull_request_review_comment": TriggerType.PULL_REQUEST_REVIEW_COMMENT,
            "registry_package": TriggerType.REGISTRY_PACKAGE,
            "watch": TriggerType.WATCH,
        }
        return trigger_map.get(trigger_name, TriggerType.OTHER)

    def xǁWorkflowParserǁ_get_trigger_type__mutmut_22(self, trigger_name: str) -> TriggerType:
        """Map trigger name to TriggerType enum.

        Args:
            trigger_name: Trigger name from YAML.

        Returns:
            Corresponding TriggerType.
        """
        trigger_map = {
            "workflow_dispatch": TriggerType.WORKFLOW_DISPATCH,
            "push": TriggerType.PUSH,
            "pull_request": TriggerType.PULL_REQUEST,
            "schedule": TriggerType.SCHEDULE,
            "workflow_call": TriggerType.WORKFLOW_CALL,
            "workflow_run": TriggerType.WORKFLOW_RUN,
            "repository_dispatch": TriggerType.REPOSITORY_DISPATCH,
            "release": TriggerType.RELEASE,
            "create": TriggerType.CREATE,
            "delete": TriggerType.DELETE,
            "XXforkXX": TriggerType.FORK,
            "issues": TriggerType.ISSUES,
            "issue_comment": TriggerType.ISSUE_COMMENT,
            "pull_request_target": TriggerType.PULL_REQUEST_TARGET,
            "pull_request_review": TriggerType.PULL_REQUEST_REVIEW,
            "pull_request_review_comment": TriggerType.PULL_REQUEST_REVIEW_COMMENT,
            "registry_package": TriggerType.REGISTRY_PACKAGE,
            "watch": TriggerType.WATCH,
        }
        return trigger_map.get(trigger_name, TriggerType.OTHER)

    def xǁWorkflowParserǁ_get_trigger_type__mutmut_23(self, trigger_name: str) -> TriggerType:
        """Map trigger name to TriggerType enum.

        Args:
            trigger_name: Trigger name from YAML.

        Returns:
            Corresponding TriggerType.
        """
        trigger_map = {
            "workflow_dispatch": TriggerType.WORKFLOW_DISPATCH,
            "push": TriggerType.PUSH,
            "pull_request": TriggerType.PULL_REQUEST,
            "schedule": TriggerType.SCHEDULE,
            "workflow_call": TriggerType.WORKFLOW_CALL,
            "workflow_run": TriggerType.WORKFLOW_RUN,
            "repository_dispatch": TriggerType.REPOSITORY_DISPATCH,
            "release": TriggerType.RELEASE,
            "create": TriggerType.CREATE,
            "delete": TriggerType.DELETE,
            "FORK": TriggerType.FORK,
            "issues": TriggerType.ISSUES,
            "issue_comment": TriggerType.ISSUE_COMMENT,
            "pull_request_target": TriggerType.PULL_REQUEST_TARGET,
            "pull_request_review": TriggerType.PULL_REQUEST_REVIEW,
            "pull_request_review_comment": TriggerType.PULL_REQUEST_REVIEW_COMMENT,
            "registry_package": TriggerType.REGISTRY_PACKAGE,
            "watch": TriggerType.WATCH,
        }
        return trigger_map.get(trigger_name, TriggerType.OTHER)

    def xǁWorkflowParserǁ_get_trigger_type__mutmut_24(self, trigger_name: str) -> TriggerType:
        """Map trigger name to TriggerType enum.

        Args:
            trigger_name: Trigger name from YAML.

        Returns:
            Corresponding TriggerType.
        """
        trigger_map = {
            "workflow_dispatch": TriggerType.WORKFLOW_DISPATCH,
            "push": TriggerType.PUSH,
            "pull_request": TriggerType.PULL_REQUEST,
            "schedule": TriggerType.SCHEDULE,
            "workflow_call": TriggerType.WORKFLOW_CALL,
            "workflow_run": TriggerType.WORKFLOW_RUN,
            "repository_dispatch": TriggerType.REPOSITORY_DISPATCH,
            "release": TriggerType.RELEASE,
            "create": TriggerType.CREATE,
            "delete": TriggerType.DELETE,
            "fork": TriggerType.FORK,
            "XXissuesXX": TriggerType.ISSUES,
            "issue_comment": TriggerType.ISSUE_COMMENT,
            "pull_request_target": TriggerType.PULL_REQUEST_TARGET,
            "pull_request_review": TriggerType.PULL_REQUEST_REVIEW,
            "pull_request_review_comment": TriggerType.PULL_REQUEST_REVIEW_COMMENT,
            "registry_package": TriggerType.REGISTRY_PACKAGE,
            "watch": TriggerType.WATCH,
        }
        return trigger_map.get(trigger_name, TriggerType.OTHER)

    def xǁWorkflowParserǁ_get_trigger_type__mutmut_25(self, trigger_name: str) -> TriggerType:
        """Map trigger name to TriggerType enum.

        Args:
            trigger_name: Trigger name from YAML.

        Returns:
            Corresponding TriggerType.
        """
        trigger_map = {
            "workflow_dispatch": TriggerType.WORKFLOW_DISPATCH,
            "push": TriggerType.PUSH,
            "pull_request": TriggerType.PULL_REQUEST,
            "schedule": TriggerType.SCHEDULE,
            "workflow_call": TriggerType.WORKFLOW_CALL,
            "workflow_run": TriggerType.WORKFLOW_RUN,
            "repository_dispatch": TriggerType.REPOSITORY_DISPATCH,
            "release": TriggerType.RELEASE,
            "create": TriggerType.CREATE,
            "delete": TriggerType.DELETE,
            "fork": TriggerType.FORK,
            "ISSUES": TriggerType.ISSUES,
            "issue_comment": TriggerType.ISSUE_COMMENT,
            "pull_request_target": TriggerType.PULL_REQUEST_TARGET,
            "pull_request_review": TriggerType.PULL_REQUEST_REVIEW,
            "pull_request_review_comment": TriggerType.PULL_REQUEST_REVIEW_COMMENT,
            "registry_package": TriggerType.REGISTRY_PACKAGE,
            "watch": TriggerType.WATCH,
        }
        return trigger_map.get(trigger_name, TriggerType.OTHER)

    def xǁWorkflowParserǁ_get_trigger_type__mutmut_26(self, trigger_name: str) -> TriggerType:
        """Map trigger name to TriggerType enum.

        Args:
            trigger_name: Trigger name from YAML.

        Returns:
            Corresponding TriggerType.
        """
        trigger_map = {
            "workflow_dispatch": TriggerType.WORKFLOW_DISPATCH,
            "push": TriggerType.PUSH,
            "pull_request": TriggerType.PULL_REQUEST,
            "schedule": TriggerType.SCHEDULE,
            "workflow_call": TriggerType.WORKFLOW_CALL,
            "workflow_run": TriggerType.WORKFLOW_RUN,
            "repository_dispatch": TriggerType.REPOSITORY_DISPATCH,
            "release": TriggerType.RELEASE,
            "create": TriggerType.CREATE,
            "delete": TriggerType.DELETE,
            "fork": TriggerType.FORK,
            "issues": TriggerType.ISSUES,
            "XXissue_commentXX": TriggerType.ISSUE_COMMENT,
            "pull_request_target": TriggerType.PULL_REQUEST_TARGET,
            "pull_request_review": TriggerType.PULL_REQUEST_REVIEW,
            "pull_request_review_comment": TriggerType.PULL_REQUEST_REVIEW_COMMENT,
            "registry_package": TriggerType.REGISTRY_PACKAGE,
            "watch": TriggerType.WATCH,
        }
        return trigger_map.get(trigger_name, TriggerType.OTHER)

    def xǁWorkflowParserǁ_get_trigger_type__mutmut_27(self, trigger_name: str) -> TriggerType:
        """Map trigger name to TriggerType enum.

        Args:
            trigger_name: Trigger name from YAML.

        Returns:
            Corresponding TriggerType.
        """
        trigger_map = {
            "workflow_dispatch": TriggerType.WORKFLOW_DISPATCH,
            "push": TriggerType.PUSH,
            "pull_request": TriggerType.PULL_REQUEST,
            "schedule": TriggerType.SCHEDULE,
            "workflow_call": TriggerType.WORKFLOW_CALL,
            "workflow_run": TriggerType.WORKFLOW_RUN,
            "repository_dispatch": TriggerType.REPOSITORY_DISPATCH,
            "release": TriggerType.RELEASE,
            "create": TriggerType.CREATE,
            "delete": TriggerType.DELETE,
            "fork": TriggerType.FORK,
            "issues": TriggerType.ISSUES,
            "ISSUE_COMMENT": TriggerType.ISSUE_COMMENT,
            "pull_request_target": TriggerType.PULL_REQUEST_TARGET,
            "pull_request_review": TriggerType.PULL_REQUEST_REVIEW,
            "pull_request_review_comment": TriggerType.PULL_REQUEST_REVIEW_COMMENT,
            "registry_package": TriggerType.REGISTRY_PACKAGE,
            "watch": TriggerType.WATCH,
        }
        return trigger_map.get(trigger_name, TriggerType.OTHER)

    def xǁWorkflowParserǁ_get_trigger_type__mutmut_28(self, trigger_name: str) -> TriggerType:
        """Map trigger name to TriggerType enum.

        Args:
            trigger_name: Trigger name from YAML.

        Returns:
            Corresponding TriggerType.
        """
        trigger_map = {
            "workflow_dispatch": TriggerType.WORKFLOW_DISPATCH,
            "push": TriggerType.PUSH,
            "pull_request": TriggerType.PULL_REQUEST,
            "schedule": TriggerType.SCHEDULE,
            "workflow_call": TriggerType.WORKFLOW_CALL,
            "workflow_run": TriggerType.WORKFLOW_RUN,
            "repository_dispatch": TriggerType.REPOSITORY_DISPATCH,
            "release": TriggerType.RELEASE,
            "create": TriggerType.CREATE,
            "delete": TriggerType.DELETE,
            "fork": TriggerType.FORK,
            "issues": TriggerType.ISSUES,
            "issue_comment": TriggerType.ISSUE_COMMENT,
            "XXpull_request_targetXX": TriggerType.PULL_REQUEST_TARGET,
            "pull_request_review": TriggerType.PULL_REQUEST_REVIEW,
            "pull_request_review_comment": TriggerType.PULL_REQUEST_REVIEW_COMMENT,
            "registry_package": TriggerType.REGISTRY_PACKAGE,
            "watch": TriggerType.WATCH,
        }
        return trigger_map.get(trigger_name, TriggerType.OTHER)

    def xǁWorkflowParserǁ_get_trigger_type__mutmut_29(self, trigger_name: str) -> TriggerType:
        """Map trigger name to TriggerType enum.

        Args:
            trigger_name: Trigger name from YAML.

        Returns:
            Corresponding TriggerType.
        """
        trigger_map = {
            "workflow_dispatch": TriggerType.WORKFLOW_DISPATCH,
            "push": TriggerType.PUSH,
            "pull_request": TriggerType.PULL_REQUEST,
            "schedule": TriggerType.SCHEDULE,
            "workflow_call": TriggerType.WORKFLOW_CALL,
            "workflow_run": TriggerType.WORKFLOW_RUN,
            "repository_dispatch": TriggerType.REPOSITORY_DISPATCH,
            "release": TriggerType.RELEASE,
            "create": TriggerType.CREATE,
            "delete": TriggerType.DELETE,
            "fork": TriggerType.FORK,
            "issues": TriggerType.ISSUES,
            "issue_comment": TriggerType.ISSUE_COMMENT,
            "PULL_REQUEST_TARGET": TriggerType.PULL_REQUEST_TARGET,
            "pull_request_review": TriggerType.PULL_REQUEST_REVIEW,
            "pull_request_review_comment": TriggerType.PULL_REQUEST_REVIEW_COMMENT,
            "registry_package": TriggerType.REGISTRY_PACKAGE,
            "watch": TriggerType.WATCH,
        }
        return trigger_map.get(trigger_name, TriggerType.OTHER)

    def xǁWorkflowParserǁ_get_trigger_type__mutmut_30(self, trigger_name: str) -> TriggerType:
        """Map trigger name to TriggerType enum.

        Args:
            trigger_name: Trigger name from YAML.

        Returns:
            Corresponding TriggerType.
        """
        trigger_map = {
            "workflow_dispatch": TriggerType.WORKFLOW_DISPATCH,
            "push": TriggerType.PUSH,
            "pull_request": TriggerType.PULL_REQUEST,
            "schedule": TriggerType.SCHEDULE,
            "workflow_call": TriggerType.WORKFLOW_CALL,
            "workflow_run": TriggerType.WORKFLOW_RUN,
            "repository_dispatch": TriggerType.REPOSITORY_DISPATCH,
            "release": TriggerType.RELEASE,
            "create": TriggerType.CREATE,
            "delete": TriggerType.DELETE,
            "fork": TriggerType.FORK,
            "issues": TriggerType.ISSUES,
            "issue_comment": TriggerType.ISSUE_COMMENT,
            "pull_request_target": TriggerType.PULL_REQUEST_TARGET,
            "XXpull_request_reviewXX": TriggerType.PULL_REQUEST_REVIEW,
            "pull_request_review_comment": TriggerType.PULL_REQUEST_REVIEW_COMMENT,
            "registry_package": TriggerType.REGISTRY_PACKAGE,
            "watch": TriggerType.WATCH,
        }
        return trigger_map.get(trigger_name, TriggerType.OTHER)

    def xǁWorkflowParserǁ_get_trigger_type__mutmut_31(self, trigger_name: str) -> TriggerType:
        """Map trigger name to TriggerType enum.

        Args:
            trigger_name: Trigger name from YAML.

        Returns:
            Corresponding TriggerType.
        """
        trigger_map = {
            "workflow_dispatch": TriggerType.WORKFLOW_DISPATCH,
            "push": TriggerType.PUSH,
            "pull_request": TriggerType.PULL_REQUEST,
            "schedule": TriggerType.SCHEDULE,
            "workflow_call": TriggerType.WORKFLOW_CALL,
            "workflow_run": TriggerType.WORKFLOW_RUN,
            "repository_dispatch": TriggerType.REPOSITORY_DISPATCH,
            "release": TriggerType.RELEASE,
            "create": TriggerType.CREATE,
            "delete": TriggerType.DELETE,
            "fork": TriggerType.FORK,
            "issues": TriggerType.ISSUES,
            "issue_comment": TriggerType.ISSUE_COMMENT,
            "pull_request_target": TriggerType.PULL_REQUEST_TARGET,
            "PULL_REQUEST_REVIEW": TriggerType.PULL_REQUEST_REVIEW,
            "pull_request_review_comment": TriggerType.PULL_REQUEST_REVIEW_COMMENT,
            "registry_package": TriggerType.REGISTRY_PACKAGE,
            "watch": TriggerType.WATCH,
        }
        return trigger_map.get(trigger_name, TriggerType.OTHER)

    def xǁWorkflowParserǁ_get_trigger_type__mutmut_32(self, trigger_name: str) -> TriggerType:
        """Map trigger name to TriggerType enum.

        Args:
            trigger_name: Trigger name from YAML.

        Returns:
            Corresponding TriggerType.
        """
        trigger_map = {
            "workflow_dispatch": TriggerType.WORKFLOW_DISPATCH,
            "push": TriggerType.PUSH,
            "pull_request": TriggerType.PULL_REQUEST,
            "schedule": TriggerType.SCHEDULE,
            "workflow_call": TriggerType.WORKFLOW_CALL,
            "workflow_run": TriggerType.WORKFLOW_RUN,
            "repository_dispatch": TriggerType.REPOSITORY_DISPATCH,
            "release": TriggerType.RELEASE,
            "create": TriggerType.CREATE,
            "delete": TriggerType.DELETE,
            "fork": TriggerType.FORK,
            "issues": TriggerType.ISSUES,
            "issue_comment": TriggerType.ISSUE_COMMENT,
            "pull_request_target": TriggerType.PULL_REQUEST_TARGET,
            "pull_request_review": TriggerType.PULL_REQUEST_REVIEW,
            "XXpull_request_review_commentXX": TriggerType.PULL_REQUEST_REVIEW_COMMENT,
            "registry_package": TriggerType.REGISTRY_PACKAGE,
            "watch": TriggerType.WATCH,
        }
        return trigger_map.get(trigger_name, TriggerType.OTHER)

    def xǁWorkflowParserǁ_get_trigger_type__mutmut_33(self, trigger_name: str) -> TriggerType:
        """Map trigger name to TriggerType enum.

        Args:
            trigger_name: Trigger name from YAML.

        Returns:
            Corresponding TriggerType.
        """
        trigger_map = {
            "workflow_dispatch": TriggerType.WORKFLOW_DISPATCH,
            "push": TriggerType.PUSH,
            "pull_request": TriggerType.PULL_REQUEST,
            "schedule": TriggerType.SCHEDULE,
            "workflow_call": TriggerType.WORKFLOW_CALL,
            "workflow_run": TriggerType.WORKFLOW_RUN,
            "repository_dispatch": TriggerType.REPOSITORY_DISPATCH,
            "release": TriggerType.RELEASE,
            "create": TriggerType.CREATE,
            "delete": TriggerType.DELETE,
            "fork": TriggerType.FORK,
            "issues": TriggerType.ISSUES,
            "issue_comment": TriggerType.ISSUE_COMMENT,
            "pull_request_target": TriggerType.PULL_REQUEST_TARGET,
            "pull_request_review": TriggerType.PULL_REQUEST_REVIEW,
            "PULL_REQUEST_REVIEW_COMMENT": TriggerType.PULL_REQUEST_REVIEW_COMMENT,
            "registry_package": TriggerType.REGISTRY_PACKAGE,
            "watch": TriggerType.WATCH,
        }
        return trigger_map.get(trigger_name, TriggerType.OTHER)

    def xǁWorkflowParserǁ_get_trigger_type__mutmut_34(self, trigger_name: str) -> TriggerType:
        """Map trigger name to TriggerType enum.

        Args:
            trigger_name: Trigger name from YAML.

        Returns:
            Corresponding TriggerType.
        """
        trigger_map = {
            "workflow_dispatch": TriggerType.WORKFLOW_DISPATCH,
            "push": TriggerType.PUSH,
            "pull_request": TriggerType.PULL_REQUEST,
            "schedule": TriggerType.SCHEDULE,
            "workflow_call": TriggerType.WORKFLOW_CALL,
            "workflow_run": TriggerType.WORKFLOW_RUN,
            "repository_dispatch": TriggerType.REPOSITORY_DISPATCH,
            "release": TriggerType.RELEASE,
            "create": TriggerType.CREATE,
            "delete": TriggerType.DELETE,
            "fork": TriggerType.FORK,
            "issues": TriggerType.ISSUES,
            "issue_comment": TriggerType.ISSUE_COMMENT,
            "pull_request_target": TriggerType.PULL_REQUEST_TARGET,
            "pull_request_review": TriggerType.PULL_REQUEST_REVIEW,
            "pull_request_review_comment": TriggerType.PULL_REQUEST_REVIEW_COMMENT,
            "XXregistry_packageXX": TriggerType.REGISTRY_PACKAGE,
            "watch": TriggerType.WATCH,
        }
        return trigger_map.get(trigger_name, TriggerType.OTHER)

    def xǁWorkflowParserǁ_get_trigger_type__mutmut_35(self, trigger_name: str) -> TriggerType:
        """Map trigger name to TriggerType enum.

        Args:
            trigger_name: Trigger name from YAML.

        Returns:
            Corresponding TriggerType.
        """
        trigger_map = {
            "workflow_dispatch": TriggerType.WORKFLOW_DISPATCH,
            "push": TriggerType.PUSH,
            "pull_request": TriggerType.PULL_REQUEST,
            "schedule": TriggerType.SCHEDULE,
            "workflow_call": TriggerType.WORKFLOW_CALL,
            "workflow_run": TriggerType.WORKFLOW_RUN,
            "repository_dispatch": TriggerType.REPOSITORY_DISPATCH,
            "release": TriggerType.RELEASE,
            "create": TriggerType.CREATE,
            "delete": TriggerType.DELETE,
            "fork": TriggerType.FORK,
            "issues": TriggerType.ISSUES,
            "issue_comment": TriggerType.ISSUE_COMMENT,
            "pull_request_target": TriggerType.PULL_REQUEST_TARGET,
            "pull_request_review": TriggerType.PULL_REQUEST_REVIEW,
            "pull_request_review_comment": TriggerType.PULL_REQUEST_REVIEW_COMMENT,
            "REGISTRY_PACKAGE": TriggerType.REGISTRY_PACKAGE,
            "watch": TriggerType.WATCH,
        }
        return trigger_map.get(trigger_name, TriggerType.OTHER)

    def xǁWorkflowParserǁ_get_trigger_type__mutmut_36(self, trigger_name: str) -> TriggerType:
        """Map trigger name to TriggerType enum.

        Args:
            trigger_name: Trigger name from YAML.

        Returns:
            Corresponding TriggerType.
        """
        trigger_map = {
            "workflow_dispatch": TriggerType.WORKFLOW_DISPATCH,
            "push": TriggerType.PUSH,
            "pull_request": TriggerType.PULL_REQUEST,
            "schedule": TriggerType.SCHEDULE,
            "workflow_call": TriggerType.WORKFLOW_CALL,
            "workflow_run": TriggerType.WORKFLOW_RUN,
            "repository_dispatch": TriggerType.REPOSITORY_DISPATCH,
            "release": TriggerType.RELEASE,
            "create": TriggerType.CREATE,
            "delete": TriggerType.DELETE,
            "fork": TriggerType.FORK,
            "issues": TriggerType.ISSUES,
            "issue_comment": TriggerType.ISSUE_COMMENT,
            "pull_request_target": TriggerType.PULL_REQUEST_TARGET,
            "pull_request_review": TriggerType.PULL_REQUEST_REVIEW,
            "pull_request_review_comment": TriggerType.PULL_REQUEST_REVIEW_COMMENT,
            "registry_package": TriggerType.REGISTRY_PACKAGE,
            "XXwatchXX": TriggerType.WATCH,
        }
        return trigger_map.get(trigger_name, TriggerType.OTHER)

    def xǁWorkflowParserǁ_get_trigger_type__mutmut_37(self, trigger_name: str) -> TriggerType:
        """Map trigger name to TriggerType enum.

        Args:
            trigger_name: Trigger name from YAML.

        Returns:
            Corresponding TriggerType.
        """
        trigger_map = {
            "workflow_dispatch": TriggerType.WORKFLOW_DISPATCH,
            "push": TriggerType.PUSH,
            "pull_request": TriggerType.PULL_REQUEST,
            "schedule": TriggerType.SCHEDULE,
            "workflow_call": TriggerType.WORKFLOW_CALL,
            "workflow_run": TriggerType.WORKFLOW_RUN,
            "repository_dispatch": TriggerType.REPOSITORY_DISPATCH,
            "release": TriggerType.RELEASE,
            "create": TriggerType.CREATE,
            "delete": TriggerType.DELETE,
            "fork": TriggerType.FORK,
            "issues": TriggerType.ISSUES,
            "issue_comment": TriggerType.ISSUE_COMMENT,
            "pull_request_target": TriggerType.PULL_REQUEST_TARGET,
            "pull_request_review": TriggerType.PULL_REQUEST_REVIEW,
            "pull_request_review_comment": TriggerType.PULL_REQUEST_REVIEW_COMMENT,
            "registry_package": TriggerType.REGISTRY_PACKAGE,
            "WATCH": TriggerType.WATCH,
        }
        return trigger_map.get(trigger_name, TriggerType.OTHER)

    def xǁWorkflowParserǁ_get_trigger_type__mutmut_38(self, trigger_name: str) -> TriggerType:
        """Map trigger name to TriggerType enum.

        Args:
            trigger_name: Trigger name from YAML.

        Returns:
            Corresponding TriggerType.
        """
        trigger_map = {
            "workflow_dispatch": TriggerType.WORKFLOW_DISPATCH,
            "push": TriggerType.PUSH,
            "pull_request": TriggerType.PULL_REQUEST,
            "schedule": TriggerType.SCHEDULE,
            "workflow_call": TriggerType.WORKFLOW_CALL,
            "workflow_run": TriggerType.WORKFLOW_RUN,
            "repository_dispatch": TriggerType.REPOSITORY_DISPATCH,
            "release": TriggerType.RELEASE,
            "create": TriggerType.CREATE,
            "delete": TriggerType.DELETE,
            "fork": TriggerType.FORK,
            "issues": TriggerType.ISSUES,
            "issue_comment": TriggerType.ISSUE_COMMENT,
            "pull_request_target": TriggerType.PULL_REQUEST_TARGET,
            "pull_request_review": TriggerType.PULL_REQUEST_REVIEW,
            "pull_request_review_comment": TriggerType.PULL_REQUEST_REVIEW_COMMENT,
            "registry_package": TriggerType.REGISTRY_PACKAGE,
            "watch": TriggerType.WATCH,
        }
        return trigger_map.get(None, TriggerType.OTHER)

    def xǁWorkflowParserǁ_get_trigger_type__mutmut_39(self, trigger_name: str) -> TriggerType:
        """Map trigger name to TriggerType enum.

        Args:
            trigger_name: Trigger name from YAML.

        Returns:
            Corresponding TriggerType.
        """
        trigger_map = {
            "workflow_dispatch": TriggerType.WORKFLOW_DISPATCH,
            "push": TriggerType.PUSH,
            "pull_request": TriggerType.PULL_REQUEST,
            "schedule": TriggerType.SCHEDULE,
            "workflow_call": TriggerType.WORKFLOW_CALL,
            "workflow_run": TriggerType.WORKFLOW_RUN,
            "repository_dispatch": TriggerType.REPOSITORY_DISPATCH,
            "release": TriggerType.RELEASE,
            "create": TriggerType.CREATE,
            "delete": TriggerType.DELETE,
            "fork": TriggerType.FORK,
            "issues": TriggerType.ISSUES,
            "issue_comment": TriggerType.ISSUE_COMMENT,
            "pull_request_target": TriggerType.PULL_REQUEST_TARGET,
            "pull_request_review": TriggerType.PULL_REQUEST_REVIEW,
            "pull_request_review_comment": TriggerType.PULL_REQUEST_REVIEW_COMMENT,
            "registry_package": TriggerType.REGISTRY_PACKAGE,
            "watch": TriggerType.WATCH,
        }
        return trigger_map.get(trigger_name, None)

    def xǁWorkflowParserǁ_get_trigger_type__mutmut_40(self, trigger_name: str) -> TriggerType:
        """Map trigger name to TriggerType enum.

        Args:
            trigger_name: Trigger name from YAML.

        Returns:
            Corresponding TriggerType.
        """
        trigger_map = {
            "workflow_dispatch": TriggerType.WORKFLOW_DISPATCH,
            "push": TriggerType.PUSH,
            "pull_request": TriggerType.PULL_REQUEST,
            "schedule": TriggerType.SCHEDULE,
            "workflow_call": TriggerType.WORKFLOW_CALL,
            "workflow_run": TriggerType.WORKFLOW_RUN,
            "repository_dispatch": TriggerType.REPOSITORY_DISPATCH,
            "release": TriggerType.RELEASE,
            "create": TriggerType.CREATE,
            "delete": TriggerType.DELETE,
            "fork": TriggerType.FORK,
            "issues": TriggerType.ISSUES,
            "issue_comment": TriggerType.ISSUE_COMMENT,
            "pull_request_target": TriggerType.PULL_REQUEST_TARGET,
            "pull_request_review": TriggerType.PULL_REQUEST_REVIEW,
            "pull_request_review_comment": TriggerType.PULL_REQUEST_REVIEW_COMMENT,
            "registry_package": TriggerType.REGISTRY_PACKAGE,
            "watch": TriggerType.WATCH,
        }
        return trigger_map.get(TriggerType.OTHER)

    def xǁWorkflowParserǁ_get_trigger_type__mutmut_41(self, trigger_name: str) -> TriggerType:
        """Map trigger name to TriggerType enum.

        Args:
            trigger_name: Trigger name from YAML.

        Returns:
            Corresponding TriggerType.
        """
        trigger_map = {
            "workflow_dispatch": TriggerType.WORKFLOW_DISPATCH,
            "push": TriggerType.PUSH,
            "pull_request": TriggerType.PULL_REQUEST,
            "schedule": TriggerType.SCHEDULE,
            "workflow_call": TriggerType.WORKFLOW_CALL,
            "workflow_run": TriggerType.WORKFLOW_RUN,
            "repository_dispatch": TriggerType.REPOSITORY_DISPATCH,
            "release": TriggerType.RELEASE,
            "create": TriggerType.CREATE,
            "delete": TriggerType.DELETE,
            "fork": TriggerType.FORK,
            "issues": TriggerType.ISSUES,
            "issue_comment": TriggerType.ISSUE_COMMENT,
            "pull_request_target": TriggerType.PULL_REQUEST_TARGET,
            "pull_request_review": TriggerType.PULL_REQUEST_REVIEW,
            "pull_request_review_comment": TriggerType.PULL_REQUEST_REVIEW_COMMENT,
            "registry_package": TriggerType.REGISTRY_PACKAGE,
            "watch": TriggerType.WATCH,
        }
        return trigger_map.get(trigger_name, )
    
    xǁWorkflowParserǁ_get_trigger_type__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁWorkflowParserǁ_get_trigger_type__mutmut_1': xǁWorkflowParserǁ_get_trigger_type__mutmut_1, 
        'xǁWorkflowParserǁ_get_trigger_type__mutmut_2': xǁWorkflowParserǁ_get_trigger_type__mutmut_2, 
        'xǁWorkflowParserǁ_get_trigger_type__mutmut_3': xǁWorkflowParserǁ_get_trigger_type__mutmut_3, 
        'xǁWorkflowParserǁ_get_trigger_type__mutmut_4': xǁWorkflowParserǁ_get_trigger_type__mutmut_4, 
        'xǁWorkflowParserǁ_get_trigger_type__mutmut_5': xǁWorkflowParserǁ_get_trigger_type__mutmut_5, 
        'xǁWorkflowParserǁ_get_trigger_type__mutmut_6': xǁWorkflowParserǁ_get_trigger_type__mutmut_6, 
        'xǁWorkflowParserǁ_get_trigger_type__mutmut_7': xǁWorkflowParserǁ_get_trigger_type__mutmut_7, 
        'xǁWorkflowParserǁ_get_trigger_type__mutmut_8': xǁWorkflowParserǁ_get_trigger_type__mutmut_8, 
        'xǁWorkflowParserǁ_get_trigger_type__mutmut_9': xǁWorkflowParserǁ_get_trigger_type__mutmut_9, 
        'xǁWorkflowParserǁ_get_trigger_type__mutmut_10': xǁWorkflowParserǁ_get_trigger_type__mutmut_10, 
        'xǁWorkflowParserǁ_get_trigger_type__mutmut_11': xǁWorkflowParserǁ_get_trigger_type__mutmut_11, 
        'xǁWorkflowParserǁ_get_trigger_type__mutmut_12': xǁWorkflowParserǁ_get_trigger_type__mutmut_12, 
        'xǁWorkflowParserǁ_get_trigger_type__mutmut_13': xǁWorkflowParserǁ_get_trigger_type__mutmut_13, 
        'xǁWorkflowParserǁ_get_trigger_type__mutmut_14': xǁWorkflowParserǁ_get_trigger_type__mutmut_14, 
        'xǁWorkflowParserǁ_get_trigger_type__mutmut_15': xǁWorkflowParserǁ_get_trigger_type__mutmut_15, 
        'xǁWorkflowParserǁ_get_trigger_type__mutmut_16': xǁWorkflowParserǁ_get_trigger_type__mutmut_16, 
        'xǁWorkflowParserǁ_get_trigger_type__mutmut_17': xǁWorkflowParserǁ_get_trigger_type__mutmut_17, 
        'xǁWorkflowParserǁ_get_trigger_type__mutmut_18': xǁWorkflowParserǁ_get_trigger_type__mutmut_18, 
        'xǁWorkflowParserǁ_get_trigger_type__mutmut_19': xǁWorkflowParserǁ_get_trigger_type__mutmut_19, 
        'xǁWorkflowParserǁ_get_trigger_type__mutmut_20': xǁWorkflowParserǁ_get_trigger_type__mutmut_20, 
        'xǁWorkflowParserǁ_get_trigger_type__mutmut_21': xǁWorkflowParserǁ_get_trigger_type__mutmut_21, 
        'xǁWorkflowParserǁ_get_trigger_type__mutmut_22': xǁWorkflowParserǁ_get_trigger_type__mutmut_22, 
        'xǁWorkflowParserǁ_get_trigger_type__mutmut_23': xǁWorkflowParserǁ_get_trigger_type__mutmut_23, 
        'xǁWorkflowParserǁ_get_trigger_type__mutmut_24': xǁWorkflowParserǁ_get_trigger_type__mutmut_24, 
        'xǁWorkflowParserǁ_get_trigger_type__mutmut_25': xǁWorkflowParserǁ_get_trigger_type__mutmut_25, 
        'xǁWorkflowParserǁ_get_trigger_type__mutmut_26': xǁWorkflowParserǁ_get_trigger_type__mutmut_26, 
        'xǁWorkflowParserǁ_get_trigger_type__mutmut_27': xǁWorkflowParserǁ_get_trigger_type__mutmut_27, 
        'xǁWorkflowParserǁ_get_trigger_type__mutmut_28': xǁWorkflowParserǁ_get_trigger_type__mutmut_28, 
        'xǁWorkflowParserǁ_get_trigger_type__mutmut_29': xǁWorkflowParserǁ_get_trigger_type__mutmut_29, 
        'xǁWorkflowParserǁ_get_trigger_type__mutmut_30': xǁWorkflowParserǁ_get_trigger_type__mutmut_30, 
        'xǁWorkflowParserǁ_get_trigger_type__mutmut_31': xǁWorkflowParserǁ_get_trigger_type__mutmut_31, 
        'xǁWorkflowParserǁ_get_trigger_type__mutmut_32': xǁWorkflowParserǁ_get_trigger_type__mutmut_32, 
        'xǁWorkflowParserǁ_get_trigger_type__mutmut_33': xǁWorkflowParserǁ_get_trigger_type__mutmut_33, 
        'xǁWorkflowParserǁ_get_trigger_type__mutmut_34': xǁWorkflowParserǁ_get_trigger_type__mutmut_34, 
        'xǁWorkflowParserǁ_get_trigger_type__mutmut_35': xǁWorkflowParserǁ_get_trigger_type__mutmut_35, 
        'xǁWorkflowParserǁ_get_trigger_type__mutmut_36': xǁWorkflowParserǁ_get_trigger_type__mutmut_36, 
        'xǁWorkflowParserǁ_get_trigger_type__mutmut_37': xǁWorkflowParserǁ_get_trigger_type__mutmut_37, 
        'xǁWorkflowParserǁ_get_trigger_type__mutmut_38': xǁWorkflowParserǁ_get_trigger_type__mutmut_38, 
        'xǁWorkflowParserǁ_get_trigger_type__mutmut_39': xǁWorkflowParserǁ_get_trigger_type__mutmut_39, 
        'xǁWorkflowParserǁ_get_trigger_type__mutmut_40': xǁWorkflowParserǁ_get_trigger_type__mutmut_40, 
        'xǁWorkflowParserǁ_get_trigger_type__mutmut_41': xǁWorkflowParserǁ_get_trigger_type__mutmut_41
    }
    
    def _get_trigger_type(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁWorkflowParserǁ_get_trigger_type__mutmut_orig"), object.__getattribute__(self, "xǁWorkflowParserǁ_get_trigger_type__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _get_trigger_type.__signature__ = _mutmut_signature(xǁWorkflowParserǁ_get_trigger_type__mutmut_orig)
    xǁWorkflowParserǁ_get_trigger_type__mutmut_orig.__name__ = 'xǁWorkflowParserǁ_get_trigger_type'

    def clear_cache(self) -> None:
        """Clear the parser cache."""
        self._cache.clear()
