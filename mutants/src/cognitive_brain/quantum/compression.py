"""
Pattern Compression for Long-Term Memory Storage

Implements dimensionality reduction and compression for efficient LTM storage.
Target: 60% size reduction while maintaining pattern distinctiveness.

PDA Loop + AfterMath:
- PLAN: Define compression strategy (PCA, quantization)
- DO: Compress patterns for LTM storage
- ASSESS: Measure compression ratio, reconstruction accuracy
- AfterMath: Track storage efficiency, retrieval performance impact

Compression Techniques:
- Feature dimensionality reduction (PCA-based)
- Quantization of continuous values
- Sparse representation for near-zero features
"""

import logging
from dataclasses import dataclass
from typing import Any, Dict, List, Optional

import numpy as np

# Configure logging
logger = logging.getLogger(__name__)


# Constants
DEFAULT_QUANTIZATION_BITS = 8
DEFAULT_SPARSITY_THRESHOLD = 0.01
EPSILON_STABILITY = 1e-8  # For numerical stability in calculations
from inspect import signature as _mutmut_signature
from typing import Annotated
from typing import Callable
from typing import ClassVar


MutantDict = Annotated[dict[str, Callable], "Mutant"]


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg = None):
    """Forward call to original or mutated function, depending on the environment"""
    import os
    mutant_under_test = os.environ['MUTANT_UNDER_TEST']
    if mutant_under_test == 'fail':
        from mutmut.__main__ import MutmutProgrammaticFailException
        raise MutmutProgrammaticFailException('Failed programmatically')      
    elif mutant_under_test == 'stats':
        from mutmut.__main__ import record_trampoline_hit
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__)
        result = orig(*call_args, **call_kwargs)
        return result
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_'
    if not mutant_under_test.startswith(prefix):
        result = orig(*call_args, **call_kwargs)
        return result
    mutant_name = mutant_under_test.rpartition('.')[-1]
    if self_arg is not None:
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs)
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs)
    return result


@dataclass
class CompressedPattern:
    """
    Compressed representation of a memory pattern.

    Stores pattern data in reduced form for efficient LTM storage.

    Attributes:
        pattern_id: Original pattern identifier
        compressed_features: Reduced feature representation
        decision: Preserved decision (not compressed)
        confidence: Preserved confidence (not compressed)
        feature_keys: Ordered list of feature keys for reconstruction
        compression_metadata: Metadata for decompression
    """

    pattern_id: str
    compressed_features: np.ndarray  # Reduced dimensionality vector
    decision: str
    confidence: float
    feature_keys: List[str]
    compression_metadata: Dict[str, Any]

    def get_size_bytes(self) -> int:
        """
        Estimate compressed size in bytes.

        Returns:
            Approximate size in bytes
        """
        # Numpy array size + string sizes + metadata
        array_size = self.compressed_features.nbytes
        string_size = len(self.pattern_id) + len(self.decision)
        string_size += sum(len(k) for k in self.feature_keys)
        metadata_size = sum(len(str(v)) for v in self.compression_metadata.values())

        return array_size + string_size + metadata_size


class PatternCompressor:
    """
    Enhanced pattern compression system for efficient LTM storage.

    Compression Strategy:
    1. Adaptive dimensionality reduction (target: 65% reduction, variance-based)
    2. Variable quantization (4-8 bits based on feature importance)
    3. Sparse representation (remove near-zero features)
    4. Feature importance weighting

    Target: 70% overall size reduction while maintaining >95% reconstruction accuracy.

    Enhancements (Phase 8.1.1):
    - Adaptive PCA: Retain 95% variance instead of fixed 50% dimensions
    - Variable quantization: Use fewer bits for less important features
    - Feature importance scoring for intelligent compression
    """

    def xǁPatternCompressorǁ__init____mutmut_orig(
        self,
        target_dimensions: Optional[int] = None,
        quantization_bits: int = 8,
        sparsity_threshold: float = 0.01,
        variance_threshold: float = 0.95,
    ):
        """
        Initialize enhanced pattern compressor.

        Args:
            target_dimensions: Target reduced dimensionality (None = auto, variance-based)
            quantization_bits: Max bits for value quantization (default: 8-bit)
            sparsity_threshold: Threshold below which features are zeroed (default: 0.01)
            variance_threshold: Variance to retain in adaptive PCA (default: 0.95)
        """
        self.target_dimensions = target_dimensions
        self.quantization_bits = quantization_bits
        self.sparsity_threshold = sparsity_threshold
        self.variance_threshold = variance_threshold

        # Learned compression parameters
        self.feature_mean: Optional[np.ndarray] = None
        self.feature_std: Optional[np.ndarray] = None
        self.projection_matrix: Optional[np.ndarray] = None
        self.feature_importance: Optional[np.ndarray] = None  # New: importance scores
        self.explained_variance_ratio: Optional[np.ndarray] = (
            None  # For variable quantization
        )
        self.is_fitted = False

        # Statistics
        self.total_compressed = 0
        self.total_decompressed = 0
        self.compression_ratios: List[float] = []

    def xǁPatternCompressorǁ__init____mutmut_1(
        self,
        target_dimensions: Optional[int] = None,
        quantization_bits: int = 9,
        sparsity_threshold: float = 0.01,
        variance_threshold: float = 0.95,
    ):
        """
        Initialize enhanced pattern compressor.

        Args:
            target_dimensions: Target reduced dimensionality (None = auto, variance-based)
            quantization_bits: Max bits for value quantization (default: 8-bit)
            sparsity_threshold: Threshold below which features are zeroed (default: 0.01)
            variance_threshold: Variance to retain in adaptive PCA (default: 0.95)
        """
        self.target_dimensions = target_dimensions
        self.quantization_bits = quantization_bits
        self.sparsity_threshold = sparsity_threshold
        self.variance_threshold = variance_threshold

        # Learned compression parameters
        self.feature_mean: Optional[np.ndarray] = None
        self.feature_std: Optional[np.ndarray] = None
        self.projection_matrix: Optional[np.ndarray] = None
        self.feature_importance: Optional[np.ndarray] = None  # New: importance scores
        self.explained_variance_ratio: Optional[np.ndarray] = (
            None  # For variable quantization
        )
        self.is_fitted = False

        # Statistics
        self.total_compressed = 0
        self.total_decompressed = 0
        self.compression_ratios: List[float] = []

    def xǁPatternCompressorǁ__init____mutmut_2(
        self,
        target_dimensions: Optional[int] = None,
        quantization_bits: int = 8,
        sparsity_threshold: float = 1.01,
        variance_threshold: float = 0.95,
    ):
        """
        Initialize enhanced pattern compressor.

        Args:
            target_dimensions: Target reduced dimensionality (None = auto, variance-based)
            quantization_bits: Max bits for value quantization (default: 8-bit)
            sparsity_threshold: Threshold below which features are zeroed (default: 0.01)
            variance_threshold: Variance to retain in adaptive PCA (default: 0.95)
        """
        self.target_dimensions = target_dimensions
        self.quantization_bits = quantization_bits
        self.sparsity_threshold = sparsity_threshold
        self.variance_threshold = variance_threshold

        # Learned compression parameters
        self.feature_mean: Optional[np.ndarray] = None
        self.feature_std: Optional[np.ndarray] = None
        self.projection_matrix: Optional[np.ndarray] = None
        self.feature_importance: Optional[np.ndarray] = None  # New: importance scores
        self.explained_variance_ratio: Optional[np.ndarray] = (
            None  # For variable quantization
        )
        self.is_fitted = False

        # Statistics
        self.total_compressed = 0
        self.total_decompressed = 0
        self.compression_ratios: List[float] = []

    def xǁPatternCompressorǁ__init____mutmut_3(
        self,
        target_dimensions: Optional[int] = None,
        quantization_bits: int = 8,
        sparsity_threshold: float = 0.01,
        variance_threshold: float = 1.95,
    ):
        """
        Initialize enhanced pattern compressor.

        Args:
            target_dimensions: Target reduced dimensionality (None = auto, variance-based)
            quantization_bits: Max bits for value quantization (default: 8-bit)
            sparsity_threshold: Threshold below which features are zeroed (default: 0.01)
            variance_threshold: Variance to retain in adaptive PCA (default: 0.95)
        """
        self.target_dimensions = target_dimensions
        self.quantization_bits = quantization_bits
        self.sparsity_threshold = sparsity_threshold
        self.variance_threshold = variance_threshold

        # Learned compression parameters
        self.feature_mean: Optional[np.ndarray] = None
        self.feature_std: Optional[np.ndarray] = None
        self.projection_matrix: Optional[np.ndarray] = None
        self.feature_importance: Optional[np.ndarray] = None  # New: importance scores
        self.explained_variance_ratio: Optional[np.ndarray] = (
            None  # For variable quantization
        )
        self.is_fitted = False

        # Statistics
        self.total_compressed = 0
        self.total_decompressed = 0
        self.compression_ratios: List[float] = []

    def xǁPatternCompressorǁ__init____mutmut_4(
        self,
        target_dimensions: Optional[int] = None,
        quantization_bits: int = 8,
        sparsity_threshold: float = 0.01,
        variance_threshold: float = 0.95,
    ):
        """
        Initialize enhanced pattern compressor.

        Args:
            target_dimensions: Target reduced dimensionality (None = auto, variance-based)
            quantization_bits: Max bits for value quantization (default: 8-bit)
            sparsity_threshold: Threshold below which features are zeroed (default: 0.01)
            variance_threshold: Variance to retain in adaptive PCA (default: 0.95)
        """
        self.target_dimensions = None
        self.quantization_bits = quantization_bits
        self.sparsity_threshold = sparsity_threshold
        self.variance_threshold = variance_threshold

        # Learned compression parameters
        self.feature_mean: Optional[np.ndarray] = None
        self.feature_std: Optional[np.ndarray] = None
        self.projection_matrix: Optional[np.ndarray] = None
        self.feature_importance: Optional[np.ndarray] = None  # New: importance scores
        self.explained_variance_ratio: Optional[np.ndarray] = (
            None  # For variable quantization
        )
        self.is_fitted = False

        # Statistics
        self.total_compressed = 0
        self.total_decompressed = 0
        self.compression_ratios: List[float] = []

    def xǁPatternCompressorǁ__init____mutmut_5(
        self,
        target_dimensions: Optional[int] = None,
        quantization_bits: int = 8,
        sparsity_threshold: float = 0.01,
        variance_threshold: float = 0.95,
    ):
        """
        Initialize enhanced pattern compressor.

        Args:
            target_dimensions: Target reduced dimensionality (None = auto, variance-based)
            quantization_bits: Max bits for value quantization (default: 8-bit)
            sparsity_threshold: Threshold below which features are zeroed (default: 0.01)
            variance_threshold: Variance to retain in adaptive PCA (default: 0.95)
        """
        self.target_dimensions = target_dimensions
        self.quantization_bits = None
        self.sparsity_threshold = sparsity_threshold
        self.variance_threshold = variance_threshold

        # Learned compression parameters
        self.feature_mean: Optional[np.ndarray] = None
        self.feature_std: Optional[np.ndarray] = None
        self.projection_matrix: Optional[np.ndarray] = None
        self.feature_importance: Optional[np.ndarray] = None  # New: importance scores
        self.explained_variance_ratio: Optional[np.ndarray] = (
            None  # For variable quantization
        )
        self.is_fitted = False

        # Statistics
        self.total_compressed = 0
        self.total_decompressed = 0
        self.compression_ratios: List[float] = []

    def xǁPatternCompressorǁ__init____mutmut_6(
        self,
        target_dimensions: Optional[int] = None,
        quantization_bits: int = 8,
        sparsity_threshold: float = 0.01,
        variance_threshold: float = 0.95,
    ):
        """
        Initialize enhanced pattern compressor.

        Args:
            target_dimensions: Target reduced dimensionality (None = auto, variance-based)
            quantization_bits: Max bits for value quantization (default: 8-bit)
            sparsity_threshold: Threshold below which features are zeroed (default: 0.01)
            variance_threshold: Variance to retain in adaptive PCA (default: 0.95)
        """
        self.target_dimensions = target_dimensions
        self.quantization_bits = quantization_bits
        self.sparsity_threshold = None
        self.variance_threshold = variance_threshold

        # Learned compression parameters
        self.feature_mean: Optional[np.ndarray] = None
        self.feature_std: Optional[np.ndarray] = None
        self.projection_matrix: Optional[np.ndarray] = None
        self.feature_importance: Optional[np.ndarray] = None  # New: importance scores
        self.explained_variance_ratio: Optional[np.ndarray] = (
            None  # For variable quantization
        )
        self.is_fitted = False

        # Statistics
        self.total_compressed = 0
        self.total_decompressed = 0
        self.compression_ratios: List[float] = []

    def xǁPatternCompressorǁ__init____mutmut_7(
        self,
        target_dimensions: Optional[int] = None,
        quantization_bits: int = 8,
        sparsity_threshold: float = 0.01,
        variance_threshold: float = 0.95,
    ):
        """
        Initialize enhanced pattern compressor.

        Args:
            target_dimensions: Target reduced dimensionality (None = auto, variance-based)
            quantization_bits: Max bits for value quantization (default: 8-bit)
            sparsity_threshold: Threshold below which features are zeroed (default: 0.01)
            variance_threshold: Variance to retain in adaptive PCA (default: 0.95)
        """
        self.target_dimensions = target_dimensions
        self.quantization_bits = quantization_bits
        self.sparsity_threshold = sparsity_threshold
        self.variance_threshold = None

        # Learned compression parameters
        self.feature_mean: Optional[np.ndarray] = None
        self.feature_std: Optional[np.ndarray] = None
        self.projection_matrix: Optional[np.ndarray] = None
        self.feature_importance: Optional[np.ndarray] = None  # New: importance scores
        self.explained_variance_ratio: Optional[np.ndarray] = (
            None  # For variable quantization
        )
        self.is_fitted = False

        # Statistics
        self.total_compressed = 0
        self.total_decompressed = 0
        self.compression_ratios: List[float] = []

    def xǁPatternCompressorǁ__init____mutmut_8(
        self,
        target_dimensions: Optional[int] = None,
        quantization_bits: int = 8,
        sparsity_threshold: float = 0.01,
        variance_threshold: float = 0.95,
    ):
        """
        Initialize enhanced pattern compressor.

        Args:
            target_dimensions: Target reduced dimensionality (None = auto, variance-based)
            quantization_bits: Max bits for value quantization (default: 8-bit)
            sparsity_threshold: Threshold below which features are zeroed (default: 0.01)
            variance_threshold: Variance to retain in adaptive PCA (default: 0.95)
        """
        self.target_dimensions = target_dimensions
        self.quantization_bits = quantization_bits
        self.sparsity_threshold = sparsity_threshold
        self.variance_threshold = variance_threshold

        # Learned compression parameters
        self.feature_mean: Optional[np.ndarray] = ""
        self.feature_std: Optional[np.ndarray] = None
        self.projection_matrix: Optional[np.ndarray] = None
        self.feature_importance: Optional[np.ndarray] = None  # New: importance scores
        self.explained_variance_ratio: Optional[np.ndarray] = (
            None  # For variable quantization
        )
        self.is_fitted = False

        # Statistics
        self.total_compressed = 0
        self.total_decompressed = 0
        self.compression_ratios: List[float] = []

    def xǁPatternCompressorǁ__init____mutmut_9(
        self,
        target_dimensions: Optional[int] = None,
        quantization_bits: int = 8,
        sparsity_threshold: float = 0.01,
        variance_threshold: float = 0.95,
    ):
        """
        Initialize enhanced pattern compressor.

        Args:
            target_dimensions: Target reduced dimensionality (None = auto, variance-based)
            quantization_bits: Max bits for value quantization (default: 8-bit)
            sparsity_threshold: Threshold below which features are zeroed (default: 0.01)
            variance_threshold: Variance to retain in adaptive PCA (default: 0.95)
        """
        self.target_dimensions = target_dimensions
        self.quantization_bits = quantization_bits
        self.sparsity_threshold = sparsity_threshold
        self.variance_threshold = variance_threshold

        # Learned compression parameters
        self.feature_mean: Optional[np.ndarray] = None
        self.feature_std: Optional[np.ndarray] = ""
        self.projection_matrix: Optional[np.ndarray] = None
        self.feature_importance: Optional[np.ndarray] = None  # New: importance scores
        self.explained_variance_ratio: Optional[np.ndarray] = (
            None  # For variable quantization
        )
        self.is_fitted = False

        # Statistics
        self.total_compressed = 0
        self.total_decompressed = 0
        self.compression_ratios: List[float] = []

    def xǁPatternCompressorǁ__init____mutmut_10(
        self,
        target_dimensions: Optional[int] = None,
        quantization_bits: int = 8,
        sparsity_threshold: float = 0.01,
        variance_threshold: float = 0.95,
    ):
        """
        Initialize enhanced pattern compressor.

        Args:
            target_dimensions: Target reduced dimensionality (None = auto, variance-based)
            quantization_bits: Max bits for value quantization (default: 8-bit)
            sparsity_threshold: Threshold below which features are zeroed (default: 0.01)
            variance_threshold: Variance to retain in adaptive PCA (default: 0.95)
        """
        self.target_dimensions = target_dimensions
        self.quantization_bits = quantization_bits
        self.sparsity_threshold = sparsity_threshold
        self.variance_threshold = variance_threshold

        # Learned compression parameters
        self.feature_mean: Optional[np.ndarray] = None
        self.feature_std: Optional[np.ndarray] = None
        self.projection_matrix: Optional[np.ndarray] = ""
        self.feature_importance: Optional[np.ndarray] = None  # New: importance scores
        self.explained_variance_ratio: Optional[np.ndarray] = (
            None  # For variable quantization
        )
        self.is_fitted = False

        # Statistics
        self.total_compressed = 0
        self.total_decompressed = 0
        self.compression_ratios: List[float] = []

    def xǁPatternCompressorǁ__init____mutmut_11(
        self,
        target_dimensions: Optional[int] = None,
        quantization_bits: int = 8,
        sparsity_threshold: float = 0.01,
        variance_threshold: float = 0.95,
    ):
        """
        Initialize enhanced pattern compressor.

        Args:
            target_dimensions: Target reduced dimensionality (None = auto, variance-based)
            quantization_bits: Max bits for value quantization (default: 8-bit)
            sparsity_threshold: Threshold below which features are zeroed (default: 0.01)
            variance_threshold: Variance to retain in adaptive PCA (default: 0.95)
        """
        self.target_dimensions = target_dimensions
        self.quantization_bits = quantization_bits
        self.sparsity_threshold = sparsity_threshold
        self.variance_threshold = variance_threshold

        # Learned compression parameters
        self.feature_mean: Optional[np.ndarray] = None
        self.feature_std: Optional[np.ndarray] = None
        self.projection_matrix: Optional[np.ndarray] = None
        self.feature_importance: Optional[np.ndarray] = ""  # New: importance scores
        self.explained_variance_ratio: Optional[np.ndarray] = (
            None  # For variable quantization
        )
        self.is_fitted = False

        # Statistics
        self.total_compressed = 0
        self.total_decompressed = 0
        self.compression_ratios: List[float] = []

    def xǁPatternCompressorǁ__init____mutmut_12(
        self,
        target_dimensions: Optional[int] = None,
        quantization_bits: int = 8,
        sparsity_threshold: float = 0.01,
        variance_threshold: float = 0.95,
    ):
        """
        Initialize enhanced pattern compressor.

        Args:
            target_dimensions: Target reduced dimensionality (None = auto, variance-based)
            quantization_bits: Max bits for value quantization (default: 8-bit)
            sparsity_threshold: Threshold below which features are zeroed (default: 0.01)
            variance_threshold: Variance to retain in adaptive PCA (default: 0.95)
        """
        self.target_dimensions = target_dimensions
        self.quantization_bits = quantization_bits
        self.sparsity_threshold = sparsity_threshold
        self.variance_threshold = variance_threshold

        # Learned compression parameters
        self.feature_mean: Optional[np.ndarray] = None
        self.feature_std: Optional[np.ndarray] = None
        self.projection_matrix: Optional[np.ndarray] = None
        self.feature_importance: Optional[np.ndarray] = None  # New: importance scores
        self.explained_variance_ratio: Optional[np.ndarray] = ""
        self.is_fitted = False

        # Statistics
        self.total_compressed = 0
        self.total_decompressed = 0
        self.compression_ratios: List[float] = []

    def xǁPatternCompressorǁ__init____mutmut_13(
        self,
        target_dimensions: Optional[int] = None,
        quantization_bits: int = 8,
        sparsity_threshold: float = 0.01,
        variance_threshold: float = 0.95,
    ):
        """
        Initialize enhanced pattern compressor.

        Args:
            target_dimensions: Target reduced dimensionality (None = auto, variance-based)
            quantization_bits: Max bits for value quantization (default: 8-bit)
            sparsity_threshold: Threshold below which features are zeroed (default: 0.01)
            variance_threshold: Variance to retain in adaptive PCA (default: 0.95)
        """
        self.target_dimensions = target_dimensions
        self.quantization_bits = quantization_bits
        self.sparsity_threshold = sparsity_threshold
        self.variance_threshold = variance_threshold

        # Learned compression parameters
        self.feature_mean: Optional[np.ndarray] = None
        self.feature_std: Optional[np.ndarray] = None
        self.projection_matrix: Optional[np.ndarray] = None
        self.feature_importance: Optional[np.ndarray] = None  # New: importance scores
        self.explained_variance_ratio: Optional[np.ndarray] = (
            None  # For variable quantization
        )
        self.is_fitted = None

        # Statistics
        self.total_compressed = 0
        self.total_decompressed = 0
        self.compression_ratios: List[float] = []

    def xǁPatternCompressorǁ__init____mutmut_14(
        self,
        target_dimensions: Optional[int] = None,
        quantization_bits: int = 8,
        sparsity_threshold: float = 0.01,
        variance_threshold: float = 0.95,
    ):
        """
        Initialize enhanced pattern compressor.

        Args:
            target_dimensions: Target reduced dimensionality (None = auto, variance-based)
            quantization_bits: Max bits for value quantization (default: 8-bit)
            sparsity_threshold: Threshold below which features are zeroed (default: 0.01)
            variance_threshold: Variance to retain in adaptive PCA (default: 0.95)
        """
        self.target_dimensions = target_dimensions
        self.quantization_bits = quantization_bits
        self.sparsity_threshold = sparsity_threshold
        self.variance_threshold = variance_threshold

        # Learned compression parameters
        self.feature_mean: Optional[np.ndarray] = None
        self.feature_std: Optional[np.ndarray] = None
        self.projection_matrix: Optional[np.ndarray] = None
        self.feature_importance: Optional[np.ndarray] = None  # New: importance scores
        self.explained_variance_ratio: Optional[np.ndarray] = (
            None  # For variable quantization
        )
        self.is_fitted = True

        # Statistics
        self.total_compressed = 0
        self.total_decompressed = 0
        self.compression_ratios: List[float] = []

    def xǁPatternCompressorǁ__init____mutmut_15(
        self,
        target_dimensions: Optional[int] = None,
        quantization_bits: int = 8,
        sparsity_threshold: float = 0.01,
        variance_threshold: float = 0.95,
    ):
        """
        Initialize enhanced pattern compressor.

        Args:
            target_dimensions: Target reduced dimensionality (None = auto, variance-based)
            quantization_bits: Max bits for value quantization (default: 8-bit)
            sparsity_threshold: Threshold below which features are zeroed (default: 0.01)
            variance_threshold: Variance to retain in adaptive PCA (default: 0.95)
        """
        self.target_dimensions = target_dimensions
        self.quantization_bits = quantization_bits
        self.sparsity_threshold = sparsity_threshold
        self.variance_threshold = variance_threshold

        # Learned compression parameters
        self.feature_mean: Optional[np.ndarray] = None
        self.feature_std: Optional[np.ndarray] = None
        self.projection_matrix: Optional[np.ndarray] = None
        self.feature_importance: Optional[np.ndarray] = None  # New: importance scores
        self.explained_variance_ratio: Optional[np.ndarray] = (
            None  # For variable quantization
        )
        self.is_fitted = False

        # Statistics
        self.total_compressed = None
        self.total_decompressed = 0
        self.compression_ratios: List[float] = []

    def xǁPatternCompressorǁ__init____mutmut_16(
        self,
        target_dimensions: Optional[int] = None,
        quantization_bits: int = 8,
        sparsity_threshold: float = 0.01,
        variance_threshold: float = 0.95,
    ):
        """
        Initialize enhanced pattern compressor.

        Args:
            target_dimensions: Target reduced dimensionality (None = auto, variance-based)
            quantization_bits: Max bits for value quantization (default: 8-bit)
            sparsity_threshold: Threshold below which features are zeroed (default: 0.01)
            variance_threshold: Variance to retain in adaptive PCA (default: 0.95)
        """
        self.target_dimensions = target_dimensions
        self.quantization_bits = quantization_bits
        self.sparsity_threshold = sparsity_threshold
        self.variance_threshold = variance_threshold

        # Learned compression parameters
        self.feature_mean: Optional[np.ndarray] = None
        self.feature_std: Optional[np.ndarray] = None
        self.projection_matrix: Optional[np.ndarray] = None
        self.feature_importance: Optional[np.ndarray] = None  # New: importance scores
        self.explained_variance_ratio: Optional[np.ndarray] = (
            None  # For variable quantization
        )
        self.is_fitted = False

        # Statistics
        self.total_compressed = 1
        self.total_decompressed = 0
        self.compression_ratios: List[float] = []

    def xǁPatternCompressorǁ__init____mutmut_17(
        self,
        target_dimensions: Optional[int] = None,
        quantization_bits: int = 8,
        sparsity_threshold: float = 0.01,
        variance_threshold: float = 0.95,
    ):
        """
        Initialize enhanced pattern compressor.

        Args:
            target_dimensions: Target reduced dimensionality (None = auto, variance-based)
            quantization_bits: Max bits for value quantization (default: 8-bit)
            sparsity_threshold: Threshold below which features are zeroed (default: 0.01)
            variance_threshold: Variance to retain in adaptive PCA (default: 0.95)
        """
        self.target_dimensions = target_dimensions
        self.quantization_bits = quantization_bits
        self.sparsity_threshold = sparsity_threshold
        self.variance_threshold = variance_threshold

        # Learned compression parameters
        self.feature_mean: Optional[np.ndarray] = None
        self.feature_std: Optional[np.ndarray] = None
        self.projection_matrix: Optional[np.ndarray] = None
        self.feature_importance: Optional[np.ndarray] = None  # New: importance scores
        self.explained_variance_ratio: Optional[np.ndarray] = (
            None  # For variable quantization
        )
        self.is_fitted = False

        # Statistics
        self.total_compressed = 0
        self.total_decompressed = None
        self.compression_ratios: List[float] = []

    def xǁPatternCompressorǁ__init____mutmut_18(
        self,
        target_dimensions: Optional[int] = None,
        quantization_bits: int = 8,
        sparsity_threshold: float = 0.01,
        variance_threshold: float = 0.95,
    ):
        """
        Initialize enhanced pattern compressor.

        Args:
            target_dimensions: Target reduced dimensionality (None = auto, variance-based)
            quantization_bits: Max bits for value quantization (default: 8-bit)
            sparsity_threshold: Threshold below which features are zeroed (default: 0.01)
            variance_threshold: Variance to retain in adaptive PCA (default: 0.95)
        """
        self.target_dimensions = target_dimensions
        self.quantization_bits = quantization_bits
        self.sparsity_threshold = sparsity_threshold
        self.variance_threshold = variance_threshold

        # Learned compression parameters
        self.feature_mean: Optional[np.ndarray] = None
        self.feature_std: Optional[np.ndarray] = None
        self.projection_matrix: Optional[np.ndarray] = None
        self.feature_importance: Optional[np.ndarray] = None  # New: importance scores
        self.explained_variance_ratio: Optional[np.ndarray] = (
            None  # For variable quantization
        )
        self.is_fitted = False

        # Statistics
        self.total_compressed = 0
        self.total_decompressed = 1
        self.compression_ratios: List[float] = []

    def xǁPatternCompressorǁ__init____mutmut_19(
        self,
        target_dimensions: Optional[int] = None,
        quantization_bits: int = 8,
        sparsity_threshold: float = 0.01,
        variance_threshold: float = 0.95,
    ):
        """
        Initialize enhanced pattern compressor.

        Args:
            target_dimensions: Target reduced dimensionality (None = auto, variance-based)
            quantization_bits: Max bits for value quantization (default: 8-bit)
            sparsity_threshold: Threshold below which features are zeroed (default: 0.01)
            variance_threshold: Variance to retain in adaptive PCA (default: 0.95)
        """
        self.target_dimensions = target_dimensions
        self.quantization_bits = quantization_bits
        self.sparsity_threshold = sparsity_threshold
        self.variance_threshold = variance_threshold

        # Learned compression parameters
        self.feature_mean: Optional[np.ndarray] = None
        self.feature_std: Optional[np.ndarray] = None
        self.projection_matrix: Optional[np.ndarray] = None
        self.feature_importance: Optional[np.ndarray] = None  # New: importance scores
        self.explained_variance_ratio: Optional[np.ndarray] = (
            None  # For variable quantization
        )
        self.is_fitted = False

        # Statistics
        self.total_compressed = 0
        self.total_decompressed = 0
        self.compression_ratios: List[float] = None
    
    xǁPatternCompressorǁ__init____mutmut_mutants : ClassVar[MutantDict] = {
    'xǁPatternCompressorǁ__init____mutmut_1': xǁPatternCompressorǁ__init____mutmut_1, 
        'xǁPatternCompressorǁ__init____mutmut_2': xǁPatternCompressorǁ__init____mutmut_2, 
        'xǁPatternCompressorǁ__init____mutmut_3': xǁPatternCompressorǁ__init____mutmut_3, 
        'xǁPatternCompressorǁ__init____mutmut_4': xǁPatternCompressorǁ__init____mutmut_4, 
        'xǁPatternCompressorǁ__init____mutmut_5': xǁPatternCompressorǁ__init____mutmut_5, 
        'xǁPatternCompressorǁ__init____mutmut_6': xǁPatternCompressorǁ__init____mutmut_6, 
        'xǁPatternCompressorǁ__init____mutmut_7': xǁPatternCompressorǁ__init____mutmut_7, 
        'xǁPatternCompressorǁ__init____mutmut_8': xǁPatternCompressorǁ__init____mutmut_8, 
        'xǁPatternCompressorǁ__init____mutmut_9': xǁPatternCompressorǁ__init____mutmut_9, 
        'xǁPatternCompressorǁ__init____mutmut_10': xǁPatternCompressorǁ__init____mutmut_10, 
        'xǁPatternCompressorǁ__init____mutmut_11': xǁPatternCompressorǁ__init____mutmut_11, 
        'xǁPatternCompressorǁ__init____mutmut_12': xǁPatternCompressorǁ__init____mutmut_12, 
        'xǁPatternCompressorǁ__init____mutmut_13': xǁPatternCompressorǁ__init____mutmut_13, 
        'xǁPatternCompressorǁ__init____mutmut_14': xǁPatternCompressorǁ__init____mutmut_14, 
        'xǁPatternCompressorǁ__init____mutmut_15': xǁPatternCompressorǁ__init____mutmut_15, 
        'xǁPatternCompressorǁ__init____mutmut_16': xǁPatternCompressorǁ__init____mutmut_16, 
        'xǁPatternCompressorǁ__init____mutmut_17': xǁPatternCompressorǁ__init____mutmut_17, 
        'xǁPatternCompressorǁ__init____mutmut_18': xǁPatternCompressorǁ__init____mutmut_18, 
        'xǁPatternCompressorǁ__init____mutmut_19': xǁPatternCompressorǁ__init____mutmut_19
    }
    
    def __init__(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁPatternCompressorǁ__init____mutmut_orig"), object.__getattribute__(self, "xǁPatternCompressorǁ__init____mutmut_mutants"), args, kwargs, self)
        return result 
    
    __init__.__signature__ = _mutmut_signature(xǁPatternCompressorǁ__init____mutmut_orig)
    xǁPatternCompressorǁ__init____mutmut_orig.__name__ = 'xǁPatternCompressorǁ__init__'

    def xǁPatternCompressorǁfit__mutmut_orig(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_1(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_2(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError(None)

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_3(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("XXCannot fit on empty pattern listXX")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_4(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_5(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("CANNOT FIT ON EMPTY PATTERN LIST")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_6(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = None
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_7(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(None)
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_8(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[1].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_9(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = None

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_10(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(None)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_11(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(None, start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_12(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=None):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_13(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_14(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], ):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_15(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[2:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_16(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=2):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_17(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = None
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_18(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(None)
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_19(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys == base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_20(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = None
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_21(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(None)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_22(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set + current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_23(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = None
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_24(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(None)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_25(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys + base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_26(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    None
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_27(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = None

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_28(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array(None)

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_29(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(None, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_30(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, None) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_31(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_32(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, ) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_33(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 1.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_34(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = None

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_35(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = None
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_36(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(None, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_37(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=None)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_38(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_39(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, )
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_40(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=1)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_41(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = None

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_42(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(None, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_43(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=None)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_44(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_45(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, )

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_46(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=1)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_47(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = None

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_48(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            None,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_49(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            None,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_50(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            None,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_51(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_52(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_53(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_54(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw <= EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_55(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            2.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_56(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw - EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_57(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = None

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_58(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) * self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_59(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X + self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_60(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = None

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_61(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(None)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_62(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = None

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_63(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(None)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_64(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = None
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_65(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::+1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_66(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-2]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_67(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = None
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_68(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = None

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_69(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = None

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_70(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues * eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_71(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is not None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_72(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = None
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_73(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(None)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_74(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = None
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_75(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) - 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_76(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(None) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_77(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance > self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_78(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 2
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_79(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = None
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_80(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 2
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_81(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = None  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_82(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(None, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_83(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, None)  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_84(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_85(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, )  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_86(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(2, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_87(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(None))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_88(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] / 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_89(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[2] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_90(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 1.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_91(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = None

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_92(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                None, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_93(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, None
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_94(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_95(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_96(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(None, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_97(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, None)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_98(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_99(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, )
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_100(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = None
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_101(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = None
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_102(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(None, axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_103(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=None)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_104(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_105(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), )
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_106(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(None), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_107(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=2)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_108(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance = self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_109(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance *= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_110(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = None

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_111(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = None
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_112(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = None
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_113(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) / 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_114(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 + self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_115(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (2 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_116(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions * X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_117(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[2]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_118(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 101
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_119(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            None
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_120(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[2]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_121(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = None

        self.is_fitted = True

    def xǁPatternCompressorǁfit__mutmut_122(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = None

    def xǁPatternCompressorǁfit__mutmut_123(self, patterns: List[Dict[str, float]]) -> None:
        """
        Fit compression model to pattern data.

        Learns PCA projection matrix for dimensionality reduction.

        Args:
            patterns: List of feature dicts to learn from. All patterns must share
                the exact same set of feature keys; patterns with mismatched keys
                will cause a ValueError to be raised.
        """
        if not patterns:
            raise ValueError("Cannot fit on empty pattern list")

        # Convert patterns to matrix and validate key consistency
        feature_keys = sorted(patterns[0].keys())
        base_keys_set = set(feature_keys)

        for idx, p in enumerate(patterns[1:], start=1):
            current_keys = set(p.keys())
            if current_keys != base_keys_set:
                missing = sorted(base_keys_set - current_keys)
                extra = sorted(current_keys - base_keys_set)
                raise ValueError(
                    f"All patterns must share the same feature keys. "
                    f"Pattern at index {idx} has mismatched keys. "
                    f"Missing keys: {missing}; Extra keys: {extra}"
                )

        X = np.array([[p.get(k, 0.0) for k in feature_keys] for p in patterns])

        # Store feature keys for later use
        self.feature_keys = feature_keys

        # Calculate statistics
        self.feature_mean = np.mean(X, axis=0)
        feature_std_raw = np.std(X, axis=0)

        # Handle zero-variance features
        # Features with zero variance get std=1.0 to avoid division issues
        # (they won't contribute to compression anyway)
        self.feature_std = np.where(
            feature_std_raw < EPSILON_STABILITY,
            1.0,  # Set to 1.0 for zero-variance features
            feature_std_raw + EPSILON_STABILITY,  # Add epsilon for numerical stability
        )

        # Normalize
        X_norm = (X - self.feature_mean) / self.feature_std

        # Compute covariance matrix
        cov_matrix = np.cov(X_norm.T)

        # Eigen decomposition
        eigenvalues, eigenvectors = np.linalg.eigh(cov_matrix)

        # Sort by eigenvalues (descending)
        # Reorder eigenvalues to match sorted eigenvectors
        idx = eigenvalues.argsort()[::-1]
        eigenvalues = eigenvalues[idx]
        eigenvectors = eigenvectors[:, idx]

        # Calculate explained variance for adaptive dimension selection
        explained_variance_ratio = eigenvalues / eigenvalues.sum()

        # ENHANCEMENT: Adaptive PCA based on variance threshold
        if self.target_dimensions is None:
            # Find minimum dimensions to retain variance_threshold (default 95%)
            cumulative_variance = np.cumsum(explained_variance_ratio)
            self.target_dimensions = (
                np.argmax(cumulative_variance >= self.variance_threshold) + 1
            )
            # Ensure at least 1 dimension and at most 65% reduction (35% of original)
            min_dims = 1
            max_dims = max(1, int(X.shape[1] * 0.35))  # 65% reduction target
            self.target_dimensions = max(
                min_dims, min(self.target_dimensions, max_dims)
            )

        # Calculate feature importance scores based on loadings
        # Higher importance = more contribution to top principal components
        loadings = eigenvectors[:, : self.target_dimensions]
        self.feature_importance = np.sum(np.abs(loadings), axis=1)
        self.feature_importance /= self.feature_importance.sum()  # Normalize

        # Store explained variance for variable quantization
        self.explained_variance_ratio = explained_variance_ratio[
            : self.target_dimensions
        ]

        # Log compression info
        actual_variance = self.explained_variance_ratio.sum()
        reduction_pct = (1 - self.target_dimensions / X.shape[1]) * 100
        logger.info(
            f"Enhanced compression: {X.shape[1]}→{self.target_dimensions} dimensions "
            f"({reduction_pct:.1f}% reduction), retaining {actual_variance:.1%} variance"
        )

        # Select top principal components
        self.projection_matrix = eigenvectors[:, : self.target_dimensions]

        self.is_fitted = False
    
    xǁPatternCompressorǁfit__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁPatternCompressorǁfit__mutmut_1': xǁPatternCompressorǁfit__mutmut_1, 
        'xǁPatternCompressorǁfit__mutmut_2': xǁPatternCompressorǁfit__mutmut_2, 
        'xǁPatternCompressorǁfit__mutmut_3': xǁPatternCompressorǁfit__mutmut_3, 
        'xǁPatternCompressorǁfit__mutmut_4': xǁPatternCompressorǁfit__mutmut_4, 
        'xǁPatternCompressorǁfit__mutmut_5': xǁPatternCompressorǁfit__mutmut_5, 
        'xǁPatternCompressorǁfit__mutmut_6': xǁPatternCompressorǁfit__mutmut_6, 
        'xǁPatternCompressorǁfit__mutmut_7': xǁPatternCompressorǁfit__mutmut_7, 
        'xǁPatternCompressorǁfit__mutmut_8': xǁPatternCompressorǁfit__mutmut_8, 
        'xǁPatternCompressorǁfit__mutmut_9': xǁPatternCompressorǁfit__mutmut_9, 
        'xǁPatternCompressorǁfit__mutmut_10': xǁPatternCompressorǁfit__mutmut_10, 
        'xǁPatternCompressorǁfit__mutmut_11': xǁPatternCompressorǁfit__mutmut_11, 
        'xǁPatternCompressorǁfit__mutmut_12': xǁPatternCompressorǁfit__mutmut_12, 
        'xǁPatternCompressorǁfit__mutmut_13': xǁPatternCompressorǁfit__mutmut_13, 
        'xǁPatternCompressorǁfit__mutmut_14': xǁPatternCompressorǁfit__mutmut_14, 
        'xǁPatternCompressorǁfit__mutmut_15': xǁPatternCompressorǁfit__mutmut_15, 
        'xǁPatternCompressorǁfit__mutmut_111': xǁPatternCompressorǁfit__mutmut_111, 
        'xǁPatternCompressorǁfit__mutmut_112': xǁPatternCompressorǁfit__mutmut_112, 
        'xǁPatternCompressorǁfit__mutmut_113': xǁPatternCompressorǁfit__mutmut_113, 
        'xǁPatternCompressorǁfit__mutmut_114': xǁPatternCompressorǁfit__mutmut_114, 
        'xǁPatternCompressorǁfit__mutmut_115': xǁPatternCompressorǁfit__mutmut_115, 
        'xǁPatternCompressorǁfit__mutmut_116': xǁPatternCompressorǁfit__mutmut_116, 
        'xǁPatternCompressorǁfit__mutmut_117': xǁPatternCompressorǁfit__mutmut_117, 
        'xǁPatternCompressorǁfit__mutmut_118': xǁPatternCompressorǁfit__mutmut_118, 
        'xǁPatternCompressorǁfit__mutmut_119': xǁPatternCompressorǁfit__mutmut_119, 
        'xǁPatternCompressorǁfit__mutmut_120': xǁPatternCompressorǁfit__mutmut_120, 
        'xǁPatternCompressorǁfit__mutmut_121': xǁPatternCompressorǁfit__mutmut_121, 
        'xǁPatternCompressorǁfit__mutmut_122': xǁPatternCompressorǁfit__mutmut_122, 
        'xǁPatternCompressorǁfit__mutmut_123': xǁPatternCompressorǁfit__mutmut_123
    }
    
    def fit(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁPatternCompressorǁfit__mutmut_orig"), object.__getattribute__(self, "xǁPatternCompressorǁfit__mutmut_mutants"), args, kwargs, self)
        return result 
    
    fit.__signature__ = _mutmut_signature(xǁPatternCompressorǁfit__mutmut_orig)
    xǁPatternCompressorǁfit__mutmut_orig.__name__ = 'xǁPatternCompressorǁfit'

    def xǁPatternCompressorǁget_importance_scores__mutmut_orig(self) -> Dict[str, float]:
        """
        Get feature importance scores after fitting.

        Returns:
            Dictionary mapping feature names to importance scores (0.0-1.0)

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted or self.feature_importance is None:
            raise RuntimeError(
                "Compressor must be fitted before getting importance scores"
            )

        return {
            feature: importance
            for feature, importance in zip(self.feature_keys, self.feature_importance)
        }

    def xǁPatternCompressorǁget_importance_scores__mutmut_1(self) -> Dict[str, float]:
        """
        Get feature importance scores after fitting.

        Returns:
            Dictionary mapping feature names to importance scores (0.0-1.0)

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted and self.feature_importance is None:
            raise RuntimeError(
                "Compressor must be fitted before getting importance scores"
            )

        return {
            feature: importance
            for feature, importance in zip(self.feature_keys, self.feature_importance)
        }

    def xǁPatternCompressorǁget_importance_scores__mutmut_2(self) -> Dict[str, float]:
        """
        Get feature importance scores after fitting.

        Returns:
            Dictionary mapping feature names to importance scores (0.0-1.0)

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if self.is_fitted or self.feature_importance is None:
            raise RuntimeError(
                "Compressor must be fitted before getting importance scores"
            )

        return {
            feature: importance
            for feature, importance in zip(self.feature_keys, self.feature_importance)
        }

    def xǁPatternCompressorǁget_importance_scores__mutmut_3(self) -> Dict[str, float]:
        """
        Get feature importance scores after fitting.

        Returns:
            Dictionary mapping feature names to importance scores (0.0-1.0)

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted or self.feature_importance is not None:
            raise RuntimeError(
                "Compressor must be fitted before getting importance scores"
            )

        return {
            feature: importance
            for feature, importance in zip(self.feature_keys, self.feature_importance)
        }

    def xǁPatternCompressorǁget_importance_scores__mutmut_4(self) -> Dict[str, float]:
        """
        Get feature importance scores after fitting.

        Returns:
            Dictionary mapping feature names to importance scores (0.0-1.0)

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted or self.feature_importance is None:
            raise RuntimeError(
                None
            )

        return {
            feature: importance
            for feature, importance in zip(self.feature_keys, self.feature_importance)
        }

    def xǁPatternCompressorǁget_importance_scores__mutmut_5(self) -> Dict[str, float]:
        """
        Get feature importance scores after fitting.

        Returns:
            Dictionary mapping feature names to importance scores (0.0-1.0)

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted or self.feature_importance is None:
            raise RuntimeError(
                "XXCompressor must be fitted before getting importance scoresXX"
            )

        return {
            feature: importance
            for feature, importance in zip(self.feature_keys, self.feature_importance)
        }

    def xǁPatternCompressorǁget_importance_scores__mutmut_6(self) -> Dict[str, float]:
        """
        Get feature importance scores after fitting.

        Returns:
            Dictionary mapping feature names to importance scores (0.0-1.0)

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted or self.feature_importance is None:
            raise RuntimeError(
                "compressor must be fitted before getting importance scores"
            )

        return {
            feature: importance
            for feature, importance in zip(self.feature_keys, self.feature_importance)
        }

    def xǁPatternCompressorǁget_importance_scores__mutmut_7(self) -> Dict[str, float]:
        """
        Get feature importance scores after fitting.

        Returns:
            Dictionary mapping feature names to importance scores (0.0-1.0)

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted or self.feature_importance is None:
            raise RuntimeError(
                "COMPRESSOR MUST BE FITTED BEFORE GETTING IMPORTANCE SCORES"
            )

        return {
            feature: importance
            for feature, importance in zip(self.feature_keys, self.feature_importance)
        }

    def xǁPatternCompressorǁget_importance_scores__mutmut_8(self) -> Dict[str, float]:
        """
        Get feature importance scores after fitting.

        Returns:
            Dictionary mapping feature names to importance scores (0.0-1.0)

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted or self.feature_importance is None:
            raise RuntimeError(
                "Compressor must be fitted before getting importance scores"
            )

        return {
            feature: importance
            for feature, importance in zip(None, self.feature_importance)
        }

    def xǁPatternCompressorǁget_importance_scores__mutmut_9(self) -> Dict[str, float]:
        """
        Get feature importance scores after fitting.

        Returns:
            Dictionary mapping feature names to importance scores (0.0-1.0)

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted or self.feature_importance is None:
            raise RuntimeError(
                "Compressor must be fitted before getting importance scores"
            )

        return {
            feature: importance
            for feature, importance in zip(self.feature_keys, None)
        }

    def xǁPatternCompressorǁget_importance_scores__mutmut_10(self) -> Dict[str, float]:
        """
        Get feature importance scores after fitting.

        Returns:
            Dictionary mapping feature names to importance scores (0.0-1.0)

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted or self.feature_importance is None:
            raise RuntimeError(
                "Compressor must be fitted before getting importance scores"
            )

        return {
            feature: importance
            for feature, importance in zip(self.feature_importance)
        }

    def xǁPatternCompressorǁget_importance_scores__mutmut_11(self) -> Dict[str, float]:
        """
        Get feature importance scores after fitting.

        Returns:
            Dictionary mapping feature names to importance scores (0.0-1.0)

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted or self.feature_importance is None:
            raise RuntimeError(
                "Compressor must be fitted before getting importance scores"
            )

        return {
            feature: importance
            for feature, importance in zip(self.feature_keys, )
        }
    
    xǁPatternCompressorǁget_importance_scores__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁPatternCompressorǁget_importance_scores__mutmut_1': xǁPatternCompressorǁget_importance_scores__mutmut_1, 
        'xǁPatternCompressorǁget_importance_scores__mutmut_2': xǁPatternCompressorǁget_importance_scores__mutmut_2, 
        'xǁPatternCompressorǁget_importance_scores__mutmut_3': xǁPatternCompressorǁget_importance_scores__mutmut_3, 
        'xǁPatternCompressorǁget_importance_scores__mutmut_4': xǁPatternCompressorǁget_importance_scores__mutmut_4, 
        'xǁPatternCompressorǁget_importance_scores__mutmut_5': xǁPatternCompressorǁget_importance_scores__mutmut_5, 
        'xǁPatternCompressorǁget_importance_scores__mutmut_6': xǁPatternCompressorǁget_importance_scores__mutmut_6, 
        'xǁPatternCompressorǁget_importance_scores__mutmut_7': xǁPatternCompressorǁget_importance_scores__mutmut_7, 
        'xǁPatternCompressorǁget_importance_scores__mutmut_8': xǁPatternCompressorǁget_importance_scores__mutmut_8, 
        'xǁPatternCompressorǁget_importance_scores__mutmut_9': xǁPatternCompressorǁget_importance_scores__mutmut_9, 
        'xǁPatternCompressorǁget_importance_scores__mutmut_10': xǁPatternCompressorǁget_importance_scores__mutmut_10, 
        'xǁPatternCompressorǁget_importance_scores__mutmut_11': xǁPatternCompressorǁget_importance_scores__mutmut_11
    }
    
    def get_importance_scores(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁPatternCompressorǁget_importance_scores__mutmut_orig"), object.__getattribute__(self, "xǁPatternCompressorǁget_importance_scores__mutmut_mutants"), args, kwargs, self)
        return result 
    
    get_importance_scores.__signature__ = _mutmut_signature(xǁPatternCompressorǁget_importance_scores__mutmut_orig)
    xǁPatternCompressorǁget_importance_scores__mutmut_orig.__name__ = 'xǁPatternCompressorǁget_importance_scores'

    def xǁPatternCompressorǁcompress__mutmut_orig(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_1(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_2(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError(None)

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_3(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("XXCompressor must be fitted before compressing patternsXX")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_4(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_5(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("COMPRESSOR MUST BE FITTED BEFORE COMPRESSING PATTERNS")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_6(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is not None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_7(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError(None)

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_8(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("XXProjection matrix is None - fit() may have failedXX")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_9(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("projection matrix is none - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_10(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("PROJECTION MATRIX IS NONE - FIT() MAY HAVE FAILED")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_11(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = None

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_12(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array(None)

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_13(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(None, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_14(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, None) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_15(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_16(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, ) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_17(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 1.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_18(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = None

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_19(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) * self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_20(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec + self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_21(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = None

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_22(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_23(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = None
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_24(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio * self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_25(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = None

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_26(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(None, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_27(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, None, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_28(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, None)

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_29(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_30(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_31(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, )

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_32(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(2.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_33(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 1.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_34(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = None
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_35(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(None)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_36(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits / component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_37(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = None  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_38(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            None, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_39(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, None, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_40(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, None
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_41(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_42(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_43(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_44(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 5, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_45(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = None
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_46(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(None)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_47(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(None):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_48(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(None, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_49(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, None)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_50(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_51(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, )):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_52(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = None
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_53(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) + 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_54(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 * (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_55(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 3 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_56(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits + 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_57(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 2) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_58(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 2
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_59(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = None
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_60(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = +(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_61(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 * (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_62(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(3 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_63(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits + 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_64(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 2))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_65(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = None
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_66(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(None)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_67(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val / max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_68(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = None

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_69(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) * max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_70(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(None, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_71(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, None, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_72(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, None) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_73(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_74(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_75(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, ) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_76(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = None

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_77(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(None) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_78(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) <= self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_79(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 1.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_80(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = None

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_81(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=None,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_82(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=None,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_83(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=None,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_84(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=None,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_85(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=None,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_86(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata=None,
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_87(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_88(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_89(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_90(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_91(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_92(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_93(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "XXoriginal_dimsXX": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_94(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "ORIGINAL_DIMS": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_95(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "XXcompressed_dimsXX": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_96(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "COMPRESSED_DIMS": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_97(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "XXvariable_bitsXX": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_98(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "VARIABLE_BITS": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_99(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "XXsparsityXX": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_100(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "SPARSITY": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_101(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(None),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_102(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) * len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_103(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(None) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_104(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized != 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_105(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 1) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_106(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = None  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_107(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) / 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_108(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 9  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_109(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = None
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_110(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(None)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_111(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = None  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_112(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) * 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_113(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used / 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_114(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 2.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_115(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 9.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_116(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = None
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_117(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size * original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_118(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(None)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_119(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed = 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_120(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed -= 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_121(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 2

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['sparsity']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_122(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            None
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_123(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['XXsparsityXX']:.1%} sparse)"
        )

        return compressed

    def xǁPatternCompressorǁcompress__mutmut_124(
        self,
        pattern: Dict[str, float],
        pattern_id: str,
        decision: str,
        confidence: float,
    ) -> CompressedPattern:
        """
        Compress pattern for LTM storage with enhanced variable quantization.

        Steps:
        1. Extract feature vector in consistent order
        2. Normalize features
        3. Project to lower-dimensional space (PCA)
        4. Apply variable quantization based on feature importance
        5. Apply sparsity threshold

        Enhancements:
        - Variable quantization: 4-bit for low importance, 8-bit for high importance
        - Importance-weighted precision allocation

        Args:
            pattern: Feature dictionary
            pattern_id: Pattern identifier
            decision: Decision associated with pattern
            confidence: Confidence score

        Returns:
            Compressed pattern for LTM storage

        Raises:
            RuntimeError: If compressor has not been fitted yet
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor must be fitted before compressing patterns")

        if self.projection_matrix is None:
            raise RuntimeError("Projection matrix is None - fit() may have failed")

        # Extract features in consistent order
        feature_vec = np.array([pattern.get(k, 0.0) for k in self.feature_keys])

        # Normalize
        normalized = (feature_vec - self.feature_mean) / self.feature_std

        # Project to lower-dimensional space
        compressed_vec = self.projection_matrix.T @ normalized

        # ENHANCEMENT: Variable quantization based on eigenvalue importance
        # Components with higher explained variance get more bits for precision
        if self.explained_variance_ratio is not None:
            # Use actual eigenvalue importance instead of arbitrary decay
            component_importance = (
                self.explained_variance_ratio / self.explained_variance_ratio.max()
            )
        else:
            # Fallback to linear decay if not available
            component_importance = np.linspace(1.0, 0.5, len(compressed_vec))

        variable_bits = (self.quantization_bits * component_importance).astype(int)
        variable_bits = np.clip(
            variable_bits, 4, self.quantization_bits
        )  # 4-8 bit range

        # Apply variable quantization per component with correct signed integer range
        quantized = np.zeros_like(compressed_vec)
        for i, (val, bits) in enumerate(zip(compressed_vec, variable_bits)):
            # Correct signed integer range: -(2^(bits-1)) to (2^(bits-1) - 1)
            max_val = 2 ** (bits - 1) - 1
            min_val = -(2 ** (bits - 1))
            # Quantize and clip to valid range
            quantized_val = np.round(val * max_val)
            quantized[i] = np.clip(quantized_val, min_val, max_val) / max_val

        # Apply sparsity threshold
        quantized[np.abs(quantized) < self.sparsity_threshold] = 0.0

        # Create compressed pattern
        compressed = CompressedPattern(
            pattern_id=pattern_id,
            compressed_features=quantized,
            decision=decision,
            confidence=confidence,
            feature_keys=self.feature_keys,
            compression_metadata={
                "original_dims": len(self.feature_keys),
                "compressed_dims": len(quantized),
                "variable_bits": variable_bits.tolist(),
                "sparsity": float(np.sum(quantized == 0) / len(quantized)),
            },
        )

        # Calculate and track compression ratio
        original_size = len(self.feature_keys) * 8  # 64-bit (8 bytes) per feature
        # Compute logical compressed size based on variable quantization + padding overhead
        bits_used = sum(variable_bits)
        # Add 10% padding overhead for realistic bit packing/alignment
        compressed_size = (bits_used * 1.1) / 8.0  # Convert bits to bytes with overhead

        # Store actual compression ratio (compressed/original, e.g., 0.3 = 30% of original size)
        compression_ratio = compressed_size / original_size
        self.compression_ratios.append(compression_ratio)
        self.total_compressed += 1

        logger.debug(
            f"Compressed pattern {pattern_id}: {original_size}→{compressed_size:.1f} bytes "
            f"({compression_ratio:.1%} reduction, {compressed.compression_metadata['SPARSITY']:.1%} sparse)"
        )

        return compressed
    
    xǁPatternCompressorǁcompress__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁPatternCompressorǁcompress__mutmut_1': xǁPatternCompressorǁcompress__mutmut_1, 
        'xǁPatternCompressorǁcompress__mutmut_2': xǁPatternCompressorǁcompress__mutmut_2, 
        'xǁPatternCompressorǁcompress__mutmut_3': xǁPatternCompressorǁcompress__mutmut_3, 
        'xǁPatternCompressorǁcompress__mutmut_4': xǁPatternCompressorǁcompress__mutmut_4, 
        'xǁPatternCompressorǁcompress__mutmut_5': xǁPatternCompressorǁcompress__mutmut_5, 
        'xǁPatternCompressorǁcompress__mutmut_97': xǁPatternCompressorǁcompress__mutmut_97, 
        'xǁPatternCompressorǁcompress__mutmut_98': xǁPatternCompressorǁcompress__mutmut_98, 
        'xǁPatternCompressorǁcompress__mutmut_99': xǁPatternCompressorǁcompress__mutmut_99, 
        'xǁPatternCompressorǁcompress__mutmut_100': xǁPatternCompressorǁcompress__mutmut_100, 
        'xǁPatternCompressorǁcompress__mutmut_101': xǁPatternCompressorǁcompress__mutmut_101, 
        'xǁPatternCompressorǁcompress__mutmut_102': xǁPatternCompressorǁcompress__mutmut_102, 
        'xǁPatternCompressorǁcompress__mutmut_103': xǁPatternCompressorǁcompress__mutmut_103, 
        'xǁPatternCompressorǁcompress__mutmut_104': xǁPatternCompressorǁcompress__mutmut_104, 
        'xǁPatternCompressorǁcompress__mutmut_105': xǁPatternCompressorǁcompress__mutmut_105, 
        'xǁPatternCompressorǁcompress__mutmut_106': xǁPatternCompressorǁcompress__mutmut_106, 
        'xǁPatternCompressorǁcompress__mutmut_107': xǁPatternCompressorǁcompress__mutmut_107, 
        'xǁPatternCompressorǁcompress__mutmut_108': xǁPatternCompressorǁcompress__mutmut_108, 
        'xǁPatternCompressorǁcompress__mutmut_109': xǁPatternCompressorǁcompress__mutmut_109, 
        'xǁPatternCompressorǁcompress__mutmut_110': xǁPatternCompressorǁcompress__mutmut_110, 
        'xǁPatternCompressorǁcompress__mutmut_111': xǁPatternCompressorǁcompress__mutmut_111, 
        'xǁPatternCompressorǁcompress__mutmut_112': xǁPatternCompressorǁcompress__mutmut_112, 
        'xǁPatternCompressorǁcompress__mutmut_113': xǁPatternCompressorǁcompress__mutmut_113, 
        'xǁPatternCompressorǁcompress__mutmut_114': xǁPatternCompressorǁcompress__mutmut_114, 
        'xǁPatternCompressorǁcompress__mutmut_115': xǁPatternCompressorǁcompress__mutmut_115, 
        'xǁPatternCompressorǁcompress__mutmut_116': xǁPatternCompressorǁcompress__mutmut_116, 
        'xǁPatternCompressorǁcompress__mutmut_117': xǁPatternCompressorǁcompress__mutmut_117, 
        'xǁPatternCompressorǁcompress__mutmut_118': xǁPatternCompressorǁcompress__mutmut_118, 
        'xǁPatternCompressorǁcompress__mutmut_119': xǁPatternCompressorǁcompress__mutmut_119, 
        'xǁPatternCompressorǁcompress__mutmut_120': xǁPatternCompressorǁcompress__mutmut_120, 
        'xǁPatternCompressorǁcompress__mutmut_121': xǁPatternCompressorǁcompress__mutmut_121, 
        'xǁPatternCompressorǁcompress__mutmut_122': xǁPatternCompressorǁcompress__mutmut_122, 
        'xǁPatternCompressorǁcompress__mutmut_123': xǁPatternCompressorǁcompress__mutmut_123, 
        'xǁPatternCompressorǁcompress__mutmut_124': xǁPatternCompressorǁcompress__mutmut_124
    }
    
    def compress(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁPatternCompressorǁcompress__mutmut_orig"), object.__getattribute__(self, "xǁPatternCompressorǁcompress__mutmut_mutants"), args, kwargs, self)
        return result 
    
    compress.__signature__ = _mutmut_signature(xǁPatternCompressorǁcompress__mutmut_orig)
    xǁPatternCompressorǁcompress__mutmut_orig.__name__ = 'xǁPatternCompressorǁcompress'

    def xǁPatternCompressorǁdecompress__mutmut_orig(self, compressed: CompressedPattern) -> Dict[str, float]:
        """
        Reconstruct pattern from compressed form.

        Note: This is a lossy reconstruction. Some information is lost in compression.
        Enhanced to handle variable quantization from compress() method.

        Args:
            compressed: Compressed pattern to reconstruct

        Returns:
            Reconstructed feature dict (approximate)
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor not fitted. Call fit() first.")

        # Extract variable_bits from metadata if available
        variable_bits = compressed.compression_metadata.get("variable_bits")

        if variable_bits is not None:
            # Enhanced decompression with variable quantization
            # Values are already normalized in compress(), just copy them
            dequantized = compressed.compressed_features.copy()
        else:
            # Fallback to uniform dequantization for old compressed patterns
            dequantized = self._dequantize(compressed.compressed_features)

        # Project back to original space
        reconstructed_norm = self.projection_matrix @ dequantized

        # Denormalize
        reconstructed = reconstructed_norm * self.feature_std + self.feature_mean

        # Convert back to dict
        reconstructed_dict = {
            k: float(v) for k, v in zip(compressed.feature_keys, reconstructed)
        }

        self.total_decompressed += 1

        return reconstructed_dict

    def xǁPatternCompressorǁdecompress__mutmut_1(self, compressed: CompressedPattern) -> Dict[str, float]:
        """
        Reconstruct pattern from compressed form.

        Note: This is a lossy reconstruction. Some information is lost in compression.
        Enhanced to handle variable quantization from compress() method.

        Args:
            compressed: Compressed pattern to reconstruct

        Returns:
            Reconstructed feature dict (approximate)
        """
        if self.is_fitted:
            raise RuntimeError("Compressor not fitted. Call fit() first.")

        # Extract variable_bits from metadata if available
        variable_bits = compressed.compression_metadata.get("variable_bits")

        if variable_bits is not None:
            # Enhanced decompression with variable quantization
            # Values are already normalized in compress(), just copy them
            dequantized = compressed.compressed_features.copy()
        else:
            # Fallback to uniform dequantization for old compressed patterns
            dequantized = self._dequantize(compressed.compressed_features)

        # Project back to original space
        reconstructed_norm = self.projection_matrix @ dequantized

        # Denormalize
        reconstructed = reconstructed_norm * self.feature_std + self.feature_mean

        # Convert back to dict
        reconstructed_dict = {
            k: float(v) for k, v in zip(compressed.feature_keys, reconstructed)
        }

        self.total_decompressed += 1

        return reconstructed_dict

    def xǁPatternCompressorǁdecompress__mutmut_2(self, compressed: CompressedPattern) -> Dict[str, float]:
        """
        Reconstruct pattern from compressed form.

        Note: This is a lossy reconstruction. Some information is lost in compression.
        Enhanced to handle variable quantization from compress() method.

        Args:
            compressed: Compressed pattern to reconstruct

        Returns:
            Reconstructed feature dict (approximate)
        """
        if not self.is_fitted:
            raise RuntimeError(None)

        # Extract variable_bits from metadata if available
        variable_bits = compressed.compression_metadata.get("variable_bits")

        if variable_bits is not None:
            # Enhanced decompression with variable quantization
            # Values are already normalized in compress(), just copy them
            dequantized = compressed.compressed_features.copy()
        else:
            # Fallback to uniform dequantization for old compressed patterns
            dequantized = self._dequantize(compressed.compressed_features)

        # Project back to original space
        reconstructed_norm = self.projection_matrix @ dequantized

        # Denormalize
        reconstructed = reconstructed_norm * self.feature_std + self.feature_mean

        # Convert back to dict
        reconstructed_dict = {
            k: float(v) for k, v in zip(compressed.feature_keys, reconstructed)
        }

        self.total_decompressed += 1

        return reconstructed_dict

    def xǁPatternCompressorǁdecompress__mutmut_3(self, compressed: CompressedPattern) -> Dict[str, float]:
        """
        Reconstruct pattern from compressed form.

        Note: This is a lossy reconstruction. Some information is lost in compression.
        Enhanced to handle variable quantization from compress() method.

        Args:
            compressed: Compressed pattern to reconstruct

        Returns:
            Reconstructed feature dict (approximate)
        """
        if not self.is_fitted:
            raise RuntimeError("XXCompressor not fitted. Call fit() first.XX")

        # Extract variable_bits from metadata if available
        variable_bits = compressed.compression_metadata.get("variable_bits")

        if variable_bits is not None:
            # Enhanced decompression with variable quantization
            # Values are already normalized in compress(), just copy them
            dequantized = compressed.compressed_features.copy()
        else:
            # Fallback to uniform dequantization for old compressed patterns
            dequantized = self._dequantize(compressed.compressed_features)

        # Project back to original space
        reconstructed_norm = self.projection_matrix @ dequantized

        # Denormalize
        reconstructed = reconstructed_norm * self.feature_std + self.feature_mean

        # Convert back to dict
        reconstructed_dict = {
            k: float(v) for k, v in zip(compressed.feature_keys, reconstructed)
        }

        self.total_decompressed += 1

        return reconstructed_dict

    def xǁPatternCompressorǁdecompress__mutmut_4(self, compressed: CompressedPattern) -> Dict[str, float]:
        """
        Reconstruct pattern from compressed form.

        Note: This is a lossy reconstruction. Some information is lost in compression.
        Enhanced to handle variable quantization from compress() method.

        Args:
            compressed: Compressed pattern to reconstruct

        Returns:
            Reconstructed feature dict (approximate)
        """
        if not self.is_fitted:
            raise RuntimeError("compressor not fitted. call fit() first.")

        # Extract variable_bits from metadata if available
        variable_bits = compressed.compression_metadata.get("variable_bits")

        if variable_bits is not None:
            # Enhanced decompression with variable quantization
            # Values are already normalized in compress(), just copy them
            dequantized = compressed.compressed_features.copy()
        else:
            # Fallback to uniform dequantization for old compressed patterns
            dequantized = self._dequantize(compressed.compressed_features)

        # Project back to original space
        reconstructed_norm = self.projection_matrix @ dequantized

        # Denormalize
        reconstructed = reconstructed_norm * self.feature_std + self.feature_mean

        # Convert back to dict
        reconstructed_dict = {
            k: float(v) for k, v in zip(compressed.feature_keys, reconstructed)
        }

        self.total_decompressed += 1

        return reconstructed_dict

    def xǁPatternCompressorǁdecompress__mutmut_5(self, compressed: CompressedPattern) -> Dict[str, float]:
        """
        Reconstruct pattern from compressed form.

        Note: This is a lossy reconstruction. Some information is lost in compression.
        Enhanced to handle variable quantization from compress() method.

        Args:
            compressed: Compressed pattern to reconstruct

        Returns:
            Reconstructed feature dict (approximate)
        """
        if not self.is_fitted:
            raise RuntimeError("COMPRESSOR NOT FITTED. CALL FIT() FIRST.")

        # Extract variable_bits from metadata if available
        variable_bits = compressed.compression_metadata.get("variable_bits")

        if variable_bits is not None:
            # Enhanced decompression with variable quantization
            # Values are already normalized in compress(), just copy them
            dequantized = compressed.compressed_features.copy()
        else:
            # Fallback to uniform dequantization for old compressed patterns
            dequantized = self._dequantize(compressed.compressed_features)

        # Project back to original space
        reconstructed_norm = self.projection_matrix @ dequantized

        # Denormalize
        reconstructed = reconstructed_norm * self.feature_std + self.feature_mean

        # Convert back to dict
        reconstructed_dict = {
            k: float(v) for k, v in zip(compressed.feature_keys, reconstructed)
        }

        self.total_decompressed += 1

        return reconstructed_dict

    def xǁPatternCompressorǁdecompress__mutmut_6(self, compressed: CompressedPattern) -> Dict[str, float]:
        """
        Reconstruct pattern from compressed form.

        Note: This is a lossy reconstruction. Some information is lost in compression.
        Enhanced to handle variable quantization from compress() method.

        Args:
            compressed: Compressed pattern to reconstruct

        Returns:
            Reconstructed feature dict (approximate)
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor not fitted. Call fit() first.")

        # Extract variable_bits from metadata if available
        variable_bits = None

        if variable_bits is not None:
            # Enhanced decompression with variable quantization
            # Values are already normalized in compress(), just copy them
            dequantized = compressed.compressed_features.copy()
        else:
            # Fallback to uniform dequantization for old compressed patterns
            dequantized = self._dequantize(compressed.compressed_features)

        # Project back to original space
        reconstructed_norm = self.projection_matrix @ dequantized

        # Denormalize
        reconstructed = reconstructed_norm * self.feature_std + self.feature_mean

        # Convert back to dict
        reconstructed_dict = {
            k: float(v) for k, v in zip(compressed.feature_keys, reconstructed)
        }

        self.total_decompressed += 1

        return reconstructed_dict

    def xǁPatternCompressorǁdecompress__mutmut_7(self, compressed: CompressedPattern) -> Dict[str, float]:
        """
        Reconstruct pattern from compressed form.

        Note: This is a lossy reconstruction. Some information is lost in compression.
        Enhanced to handle variable quantization from compress() method.

        Args:
            compressed: Compressed pattern to reconstruct

        Returns:
            Reconstructed feature dict (approximate)
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor not fitted. Call fit() first.")

        # Extract variable_bits from metadata if available
        variable_bits = compressed.compression_metadata.get(None)

        if variable_bits is not None:
            # Enhanced decompression with variable quantization
            # Values are already normalized in compress(), just copy them
            dequantized = compressed.compressed_features.copy()
        else:
            # Fallback to uniform dequantization for old compressed patterns
            dequantized = self._dequantize(compressed.compressed_features)

        # Project back to original space
        reconstructed_norm = self.projection_matrix @ dequantized

        # Denormalize
        reconstructed = reconstructed_norm * self.feature_std + self.feature_mean

        # Convert back to dict
        reconstructed_dict = {
            k: float(v) for k, v in zip(compressed.feature_keys, reconstructed)
        }

        self.total_decompressed += 1

        return reconstructed_dict

    def xǁPatternCompressorǁdecompress__mutmut_8(self, compressed: CompressedPattern) -> Dict[str, float]:
        """
        Reconstruct pattern from compressed form.

        Note: This is a lossy reconstruction. Some information is lost in compression.
        Enhanced to handle variable quantization from compress() method.

        Args:
            compressed: Compressed pattern to reconstruct

        Returns:
            Reconstructed feature dict (approximate)
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor not fitted. Call fit() first.")

        # Extract variable_bits from metadata if available
        variable_bits = compressed.compression_metadata.get("XXvariable_bitsXX")

        if variable_bits is not None:
            # Enhanced decompression with variable quantization
            # Values are already normalized in compress(), just copy them
            dequantized = compressed.compressed_features.copy()
        else:
            # Fallback to uniform dequantization for old compressed patterns
            dequantized = self._dequantize(compressed.compressed_features)

        # Project back to original space
        reconstructed_norm = self.projection_matrix @ dequantized

        # Denormalize
        reconstructed = reconstructed_norm * self.feature_std + self.feature_mean

        # Convert back to dict
        reconstructed_dict = {
            k: float(v) for k, v in zip(compressed.feature_keys, reconstructed)
        }

        self.total_decompressed += 1

        return reconstructed_dict

    def xǁPatternCompressorǁdecompress__mutmut_9(self, compressed: CompressedPattern) -> Dict[str, float]:
        """
        Reconstruct pattern from compressed form.

        Note: This is a lossy reconstruction. Some information is lost in compression.
        Enhanced to handle variable quantization from compress() method.

        Args:
            compressed: Compressed pattern to reconstruct

        Returns:
            Reconstructed feature dict (approximate)
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor not fitted. Call fit() first.")

        # Extract variable_bits from metadata if available
        variable_bits = compressed.compression_metadata.get("VARIABLE_BITS")

        if variable_bits is not None:
            # Enhanced decompression with variable quantization
            # Values are already normalized in compress(), just copy them
            dequantized = compressed.compressed_features.copy()
        else:
            # Fallback to uniform dequantization for old compressed patterns
            dequantized = self._dequantize(compressed.compressed_features)

        # Project back to original space
        reconstructed_norm = self.projection_matrix @ dequantized

        # Denormalize
        reconstructed = reconstructed_norm * self.feature_std + self.feature_mean

        # Convert back to dict
        reconstructed_dict = {
            k: float(v) for k, v in zip(compressed.feature_keys, reconstructed)
        }

        self.total_decompressed += 1

        return reconstructed_dict

    def xǁPatternCompressorǁdecompress__mutmut_10(self, compressed: CompressedPattern) -> Dict[str, float]:
        """
        Reconstruct pattern from compressed form.

        Note: This is a lossy reconstruction. Some information is lost in compression.
        Enhanced to handle variable quantization from compress() method.

        Args:
            compressed: Compressed pattern to reconstruct

        Returns:
            Reconstructed feature dict (approximate)
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor not fitted. Call fit() first.")

        # Extract variable_bits from metadata if available
        variable_bits = compressed.compression_metadata.get("variable_bits")

        if variable_bits is None:
            # Enhanced decompression with variable quantization
            # Values are already normalized in compress(), just copy them
            dequantized = compressed.compressed_features.copy()
        else:
            # Fallback to uniform dequantization for old compressed patterns
            dequantized = self._dequantize(compressed.compressed_features)

        # Project back to original space
        reconstructed_norm = self.projection_matrix @ dequantized

        # Denormalize
        reconstructed = reconstructed_norm * self.feature_std + self.feature_mean

        # Convert back to dict
        reconstructed_dict = {
            k: float(v) for k, v in zip(compressed.feature_keys, reconstructed)
        }

        self.total_decompressed += 1

        return reconstructed_dict

    def xǁPatternCompressorǁdecompress__mutmut_11(self, compressed: CompressedPattern) -> Dict[str, float]:
        """
        Reconstruct pattern from compressed form.

        Note: This is a lossy reconstruction. Some information is lost in compression.
        Enhanced to handle variable quantization from compress() method.

        Args:
            compressed: Compressed pattern to reconstruct

        Returns:
            Reconstructed feature dict (approximate)
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor not fitted. Call fit() first.")

        # Extract variable_bits from metadata if available
        variable_bits = compressed.compression_metadata.get("variable_bits")

        if variable_bits is not None:
            # Enhanced decompression with variable quantization
            # Values are already normalized in compress(), just copy them
            dequantized = None
        else:
            # Fallback to uniform dequantization for old compressed patterns
            dequantized = self._dequantize(compressed.compressed_features)

        # Project back to original space
        reconstructed_norm = self.projection_matrix @ dequantized

        # Denormalize
        reconstructed = reconstructed_norm * self.feature_std + self.feature_mean

        # Convert back to dict
        reconstructed_dict = {
            k: float(v) for k, v in zip(compressed.feature_keys, reconstructed)
        }

        self.total_decompressed += 1

        return reconstructed_dict

    def xǁPatternCompressorǁdecompress__mutmut_12(self, compressed: CompressedPattern) -> Dict[str, float]:
        """
        Reconstruct pattern from compressed form.

        Note: This is a lossy reconstruction. Some information is lost in compression.
        Enhanced to handle variable quantization from compress() method.

        Args:
            compressed: Compressed pattern to reconstruct

        Returns:
            Reconstructed feature dict (approximate)
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor not fitted. Call fit() first.")

        # Extract variable_bits from metadata if available
        variable_bits = compressed.compression_metadata.get("variable_bits")

        if variable_bits is not None:
            # Enhanced decompression with variable quantization
            # Values are already normalized in compress(), just copy them
            dequantized = compressed.compressed_features.copy()
        else:
            # Fallback to uniform dequantization for old compressed patterns
            dequantized = None

        # Project back to original space
        reconstructed_norm = self.projection_matrix @ dequantized

        # Denormalize
        reconstructed = reconstructed_norm * self.feature_std + self.feature_mean

        # Convert back to dict
        reconstructed_dict = {
            k: float(v) for k, v in zip(compressed.feature_keys, reconstructed)
        }

        self.total_decompressed += 1

        return reconstructed_dict

    def xǁPatternCompressorǁdecompress__mutmut_13(self, compressed: CompressedPattern) -> Dict[str, float]:
        """
        Reconstruct pattern from compressed form.

        Note: This is a lossy reconstruction. Some information is lost in compression.
        Enhanced to handle variable quantization from compress() method.

        Args:
            compressed: Compressed pattern to reconstruct

        Returns:
            Reconstructed feature dict (approximate)
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor not fitted. Call fit() first.")

        # Extract variable_bits from metadata if available
        variable_bits = compressed.compression_metadata.get("variable_bits")

        if variable_bits is not None:
            # Enhanced decompression with variable quantization
            # Values are already normalized in compress(), just copy them
            dequantized = compressed.compressed_features.copy()
        else:
            # Fallback to uniform dequantization for old compressed patterns
            dequantized = self._dequantize(None)

        # Project back to original space
        reconstructed_norm = self.projection_matrix @ dequantized

        # Denormalize
        reconstructed = reconstructed_norm * self.feature_std + self.feature_mean

        # Convert back to dict
        reconstructed_dict = {
            k: float(v) for k, v in zip(compressed.feature_keys, reconstructed)
        }

        self.total_decompressed += 1

        return reconstructed_dict

    def xǁPatternCompressorǁdecompress__mutmut_14(self, compressed: CompressedPattern) -> Dict[str, float]:
        """
        Reconstruct pattern from compressed form.

        Note: This is a lossy reconstruction. Some information is lost in compression.
        Enhanced to handle variable quantization from compress() method.

        Args:
            compressed: Compressed pattern to reconstruct

        Returns:
            Reconstructed feature dict (approximate)
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor not fitted. Call fit() first.")

        # Extract variable_bits from metadata if available
        variable_bits = compressed.compression_metadata.get("variable_bits")

        if variable_bits is not None:
            # Enhanced decompression with variable quantization
            # Values are already normalized in compress(), just copy them
            dequantized = compressed.compressed_features.copy()
        else:
            # Fallback to uniform dequantization for old compressed patterns
            dequantized = self._dequantize(compressed.compressed_features)

        # Project back to original space
        reconstructed_norm = None

        # Denormalize
        reconstructed = reconstructed_norm * self.feature_std + self.feature_mean

        # Convert back to dict
        reconstructed_dict = {
            k: float(v) for k, v in zip(compressed.feature_keys, reconstructed)
        }

        self.total_decompressed += 1

        return reconstructed_dict

    def xǁPatternCompressorǁdecompress__mutmut_15(self, compressed: CompressedPattern) -> Dict[str, float]:
        """
        Reconstruct pattern from compressed form.

        Note: This is a lossy reconstruction. Some information is lost in compression.
        Enhanced to handle variable quantization from compress() method.

        Args:
            compressed: Compressed pattern to reconstruct

        Returns:
            Reconstructed feature dict (approximate)
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor not fitted. Call fit() first.")

        # Extract variable_bits from metadata if available
        variable_bits = compressed.compression_metadata.get("variable_bits")

        if variable_bits is not None:
            # Enhanced decompression with variable quantization
            # Values are already normalized in compress(), just copy them
            dequantized = compressed.compressed_features.copy()
        else:
            # Fallback to uniform dequantization for old compressed patterns
            dequantized = self._dequantize(compressed.compressed_features)

        # Project back to original space
        reconstructed_norm = self.projection_matrix @ dequantized

        # Denormalize
        reconstructed = None

        # Convert back to dict
        reconstructed_dict = {
            k: float(v) for k, v in zip(compressed.feature_keys, reconstructed)
        }

        self.total_decompressed += 1

        return reconstructed_dict

    def xǁPatternCompressorǁdecompress__mutmut_16(self, compressed: CompressedPattern) -> Dict[str, float]:
        """
        Reconstruct pattern from compressed form.

        Note: This is a lossy reconstruction. Some information is lost in compression.
        Enhanced to handle variable quantization from compress() method.

        Args:
            compressed: Compressed pattern to reconstruct

        Returns:
            Reconstructed feature dict (approximate)
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor not fitted. Call fit() first.")

        # Extract variable_bits from metadata if available
        variable_bits = compressed.compression_metadata.get("variable_bits")

        if variable_bits is not None:
            # Enhanced decompression with variable quantization
            # Values are already normalized in compress(), just copy them
            dequantized = compressed.compressed_features.copy()
        else:
            # Fallback to uniform dequantization for old compressed patterns
            dequantized = self._dequantize(compressed.compressed_features)

        # Project back to original space
        reconstructed_norm = self.projection_matrix @ dequantized

        # Denormalize
        reconstructed = reconstructed_norm * self.feature_std - self.feature_mean

        # Convert back to dict
        reconstructed_dict = {
            k: float(v) for k, v in zip(compressed.feature_keys, reconstructed)
        }

        self.total_decompressed += 1

        return reconstructed_dict

    def xǁPatternCompressorǁdecompress__mutmut_17(self, compressed: CompressedPattern) -> Dict[str, float]:
        """
        Reconstruct pattern from compressed form.

        Note: This is a lossy reconstruction. Some information is lost in compression.
        Enhanced to handle variable quantization from compress() method.

        Args:
            compressed: Compressed pattern to reconstruct

        Returns:
            Reconstructed feature dict (approximate)
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor not fitted. Call fit() first.")

        # Extract variable_bits from metadata if available
        variable_bits = compressed.compression_metadata.get("variable_bits")

        if variable_bits is not None:
            # Enhanced decompression with variable quantization
            # Values are already normalized in compress(), just copy them
            dequantized = compressed.compressed_features.copy()
        else:
            # Fallback to uniform dequantization for old compressed patterns
            dequantized = self._dequantize(compressed.compressed_features)

        # Project back to original space
        reconstructed_norm = self.projection_matrix @ dequantized

        # Denormalize
        reconstructed = reconstructed_norm / self.feature_std + self.feature_mean

        # Convert back to dict
        reconstructed_dict = {
            k: float(v) for k, v in zip(compressed.feature_keys, reconstructed)
        }

        self.total_decompressed += 1

        return reconstructed_dict

    def xǁPatternCompressorǁdecompress__mutmut_18(self, compressed: CompressedPattern) -> Dict[str, float]:
        """
        Reconstruct pattern from compressed form.

        Note: This is a lossy reconstruction. Some information is lost in compression.
        Enhanced to handle variable quantization from compress() method.

        Args:
            compressed: Compressed pattern to reconstruct

        Returns:
            Reconstructed feature dict (approximate)
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor not fitted. Call fit() first.")

        # Extract variable_bits from metadata if available
        variable_bits = compressed.compression_metadata.get("variable_bits")

        if variable_bits is not None:
            # Enhanced decompression with variable quantization
            # Values are already normalized in compress(), just copy them
            dequantized = compressed.compressed_features.copy()
        else:
            # Fallback to uniform dequantization for old compressed patterns
            dequantized = self._dequantize(compressed.compressed_features)

        # Project back to original space
        reconstructed_norm = self.projection_matrix @ dequantized

        # Denormalize
        reconstructed = reconstructed_norm * self.feature_std + self.feature_mean

        # Convert back to dict
        reconstructed_dict = None

        self.total_decompressed += 1

        return reconstructed_dict

    def xǁPatternCompressorǁdecompress__mutmut_19(self, compressed: CompressedPattern) -> Dict[str, float]:
        """
        Reconstruct pattern from compressed form.

        Note: This is a lossy reconstruction. Some information is lost in compression.
        Enhanced to handle variable quantization from compress() method.

        Args:
            compressed: Compressed pattern to reconstruct

        Returns:
            Reconstructed feature dict (approximate)
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor not fitted. Call fit() first.")

        # Extract variable_bits from metadata if available
        variable_bits = compressed.compression_metadata.get("variable_bits")

        if variable_bits is not None:
            # Enhanced decompression with variable quantization
            # Values are already normalized in compress(), just copy them
            dequantized = compressed.compressed_features.copy()
        else:
            # Fallback to uniform dequantization for old compressed patterns
            dequantized = self._dequantize(compressed.compressed_features)

        # Project back to original space
        reconstructed_norm = self.projection_matrix @ dequantized

        # Denormalize
        reconstructed = reconstructed_norm * self.feature_std + self.feature_mean

        # Convert back to dict
        reconstructed_dict = {
            k: float(None) for k, v in zip(compressed.feature_keys, reconstructed)
        }

        self.total_decompressed += 1

        return reconstructed_dict

    def xǁPatternCompressorǁdecompress__mutmut_20(self, compressed: CompressedPattern) -> Dict[str, float]:
        """
        Reconstruct pattern from compressed form.

        Note: This is a lossy reconstruction. Some information is lost in compression.
        Enhanced to handle variable quantization from compress() method.

        Args:
            compressed: Compressed pattern to reconstruct

        Returns:
            Reconstructed feature dict (approximate)
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor not fitted. Call fit() first.")

        # Extract variable_bits from metadata if available
        variable_bits = compressed.compression_metadata.get("variable_bits")

        if variable_bits is not None:
            # Enhanced decompression with variable quantization
            # Values are already normalized in compress(), just copy them
            dequantized = compressed.compressed_features.copy()
        else:
            # Fallback to uniform dequantization for old compressed patterns
            dequantized = self._dequantize(compressed.compressed_features)

        # Project back to original space
        reconstructed_norm = self.projection_matrix @ dequantized

        # Denormalize
        reconstructed = reconstructed_norm * self.feature_std + self.feature_mean

        # Convert back to dict
        reconstructed_dict = {
            k: float(v) for k, v in zip(None, reconstructed)
        }

        self.total_decompressed += 1

        return reconstructed_dict

    def xǁPatternCompressorǁdecompress__mutmut_21(self, compressed: CompressedPattern) -> Dict[str, float]:
        """
        Reconstruct pattern from compressed form.

        Note: This is a lossy reconstruction. Some information is lost in compression.
        Enhanced to handle variable quantization from compress() method.

        Args:
            compressed: Compressed pattern to reconstruct

        Returns:
            Reconstructed feature dict (approximate)
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor not fitted. Call fit() first.")

        # Extract variable_bits from metadata if available
        variable_bits = compressed.compression_metadata.get("variable_bits")

        if variable_bits is not None:
            # Enhanced decompression with variable quantization
            # Values are already normalized in compress(), just copy them
            dequantized = compressed.compressed_features.copy()
        else:
            # Fallback to uniform dequantization for old compressed patterns
            dequantized = self._dequantize(compressed.compressed_features)

        # Project back to original space
        reconstructed_norm = self.projection_matrix @ dequantized

        # Denormalize
        reconstructed = reconstructed_norm * self.feature_std + self.feature_mean

        # Convert back to dict
        reconstructed_dict = {
            k: float(v) for k, v in zip(compressed.feature_keys, None)
        }

        self.total_decompressed += 1

        return reconstructed_dict

    def xǁPatternCompressorǁdecompress__mutmut_22(self, compressed: CompressedPattern) -> Dict[str, float]:
        """
        Reconstruct pattern from compressed form.

        Note: This is a lossy reconstruction. Some information is lost in compression.
        Enhanced to handle variable quantization from compress() method.

        Args:
            compressed: Compressed pattern to reconstruct

        Returns:
            Reconstructed feature dict (approximate)
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor not fitted. Call fit() first.")

        # Extract variable_bits from metadata if available
        variable_bits = compressed.compression_metadata.get("variable_bits")

        if variable_bits is not None:
            # Enhanced decompression with variable quantization
            # Values are already normalized in compress(), just copy them
            dequantized = compressed.compressed_features.copy()
        else:
            # Fallback to uniform dequantization for old compressed patterns
            dequantized = self._dequantize(compressed.compressed_features)

        # Project back to original space
        reconstructed_norm = self.projection_matrix @ dequantized

        # Denormalize
        reconstructed = reconstructed_norm * self.feature_std + self.feature_mean

        # Convert back to dict
        reconstructed_dict = {
            k: float(v) for k, v in zip(reconstructed)
        }

        self.total_decompressed += 1

        return reconstructed_dict

    def xǁPatternCompressorǁdecompress__mutmut_23(self, compressed: CompressedPattern) -> Dict[str, float]:
        """
        Reconstruct pattern from compressed form.

        Note: This is a lossy reconstruction. Some information is lost in compression.
        Enhanced to handle variable quantization from compress() method.

        Args:
            compressed: Compressed pattern to reconstruct

        Returns:
            Reconstructed feature dict (approximate)
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor not fitted. Call fit() first.")

        # Extract variable_bits from metadata if available
        variable_bits = compressed.compression_metadata.get("variable_bits")

        if variable_bits is not None:
            # Enhanced decompression with variable quantization
            # Values are already normalized in compress(), just copy them
            dequantized = compressed.compressed_features.copy()
        else:
            # Fallback to uniform dequantization for old compressed patterns
            dequantized = self._dequantize(compressed.compressed_features)

        # Project back to original space
        reconstructed_norm = self.projection_matrix @ dequantized

        # Denormalize
        reconstructed = reconstructed_norm * self.feature_std + self.feature_mean

        # Convert back to dict
        reconstructed_dict = {
            k: float(v) for k, v in zip(compressed.feature_keys, )
        }

        self.total_decompressed += 1

        return reconstructed_dict

    def xǁPatternCompressorǁdecompress__mutmut_24(self, compressed: CompressedPattern) -> Dict[str, float]:
        """
        Reconstruct pattern from compressed form.

        Note: This is a lossy reconstruction. Some information is lost in compression.
        Enhanced to handle variable quantization from compress() method.

        Args:
            compressed: Compressed pattern to reconstruct

        Returns:
            Reconstructed feature dict (approximate)
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor not fitted. Call fit() first.")

        # Extract variable_bits from metadata if available
        variable_bits = compressed.compression_metadata.get("variable_bits")

        if variable_bits is not None:
            # Enhanced decompression with variable quantization
            # Values are already normalized in compress(), just copy them
            dequantized = compressed.compressed_features.copy()
        else:
            # Fallback to uniform dequantization for old compressed patterns
            dequantized = self._dequantize(compressed.compressed_features)

        # Project back to original space
        reconstructed_norm = self.projection_matrix @ dequantized

        # Denormalize
        reconstructed = reconstructed_norm * self.feature_std + self.feature_mean

        # Convert back to dict
        reconstructed_dict = {
            k: float(v) for k, v in zip(compressed.feature_keys, reconstructed)
        }

        self.total_decompressed = 1

        return reconstructed_dict

    def xǁPatternCompressorǁdecompress__mutmut_25(self, compressed: CompressedPattern) -> Dict[str, float]:
        """
        Reconstruct pattern from compressed form.

        Note: This is a lossy reconstruction. Some information is lost in compression.
        Enhanced to handle variable quantization from compress() method.

        Args:
            compressed: Compressed pattern to reconstruct

        Returns:
            Reconstructed feature dict (approximate)
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor not fitted. Call fit() first.")

        # Extract variable_bits from metadata if available
        variable_bits = compressed.compression_metadata.get("variable_bits")

        if variable_bits is not None:
            # Enhanced decompression with variable quantization
            # Values are already normalized in compress(), just copy them
            dequantized = compressed.compressed_features.copy()
        else:
            # Fallback to uniform dequantization for old compressed patterns
            dequantized = self._dequantize(compressed.compressed_features)

        # Project back to original space
        reconstructed_norm = self.projection_matrix @ dequantized

        # Denormalize
        reconstructed = reconstructed_norm * self.feature_std + self.feature_mean

        # Convert back to dict
        reconstructed_dict = {
            k: float(v) for k, v in zip(compressed.feature_keys, reconstructed)
        }

        self.total_decompressed -= 1

        return reconstructed_dict

    def xǁPatternCompressorǁdecompress__mutmut_26(self, compressed: CompressedPattern) -> Dict[str, float]:
        """
        Reconstruct pattern from compressed form.

        Note: This is a lossy reconstruction. Some information is lost in compression.
        Enhanced to handle variable quantization from compress() method.

        Args:
            compressed: Compressed pattern to reconstruct

        Returns:
            Reconstructed feature dict (approximate)
        """
        if not self.is_fitted:
            raise RuntimeError("Compressor not fitted. Call fit() first.")

        # Extract variable_bits from metadata if available
        variable_bits = compressed.compression_metadata.get("variable_bits")

        if variable_bits is not None:
            # Enhanced decompression with variable quantization
            # Values are already normalized in compress(), just copy them
            dequantized = compressed.compressed_features.copy()
        else:
            # Fallback to uniform dequantization for old compressed patterns
            dequantized = self._dequantize(compressed.compressed_features)

        # Project back to original space
        reconstructed_norm = self.projection_matrix @ dequantized

        # Denormalize
        reconstructed = reconstructed_norm * self.feature_std + self.feature_mean

        # Convert back to dict
        reconstructed_dict = {
            k: float(v) for k, v in zip(compressed.feature_keys, reconstructed)
        }

        self.total_decompressed += 2

        return reconstructed_dict
    
    xǁPatternCompressorǁdecompress__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁPatternCompressorǁdecompress__mutmut_1': xǁPatternCompressorǁdecompress__mutmut_1, 
        'xǁPatternCompressorǁdecompress__mutmut_2': xǁPatternCompressorǁdecompress__mutmut_2, 
        'xǁPatternCompressorǁdecompress__mutmut_3': xǁPatternCompressorǁdecompress__mutmut_3, 
        'xǁPatternCompressorǁdecompress__mutmut_4': xǁPatternCompressorǁdecompress__mutmut_4, 
        'xǁPatternCompressorǁdecompress__mutmut_5': xǁPatternCompressorǁdecompress__mutmut_5, 
        'xǁPatternCompressorǁdecompress__mutmut_6': xǁPatternCompressorǁdecompress__mutmut_6, 
        'xǁPatternCompressorǁdecompress__mutmut_7': xǁPatternCompressorǁdecompress__mutmut_7, 
        'xǁPatternCompressorǁdecompress__mutmut_8': xǁPatternCompressorǁdecompress__mutmut_8, 
        'xǁPatternCompressorǁdecompress__mutmut_9': xǁPatternCompressorǁdecompress__mutmut_9, 
        'xǁPatternCompressorǁdecompress__mutmut_10': xǁPatternCompressorǁdecompress__mutmut_10, 
        'xǁPatternCompressorǁdecompress__mutmut_11': xǁPatternCompressorǁdecompress__mutmut_11, 
        'xǁPatternCompressorǁdecompress__mutmut_12': xǁPatternCompressorǁdecompress__mutmut_12, 
        'xǁPatternCompressorǁdecompress__mutmut_13': xǁPatternCompressorǁdecompress__mutmut_13, 
        'xǁPatternCompressorǁdecompress__mutmut_14': xǁPatternCompressorǁdecompress__mutmut_14, 
        'xǁPatternCompressorǁdecompress__mutmut_15': xǁPatternCompressorǁdecompress__mutmut_15, 
        'xǁPatternCompressorǁdecompress__mutmut_16': xǁPatternCompressorǁdecompress__mutmut_16, 
        'xǁPatternCompressorǁdecompress__mutmut_17': xǁPatternCompressorǁdecompress__mutmut_17, 
        'xǁPatternCompressorǁdecompress__mutmut_18': xǁPatternCompressorǁdecompress__mutmut_18, 
        'xǁPatternCompressorǁdecompress__mutmut_19': xǁPatternCompressorǁdecompress__mutmut_19, 
        'xǁPatternCompressorǁdecompress__mutmut_20': xǁPatternCompressorǁdecompress__mutmut_20, 
        'xǁPatternCompressorǁdecompress__mutmut_21': xǁPatternCompressorǁdecompress__mutmut_21, 
        'xǁPatternCompressorǁdecompress__mutmut_22': xǁPatternCompressorǁdecompress__mutmut_22, 
        'xǁPatternCompressorǁdecompress__mutmut_23': xǁPatternCompressorǁdecompress__mutmut_23, 
        'xǁPatternCompressorǁdecompress__mutmut_24': xǁPatternCompressorǁdecompress__mutmut_24, 
        'xǁPatternCompressorǁdecompress__mutmut_25': xǁPatternCompressorǁdecompress__mutmut_25, 
        'xǁPatternCompressorǁdecompress__mutmut_26': xǁPatternCompressorǁdecompress__mutmut_26
    }
    
    def decompress(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁPatternCompressorǁdecompress__mutmut_orig"), object.__getattribute__(self, "xǁPatternCompressorǁdecompress__mutmut_mutants"), args, kwargs, self)
        return result 
    
    decompress.__signature__ = _mutmut_signature(xǁPatternCompressorǁdecompress__mutmut_orig)
    xǁPatternCompressorǁdecompress__mutmut_orig.__name__ = 'xǁPatternCompressorǁdecompress'

    def xǁPatternCompressorǁget_compression_ratio__mutmut_orig(self) -> float:
        """
        Get average compression ratio.

        Returns:
            Average compression ratio (e.g., 0.4 = 60% size reduction)
        """
        if not self.compression_ratios:
            return 1.0
        return float(np.mean(self.compression_ratios))

    def xǁPatternCompressorǁget_compression_ratio__mutmut_1(self) -> float:
        """
        Get average compression ratio.

        Returns:
            Average compression ratio (e.g., 0.4 = 60% size reduction)
        """
        if self.compression_ratios:
            return 1.0
        return float(np.mean(self.compression_ratios))

    def xǁPatternCompressorǁget_compression_ratio__mutmut_2(self) -> float:
        """
        Get average compression ratio.

        Returns:
            Average compression ratio (e.g., 0.4 = 60% size reduction)
        """
        if not self.compression_ratios:
            return 2.0
        return float(np.mean(self.compression_ratios))

    def xǁPatternCompressorǁget_compression_ratio__mutmut_3(self) -> float:
        """
        Get average compression ratio.

        Returns:
            Average compression ratio (e.g., 0.4 = 60% size reduction)
        """
        if not self.compression_ratios:
            return 1.0
        return float(None)

    def xǁPatternCompressorǁget_compression_ratio__mutmut_4(self) -> float:
        """
        Get average compression ratio.

        Returns:
            Average compression ratio (e.g., 0.4 = 60% size reduction)
        """
        if not self.compression_ratios:
            return 1.0
        return float(np.mean(None))
    
    xǁPatternCompressorǁget_compression_ratio__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁPatternCompressorǁget_compression_ratio__mutmut_1': xǁPatternCompressorǁget_compression_ratio__mutmut_1, 
        'xǁPatternCompressorǁget_compression_ratio__mutmut_2': xǁPatternCompressorǁget_compression_ratio__mutmut_2, 
        'xǁPatternCompressorǁget_compression_ratio__mutmut_3': xǁPatternCompressorǁget_compression_ratio__mutmut_3, 
        'xǁPatternCompressorǁget_compression_ratio__mutmut_4': xǁPatternCompressorǁget_compression_ratio__mutmut_4
    }
    
    def get_compression_ratio(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁPatternCompressorǁget_compression_ratio__mutmut_orig"), object.__getattribute__(self, "xǁPatternCompressorǁget_compression_ratio__mutmut_mutants"), args, kwargs, self)
        return result 
    
    get_compression_ratio.__signature__ = _mutmut_signature(xǁPatternCompressorǁget_compression_ratio__mutmut_orig)
    xǁPatternCompressorǁget_compression_ratio__mutmut_orig.__name__ = 'xǁPatternCompressorǁget_compression_ratio'

    def xǁPatternCompressorǁget_statistics__mutmut_orig(self) -> Dict[str, Any]:
        """
        Get compression statistics.

        Returns:
            Dictionary with compression metrics
        """
        return {
            "total_compressed": self.total_compressed,
            "total_decompressed": self.total_decompressed,
            "avg_compression_ratio": self.get_compression_ratio(),
            "target_dimensions": self.target_dimensions,
            "is_fitted": self.is_fitted,
            "quantization_bits": self.quantization_bits,
        }

    def xǁPatternCompressorǁget_statistics__mutmut_1(self) -> Dict[str, Any]:
        """
        Get compression statistics.

        Returns:
            Dictionary with compression metrics
        """
        return {
            "XXtotal_compressedXX": self.total_compressed,
            "total_decompressed": self.total_decompressed,
            "avg_compression_ratio": self.get_compression_ratio(),
            "target_dimensions": self.target_dimensions,
            "is_fitted": self.is_fitted,
            "quantization_bits": self.quantization_bits,
        }

    def xǁPatternCompressorǁget_statistics__mutmut_2(self) -> Dict[str, Any]:
        """
        Get compression statistics.

        Returns:
            Dictionary with compression metrics
        """
        return {
            "TOTAL_COMPRESSED": self.total_compressed,
            "total_decompressed": self.total_decompressed,
            "avg_compression_ratio": self.get_compression_ratio(),
            "target_dimensions": self.target_dimensions,
            "is_fitted": self.is_fitted,
            "quantization_bits": self.quantization_bits,
        }

    def xǁPatternCompressorǁget_statistics__mutmut_3(self) -> Dict[str, Any]:
        """
        Get compression statistics.

        Returns:
            Dictionary with compression metrics
        """
        return {
            "total_compressed": self.total_compressed,
            "XXtotal_decompressedXX": self.total_decompressed,
            "avg_compression_ratio": self.get_compression_ratio(),
            "target_dimensions": self.target_dimensions,
            "is_fitted": self.is_fitted,
            "quantization_bits": self.quantization_bits,
        }

    def xǁPatternCompressorǁget_statistics__mutmut_4(self) -> Dict[str, Any]:
        """
        Get compression statistics.

        Returns:
            Dictionary with compression metrics
        """
        return {
            "total_compressed": self.total_compressed,
            "TOTAL_DECOMPRESSED": self.total_decompressed,
            "avg_compression_ratio": self.get_compression_ratio(),
            "target_dimensions": self.target_dimensions,
            "is_fitted": self.is_fitted,
            "quantization_bits": self.quantization_bits,
        }

    def xǁPatternCompressorǁget_statistics__mutmut_5(self) -> Dict[str, Any]:
        """
        Get compression statistics.

        Returns:
            Dictionary with compression metrics
        """
        return {
            "total_compressed": self.total_compressed,
            "total_decompressed": self.total_decompressed,
            "XXavg_compression_ratioXX": self.get_compression_ratio(),
            "target_dimensions": self.target_dimensions,
            "is_fitted": self.is_fitted,
            "quantization_bits": self.quantization_bits,
        }

    def xǁPatternCompressorǁget_statistics__mutmut_6(self) -> Dict[str, Any]:
        """
        Get compression statistics.

        Returns:
            Dictionary with compression metrics
        """
        return {
            "total_compressed": self.total_compressed,
            "total_decompressed": self.total_decompressed,
            "AVG_COMPRESSION_RATIO": self.get_compression_ratio(),
            "target_dimensions": self.target_dimensions,
            "is_fitted": self.is_fitted,
            "quantization_bits": self.quantization_bits,
        }

    def xǁPatternCompressorǁget_statistics__mutmut_7(self) -> Dict[str, Any]:
        """
        Get compression statistics.

        Returns:
            Dictionary with compression metrics
        """
        return {
            "total_compressed": self.total_compressed,
            "total_decompressed": self.total_decompressed,
            "avg_compression_ratio": self.get_compression_ratio(),
            "XXtarget_dimensionsXX": self.target_dimensions,
            "is_fitted": self.is_fitted,
            "quantization_bits": self.quantization_bits,
        }

    def xǁPatternCompressorǁget_statistics__mutmut_8(self) -> Dict[str, Any]:
        """
        Get compression statistics.

        Returns:
            Dictionary with compression metrics
        """
        return {
            "total_compressed": self.total_compressed,
            "total_decompressed": self.total_decompressed,
            "avg_compression_ratio": self.get_compression_ratio(),
            "TARGET_DIMENSIONS": self.target_dimensions,
            "is_fitted": self.is_fitted,
            "quantization_bits": self.quantization_bits,
        }

    def xǁPatternCompressorǁget_statistics__mutmut_9(self) -> Dict[str, Any]:
        """
        Get compression statistics.

        Returns:
            Dictionary with compression metrics
        """
        return {
            "total_compressed": self.total_compressed,
            "total_decompressed": self.total_decompressed,
            "avg_compression_ratio": self.get_compression_ratio(),
            "target_dimensions": self.target_dimensions,
            "XXis_fittedXX": self.is_fitted,
            "quantization_bits": self.quantization_bits,
        }

    def xǁPatternCompressorǁget_statistics__mutmut_10(self) -> Dict[str, Any]:
        """
        Get compression statistics.

        Returns:
            Dictionary with compression metrics
        """
        return {
            "total_compressed": self.total_compressed,
            "total_decompressed": self.total_decompressed,
            "avg_compression_ratio": self.get_compression_ratio(),
            "target_dimensions": self.target_dimensions,
            "IS_FITTED": self.is_fitted,
            "quantization_bits": self.quantization_bits,
        }

    def xǁPatternCompressorǁget_statistics__mutmut_11(self) -> Dict[str, Any]:
        """
        Get compression statistics.

        Returns:
            Dictionary with compression metrics
        """
        return {
            "total_compressed": self.total_compressed,
            "total_decompressed": self.total_decompressed,
            "avg_compression_ratio": self.get_compression_ratio(),
            "target_dimensions": self.target_dimensions,
            "is_fitted": self.is_fitted,
            "XXquantization_bitsXX": self.quantization_bits,
        }

    def xǁPatternCompressorǁget_statistics__mutmut_12(self) -> Dict[str, Any]:
        """
        Get compression statistics.

        Returns:
            Dictionary with compression metrics
        """
        return {
            "total_compressed": self.total_compressed,
            "total_decompressed": self.total_decompressed,
            "avg_compression_ratio": self.get_compression_ratio(),
            "target_dimensions": self.target_dimensions,
            "is_fitted": self.is_fitted,
            "QUANTIZATION_BITS": self.quantization_bits,
        }
    
    xǁPatternCompressorǁget_statistics__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁPatternCompressorǁget_statistics__mutmut_1': xǁPatternCompressorǁget_statistics__mutmut_1, 
        'xǁPatternCompressorǁget_statistics__mutmut_2': xǁPatternCompressorǁget_statistics__mutmut_2, 
        'xǁPatternCompressorǁget_statistics__mutmut_3': xǁPatternCompressorǁget_statistics__mutmut_3, 
        'xǁPatternCompressorǁget_statistics__mutmut_4': xǁPatternCompressorǁget_statistics__mutmut_4, 
        'xǁPatternCompressorǁget_statistics__mutmut_5': xǁPatternCompressorǁget_statistics__mutmut_5, 
        'xǁPatternCompressorǁget_statistics__mutmut_6': xǁPatternCompressorǁget_statistics__mutmut_6, 
        'xǁPatternCompressorǁget_statistics__mutmut_7': xǁPatternCompressorǁget_statistics__mutmut_7, 
        'xǁPatternCompressorǁget_statistics__mutmut_8': xǁPatternCompressorǁget_statistics__mutmut_8, 
        'xǁPatternCompressorǁget_statistics__mutmut_9': xǁPatternCompressorǁget_statistics__mutmut_9, 
        'xǁPatternCompressorǁget_statistics__mutmut_10': xǁPatternCompressorǁget_statistics__mutmut_10, 
        'xǁPatternCompressorǁget_statistics__mutmut_11': xǁPatternCompressorǁget_statistics__mutmut_11, 
        'xǁPatternCompressorǁget_statistics__mutmut_12': xǁPatternCompressorǁget_statistics__mutmut_12
    }
    
    def get_statistics(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁPatternCompressorǁget_statistics__mutmut_orig"), object.__getattribute__(self, "xǁPatternCompressorǁget_statistics__mutmut_mutants"), args, kwargs, self)
        return result 
    
    get_statistics.__signature__ = _mutmut_signature(xǁPatternCompressorǁget_statistics__mutmut_orig)
    xǁPatternCompressorǁget_statistics__mutmut_orig.__name__ = 'xǁPatternCompressorǁget_statistics'

    def xǁPatternCompressorǁ_quantize__mutmut_orig(self, vector: np.ndarray) -> np.ndarray:
        """
        Quantize vector to reduce precision.

        Args:
            vector: Vector to quantize

        Returns:
            Quantized vector
        """
        # Map to quantization levels
        max_val = np.max(np.abs(vector)) + 1e-8
        levels = 2**self.quantization_bits

        # Quantize
        quantized = np.round(vector / max_val * (levels // 2)) / (levels // 2) * max_val

        return quantized

    def xǁPatternCompressorǁ_quantize__mutmut_1(self, vector: np.ndarray) -> np.ndarray:
        """
        Quantize vector to reduce precision.

        Args:
            vector: Vector to quantize

        Returns:
            Quantized vector
        """
        # Map to quantization levels
        max_val = None
        levels = 2**self.quantization_bits

        # Quantize
        quantized = np.round(vector / max_val * (levels // 2)) / (levels // 2) * max_val

        return quantized

    def xǁPatternCompressorǁ_quantize__mutmut_2(self, vector: np.ndarray) -> np.ndarray:
        """
        Quantize vector to reduce precision.

        Args:
            vector: Vector to quantize

        Returns:
            Quantized vector
        """
        # Map to quantization levels
        max_val = np.max(np.abs(vector)) - 1e-8
        levels = 2**self.quantization_bits

        # Quantize
        quantized = np.round(vector / max_val * (levels // 2)) / (levels // 2) * max_val

        return quantized

    def xǁPatternCompressorǁ_quantize__mutmut_3(self, vector: np.ndarray) -> np.ndarray:
        """
        Quantize vector to reduce precision.

        Args:
            vector: Vector to quantize

        Returns:
            Quantized vector
        """
        # Map to quantization levels
        max_val = np.max(None) + 1e-8
        levels = 2**self.quantization_bits

        # Quantize
        quantized = np.round(vector / max_val * (levels // 2)) / (levels // 2) * max_val

        return quantized

    def xǁPatternCompressorǁ_quantize__mutmut_4(self, vector: np.ndarray) -> np.ndarray:
        """
        Quantize vector to reduce precision.

        Args:
            vector: Vector to quantize

        Returns:
            Quantized vector
        """
        # Map to quantization levels
        max_val = np.max(np.abs(None)) + 1e-8
        levels = 2**self.quantization_bits

        # Quantize
        quantized = np.round(vector / max_val * (levels // 2)) / (levels // 2) * max_val

        return quantized

    def xǁPatternCompressorǁ_quantize__mutmut_5(self, vector: np.ndarray) -> np.ndarray:
        """
        Quantize vector to reduce precision.

        Args:
            vector: Vector to quantize

        Returns:
            Quantized vector
        """
        # Map to quantization levels
        max_val = np.max(np.abs(vector)) + 1.00000001
        levels = 2**self.quantization_bits

        # Quantize
        quantized = np.round(vector / max_val * (levels // 2)) / (levels // 2) * max_val

        return quantized

    def xǁPatternCompressorǁ_quantize__mutmut_6(self, vector: np.ndarray) -> np.ndarray:
        """
        Quantize vector to reduce precision.

        Args:
            vector: Vector to quantize

        Returns:
            Quantized vector
        """
        # Map to quantization levels
        max_val = np.max(np.abs(vector)) + 1e-8
        levels = None

        # Quantize
        quantized = np.round(vector / max_val * (levels // 2)) / (levels // 2) * max_val

        return quantized

    def xǁPatternCompressorǁ_quantize__mutmut_7(self, vector: np.ndarray) -> np.ndarray:
        """
        Quantize vector to reduce precision.

        Args:
            vector: Vector to quantize

        Returns:
            Quantized vector
        """
        # Map to quantization levels
        max_val = np.max(np.abs(vector)) + 1e-8
        levels = 2 * self.quantization_bits

        # Quantize
        quantized = np.round(vector / max_val * (levels // 2)) / (levels // 2) * max_val

        return quantized

    def xǁPatternCompressorǁ_quantize__mutmut_8(self, vector: np.ndarray) -> np.ndarray:
        """
        Quantize vector to reduce precision.

        Args:
            vector: Vector to quantize

        Returns:
            Quantized vector
        """
        # Map to quantization levels
        max_val = np.max(np.abs(vector)) + 1e-8
        levels = 3**self.quantization_bits

        # Quantize
        quantized = np.round(vector / max_val * (levels // 2)) / (levels // 2) * max_val

        return quantized

    def xǁPatternCompressorǁ_quantize__mutmut_9(self, vector: np.ndarray) -> np.ndarray:
        """
        Quantize vector to reduce precision.

        Args:
            vector: Vector to quantize

        Returns:
            Quantized vector
        """
        # Map to quantization levels
        max_val = np.max(np.abs(vector)) + 1e-8
        levels = 2**self.quantization_bits

        # Quantize
        quantized = None

        return quantized

    def xǁPatternCompressorǁ_quantize__mutmut_10(self, vector: np.ndarray) -> np.ndarray:
        """
        Quantize vector to reduce precision.

        Args:
            vector: Vector to quantize

        Returns:
            Quantized vector
        """
        # Map to quantization levels
        max_val = np.max(np.abs(vector)) + 1e-8
        levels = 2**self.quantization_bits

        # Quantize
        quantized = np.round(vector / max_val * (levels // 2)) / (levels // 2) / max_val

        return quantized

    def xǁPatternCompressorǁ_quantize__mutmut_11(self, vector: np.ndarray) -> np.ndarray:
        """
        Quantize vector to reduce precision.

        Args:
            vector: Vector to quantize

        Returns:
            Quantized vector
        """
        # Map to quantization levels
        max_val = np.max(np.abs(vector)) + 1e-8
        levels = 2**self.quantization_bits

        # Quantize
        quantized = np.round(vector / max_val * (levels // 2)) * (levels // 2) * max_val

        return quantized

    def xǁPatternCompressorǁ_quantize__mutmut_12(self, vector: np.ndarray) -> np.ndarray:
        """
        Quantize vector to reduce precision.

        Args:
            vector: Vector to quantize

        Returns:
            Quantized vector
        """
        # Map to quantization levels
        max_val = np.max(np.abs(vector)) + 1e-8
        levels = 2**self.quantization_bits

        # Quantize
        quantized = np.round(None) / (levels // 2) * max_val

        return quantized

    def xǁPatternCompressorǁ_quantize__mutmut_13(self, vector: np.ndarray) -> np.ndarray:
        """
        Quantize vector to reduce precision.

        Args:
            vector: Vector to quantize

        Returns:
            Quantized vector
        """
        # Map to quantization levels
        max_val = np.max(np.abs(vector)) + 1e-8
        levels = 2**self.quantization_bits

        # Quantize
        quantized = np.round(vector / max_val / (levels // 2)) / (levels // 2) * max_val

        return quantized

    def xǁPatternCompressorǁ_quantize__mutmut_14(self, vector: np.ndarray) -> np.ndarray:
        """
        Quantize vector to reduce precision.

        Args:
            vector: Vector to quantize

        Returns:
            Quantized vector
        """
        # Map to quantization levels
        max_val = np.max(np.abs(vector)) + 1e-8
        levels = 2**self.quantization_bits

        # Quantize
        quantized = np.round(vector * max_val * (levels // 2)) / (levels // 2) * max_val

        return quantized

    def xǁPatternCompressorǁ_quantize__mutmut_15(self, vector: np.ndarray) -> np.ndarray:
        """
        Quantize vector to reduce precision.

        Args:
            vector: Vector to quantize

        Returns:
            Quantized vector
        """
        # Map to quantization levels
        max_val = np.max(np.abs(vector)) + 1e-8
        levels = 2**self.quantization_bits

        # Quantize
        quantized = np.round(vector / max_val * (levels / 2)) / (levels // 2) * max_val

        return quantized

    def xǁPatternCompressorǁ_quantize__mutmut_16(self, vector: np.ndarray) -> np.ndarray:
        """
        Quantize vector to reduce precision.

        Args:
            vector: Vector to quantize

        Returns:
            Quantized vector
        """
        # Map to quantization levels
        max_val = np.max(np.abs(vector)) + 1e-8
        levels = 2**self.quantization_bits

        # Quantize
        quantized = np.round(vector / max_val * (levels // 3)) / (levels // 2) * max_val

        return quantized

    def xǁPatternCompressorǁ_quantize__mutmut_17(self, vector: np.ndarray) -> np.ndarray:
        """
        Quantize vector to reduce precision.

        Args:
            vector: Vector to quantize

        Returns:
            Quantized vector
        """
        # Map to quantization levels
        max_val = np.max(np.abs(vector)) + 1e-8
        levels = 2**self.quantization_bits

        # Quantize
        quantized = np.round(vector / max_val * (levels // 2)) / (levels / 2) * max_val

        return quantized

    def xǁPatternCompressorǁ_quantize__mutmut_18(self, vector: np.ndarray) -> np.ndarray:
        """
        Quantize vector to reduce precision.

        Args:
            vector: Vector to quantize

        Returns:
            Quantized vector
        """
        # Map to quantization levels
        max_val = np.max(np.abs(vector)) + 1e-8
        levels = 2**self.quantization_bits

        # Quantize
        quantized = np.round(vector / max_val * (levels // 2)) / (levels // 3) * max_val

        return quantized
    
    xǁPatternCompressorǁ_quantize__mutmut_mutants : ClassVar[MutantDict] = {
    'xǁPatternCompressorǁ_quantize__mutmut_1': xǁPatternCompressorǁ_quantize__mutmut_1, 
        'xǁPatternCompressorǁ_quantize__mutmut_2': xǁPatternCompressorǁ_quantize__mutmut_2, 
        'xǁPatternCompressorǁ_quantize__mutmut_3': xǁPatternCompressorǁ_quantize__mutmut_3, 
        'xǁPatternCompressorǁ_quantize__mutmut_4': xǁPatternCompressorǁ_quantize__mutmut_4, 
        'xǁPatternCompressorǁ_quantize__mutmut_5': xǁPatternCompressorǁ_quantize__mutmut_5, 
        'xǁPatternCompressorǁ_quantize__mutmut_6': xǁPatternCompressorǁ_quantize__mutmut_6, 
        'xǁPatternCompressorǁ_quantize__mutmut_7': xǁPatternCompressorǁ_quantize__mutmut_7, 
        'xǁPatternCompressorǁ_quantize__mutmut_8': xǁPatternCompressorǁ_quantize__mutmut_8, 
        'xǁPatternCompressorǁ_quantize__mutmut_9': xǁPatternCompressorǁ_quantize__mutmut_9, 
        'xǁPatternCompressorǁ_quantize__mutmut_10': xǁPatternCompressorǁ_quantize__mutmut_10, 
        'xǁPatternCompressorǁ_quantize__mutmut_11': xǁPatternCompressorǁ_quantize__mutmut_11, 
        'xǁPatternCompressorǁ_quantize__mutmut_12': xǁPatternCompressorǁ_quantize__mutmut_12, 
        'xǁPatternCompressorǁ_quantize__mutmut_13': xǁPatternCompressorǁ_quantize__mutmut_13, 
        'xǁPatternCompressorǁ_quantize__mutmut_14': xǁPatternCompressorǁ_quantize__mutmut_14, 
        'xǁPatternCompressorǁ_quantize__mutmut_15': xǁPatternCompressorǁ_quantize__mutmut_15, 
        'xǁPatternCompressorǁ_quantize__mutmut_16': xǁPatternCompressorǁ_quantize__mutmut_16, 
        'xǁPatternCompressorǁ_quantize__mutmut_17': xǁPatternCompressorǁ_quantize__mutmut_17, 
        'xǁPatternCompressorǁ_quantize__mutmut_18': xǁPatternCompressorǁ_quantize__mutmut_18
    }
    
    def _quantize(self, *args, **kwargs):
        result = _mutmut_trampoline(object.__getattribute__(self, "xǁPatternCompressorǁ_quantize__mutmut_orig"), object.__getattribute__(self, "xǁPatternCompressorǁ_quantize__mutmut_mutants"), args, kwargs, self)
        return result 
    
    _quantize.__signature__ = _mutmut_signature(xǁPatternCompressorǁ_quantize__mutmut_orig)
    xǁPatternCompressorǁ_quantize__mutmut_orig.__name__ = 'xǁPatternCompressorǁ_quantize'

    def _dequantize(self, vector: np.ndarray) -> np.ndarray:
        """
        Dequantize vector (no-op, already in float form).

        Args:
            vector: Quantized vector

        Returns:
            Dequantized vector (same as input in this implementation)
        """
        return vector
